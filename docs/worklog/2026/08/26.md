# WORKLOG — 2026/08/26

## 07:00 — [Diseñador Jefe P5] Revisión final del diseño + protocolo de testeo diario

**QUÉ:**
- Leído `docs/DESIGN.md` completo (1.100 líneas) y contrastado contra las
  tres fuentes de verdad: `RESEARCH-MECANICAS.md`, `INVESTIGACION-STACK.md`
  y `SKILLS-ANTISLOP.md`. Correcciones quirúrgicas, sin reescrituras.
- Cerradas las decisiones «decide P5» de §9 y los pendientes que apuntaban
  a este pase; reorganizado §9 como ledger con estado (cerradas en P5 /
  abiertas con dueño).
- Creado `docs/TESTEO-DIARIO.md`: protocolo de testeo del Concilio con
  tres capas no solapadas y la zona 🔬 Gwyn→Havel.

**POR QUÉ (hallazgos y decisiones de esta revisión):**
- El hueco más grande era §3.4: la tabla de finales prometía «condiciones
  exactas en P5» desde el Pase 2 y nadie las había escrito. Ahora §3.4.1
  define condiciones mecánicas sobre el karma K ya existente (§3.3): LUZ
  PLENA = historial azul sostenido + prueba íntegra; NOCHE LARGA = acción
  final deliberada (cualquier K); EL TRATO = banda mixta + logs del
  Auditor como palanca; APAGÓN PROPIO = dominio total (arcos + banda +
  fragmento), no perfil moral. Umbrales T_alto/T_bajo marcados ⚠️ v1
  calibrables — lo normativo es el mecanismo.
- Coherencia reforzada al cerrar el arco del Auditor (beat 8): «aliado a
  medias» sin traición dramática; sus informes son la palanca de EL TRATO.
  Así el giro narrativo y la condición del final son EL MISMO dato.
- Error aritmético real corregido: §6.3 decía «~50 encargos» pero la tabla
  suma 39–45 (~42). Añadida fila TOTAL auditable; §6.5 actualizado;
  dimensionado consolidado en §6.0.5 dentro del objetivo contractual de
  10–15 h del stack research (campaña 12–14 h + finales/Pactos → 15 h+).
  También corregidas dos erratas («la Pase») y el pie de relevo obsoleto.
- Contraste con research: sin desviaciones sustantivas detectadas. El
  diseño respeta aprender-por-necesidad (RM §1), muerte=lección (RM §3),
  RNG jamás decide semántica (RM §3.3.3), juice=código-informativo (RM
  §2.3), legibilidad>espectáculo y frontera core/render headless
  (INVESTIGACION-STACK). La prosa de diseño pasó grep anti-slop limpio.
- Protocolo de testeo (TESTEO-DIARIO.md): división por CAPA (juega/
  verifica/juzga) para que los tres perfiles no se pisen aunque toquen lo
  mismo. Pieza central: Gwyn decide al cierre la «zona de mañana» en TODO
  con formato fijo (máx. 2 prioridades + smoke); Havel la lee a las 07:00
  junto a su `git log --since`. Fallback si Gwyn falla; prioridades
  explícitas para Gwyndolin (bugs > camino principal > ideas); línea
  CICLO verde/ámbar/rojo barata para el reporte a Juanma.

**ENTREGABLE / ESTADO:**
- `docs/DESIGN.md` cerrado: header ✅ P1–P5, §3.4.1 nuevo, §6.0.5/§6.3
  consolidados, §9 reorganizado, §10 «Resumen ejecutivo» añadido para el
  gate, pie de relevo actualizado. Referencias cruzadas verificadas por
  grep (sin «P5» pendientes huérfanas ni § inexistentes).
- `docs/TESTEO-DIARIO.md` creado y registrado en PROJECT-MAP; tarea del
  TODO (24/08) marcada `[HECHO]`.
- Commit + push a `origin/main`.

**RELEVO:**
- **Arquitecto (27/08 11:00)**: tu entrada es DESIGN.md §10 + las reglas de
  frontera de INVESTIGACION-STACK. Te queda validar la lista comando a
  comando de ~60 boons (§4.4/§6.2) contra el sandbox soportable, y definir
  módulos con el testeo de §TESTEO-DIARIO en mente (core headless primero).
- **Ornstein (Fase 1)**: números v1 tuyos de calibrar — N=8 (§3.2), bandas
  T_alto/T_bajo (§3.4.1), 50 % parcial (§7.7), umbral 40 % contraste (§8.6).
- **Gwyn (esta noche, 23:00)**: estrena la zona 🔬 — déjala en TODO según
  el formato de TESTEO-DIARIO §4 aunque hoy solo haya docs.
- **Juanma**: gate de Fase 0 cuando quieras; §10 es tu resumen de lectura.
- Memoria persistente de Raiden (vault Obsidian): no consultada/actualizada
  este turno por presupuesto — el aprendizaje del pase quedó registrado
  aquí y en la skill `relay-doc-passes`.

CICLO: verde

---

## 11:00 — [Corrector one-shot] Oscar (05:00) como 4º perfil de testeo + coherencia a 9 agentes

**QUÉ:**
- `docs/TESTEO-DIARIO.md` reescrito de 3 a **4 perfiles** que no se pisan:
  - **Oscar (05:00)** — capa EXPERIENCIA/PROGRESIÓN: run de referencia desde
    SAVE LIMPIO + veterano (20+ h) + mantiene `docs/ESTADO-JUGADOR.md`.
    Pregunta: «¿es apto el camino para un jugador de cero?».
  - **Havel (07:00)** — capa NOVEDAD+CONJUNTO: lo nuevo por `git log` + smoke
    del conjunto. **Ya NO hace el save limpio** (queda como capa exclusiva de
    Oscar); su smoke solo verifica que el juego ENTERO sigue arrancando/
    avanzando. Pregunta: «¿funciona? ¿mola?».
  - **Artorias (21:00)** — capa TÉCNICA por PR (tests/lint/smoke técnico).
    Pregunta: «¿está bien hecho?».
  - **Gwyn (23:00)** — capa DISEÑO/SABOR; decide la zona 🔬.
    Pregunta: «¿es buen juego?».
- **Relevo de la zona corregido a Gwyn → Oscar (05:00) → Havel (07:00)**:
  formato de la zona §4, diagrama del ciclo §5 y todas las menciones del
  relevo actualizados. Fallback si Gwyn falla: Oscar usa su run habitual,
  Havel su `git log --since` + smoke.
- Colisión del save-limpio resuelta explícitamente (§0 y §7 anti-pisoteo):
  el save limpio es de Oscar; smoke de Havel y smoke técnico de Artorias
  trabajan sobre el estado existente.
- §6 ampliado: los CUATRO perfiles escriben línea CICLO (verde/ámbar/rojo);
  Gwyn las agrega en su reporte. Se conservan intactas prioridades de
  Gwyndolin (§5), fallback y anti-pisoteo (§7).
- Auditoría repo→Oscar: `AGENTS-PLAN.md` (sección nueva 05:00 en §4, Havel
  «SIN save limpio», Gwyn integra notas de dirección y decide la zona 🔬,
  relevo Paso 4 con Oscar, «8 agentes»→«9», checklist §9 lista el Concilio
  completo, harness debe soportar reset a SAVE LIMPIO); `PROJECT-MAP.md`
  (TESTEO-DIARIO = 4 capas, ESTADO-JUGADOR.md añadido al árbol, fila Havel y
  Gwyn actualizadas); `README.md` (flujo diario muestra el relevo del testeo);
  `DESIGN.md` §10 (testeo descrito como 4 capas con relevo Gwyn→Oscar→Havel);
  `backlog/TODO.md` (flujo diario incluye a Oscar como fuente matinal).

**POR QUÉ:** Oscar se añadió el 24/08 pero el protocolo de testeo creado
esa mañana (P5, 07:00) seguía con 3 perfiles: Havel repetía el save limpio
de Oscar sin saberlo, la zona 🔬 no le llegaba, y varios docs/prompts
decían «8 agentes» o ignoraban su capa. Sin esta corrección, dos capas se
pisan cada mañana y Gwyndolin planificaría ciego a las notas de dirección.

**ENTREGABLE / Prompts de crons corregidos vía `hermes cron edit --prompt`**
(verificados releyendo jobs.json; NUNCA editado a mano):
- `e3c150781f9d` (Havel): lee TESTEO-DIARIO/ESTADO-JUGADOR, juega lo nuevo +
  smoke SIN save limpio, continúa la zona 🔬 tras Oscar, CICLO en worklog.
- `d5c8def555cd` (Gwyndolin): consume también las NOTAS DE DIRECCIÓN de
  Oscar validadas por Gwyn.
- `c4c98c5d8950` (Artorias): uno de los CUATRO perfiles (capa técnica), no
  resetea a save limpio, cruza [BUG]s de Oscar/Havel con PRs, CICLO.
- `d972fdc912b7` (Gwyn): uno de los CUATRO perfiles, integra notas de
  dirección de Oscar, decide la ZONA 🔬 para el relevo Oscar→Havel,
  agrega los CICLO en el reporte.
- `ee900afb19da` (Oscar): integrado en TESTEO-DIARIO (dueño del SAVE LIMPIO,
  ejecuta primero la zona 🔬), typos corregidos («SEMWRE»→SIEMPRE, «SEMPRE»
  →SIEMPRE, bold sin cerrar, «melo»), CICLO, reporte final sin spanglish.
- `7dec77a6d301` (Vigilante): «los 8 del Concilio» → «los 9 del Concilio».

**PROPUESTAS PARA GWYN (auto-mejora, no aplicadas aquí):**
1. El mapa GitHub (`docs/mapa/index.html`) ya refleja a Oscar y el relevo,
   pero conviene que alguien valide visualmente la página publicada tras
   este cambio de protocolo.
2. `AGENTS-PLAN.md` §4 aún describe el rol de Artorias/Gwyn sin desglosar
   sus preguntas-filtro del TESTEO-DIARIO; un futuro pase podría unificar
   ambos documentos para evitar divergencia (hoy quedan coherentes pero
   duplicados en parte).
3. Cuando Ornstein construya el harness (Fase 1): exponer comando explícito
   de reset-a-save-limpio + save-veterano, requisito duro de la capa Oscar.

CICLO: verde

---

## 12:00 — [Reestructurador one-shot] División del backlog en carpetas por estado + prompts coherentes

**QUÉ:**
- `backlog/TODO.md` y `backlog/MEJORAS.md` eliminados (git conserva la historia) y
  sustituidos por estructura por estado. Nuevo árbol:
```
backlog/
  INDICE.md                    ← mapa corto: qué hay en cada subcarpeta + qué LEE/ESCRIBE cada agente
  zona-testeo.md               ← la ZONA 🔬 del día (Gwyn SOBRESCRIBE al cierre; Oscar/Havel leen)
  notas-manana.md              ← notas rodantes: 🧭 dirección de Oscar + 🎯 revisores → Gwyndolin
  tmp-reintentos.md            ← contador del Vigilante F0 (sin cambios)
  tareas/
    pendiente/abierto.md       ← [PENDIENTE]/[BUG]/ideas abiertas
    en-curso/activo.md         ← lo que se ejecuta HOY + veredictos 💥/✅
    hecho/2026-08.md           ← ARCHIVO POR MES de lo completado (se abre fichero nuevo cada mes)
    descartado/historico.md    ← rechazadas con motivo
  mejoras/
    pendiente/propuestas.md    ← propuestas de auto-mejora
    aplicadas/historico.md     ← registro [APLICADA] de Gwyn (trazabilidad pública)
  planes/ · historia/          ← sin cambios
```
- Migración fiel del contenido: tareas `[HECHO]` → `hecho/2026-08.md`; abiertas →
  `pendiente/abierto.md` (gate de Juanma, fuente bitmap); Fase 0 y Concilio en curso →
  `en-curso/activo.md`; propuesta de Oscar ya aprobada marcada como cerrada;
  propuestas de mejora → `mejoras/pendiente/propuestas.md`.
- **Prompts actualizados vía `hermes cron edit --prompt` (NUNCA jobs.json a mano),
  verificados releyendo jobs.json — cero referencias viejas, schedules/skills intactos:**
  - `f6bef0f8e3d8` Manus · `ee900afb19da` Oscar · `e3c150781f9d` Havel ·
    `d5c8def555cd` Gwyndolin · `1ebe58fd86a3` Ornstein · `55bb406c6e4c` Smough ·
    `65ccfc807dd6` Seath · `c4c98c5d8950` Artorias · `d972fdc912b7` Gwyn ·
    `7dec77a6d301` Vigilante · `70997a08ff3a` Arquitecto F0 (dispara hoy 15:00) ·
    `c206c75818eb` Coordinador F0 (hoy 21:00).
  - Cada agente ahora Lee SOLO los ficheros de su rol (tabla en INDICE.md): p. ej.
    Havel escribe en `pendiente/abierto.md` y lee `zona-testeo.md`; Gwyndolin consume
    `pendiente/abierto.md` + `notas-manana.md`; Artorias deja 💥/✅ junto a las líneas
    de `en-curso/activo.md`; Gwyn archiva lo mergeado en `hecho/<mes>`, sobrescribe la
    zona 🔬 y registra mejoras en `mejoras/aplicadas/historico.md`.
- Docs cuadrados con grep global (referencias vivas = 0): `PROJECT-MAP.md` (índice
  maestro reforzado: árbol nuevo + tabla «Lee/Escribe» por agente), `AGENTES.md`,
  `AGENTS-PLAN.md` (§2, §2.5, §2.6, §4), `README.md`, `TESTEO-DIARIO.md`,
  `AGENTES-MAPA.md` (Mermaid), `worklog/index.md`, `ESTADO-JUGADOR.md`,
  `RESEARCH-MECANICAS.md`, `DESIGN.md`. Los worklogs históricos (23-26/08 antes de
  esta entrada) se dejan tal cual: son registro fechado; la equivalencia queda
  documentada en `worklog/index.md` e `INDICE.md`.
- GitHub Page (`docs/mapa/index.html`): Regla de Oro (g3/g6 ES/EN), fichas de los 9
  agentes (qué lee/escribe ahora cada uno) y zonas del repo (`INDICE.md`,
  `zona-testeo.md`, `notas-manana.md`, `tareas/*`, `mejoras/*`) actualizadas con
  bilingüe exacto. Validado programáticamente: JS sin errores de sintaxis, 9 agentes,
  46 referencias agente↔fichero sin huérfanas, 0 menciones a rutas viejas en sus datos.

**POR QUÉ:**
- Decisión de Juanma: dentro de 3 meses TODO.md/MEJORAS.md serían megaficheros que
  todos leerían enteros, violando la regla de oro («lee solo lo que tu tarea
  demande»). La división solo sirve si CADA prompt apunta al subconjunto correcto:
  por eso el paso crítico fue reescribir qué lee/escribe cada agente, no mover
  ficheros. «Hecho» además crecía sin límite: ahora se archiva POR MES.

**ENTREGABLE / ESTADO:**
- Estructura nueva operativa + 12 prompts corregidos + docs/GitHub Page coherentes.
- Riesgo declarado: primer día de uso real; el relevo de mañana (Gwyn 23:00 → Oscar/
  Havel) estrena `zona-testeo.md` con una zona de verificación de esta migración.
- Si algún prompt viejo quedara cacheado en una ejecución en marcha, corregirá al
  siguiente turno (los crons releen el prompt en cada disparo).

CICLO: verde

## 12:40 — [Raiden, por decisión de Juanma] Higiene del backlog asignada a Gwyndolin

**QUÉ / POR QUÉ:** Tras la reestructuración de `backlog/` (carpetas por estado +
índice) se detectó que NINGÚN agente tenía como misión mantener esa estructura
sana: el reestructurador la creó, pero nadie la barre a diario. Esto ya provocó
incoherencias (una propuesta `[APLICADA]` seguía en `pendiente/`, y `[PENDIENTE]`
residuales dentro de `tareas/hecho/` — corregidas a mano en commit `045a42b`).
Para que no vuelva a suceder, se asigna la HIGIENE DEL BACKLOG al agente natural:
Gwyndolin (11:00, planificador), que ya cura el backlog cada día.

**CAMBIO APLICADO:** prompt de Gwyndolin (`d5c8def555cd`) vía update seguro.
Añadido bullet "HIGIENE GENERAL DEL BACKLOG (hazlo PRIMERO)" al inicio de su
MISIÓN: revisa TODAS las carpetas de `backlog/tareas/` (pendiente, en-curso,
hecho, descartado) y `backlog/mejoras/` (pendiente, aplicadas), verifica que el
ESTADO de cada línea concuerde con su carpeta (`[PENDIENTE]`/`[EN CURSO]`/`[HECHO]`/
`[DESCARTADO]`, y `[APLICADA]` no en pendiente), REPARA lo mal clasificado
(mover + normalizar, sin tocar contenido) y mantiene `backlog/INDICE.md` al día.
(Refinado tras feedback de Juanma: la primera versión enfatizaba solo HECHO/
APLICADA; se amplió a revisión GENERAL.) Ajustado el Paso 0: la nota "NO abras
tareas/hecho/ ni mejoras" salva la inspección de higiene. Verificado:
`HIGIENE GENERAL DEL BACKLOG` presente, cubre los 4 estados + TODAS las carpetas,
0 referencias a `TODO.md`/`MEJORAS.md` viejos, job `paused` (Concilio hasta gate).

**SEGUNDA RIENDA (feedback de Juanma, misma decisión):** reforzado que el plan
ES el entregable nº1 de Gwyndolin y que la higiene NUNCA sustituye a un plan
fuerte. Prompt reestructurado: aviso "TU ENTREGABLE ES EL PLAN" al inicio +
misión numerada que comienza con sanear el backlog (punto 0) pero deja claro
que el plan manda y lo exige bueno (selección con criterio por valor,
tareas concretas con módulo/descripción/criterio, regla de tamaño,
coherencia con DESIGN e historia, reparto sin colisiones en git). Verificado en
jobs.json.

**RELEVO:** Gwyn (23:00) es quien valida/aplica mejoras vía auto-mejora; este
cambio lo aplicó Raiden por orden directa de Juanma, registrado aquí. Concilio
sigue pausado hasta el gate (21:00 hoy).

CICLO: verde

---

## 26/08/2026 — 15:20 — [Arquitecto] Módulos src/, estructura por agente y tabla PROJECT-MAP §3

**QUÉ:**
- **`src/` definido con README por módulo** (guía para los ejecutores; aún no
  hay código): `src/README.md` (mapa + reglas + dueños), `core/README.md` +
  `core/ARCHITECTURE.md` (normativa operativa: frontera core/render, contrato
  comandos→eventos, grafo de dependencias entre paquetes, convenciones) y
  README propio en `sandbox/`, `curriculum/`, `generator/`, `engine/`,
  `state/`, `progression/`, `karma/`, `render/`, `data/`, `tests/`, `assets/`.
- **Estructura de carpetas por agente** (`backlog/`): la vigente se respeta
  TAL CUAL (tareas/mejoras/zona-testeo/notas-manana/planes). Solo materialicé
  lo ya documentado que faltaba físicamente: `backlog/historia/` completa
  según AGENTS-PLAN §6.1 (INDICE + PERSONAJES + ESCENARIOS + FRAGMENTOS +
  CAPITULOS/) y `backlog/planes/README.md` (formato del plan diario).
- **PROJECT-MAP §3 cerrado**: tabla de 13 módulos con dueño y rama
  (Ornstein=feat/engine, Smough=feat/sandbox, Seath=feat/meta-ui; una rama =
  un dueño = rutas disjuntas), §3.2 nueva (espacio de trabajo por agente),
  orden de arranque sugerido para Fase 1, §1 sincronizado.
- **`docs/ADR/ADR-0001-arquitectura-core-render.md`** estrenando el directorio
  ADR que ya estaba anunciado en el mapa pero vacío.
- **`backlog/mejoras/pendiente/propuestas.md`**: propuesta [NUEVA] con las dos
  decisiones fuera de estructura existente (harness → `tools/harness/` raíz;
  estreno de docs/ADR/) para trazabilidad de Gwyn.
- Sincronizados `backlog/INDICE.md`, README raíz (sección libreta).

**POR QUÉ:**
- El encargo pedía NO reinventar el backlog: correcto y así ha sido — los dos
  huecos reales (`historia/` materializada, `planes/` documentada) ya estaban
  definidos en AGENTS-PLAN §6.1 e INDICE.md; crearlos no es rediseño, es hacer
  que el repo coincida con su propia documentación.
- La única pieza sin hogar era el harness de Ornstein: herramienta de CI/métricas,
  no juego (rompería la frontera si viviera en `src/`) ni entrega narrativa. Va a
  `tools/harness/` (raíz), como propuesta formal porque afecta a otro agente.
- Reparto de ejecutores pensado para cero conflictos de git: sandbox/curriculum
  (Smough) comparten verdad de comandos; generator/engine/common (Ornstein)
  consumen esa verdad contra contratos; state/progression/karma/render/assets
  (Seath) son la capa meta+UI. `data/` repartido fichero a fichero; la prosa de
  Manus entra SIEMPRE vía ejecutor integrador (formato JSON protegido).
- Los números ⚠️ v1 del diseño viven en `data/*.json`, no en código: el harness
  calibrará balance con PRs de datos, no de lógica.

**ENTREGABLE / ESTADO:**
- `src/**/README.md` ×11 + `core/ARCHITECTURE.md` · `docs/PROJECT-MAP.md` §3.1/§3.2
  · `docs/ADR/ADR-0001…` · `backlog/historia/*` · `backlog/planes/README.md`
- `[HECHO]` marcado junto a la línea de crons de Fase 0 en
  `backlog/tareas/en-curso/activo.md`. Commit + push a origin/main.

**RELEVO:**
- **Gwyn (23:00 hoy)**: 2 propuestas [NUEVA] mías en `mejoras/pendiente/`
  (harness en tools/, ADR estrenado) — validar/aplicar o rechazar.
- **Coordinador de cierre (18:00)**: arquitectura lista para el gate de Juanma;
  resumen de lectura: ADR-0001 + PROJECT-MAP §3.
- **Ejecutores (Fase 1)**: leer SU fila de §3.1 + el README de su módulo antes
  del primer commit; arranque sugerido common → ramas paralelas contra stubs.
  Pendiente nombrado: validar lista comando-a-comando (~60 boons, DESIGN §9)
  contra el sandbox soportable — dueño Smough+Arquitecto, bloquea contenido,
  no estructura.

CICLO: verde
