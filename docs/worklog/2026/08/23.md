# WORKLOG — 2026-08-23

> Entradas del día. Un bloque por agente/turno. Índice en `../index.md`.

## Raiden — Configuración de los crons del proyecto (Fase 0 + Concilio)

**QUÉ:**
- Creados los **crons de Fase 0** (activos, arrancan mañana 24/08):
  - `1362fe72e94c` Panel de uso diario (00:00, script `cyberroot_usage.py`, sin agente → 0 tokens de razonamiento).
  - `87baf5198051` Research Stack (03:00, deepseek-v4-flash).
  - `3474e0b2ff3e` Research anti-slop (05:00, deepseek-v4-flash + skill humanizer).
  - `f93944903cf6` Research mecánicas + dopamina (07:00, deepseek-v4-flash).
  - `4326cbe71e52` Diseñador Jefe / DESIGN (11:00, deepseek-v4-pro — caro).
  - `70997a08ff3a` Arquitecto de módulos (16:00, deepseek-v4-pro — caro).
- Creados los **crons del Concilio (Fase 1)** PERO **pausados** (se activarán tras el gate de la Fase 0):
  - Manus (03:00, flash, humanizer) · Havel (07:00, flash) · Gwyndolin (11:00, pro) · Ornstein (13:00) · Smough (16:00) · Seath (19:00) · Artorias (21:00) · Gwyn (23:00, gpt-5.6-luna).
- Confirmado: timezone del sistema = `Europe/Madrid` (los crons usan hora Madrid, verificado con el briefing existente).
- Confirmado: proveedor `opencode-go`, modelos verificados (deepseek-v4-flash/pro, gpt-5.6-luna) vía `opencode models`.

**POR QUÉ:**
- Fase 0 (primeros ~4 días) = research/diseño con crons provisionales. **NO coincide con el Concilio**: los del Concilio están PAUSADOS para que no disparen mientras la Fase 0 corre (comparten horas 03/07/11/16). Al cerrar el gate de Juanma: pausar Fase 0 y `resume` del Concilio.
- Cada cron lleva su modelo asignado por el concilio (caros restringidos en tokens; baratos para ejecutar/testear) y su `workdir=/home/juanma/CyberRoot`.
- Panel de uso = script sin agente para monitorizar el gasto (suscripción OpenCode Go ~$10/mes) sin gastar razonamiento.

**ENTREGABLE / ESTADO:**
- Jobs de Hermes creados y verificados (`cronjob list`). Los 6 de Fase 0 + panel = `scheduled`; los 8 del Concilio = `paused`.
- Script en `~/.hermes/scripts/cyberroot_usage.py`.

**RELEVO (próximos pasos):**
- Pipeline escalonada de Fase 0 — tareas LARGAS descompuestas en varias pasadas
  encadenadas, cada una leyendo y REVISANDO lo anterior (nada se hace en un solo
  prompt gigante). Diseñador = 5 pasadas (elegido por Juanma).
  - **DÍA 1 (24/08):** 03:00 Research Stack · 05:00 Research anti-slop · 07:00 Research mecánicas+dopamina.
  - **DÍA 2 (25/08):** 11:00 Diseñador P1 (concepto+historia) · 16:00 Diseñador P2 (revisión + roguelite).
  - **DÍA 3 (26/08):** 09:00 Diseñador P3 (capítulos/niveles) · 14:00 Diseñador P4 (dopamina+UX) · 18:00 Diseñador P5 (revisión final).
  - **DÍA 4 (27/08):** 11:00 Arquitecto → módulos · 18:00 Coordinador → resumen + gate.
- Tras el research/diseño, **gate de Juanma** sobre `DESIGN.md`.
- Aprobado → los jobs de Fase 0 ya serán one-shot ejecutados; `resume` de los 8 del Concilio.
- Verificar que los crons escriben en la libreta (worklog/TODO) y hacen push.

---

## Raiden — Ajustes post-conciliación (modelo Fase 0 + harness + relevo tester)

**QUÉ:**
- **Modelo de Fase 0 → `opencode-go/ox-alpha-free`**: potente y GRATIS ~1 semana (decisión de Juanma). Aplicado a los 10 jobs de Fase 0 (3 research + 5 pasadas de diseño + arquitecto + coordinador). Confirmado su existencia con `opencode models`.
- **Harness de playtest automático**: integrado en el prompt de Ornstein (ejecutor 1) — construirá un script que lanza runs headless y mide resolubilidad, duración y balance, para que el comité "juegue" a cientos de runs con datos.
- **Relevo del tester reforzado**: Havel (07:00) ahora deja una sección "🔮 Para el plan de mañana" en `backlog/TODO.md` (bugs críticos + 3 mejores ideas + qué priorizar), para que Gwyndolin arranque con dirección y el trabajo del tester no se pierda.
- **Flujo 100% autónomo ya documentado**: ideas de Havel → Gwyndolin sin aprobación humana (confirmado en sesión previa).

**POR QUÉ:**
- ox-alpha-free: Juanma quiere máxima potencia para el diseño/research y es gratis ahora mismo (optimización de coste para la Fase 0).
- Harness: que el tester (Havel) no teste "a ciegas"; con datos de balance real el comité mejora el juego con métricas, no intuiciones.
- Relevo del tester: de nada sirve un tester si sus notas no alimentan el plan del día siguiente — es lo que da sentido a su turno.

**ENTREGABLE / ESTADO:**
- 10 jobs de Fase 0 → modelo `ox-alpha-free`.
- Prompts de Havel y Ornstein actualizados. Docs (AGENTS-PLAN) reflejan modelo, harness y relevo.
- Concilio sigue pausado (se activa tras el gate de Fase 0).

**RELEVO:**
- Fase 0 arranca mañana 24/08 03:00 con ox-alpha-free.

---

## Raiden — CONCILIO: feedback crítico del tester + auto-mejora del comité

**QUÉ:**
- **Havel ahora da feedback CRÍTICO** (no solo ideas): señala qué está mal/roto, qué no debe mergear, qué no es divertido y cómo mejorarlo, y qué ideas del backlog descartar (`[DESCARTADO]` con motivo). Además deja "🔮 Para el plan de mañana".
- **Auto-mejora del comité creada.** Nuevo `docs/AGENTES.md` (roles de cada agente, para que todos sepan qué hace el resto) y `backlog/MEJORAS.md` (donde cualquier agente propone mejoras a su rol/prompt/flujo, que Gwyndolin o un humano aplican). Añadido AGENTS-PLAN §2.6.
- **Visibilidad entre agentes**: los 8 prompts del Concilio ahora leen `docs/AGENTES.md` (saben con quién coordinan) y revisan `backlog/MEJORAS.md` en su Paso 0. Gwyn además valida las propuestas de auto-mejora.
- **PROJECT-MAP actualizado**: navegación incluye AGENTES.md y MEJORAS.md.

**POR QUÉ:**
- Juanma quiere que el comité construya el juego solo DESDE CERO: el tester no solo aprueba, CRITICA y descarta; y los agentes pueden mejorarse a sí mismos si su rol no funciona.
- Que todos sepan qué hace cada uno evita pisarse y permite proponer mejoras con criterio.

**ENTREGABLE / ESTADO:**
- 8 prompts del Concilio actualizados (feedback crítico + AGENTES + MEJORAS).
- `docs/AGENTES.md`, `backlog/MEJORAS.md` creados. AGENTS-PLAN §2.6 y PROJECT-MAP actualizados.
- Concilio sigue pausado (se activa tras el gate de Fase 0).

**RELEVO:**
- Fase 0 arranca mañana 24/08 03:00 (usa `ox-alpha-free`).
- Tras el gate, resume de los 8 del Concilio.

---

## Raiden — Corrección de división de trabajo (quién deja notas de gusto)

**QUÉ/POR QUÉ:**
- Juanma aclaró que los "tester" que deben dejar notas de gustos/ideas para mañana
  son **Artorias (21:00) y Gwyn (23:00)** — los revisores finales — NO Havel.
- **Havel** ya tenía demasiada carga (jugar + generar ideas + crítica + relevo) →
  ahora su rol es LIGERO y enfocado: 1) JUEGAR lo nuevo, 2) GENERAR ideas frescas,
  3) anotar bugs. SIN crítica de diseño ni notas de gusto (eso sobrecargaba).
- **Artorias**: revisa técnicamente (💥/✅) + deja NOTAS DE GUSTO + "qué no mergear"
  + sus ideas, en `backlog/TODO.md`.
- **Gwyn**: merge final + reporte a Juanma + DEJA SUS NOTAS DE GUSTO/ideas para mañana.
- Actualizados los prompts de Havel/Artorias/Gwyn y la documentación (AGENTES.md, AGENTS-PLAN).

**ENTREGABLE / ESTADO:**
- 3 prompts del Concilio reequilibrados. Docs actualizados.
- Concilio sigue pausado (se activa tras el gate de Fase 0).

**RELEVO:**
- Fase 0 arranca mañana 24/08 03:00 (ox-alpha-free). Tras el gate, resume del Concilio.

---

## Raiden — Flujo de ramas: Gwyn mergea solo; ramas no mergeadas quedan con explicación

**QUÉ/POR QUÉ:**
- Juanma recordó que **es Gwyn quien mergea**. Si un ejecutor trabaja en una rama
  y esa rama no se mergea, **Gwyn deja por escrito el POR QUÉ no se ha mergeado y
  CÓMO arreglarlo**, manteniendo la rama viva para que el ejecutor la coja.
- Alineado el flujo de ramas completo:
  - Ejecutores (Ornstein/Smough/Seath) trabajan en RAMA (`feat/<modulo>`), abren P.R.
    a `main`, nunca tocan main directamente. Si su rama fue rechazada, la arreglan PRIMERO.
  - Artorias revisa las ramas/PRs, marca 💥/✅ y avisa a Gwyn qué no mergear.
  - **Gwyn es el ÚNICO que mergea**: si no mergea, NO borra la rama, documenta
    por qué + cómo arreglarlo en `backlog/TODO.md` (+ comentario del PR), y la tarea
    vuelve a `[EN CURSO]`.
- Actualizados los prompts de los 3 ejecutores (ramas + fix de rechazos), Artorias
  (revisa PRs) y Gwyn (regla HARD de merge/no-merge). AGENTS-PLAN: cadena de PRs ★.

**ENTREGABLE / ESTADO:**
- 5 prompts del Concilio actualizados al flujo de ramas. AGENTS-PLAN documenta la cadena.
- Concilio sigue pausado (se activa tras el gate de Fase 0).

**RELEVO:**
- Fase 0 arranca mañana 24/08 03:00 (ox-alpha-free). Tras el gate, resume del Concilio.

---

## Raiden — Visto bueno final del Concilio + firma por agente + README

**QUÉ:**
- **Revisado y verificado el sistema del Concilio** (list de 20 jobs): 6 de Fase 0
  activos (research + diseño P1-P5 + arquitecto + coordinador, todos `ox-alpha-free`),
  8 del Concilio pausados (listos para activar tras el gate), panel de uso activo.
  Visto bueno de Raiden al diseño del sistema.
- **Firma por agente**: los 8 prompts del Concilio ahora configuran su identidad
  git con el nombre del agente antes de commitear (`git config user.name "X"` +
  email `x@cyberroot`). El historial del repo atribuye cada pieza a su agente.
- **README reescrito y documentado**: El Concilio (nombres/horas/roles/modelos),
  el flujo diario, la libreta, la auto-mejora, la Fase 0 y el estado actual.
  Incluye una nota honesta sobre las "fotos": como es un único repo bajo la
  cuenta de Juanma, GitHub muestra su avatar, pero la autoría se atribuye por nombre.

**POR QUÉ:**
- Juanma pidió revisar y dar visto bueno final al Concilio, documentarlo en el
  README, y que cada agente firme con su nombre (no como "lo hizo el humano").

**ENTREGABLE / ESTADO:**
- 8 prompts del Concilio con firma por agente.
- `README.md` reescrito (Concilio + flujo + Fase 0 + nota de avatares).
- Concilio sigue pausado (se activa tras el gate de Fase 0).

**RELEVO:**
- Fase 0 arranca mañana 24/08 03:00 (`ox-alpha-free`). Tras el gate, resume del Concilio.

---

## Raiden — Estructura de carpetas por agente + planes por fecha

**QUÉ (3 cambios pedidos por Juanma):**
1. **Estructura de CARPETAS por agente** (más allá de los módulos de código):
   - Añadido en AGENTS-PLAN §6.1 y en el prompt del Arquitecto de Fase 0: cada
     agente tendrá su carpeta de trabajo definida. Ejemplo Manus:
     `backlog/historia/` con `INDICE.md`, `PERSONAJES.md`, `ESCENARIOS.md` y
     `CAPITULOS/<capitulo>.md` (esquema primero, texto después).
   - Prompt de Manus actualizado para mantener esos documentos vivos y coherentes.
   - El Arquitecto (día 4 de Fase 0) ahora define módulos de código + carpetas
     por agente, y lo registra en PROJECT-MAP.
2. **Planes por fecha**: en vez de un único `backlog/PLAN-del-dia.md`, ahora cada
   día guarda su plan en `backlog/planes/YYYY/MM/DD.md` (histórico completo). Los
   ejecutores leen "el plan de HOY" por fecha. Migradas todas las referencias
   (AGENTS-PLAN, PROJECT-MAP, AGENTES, README, TODO) y los 8 prompts del Concilio.
3. **Todos los agentes tienen en cuenta el plan y la historia**: Gwyndolin lee la
   historia para dar coherencia narrativa al plan; Manus lee el plan de hoy;
   Havel inspira ideas con la historia; ejecutores y revisores consultan
   el plan del día. Confirmado y reforzado en los prompts.

**POR QUÉ:**
- Que cada agente tenga su carpeta estructurada (índice, personajes, escenarios,
  capítulos) hace la documentación sostenible y evita docs kilométricos.
- Planes por fecha conservan el histórico, de igual forma que el worklog, para
  que el sistema registre su travesía completa.

**ENTREGABLE / ESTADO:**
- Docs y 8 prompts del Concilio migrados a `backlog/planes/YYYY/MM/DD.md`.
- AGENTS-PLAN §6.1 + prompt del Arquitecto con estructura de carpetas por agente.
- 0 referencias a `PLAN-del-dia` en prompts de crons; solo la explicativa en AGENTS-PLAN.
- Concilio sigue pausado (se activa tras el gate de Fase 0).

**RELEVO:**
- Fase 0 arranca mañana 24/08 03:00. Tras el gate, `resume` del Concilio.

---

## Raiden — Delegación: los agentes pueden invocar sub-agentes

**QUÉ:**
- Habilitado el toolset `delegation` (`delegate_task`) en los **8 agentes del Concilio** → se convierten en coordinadores que pueden invocar sub-agentes.
- **Config global:** `delegation.model = deepseek-v4-flash`, `delegation.provider = opencode-go` → los sub-agentes SIEMPRE usan el modelo barato, incluso cuando el coordinador es Gwyn (gpt-5.6-luna, caro).
- **Patrón documentado** (plan → delega → verifica → reinvoque): el agente decide si conviene delegar, invoca sub-agentes enfocados, VERIFICA el resultado real (no se fía del resumen) y reinvoca el ciclo si falla.
- **Límites:** 3 sub-agentes simultáneos; anidamiento de 1 nivel (sub-agentes son hojas). Tareas grandes = repetir el ciclo.
- Documentado en `docs/AGENTES.md` (sección DELEGACIÓN), `docs/AGENTS-PLAN.md` §2.7 y `docs/PROJECT-MAP.md` (paso 9).

**POR QUÉ:**
- Juanma quiere que cada agente sea un orquestador: coordina sub-agentes para ejecutar tareas sin llenar su ventana de contexto, más eficiente y con mejor cumplimiento de tareas.
- Fijar el modelo de delegación a flash evita que Gwyn (caro) dispare sub-agentes caros.

**ENTREGABLE / ESTADO:**
- Config `delegation.model/provider` → flash/opencode-go. 8 prompts con toolset `delegation`.
- **Fase 0 también delegada**: los 10 jobs one-shot de Fase 0 (3 research + 5 diseñador + arquitecto + coordinador) ahora tienen el toolset `delegation`.
- 18/19 jobs de CyberRoot con delegación (el único sin ella es el Panel de uso, que es un script `no_agent` y no la necesita).
- Docs actualizados (AGENTES, AGENTS-PLAN §2.7, PROJECT-MAP).
- Concilio sigue pausado (se activa tras el gate de Fase 0).

**RELEVO:**
- Fase 0 arranca mañana 24/08 03:00. Tras el gate, `resume` del Concilio.

---

## Raiden — Skills de proyecto: los agentes pueden crear skills locales

**QUÉ:**
- Añadido el toolset `skills` a todos los jobs de Fase 0 (ahora 10/10 tienen skills + delegación) y a los 7 agentes del Concilio que no lo tenían (Manus ya lo tenía) → los 8 del Concilio pueden crear/mejorar skills.
- Documentado **AGENTS-PLAN §2.8 (SKILLS DE PROYECTO)** y sección SKILLS en `AGENTES.md`: los agentes pueden usar `skill_manage` para crear skills locales que estandaricen tareas (escribir niveles, limpiar AI-slop, revisar PRs, harness...), y mejorarlas conforme avanza el proyecto (mismo mecanismo que la auto-mejora).
- **Decisión de Juanma**: NO hace falta `AGENTS.md` de proyecto (el índice PROJECT-MAP + libreta cubre la navegación); las skills complementan con procedimientos reutilizables.
- Añadida la tarea "PLANEAR skills de proyecto iniciales" a los pendientes de la Fase 0 (que el Diseñador/Arquitecto las definan).

**POR QUÉ:**
- Juanma quiere que los agentes sepan que pueden crear skills a nivel local para estandarizar tareas, que se planee en la fase inicial y se mejore conforme avanza el proyecto (reforzando la auto-mejora).

**ENTREGABLE / ESTADO:**
- 10/10 jobs de Fase 0 y 8/8 del Concilio con toolset `skills` + `delegation`.
- AGENTS-PLAN §2.8 + AGENTES.md SKILLS + pendiente de Fase 0 actualizado.
- Concilio sigue pausado (se activa tras el gate de Fase 0).

**RELEVO:**
- Fase 0 arranca mañana 24/08 03:00. Tras el gate, `resume` del Concilio.

---

## Raiden — en sesión de diseño con Juanma (entrada original)

**QUÉ:**
- Documentamos la Fase de Diseño del proyecto CyberRoot en
  `docs/AGENTS-PLAN.md` y montamos la infraestructura del repo:
  repo público `amcgiluma/CyberRoot` creado y conectado (commit inicial
  `9d4c0f9`, hasta el HEAD del día).
- Creada la estructura de la libreta: `docs/PROJECT-MAP.md` (índice maestro),
  `backlog/TODO.md` (cola con estados), `docs/BRAINSTORM.md` (ideas),
  `docs/INVESTIGACION-STACK.md` (research preliminar, NO definitivo — se ha de
  ampliar en la Fase 0), `docs/AGENTS-PLAN.md` (sistema de agentes),
  `docs/worklog/` (registro diario por fechas, este sistema).

**POR QUÉ:**
- Decidido con Juanma el modelo de desarrollo: un **comité de IAs diario y
  autónomo** que construye el juego poco a poco, documentando todo y siendo
  transparente en GitHub (es la feature y el gancho público).
- Cada cron arranca sin memoria; por eso la **libreta es la fuente de verdad
  única** (PROJECT-MAP + TODO + WORKLOG + ADR). Sin ella el sistema no se
  comunica entre sesiones.
- El WORKLOG se organiza **por fechas** (`worklog/YYYY/MM/DD.md`) en lugar de
  un archivo único: decisión de Juanma para que el registro escale sin volverse
  kilométrico. Cada día es un fichero; un agente lee "lo de ayer" en un solo paso.

**DECISIONES CLAVE cerradas hoy (registro):**
- **Nombres del concilio** (lore Dark Souls): Manus (historiador 3:00),
  Havel la Roca (tester 7:00), Gwyndolin (planificador 11:00), Ornstein
  (13:00), Smough (16:00), Seath (19:00), Artorias (revisor 21:00),
  Gwyn (revisor + merge 23:00).
- **Modelos (verificados con `opencode models opencode-go`):** planificador
  y diseñador jefe = `opencode-go/deepseek-v4-pro` (caro, tokens restringidos);
  el resto del concilio = `opencode-go/deepseek-v4-flash` (barato); revisor de
  diseño + merge = `opencode-go/gpt-5.6-luna`.
- **Diseño del juego:** roguelite HÍBRIDO estilo Hades (muerte alimenta el
  avance, base/Hub entre runs, boons de conocimiento, metaprogresión,
  karma Blue/Red con finales múltiples) + dopamina tipo Balatro (números,
  combos, feedback chillón). Interfaz: mapa de nodos/HUD visual (pixel-art),
  interacción de resolución en terminal real. → referencia oficial del
  `DESIGN.md` de la Fase 0.
- **Cadena de PRs:** ejecutor crea branch/PR → Artorias valida/filtra → Gwyn
  revisa en profundidad y hace el MERGE final → reporta a Juanma.
- **Eficiencia de tokens:** panel de uso a `docs/USAGE.md` vía
  `opencode stats --days N --models` (suscripción OpenCode Go ~ $10/mes).

**ENTREGABLE / ESTADO:**
- `docs/AGENTS-PLAN.md`, `docs/PROJECT-MAP.md`, `backlog/TODO.md`,
  `docs/BRAINSTORM.md`, `docs/INVESTIGACION-STACK.md`, `docs/worklog/`
  → en main (repo público `amcgiluma/CyberRoot`).
- Tareas aceptadas actuales en TODO como `[PENDIENTE]`/`[APROBADO]`.

**RELEVO (próximos pasos):**
- Definir/programar el cron del panel de uso (métricas diarias → `USAGE.md`).
- Configurar los **crons de Fase 0** (research stack/mecánicas/anti-slop,
  diseñador jefe, arquitectura) → producir `DESIGN.md` + plot + mapa de
  módulos. Gate de Juanma al cierre.
- Aprobado el diseño → instalar los crons del **concilio diario** (7 agentes)
  y eliminar los provisionales.