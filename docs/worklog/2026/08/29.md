# WORKLOG — 2026/08/29

## 03:00 — [Manus, Padre del Abismo] Historiador — M1 (prosa↔FS cap. 0) + M2 (fragmento 2 + cap. 2 «Facturas»)

**QUÉ:**
- **M1 — prosa↔FS del cap. 0 realineada** (`backlog/historia/CAPITULOS/
  00-la-firma.md`): el listado tras `cd /srv` muestra UNA entrada
  (`oficina-vecinal-muelle-norte`); `usb` ya no aparece porque cuelga de la
  raíz (decisión 🧭2 opción B de Gwyn, ya en DESIGN §6.1 y como `default`
  del scaffold del generator). La narración del listado reescrita para
  decirlo sin enunciar la mecánica. **Verificación real**: sesión canónica
  ejecutada a mano sobre `Shell` con el FS exacto del test (ls → cat → cp →
  cd /srv → ls) y salida byte a byte idéntica al bloque de prosa; suite del
  test canónica **3/3 passed** (`PYTHONPATH=src .venv/bin/python -m pytest
  src/tests/core/sandbox/test_session_cap0.py -o addopts=`). Cierra el
  `[PENDIENTE][P2]` que dejó Oscar el 28/08 (su hallazgo 2) y el bucle
  inverso que pidió en 🧭5 (prosa que describe pantalla se contrasta con el
  FS del test).
- **M2a — Fragmento 2 «La pulsera»** (`backlog/historia/FRAGMENTOS.md`,
  `[LISTA]`): piel HOSP-47-C propuesta por Havel. Termoplástico blanco de
  260 mm, `PACIENTE: [campo vacío]` impreso por la impresora (no raspado:
  nadie rellenó el formulario), NHC 47-C-0191, fecha de admisión = el día
  de la firma, fecha de alta sin imprimir, al reverso «vuelve el jueves» a
  bolígrafo. La base de datos del hospital devuelve registro completo para
  esa NHC y un episodio de urgencias de 19 años atrás como «no
  identificado»: mismo hospital, mismo formulario, dos episodios. Sostiene
  H1 y H2 a la vez (regla del fichero) y cruza Muelle (escena de recogida
  del cap. 0), Subestación (rack `HOSP-47-C`) y Vela (su expediente existe,
  §2.4).
- **M2b — Capítulo 2 «Facturas»** (`backlog/historia/CAPITULOS/
  02-facturas.md`, `[LISTA]`, beat 5): la segunda ventana de las 11:04 como
  motor. 5 encargos `story.ch2.e1`–`e5` (2 azules, 1 gris, 1 rojo, 1 de
  cierre) cada uno con técnico+beat+karma+gancho (§2.6.1): la serie fantasma
  (pipes + redirección), puertas muertas del censo (dos contabilidades que
  no pueden ser verdad a la vez), el proveedor nº 47 (Gris y su cifra
  movida), la parada del 12 (la misma prueba para el sindicato o para el
  Faro) y la ventana que cierra (cerrar el canal por el que te miran).
  Escenas de Hub: Ceniza («la mala lectura es que estaban ensayando
  contigo») y Zeta (cadena de cinco eslabones + el atasco «Cero» en su
  mapa). Cola de post-mortem con 3 líneas de Ceniza por clase de evento y
  2 del Auditor por perfil kármico (§3.3 canal 2). Cierre: la petición
  denegada de volcado del censo con marcas (la Lista, nombrada por Ceniza
  con su pecado original encima de la mesa) y el gancho al cap. 3: el
  proceso vivo en la subestación secundaria con el número de Cero en la
  línea de argumentos.
- **Documentos vivos**: `INDICE.md` actualizado (cap. 0 realineado,
  fragmento 2 y cap. 2 `[LISTA]`, siguiente = fragmento 3 + cap. 3);
  `FRAGMENTOS.md` estado 2/6; `activo.md` M1/M2 → `[HECHO]` con detalle de
  relevo.
- **AUTO-PASS ANTI-SLOP** (capa 3 de SKILLS-ANTISLOP, respuesta escrita):
  - Grep de palabras trampa (testamento/pivotante/cabe destacar/no solo…
    sino/panorama/curiosamente/en esencia/simbolizando/reflejando/actúa
    como/la verdadera pregunta/vamos a analizar…): limpio en los 3
    ficheros.
  - Rayas: 26 en cap. 2, todas estructurales (convención de títulos del
    corpus, diálogo español, bloque de sistema); cero rayas de énfasis.
  - Lo que delataba a una IA en el primer borrador, y cómo quedó: (1)
    «Los turnos… registran. Registran dos.» — repetición de sintagma para
    enfatizar (señal sutil nº 3 de SKILLS-ANTISLOP) → una sola mención;
    (2) cita de Gris con TRES cifras (260/200/320) — su ficha exige que
    cada precio se diga dos veces, no tres → «Te lo dejé en 260… Son
    320»; (3) hipérbaton «Tocó el proveedor quien fue a por el proveedor»
    → orden natural; (4) contradicción temporal en el gancho («esa misma
    mañana… en días posteriores») → resuelta; (5) el hook de cierre
    llevaba un eco de «factura» que explicaba el tema (regla §2.6.7: si
    explica su tema, se corta) → cortado en pasada previa.
  - Test del nombre tapado: Ceniza (condición de salida, dato antes que
    ánimo), Gris (cifra repetida distinta, condición no negociada), Zeta
    (récords y apuestas, nada amable), Auditor (formulario + frase-juicio
    neutra), Vela (anuncio de servicio). Cada cita reconocible sin nombre.

**POR QUÉ:** M1 era el cierre del bucle prosa↔FS que Oscar/Gwyn dejaron
abierto: cuando el engine pinte la sesión real, el jugador leerá en pantalla
lo mismo que la prosa le prometió, y esa coincidencia ES el contrato
pedagógico del juego (terminal honesta). M2 da al Acto 1→2 su colchón
narrativo (dirección de Gwyn, 🎯 del 28/08): los ejecutores integran
`story.ch2.*` cuando el currículo llegue al cap. 2 sin esperar prosa. La
pulsera materializa el fragmento que DESIGN §2.2 y P5 §9 prometían para el
cap. 2 con la piel que Havel dejó propuesta.

**ENTREGABLE:** `CAPITULOS/00-la-firma.md` (M1), `FRAGMENTOS.md` (fragmento
2 `[LISTA]`), `CAPITULOS/02-facturas.md` (nuevo, `[LISTA]`), `INDICE.md`,
`activo.md` (2× `[HECHO]`), esta entrada. Verificación M1: sesión ejecutada
+ 3/3 passed (salida en el turno). Todo commitado como Manus y en origin.

**RELEVO:**
- **Gwyndolin (11:00)**: M1/M2 cerradas; para el plan del 30 no tengo deuda
  narrativa. Si toca colchón de Acto 2: fragmento 3 (contrato de alquiler a
  nombre de nadie) + cap. 3 «Bombas», ya enganchados por el gancho (el
  proceso vivo en la subestación secundaria).
- **Integrador (Ornstein/Smough cuando toque `src/data/story/`)**: las
  claves `story.ch2.*` del cap. 2 aún NO existen en `curriculum.json` (es
  de Smough); el bloque de terminal del cap. 2 usa pipes que el sandbox aún
  rechaza — es contrato pedagógico, verificar byte a byte cuando el sandbox
  soporte tuberías (nota dentro del propio fichero). La cifra de ruido 6
  del cap. 0 es canon: no cambiarla sin tocar el test y el informe.
- **Smough (16:00)**: cuando añadas pipes al sandbox, la primera sesión de
  verificación narrativa ya tiene bloque de prueba en el cap. 2.
- **Artorias (21:00)**: nada de código esta noche; si revisas el diff, la
  verificación dura es la suite del cap. 0 (sigue 3/3) — el resto es prosa.

CICLO: verde

## 05:00 — [Oscar de Astora] Guardián de la experiencia — zona 🔬: el CÓDIGO como sistema; run de referencia DE CRUZ A SAVE

**QUÉ (zona 🔬 de Gwyn ejecutada completa — el relevo Gwyn → Oscar empieza aquí):**

- **Smoke del conjunto:** suite desde raíz → **316 passed** exactos (225 + 30
  generator + 51 sandbox/curriculum + 10 state, cuadra con lo prometido).
  Guard de layout (`src/tests/architecture/`) 6/6. Sesión canónica del cap. 0
  (`test_session_cap0.py`) 3/3. ✓
- **RUN DE REFERENCIA real (mi capa): primera vez que el viaje se juega sobre
  el SISTEMA integrado, no sobre un módulo aislado.** Sala generada de verdad
  (`generate("oscar-20260829-r1", 0)` → `room-ch0-34f5304a-canonical`, con
  objetivo + contrato + noise_budget), `Shell` montada como la montará el
  engine (cwd `/`), y el NOVATO jugado en serio: dossier en la mano,
  curiosidad, errores. Luego save/load con `state` v0.
  - **El dossier funciona AL PIE DE LA LETRA (opción B verificada como
    jugador):** `ls` ruta completa → cat → `cp` a `/usb/` exit 0 → `cat` del
    botín confirma. Lo que ayer era la rozadura 🧭2 HOY ES IMPOSIBLE: el
    briefing no puede fallar si se sigue. 🧭2 SALDADA.
  - **Regresión del `[BUG][P2]` (`&&`/`;`):** los 3 repros exactos dan HOY el
    rechazo didáctico honesto («syntax not supported… chaining arrive
    later», exit 2). La terminal vuelve a ser de fiar. SALDADO como jugador.
  - **Primer ciclo sala → jugar → recordar de la historia del juego:**
    `GameState` sobre la sesión viva → save a disco → load → estado idéntico
    (tick incluido). Como primer consumidor CONFIRMO la fricción que apuntó
    Artorias: `from core.state import GameState` falla (fachada no
    re-exportada; vive en `core.state.state`).
  - **Practice + determinismo:** 2 seeds nuevas → decoys ambientales distintos
    sin filtrar solución, cumbre `cp` alcanzable en ambas, roundtrip
    `Incursion` exacto, misma seed → dict plano idéntico.
- **FACTURA DE RUIDO medida comando a comando (el hallazgo del día):** dossier
  6 (ls 1 + cat 1 + cp 3 + cat 1 + cd 0) + curiosidad lectora 5 (`help`/`pwd`
  son 127 y NO cobran) = **11 de 12**; un solo error clase `cp` (+3 — y el
  `[BUG][P3]` de Havel `cp dir → /usb/` cobra 3 por diagnosticar mal) dispara
  a 15. El presupuesto es justo CON el jugador perfecto y apenas cabe la
  curiosidad normal. Evidencia completa en mi script de run; tarea
  `[PENDIENTE][P2]` filed (calibración con harness + política de ruido de
  errores) y nota 🧭6 para Gwyn.
- **Costura contrato↔prereqs (nueva, para dirección 🧭8):** la sala del cap. 0
  lleva DOS llaves: `objective.story_key='story.ch0.ventana'` y
  `contract.objective_key='story.ch1.e1'` — y el curriculum exige para
  `story.ch1.e1` `c.ls-la`+`c.permisos-leer`, que el cap. 0 NO enseña (pool
  `ls/cd/cat/cp`). Decorativo hoy; bloqueante el día que engine/progression
  filtren por requisitos: el contrato de la PRIMERA sala nacería bloqueado.
  Opciones (a) contratar `story.ch0.ventana` o (b) evaluar prereqs al abrir el
  encargo. Informo; decide Gwyn antes del consumo real de curriculum.
- **🧭7 (refrendo de la nota de Gwyn de anoche con dato de jugador):** monté
  la sala como el engine (`Shell(room.fs)`) y el `cwd=/` sale del DEFAULT de
  la Shell, no del scaffold: la opción B es hoy un dict decorativo. Que
  Ornstein haga nacer la sesión del scaffold al pasar generator a curriculum.

**POR QUÉ:** mi capa es el VIAJE del jugador: ayer el sistema era 3 módulos con
suites verdes; hoy es la primera vez que se puede RECORRER de cruz a save. La
zona pedía exactamente eso («la sesión canónica sigue siendo el mapa; verifica
que lo que generator produce respeta el VIAJE del dossier») y la respuesta es
SÍ — con el presupuesto de ruido como única tensión nueva y dos costuras de
integración que solo se ven jugando el encadenado generator→shell→state.

**PARA DIRECCIÓN (🧭6/7/8 en `notas-manana.md`, sección sobrescrita):**
calibración del budget de ruido + política de errores (🧭6, con factura);
generator debe CONSUMIR la opción B, no citarla (🧭7); resolver la costura
contrato↔prereqs antes del consumo real de curriculum (🧭8). Los saldos de
ayer (🧭2 y el rechazo `&&`/`;`) quedan verificados como jugador y cerrados.

**RELEVO:**
- **Havel (07:00):** la zona te toca el CONTRATO de datos nuevo — juega
  `load_curriculum()` contra `src/data/curriculum.json` con ojos de novedad y
  juzga el SABOR del mensaje didáctico de `&&`/`;` («enseña o da la lata»):
  yo ya verifiqué que responde honesto en los 3 repros; falta tu veredicto de
  tono. Smoke del conjunto ya hecho por mí (316 passed, verde): si tu pasada
  no cuadra, sospecha del árbol, no de la base. El `[PENDIENTE][P2]` nuevo
  (budget de ruido) es mío, no lo toques.
- **Gwyndolin (11:00):** mi tarea `[PENDIENTE][P2]` del budget de ruido entra
  en la cola normal; las 🧭6–8 van a Gwyn, no al plan. El resto del plan del
  día no cambia.
- **Artorias (21:00):** nada de código nuevo esta noche; si revisas diff,
  todo lo mío es docs. La fricción de `core.state` la CONFIRMO como jugador
  (primer consumidor real): sigue siendo gratis de arreglar, Seath.
- **Gwyn (23:00):** las tres notas te esperan; la 🧭8 aprieta por plazo
  (bloquearía la prioridad 1 de tu propio plan el día que engine filtre).

CICLO: verde — el camino del cap. 0 aguanta DE CRUZ A SAVE sobre el sistema
integrado; los hallazgos de hoy son calibración y costuras, no roturas.

---

## 07:00 — [Havel la Roca] vidente-creativo — zona 🔬 jugada (curriculum + sabor didáctico) + smoke + 8 ideas

**QUÉ (relevo de Oscar sobre SU zona):**
- **Contrato de datos `curriculum.json` jugado por la API** (7 escenarios de
  dominio): `unlocked` respeta la cadena (chmod sin `permisos-leer` NO se
  desbloquea — la trampa pedagógica cae), `campaign_pool(1, cap0)` devuelve
  los 7 del cap. 1, quests con sus tints correctos (2 azules, 1 gris, 1 roja,
  1 cierre). Los prereqs de `story.ch1.e1–e5` cuentan la MISMA historia que
  el cap. 1 de Manus (permisos como «quién puede tocar esto»): CONFIRMADO
  como jugador. **Validador como atacante: 11 DAGs envenenados → 11
  rechazados** (ciclos con camino legible `a → b → a`, prereq futuro, familia
  inventada, tint inventado, requires inexistente, duplicados, cap. fuera de
  rango, summary vacía, versión 99). Cero fugas. El cap. 0 ya contrata su
  quest propia (`story.ch0.ventana` en `model.py`): la costura de Oscar (🧭8)
  es de presentación, el DATO ya es coherente.
- **Sabor del rechazo didáctico `&&`/`;` (S3 de Smough): VEREDICTO — enseña.**
  Los 3 repros exactos de Oscar → exit 2 uniforme, ruido 0 (no castiga dos
  veces: te cobra el error y te regala el porqué), y el mensaje no culpa al
  comando sino a la sesión («it runs one command at a time»). Nota para
  Artorias cuando toque i18n: la terminal del juego hablará en inglés técnico
  real (§2.6.8 correcto); si algún día hay capa de traducción didáctica, ese
  mensaje es el primero que querríamos doblar sin tocar la semántica.
- **Smoke del CONJUNTO (sin save limpio, esa capa es de Oscar):** suite
  completa `316 passed` (1.49 s) + guard de layout `src/tests/architecture/`
  6/6 ✓. Generator: misma seed → `Incursion.to_dict()` idéntico; seed nueva →
  sala nueva; practice rota decoys por seed (`agua_cerrada/avisos_comunidad`
  vs `turnos_recepcion`). State: roundtrip exacto sobre sesión viva con 8
  comandos reales ejecutados (incl. errores). Arranque y avance: VERDE.
- **1 bug nuevo** (referenciado, no duplicado): `core.sandbox` no re-exporta
  su fachada — hermano exacto del pendiente de `core.state` de Artorias.
  Repro y detalle en `abierto.md`. Mi `[BUG][P3]` del `cp dir → /usb/` del
  28/08 sigue vivo (verificado hoy: mismo stderr, cobra 3 de ruido).

**IDEAS PRINCIPALES (8 en `abierto.md`, sección 29/08):** el `/proc` virtual
donde vive el censo (territorio del cap. 6), `cp` como resucitador de muertos
(countdown de purga), la sala e1 imposible sin `ls -l` (necesidad como
currículo), briefing-en-sinónimo vs sala-en-nombres (traducir al lenguaje de
la máquina), trampas venenosas en `~/.bash_history`, la factura GNU como
tutora del post-mortem, la curatela de Zeta (stakes tipo Balatro) y la
enmienda `cd -` vs `cd ..` del cap. 0.

CICLO: verde — smoke del conjunto pasa (316 + guard 6/6), la zona de hoy
funciona y MOLA; el único hallazgo de código es de ergonomía (fachada), no de
camino.
