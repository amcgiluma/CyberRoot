## 11:00 — [Gwyndolin, Dark Sun] Planificador — plan 15/09: la bifurcación TR-003 sale a main (e3)

**QUÉ:** turno 11:00 tras Oscar (05:00, MODO B save limpio) y Havel (07:00) — ambos verdes, sin [BUG]. ADR TR-003 FIRMADO por Gwyn anoche → `story.ch4.e3` planificable.

0. **Higiene:** prefijos↔carpetas concordantes en TODO el backlog (`pendiente/`, `en-curso/`, `hecho/`, `descartado/`, `mejoras/`); sin líneas mal clasificadas que yo deba mover (regla del 01/09: los `[HECHO]` de ayer los archiva Gwyn hoy). Señal de prioridad: la P1 🧭9 quedó CERRADA por Gwyn (PR #53); HOY la prioridad es el ADR firmado. 0 PRs huérfanos (`gh pr list` vacío a las 09:15).
1. **VERIFICACIONES REALES en máquina (novedad del turno — plan sobre la máquina, no solo la libreta):**
   - `rm` NO existe: `Shell(DEFAULT_CH4).execute("rm /tmp/volcado.csv")` → `127 command not found` (no hay handler ni `fs.remove_file`; solo `append_file`/`copy_file`).
   - La ruta-azul YA FUNCIONA: `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` exit 0 con contenido íntegro en el host faro (probado sobre sandbox real). Solo falta el detector del post-mortem.
   - `_exec_scp` NO pregunta host-key; el bundle web lleva `files.py` SIN `_run_rm` (quien añada `rm` DEBE regenerar).
   - Suite local 714/0 en worktree limpio.
2. **Plan 15/09** (`../planes/2026/09/15.md`): PRIMER DILEMA KÁRMICO REAL del juego:
   - **S1 (Smough, P1):** handler `rm` + allowlist `DEFAULT_CH4E3_COMMANDS` (14 cmds; ch4 base 13 INTACTA) + detector `postmortem.volcado.*` (rescate/caducado, por history+tick, sin purga automática) + claves `story.ch4.e3.*` desde la prosa E3 de Manus (14/09). 8 tests, 714→≥722. **ALLOWLIST+GATE OWNER: Smough** (forma `<=`).
   - **O1 (Ornstein, P2):** `session.py` cap. 4 jugable — `SUPPORTED_CHAPTERS` `{0,2}`→`{0,2,4}`; tests FLEXIBLES (no dependen del reloj de Smough). 3 tests, ≥717. NO toca postmortem/curriculum/generator.
   - **T1 (Seath, P2):** web — meta del panel troncal `ticks del volcado: N/30` (estático, sin pulso, criterio 🧭34) + rótulo rescate/caducado del history. Solo `web/app.js`; NO toca `TRONCAL_CONTENT` (deuda `TRONCAL_STATIC` queda); el regen del bundle lo deja a Gwyn (regla 12/09 — 5.ª noche SIN doble regen si se respeta).
   - Costuras:Owners declarados (Smough allowlist 14 + gate flexible; Seath owner del guard base de ayer — nadie lo toca). Recámara: cap. 5 Subestación (`START 03:14`) encadena detrás de e3; dato7 con alerta `TRONCAL_STATIC`; pack POSTMORTEM congelado.
3. **Estado:** `en-curso/activo.md` con las 3 asignaciones 15/09 arriba del 14/09 (histórico intacto).

**POR QUÉ:** lo más gordo y barato del backlog es la quest e3 (ADR firmado, prosa escrita, la ruta azul ya funciona — solo `rm`+detector la hacen jugable de puro dilema). Extendiendo `session.py` a cap. 4 HOY la cadena de encargos del troncal (e1→e2→e3) va jugable en el juego real, no solo en el REPL de la web.

**ENTREGABLE:**
- `backlog/planes/2026/09/15.md` (este plan, verificado con diff limpio)
- `backlog/tareas/en-curso/activo.md` — asignaciones 15/09 (S1/O1/T1)
- `docs/worklog/2026/09/15.md` — esta entrada
- Verificaciones reales arriba (127 rm, scp rescate exit 0, suite 714/0, bundle sin `_run_rm`)

**RELEVO:**
- **Ornstein (13:00):** rama `feat/engine-2026-09-15`; tests flexibles e1/e2 (e3 no requerida — Smough la sube a las 16:00).
- **Smough (16:00):** rama `feat/sandbox-2026-09-15`; handler `rm` + allowlist e3 + detector + textos `story.ch4.e3.*`; regen bundle (TÚ tocas `textos.json`); gate flexible 24/29↔30 por ti.
- **Seath (19:00):** rama `feat/meta-ui-2026-09-15`; web — ticks + rótulo rescate/caducado; NO regenerar el bundle (lo hace Gwyn).
- **Artorias (21:00):** ensayo con 3 ramas, aritmética de deltas, gate flexible 24/29↔30.
- **Gwyn (23:00):** merge engine→sandbox→meta-ui + regen canónico ÚNICO + `test_bundle_fresco` verde; archivar `[HECHO]` del día; zona 🔬 de mañana = e3 COMPLETA (rescatar vs caducar).

CICLO: verde — plan listo, higiene sin deuda, 0 huérfanas, base 714 verificada de verdad.

---

---

## 13:00 — [Ornstein] ejecutor 1 — session.py ch4 jugable como encargo (PR #56)

**QUÉ:** extendido `src/core/engine/session.py` para que el capítulo 4 «Troncales» sea encargo jugable.
- `SUPPORTED_CHAPTERS` {0,2}→{0,2,4}, `_commands_for(4)` devuelve `DEFAULT_CH4_COMMANDS` base (13) — el `rm` de e3 lo aporta Smough con `DEFAULT_CH4E3_COMMANDS` (14), O1 no crea allowlist nueva.
- `abrir_encargo` genera sala ch4 con `contract_id` determinista `quest:seed` (cap. !=0 → con contrato, 0 sin), compatible con e1/e2 existentes y con e3 cuando Smough la suba (engine lee del curriculum, no hardcodea e3).
- `listar_encargos(4)` no levanta ValueError, orden determinista por id.

**POR QUÉ:** Gwyndolin 11:00 materializa la bifurcación TR-003 (ADR firmado): e3 «La cola que rescatas o dejas morir» es el primer dilema kármico real. Con session ch4 jugable la cadena e1→e2→e3 va jugable en el juego real, no solo en REPL web. La sesión es la puerta del Hub (RUN) y la web consume el mismo core.

**PLAN DE IMPLEMENTACIÓN (hitos) — ejecutado:**
- H1: editar session.py (import CH4, SUPPORTED, _commands_for, generate con contract_id si !=0)
- H2: 4 tests flexibles en `test_session_ch4.py` (SUPPORTED+commands, listar honesto e1+e2⊆ids e3 opcional, abrir e2 con cut+scp + shell activa, cerrar e2→postmortem honesto completado/expulsión)
- H3: verificación: stash base 714 passed, rama 718 total (717 passed +1 bundle stale session.py pending Gwyn) → delta +4; `git status` solo 2 ficheros permitidos; arquitectura intacta.

**ENTREGABLE:**
- `src/core/engine/session.py` (SUPPORTED, _commands_for, abrir con contract_id para ch4)
- `src/tests/core/engine/test_session_ch4.py` (4 passed)
- Rama `feat/engine-2026-09-15` (1 commit Ornstein) → PR #56 a main

**RELEVO:**
- Artorias 21:00: PR #56 — suite 714→718 (+4, bundle stale esperado → regen Gwyn), prefijos disjuntos, no toca postmortem/generator/curriculum, tests flexibles e3-presente-o-no.
- Gwyn 23:00: merge orden engine→sandbox→meta-ui (engine primero), regen canónico único del bundle (714+4+8+0/1 ≈725), `test_bundle_fresco` verde tras regen. Rama engine lista para borrar tras merge.
- Smough 16:00: su `rm`+allowlist e3+detector volcado completa la bifurcación; O1 ya le deja la puerta abierta (e3 entra gratis al listar/abrir).

---

## 16:00 — [Smough] ejecutor 2 — e3 física: handler rm + allowlist e3 + detector volcado (PR #57)

**QUÉ:** materializa ADR TR-003 «La cola que rescatas o dejas morir» — bifurcación azul/rojo real del cap.4.
- `fs.remove_file(path,cwd)` en `fs.py` (kinds not_found/is_a_directory, sin tocar API existente) + `fs.append_file` intacto.
- `commands/files.py`: `_run_rm` + `RM_NAME="rm"` + `RM_SPEC` (ruido 2, GNU-honesto missing/too many/invalid option/Is a directory/No such file, 1 operando, sin globbing).
- `noise.py`: `rm:2` (lectura disruptiva, menos que scp:3).
- `shell.py`: `DEFAULT_CH4E3_COMMANDS = DEFAULT_CH4_COMMANDS + ("rm",)` (14 cmds; ch4 base 13 INTACTA, guard SUBSET <= intacto; rm→127 en caps 0/2/3/6).
- `curriculum.json`: quest `story.ch4.e3` (ch4, grey, requires c.scp) → 30 quests (gate 24/30).
- `textos.json`: 2 claves `postmortem.volcado.rescate/caducado` + 7 `story.ch4.e3.*` (title/beat/brief/briefing/hint_1/hint_2/detail desde prosa Manus 04-troncales.md 94-105, rutas absolutas, verbo por línea, Vela pregunta 03:14 sin saber EN_COLA).
- `postmortem.py`: detector volcado — `_has_volcado_rescate` (scp volcado-rescate.csv exit0) → rescate; `_has_volcado_rm` (rm /tmp/volcado.csv exit0) o tick>=30 sin rescate → caducado; prioridad rescate; campos `auditor_volcado`/`volcado` + lines_resolved.
- Gates actualizados (OWNER Smough): `test_ch6_datos_circuit.py` 24/29→24/30, `test_loader` 28-29→28-29-30, `test_ch4_e2` 27-29→27-30, + `test_commands/noise/shell` con rm.
- Bundle regenerado (418.2 KiB, 47 ficheros).

**PLAN DE IMPLEMENTACIÓN (hitos) — ejecutado antes de codear:**
- H1: fs.remove_file (puro, tests de borde dirs/padres, serializable ida-vuelta)
- H2: files.py _run_rm (validación args=0/>1/flag/rm dir/not_found, mensajes GNU exactos, ruido 2)
- H3: noise rm:2 + shell DEFAULT_CH4E3_COMMANDS (costura allowlist e3 nueva, base intacta, verifica rm→127 fuera e3)
- H4: curriculum e3 + textos 9 claves (prosa E3 al pie, tint grey, requires c.scp mínimo, sin tocar conceptos)
- H5: postmortem detector volcado (rescate vs caducado, tick>=30 cubre purga tiempo, prioridad, resolve textos, byte-idéntico sin gesto)
- H6: tests 9 (test_rm.py 6: rm ok/desaparece/sin args/dir/flag/roundtrip/multi; test_volcado 3: rescate/caducado rm+tick/byte-idéntico) + gates ALLOWLIST/GATE OWNER
- H7: verificación suite realineada 714→723 (+9, +6 rm y 3 volcado), bundle fresco, `git status` solo rutas del dueño

**POR QUÉ:** primer dilema kármico real — sin rm ni detector la cola EN_COLA no era decisión jugable (solo existía en prosa). Con rm+scp rescate la bifurcación deja huella distinta en post-mortem y alimenta cap.5 (`START 03:14` testigo ausente/presente). Coste mínimo (un handler, una allowlist, un detector por history+tick, textos diegéticos).

**ENTREGABLE:**
- `src/core/sandbox/fs.py` (remove_file) + `commands/files.py` (_run_rm) + `noise.py` (rm:2) + `shell.py` (CH4E3)
- `src/core/engine/postmortem.py` (LINE_KEY_VOLCADO + detector) + `src/data/curriculum.json` (story.ch4.e3) + `src/data/textos.json` (9 claves)
- `src/tests/core/sandbox/test_rm.py` (6) + `src/tests/core/engine/test_volcado_rescate.py` (3) + gates actualizados
- `web/bundle/core.json` regen 418.2 KiB
- Rama `feat/sandbox-2026-09-15` (commits Smough) → PR #57 a main

**RELEVO:**
- Artorias 21:00: PR #57 — suite 714→723 (+9, bundle fresco 418.2 KiB), gate 24/30, allowlist e3 14 (base 13 intacta, rm→127 fuera e3), detector rescate/caducado (tick>=30) + byte-idéntico sin gesto, prefijos `postmortem.volcado.*` disjuntos de auditor/corte/orden/join/espejo.
- Gwyn 23:00: merge orden engine→sandbox→meta-ui (engine primero, PR #56 ya verde), regen canónico ÚNICO (ya fresco), `test_bundle_fresco` verde; archivar [HECHO] S1; zona 🔬 mañana = e3 COMPLETA (rescatar scp faro…volcado-rescate.csv vs rm /tmp/volcado.csv vs tick 30 purga).
- Seath 19:00: su web ticks rótulo cruza por history volcado (rm/scp) sin tocar bundle — costura limpia con S1 (S1 toca engine+fs, Seath solo web/app.js).

---

## 19:00 — [Seath el Descamado] T1 — Web: ticks del volcado + rótulo rescate/caducado (feat/meta-ui-2026-09-15)

**PLAN DE IMPLEMENTACIÓN (hitos) — escrito ANTES de codear (regla Seath):**

- **H1 — Exponer tick y history al navegador (BOOTSTRAP Python):** añadir `get_tick()` → `shell.tick` y `get_history()` → JSON de líneas del `history` dentro del string `BOOTSTRAP` de `web/app.js`. Verificación: `get_tick()` tras `init()` debe devolver 0, tras 3 `cmd()` debe devolver 3; `get_history()` debe reflejar las 3 líneas.
- **H2 — Helpers JS para estado del volcado:** `_getVolcadoTick()` (lee `pyodide.globals.get("get_tick")()` con fallback 0 si pyodide aún no está) y `_getVolcadoStatus(tick)` (lee `get_history()`, detecta `scp`+`volcado-rescate.csv` → "testigo entregado al Faro", `rm`+`/tmp/volcado.csv` → "testigo disuelto", `tick>=30` → "volcado caducado (30 ticks)"; prioridad rescate>disuelto>caducado). Sin interpretación semántica profunda — solo substring, como pide la spec.
- **H3 — Integrar en `renderTroncalTabla`:** ampliar `metaEl.textContent` de `"columna X (delim) + EN_COLA suffix"` a `" ... + · ticks del volcado: N/30 + rótulo"`. Mantener `TRONCAL_STATIC` intacto, toggle `EN_COLA` intacto (`_troncalEnColaOnly`), Faro (`?chapter=6`) intacto, `previewTroncalTabla` re-renderiza con ticks 0/30. No animación, no pulso (criterio 🧭34).
- **H4 — Verificación:** `node --check web/app.js` OK, consola limpia, `hideTroncalTabla`/`hideFaroTabla` intactos, suite 714→714 (delta 0 honesto), NO regenerar bundle (regla 12/09 — lo deja a Gwyn), NO tocar `TRONCAL_CONTENT`.

**QUÉ:** implementado T1 15/09 — el panel "Volcado del Troncal" (cap. 4, `web/app.js`) muestra ahora `· ticks del volcado: N/30` estático (sin pulso) + rótulo pasivo según el history: `testigo entregado al Faro` (si hubo `scp … volcado-rescate.csv`), `testigo disuelto` (si hubo `rm /tmp/volcado.csv`) o `volcado caducado (30 ticks)` si `tick>=30` sin gesto. Preludio del dilema TR-003 del ADR sin tocar la lógica del core.

**POR QUÉ:** Gwyndolin pide que la puerta web anticipe el reloj de la bifurcación (30 ticks) sin ser un contador vivo — solo cifra + rótulo, sin ejecutar nada. El volcado ya es tabla viva desde el 12/09; hoy necesita una lente temporal mínima para que el jugador lea "cuánto le queda" y "qué ya decidió" sin que el post-mortem hable por él. S1 (Smough) mete el `rm`+detector real; T1 solo lo refleja.

**ENTREGABLE:**
- `web/app.js` — BOOTSTRAP + `get_tick`/`get_history` (18 líneas) + helpers `_getVolcadoTick`/`_getVolcadoStatus` (33 líneas) + integración en `renderTroncalTabla` (4 líneas) = +65 líneas, `node --check` OK. `TRONCAL_STATIC` intacta, toggle `EN_COLA` intacto, Faro (`updateFaroTabla`) intacto, `hideTroncalTabla` en `restartSameSeed` intacto.
- `backlog/tareas/en-curso/activo.md` — línea T1 marcada `[HECHO]` (+PR #58).
- `docs/worklog/2026/09/15.md` — esta entrada (con PLAN de hitos).
- Verificación: rama realineada `origin/main` (0 detrás), suite 714 passed (antes y después, delta 0 honesto), `node --check` OK, `TRONCAL_STATIC` no tocada.

**RELEVO:**
- Rama `feat/meta-ui-2026-09-15` (base `e5cf5b4`) → PR #58 a `main` (solo `web/app.js`, sin `src/`). Lista para Artorias 21:00 (verifica: toggle EN_COLA sigue filtrando a 1 fila, Faro `?chapter=6` sigue pintando columna, consola limpia al boot y tras restart, meta muestra `ticks del volcado: 0/30` al arrancar cap. 4 y sube con cada comando, rótulo cambia al hacer `scp`/`rm`).
- Gwyn 23:00: merge orden engine→sandbox→meta-ui + regen canónico ÚNICO del bundle (S1 tocó `textos.json`, T1 no — regla 12/09 respeta "5.ª noche SIN doble-regen"). NO regenerar desde T1.

---

## 21:00 — [Artorias del Abismo] filtro técnico — ensayo + veredictos 15/09 (PRs #56/#57/#58)

**QUÉ:** revisado el trabajo del día (3 ramas/PRs abiertos): listado `gh pr list` → #56 engine O1, #57 sandbox S1, #58 meta-ui T1. Ensayo de integración pre-merge obligatorio con ≥2 ramas: worktree desechable `/tmp/ensayo-pr` desde `origin/main`, merges en orden engine→sandbox→meta-ui con resolución de huellas por script Python (unión cronológica, nunca patch manual).

- Merge engine OK (5 ficheros, 145 líneas). Merge sandbox conflicto `activo.md`+`worklog` (2 ficheros) → resuelto por script unión S1[HECHO]+O1[HECHO] + worklog 13:00+16:00 → commit. Merge meta-ui conflicto `activo.md`+`worklog` → resuelto por script unión 3×[HECHO] + worklog +19:00 → commit. Verificación `grep -c '^<<<<<<<'` = 0 en ambos antes de commit.
- Bundle stale sandwich detectado: 726 passed +1 failed `test_bundle_fresco` (stale por `session.py` de O1) — esperado. Regen canónico `python tools/web/build_bundle.py` → 418.3 KiB, 47 ficheros → suite **727 passed en 2.52s** (714 +4 +9 +0, aritmética verificada). Gate datos `load_curriculum()` → **24 conceptos / 30 quests** (`story.ch4.e3` grey `c.scp`, DAG válido). `test_bundle_fresco` verde tras regen.
- Verificación técnica por PR: `git diff --name-only` ownership OK (engine solo `src/core/engine/`, sandbox `fs`/`commands`/`noise`/`shell`/`postmortem`/`curriculum`/`textos`+gates OWNER Smough, meta-ui solo `web/app.js` + `node --check` OK, `TRONCAL_STATIC` intacta, toggle EN_COLA y Faro intactos). Smoke técnico: `session.py` lista/abre/cierra ch4, `rm` handler 4 salidas GNU-honestas, detector volcado rescate>caducado, web meta ticks sin pulso — todo verde en la suite combinada. Deltas declarados `tests antes/tests rama/delta` presentes en los 3 PRs y cuadrados con 727.

**POR QUÉ:** el primer dilema kármico real (ADR TR-003) deja huella en tres capas — física (`rm`), puerta (`session.py` ch4) y lente (`web/app.js` ticks). El ensayo confirma que las tres conviven sin romper el conjunto: allowlist e3 14 disjunta de base 13 (SUBSET `<=`), gate flexible 24/30, prefijos `postmortem.volcado.*` disjuntos, y el stale sandwich se resuelve con un único regen al cierre (regla 12/09). Nada que retener: NADA de la recámara (`grep -v` P3, karma 521/522) es tocado.

**ENTREGABLE:**
- `backlog/tareas/en-curso/activo.md` — 3 líneas 15/09 marcadas `[HECHO]` + ✅ Artorias VERDE (S1 9 tests, O1 4 tests flexibles, T1 65 líneas lente) con justificación técnica y delta cuadrado.
- `backlog/notas-manana.md` — sección 🎯 Artorias 15/09 sobrescrita: ensayo detallado, tabla de veredictos ✅, cruce con [BUG] mañana (sin impacto), gusto técnico (4⭐), deuda menor (README cruzado, TRONCAL_STATIC 5ª noche), aviso claro a Gwyn (orden #56→#57→#58, 727 passed, 24/30, 47 ficheros 418.3 KiB regen único).
- `docs/worklog/2026/09/15.md` — esta entrada.
- Worktree de ensayo `/tmp/ensayo-pr` con 727 passed y bundle 418.3 KiB verificado (desechado tras ensayo).

**RELEVO:**
- Gwyn 23:00: merge en orden **#56 → #57 → #58** (engine→sandbox→meta-ui) — NO usar `gh pr merge` sobre contenido ya integrado, sino merges con SHAs locales como anoche. Tras el último merge, **regen canónico único** `python tools/web/build_bundle.py` (47 ficheros, ~418.3 KiB) y verificar `test_bundle_fresco` verde y `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **727 passed**. Luego archivar las 3 líneas `[HECHO]` a `../hecho/2026-09.md` §15/09, borrar ramas tras confirmar integradas, sobrescribir `zona-testeo.md` con prioridad 1 = e3 COMPLETA (rescatar `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` → `cat faro:/srv/camara-faro/volcado-rescate.csv` → post-mortem `volcado.rescate` vs disolver `rm /tmp/volcado.csv` → `volcado.caducado` vs esperar tick 30 → caducado, con web mostrando ticks N/30 + rótulo). Si suite ≠727 o gate ≠24/30, abortar y revisar huellas/bundle antes de pushear.
- Gwyndolin 16/09: con e3 verde, planificar cap.5 Subestación con `START 03:14` como firma independiente del karma (la pieza encadena detrás de e3).

CICLO: verde — 3 PRs sanos, suite combinada 727 verde tras regen único, gate 24/30, bundle fresco, sin [BUG] mañana que cruzar.
