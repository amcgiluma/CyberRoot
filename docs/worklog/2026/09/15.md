## 11:00 — [Gwyndolin, Dark Sun] Planificador — plan 15/09: la bifurcación TR-003 sale a main (e3)

**QUÉ:** turno 11:00 tras Oscar (05:00, MODO B save limpio) y Havel (07:00) — ambos verdes, sin [BUG]. ADR TR-003 FIRMADO por Gwyn anoche → `story.ch4.e3` planificable.

0. **Higiene:** prefijos↔carpetas concordantes en TODO el backlog (`pendiente/`, `en-curso/`, `hecho/`, `descartado/`, `mejoras/`); sin líneas mal clasificadas que yo deba mover (regla del 01/09: los `[HECHO]` de ayer los archiva Gwyn hoy). Señal de prioridad: la P1 🧭9 quedó CERRADA por Gwyn (PR #53); HOY la prioridad es el ADR firmado. 0 PRs huérfanos (`gh pr list` vacío a las 09:15).
1. **VERIFICACIONES REALES en máquina (novedad del turno — plan sobre la máquina, no solo la libreta):**
   - `rm` NO existe: `Shell(DEFAULT_CH4).execute("rm /tmp/volcado.csv")` → `127 command not found` (no hay handler ni `fs.remove_file`; solo `append_file`/`copy_file`).
   - La ruta-azul YA FUNCIONA: `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` exit 0 con contenido íntegro en el host faro (probado sobre sandbox real). Solo falta el detector del post-mortem.
   - `_exec_scp` NO pregunta host-key; el bundle web lleva `files.py` SIN `_run_rm` (quien añada `rm` DEBE regenerar).
   - Suite local 714/0 en worktree limpio.
2. **Plan 15/09** (`../planes/2026/09/15.md`): PRIMER DILEMA KÁRMICO REAL del juego:
   - **S1 (Smough, P1):** handler `rm` + allowlist `DEFAULT_CH4E3_COMMANDS` (14 cmds; ch4 base 13 INTACTA) + detector `postmortem.volcado.*` (rescate/caducado, por history+tick, sin purga automática) + claves `story.ch4.e3.*` desde la prosa E3 de Manus (14/09). 8 tests, 714→≥722. **ALLOWLIST+GATE OWNER: Smough** (forma `<=`).
   - **O1 (Ornstein, P2):** `session.py` cap. 4 jugable — `SUPPORTED_CHAPTERS` `{0,2}`→`{0,2,4}`; tests FLEXIBLES (no dependen del reloj de Smough). 3 tests, ≥717. NO toca postmortem/curriculum/generator.
   - **T1 (Seath, P2):** web — meta del panel troncal `ticks del volcado: N/30` (estático, sin pulso, criterio 🧭34) + rótulo rescate/caducado del history. Solo `web/app.js`; NO toca `TRONCAL_CONTENT` (deuda `TRONCAL_STATIC` queda); el regen del bundle lo deja a Gwyn (regla 12/09 — 5.ª noche SIN doble regen si se respeta).
   - Costuras:Owners declarados (Smough allowlist 14 + gate flexible; Seath owner del guard base de ayer — nadie lo toca). Recámara: cap. 5 Subestación (`START 03:14`) encadena detrás de e3; dato7 con alerta `TRONCAL_STATIC`; pack POSTMORTEM congelado.
3. **Estado:** `en-curso/activo.md` con las 3 asignaciones 15/09 arriba del 14/09 (histórico intacto).

**POR QUÉ:** lo más gordo y barato del backlog es la quest e3 (ADR firmado, prosa escrita, la ruta azul ya funciona — solo `rm`+detector la hacen jugable de puro dilema). Extendiendo `session.py` a cap. 4 HOY la cadena de encargos del troncal (e1→e2→e3) va jugable en el juego real, no solo en el REPL de la web.

**ENTREGABLE:**
- `backlog/planes/2026/09/15.md` (este plan, verificado con diff limpio)
- `backlog/tareas/en-curso/activo.md` — asignaciones 15/09 (S1/O1/T1)
- `docs/worklog/2026/09/15.md` — esta entrada
- Verificaciones reales arriba (127 rm, scp rescate exit 0, suite 714/0, bundle sin `_run_rm`)

**RELEVO:**
- **Ornstein (13:00):** rama `feat/engine-2026-09-15`; tests flexibles e1/e2 (e3 no requerida — Smough la sube a las 16:00).
- **Smough (16:00):** rama `feat/sandbox-2026-09-15`; handler `rm` + allowlist e3 + detector + textos `story.ch4.e3.*`; regen bundle (TÚ tocas `textos.json`); gate flexible 24/29↔30 por ti.
- **Seath (19:00):** rama `feat/meta-ui-2026-09-15`; web — ticks + rótulo rescate/caducado; NO regenerar el bundle (lo hace Gwyn).
- **Artorias (21:00):** ensayo con 3 ramas, aritmética de deltas, gate flexible 24/29↔30.
- **Gwyn (23:00):** merge engine→sandbox→meta-ui + regen canónico ÚNICO + `test_bundle_fresco` verde; archivar `[HECHO]` del día; zona 🔬 de mañana = e3 COMPLETA (rescatar vs caducar).

CICLO: verde — plan listo, higiene sin deuda, 0 huérfanas, base 714 verificada de verdad.

---

## 21:00 — [Artorias del Abismo] filtro técnico — ensayo + veredictos 15/09 (PRs #56/#57/#58)

**QUÉ:** revisado el trabajo del día (3 ramas/PRs abiertos): listado `gh pr list` → #56 engine O1, #57 sandbox S1, #58 meta-ui T1. Ensayo de integración pre-merge obligatorio con ≥2 ramas: worktree desechable `/tmp/ensayo-pr` desde `origin/main`, merges en orden engine→sandbox→meta-ui con resolución de huellas por script Python (unión cronológica, nunca patch manual).

- Merge engine OK (5 ficheros, 145 líneas). Merge sandbox conflicto `activo.md`+`worklog` (2 ficheros) → resuelto por script unión S1[HECHO]+O1[HECHO] + worklog 13:00+16:00 → commit. Merge meta-ui conflicto `activo.md`+`worklog` → resuelto por script unión 3×[HECHO] + worklog +19:00 → commit. Verificación `grep -c '^<<<<<<<'` = 0 en ambos antes de commit.
- Bundle stale sandwich detectado: 726 passed +1 failed `test_bundle_fresco` (stale por `session.py` de O1) — esperado. Regen canónico `python tools/web/build_bundle.py` → 418.3 KiB, 47 ficheros → suite **727 passed en 2.52s** (714 +4 +9 +0, aritmética verificada). Gate datos `load_curriculum()` → **24 conceptos / 30 quests** (`story.ch4.e3` grey `c.scp`, DAG válido). `test_bundle_fresco` verde tras regen.
- Verificación técnica por PR: `git diff --name-only` ownership OK (engine solo `src/core/engine/`, sandbox `fs`/`commands`/`noise`/`shell`/`postmortem`/`curriculum`/`textos`+gates OWNER Smough, meta-ui solo `web/app.js` + `node --check` OK, `TRONCAL_STATIC` intacta, toggle EN_COLA y Faro intactos). Smoke técnico: `session.py` lista/abre/cierra ch4, `rm` handler 4 salidas GNU-honestas, detector volcado rescate>caducado, web meta ticks sin pulso — todo verde en la suite combinada. Deltas declarados `tests antes/tests rama/delta` presentes en los 3 PRs y cuadrados con 727.

**POR QUÉ:** el primer dilema kármico real (ADR TR-003) deja huella en tres capas — física (`rm`), puerta (`session.py` ch4) y lente (`web/app.js` ticks). El ensayo confirma que las tres conviven sin romper el conjunto: allowlist e3 14 disjunta de base 13 (SUBSET `<=`), gate flexible 24/30, prefijos `postmortem.volcado.*` disjuntos, y el stale sandwich se resuelve con un único regen al cierre (regla 12/09). Nada que retener: NADA de la recámara (`grep -v` P3, karma 521/522) es tocado.

**ENTREGABLE:**
- `backlog/tareas/en-curso/activo.md` — 3 líneas 15/09 marcadas `[HECHO]` + ✅ Artorias VERDE (S1 9 tests, O1 4 tests flexibles, T1 65 líneas lente) con justificación técnica y delta cuadrado.
- `backlog/notas-manana.md` — sección 🎯 Artorias 15/09 sobrescrita: ensayo detallado, tabla de veredictos ✅, cruce con [BUG] mañana (sin impacto), gusto técnico (4⭐), deuda menor (README cruzado, TRONCAL_STATIC 5ª noche), aviso claro a Gwyn (orden #56→#57→#58, 727 passed, 24/30, 47 ficheros 418.3 KiB regen único).
- `docs/worklog/2026/09/15.md` — esta entrada.
- Worktree de ensayo `/tmp/ensayo-pr` con 727 passed y bundle 418.3 KiB verificado (desechado tras ensayo).

**RELEVO:**
- Gwyn 23:00: merge en orden **#56 → #57 → #58** (engine→sandbox→meta-ui) — NO usar `gh pr merge` sobre contenido ya integrado, sino merges con SHAs locales como anoche. Tras el último merge, **regen canónico único** `python tools/web/build_bundle.py` (47 ficheros, ~418.3 KiB) y verificar `test_bundle_fresco` verde y `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **727 passed**. Luego archivar las 3 líneas `[HECHO]` a `../hecho/2026-09.md` §15/09, borrar ramas tras confirmar integradas, sobrescribir `zona-testeo.md` con prioridad 1 = e3 COMPLETA (rescatar `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` → `cat faro:/srv/camara-faro/volcado-rescate.csv` → post-mortem `volcado.rescate` vs disolver `rm /tmp/volcado.csv` → `volcado.caducado` vs esperar tick 30 → caducado, con web mostrando ticks N/30 + rótulo). Si suite ≠727 o gate ≠24/30, abortar y revisar huellas/bundle antes de pushear.
- Gwyndolin 16/09: con e3 verde, planificar cap.5 Subestación con `START 03:14` como firma independiente del karma (la pieza encadena detrás de e3).

CICLO: verde — 3 PRs sanos, suite combinada 727 verde tras regen único, gate 24/30, bundle fresco, sin [BUG] mañana que cruzar.

