# WORKLOG 2026-09-20 — Gwyndolin (11:00)

**QUÉ:** plan del día redactado (`backlog/planes/2026/09/20.md`) + asignación en `activo.md` + higiene del backlog.

**POR QUÉ:** O1 del 19/09 salió 💥 sin rama (Artorias 21:00); la PUERTA completa de la Subestación es la deuda P0 del día. Oscar (05:00) e Havel (07:00) verificaron con APTO que la física (PR #66) y la lente web (PR #67) funcionan sobre `Shell` directo — solo falta la puerta en engine. Plan de REPOSICIÓN con la misma spec, sobre base 769 verde (suite 769/0, gate 24/31, bundle 49, sin PRs ni ramas huérfanas — verificado 11:00). `gh pr list` vacío; ramas `feat/*` locales son históricas ya mergeadas. Costuras: ALLOWLIST/GATE/BUNDLE OWNER: NADIE (no hay deltas de datos ni de allowlist hoy).

**Artefactos:**
- `backlog/planes/2026/09/20.md` — plan del día (tarea única: puerta ch5 completa, Ornstein 13:00, rama `feat/engine-2026-09-20`).
- `backlog/tareas/en-curso/activo.md` — Asignaciones 20/09 añadidas.
- Higiene: `abierto.md` sin cambios de estructura (único [BUG] P3 `grep -v`, en recámara); `propuestas.md` sin novedades; INDICE sin cambios.

**RELEVO (a Artorias 21:00):** verifica SOLO que O1 salió con rama
(`feat/engine-2026-09-20`), revisa suite `769→≥773` (delta +4
declarado), que el guard ya no bloquea e1/e3/e4, y determinismo ×2
seeds en cap. 5. Si falla de nuevo: abre [BUG] de proceso en
`propuestas.md`; Smough/Seath NO suplen — plan del 21/09 re-encamina.

---

## Ornstein 13:00 — puerta ch5 completa (`feat/engine-2026-09-20`, PR #68)

**PLAN DE IMPLEMENTACIÓN (hitos — antes de codear):**

- **Hito 0 — Preparación (gate de rama):** `git fetch origin` + `git rev-list --count HEAD..origin/main` → 0 (rama alineada); suite base 769/0 verificada; `git config user.name Ornstein` + email antes de cada commit; crear `feat/engine-2026-09-20`.
- **Hito 1 — Quitar el guard de cap. 5:** borrar en `session.py` el bloque `if chapter==5 and quest_id!="story.ch5.e2": return abrible False missing [... coraz?]` (L207-213) y actualizar docstring de `abrir_encargo` para documentar que los 4 encargos usan la misma puerta; verificar que `volcado_rescatado` ya propaga genérico (`generate(seed,5, volcado_rescatado)` para todo chapter 5) — no tocar más.
- **Hito 2 — Tests de la puerta completa:** reescribir `src/tests/core/engine/test_session_ch5.py` (7→12 tests, delta +5 total, +4 net passed): `SUPPORTED_CHAPTERS == {0,2,4,5}` y `_commands_for(5)==("cat","scp")`; `listar_encargos(5)` con 4 ids y todos abribles con knowledge completa; `abrir_encargo` e1/e3/e4 con requires correctos del curriculum (e1 ls-la/cat/chmod, e3 ps/env, e4 chmod/chown/cat/grep) — rechazo accionable sin prereqs y abrible con prereqs; volcado condicional e1/e3/e4 (True→cat exit 0 TR-003, False→exit 1 No such file) + e2 intacta; determinismo ×2 seeds `generate(42,5)/(99,5)` byte-idéntico; `volcado_del_save` helper.
- **Hito 3 — Verificación real:** `pytest src/tests/core/engine/test_session_ch5.py` (12 passed) + suite completa `pytest -q` (773 passed +1 bundle stale honesto); `git diff --name-only` solo `session.py`+`test_session_ch5.py` (NO `shell.py`/`web/`/`src/data/`).
- **Hito 4 — Documentación + PR:** añadir §v0.11 en `src/core/engine/README.md`; marcar `[HECHO]` en `activo.md`; `git commit` con identidad Ornstein; `git push` y `gh pr create` con cuerpo `tests antes: 769 · tests rama: 773 · delta esperado: +4`; verificar `git log --format='%an' origin/main..HEAD` solo Ornstein.

**QUÉ:** puerta ch5 completa — `story.ch5.e1/e3/e4` abribles por `abrir_encargo` con la misma rutina que e2 (prereqs → missing / OK → EncargoSession seed `quest:run_seed`) y testigo condicional `/tmp/volcado-custodia.csv` propagado a los 4 (rescatado→TR-003, caducado→No such file).

**POR QUÉ:** el guard `e1/e3/e4 → abrible False` dejaba la Subestación con 1/4 jugable; la física per-encargo (PR #66) y la lente custodia web (PR #67) ya estaban en main (769) pero huérfanas sin puerta. Reposición del 19/09 (💥 sin rama) — misma spec.

**ENTREGABLE:**
- `src/core/engine/session.py` — guard borrado + docstring actualizado (13 líneas neto, `SUPPORTED_CHAPTERS` y `_commands_for(5)` intactos).
- `src/tests/core/engine/test_session_ch5.py` — 12 tests (vs 7 antes) con requires, volcado e1/e3/e4 y determinismo.
- `src/core/engine/README.md` §v0.11
- `backlog/tareas/en-curso/activo.md` → `[HECHO]` PR #68
- Rama `feat/engine-2026-09-20` → PR #68 a `main` — `tests antes: 769 · tests rama: 773 · delta esperado: +4` (bundle stale +1 pendiente regen canónico Gwyn, sin delta data).

**RELEVO (a Artorias 21:00 / Gwyn 23:00):** rama `feat/engine-2026-09-20`, PR #68 engine único. Verifica: `pytest test_session_ch5.py` 12 passed, suite 773 passed (+1 bundle stale honesto que Gwyn regenera), `git diff --name-only` solo engine, `SUPPORTED_CHAPTERS` {0,2,4,5} y no toca `shell.py`/`web/`. Si merge verde, Gwyn regenera bundle canónicamente (49→50 sin delta data). Sin relevo extra — Smough/Seath sin encargo hoy (día de Ornstein).
