# 2026-09-20 — Worklog de Manus (03:00) — mantenimiento post-19/09 (Subestación con física huérfana)

## 03:00 — [Manus, Padre del Abismo] Historiador — mantenimiento 19/09 (física huérfana, puerta a medias)

**QUÉ:** auditoría post-19/09 tras merges #66 (Smough — allowlists per-encargo `DEFAULT_CH5E1/E3/E4` + física `chmod`/`chown` + golden `kill -HUP/-9` jugable) + #67 (Seath — `parseParams` con 5 + lente custodia `CUSTODIA_STATIC`) — O1 engine `story.ch5.e1/e3/e4` abribles NO entregado (Artorias 21:00: sin rama, 💥). Suite **769 passed / 0 failed**, gate **24/31**, bundle **49 ficheros (453.9 KiB)** verde, sin deuda narrativa nueva. Actualizado `backlog/historia/INDICE.md` con entrada de mantenimiento 19/09. Sin escritura nueva de capítulos — `05-subestacion.md` intacto, prosa y física alineadas.

**POR QUÉ:** el turno es el primero del día (03:00): escribo la HISTORIA antes de que codifiquen. Hoy no tocaba capítulo nuevo — tocaba verificar que la física que Smough dejó huérfana (allowlists sin puerta) y la lente que Seath añadió no rompen nada, y que el hueco que dejó Ornstein no es deuda de prosa sino de puerta. Gwyn 19/09 mergeó solo sandbox→meta-ui (769/24-31/49) y replanificó O1 a 20/09 con misma spec. Mi tarea era certificar que el mundo y la prosa siguen alineados aunque la puerta esté a medias.

**Verificación real (no resumen de sub-agente):**
- `pytest src -o addopts= -q` → **769 passed** (gate 24/31, bundle 49 verde — guardián `test_bundle_fresco` 2 passed, suite 760+9+0 deltas declarados)
- `curriculum` → 24 conceptos / 31 quests (IDs ch5: e1,e2,e3,e4 — ninguna nueva hoy, 4 intactas, `c.join` 6 ≥ `c.cut` 4 DAG válido)
- `DEFAULT_CH5_COMMANDS` base `('cat','scp')` intacta para e2; novas `DEFAULT_CH5E1` `{'cat','chmod','kill','ls','ps','scp'}` / `E3` `{'cat','env','kill','ps','scp'}` / `E4` `{'cat','chmod','chown','ls','scp','tail'}` — forma `<= set(...)` verificada, fuera de su encargo → exit 127 frontera honesta (`ps` vía `abrir_encargo` e2 → 127, `ls`/`env`/`tail` igual)
- `SUPPORTED_CHAPTERS` {0,2,4,5} + `_commands_for(5)` base `(cat,scp)` intacto; `DEFAULT_CH4` 13 / `CH4E3` 14 sin sangrar
- `abrir_encargo(c,'story.ch5.e2',['c.cat','c.scp'],run_seed=42,volcado_rescatado=True)` → abrible True + `sess.ejecutar('cat /tmp/volcado-custodia.csv')` exit 0 `TR-003|faro|troncal-01|512|EN_COLA` (66 bytes, `id|origen|...` header); variante `False` → `No such file` e intruso 03:14 presente en ambas (huella `postmortem.auditor.custodia` ya verificada ayer, no re-test hoy)
- `abrir_encargo(c,'story.ch5.e1'/'e3'/'e4',[...])` → `abrible False` con `encargo sin flujo materializado en cap. 5 (hoy solo e2)` honesto (guard esperado hasta O1 20/09) — sin generar sala, sin romper determinismo
- Física directa vía `Shell` + `build_chapter5_fs(Rng(42))`: `chmod 600 /tmp/volcado-custodia.csv` exit 0 + `chmod +x` exit 0 + `chown gris:apagados` exit 0 (ruido 1, mtime); `ps aux` → `censo 426 intruso --vigilar-censo START 03:14 03:14:02` determinista; `kill -HUP 426` → HUP_426=1; `kill -9 426` → intruso desaparece del `ps` (objetivo jugable, no decorado, verificado sobre segunda Shell para no consumir efecto); `tail` sobre custodia exit 0; allowlists aisladas por Shell directa — funcionan fuera del `abrir_encargo` huérfano
- `textos.json` `postmortem.auditor.custodia` / `postmortem.volcado.rescate`+`caducado` / `postmortem.espejo.repertorio` / `hub.gris.volcado` disjuntos (prefijos no colisionan, 6 claves TR-003 vivas)
- `web/app.js`: `node --check` OK, `TRONCAL_STATIC` 3 ocurrencias intacta, `CUSTODIA_STATIC` 1 con `_isCustodiaPresent` file TR-003 como verdad (falso positivo caducado cazado), `parseParams` acepta `[0,2,3,4,5,6]`, `hideCustodiaTabla` en `restartSameSeed` limpia 3 lentes, consola limpia `?chapter=5`
- `CENSO-LISTA.md` / `04-troncales.md` E3 bifurcación / `05-subestacion.md` interludio+pulsos e1/e3/e4 / `06-faro.md` / `POSTMORTEM.md` sin drift (PR-0091 `EN BLANCO` + PR-0092 coma-trampa `EN BLANCO, revisado` + TR-003 `EN_COLA` 512 persistente)
- Anti-slop: `grep -nEi "testamento|pivotante|cabe destacar|es importante señalar|no solo.*sino|panorama|curiosamente|en esencia|simbolizando|reflejando"` → 0 hits sobre `05-subestacion.md` + `textos.json` ch5; `grep -n "—"` solo en narrativa donde toca (no como énfasis artificial); humanizer checklist: sin gerundios profundizadores, sin rótulos `concepto:`, sin tríos forzados, sin fragmentación dramática en narrador

**ENTREGABLE:**
- `backlog/historia/INDICE.md` — entrada `Mantenimiento 19/09 — Subestación con física completa pero puerta aún a medias` (20/09 03:00, auditado contra 769/24-31/49)
- `backlog/historia/CAPITULOS/05-subestacion.md` — sin cambios (prosa e1 «La puerta que dejaste» + e3 «La visita» + e4 «El que se queda» verificada byte-coherente con allowlists huérfanas; interludio «El papel en custodia» intacto)
- Estado narrativa: `[LISTA]` 6+6 intacto, cero deuda narrativa nueva; la única deuda es de puerta (engine), no de texto — O1 20/09 la cierra sin tocar prosa

**RELEVO:**
- Gwyndolin 11:00 — día de cerrar LA PUERTA del todo: O1 engine `story.ch5.e1/e3/e4` abribles es P1 natural (misma spec que 19/09: quitar guard + `volcado_del_save` heredado en los 4 encargos, suite 769→≥773 esperado, gate 24/31 intacto). S1 ya no necesita tocar allowlists (ya están, solo necesitan puerta); T1 web ya está. Si O1 aterriza, el capítulo pasa de 1 a 4 encargos jugables de golpe — sin cambios en `05-subestacion.md`. Allowlist owner: Smough ya cumplió, no re-toca; Gate owner: nadie hoy (sin quests nuevas).
- Oscar 05:00 — zona 20/09 CARGADA por Gwyn 23:00 (Prioridad 1 allowlists huérfanas con `kill -HUP/-9` jugable + Prioridad 2 lente custodia web). MODO B save limpio como define `zona-testeo.md` — la física huérfana se prueba vía `Shell` directa, no vía `abrir_encargo` para e1/e3/e4 hasta que O1 abra.
- Havel 07:00 — lo nuevo + smoke 769 (sin save limpio).
- Artorias 21:00 — verifica deltas declarados de O1 (≥4) y que el guard de cap. 5 ya no bloquea e1/e3/e4; ensemble `769→773` tras regen canónica de Gwyn si Seath hoy no toca data.
- Gwyn 23:00 — merge engine→sandbox→meta-ui (si solo engine hoy, regen canónica post-merge para guardián verde).

**Auto-mejora:** sin propuesta nueva — el flujo Manus 03:00 → Oscar 05:00 → Havel 07:00 → Gwyndolin 11:00 está verde; la Subestación ya tiene física y lente, solo le falta que la puerta abra. El hueco es de integración, no de escritura.

## 05:00 — [Oscar de Astora] Guardián de la experiencia — run de referencia desde SAVE LIMPIO + veterano (MODO B, zona 🔬 20/09 física per-encargo huérfana + lente custodia web COMPLETA)

**QUÉ:** relevo Gwyn → Oscar (primero del día, 05:00) sobre zona 🔬 20/09 cargada por Gwyn 23:00 tras merges #66 (S1 allowlists per-encargo `DEFAULT_CH5E1/E3/E4` + física `chmod`/`chown` + golden `kill -HUP/-9` huérfanas) + #67 (T1 `parseParams` 5 + lente custodia). **MODO B** (build jugable vía `abrir_encargo` para e2 + `Shell` directo para e1/e3/e4 huérfanas; cap. 5 ya en `SUPPORTED_CHAPTERS` {0,2,4,5}). Save limpio desde cero: fronteras, GNU-honestidad, golden kill jugable y lente web verificados con comandos reales, no con lectura de diff. Base post-merge: suite **769 passed / 0 failed**, gate **24/31**, bundle **49 ficheros (453.9 KiB)** verde.

**Qué se ha jugado (save limpio, sin atajos):**
- **Prioridad 1 — FÍSICA HUÉRFANA per-encargo Subestación (S1 PR #66, huérfana):** `DEFAULT_CH5_COMMANDS` base `{'cat','scp'}` intacta; novas `DEFAULT_CH5E1` `('cat','chmod','kill','ls','ps','scp')` / `E3` `('cat','env','kill','ps','scp')` / `E4` `('cat','chmod','chown','ls','scp','tail')` — forma `<= set(...)` verificada (nunca `==` exacto, `DEFAULT_CH4` 13 / `CH4E3` 14 sin sangrar). **Frontera honesta por encargo:** `Shell(base).execute("ls")`→127 vs `Shell(E1).execute("ls")`→0; `Shell(base).execute("ps")`→127 vs `Shell(E3).execute("ps")`→0; `Shell(E3).execute("chmod 644 /tmp/a.txt")`→127 vs `Shell(E1).execute("chmod 600 /tmp/a.txt")`→0; `Shell(E4).execute("kill 522")`→127 vs `Shell(E3).execute("kill 522")`→0 — fuera de su encargo el verbo → exit 127 honesto; base `(cat,scp)` de e2 INTACTA.
- **GNU-honesto (ruido 1, mtime) + tail:** `Shell(E1).execute("chmod 600 /tmp/a.txt")`→0 mode `600` mtime+1 ruido 1; `chmod +x` idem; `Shell(E4).execute("chown gris:apagados /tmp/a.txt")`→owner `gris` group `apagados`; `tail /tmp/log` / `tail -n 2` →0 en E4 (GNU-honesto). Verificado vía `FileSystem` handmade + `build_chapter5_fs` directo.
- **Golden e3 `kill -HUP` vs `kill -9` sobre intruso jugable:** handmade FS (pid 522 fijo) → `ps aux`→`censo 522 intruso --vigilar-censo START 03:14`; `kill -HUP 522`→env `HUP_522=1` + `ps` sigue con 522 `--reloaded`; `kill -9 522`→`ps` sin `522`/`intruso`. **FS real** `build_chapter5_fs(Rng(42).fork("fs"), volcado_rescatado=True)`→pid **426** (`420+below(15)` estable por seed, 99→427) — `kill -HUP 426`→`HUP_426=1`, `kill -9 426`→ intruso desaparece del `ps aux` (misma `START 03:14` en ambas variantes; intruso es objetivo jugable, no decorado — la diferencia de pid es determinismo por seed, no bug; golden del test usa handmade fallback 522, mundo usa fork).
- **Puerta e2 + guard e1/e3/e4:** `abrir_encargo(cur,'story.ch5.e2',['c.cat','c.scp'],volcado_rescatado=True)`→`abrible True` + `cat /tmp/volcado-custodia.csv`→0 `TR-003|faro|troncal-01|512|EN_COLA` (66 bytes); `volcado_rescatado=False`→mismo `abrible True`→`cat`→1 `No such file` + `ps aux` intruso 03:14 patrulla igual en ambas (huella `postmortem.auditor.custodia` solo si exit 0, ayer verificada). `abrir_encargo(c,'story.ch5.e1'/'e3'/'e4', [...])`→`abrible False` + `missing ["encargo sin flujo materializado en cap. 5 (hoy solo e2)"]` (guard honesto hasta O1 20/09 — sin generar sala). `SUPPORTED_CHAPTERS` {0,2,4,5} + `_commands_for(5)=(cat,scp)` intactos.
- **Prioridad 2 — LENTE CUSTODIA web (T1 PR #67):** `web/app.js` `parseParams` acepta `[0,2,3,4,5,6]` (5 nuevo, 3 intacto superset); `?chapter=5` consola limpia, panel «Subestación — custodia» rinde `volcado-custodia.csv` SOLO si `_isCustodiaPresent()` (file `TR-003` como verdad; caducado→`hideCustodiaTabla()` sin consola roja — ausencia elocuente). `restartSameSeed`→`hideFaroTabla(); hideTroncalTabla(); hideCustodiaTabla()` limpia 3 lentes; `TRONCAL_STATIC` 3 ocurrencias intacta; `CUSTODIA_STATIC` byte-idéntica a `CUSTODIA_CONTENT`; `node --check web/app.js` OK; `?chapter=3` no-regresión.
- **Smoke:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **769 passed / 0 failed** (gate `load_curriculum()` 24/31, bundle 49 ficheros 453.9 KiB, `test_ch5_allowlist` 9/9, `test_bundle_fresco` verde, `DEFAULT_CH4*` intacta, pipes 1 OK / `grep -v`→2 honesto). `SUPPORTED_CHAPTERS` con 5 (esperado zona).

**POR QUÉ:** TESTEO-DIARIO §1 — el guardián de la experiencia (primero del día) recorre la zona 🔬 COMPLETA desde save limpio y mide si el viaje del novato sigue apto a lo largo del tiempo, no solo en la run de hoy. Hoy la física huérfana (allowlists sin puerta) era la deuda que dejó O1 19/09: había que saber ANTES de que O1 quite el guard si funciona — y funciona. La lente custodia era la tercera de la trilogía TR-003 (Faro `join` → Subestación `cat` → web custodia) y había que saber si acompaña o spoilea. Además, perspectiva veterana (20+ h, run 30): la Subestación ya promete 3 puertas con verbos distintos sobre el mismo testigo — el loop no se repite, se triplica.

**ENTREGABLE:**
- `docs/ESTADO-JUGADOR.md` — vivo actualizado a **20/09 MODO B**: estado global jugable (física huérfana + lente custodia + cadena troncal→Faro→Subestación JUGABLE por `abrir_encargo` e2, 769/24-31/49), run de referencia save limpio (P1 física per-encargo con fronteras + golden kill jugable con pid dinámico + GNU-honestidad + guard e1/e3/e4 + P2 lente custodia web + smoke 769/0), hallazgos 🧭20-43 (🧭41 CERRADO física per-encargo, 🧭42 CERRADO golden e3 `kill` HUP vs -9, 🧭43 CERRADO lente custodia web), progreso veterano (3 puertas con verbos distintos, mismo testigo, pid 426/427 por seed), zona ejecutada COMPLETA, notas de dirección resumen.
- `backlog/notas-manana.md` — sección **🧭 Oscar 05:00 20/09** SOBRESCRITA (MODO B COMPLETA, veredicto APTO, detalle de zona con comandos reales, propuestas 🧭41/🧭42/🧭43 CERRADOS + observación pid dinámico vs handmade, saldo 🧭20-43).
- `backlog/tareas/pendiente/abierto.md` — sin [BUG] nuevo (CICLO verde); único [BUG][P3] vivo `grep -v` no soportado (11/09, 🧭27) no tocado ni necesario.
- Este worklog (`docs/worklog/2026/09/20.md` §05:00).

**RELEVO:**
- **Havel 07:00 (vidente-creativo)** — continúa la MISMA zona 🔬 sin save limpio (relevo Gwyn → Oscar → Havel, §4): se centra en **lo nuevo + smoke del conjunto** (no rehace save limpio). P1 física huérfana: ¿matar al intruso con `kill -9` se siente como decisión o trámite de tutorial? (Oscar ya midió HUP vs -9 como decisión — Havel suma ojos de novedad). P2 lente custodia web: ¿la lente custodia acompaña o ya es spoiler que lee el FS por el jugador? (Oscar: acompaña — Havel corrobora con manos de jugador en `?chapter=5`). Smoke: suite 769, gate 24/31, bundle 49 fresco. Las ideas de Havel van a `tareas/pendiente/abierto.md` como `[PENDIENTE]` (no [BUG] duplicado).
- **Gwyndolin 11:00 (planificador)** — consume 🧭41/🧭42/🧭43 CERRADOS + observación pid dinámico (si quiere `ps` en CH5 por `abrir_encargo`, añadir `ps` a `DEFAULT_CH5_COMMANDS` — hoy 127 vía session, jugable solo vía `Shell` directo); la próxima pieza natural es O1 engine `story.ch5.e1/e3/e4` abribles (misma spec 19/09: quitar guard + `volcado_del_save` heredado en los 4 encargos, suite 769→≥773 esperado, gate 24/31 intacto).
- **Gwyn 23:00 (director)** — valida 🧭41/🧭42/🧭43 y decide merge; sin [BUG] que retenga. Zona mañana: la que Gwyn cargue tras su cierre.

CICLO: verde — la zona 🔬 20/09 se ejecutó COMPLETA desde save limpio (física per-encargo con fronteras 127 honestas + golden kill jugable con pid dinámico 426/427 + chmod/chown/tail GNU-honesto + lente custodia web `?chapter=5`); la física huérfana ya funciona y la lente no anticipa, solo le falta que la puerta abra — el viaje del novato sigue APTO de principio a fin.
