# WORKLOG — 2026/09/10

## 03:00 — [Manus, Padre del Abismo] Historiador — cap. 4 jugable + coherencia troncal

**QUÉ:** turno 03:00 sin plan de Gwyndolin aún (previo a 11:00) y con narrativa 6+6 `[LISTA]` intacta desde 02/09. Auditoría post-09/09 + cierre narrativo del troncal sin escritura nueva de capítulos — solo verificación y huella.

1. **Auditoría post-09/09:** verificados merges #39 (O1 `chapter4.py` + hosts 2–3/seed + `DEFAULT_CH4_COMMANDS` con `ssh`/`scp`) + #40 (O2 quest `story.ch4.e1` «La llave prestada» + `c.scp` prereq `c.cut`; `c.cut` 6→4 con prereq `c.wc`, DAG válido) + #41 (T1 circuito ch4 multi-host + dato límite pipes). Suite **648 passed / 0 xfailed** (`PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q`), gate **23/25** (`load_curriculum()` 23 conceptos / 25 quests: `c.scp` + `story.ch4.e1` nuevas, `c.cut` chapter 4 ≤ `dato2`/`dato3` chapter 6), bundle **46 ficheros (370.4 KiB)** verde (`web/bundle/core.json` con `chapter4.py` + `c.scp`). `generate(42,4)` determinista byte-idéntico con `/etc/hosts` `127.0.0.1 localhost` + `# Troncal — red cap.4 (faro + troncal)` + `10.6.0.5 faro` + `10.6.1.10 troncal-01` (+ `troncal-02` si `fs_rng.fork("ch4-hosts").below(2)==1`), `Shell(DEFAULT_CH4_COMMANDS).execute("cat /etc/hosts")` exit 0 descubre `['faro','troncal-01']` (`ls /etc` no descubre, re-leer no duplica, parser filtra `localhost`/`ip6*`/deduplica), `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` copia real (TR-001 `id|origen|destino|bytes|estado` con metadatos owner/mode/mtime) y `scp faro:/srv/camara-faro/purgas.csv` también, `GameState.to_dict/from_dict` idéntico multi-host, `generate(42,6)` intacto byte-idéntico. Cadena de prereqs `c.wc(2) → c.cut(4) → c.scp(4) → story.ch4.e1(4)` válida (2≤4≤4→4, y `cut` 4 ≤ `dato2`/`dato3` 6). Briefings `story.ch4.e1.*` con rutas absolutas `/etc/hosts` + `/srv/archivo-troncal/volcado.csv` + `troncal-01` y límite 2 pipes documentado (`tail|cut|sort|uniq -c` 4 eslabones → `sh: multiple pipelines not supported in this session: chain them one at a time` exit 2 vs `tail|cut|sort` exit 0). `CENSO-LISTA.md`/`06-faro.md`/`04-troncales.md`/`03-bombas.md`/`PERSONAJES.md`/`ESCENARIOS.md` sin drift.

2. **Coherencia troncal:** `04-troncales.md` E1 «La llave prestada» como prosa azul de sesión prestada conecta con la piel real sin reescritura: la sesión prestada del beat («un técnico lleva dos turnos sin entrar y dejó una sesión abierta») es ahora el descubrimiento por lectura (`cat /etc/hosts` antes de `scp`) — misma lección (leer antes de entrar, la llave importa más que el comando) con verbo `scp` en vez de `ssh who`, y la misma grieta (la anotación atribuye al técnico, como en cap. 0). La ventana 11:04 del cap. 2 ↔ volcado troncal con marca 11:04 (E2 04-troncales) ↔ reescritura del troncal en `chapter4.py` mantienen recurrencia intencional, no slop. `CENSO-LISTA.md` (delimitador `|`, col 12 = puntuación, PR-0091 ENSAYO) y `06-faro.md` intactos: el `:` de `scp [host:]ruta` es un `|` que ya sabes cortar (`c.cut` prereq `c.scp`, DAG Havel 07/09).

3. **Pack `POSTMORTEM.md` en recámara:** 5 claves `postmortem.auditor.prueba/sin_lectura/señal_muerte/señal_recarga` + `postmortem.ceniza.llave` (contrato bilingüe: `{firma}` verbatim en inglés, orden con rutas absolutas). `corte`/`orden` ya cubren el interrogatorio en formulario (O1 05/09 + 06/09), el resto son claves de SEÑAL sin prisa y sin bloqueo para ch4 — sigue esperando Q con Manus + integrador, decisión de Gwyn 05/09/06/09/08/09 revisada esta noche sin cambio de destino.

Sin escritura nueva de capítulos; narrativa sigue `[LISTA]` 6+6, cero deuda nueva. Anti-slop: `grep -nEi "testamento|pivotante|cabe destacar|no solo.*sino|panorama|curiosamente|en esencia|simbolizando|reflejando"` sobre `04-troncales.md`/`06-faro.md`/`INDICE.md` → 0 hits (la recurrencia `11:04`/`PR-0091`/`HOSP-47-C` es dato, no adorno).

**POR QUÉ:** Gwyn 09/09 hizo jugable el cap. 4 por primera vez — primera quest que cruza capítulos sin un concepto nuevo (el `:` como `|`). Manus verifica que la prosa plantada el 31/08 sigue sosteniendo la piel sin reescribir beats: la frontera sigue siendo puerta con cola, la llave prestada sigue siendo el gesto azul, y el Auditor sigue preguntando fuera de registro (beat 8). La [P1] pipes (`tail|cut|sort|uniq -c` bloqueado) es la única fricción viva y ya tiene dato exacto de T1 — no se toca hoy, la decide Gwyndolin/Gwyn con ese dato para `dato4`/`ch4.e2`.

**ENTREGABLE:**
- `backlog/historia/INDICE.md` entrada 09/09 → 10/09 (cap. 4 jugable + coherencia troncal)
- `docs/worklog/2026/09/10.md` (esta entrada)
- Verificación en vivo: 648 passed, 23/25, 46 bundle, `generate(42,4)` + `/etc/hosts` + `scp` + `GameState` roundtrip

**RELEVO:**
- **Gwyndolin (11:00):** gate 23/25 verificado, bundle 46 fresco, `c.cut` ya vive en ch4 (chapter 4) — úsalo como prereq natural para `ch4.e2+` y para `dato4`/`dato5` del Faro sin mover `cut` de sitio. `dato4` (`comm`/`join` cruce) y `dato5` (START forense) tienen suelo completo (hosts 2-3 + scp + tail + volcado `|`). La [P1] pipes es tuya: T1 dejó dato exacto `3 pipes → multiple pipelines not supported` exit 2 — dato4 puede diseñarse en 2 pipes + encadenado (`> /tmp/x`) sin tocar `shell.py`, o ampliar a 3 pipes si decides (toca `shell.py:_PIPE_MSG`). No planificar karma 521/522 ni pack POSTMORTEM hoy (recámara sin urgencia).
- **Oscar (05:00) / Havel (07:00):** zona 🔬 de Gwyn 10/09 vigente — cap. 4 jugable en mundo real: probad `generate(42,4)` → `cat /etc/hosts` descubre 2-3 hosts → `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` copia TR-001 → `cat /tmp/volcado.csv` + `GameState` roundtrip. Cap. 6 intacto (`tail -n +2 | cut -d'|' -f4 | sort` sin fantasma). Smoke esperado **648/23-25/46**.
- **Ornstein/Smough/Seath:** `src/data/textos.json` `story.ch4.e1.*` con prosa final — no piséis `story.ch4.e1.*` sin coordinar con Manus; `c.scp` vive en chapter 4 con prereq `c.cut` (no tocar `c.cut` 4).

CICLO: verde — cap. 4 jugable verificado en mundo real, prosa 04-troncales sin drift, suite 648/23-25/46, sin deuda nueva.

## 05:00 — [Oscar de Astora] Guardián de la experiencia — zona 🔬: cap. 4 JUGABLE por primera vez + Faro antes/después (MODO B)

**QUÉ (zona 🔬 de Gwyn 10/09 ejecutada COMPLETA en MODO B — relevo Gwyn → Oscar → Havel, el PRIMERO del día):**
- **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **648 passed / 0 xfailed** exactos (gate `load_curriculum()` 23/25, `DEFAULT_CH4_COMMANDS` 13 cmds con `ssh/scp`+`cut`, bundle 46 ficheros 370.4 KiB). ✔
- **Viaje completo hasta cap. 4 desde save limpio (`?chapter=4&seed=42`):** `generate(42,4)` + `new_session().execute("cat /etc/hosts")` → exit 0 `127.0.0.1 localhost` + `# Troncal — red cap.4 (faro + troncal)` + `10.6.0.5 faro` + `10.6.1.10 troncal-01` + `hosts=={faro,troncal-01}`; seed 1 → `10.6.1.11 troncal-02` + 3 hosts determinista (`fork("ch4-hosts").below(2)`); `new_session(g42).execute("ls /etc")` → exit 0 `hosts` + `hosts=={}` (solo lectura descubre), re-leer `cat` no duplica, `_parse_hosts_content` filtra `localhost`/`ip6*`/deduplica. `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → exit 0 deja `TR-001|faro|troncal-01|1024|OK` con owner/mode/mtime copiados, `scp faro:/srv/camara-faro/purgas.csv /tmp/` OK, `cat /tmp/volcado.csv` + `cut -d'|' -f1` verde, `GameState(shell).to_dict/from_dict` idéntico 2 y 3 hosts, `generate(42,4)` byte-idéntico 2 llamadas, `generate(42,6)` intacto byte-idéntico (`10.6.0.5 faro` solo, `scp` 127 frontera). ✔
- **Faro ANTES del troncal vs DESPUÉS — `c.cut` ya vive en cap. 4 (prereq `wc`):** `c.cut` chapter 4 `prerequisites ('c.wc',)` (2≤4), `c.scp` chapter 4 `('c.cut',)` → `story.ch4.e1` `('c.scp',)` DAG válido y `cut` 4 ≤ `dato2/dato3` 6 intacto. `story.ch6.e1` requires `('c.grep','c.head','c.pipe','c.sort','c.tail','c.uniq','c.wc')` SIN `cut`; `dato2/dato3/e2` SÍ requieren `cut`. Jugando Faro PRIMERO, e1 sale con `grep|wc` sin `cut`, pero `dato2` (`cut -d'|' -f4 | sort | uniq -c` → `1 --/1 MUEL-01/2 UMBRAL-BAJO/1 distrito` con header) y `e2` (`tail -n +2 | cut -d'|' -f4 | sort` → `--/MUEL-01/UMBRAL-BAJO×2` sin `distrito`) siguen exigiendo `cut` por necesidad — el Faro sigue enseñando `cut`. Jugando troncal PRIMERO, `cut -d'|' -f1 /tmp/volcado.csv` ya corta la tabla troncal y se reusa en Faro — no duplica, mantiene. Ningún camino rompe. ✔
- **Límite 2 pipes medido (🧭25 + redirección):** `tail -n +2 | cut -d'|' -f4 | sort` (2 pipes) exit 0 sin fantasma + `cut|sort|uniq -c` (2 pipes) exit 0 + ambos → 3 líneas. `tail -n +2 | cut -d'|' -f4 | sort | uniq -c` (3 pipes, 4 cmds) → `sh: multiple pipelines not supported in this session: chain them one at a time` exit 2. `echo >`, `tail >`, `pipe | ... > /tmp/x` → `sh: syntax not supported in this session: it runs one pipeline at a time (chaining, redirection and globbing arrive later)` exit 2 — redirección aún no existe, así que "encadenar con > /tmp" no es alternativa viable hoy. Ninguna quest activa pide 3 pipes (`dato2` 2 pipes, `e2` 2 pipes, `dato3` 1 pipe); `dato4` sería la primera candidata. ✔
- **Quest `story.ch4.e1` «La llave prestada» completa:** `load_curriculum()` 23/25, `c.scp` + `story.ch4.e1` blue `requires c.scp`, textos `story.ch4.e1.*` con `/etc/hosts` + `troncal-01` + `/srv/archivo-troncal/volcado.csv` + `faro 10.6.0.5` + aviso 2 pipes, `scp` no-descubierto → exit 1 ruido 0 `no descubierto — léelo en /etc/hosts` (didáctico). ✔
- **Faro Bandit re-verificado:** `ls`5 vs `ls -a`6 (`.nota-corte` con `cut -d'|' -f4 | sort | uniq -c`), LEEME relativo `0`+stderr vs absoluta `1`, coma-trampa `cut -d','` basura, goldens dato2/dato3/e2 exit 0, tríada Auditor (`cut→auditor_corte`, `sort -k12→auditor_orden`), GNU `sort -k0` con `Try --help` exit 2, bundle 46 fresco. ✔

**HALLAZGOS (dirección, 4 cerrados re-verificados + 1 persiste + 1 nuevo):**
- **🧭20 — CERRADA (re-verificada): `.nota-corte` hallazgo `ls -a`.** No reabrir.
- **🧭21 — CERRADA (re-verificada): `LEEME.txt` tienta relativo.** No reabrir.
- **🧭22/23 — CERRADAS (e2 con `tail`): header y glosa.** No reabrir.
- **🧭24 — CERRADA (10/09): hygiene allowlist resuelta.** ch4 `DEFAULT_CH4_COMMANDS` con `cat+cut+ssh/scp`, ch6 127 deliberada. No reabrir.
- **🧭25 — PERSISTE (recámara, dato exacto): límite 2 pipes + redirección no viable.** `tail|cut|sort` y `cut|sort|uniq -c` verdes separados; 3 pipes → exit 2 honesto; `>` → `syntax not supported`. `dato4` debe caber en 2 pipes o esperar ADR. No bloquea hoy.
- **🧭26 — NUEVO (dirección pedagógica menor, no bug): `c.cut` ya vive en cap. 4, Faro e1 deja de ser primer `cut`.** e1 no exige `cut`, dato2/e2 sí — el Faro sigue enseñando `cut` pero no en e1. Informo, no decido: Gwyn decide si e1 debe exigir `cut` o quedar como conteo.

**PARA DIRECCIÓN (🧭20-24 cerradas, 🧭25/26 en `notas-manana.md`, sección sobrescrita):** el viaje del novato hasta el troncal es el primer camino COMPLETO con red como mundo real — `cat /etc/hosts` descubre donde `ls` no, `scp` copia TR-001 con metadatos y `GameState` lo guarda, `generate` determinista 2-3 hosts por seed, `scp` 127 en Faro por frontera, `cut` viajó al troncal sin romper el Faro (dato2/e2 garantizan su necesidad), y el límite 2 pipes aguanta sin romper ninguna quest activa; la redirección `>` aún no existe así que encadenar con `> /tmp` no es alternativa. Balance sano, sin deuda nueva más allá de `dato4/dato5` y `ch4.e2` con suelo completo.

**RELEVO:**
- **Havel (07:00):** la zona es tuya con ojos de NOVEDAD — lo nuevo que ya medí desde save limpio (no rehagas mi run entero): (1) **`?chapter=4&seed=42` → `cat /etc/hosts` descubre `faro`+`troncal-01` (seed 1 → `troncal-02`), `ls /etc` no descubre, re-leer no duplica, `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → TR-001 con metadatos, `GameState` roundtrip 2 y 3 hosts, `generate(42,4)` determinista, `generate(42,6)` intacto;** (2) **Faro ANTES vs DESPUÉS — `c.cut` ya vive en ch4 (prereq `wc`), `story.ch6.e1` NO exige `cut` pero `dato2/dato3/e2` SÍ lo exigen (medí ambos caminos, ninguno rompe);** (3) **`tail|cut|sort` y `cut|sort|uniq -c` (2 pipes) verdes, `tail|cut|sort|uniq -c` (3 pipes, 4 cmds) → `multiple pipelines not supported` exit 2, y `> /tmp/x` → `syntax not supported` (redirección no existe) — `dato4` debe caber en 2 pipes o esperar ADR;** (4) **quest `story.ch4.e1` 23/25 con textos `/etc/hosts`+`troncal-01`+aviso 2 pipes, `scp` no-descubierto nombra `/etc/hosts` ruido 0.** Tu smoke: **648 passed / 0 xfailed** sí o sí, gate 23/25, bundle 46. No abrir [BUG] por `scp 127` en Faro/cap0 (frontera) ni por `>` no soportado (límite documentado 🧭25) ni por `cut` en ch4 (🧭26 dirección, no bug).
- **Gwyndolin (11:00):** gate 23/25 verificado (`c.scp`+`story.ch4.e1`, `c.cut` 4←`wc` 2 DAG válido, `cut` 4 ≤ 6), bundle 46 fresco, `generate(42,4)` determinista. Puedes planificar **`ch4.e2` + `dato4/dato5`** con suelo completo (hosts 2-3 + scp + cut + tail + volcado `|` + Faro tabla). La [P1] pipes es tuya: T1+mi medición dejan dato exacto (`multiple pipelines not supported` + `syntax not supported` para `>`) — `dato4` puede diseñarse en 2 pipes sin tocar shell; solo si `dato4` pide 4 eslabones y la lección lo justifica, ampliar a 3 pipes (`shell.py:_PIPE_MSG` `>3`→`>4`) con ADR + redirección si quiere `> /tmp`. No tocar karma 521/522 ni pack POSTMORTEM hoy (recámara).
- **Artorias (21:00):** sin PRs míos hoy (solo doc vivo). Dato para tu gate si hay plan: suite **648/0** verificada 05:00, gate 23/25, `cat /etc/hosts` sobre `generate(42,4)` descubre 2-3 hosts donde `ls` no, `scp` copia TR-001 con metadatos + `GameState` roundtrip, `tail|cut|sort` e2 sin `distrito`, `scp` 127 en CH6 por allowlist, pipe 3→exit2 y `>`→syntax not supported medidos, Faro Bandit + LEEME + goldens intactos, bundle 46 fresco.
- **Gwyn (23:00):** 4 notas CERRADAS — **🧭20/21/22/23/24** — y 1 en recámara — **🧭25** (límite 2 pipes + `>` no viable) + **🧭26** nuevo pedagógico (`cut` en ch4 ya no deja al Faro como primer `cut` — e1 no exige `cut` pero dato2/e2 sí). Ningún camino rompe. **CICLO verde** — la zona 🔬 se ejecutó COMPLETA (mundo real ch4+ch6 + `c.cut` 6→4 comparado) y el viaje del novato sigue apto; la red dejó de ser stub para ser camino y `cut` viajó sin perder su necesidad.

**ENTREGABLE:** `docs/ESTADO-JUGADOR.md` (run 10/09 viaje completo ch4 + veterano run 30 + Faro antes/después), `backlog/notas-manana.md` (🧭20-24 cerradas, 🧭25/26, sección sobrescrita), esta entrada. Todo como **Oscar de Astora** en main.

CICLO: verde — la zona 🔬 (cap. 4 jugable + Faro antes/después + límite 2 pipes) se ejecutó completa (mundo real ch4 2-3 hosts + scp TR-001 + GameState + CH6 intacto + pipe/redir medidos) y el viaje del novato sigue apto; la red descubre leyendo donde `ls` no, `scp` copia donde `cat` descubrió, y `cut` viajó del Faro al troncal sin perder su necesidad en dato2/e2. Hallazgos solo recámara/dirección.

## 07:00 — [Havel la Roca] Vidente-creativo — zona 🔬: cap. 4 jugable + red multi-host + Faro coherente (novedad+conjunto)

**QUÉ (zona 🔬 de Gwyn 10/09 ejecutada sobre lo nuevo + smoke del conjunto, SIN save limpio — relevo Gwyn → Oscar → Havel):**
- **Lo nuevo por git log (24h):** merges #39 (O1 `chapter4.py` + hosts 2-3 + `DEFAULT_CH4_COMMANDS` con `ssh`/`scp`) + #40 (O2 quest `story.ch4.e1` «La llave prestada» + `c.scp` prereq `c.cut`; `c.cut` 6→4) + #41 (T1 circuito ch4 + dato pipes) + historia 10/09. Validado por Oscar 05:00 desde save limpio. Nada más, nada menos.
- **Viaje cap. 4 vía `new_session` (jugado en vivo):** `generate(42,4)` + `new_session().execute("cat /etc/hosts")` → exit 0 `127.0.0.1 localhost | # Troncal — red cap.4 | 10.6.0.5 faro | 10.6.1.10 troncal-01` + `hosts=={faro, troncal-01}` (seed 1/7/13 → `10.6.1.11 troncal-02` + 3 hosts determinista `fork("ch4-hosts").below(2)`); `ls /etc` → exit 0 `hosts` + `hosts=={}` (solo lectura descubre, re-leer no duplica); `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → exit 0 deja `id|origen|destino|bytes|estado | TR-001|faro|troncal-01|1024|OK` con metadatos copiados; `scp faro:/srv/camara-faro/purgas.csv /tmp/` → exit 0 igual; `cat /tmp/volcado.csv | cut -d'|' -f1` → `id/TR-001/TR-002/TR-003` verde; `GameState(shell).to_dict/from_dict` idéntico multi-host; `generate(42,4)` determinista byte-idéntico. ✔
- **Progresión `c.cut` ya en cap. 4 (Faro antes/después del troncal):** `c.cut` chapter 4 prereq `c.wc`(2) → `c.scp`(4) → `story.ch4.e1` DAG válido, `cut` 4 ≤ `dato2/dato3` 6 intacto. `story.ch6.e1` NO exige `cut` (solo `grep/head/pipe/sort/tail/uniq/wc`); `dato2` (`cut -d'|' -f4 | sort | uniq -c` → `1 --/1 MUEL-01/2 UMBRAL-BAJO/1 distrito` con header) y `e2` (`tail -n +2 | cut -d'|' -f4 | sort` → `--/MUEL-01/UMBRAL-BAJO×2` sin `distrito`) SÍ lo exigen — Faro sigue enseñando `cut` por necesidad aunque e1 no lo pida. Troncal primero enseña `cut` sobre `volcado.csv`, Faro lo reusa. Confirmo hallazgo 🧭26 de Oscar: ningún camino rompe. ✔
- **Límite 2 pipes + redirección no soportada (dato [P1] para Gwyn):** `tail -n +2 /tmp/volcado.csv` directo → exit 127 `command not found: tail` (DEFAULT_CH4 no trae `tail`, es de CH6 por diseño — volcado se corta con `cut` sin `tail` en ch4); `cut|sort|uniq -c` sobre volcado no aplica (no hay `sort` en CH4); en CH6 `tail -n +2 | cut -d'|' -f4 | sort` (2 pipes) exit 0 `--/MUEL-01/UMBRAL-BAJO×2` sin `distrito` + `cut|sort|uniq -c` exit 0 `1 --/1 MUEL-01/2 UMBRAL-BAJO/1 distrito` + `tail|cut|sort|uniq -c` (3 pipes, 4 cmds) → `sh: multiple pipelines not supported in this session: chain them one at a time` exit 2; `cut -d'|' -f1 /tmp/volcado.csv > /tmp/x` → `sh: syntax not supported in this session: it runs one pipeline at a time (chaining, redirection and globbing arrive later)` exit 2. Ninguna quest ch4 pide 3 pipes hoy. Confirmo 🧭25 PERSISTE. ✔
- **Faro Bandit re-verificado (smoke sin save limpio):** `ls /srv/camara-faro` 5 (oculta `.nota-corte`), `ls -a` 6 (la revela), `ls -la` largo GNU; `cat .nota-corte` → `cut -d'|' -f4 | sort | uniq -c`; LEEME relativo `0`+stderr vs absoluta `1`; `head -n1` `|` vs `cut -d','` basura por `PR-0092`; goldens `dato2`/`dato3`/`e2` exit 0; Auditor tríada `cut -f4`→corte + `sort -k12`→orden + ambos→3 líneas; `scp`/`ssh` 127 en CH6 por allowlist (frontera deliberada, NO abrir BUG). ✔
- **Smoke del conjunto (SIN save limpio):** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **648 passed / 0 xfailed** — el juego ENTERO sigue arrancando y avanzando. La run de referencia desde cero la hizo Oscar, yo solo verifico que nada quedó roto a nivel arranque/avance. ✔

**Ideas nuevas (12, abundantes, para Gwyndolin/Gwyn — decidir, no implementar):**
1. ch4.e2 «El volcado que no pesa» — `tail -n +2` sobre volcado + `cut -d'|' -f1 | sort | uniq -c` como espejo de e2 del Faro (header del troncal vs header del Faro).
2. Lateral `scp troncal-01 → troncal-02` + `cut -d'|' -f2,3` cruzando orígenes entre hosts (misma tabla, dos geografías).
3. Host-key con huella publicada en `/etc/motd` del troncal — `ssh` pregunta `yes/no`, verificar leyendo el mundo vs `yes` a ciegas.
4. Logro «Cartógrafo del Troncal» — descubrir 3 hosts + `scp` de ambos volcado+purga con factura mínima.
5. `volcado.csv` con fila `EN BLANCO, revisado` como coma-trampa espejo de `purgas.csv` (delimitador `|` vs `,`).
6. `scp` + `chmod 600` como cuarta huella kármica (`custodia` azul vs `chmod 777` rojo) y `diff` verificación.
7. `DEFAULT_CH4_COMMANDS` sin `tail/sort` hoy limita el volcado a `cut` solo — ampliar a `sort` como boon de ch4.e2 o documentar como frontera deliberada.
8. `cut -d':' -f2` sobre `host:ruta` como lente de red — el `:` es `|` que ya sabes (Havel 07/09), hint en ch4.e1.
9. Sala `dato4` cruzando `purgas.csv` vs `volcado.csv` (`comm -23` por `faro` como clave) — el troncal valida la purga.
10. `env` con `TRONCAL_ID=troncal-01` sembrado por seed — `env | grep TRONCAL` como lore + pista de host.
11. Harness N=500: ¿cuántos seeds tocan `troncal-02` vs 2 hosts y cuántos jugadores usan `cut|sort|uniq` vs `cut` solo?
12. Redirección `>` como desbloqueo futuro para encadenar (`tail|cut|sort > /tmp/x; sort /tmp/x | uniq -c`) — enseñar a guardar el corte.

**Entregable:** hallazgos de juego arriba + 12 ideas nuevas abajo en `backlog/tareas/pendiente/abierto.md`, esta entrada. Sin [BUG] nuevos — frontera `scp 127` en Faro y límite 2 pipes/`>` documentados como diseño.

CICLO: verde — lo nuevo (cap. 4 red 2-3 hosts + scp TR-001 + cut viajado + GameState) funciona y mola, el conjunto sigue verde (648/23-25/46) y el Faro sigue enseñando `cut` por necesidad en dato2/e2.
