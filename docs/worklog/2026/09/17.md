# WORKLOG 2026-09-17

## 11:00 — [Gwyndolin] Planificador — plan del día + higiene

**QUÉ:** leído el protocolo completo (PROJECT-MAP, AGENTES, abierto.md §575
líneas, activo.md, notas-mañana 🧭17/09 + 🎯, DESIGN, INDICE, história,
worklog 16/09). Rama/PR: UNA huérfana viva — **PR #61** (Seath, tooltip
N/30 + lente rescate Faro, sin veredicto de Artorias). Sin más ramas/PRs.

**HIGIENE del backlog:** abierto.md verificado de punta a punta — estados↔carpetas
coherentes (ningún `[PENDIENTE]` en `hecho/`, ningún `[HECHO]` en `pendiente/`,
ningún `[EN CURSO]` con veredicto ✅ pendiente de archivar erróneamente).
Las resoluciones inline tipo «ELEGIDA para HOY» que ya se ejecutaron (PRs #19, #12,
#34, #42-#44) llevan su nota de higiene con fecha — intactas, no se replanifican.
Lecciones registradas al 01/09 respetadas: NO re-clasificado ningún `[HECHO]` de
`en-curso/` (el archivado es de Gwyn). Prioridades de hoy sin cambios: sin
nuevas `[P0]/[P1]` muertas detectadas — la única señal viva de máxima es un
residuo de la P1 del eco (13/09) que Gwyn ya cerró ayer (ARCHIVADA en
`hecho/2026-09.md`, línea retirada de `abierto.md` para que no compita de nuevo
y haya señal limpia de hoy). INDICE.md sin cambios de estructura.
mejoras/pendiente: sin propuestas nuevas desde 15/09 — no toca.

**DIRECCIÓN del día:** nota 🧭17/09 de Oscar converge con Gwyn (14/09: «cap. 5
encadenado avalado») + Havel 17/09 (2 ideas cap. 5): la cadena TR-003 ya cierra
(e3 + dato7); lo con mejor ROI es **cap. 5 «Subestación» que LEA el testigo**
con verbos ya dominados — el fichero `volcado-rescate.csv` existe o no según la
bifurcación de hace 2 capítulos. Sin comando ni huella nueva.

**PLAN de hoy (`backlog/planes/2026/09/17.md`):**
- **T1 — Seath** (19:00, PRIORIDAD 1): CIERRE del PR #61 (rama huérfana,
  solo `web/app.js`, delta 0). Propietario de su propia bolsa de ayer.
- **O1 — Ornstein** (13:00, PR #62): pieza 1 de cap. 5 — `chapter5.py` NUEVO
  con FS del asalto: proceso intruso `START 03:14` + testigo condicional
  (`/tmp/volcado-custodia.csv` con `TR-003` si rescate; ausencia honesta si
  caducado). Conector `_generate_cap5` (firma = dato7), 8 tests, suite 739→≥745.
  NO session.py (frontera 127 de cap. 5 se decide mañana).
- **S2 — Smough** (16:00, PR #63): quest `story.ch5.e2` «El testigo que no
  llegó» (grey, `c.cat`+`c.scp`, sin concepto nuevo) sobre el FS de O1 con
  FLEXIBILIDAD declarada; **GATE OWNER** flexible 24/31→24/32 (`<=`); bundle
  regen en SU rama (única que toca `src/data/`). Suite 739→≥741.
- Costuras: ALLOWLIST OWNER **NADIE** · GATE OWNER **Smough** · Bundle **Smough** ·
  `web/app.js` solo **Seath** · `chapter5.py`+`generator.py` solo **Ornstein** ·
  `postmortem.*` intocados.

**RELEVO:** Ornstein 13:00 arranca con PR #62; Smough 16:00 con PR #63
(FLEXIBLE si O1 no llega); Seath 19:00 cierra #61 antes que nada. Artorias
21:00 ensaya engine→sandbox→meta-ui (#62 → #63 → #61); Gwyn 23:00 decide
merge completo o parcial, gate esperado 24/32, suite esperada ≥747.
---

## 13:00 — [Ornstein] Ejecutor 1 — cap. 5 pieza 1: FS del asalto con testigo condicional (PR #62)

**PLAN DE IMPLEMENTACIÓN (hitos):**
1. **Hito 1 — FS condicional `chapter5.py`** (40 min): crear hoja leaf con `build_chapter5_fs(fs_rng, volcado_rescatado)` — proceso `intruso --vigilar-censo` USER `censo` START `03:14` (firma horaria del volcado, misma que Oscar midió) + testigo `/tmp/volcado-custodia.csv` con `TR-003|faro|troncal-01|512|EN_COLA` si rescate, ausencia honesta si caducado. Determinismo vía `fork("ps-subestacion")` para PID; fallback estático. Estructura mínima `/tmp`+`/srv/subestacion`+`/var/log`.
2. **Hito 2 — Conector `_generate_cap5` en `generator.py`** (30 min): importar `CUSTODIA_*`/`build_chapter5_fs`, añadir función `_generate_cap5(seed, variant, curriculum, contract_id, volcado_rescatado=False)` misma firma que dato7, con room/canon condicional (`CANON_STEPS_RAW_CH5` vs `CADUCADO`), validación `validate_incursion` rama `chapter==5` (intruso 03:14 + custodia TR-003 o No such file). NO tocar `SUPPORTED_CHAPTERS` (cap5 fuera) — sesión para mañana.
3. **Hito 3 — Tests `test_chapter5_fs.py` 8 tests** (40 min): FS rescate/caducado ×2 seeds byte-deterministas, `ps aux` con 03:14 en ambos estados, `cat`→`No such file` cuando caducado vs `TR-003` cuando rescate, no-regresión 7 goldens byte-idénticos (cap0/2/3/4/6 + dato7 rescue/absence, rescue≠caducado).
4. **Hito 4 — Verificación y entrega** (20 min): suite 739→747 (+8, 746+1 stale bundle pendiente Gwyn — NO regenero por regla 12/09), 0 xfailed, core_no_pyxel verde, PR #62, doc README v0.8, huella en activo.md ya [HECHO] por plan.

**QUÉ:** `src/core/generator/chapter5.py` NUEVO (FS del asalto) + `generator.py` con `_generate_cap5` y validación cap.5 + `test_chapter5_fs.py` (8 tests). El testigo viaja: `scp` en ch4.e3 lo pone en `/srv/camara-faro/volcado-rescate.csv`; si lo rescataste y pasan 30 ticks sin `rm`, aquí aparece como `/tmp/volcado-custodia.csv`; si lo borraste o caducó, `cat` da `No such file` — la ausencia es el detector.
**POR QUÉ:** dirección 17/09 de Gwyn/Oscar/Havel converge: la cadena TR-003 ya cierra (e3 jugable + dato7). Lo con más ROI es que cap.5 LEA el testigo con verbos ya dominados (`cat`+`ps`), sin comandos ni huellas nuevas — primera vez que una decisión cambia la geografía de OTRO capítulo. La Subestación es el único asalto invertido (defiendes, no entras).
**ENTREGABLE:** `src/core/generator/chapter5.py` + `src/core/generator/generator.py` (import + _generate_cap5 + validate cap5) + `src/tests/core/generator/test_chapter5_fs.py` (8 tests). Rama `feat/engine-2026-09-17`, PR #62. Suite 739→747 (+8, 746 passed +1 bundle stale honesto pendiente regen canónico Gwyn). `postmortem.*`, `curriculum.json`, `session.py`, `web/` intocados.
**RELEVO:** Rama `feat/engine-2026-09-17` pushada + PR #62 para Artorias 21:00 (ensayo engine→sandbox→meta-ui #62→#63→#61, gate 24/31→24/32 flex) y Gwyn 23:00 (merge en orden, regen canónico bundle con chapter5.py). Smough 16:00 puede construir `story.ch5.e2` flexible sobre este FS (ya existe) — si no llega, su quest es sobre dato7 existente. TESTS ANTES: 739 · TESTS RAMA: 747 · DELTA: +8.
