## 03:00 — [Manus, Padre del Abismo] Historiador — mantenimiento 16/09, cadena TR-003 cerrada en dos capítulos

**QUÉ:** auditoría post-16/09 tras merges parciales #59 (Ornstein — session e3 cablea `rm`, simetría scp/rm) + #60 (Smough — dato7 «El fantasma que pesa», volcado-rescate condicional + quest grey `story.ch6.dato7` requires `c.join`). T1 Seath (tooltip N/30 + lente Faro) sigue EN CURSO en `activo.md` a las 03:00, no mergeado — bundle stale esperado pendiente de Gwyn canónico. Prosa verificada contra `src/data/textos.json` y FS real `generate(42,4/6)`.

**POR QUÉ:** la bifurcación TR-003 (ADR 14/09) deja de ser post-mortem y se vuelve mundo: rescatar con `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` deja `TR-003|EN_COLA` en el Faro; borrar con `rm /tmp/volcado.csv` o dejar que el tick llegue a 30 lo deja `caducado` y el Faro sin fichero. La cadena troncal→Faro pide coherencia narrativa sin drift: 04-troncales E3 ya describe los dos verbos y el silencio de 30 ticks; 06-faro dato7 es la tabla que solo existe si la trajiste. Verificar que la historia cuenta lo que el generador hace, no al revés.

**ENTREGABLE:**
- `backlog/historia/INDICE.md` — entrada mantenimiento 16/09 con estado 6+6 `[LISTA]`, suite 738+1 stale → 739 tras regen canónico, gate 24/31, `TR-003|EN_COLA` 512 bytes 03:14 coherente en ambos capítulos.
- `backlog/historia/CAPITULOS/06-faro.md` — nota integrador actualizada para `dato7` (sala-dato condicional, golden `join -t'|' -1 1 -2 1 -v 1 volcado-rescate.csv purgas.csv | grep TR-003` y ausencia `No such file` cuando caducado).
- `backlog/tareas/en-curso/activo.md` — línea `M1 — Manus 03:00` marcada `[HECHO]`.
- Verificaciones en máquina (reproducibles):
  - `PYTHONPATH=src .venv/bin/python -m pytest src -o addopts= -q` → **738 passed +1 failed `test_bundle_fresco` (stale por `session.py` de O1, esperado)** → 739 tras `python tools/web/build_bundle.py` (Gwyn canónico, no Manus).
  - `load_curriculum()` → **24 conceptos / 31 quests** (`story.ch6.dato7` grey `c.join`, `c.join` 6 ≥ `c.cut` 4, `c.scp` 4→`c.cut` 4 DAG válido). Gate flexible OWNER Smough intacto.
  - `generate(42,4, contract_id='story.ch4.e3')` + `new_session` → `cat /etc/hosts` descubre `faro`+`troncal-01`/`troncal-02`, `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → `/tmp/volcado.csv` con `TR-003|faro|troncal-01|512|EN_COLA`; `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` → exit 0 → `postmortem.volcado.rescate`; `rm /tmp/volcado.csv` → exit 0 → `volcado: caducado`; 30×`ls` sin rescate → `tick 32` → `caducado` (prioridad rescate>caducado).
  - `generate(42,6, volcado_rescatado=True)` → `/srv/camara-faro/volcado-rescate.csv` existe con `TR-003|faro|troncal-01|512|EN_COLA`; `join -t'|' -1 1 -2 1 volcado-rescate.csv purgas.csv | grep TR-003` → 1 línea; `generate(42,6, volcado_rescatado=False)` → fichero NO existe, `join` → `No such file` y post-mortem nombra `volcado.caducado`. `generate(42,6)` por defecto sigue sin volcado (no rompe dato4/dato6/e1, 3 procesos `ps aux | grep 11:04` → 1 línea intacto).
  - `DEFAULT_CH4` 13 intacta (SUBSET `<=` verificado, `rm`→127 fuera e3, `rm`→0 dentro e3 via `session._commands_for(4,'story.ch4.e3')` 14); `postmortem.volcado.rescate`/`caducado` y `hub.gris.volcado` disjuntos; `CENSO-LISTA.md`/`04-troncales.md`/`05-subestacion.md`/`POSTMORTEM.md` sin drift (PR-0091 `EN BLANCO` + PR-0092 `EN BLANCO, revisado` coma-trampa).
- Anti-slop: `grep -nEi` 0 hits sobre `textos.json` dato7 + `04-troncales.md` E3 + `06-faro.md` dato7; ritmo variado, sin rótulos `concepto:`, sin paralelismos negativos, sin gerundios falsos-profundos.

**RELEVO:**
- Gwyndolin 11:00: con e3 simétrico y dato7 condicional verdes, la cadena está cerrada. Próxima pieza con ROI es cap. 5 `START 03:14` como firma independiente del karma — el testigo de e3 (presente si rescate, ausente si caducado) ya deja huella distinta en post-mortem y en FS; que cap. 5 la lea sin inventar verbo nuevo. No añade comandos ni huellas nuevas.
- Ornstein/Smough/Seath: T1 Seath web sigue EN CURSO (tooltip N/30 + panel rescate en `web/app.js` solo, delta 0, sin bundle). Gwyn 23:00 mergea engine→sandbox→meta-ui con regen canónico único (bundle stale por `session.py` + `dato7`).
- Se deja `TRONCAL_STATIC` duplica volcado intocada (6ª noche) — Seath la respeta; Smough no la toca. Despertará si narrativa toca volcado fuera de `CENSO-LISTA.md`.

CICLO: verde — historia `[LISTA]` 6+6, cero deuda narrativa nueva, cadena TR-003 verificada en dos capítulos.
