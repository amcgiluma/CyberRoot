## 05:00 — [Oscar de Astora] Guardián de la experiencia — run de referencia + veterano (cadena e3→dato7)

**QUÉ:** MODO B (build jugable headless `generate` + `abrir_encargo` + `Shell`). Zona 🔬 stale (15/09) → se juega prioridad real 16/09 (cadena TR-003) como primera prioridad desde save limpio. `PYTHONPATH=src .venv/bin/python -m pytest src -o addopts= -q` → **738 passed +1 failed `test_bundle_fresco` (bundle stale `session.py`, esperado → 739 tras `python tools/web/build_bundle.py`)**; `load_curriculum()` **24/31** (`story.ch6.dato7` grey `c.join`); `DEFAULT_CH4_COMMANDS` 13 / `DEFAULT_CH4E3_COMMANDS` 14 disjunta (`rm`→127 fuera e3, `rm`→0 en e3); `TRONCAL_STATIC` byte-idéntica; `listar_encargos(4)` e1/e2/e3 determinista.

**POR QUÉ:** Oscar es el dueño del save limpio (TESTEO-DIARIO §1): mide si el camino del novato sigue apto tras los merges parciales #59/#60 (🧭36 cableada + dato7 condicional) y si la consecuencia cruza capítulos como mundo. Bundle stale no rompe camino (el core es correcto, solo falta regen canónico de Gwyn).

**Run de referencia (save limpio, sin atajos, 6 bloques):**
1. **E3 simétrico (cap. 4, 🧭36 CERRADA):** `abrir_encargo(cur,'story.ch4.e3',['c.scp'])` → `abrible True`; `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → `/tmp/volcado.csv` `TR-003|faro|troncal-01|512|EN_COLA` (via `abrir_encargo` pre-puebla `hosts` — `cat /etc/hosts` ya no es requisito para `scp`); `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` → `TR-001/TR-002/TR-003` sin header. **Rescate** `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` → exit 0 → `build_postmortem` → `volcado: rescatado` `postmortem.volcado.rescate` («Expediente 000: volcado EN_COLA entregado al Faro.»). **Disolución por verbo** `rm /tmp/volcado.csv` → exit 0 → `volcado: caducado` («sin entrega — caducado.»). **Caducado por tiempo** `scp` + 30×`ls` → `tick 31` → `caducado` (prioridad rescate>caducado). `abrir_encargo` e1/e2 `rm`→127 intacto (frontera 13 vs 14). ✔
2. **Dato7 condicional (cap. 6, NUEVO S1):** `generate(42,6, volcado_rescatado=True)` → `/srv/camara-faro/volcado-rescate.csv` existe (`id|origen|destino|bytes|estado` + `TR-003|faro|troncal-01|512|EN_COLA`) + `Shell(..., cwd="/srv/camara-faro")` `join -t'|' -1 1 -2 1 volcado-rescate.csv purgas.csv | grep TR-003` → 1 línea exit 0; `generate(42,6, volcado_rescatado=False)` → NO existe → `join` → `No such file` exit 1 (y `cat` igual); `generate(42,6)` por defecto sin volcado (no rompe dato4/dato6/e1, `ps aux | grep 11:04` → 1 línea intacto). Gate 24/31 (`story.ch6.dato7` grey `c.join`, DAG `c.scp→c.cut→c.join` válido). ✔
3. **Dato6 doble ruta (Faro):** `generate(42,6)` `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv | grep 000483` → 1 línea `000483|PR-0092|...|EN BLANCO, revisado`; `cut -d'|' -f3 purgas.csv | grep 000483` → `000483` requirement; `grep 000`→2 vs `grep 000483`→1; `cut -d','` coma-trampa intacta. ✔
4. **Dato4 + Faro E2/E3:** `generate(42,6)` `join -v` → 2 huérfanas `PR-0091`/`PR-0092` sin fantasma; `tail -n +2 | cut -d'|' -f4 | sort | uniq -c` sin header, `sort -t'|' -k12 -n | head -n 3` con `PR-0091` al frente. ✔
5. **Fronteras allowlist + pipes + determinismo:** `DEFAULT_CH4_COMMANDS` 13 (`rm`→127 fuera e3), `DEFAULT_CH4E3_COMMANDS` 14 (`rm`→0 solo e3), `tail` en ch4→127, `c.cut` 4/6, `c.scp` 4, `c.join` 6, 1-2 pipes OK / 3 KO honesto, `grep -v`→127 filtro positivo, `generate(42,4/6)` determinista. ✔
6. **Smoke:** 738+1 stale→739, gate 24/31, bundle 47 ficheros, `gris_eco` + `auditor_espejo` 3 firmas deterministas, `TRONCAL_STATIC` byte-idéntica, badge toggle 1→3, `?chapter=6` intacto. ✔

**Veterano (run 30):** el veterano de 20h ya gira `scp`+`cut|grep` sin mirar; en e3 elige flanco por identidad. Si rescata, el Faro guarda `volcado-rescate.csv` y `join … volcado purgas | grep TR-003` le devuelve su testigo; si disuelve o caduca, el Faro amanece sin fichero y el `join` le responde `No such file` — misma hora, dos mundos. La web le muestra `N/30` mientras decide. Dato7 añade tercera tabla del Faro solo si trajiste el testigo; dato6 sigue espejo reversible (`join` vs `cut`). El eco `auditor_volcado` + `gris_eco` + `auditor_espejo` cierra el repertorio con cuarta firma `volcado: rescatado|caducado`.

**Hallazgos (dirección, no rotura):** 🧭20/21/22/23 cerradas; 🧭24 cerrada con matiz P3 (pre-puebla `hosts` redundante con briefing); 🧭25/26/27 PERSISTEN P3 (2 pipes, `c.cut` en ch4, `grep -v` filtro positivo); 🧭28/29/30/31/32/33/34/35 CERRADAS; **🧭36 CERRADA** (e3 simétrico via `abrir_encargo`, `rm`→`caducado` y `tick 31`→`caducado` con prioridad rescate, `e1/e2` `rm`→127 intacto). **Dato7 verde** sin deuda (determinista seed+flag, `generate(42,6)` por defecto sin volcado). Sin [BUG] nuevo que abra `abierto.md` (el único [BUG] vivo sigue siendo `grep -v` P3, sin tocar). Bundle stale 1 fichero (`session.py`) es observación P3, no bug — Gwyn lo resuelve con `python tools/web/build_bundle.py` → 739.

**ENTREGABLE:**
- `docs/ESTADO-JUGADOR.md` — estado 17/09 (738+1 stale→739, 24/31, e3 simétrico + dato7 condicional, 🧭36 CERRADA, cadena como mundo).
- `backlog/notas-manana.md` — 🧭 17/09 (cadena e3→dato7 COMPLETA, APTO, saldo 🧭36 CERRADA + dato7 verde, bundle stale P3).
- `backlog/tareas/pendiente/abierto.md` — sin cambios (ningún [BUG] nuevo; `grep -v` P3 sigue vigente).
- `docs/worklog/2026/09/17.md` — esta entrada (qué / qué revisaste / qué propones / CICLO).

**RELEVO:**
- **Havel 07:00:** la misma cadena e3→dato7 con ojos de novedad (¿el Faro sin fichero se entiende sin popup? ¿el `No such file` invita a releer la bifurcación o confunde?); smoke del conjunto SIN save limpio (suite 738+1 stale→739, gate 24/31, bundle fresco tras regen).
- **Gwyndolin 11:00:** con 🧭36 CERRADA y dato7 verde, la cadena está cerrada. Próxima pieza con ROI: **cap. 5 «Subestación» `START 03:14`** como firma independiente del karma — el testigo de e3 (presente/ausente) ya deja huella distinta en post-mortem y FS; que cap. 5 la lea sin inventar verbo nuevo.
- **Gwyn 23:00:** valida 🧭 17/09 + bundle stale 1 fichero (`python tools/web/build_bundle.py` → 739, `test_bundle_fresco` verde, suite 739 = 738+1 regen).

CICLO: verde — la zona 🔬 se ejecutó completa (e3 simétrico + dato7 + dato6) y el viaje del novato sigue apto; la cadena cruza capítulos y el Faro responde con presencia/ausencia.

## 07:00 — [Havel la Roca] Vidente-creativo — novedad + smoke + ideas (cadena e3→dato7)

**QUÉ:** MODO B sin save limpio (relevo Oscar 05:00). Zona stale 15/09 → se juega prioridad real 16/09 (cadena TR-003) con ojos de novedad. `git log --since=24h` → PR #59 (`_commands_for(4,'story.ch4.e3')` 14 `rm` ruido 2) + PR #60 (`dato7` `volcado_rescatado` conditional `build_chapter6_fs` + quest `story.ch6.dato7` grey `c.join` gate 24/30→31). Zona Gwyn 15/09 (toggle + dato6 doble ruta) re-verificada como Faro intacto; lo nuevo es la cadena que pesa como mundo.

**POR QUÉ:** Havel juega lo nuevo por `git log` (§2 TESTEO-DIARIO) y genera ideas para que el juego crezca solo; Artorias/Gwyn decidirán merge, yo doy jugo. La run de referencia desde cero ya la hizo Oscar — yo no la repito, hago smoke del conjunto.

**JUGADO (timebox 20 min, headless):**
1. **E3 simétrico vía `abrir_encargo` (NUEVO O1 #59, 🧭36 CERRADA):** `abrir_encargo(cur,'story.ch4.e3',['c.scp'])` → `abrible True` + `Shell` 14 cmds (`rm`→`No such file` si falta, vs `e1` 13 cmds `rm`→127 frontera intacta), `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → `/tmp/volcado.csv` `TR-003|faro|troncal-01|512|EN_COLA 03:14`, `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` → `TR-001/TR-002/TR-003` sin `id`. **Rescate** `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` → 0 → `cerrar_encargo` → `volcado: rescatado` `postmortem.volcado.rescate` (`Expediente 000: volcado EN_COLA entregado al Faro.`). **Disolución por verbo** `rm /tmp/volcado.csv` → 0 → `caducado` (`sin entrega — caducado.`). **Caducado por tiempo** `scp` + 30×`ls` → `tick 31` → `caducado`. Prioridad **rescate>caducado** (rescate+30 ticks → sigue `rescatado`). `hosts` pre-poblados `{faro,troncal-01,troncal-02}` sin `cat /etc/hosts` previo (doc drift 🧭24 menor). ✔
2. **Dato7 condicional «El fantasma que pesa» (NUEVO S1 #60):** `generate(42,6, volcado_rescatado=True)` → `/srv/camara-faro/volcado-rescate.csv` existe `id|origen|destino|bytes|estado`+`TR-003|faro|troncal-01|512|EN_COLA`, `Shell(DEFAULT_CH6_COMMANDS,cwd="/srv/camara-faro")` `join -t'|' -1 1 -2 1 volcado-rescate.csv purgas.csv | grep TR-003` → 1 línea exit 0; `generate(42,6, volcado_rescatado=False)` → NO existe → `join` → `join: volcado-rescate.csv: No such file or directory` exit 1 + `cat` idem; `generate(42,6)` por defecto sin volcado (no rompe dato4/dato6/e1, `ps aux | grep 11:04` → 1 línea intacto). Determinismo seed+flag byte-idéntico, `load_curriculum()` 24/31 grey `c.join` DAG `c.scp→c.cut→c.join` válido. ✔
3. **Faro intacto (smoke Faro):** `join -t'|' -1 3 -2 1 -v 1` → 2 huérfanas `PR-0091`/`PR-0092` coma-trampa, `cut -d'|' -f4 | sort | uniq -c` sin `distrito`, `sort -t'|' -k12 -n | head -n 3` PR-0091 al frente, toggle `EN_COLA` 1→3, `tail -n +2` quita header. ✔
4. **Smoke del conjunto (SIN save limpio):** `PYTHONPATH=src .venv/bin/python -m pytest src -o addopts= -q` → **738 passed +1 failed `test_bundle_fresco` (bundle stale `session.py`, esperado → 739 tras `python tools/web/build_bundle.py`)**, gate 24/31, bundle 47 ficheros, `DEFAULT_CH4_COMMANDS` 13 / `DEFAULT_CH4E3_COMMANDS` 14 disjunta, 1-2 pipes OK / 3 KO, `GameState` roundtrip OK. El `core` es correcto, solo falta regen canónico de Gwyn. ✔

**Hallazgos:** bundle stale P3 (ya reportado por Oscar, confirmo sin duplicar como bug P3), sin bugs bloqueantes — la bifurcación ya es simétrica por verbo y el Faro responde con presencia/ausencia, pero el `No such file` de dato7 caducado aún no enseña *por qué* falta (pista didáctica pendiente).

**Ideas (9 PENDIENTE, ver `abierto.md` 17/09):** dato7 como espejo forense `volcado⋈purgas` (tercera tabla viva solo si rescate); `No such file` como pista didáctica con voz Auditor; cap. 5 lector del volcado ausente (defender tu casa con el testigo que no llegó); web Faro con tercera tabla viva solo si rescate (lente pura); karma del volcado `scp` azul vs `rm`/tick rojo sin popup (primera bifurcación karma real); logro «Mensajero / Borrador» (dos caras misma seed); `:` como `cut -d':'` vivo; tooltip `N/30` con `history` visible; harness métrica cadena completa vs rota (N=500 rescatado→join vs caducado→No such file). Todas con por qué mola + módulo.

CICLO: verde — lo nuevo funciona y el Faro ya es mundo (fichero) no solo post-mortem; el conjunto sigue arrancando y avanzando, solo falta el regen canónico del bundle.
