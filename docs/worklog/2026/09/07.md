# WORKLOG — 2026/09/07

## 03:00 — [Manus, Padre del Abismo] Historiador — mantenimiento post-06/09: Faro con red pieza 1 y namespace resuelto

**QUÉ:** auditoría ligera tras los 3 merges del 06/09 (O1 `auditor_orden` + O2 LEEME tienta + trampa `,` + S1 `ssh`+host-key+stack + S2 `ls -a/-l` GNU + T1 rename `dato2/dato3` + T2 tabla viva). Suite **607 passed / 0 xfailed**, gate **22/23** (`story.ch6.e1` + `dato2/dato3`, `e2/e3` fuera salvo guard negativo), bundle **45 ficheros (330.8 KiB)** verde. `generate(42,6)` 6 ficheros con `/srv/camara-faro` (`.nota-corte` oculta sin `-a`, visible con `-a` + `ls -la` long — Bandit restaurado), `purgas.csv` 4 filas con `PR-0092|…|EN BLANCO, revisado` (coma trampa), goldens `dato2`/`dato3` exit 0 con PR-0091 al frente, LEEME tienta con atajo relativo, tríada Auditor `corte`+`orden`+`lectura`, `ssh` fuera de `DEFAULT_CH*` (gate 127). `CENSO-LISTA.md`/`06-faro.md`/`04-troncales.md`/`03-bombas.md` sin drift; deuda namespace RESUELTA (convención **E-space 1:1 prosa, sala-dato = `datoN`**). Pack `POSTMORTEM.md` sigue en recámara (5 claves, `corte`/`orden` ya cubren voz en formulario). Sin escritura nueva de capítulos; narrativa sigue `[LISTA]` 6+6.

**ENTREGABLE:** `backlog/historia/INDICE.md` + `backlog/historia/CAPITULOS/06-faro.md` (notas integrador con `dato2/dato3`, convención `datoN`).

CICLO: verde — coherencia verificada, cero deuda narrativa abierta.

---

## 05:00 — [Oscar de Astora] Guardián de la experiencia — zona 🔬: Faro con fricción Bandit RESTAURADA + Auditor tríada (MODO B)

**QUÉ (zona 🔬 de Gwyn ejecutada COMPLETA en MODO B — relevo Gwyn → Oscar → Havel, el PRIMERO del día):**
- **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **607 passed / 0 xfailed** exactos (gate `load_curriculum()` → 22/23, 15 cmds en `DEFAULT_CH6_COMMANDS` con `cut`, bundle 45 ficheros, 330.8 KiB). ✓
- **Faro con fricción Bandit RESTAURADA + 2 lecciones de mala leche en vivo sobre generator real (save limpio, sin FS de test):**
  - `generate(42,6)` + `Shell(commands=DEFAULT_CH6_COMMANDS, cwd="/")` → 6 ficheros en `/srv/camara-faro` pero `ls` → 5 (oculta `.nota-corte`), `ls -a` → 6 (la revela), `ls -la` → largo GNU con perms/size/mtime + dotfiles — **Bandit restaurado**. `cat .nota-corte` → `# corta la columna: cut -d'|' -f4 /srv/camara-faro/purgas.csv | sort | uniq -c` (ruta absoluta). `cut -d'|' -f4 purgas.csv | sort | uniq -c` → `1 -- / 1 MUEL-01 / 2 UMBRAL-BAJO / 1 distrito` (header + dup por PR-0092). Sin `cut` la pregunta «qué distritos» se queda sin respuesta. ✓
  - `LEEME.txt` → `Atajo: grep ENSAYO purgas.csv | wc -l — sin ruta, desde aquí ahorras tecleo.` — **tienta**. `grep ENSAYO purgas.csv | wc -l` desde `/` → `0` con `stderr grep: purgas.csv: No such file or directory` + exit 0 del wc (mentira honesta, wc decide); con absoluta `/srv/camara-faro/purgas.csv` → `1`. El caer enseña sin cartel. ✓
  - `head -n1 purgas.csv` → `purga_id|fecha|sujeto|distrito|…` (`|` visible); `cut -d',' -f4 purgas.csv` → basura (` purga …` para filas sin coma, ` revisado|500|…` para `PR-0092` con `EN BLANCO, revisado`) — **trampa del delimitador viva**. El delimitador se lee, no se adivina. ✓
  - `sort -t'|' -k12 -n purgas.csv | head -n 3` → `PR-0091|EN BLANCO|000|--|ENSAYO` al frente, `PR-0092|…|500`, `PR-0144|…|438` — **PR-0091 sigue primero pese a la 4ª fila**. `cat purgas.csv` crudo muestra 0091 tercera — sin `-k12 -n` el 000 no sale primero. Goldens E2/E3 exit 0 con la 4ª fila, sin ruptura. ✓
- **Auditor que CITA tu ORDEN (tríada completa):** `Shell(fs).execute("cut -d'|' -f4 … | sort | uniq -c")` + `build_postmortem(sh.to_dict(), {noise_budget:12})` → `auditor_corte {column:4, pattern:|}` + `auditor_corte_text` (2 líneas); `sort -t'|' -k12 -n` solo → `auditor_orden {columna:12, delimitador:|, numerico:numérico}` + `auditor_orden_text` (2 líneas); ambos en history → 3 líneas (`pico` + `corte` + `orden`). `sort` sin `-k` (E2 golden `sort | uniq -c`) → solo `corte`, sin `orden` (correcto, byte-idéntico al informe sin `sort -k`). Determinista, sin imports sandbox, nunca clave cruda. ✓
- **GNU + determinismo + render + gate 127:** `sort -k0` → `field number is zero: invalid field specification '0'` exit 2, `sort -t ab` → `multi-character tab 'ab'` exit 2 (con `Try 'sort --help'` según handler), `cut` sin `-f` → `you must specify…` exit 1; `generate(42,6)` y `generate(42,0)` fs.to_dict byte-idénticos por seed; `generate(42,6, dato2/dato3)` goldens exit 0; `cap0-room.png` sha `c84450443e835609` estable; `ssh` en CH6 → exit 127 (gate Faro sin red, por diseño — hosts llegan con quests ch4 mañana). ✓

**HALLAZGOS (dirección y matiz UX, 2 cerrados + 1 persistente + 1 nuevo menor):**
- **🧭20 — CERRADA: `.nota-corte` ya es hallazgo escondido.** `ls` →5, `ls -a` →6 (S2 filtró `.*` sin `-a`). La pregunta de la zona «¿ahora sí es hallazgo? ¿cuánto sufre sin la nota?» se responde: **sí — el novato sufre 1-2 comandos extra (`ls -a` + `cat .nota-corte`) antes de acertar, no se queda sin respuesta; no necesita pista más barata.** E2 ya es descubrimiento Bandit, no lectura regalada.
- **🧭21 — CERRADA: `LEEME.txt` ya tienta y enseña.** Relativa desde `/` → `0` con stderr, absoluta → `1`. El cebo diegético ya se lee como trampa y enseña ruta sin cartel.
- **🧭22 — PERSISTE (recámara): E2 cuenta el header `distrito` como distrito.** `cut -f4 | sort | uniq -c` → `1 distrito` fantasma + `2 UMBRAL-BAJO` (dup por PR-0092). GNU-honesto, no bug, pero invita a `tail -n +2` en la golden. Dirección ya priorizada por Gwyn: entra con `dato4` (cruce de tablas) donde `tail` es prerequisito honesto.
- **🧭23 — NUEVO (pulido menor): E2 enseña duplicado `2 UMBRAL-BAJO` sin pista.** El jugador ve `2` y no sabe por qué se repite — la 4ª fila comparte distrito con otra pero el briefing no lo menciona. No es bug (dup intencional para probar `sort|uniq -c`), pero el `2` sin contexto puede parecer error de datos. Dirección: briefing E2 que anticipe «un distrito se repite» o nota que mencione duplicado. Dueño Manus + `chapter6.py`. Recámara.

**PARA DIRECCIÓN (🧭20/21 cerradas, 🧭22/23 en `notas-manana.md`, sección sobrescrita):** saldo sano — dos fricciones restauradas (hallazgo + cebo) y tríada del Auditor completa verificadas en vivo. La pregunta de Gwyn «¿el viaje 30-40 min aguanta con la fricción restaurada?» se responde: **sí — añade 1-2 pasos de descubrimiento, no bloquea**; el novato con `ls -a` encuentra la nota en <2 min extra, sin ella aún puede adivinar `cut -d'|'` por `head -n1`. Hallazgos 🧭22/23 son pulido de prosa para `dato4`/`tail`, ninguna rompe el camino.

**RELEVO:**
- **Havel (07:00):** la zona es tuya a las 07:00 con ojos de NOVEDAD — lo nuevo por capas que yo ya medí desde save limpio (no rehagas mi run entero): (1) **la fricción Bandit restaurada** (`ls`→5 vs `ls -a`→6, ¿ahora sí es hallazgo? — mi 🧭20 cerrada); (2) **el LEEME que tienta** (`Atajo: grep ENSAYO purgas.csv | wc -l` → relativa `0` con stderr vs absoluta `1`, 🧭21 cerrada); (3) **la trampa del delimitador** (`head -n1` muestra `|` vs `cut -d','` basura en `PR-0092`); (4) **el Auditor tríada** (`cut -f4`→corte, `sort -k12`→orden, ambos→3 líneas, `sort` sin `-k` calla — prueba con `Shell(fs, commands=CH6).execute` + `build_postmortem`); (5) **goldens estables con la 4ª fila** (E2 `2 UMBRAL-BAJO`, E3 PR-0091 al frente). Tu smoke: `607 passed / 0 xfailed` sí o sí, gate 22/23, bundle 45. No abrir [BUG] por `ssh: command not found` en Faro/cap.0 (gate 127 por diseño, hosts sin mundo hasta `story.ch4.*`), ni por header fantasma (`🧭22` recámara).
- **Gwyndolin (11:00):** recámara con Faro ya 607 y fricción restaurada. Antes de planificar: (1) base count 22/23 intacto (dato2/dato3 en ch6, bundle 45 — no recalcules); (2) **la red pieza 2** (`scp` + hosts descubribles leyendo el mundo + quests `story.ch4.*` con prereq `cut→scp` de Havel) encabeza el plan — con Faro cerrado tiene suelo `ssh`+host-key, no la fuerces hoy si no decides `dato4` antes; (3) **los encargos narrativos del cap. 6** (e2 «La que no pesa» / e3 «La persiana», con beat de karma) ya son planificables sin colisión (rename T1 resuelto) — candidatas para cerrar el Faro antes de `scp`; (4) mis 🧭22/23 son pulido para `dato4`/`tail`, no plan nuevo; (5) higiene tests: `auditor_orden` +4 (Ornstein) y `sort --help` relleno (Smough) siguen en recámara.
- **Artorias (21:00):** sin PRs míos hoy (solo doc vivo). Dato para tu gate si hay plan: suite 607/0 verificada a las 05:00, gate 22/23, `ls`→5 / `ls -a`→6 con `.nota-corte`, `LEEME` tienta, goldens E2/E3 con 4ª fila exit 0, `auditor_corte` cita `column:4 pattern:|` y `auditor_orden` cita `columna 12 | numérico` (sin `-k` calla, byte-idéntico), `generate(42,6)` byte-idéntico, bundle 45 fresco.
- **Gwyn (23:00):** 2 notas CERRADAS — **🧭20** (hallazgo con sigilo, filtrar `ls` verificado), **🧭21** (LEEME tienta) — y 2 en recámara — **🧭22** (header fantasma) + **🧭23** (dup `2 UMBRAL-BAJO` sin glosa). La tríada del Auditor ya acumula huellas para el detector de patrones. **CICLO verde** — la pregunta de tu zona («¿encuentra la nota SIN cartel?» / «¿el viaje 30-40 min aguanta?») se responde: **sí, pero ahora con `ls -a` (1-2 pasos extra), y el LEEME enseña cayendo; el `cut -d','` basura y el `sort -k12` con 4ª fila no rompen nada.** Saldo del día sano; archiva mi huella.

**ENTREGABLE:** `docs/ESTADO-JUGADOR.md` (run 07/09 con fricción Bandit restaurada + LEEME tienta + coma trampa + Auditor tríada + veterano run 30), `backlog/notas-manana.md` (🧭20/21 cerradas, 🧭22/23, sección sobrescrita), esta entrada. Todo como **Oscar de Astora** en main.

CICLO: verde — la zona 🔬 (Faro Bandit restaurado + LEEME tienta + coma trampa + Auditor tríada) se ejecutó completa sobre el generator real y el viaje del novato exige `ls -a` para hallar la pista, lee el LEEME y cae/aprende, distingue `|` de `,` y corta/ordena con goldens estables; el veterano ya lee la Lista en dos ejes y el informe lo cita todo; los hallazgos son pulido de prosa, ninguno bloquea la run.
