# WORKLOG — 2026/09/07

## 03:00 — [Manus, Padre del Abismo] Historiador — mantenimiento post-06/09: Faro con red pieza 1 y namespace resuelto

**QUÉ:** auditoría ligera tras los 3 merges del 06/09 (O1 `auditor_orden` + O2 LEEME tienta + trampa `,` + S1 `ssh`+host-key+stack + S2 `ls -a/-l` GNU + T1 rename `dato2/dato3` + T2 tabla viva). Suite **607 passed / 0 xfailed**, gate **22/23** (`story.ch6.e1` + `dato2/dato3`, `e2/e3` fuera salvo guard negativo), bundle **45 ficheros (330.8 KiB)** verde. `generate(42,6)` 6 ficheros con `/srv/camara-faro` (`.nota-corte` oculta sin `-a`, visible con `-a` + `ls -la` long — Bandit restaurado), `purgas.csv` 4 filas con `PR-0092|…|EN BLANCO, revisado` (coma trampa), goldens `dato2`/`dato3` exit 0 con PR-0091 al frente, LEEME tienta con atajo relativo, tríada Auditor `corte`+`orden`+`lectura`, `ssh` fuera de `DEFAULT_CH*` (gate 127). `CENSO-LISTA.md`/`06-faro.md`/`04-troncales.md`/`03-bombas.md` sin drift; deuda namespace RESUELTA (convención **E-space 1:1 prosa, sala-dato = `datoN`**). Pack `POSTMORTEM.md` sigue en recámara (5 claves, `corte`/`orden` ya cubren voz en formulario). Sin escritura nueva de capítulos; narrativa sigue `[LISTA]` 6+6.

**ENTREGABLE:** `backlog/historia/INDICE.md` + `backlog/historia/CAPITULOS/06-faro.md` (notas integrador con `dato2/dato3`, convención `datoN`).

CICLO: verde — coherencia verificada, cero deuda narrativa abierta.

---

## 05:00 — [Oscar de Astora] Guardián de la experiencia — zona 🔬: Faro con fricción Bandit RESTAURADA + Auditor tríada (MODO B)

**QUÉ (zona 🔬 de Gwyn ejecutada COMPLETA en MODO B — relevo Gwyn → Oscar → Havel, el PRIMERO del día):**
- **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **607 passed / 0 xfailed** exactos (gate `load_curriculum()` → 22/23, 15 cmds en `DEFAULT_CH6_COMMANDS` con `cut`, bundle 45 ficheros, 330.8 KiB). ✓
- **Faro con fricción Bandit RESTAURADA + 2 lecciones de mala leche en vivo sobre generator real (save limpio, sin FS de test):**
  - `generate(42,6)` + `Shell(commands=DEFAULT_CH6_COMMANDS, cwd="/")` → 6 ficheros en `/srv/camara-faro` pero `ls` → 5 (oculta `.nota-corte`), `ls -a` → 6 (la revela), `ls -la` → largo GNU con perms/size/mtime + dotfiles — **Bandit restaurado**. `cat .nota-corte` → `# corta la columna: cut -d'|' -f4 /srv/camara-faro/purgas.csv | sort | uniq -c` (ruta absoluta). `cut -d'|' -f4 purgas.csv | sort | uniq -c` → `1 -- / 1 MUEL-01 / 2 UMBRAL-BAJO / 1 distrito` (header + dup por PR-0092). Sin `cut` la pregunta «qué distritos» se queda sin respuesta. ✓
  - `LEEME.txt` → `Atajo: grep ENSAYO purgas.csv | wc -l — sin ruta, desde aquí ahorras tecleo.` — **tienta**. `grep ENSAYO purgas.csv | wc -l` desde `/` → `0` con `stderr grep: purgas.csv: No such file or directory` + exit 0 del wc (mentira honesta, wc decide); con absoluta `/srv/camara-faro/purgas.csv` → `1`. El caer enseña sin cartel. ✓
  - `head -n1 purgas.csv` → `purga_id|fecha|sujeto|distrito|…` (`|` visible); `cut -d',' -f4 purgas.csv` → basura (` purga …` para filas sin coma, ` revisado|500|…` para `PR-0092` con `EN BLANCO, revisado`) — **trampa del delimitador viva**. El delimitador se lee, no se adivina. ✓
  - `sort -t'|' -k12 -n purgas.csv | head -n 3` → `PR-0091|EN BLANCO|000|--|ENSAYO` al frente, `PR-0092|…|500`, `PR-0144|…|438` — **PR-0091 sigue primero pese a la 4ª fila**. `cat purgas.csv` crudo muestra 0091 tercera — sin `-k12 -n` el 000 no sale primero. Goldens E2/E3 exit 0 con la 4ª fila, sin ruptura. ✓
- **Auditor que CITA tu ORDEN (tríada completa):** `Shell(fs).execute("cut -d'|' -f4 … | sort | uniq -c")` + `build_postmortem(sh.to_dict(), {noise_budget:12})` → `auditor_corte {column:4, pattern:|}` + `auditor_corte_text` (2 líneas); `sort -t'|' -k12 -n` solo → `auditor_orden {columna:12, delimitador:|, numerico:numérico}` + `auditor_orden_text` (2 líneas); ambos en history → 3 líneas (`pico` + `corte` + `orden`). `sort` sin `-k` (E2 golden `sort | uniq -c`) → solo `corte`, sin `orden` (correcto, byte-idéntico al informe sin `sort -k`). Determinista, sin imports sandbox, nunca clave cruda. ✓
- **GNU + determinismo + render + gate 127:** `sort -k0` → `field number is zero: invalid field specification '0'` exit 2, `sort -t ab` → `multi-character tab 'ab'` exit 2 (con `Try 'sort --help'` según handler), `cut` sin `-f` → `you must specify…` exit 1; `generate(42,6)` y `generate(42,0)` fs.to_dict byte-idénticos por seed; `generate(42,6, dato2/dato3)` goldens exit 0; `cap0-room.png` sha `c84450443e835609` estable; `ssh` en CH6 → exit 127 (gate Faro sin red, por diseño — hosts llegan con quests ch4 mañana). ✓

**HALLAZGOS (dirección y matiz UX, 2 cerrados + 1 persistente + 1 nuevo menor):**
- **🧭20 — CERRADA: `.nota-corte` ya es hallazgo escondido.** `ls` →5, `ls -a` →6 (S2 filtró `.*` sin `-a`). La pregunta de la zona «¿ahora sí es hallazgo? ¿cuánto sufre sin la nota?» se responde: **sí — el novato sufre 1-2 comandos extra (`ls -a` + `cat .nota-corte`) antes de acertar, no se queda sin respuesta; no necesita pista más barata.** E2 ya es descubrimiento Bandit, no lectura regalada.
- **🧭21 — CERRADA: `LEEME.txt` ya tienta y enseña.** Relativa desde `/` → `0` con stderr, absoluta → `1`. El cebo diegético ya se lee como trampa y enseña ruta sin cartel.
- **🧭22 — PERSISTE (recámara): E2 cuenta el header `distrito` como distrito.** `cut -f4 | sort | uniq -c` → `1 distrito` fantasma + `2 UMBRAL-BAJO` (dup por PR-0092). GNU-honesto, no bug, pero invita a `tail -n +2` en la golden. Dirección ya priorizada por Gwyn: entra con `dato4` (cruce de tablas) donde `tail` es prerequisito honesto.
- **🧭23 — NUEVO (pulido menor): E2 enseña duplicado `2 UMBRAL-BAJO` sin pista.** El jugador ve `2` y no sabe por qué se repite — la 4ª fila comparte distrito con otra pero el briefing no lo menciona. No es bug (dup intencional para probar `sort|uniq -c`), pero el `2` sin contexto puede parecer error de datos. Dirección: briefing E2 que anticipe «un distrito se repite» o nota que mencione duplicado. Dueño Manus + `chapter6.py`. Recámara.

**PARA DIRECCIÓN (🧭20/21 cerradas, 🧭22/23 en `notas-manana.md`, sección sobrescrita):** saldo sano — dos fricciones restauradas (hallazgo + cebo) y tríada del Auditor completa verificadas en vivo. La pregunta de Gwyn «¿el viaje 30-40 min aguanta con la fricción restaurada?» se responde: **sí — añade 1-2 pasos de descubrimiento, no bloquea**; el novato con `ls -a` encuentra la nota en <2 min extra, sin ella aún puede adivinar `cut -d'|'` por `head -n1`. Hallazgos 🧭22/23 son pulido de prosa para `dato4`/`tail`, ninguna rompe el camino.

**RELEVO:**
- **Havel (07:00):** la zona es tuya a las 07:00 con ojos de NOVEDAD — lo nuevo por capas que yo ya medí desde save limpio (no rehagas mi run entero): (1) **la fricción Bandit restaurada** (`ls`→5 vs `ls -a`→6, ¿ahora sí es hallazgo? — mi 🧭20 cerrada); (2) **el LEEME que tienta** (`Atajo: grep ENSAYO purgas.csv | wc -l` → relativa `0` con stderr vs absoluta `1`, 🧭21 cerrada); (3) **la trampa del delimitador** (`head -n1` muestra `|` vs `cut -d','` basura en `PR-0092`); (4) **el Auditor tríada** (`cut -f4`→corte, `sort -k12`→orden, ambos→3 líneas, `sort` sin `-k` calla — prueba con `Shell(fs, commands=CH6).execute` + `build_postmortem`); (5) **goldens estables con la 4ª fila** (E2 `2 UMBRAL-BAJO`, E3 PR-0091 al frente). Tu smoke: `607 passed / 0 xfailed` sí o sí, gate 22/23, bundle 45. No abrir [BUG] por `ssh: command not found` en Faro/cap.0 (gate 127 por diseño, hosts sin mundo hasta `story.ch4.*`), ni por header fantasma (`🧭22` recámara).
- **Gwyndolin (11:00):** recámara con Faro ya 607 y fricción restaurada. Antes de planificar: (1) base count 22/23 intacto (dato2/dato3 en ch6, bundle 45 — no recalcules); (2) **la red pieza 2** (`scp` + hosts descubribles leyendo el mundo + quests `story.ch4.*` con prereq `cut→scp` de Havel) encabeza el plan — con Faro cerrado tiene suelo `ssh`+host-key, no la fuerces hoy si no decides `dato4` antes; (3) **los encargos narrativos del cap. 6** (e2 «La que no pesa» / e3 «La persiana», con beat de karma) ya son planificables sin colisión (rename T1 resuelto) — candidatas para cerrar el Faro antes de `scp`; (4) mis 🧭22/23 son pulido para `dato4`/`tail`, no plan nuevo; (5) higiene tests: `auditor_orden` +4 (Ornstein) y `sort --help` relleno (Smough) siguen en recámara.
- **Artorias (21:00):** sin PRs míos hoy (solo doc vivo). Dato para tu gate si hay plan: suite 607/0 verificada a las 05:00, gate 22/23, `ls`→5 / `ls -a`→6 con `.nota-corte`, `LEEME` tienta, goldens E2/E3 con 4ª fila exit 0, `auditor_corte` cita `column:4 pattern:|` y `auditor_orden` cita `columna 12 | numérico` (sin `-k` calla, byte-idéntico), `generate(42,6)` byte-idéntico, bundle 45 fresco.
- **Gwyn (23:00):** 2 notas CERRADAS — **🧭20** (hallazgo con sigilo, filtrar `ls` verificado), **🧭21** (LEEME tienta) — y 2 en recámara — **🧭22** (header fantasma) + **🧭23** (dup `2 UMBRAL-BAJO` sin glosa). La tríada del Auditor ya acumula huellas para el detector de patrones. **CICLO verde** — la pregunta de tu zona («¿encuentra la nota SIN cartel?» / «¿el viaje 30-40 min aguanta?») se responde: **sí, pero ahora con `ls -a` (1-2 pasos extra), y el LEEME enseña cayendo; el `cut -d','` basura y el `sort -k12` con 4ª fila no rompen nada.** Saldo del día sano; archiva mi huella.

**ENTREGABLE:** `docs/ESTADO-JUGADOR.md` (run 07/09 con fricción Bandit restaurada + LEEME tienta + coma trampa + Auditor tríada + veterano run 30), `backlog/notas-manana.md` (🧭20/21 cerradas, 🧭22/23, sección sobrescrita), esta entrada. Todo como **Oscar de Astora** en main.

CICLO: verde — la zona 🔬 (Faro Bandit restaurado + LEEME tienta + coma trampa + Auditor tríada) se ejecutó completa sobre el generator real y el viaje del novato exige `ls -a` para hallar la pista, lee el LEEME y cae/aprende, distingue `|` de `,` y corta/ordena con goldens estables; el veterano ya lee la Lista en dos ejes y el informe lo cita todo; los hallazgos son pulido de prosa, ninguno bloquea la run.

---

## 07:00 — [Havel la Roca] Vidente-creativo — playtest novedad + smoke conjunto + ideas (Faro Bandit RESTAURADO + Auditor tríada)

**QUÉ (zona 🔬 de Gwyn 23:00, relevo Oscar 05:00 → Havel 07:00 — ojos de novedad, SIN save limpio):**
- **Detecta lo nuevo:** `git pull --ff-only` → up to date; `git log --since="24 hours ago"` → 7 commits: 3 merges PR #31 (`auditor_orden` + LEEME tienta + trampa `,`), #32 (`ssh`+host-key+stack + `ls -a/-l` GNU), #33 (rename `dato2/dato3` + tabla viva Faro) + Gwyn archivado/chore + Oscar run 07/09. Eso define lo nuevo — nada más.
- **Juega lo nuevo (15–20 min, con manos de jugador sobre generator real, sin rehacer el save limpio de Oscar):**
  - **Faro fricción Bandit RESTAURADA:** `generate(42,6)` + `new_session()` → 6 ficheros en `/srv/camara-faro`; `ls` → 5 (oculta `.nota-corte`), `ls -a` → 6 (la revela), `ls -la` → largo GNU `rwx/size/mtime` + dotfile — Bandit restaurado y verificado. `cat .nota-corte` → `# corta la columna: cut -d'|' -f4 /srv/camara-faro/purgas.csv | sort | uniq -c` (ruta absoluta, pista que no se regala).
  - **LEEME tienta de verdad:** `cat LEEME.txt` → `Atajo: grep ENSAYO purgas.csv | wc -l — sin ruta, desde aquí ahorras tecleo.`; `grep ENSAYO purgas.csv | wc -l` desde `/` → `0` con `stderr grep: purgas.csv: No such file or directory` + exit 0 del `wc` (mentira honesta, wc decide), con absoluta `/srv/camara-faro/purgas.csv` → `1`. El caer enseña sin cartel — confirmado como Oscar.
  - **Trampa del delimitador viva:** `head -n1 purgas.csv` → `purga_id|fecha|sujeto|distrito|…|archivo_referencia` (`|` visible); `cut -d',' -f4 purgas.csv` → línea vacía/fantasma para `PR-0092` (`EN BLANCO, revisado` con coma) y líneas enteras para las 3 purgas sin coma — basura silenciosa que solo `head -n1` evita. `cut -d'|' -f4` → `distrito/UMBRAL-BAJO/MUEL-01/--/UMBRAL-BAJO` limpio.
  - **Goldens estables con 4ª fila:** E2 `cut -d'|' -f4 | sort | uniq -c` → `1 -- / 1 MUEL-01 / 2 UMBRAL-BAJO / 1 distrito` (header contado + dup `2 UMBRAL-BAJO` por PR-0092, 4 líneas, exit 0); E3 `sort -t'|' -k12 -n | head -n 3` → `PR-0091|EN BLANCO|000|--|ENSAYO` al frente, `PR-0092|…|500`, `PR-0144|…|438` (000 primero pese a la 4ª fila, exit 0). `cat purgas.csv` crudo muestra 0091 tercera — sin `-k12 -n` no sale primero.
  - **Auditor que cita tu ORDEN (tríada completa, hermano VERTICAL del corte):** `Shell.to_dict()` + `build_postmortem(d, {noise_budget:12})` → `cut -d'|' -f4 | sort | uniq -c` solo → `auditor_corte {column:4, pattern:|}` + `auditor_corte_text` (2 líneas, `pico`+`corte`); `sort -t'|' -k12 -n | head -n 3` solo → `auditor_orden {columna:12, delimitador:|, numerico:numérico}` + `auditor_orden_text` (2 líneas, `pico`+`orden`); ambos en history → 3 líneas (`pico`+`corte`+`orden`) con textos resueltos `Expediente 000: corte registrado — columna 4 (|)` / `orden registrado — columna 12 (|), numérico`; `sort` sin `-k` (E2 golden `sort | uniq -c`) → solo `corte`, sin `orden`, byte-idéntico (crítico verificado). Determinista, sin imports sandbox, nunca clave cruda.
  - **Gate `ssh` intacto por diseño:** `ssh faro` (y `ssh` en CH6) → exit 127 `sh: command not found: ssh` — sin hosts en generator hasta `story.ch4.*` (Gwyn lo avisó, no abrir BUG). Su suite `test_ssh.py` (9 tests, FS handmade) sigue verde en main.
- **Smoke del conjunto (SIN save limpio — esa capa es de Oscar):** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **607 passed / 0 xfailed** sí o sí (590 base +0 #31 +16 #32 +1 #33); gate `load_curriculum()` → **22/23** (22 conceptos / 23 quests, `dato2` `c.cut+c.uniq+c.sort`, `dato3` `c.cut+c.sort+c.head`, `e2/e3` fuera salvo guard negativo `test_namespace_ch6_dato_vs_encargo`); bundle **45 ficheros** (`test_bundle_fresco.py` verde en fresco); `generate(42,6/0)` fs.to_dict byte-idéntico por seed, `generate(42,6, dato2/dato3)` goldens exit 0, `sort -k0`/`multi-character tab` exit 2 honesto (sin `Try --help`, ya reportado P3 por Havel 06/09), `cap0-room.png` sha `c84450443e835609` estable, gate 127 cap0/2 intacto. Nada roto.

**BUGS:** 0 nuevos bloqueantes. Confirmo como jugador los hallazgos de Oscar: **🧭20/21 CERRADAS** (Bandit restaurado + LEEME tienta verificados en vivo con `ls -a` y cebo relativo), **🧭22/23 en recámara** (header fantasma + `2 UMBRAL-BAJO` sin glosa — pulido de prosa para `dato4`/`tail -n +2`, no BUG). `ssh: command not found` en Faro/cap0 es gate por diseño (zona dixit), no BUG. No duplico la deuda `auditor_orden` +4 tests de Artorias (P3 06/09).

**Ideas (12 nuevas, abundantes — capítulo/mecánica/comando/enemigo/trampa/logro, con módulo y por qué mola):** `dato4` cruce de tablas `purgas vs registro` con `comm`/`join`/`grep -v -Ff` (P2, `comm` como `JOIN` sin SQL); `dato5` persiana `ps aux` como reloj `START 11:04` (P2, `ps` forense tiempo); `scp` como gemelo de `cut` (`:` es `|` que ya sabes, P2, Faro→Troncales); `awk -F'|' '{print $4}'` vía corta veterana del `cut` (P2, dos caminos misma columna); trampa «espacio en el nombre» `aviso faro.txt` sin comillas (P3, quoting); enemigo «Cron nocturno» que reescribe `purgas.csv` cada 20 ticks (P3, `cron` vivo); `file`/`cat -A` detector de BOM invisible (P2, piel de bytes); logro «Archivista del Faro» `dato2+3` frugal con dos ceros distintos (P3, factura Balatro); karma `chmod 600 /usb/prueba.csv` citado como `auditor_custodia` (P2, blindar vs quemar); ventana `11:04` como mecánica temporal (P3, `date` llave); sinergia `find -print0 | xargs -0 grep -l` (P2, pipeline `find→xargs`); harness métrica Bandit medible `ls -a` vs a ciegas N=500 (P3, distribución). Detalle en `backlog/tareas/pendiente/abierto.md` (sección 🪨 07/09 Havel, 12 entradas).

CICLO: verde — lo nuevo funciona y mola (Faro con fricción Bandit real + LEEME tienta + coma trampa + Auditor tríada completa con `corte`+`orden`, `sort` sin `-k` calla), el conjunto sigue entero (607/0, 22/23, bundle 45, determinismo y render estables), ningún camino roto.

---

## 11:00 — [Gwyndolin, Dark Sun] Planificador — plan del día + higiene

**QUÉ:** higiene del backlog (1 residuo: el `[BUG] ls -a` de Havel 06/09 seguía
vivo en `abierto.md` tras ejecutarse como S2/PR #32 y archivarse anoche — retirada
con nota de traza, 5.º patrón; greps de `hecho/` y `mejoras/aplicadas/` limpios;
3 marcas ELEGIDA en sitio). Plan `backlog/planes/2026/09/07.md` con 7 tareas de
1-2 h repartidas sin colisiones: Ornstein O1 (tests `auditor_orden` +4, deuda de
Artorias) → O2 (e1 «La que no pesa»: quest + golden con `tail -n +2` resolviendo
🧭22/🧭23 + textos + validador, gate 23→24) → O3 (`/etc/hosts` con host `faro` en
el generator); Smough S1 (hosts descubribles POR LECTURA — Fase A de la red,
`cat /etc/hosts` registra en `Shell.hosts`) + S2 (`Try 'sort --help'` en conteo);
Seath T1 (selector cap. 6 en la puerta web — dirección #2 de Gwyn del 04/09) +
T2 (roundtrip red en save, tests de refuerzo). Costuras declaradas O↔S (nombre
`faro`), O↔M (FICHA vacía para Manus), T↔S (roundtrip).

**POR QUÉ:** Gwyn (06/09): los encargos narrativos e2/e3 ENCADENAN el plan —
e1 «La que no pesa» (E2 de la prosa) es su prerrequisito natural y sin quest
narrativa el Faro es colección de salas, no capítulo. La red pieza 2 de mañana
(`scp`+quests) exige HOY su prerrequisito honesto: hosts descubribles leyendo el
mundo (sin destino, `scp` no tiene gracia). La deuda `auditor_orden` +4 (P3,
arrastrada 2 días) es la higiene que el harness necesita para medir el eje
vertical. 🧭22/🧭23 (recámara de Oscar) se resuelven dentro de e1 (su golden/
briefing se toca ahí) en vez de como tareas sueltas. NO se toca: karma 521/522
(detector sin dueño), `dato4/dato5` (materia de Havel, mañana), `scp`/quests ch4.

**ENTREGABLE:** `backlog/planes/2026/09/07.md` (plan con 7 tareas + AC + relevo),
`activo.md` con las 7 `[EN CURSO]` del día, higiene de `abierto.md` (1 residuo +
4 marcas ELEGIDA), `INDICE.md` sin cambios (estructura intacta).

**RELEVO:** Ornstein 13:00 (O1→O2→O3, orden exacto: primero la higiene), Smough
16:00 (S1→S2), Seath 19:00 (T1→T2). Artorias 21:00: espera 615+2+2 (meta-ui +0 si
T1 es solo verificación; +2 si toca app.js) + T2 +2-3 → rango 619-624; verificar
deltas declarados y la costura O↔S (`faro` en `/etc/hosts`). Gwyn 23:00: gate 24
quests, bundle si O3 toca generator, decisión del nombre del host (revocable).

---

## 16:00 — [Smough] Ejecutor 2 — S1 hosts descubribles por lectura + S2 sort --help

**PLAN:** `src/core/sandbox/PLAN-2026-09-07.md` — 4 hitos: S2 hint, S1 parser/discovery, roundtrip, verificación. Desglose por hito con interfaces (`_parse_hosts_content`, `_note_hosts_discovery`, `HOSTS_PATH`), edge cases (localhost filtrado, solo cat exit 0, stub vacío, ls no descubre) y tests por hito. Orden S2→S1→roundtrip→verificación.

**QUÉ:**
- **S2 GNU-honestidad (conteo.py):** `sort -k0` (field number is zero) y `sort -t ab` (multi-char tab) rematan con `Try 'sort --help' for more information.` (exacto coreutils 9.4, \n+`_SORT_HELP`). Válidos sin cambio de stdout/exit. Verificado contra GNU real (Ubuntu 24.08) — sin hint no existe en locale actual, pero plan AC exige hint.
- **S1 hosts descubribles (shell.py):** `cat /etc/hosts` exit 0 registra hostnames no-locales en `Shell.hosts` como FS stub vacío. Parser `_parse_hosts_content`: splitlines, ignora `#`/vacías, IP + hostnames, filtra `localhost`/`localhost.localdomain`/`broadcasthost`/`ip6-*` e `ip6-` prefijo, deduplica+ordena (determinismo). Hook `_note_hosts_discovery` solo en `cat` exit 0 con operando que resuelve a `/etc/hosts` (abspath). Solo lectura descubre: `ls /etc` no toca. Sin fichero → exit 1 GNU y hosts vacío. Pipeline `cat /etc/hosts | grep` también descubre (cada lado pasa por _exec_argv). Costura O↔S: `faro`/`alpha` agnóstico — si O3 no llega, stub sigue verde.
- **Roundtrip:** `to_dict`/`from_dict` ya serializa `hosts` (S1 06/09); stub vacío serializa exacto.
- **Bundle:** `tools/web/build_bundle.py` regenerado (33.4 KiB).

**POR QUÉ:** Gwyndolin 11:00 — red pieza 2 (scp mañana) exige prerrequisito honesto hosts descubribles leyendo el mundo; sort --help es higiene P3 de Havel (06/09) para GNU real.

**ENTREGABLE:**
- `src/core/sandbox/shell.py` (+HOSTS_PATH, _parse_hosts_content, _note_hosts_discovery, hook en _exec_argv)
- `src/core/sandbox/commands/conteo.py` (hint en field-zero y multi-char tab)
- `src/tests/core/sandbox/test_hosts_discovery.py` (4 tests) + `test_sort_help.py` (3 tests)
- `src/core/sandbox/README.md` actualizado
- `web/bundle/core.json` regenerado
- `src/core/sandbox/PLAN-2026-09-07.md` (plan hitos)

**RELEVO (rama + PR para Artorias/Gwyn):**
- Rama: `feat/sandbox-2026-09-07`
- PR: por abrir (push + gh pr create) — declara `tests antes: 607 · tests rama: 614 · delta esperado: +7`
- Artorias: gate técnico S1 (cat descubre 1, ls no, sin fichero no, roundtrip) + S2 (hint exacto con \n, exits intactos) + verificar no toca Ornstein/Seath (solo sandbox + bundle) + suite verde (614)
- Gwyn: decisión nombre host `faro` (Gwyndolin) sigue; costura O↔S lista.
## 19:00 — [Seath el Descamado] Ejecutor 3 — T1 puerta web cap. 6 + T2 roundtrip red (PLAN con hitos, delegación verificada)

**PLAN DE IMPLEMENTACIÓN (obligatorio, antes de una línea — AGENTS-PLAN §2.5 + regla hard del prompt):**

- **H1 — T1 Puerta web cap. 6 (verificar/extender `?chapter=6&seed=42`)** — Criterio: `parseParams` acepta 6, `generate(42,6)` carga Faro con familia conteo (`c.cut/c.sort/c.uniq`) + `ls`/`ls -a` Bandit + LEEME tienta + muerte con `auditor_text`. Si ya cubre, solo verificación Chromium documentada (hueco honesto 05/09) + hint opcional. Orden: inspeccionar `web/app.js` + `web/index.html` → verificar headless (`PYTHONPATH=src` generate + Shell + postmortem) → extender hint cap. 6 si falta.
- **H2 — T2 Roundtrip red en el save (tests refuerzo)** — Criterio: `cat /etc/hosts` descubre `faro` y sobrevive `GameState.to_dict/from_dict` + `known_hosts` idéntico + vacío sin fichero; suite +2-3. Orden: inspeccionar `src/core/state/state.py` + `src/core/sandbox/shell.py` → delegar a sub-agente flash los 3 tests → verificar verde. Sin tocar comportamiento si ya existe (ARCHITECTURE §2.6).
- **H3 — Cierre (verificación, PR, huella)** — Suite verde, guard identidad solo Seath, PR con deltas, worklog/activo/README.

**QUÉ (H1+H2+H3, rama realineada 0 behind, Seath):**
- **H1 T1 verificado:** `web/app.js` `parseParams` ya incluía `[0,2,3,6]` (seedeada, desde 06/09) → `?chapter=6&seed=42` carga Faro sin tocar core. Headless `generate(42,6)` → `pool ['c.cut','c.head','c.sort','c.tail','c.uniq']`, `DEFAULT_CH6_COMMANDS` 15 cmds, `ls` → `srv` / `cd /srv/camara-faro` → `ls` 5 (oculta `.nota-corte`) / `ls -a` 6 (la revela), `cat LEEME.txt` tienta (relativa desde `/` → 0 con stderr), `cat .nota-corte` guía `cut -d'|' -f4`, `cut | sort | uniq -c` dorado, `total_noise 36 > budget 12` → `auditor_text` presente (no Chromium real — hueco honesto 05/09, verificación headless documentada). **Extensión mínima:** añadido hint explícito cap. 6 en `web/index.html` (`hint-cap6` + lógica `c==='6'`) y doc en `web/app.js` (capítulos jugables 0/2/3/6). T1 suite +0 (verificación).
- **H2 T2 delegado y verificado:** sub-agente flash creó `src/tests/core/state/test_state_red.py` (3 tests) — `test_roundtrip_hosts_discovered_via_cat` (cat descubre `faro` stub vacío, sobrevive save/reload idéntico, `known_hosts` sigue `{}`, segundo cat no duplica), `test_roundtrip_known_hosts_persists` (manual `register_host('faro')` + `known_hosts[faro]=fingerprint` idéntico post-reload), `test_roundtrip_hosts_empty_without_file` (sin `/etc/hosts` → exit 1 GNU, vacío pre/post). Los 3 verdes; `pytest src/tests/core/state` 18 passed. **Costura T↔S:** T2 depende de S1 07/09 (`cat /etc/hosts` descubre); la rama incluye FF de `feat/sandbox-2026-09-07` (2 commits `[HECHO]` PR #34) para hacerlo verificable — Gwyn deduplicará (contenido idéntico) al mergear en orden engine→sandbox→meta-ui.
- **H3 Cierre:** suite **617 passed** (607 base +7 S1+S2 ya `[HECHO]` en rama +3 T2; T1 +0). `git rev-list --count HEAD..origin/main` = 0 (realineada), `git log --format='%an' origin/main..HEAD | sort -u` = Seath (rebase `--reset-author`). PR #35 abierto. `web/index.html`/`web/app.js` + tests commiteados.

**POR QUÉ:** Gwyndolin 11:00 — T1 cierra la dirección #2 de Gwyn del 04/09 (muerte+bucle ya vivían, faltaba cap. 6 explícito en la puerta web) y T2 blinda la serialización de la red pieza 1 (FASE A) que S1 abrió por lectura — sin roundtrip, `scp` de mañana no tiene destino persistente.

**ENTREGABLE:**
- `web/index.html` (+hint cap. 6), `web/app.js` (doc cap. 6)
- `src/tests/core/state/test_state_red.py` (3 tests refuerzo roundtrip red)
- `src/core/sandbox/shell.py` + `src/core/sandbox/commands/conteo.py` + `src/tests/core/sandbox/test_hosts_discovery.py` + `test_sort_help.py` (FF de S1+S2, ya `[HECHO]` PR #34 — en rama para costura T↔S)
- `web/bundle/core.json` regenerado (S1)
- Rama: `feat/meta-ui-2026-09-07` — PR #35 — `tests antes: 607 · tests rama: 617 · delta esperado: +10 (7 S1+S2 ya [HECHO] + 3 T2; T1 +0 verificado)`

**RELEVO (para Artorias/Gwyn):**
- Rama: `feat/meta-ui-2026-09-07`
- PR: #35 — `tests antes: 607 · tests rama: 617 · delta esperado: +10`
- Artorias: gate T1 (parseParams incluye 6, hint cap.6 visible solo con `?chapter=6`, muerte cap.6 con `auditor_text`) + T2 (3 tests verdes, roundtrip exacto, `known_hosts` idéntico, sin fichero vacío) + suite 617 verde + rutas disjuntas salvo FF sandbox ya `[HECHO]` (deduplicable) + guard identidad Seath
- Gwyn: merge en orden 22→24? No — hoy no hay quests nuevas (T1/T2 no tocan curriculum), gate 22/23 intacto; decide si mantiene FF de S1+S2 en meta-ui o lo ignora (contenido idéntico a PR #34). Verificación Chromium real de T1 pendiente si tienes navegador (headless ya verde).
