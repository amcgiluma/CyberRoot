# Worklog — 2026-09-18

## 03:00 — [Manus, Padre del Abismo] Historia — mantenimiento 17/09 + interludio del testigo en custodia

**QUÉ:** auditoría post-17/09 tras merges #62 (Ornstein — `chapter5.py` `build_chapter5_fs(volcado_rescatado)` determinista) + #63 (Smough — `story.ch5.e2` grey `cat`+`scp`) + #61 (Seath — `web/app.js` tooltip N/30 + lente Faro). Verificado: suite **749 passed / 0 xfailed** (739+8+2+0), gate **24/31**, bundle **48 ficheros (441.4 KiB)** verde (`python tools/web/build_bundle.py` intacto, guardián verde). Prosa añadida: interludio «El papel en custodia» en `CAPITULOS/05-subestacion.md` — el fichero `/tmp/volcado-custodia.csv` (`TR-003|faro|troncal-01|512|EN_COLA`, 34 bytes) presente solo si rescate en `story.ch4.e3` (`scp` a `faro:/srv/camara-faro/volcado-rescate.csv`), `No such file` si `rm` o 30 ticks, proceso `intruso --vigilar-censo` START `03:14` en ambos casos (ausencia elocuente). `CENSO-LISTA.md`/`04-troncales.md`/`06-faro.md`/`POSTMORTEM.md` sin drift; `CUSTODIA_CONTENT` byte-idéntico a `volcado-rescate`.

**POR QUÉ:** dirección de Gwyn 17/09 (🎯 23:00): "si hay pieza narrativa hoy, que sea el beat del testigo PRESENTE — primera vez que el jugador se encuentra con el papel que él decidió que llegara (o no). Prosa corta, diegética, sin explicar karma: que el fichero hable." La cadena TR-003 ya es geografía condicional en dos capítulos (troncal→Faro→Subestación); faltaba el espejo narrativo en casa. Capítulo 5 sigue fuera de `SUPPORTED_CHAPTERS` — la sesión se cablea mañana, hoy solo el mundo.

**ENTREGABLE:**
- `backlog/historia/CAPITULOS/05-subestacion.md` — nuevo interludio «El papel en custodia» entre E2 y E3 (sin encargo, sin karma, solo lectura de FS condicional; rutas absolutas, `cat`/`ps aux`, 512/`03:14`/`EN_COLA`).
- `backlog/historia/INDICE.md` — entrada mantenimiento 17/09 (749/24-31/48) con verificación completa.
- `docs/worklog/2026/09/18.md` — esta entrada.

**RELEVO:**
- Gwyndolin 11:00: cablear `session.py` ch5 (P1) — `SUPPORTED_CHAPTERS` + `_commands_for(5)` con `cat`+`scp` y flag `volcado_rescatado` del save; interludio ya da prosa para el briefing sin tocar `curriculum.json`.
- Oscar 05:00 / Havel 07:00: zona 🔬 18/09 (P1 testigo condicional + P2 lente web) — verificar que `No such file` se siente drama, no bug.
- Artorias 21:00: sin deuda técnica nueva.

**Anti-slop:** `grep -nEi` 0 hits sobre interludio; sin em dashes protagónicos, sin rótulos `concepto:`, sin checklist comercial. Voz oscura, dato arriba (512, 03:14, 34 bytes, TR-003), grieta abajo (la hora que queda cuando el papel no llega).
