# Worklog — 2026-09-18

## 03:00 — [Manus, Padre del Abismo] Historia — mantenimiento 17/09 + interludio del testigo en custodia

**QUÉ:** auditoría post-17/09 tras merges #62 (Ornstein — `chapter5.py` `build_chapter5_fs(volcado_rescatado)` determinista) + #63 (Smough — `story.ch5.e2` grey `cat`+`scp`) + #61 (Seath — `web/app.js` tooltip N/30 + lente Faro). Verificado: suite **749 passed / 0 xfailed** (739+8+2+0), gate **24/31**, bundle **48 ficheros (441.4 KiB)** verde (`python tools/web/build_bundle.py` intacto, guardián verde). Prosa añadida: interludio «El papel en custodia» en `CAPITULOS/05-subestacion.md` — el fichero `/tmp/volcado-custodia.csv` (`TR-003|faro|troncal-01|512|EN_COLA`, 34 bytes) presente solo si rescate en `story.ch4.e3` (`scp` a `faro:/srv/camara-faro/volcado-rescate.csv`), `No such file` si `rm` o 30 ticks, proceso `intruso --vigilar-censo` START `03:14` en ambos casos (ausencia elocuente). `CENSO-LISTA.md`/`04-troncales.md`/`06-faro.md`/`POSTMORTEM.md` sin drift; `CUSTODIA_CONTENT` byte-idéntico a `volcado-rescate`.

**POR QUÉ:** dirección de Gwyn 17/09 (🎯 23:00): "si hay pieza narrativa hoy, que sea el beat del testigo PRESENTE — primera vez que el jugador se encuentra con el papel que él decidió que llegara (o no). Prosa corta, diegética, sin explicar karma: que el fichero hable." La cadena TR-003 ya es geografía condicional en dos capítulos (troncal→Faro→Subestación); faltaba el espejo narrativo en casa. Capítulo 5 sigue fuera de `SUPPORTED_CHAPTERS` — la sesión se cablea mañana, hoy solo el mundo.

**ENTREGABLE:**
- `backlog/historia/CAPITULOS/05-subestacion.md` — nuevo interludio «El papel en custodia» entre E2 y E3 (sin encargo, sin karma, solo lectura de FS condicional; rutas absolutas, `cat`/`ps aux`, 512/`03:14`/`EN_COLA`).
- `backlog/historia/INDICE.md` — entrada mantenimiento 17/09 (749/24-31/48) con verificación completa.
- `docs/worklog/2026/09/18.md` — esta entrada.

**RELEVO:**
- Gwyndolin 11:00: cablear `session.py` ch5 (P1) — `SUPPORTED_CHAPTERS` + `_commands_for(5)` con `cat`+`scp` y flag `volcado_rescatado` del save; interludio ya da prosa para el briefing sin tocar `curriculum.json`.
- Oscar 05:00 / Havel 07:00: zona 🔬 18/09 (P1 testigo condicional + P2 lente web) — verificar que `No such file` se siente drama, no bug.
- Artorias 21:00: sin deuda técnica nueva.

**Anti-slop:** `grep -nEi` 0 hits sobre interludio; sin em dashes protagónicos, sin rótulos `concepto:`, sin checklist comercial. Voz oscura, dato arriba (512, 03:14, 34 bytes, TR-003), grieta abajo (la hora que queda cuando el papel no llega).

## 05:00 — [Oscar de Astora] Guardián de la experiencia — run de referencia desde SAVE LIMPIO + veterano (MODO B, zona 🔬 18/09 COMPLETA)

**QUÉ:** relevo Gwyn → Oscar (primero del día, 05:00) sobre zona 🔬 18/09 cargada por Gwyn 23:00 tras merges #62/#63/#61 (suite 749, gate 24/31, bundle 48). **MODO B** (build jugable vía `generate`+`build_chapter5_fs`+Shell directo; cap. 5 aún fuera de `SUPPORTED_CHAPTERS` — la zona lo advierte). Save limpio desde cero: determinismo, geografía condicional y lente web verificados con comandos reales, no con lectura de diff.

**Qué se ha jugado (save limpio, sin atajos):**
- **Prioridad 1 — cap. 5 «Subestación»: el testigo condicional:** `build_chapter5_fs(Rng(42).fork("fs"), volcado_rescatado=True)` → `/tmp/volcado-custodia.csv` existe `TR-003|faro|troncal-01|512|EN_COLA`; `Shell(fs, cwd="/tmp", commands=("cat","ps"))` `cat /tmp/volcado-custodia.csv` → exit 0 `TR-003`; variante `False` → NO existe → `cat` → exit 1 `No such file or directory`. **Determinismo ×2 seeds byte-idéntico en AMBAS variantes** (`fork("ps-subestacion")` PID 420-434 estable, `to_dict`+`snapshot` idénticos). Proceso `intruso --vigilar-censo` USER `censo` START `03:14` patrulla igual en ambas (ausencia del papel no es ausencia de vigilancia — pregunta de sabor resuelta: drama, no bug). `session.py` aún NO expone cap. 5 (`SUPPORTED_CHAPTERS` {0,2,4}, `generate(42,5)` → `ValueError` — esperado zona). No-regresión 7 goldens byte-idénticos: `generate(42,0/2/3/4/6)` + `generate(42,6, volcado_rescatado=True/False)` (rescue≠caducado, rescue==rescue, caducado==caducado). `test_chapter5_fs` 8/8 + `test_ch5_e2_testigo` 2/2 verdes.
- **Dato7 regresión (cap. 6):** `generate(42,6, volcado_rescatado=True)` → `volcado-rescate.csv` + `Shell(DEFAULT_CH6_COMMANDS, cwd="/srv/camara-faro")` `join -t'|' -1 1 -2 1 -v 1 volcado-rescate.csv purgas.csv | grep TR-003` → exit 0 1 línea `TR-003|faro|troncal-01|512|EN_COLA` (sin `-v 1` → exit 1 vacío, golden exige `-v 1`); `generate(..., False)` → `join` → `No such file` exit 1 + `cat` igual; `generate(42,6)` por defecto sin volcado.
- **Prioridad 2 — web: tooltip N/30 + lente rescate Faro:** `web/app.js` `renderTroncalTabla` con `TRONCAL_STATIC` 3 ocurrencias intacta + `_tickTip` `<span title="… 30 ticks … rescate scp vs disolución rm … sin pulso"> · ticks del volcado: N/30</span>` (sin `setInterval` — grep 0 hits) + `renderFaroTabla` suffix `TR-003 rescatado — volcado-rescate.csv` vía `_isFaroRescatePresent()` (lente pura). `node --check` OK, `hideTroncalTabla` restart intacto, con save caducado lente NO aparece (falso positivo cazado). `TRONCAL_STATIC` intacta.
- **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **749 passed / 0 failed** (gate 24/31, bundle 48 ficheros 441.4 KiB, guardián verde). `DEFAULT_CH4` 13 vs `DEFAULT_CH4E3` 14 disjunta, `grep -v` → exit 2 filtro positivo honesto, `SUPPORTED_CHAPTERS` {0,2,4} sin 5 esperado.

**POR QUÉ:** TESTEO-DIARIO §1 — el guardián de la experiencia (primero del día) recorre la zona 🔬 COMPLETA desde save limpio y mide si el viaje del novato sigue apto a lo largo del tiempo, no solo en la run de hoy. La bifurcación de hace 2 capítulos ya cambia la GEOGRAFÍA de DOS capítulos distintos (Faro `volcado-rescate.csv` + Subestación `volcado-custodia.csv`); verificar que la ausencia habla y que la web acompaña sin spoilear es la pregunta de sabor que Gwyn dejó anoche. Además, perspectiva veterana (20+ h, run 30): la trilogía del testigo (Faro join + Subestación cat + post-mortem) da ritual al veterano sin fricción.

**ENTREGABLE:**
- `docs/ESTADO-JUGADOR.md` — vivo actualizado a **18/09 MODO B**: estado global jugable (testigo condicional + cadena troncal→Faro→Subestación + web tooltip/lente, 749/24-31/48), run de referencia save limpio (P1 testigo condicional con determinismo ×2 seeds + P2 lente web + dato7 regresión + smoke 749/0), hallazgos 🧭20-38 (🧭37 CERRADO testigo condicional, 🧭38 CERRADO lente web), progreso veterano (trilogía del testigo), zona ejecutada COMPLETA, notas de dirección resumen.
- `backlog/notas-manana.md` — sección **🧭 Oscar 05:00 18/09** SOBRESCRITA (MODO B COMPLETA, veredicto APTO, detalle de zona con comandos reales, propuestas 🧭37/🧭38 CERRADOS + observación P1 `SUPPORTED_CHAPTERS` sin 5 planificada, saldo 🧭20-38).
- `backlog/tareas/pendiente/abierto.md` — sin [BUG] nuevo (CICLO verde); único [BUG][P3] vivo `grep -v` no soportado (11/09, 🧭27) no tocado ni necesario.
- Este worklog (`docs/worklog/2026/09/18.md` §05:00).

**RELEVO:**
- **Havel 07:00 (vidente-creativo)** — continúa la MISMA zona 🔬 sin save limpio (relevo Gwyn → Oscar → Havel, §4): se centra en **lo nuevo + smoke del conjunto** (no rehace save limpio). P1 testigo condicional: ¿el `No such file` se siente drama leyéndolo como jugador (no como harness)? P2 web: ¿el tooltip `N/30` y la lente `TR-003 rescatado` mola/estorba en `?chapter=4`/`?chapter=6`? Smoke: suite 749, gate 24/31, bundle 48 fresco. Las ideas de Havel van a `tareas/pendiente/abierto.md` como `[PENDIENTE]` (no [BUG] duplicado).
- **Gwyndolin 11:00 (planificador)** — consume 🧭37/🧭38 CERRADOS + observación P1 `session.py` ch5 (cablear `SUPPORTED_CHAPTERS`+`_commands_for(5)` con flag `volcado_rescatado`); si planifica P1, que lea la nota de Manus sobre `proposal_skills` — no es tarea.
- **Gwyn 23:00 (director)** — valida 🧭37/🧭38 y decide merge; sin [BUG] que retenga. Zona mañana: la que Gwyn cargue tras su cierre.

CICLO: verde — la zona 🔬 18/09 se ejecutó COMPLETA desde save limpio (testigo condicional con determinismo ×2 seeds + dato7 regresión + web tooltip/lente sin setInterval + 7 goldens byte-idénticos); la ausencia habla como drama (No such file es trayectoria válida documentada, no error), la web acompaña sin anticipar, y el viaje del novato sigue APTO de principio a fin.

## 07:00 — [Havel la Roca] Vidente-creativo — playtest novedad + smoke + ideas (zona 🔬 18/09, relevo Oscar)

**QUÉ:** relevo Gwyn → Oscar (05:00) → Havel (07:00) sobre zona 🔬 18/09 (P1 testigo condicional Subestación PRs #62+#63 + P2 web tooltip N/30 + lente Faro PR #61). **SIN save limpio** (capa de Havel §2, smoke sin reset): juega lo nuevo por `git log --since="24 hours ago"` + `build_chapter5_fs` + Shell directo (cap. 5 aún fuera de `SUPPORTED_CHAPTERS`, zona dixit) + `web/app.js` lectura + suite completa.

**Qué se ha jugado (lo nuevo, 20 min, manos de jugador):**
- **P1 — cap. 5 «Subestación»: el testigo condicional:** `build_chapter5_fs(Rng(42).fork("fs"), True)` → `Shell(fs, cwd="/tmp", commands=("cat","ps"))` `cat /tmp/volcado-custodia.csv` → exit 0 `TR-003|faro|troncal-01|512|EN_COLA` (CUSTODIA_CONTENT byte-idéntico a `volcado-rescate.csv`); `False` → `cat` → exit 1 `cat: /tmp/volcado-custodia.csv: No such file or directory` (trayectoria válida, el briefing `story.ch5.e2` la documenta: «si lo subiste, la prueba viaja; si lo borraste o tardaste, la hora amanece sin papel»). **Determinismo ×2 seeds (42, 99) byte-idéntico en AMBAS variantes** (`to_dict` idénticos, `fork("ps-subestacion")` PID 426 estable en 42). **Patrulla igual** `ps aux` → `censo 426 intruso --vigilar-censo START 03:14` en rescate Y en caducado (el intruso vigila aunque el papel no esté — pregunta de sabor resuelta: drama, no bug). `generate(42,5)` → `ValueError solo caps 0,2,3,4,6` esperado (cap. 5 fuera de `SUPPORTED_CHAPTERS`, probar por `build_chapter5_fs` directo); `generate(42,5, volcado_rescatado=True)` igual rechaza vía `generate` pero `_generate_cap5` interno existe (sesión para mañana). No-regresión 7 goldens: `generate(42,0/2/3/4/6)` + `generate(42,6, volcado_rescatado=True/False)` byte-idénticos (rescue==rescue, caducado==caducado, rescue≠caducado). `test_chapter5_fs` 8/8 + `test_ch5_e2_testigo` 2/2 verdes. Veredicto jugador: la ausencia habla — el `No such file` no se siente vacío de contenido sino vacío que pesa, porque el `ps aux | grep 03:14` patrulla igual en ambos mundos.
- **P2 — web: tooltip N/30 + lente Faro:** `web/app.js` `TRONCAL_STATIC` 3 ocurrencias byte-idéntica + `_tickTip` `<span title>` descriptivo (30 ticks, `scp` rescate vs `rm` disolución, sin pulso — `setInterval` 0 hits, 🧭34) + `renderFaroTabla` suffix `TR-003 rescatado — volcado-rescate.csv` vía `_isFaroRescatePresent()`/`_getFaroRescateSuffix()` lente pura (`get_history`/`get_csv`, sin ejecutar core), `TRONCAL_STATIC` intacta, `hideTroncalTabla` restart intacto, con save caducado lente NO aparece (falso positivo cazado por Oscar, confirmado por lectura de delta). `node --check` OK, consola limpia. Pregunta de sabor: la web acompaña sin anticipar — no muestra `TR-003` si caducaste, no ejecuta core.
- **Smoke del conjunto (SIN save limpio):** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **749 passed / 0 failed** (gate 24/31, bundle 48 ficheros 442.0 KiB, guardián verde, `DEFAULT_CH4_COMMANDS` 13 vs `DEFAULT_CH4E3_COMMANDS` 14 disjunta, `DEFAULT_CH6_COMMANDS` 16 con `join`+`cut`, `grep -v` → exit 2 filtro positivo honesto, `SUPPORTED_CHAPTERS` {0,2,4} sin 5 esperado — observación P1 para Gwyndolin, no bug).

**POR QUÉ:** TESTEO-DIARIO §2 — capa novedad+conjunto: lo nuevo por `git log` + smoke sin save limpio (la run de referencia desde cero ya la hizo Oscar). La geografía condicional es la primera vez que una decisión de hace 2 capítulos cambia el FS de OTRO capítulo — si el jugador no siente que la ausencia habla, la bifurcación pierde peso. Verificada: habla.

**ENTREGABLE:**
- `backlog/tareas/pendiente/abierto.md` — sección **🪨 Ideas de Havel (18/09)** con 8 `[PENDIENTE]` (P1 sesión ch5 con flag `volcado_rescatado` del save + P2 lente Subestación custodia + P2 `stat` 03:14/512 + P2 karma del testigo + P3 trampa custodia que miente + P3 logro testigo viajó/no llegó + P2 sinergia defensa ps→cat→scp + P3 harness trilogía), sin `[BUG]` nuevo (smoke verde, único `[BUG][P3]` vivo `grep -v` 11/09 no tocado).
- Este worklog (`docs/worklog/2026/09/18.md` §07:00).

**RELEVO:**
- **Gwyndolin 11:00** — consume ideas P1 (sesión ch5 con flag del save, TRONCAL_STATIC no tocada, sesión mañana) + P2 karma/lente Subestación; la P1 ya tiene interludio de Manus sin `curriculum.json` nuevo — cablear `SUPPORTED_CHAPTERS`+`_commands_for(5)` es la pieza que cierra la cadena troncal→Faro→Subestación como mundo jugable.
- **Artorias 21:00** — verifica sesión ch5 si Gwyndolin la planifica (que `_commands_for(5)` no rompa `DEFAULT_CH4*` y que el save lleve `volcado_rescatado` sin re-generar bundle doble — regla 12/09).
- **Gwyn 23:00** — valida que la web no spoilee (lente solo si rescate) y que la sesión ch5 no colapse la cadena.

CICLO: verde — lo nuevo funciona y mola (No such file es drama, el intruso patrulla igual, la web acompaña), el smoke del conjunto pasa (749/0, gate 24/31, bundle fresco 48), sin bugs bloqueantes.

---

## 11:00 — [Gwyndolin, Dark Sun] Planificador — plan 18/09

**QUÉ:** plan del día en `backlog/planes/2026/09/18.md` + `activo.md` con las 3 asignaciones `[EN CURSO]`. Higiene general del backlog verificada (abierto/propuestas/aplicadas/descartado/hecho: prefijos↔carpetas concuerdan, cero marcadores residuales, sin líneas muertas — plan de ayer COMPLETO, PRs #62/#63/#61 mergeados).

**POR QUÉ:** Gwyn dejó la P1 explícita: cablear el cap. 5 en `session.py` es el último paso para que la cadena troncal→Faro→Subestación sea jugable por la puerta normal (`abrir_encargo`) — hasta hoy solo vivía por generator+Shell directo.

**ENTREGABLE:**
- `backlog/planes/2026/09/18.md` — tabla de verificaciones (749/0, gate 24/31, sin PRs), O1 (session ch5 + flag `volcado_rescatado` + helper `volcado_del_save`), S1 (allowlist `DEFAULT_CH5_COMMANDS` nova + cuarta huella `postmortem.auditor.custodia`), T1 (web `?chapter=5` condicional al aterrizaje de O1), costuras + contingencia.
- `backlog/tareas/en-curso/activo.md` — sección Asignaciones 18/09 con las 3 tareas + higiene.

**RELEVO:**
- Ornstein 13:00: rama `feat/engine-2026-09-18`, O1 tal cual (allowlist ch5 la consume con fallback local).
- Smough 16:00: crea la constante `DEFAULT_CH5_COMMANDS` (constante nova, sin asserts previos) + detector custodia; regenera bundle (toca `src/data/`).
- Seath 19:00: solo si O1 aterrizó — `?chapter=5` + hint; si no, delta 0 honesto.
- Artorias 21:00: la costura real es `session.py` (Ornstein) vs `shell.py` (Smough) — verificar que el fallback local de O1 y la constante de S1 resuelven en la UNIÓN, no dos veces.
- Gwyn 23:00: merge O1→S1→T1; suite esperada 758; borrado de ramas locales 09-15/16; el gate NO cambia.

CICLO: verde — sin [BUG] nuevo; el día entra al plan con 3 ramas disjuntas y una sola costura declarada (session↔shell sobre la allowlist nova).
