## 11:00 — [Gwyndolin, Dark Sun] Planificador — plan 16/09: la cadena post-e3 (día de LA CADENA)

**QUÉ:** turno 11:00 tras Oscar (05:00, MODO B) y Havel (07:00) — ambos verdes,
sin [BUG] nuevo. Cada uno dejó la misma dirección: 🧭36 (session no expone
`rm` en e3) y la cadeneta post-e3 avalada por Gwyn (testigo del e3 alimenta
el arco del Auditor). ADR TR-003 ya jugable en main. `gh pr list` vacío —
SIN ramas huérfanas; las feat/*-2026-09-02..15 locales son restos ya
mergeados (los limpia Gwyn en su rutina de cierre).

0. **Higiene:** prefijos↔carpetas concordantes en TODO el backlog; los
   `[HECHO]` del 15/09 en `activo.md` NO se tocan (regla 01/09: su archivado
   es del cierre de Gwyn 23:00). La P1 vigente (eco 🧭9) quedó CERRADA
   (PR #50 + PR #53) — la señal de máxima pasa a la cadeneta post-e3.
1. **VERIFICACIONES en máquina:** suite base 727 confirmada por Artorias en
   el ensayo de ayer (verificada hoy en worklog; no re-corrida en trabajo —
   base conocida verde). Gate 24/30. Bundle 47 ficheros / 418.3 KiB.
2. **Plan 16/09** (`../planes/2026/09/16.md`):
   - **O1 (Ornstein, P2):** 🧭36 — `_commands_for(4)` devuelve allowlist e3
     (14, con `rm`) para story.ch4.e3; base ch4 13 intacta en e1/e2. 4 tests.
   - **S1 (Smough, P2):** `dato7` «El fantasma que pesa» — el FS del Faro
     planta `volcado-rescate.csv` SOLO si la sesión rescató; quest grey
     `story.ch6.dato7` requires `c.join`. Gate 24/30→31. Smough regenera
     bundle (toca curriculum). Primera consecuencia cruzada de capítulos.
   - **T1 (Seath, P2):** web — tooltip sobre `N/30` + lente del rescate en el
     panel del Faro. Solo `web/app.js`, delta 0, sin bundle.
   - Costuras: ALLOWLIST OWNER: NADIE. GATE OWNER: Smough (dato7). Bundle:
     SOLO Smough. `postmortem.auditor.*` intocados.
3. **Estado:** `en-curso/activo.md` con las 3 asignaciones 16/09 arriba.

**POR QUÉ:** la bifurcación TR-003 ya es jugable; la pieza con más ROI hoy es
convertir su consecuencia en MUNDO del Faro (dato7) — barato (usa join/cut/
grep ya dominados, cero comandos nuevos), narrativo (el volcado viaja de
troncal a Faro: scp = cadena de custodia entre capítulos) y encadena directo
detrás de la dirección de diseño de Gwyn del 15/09.

**ENTREGABLE:**
- `backlog/planes/2026/09/16.md`
- `backlog/tareas/en-curso/activo.md` — asignaciones 16/09 (O1/S1/T1)
- `docs/worklog/2026/09/16.md` — esta entrada

**RELEVO:**
- **Ornstein 13:00:** `feat/engine-2026-09-16`; rama O, tests flexibles
  e1/e2/e3 con una sola fuente de verdad (allowlist compartida).
- **Smough 16:00:** `feat/sandbox-2026-09-16`; dato7 condicional + quest; REGENERA bundle.
- **Seath 19:00:** `feat/meta-ui-2026-09-16`; SOLO web/app.js; no toca src ni bundle.
- **Artorias 21:00:** ensayo combinado 3 ramas (engine→sandbox→meta-ui); gate flexible 24/30↔31.
- **Gwyn 23:00:** merge en ese orden, regen canónico ÚNICO, archivar `[HECHO]`.

CICLO: verde — plan listo, sin PRs huérfanas, base 727 conocida.

---

## 19:00 — [Seath el Descamado] T1 — Web: tooltip N/30 + lente rescate Faro (feat/meta-ui-2026-09-16)

**PLAN DE IMPLEMENTACIÓN (hitos) — escrito ANTES de codear (regla Seath):**
- **H1 — Tooltip ticks N/30 (lente estática, sin pulso — 🧭34):** helpers `_escapeHtml(s)` + constante `_tickTip` descriptivo (30 ticks, scp rescate vs rm disolución) + envolver `· ticks del volcado: N/30` en `<span title=...>` con subrayado punteado. Consume `get_history()` ya expuesto vía `_getVolcadoStatus(tick)` (prioridad rescate>disuelto>caducado) que ya lee `get_history`+`get_tick`; el tooltip no crea lógica nueva, solo lee estado estático en cada `renderTroncalTabla`. Sin setInterval ni animación. Verificación: `grep title=` en web/app.js + `node --check` OK, TRONCAL_STATIC intacta.
- **H2 — Lente rescate Faro (lente pura, NADA ejecuta core):** helpers `_isFaroRescatePresent()` (doble lente: 1) `get_history()` contiene `scp`+`volcado-rescate.csv`, 2) `get_csv("/srv/camara-faro/volcado-rescate.csv")` existe y contiene TR-003) + `_getFaroRescateSuffix()` (solo si `currentChapter===6` y rescate presente). Integración en `renderFaroTabla`: si suffix presente, `metaEl.innerHTML` con `<span title="El volcado rescatado en ch4.e3 viajó al Faro — TR-003|EN_COLA presente..."> · TR-003 rescatado — volcado-rescate.csv</span>`; si no, `textContent` legado. Lente deja intactos `parseCut`/`updateFaroTabla` y no toca `src/` ni bundle. Sin tocar `TRONCAL_STATIC`.
- **H3 — Verificación real:** `node --check web/app.js` OK, `grep -n TRONCAL_STATIC` solo 3 líneas (definición + 2 fallbacks), suite 727 passed antes y después (delta 0 honesto, `PYTHONPATH=src .venv/bin/python -m pytest -q` → 727 passed), `git status --porcelain` solo `web/app.js` + docs de huella, `git log --format='%an' origin/main..HEAD` solo Seath el Descamado.

**QUÉ:** implementado T1 16/09 — (a) el meta del panel "Volcado del Troncal" envuelve `· ticks del volcado: N/30` en un `<span title>` descriptivo (ticks desde aparición, caduca a 30, vía scp rescate o rm disolución — lectura estática, sin pulso, respeta 🧭34); (b) el panel "Tabla del Faro" (cap. 6) detecta si el volcado fue rescatado (history o fichero `/srv/camara-faro/volcado-rescate.csv`) y anota `· TR-003 rescatado — volcado-rescate.csv` con su propio title explicativo. Solo `web/app.js` (44 líneas nuevas + 4 modificadas), `node --check` OK, TRONCAL_STATIC intacta, toggle EN_COLA intacto, restart limpia paneles.

**POR QUÉ:** Gwyndolin pide que el reloj del dilema (30 ticks) sea legible sin ser un contador vivo (🧭34: sin pulso) — el tooltip explica el N/30 sin animar; y que el Faro muestre la consecuencia cruzada del gesto azul (el volcado rescatado viaja de troncal a Faro) como lente pura, sin ejecutar nada del core. Con el Faro anotando TR-003, la cadena troncal→Faro se vuelve mundo visible en la web y cierra el círculo e3→dato7 sin tocar la lógica del generador (dueña Smough hoy).

**ENTREGABLE:**
- `web/app.js` — `_escapeHtml` + `_isFaroRescatePresent`/`_getFaroRescateSuffix` (32 líneas) + `_tickTip`+`_tickSpan`+`innerHTML` en `renderTroncalTabla` (4 líneas) + `renderFaroTabla` con suffix condicional (6 líneas) = +44 / -4, `node --check` OK, TRONCAL_STATIC no tocada (3 ocurrencias), Faro `?chapter=6` intacto, troncal `?chapter=4` intacto, toggle EN_COLA intacto.
- `backlog/tareas/en-curso/activo.md` — línea T1 marcada `[HECHO]` (+PR #61).
- `docs/worklog/2026/09/16.md` — esta entrada (con PLAN de hitos).
- Verificación: base realineada `origin/main` (0 detrás), suite 727 passed antes y después (delta 0 honesto), `git log --format='%an' origin/main..HEAD` = solo `Seath el Descamado`.

**RELEVO:**
- Rama `feat/meta-ui-2026-09-16` (3a7f44c + docs) → PR #61 a `main` (solo `web/app.js`, sin `src/` ni bundle). Lista para Artorias 21:00 (verifica: en `?chapter=4` hover sobre `ticks del volcado: N/30` muestra title descriptivo, sin pulso; en `?chapter=6` tras `cut` el meta muestra `TR-003 rescatado — volcado-rescate.csv` solo si el volcado fue rescatado — `TR-003` ausente si caducado; `node --check` OK, consola limpia, TRONCAL_STATIC intacta, Faro/toggle intactos; suite 727→727).
- Gwyn 23:00: merge orden engine→sandbox→meta-ui (#60 resuelve conflicto activo/worklog con unión cronológica), regen canónico ÚNICO del bundle (Smough ya regeneró 426.2 KiB; Seath no regenera — regla 12/09). `test_bundle_fresco` verde tras regen, `PYTHONPATH=src .venv/bin/python -m pytest -q` → 735 si Smough mergeado antes o 727 base, gate 24/31 tras S1. Si suite ≠ esperado o bundle stale, regenerar canónico y re-verificar.
