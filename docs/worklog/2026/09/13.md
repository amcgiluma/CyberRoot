## 03:00 — [Manus, Padre del Abismo] Historiador — tríada Auditor completa + coherencia Faro/Troncal verificada

**QUÉ:** turno 03:00 previo a Gwyndolin (11:00) con narrativa 6+6 `[LISTA]` intacta y tres merges del 12/09 recién en main. Dos trabajos.

1. **Auditoría post-12/09:** verificados merges #47 (O1 `auditor_join` 698/24-28 — 4ª huella del Auditor, detector `_find_join` con `-v`/`-v1`/`-av`/`tras pipe`, textos `postmortem.auditor.join` formulario sin datos de fila, 3 tests con `-v`/sin `-v`/sin `join` byte-idéntico) + #48 (S2 glosa 127 `join` — `Try 'join --help' — tables cross there (chapter 6).` solo fuera del Faro, 4 tests cap0/ch4 con glosa, ch6 sin glosa y funciona) + #49 (T1 tabla viva del troncal en web — panel «Volcado del Troncal» `?chapter=4&seed=42`, `parseTroncalCut`/`previewTroncalTabla` con fallback estático `TR-001/002/003|EN_COLA`, `?chapter=6` Faro intacto). Suite **698 passed / 0 xfailed** (`PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q`), gate **24/28** (`c.cut` 4→`c.scp` 4 DAG válido, `story.ch4.e2` 28ª quest grey), bundle **47 ficheros (396.7 KiB)** verde (`python tools/web/build_bundle.py` regenerado, `test_bundle_fresco.py` 2 passed, guardián verde). `postmortem.auditor.join` presente en `src/data/textos.json` (7ª clave Auditor tras `corte`/`orden`/`corte`+`orden`, tríada hermana determinista `shlex` pipe-aware, sin `join` byte-idéntico). `generate(42,4, contract_id='story.ch4.e2')` golden `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` → `TR-001/TR-002/TR-003` sin `id` intacto, `cat /etc/hosts` descubre `faro`+`troncal-01`/`troncal-02` (`ls /etc` no descubre, re-leer no duplica), `GameState.to_dict/from_dict` idéntico preservando `/tmp/volcado.csv` multi-host; `generate(42,6)` intacto (`join -t'|' -1 3 -2 1 -v 1 /srv/camara-faro/purgas.csv /srv/camara-faro/registro.csv` → 3 líneas sujeto+PR-0091+PR-0092, `join|grep PR-0091` →1 línea, `ps aux|grep 11:04` →1 línea `PR-0091` sin señuelo, `cut|sort|uniq -c` y `sort -t'|' -k12 -n|head` intactos, 127 con glosa solo fuera de ch6). `CENSO-LISTA.md`/`06-faro.md`/`04-troncales.md`/`POSTMORTEM.md` sin drift (PR-0091 fecha en blanco `EN BLANCO`, PR-0092 coma-trampa `EN BLANCO, revisado`, frontera 127 honesta `tail`→127 en ch4/ch6, allowlist `DEFAULT_CH4_COMMANDS` 13 `DEFAULT_CH6_COMMANDS` 16 intactas). Pack `POSTMORTEM.md` (5 claves `prueba/sin_lectura/señal_muerte/señal_recarga/ceniza.llave`) sigue en recámara — la tríada `corte`/`orden`/`join` ya cubre la voz del interrogatorio en formulario y el resto son claves de SEÑAL sin urgencia (octava noche revisada, sin cambio de destino).

2. **Coherencia narrativa:** `06-faro.md` E1–E5 + dato2/dato3/dato4/dato5 + `04-troncales.md` E1–E2 sin contradicción con las tres piezas nuevas. `join` vive donde debe (cap. 6 Faro, familia conteo), su glosa fuera del Faro nombra el Faro sin mover allowlist (frontera pedagógica intacta: cap. 0/4 →127, cap. 6 →0). `auditor_join` cita el cruce como huella post-mortem sin filtrar datos de fila, voz formulario calibrada contra ficha del Auditor (test del nombre tapado: solo él dice «Continuidad del ensayo: estable»). Troncal tabla viva es panel HTML sin tocar `curriculum.json`/`textos.json`, caída estática `TR-001/002/003` byte-idéntica a `chapter4.py` `TRONCAL_CONTENT`. Sin escritura nueva de capítulos; narrativa sigue `[LISTA]` 6+6.

3. **Anti-slop:** `grep -nEi "testamento|pivotante|cabe destacar|es importante señalar|no solo.*sino|panorama|curiosamente|en esencia|simbolizando|reflejando"` sobre `src/data/textos.json` auditor_join + troncal → 0 hits. `postmortem.auditor.join` = «Expediente 000: cruce registrado — join anti-join (-v): huérfanas de la primera tabla. Continuidad del ensayo: estable.» — sin gerundios profundizadores, sin autoridad persuasiva (`la verdadera pregunta`), sin signposting, con datos concretos (`-v`, `huérfanas`, `primera tabla`). Em dash único de la glosa 127 es didáctico inglés GNU, no prosa Manus.

Sin escritura nueva de capítulos; narrativa sigue `[LISTA]` 6+6, cero deuda nueva.

**POR QUÉ:** Gwyn 12/09 cerró la 4ª huella del Auditor tras dos replanificaciones (spec intacta desde 11/09, ramas vacías por truncado de tool-call, no por capacidad — diagnóstico de Gwyndolin con datos). La tríada `corte`/`orden`/`join` completa el interrogatorio en formulario: `cut` cita la columna, `sort -k12` cita la columna/delimitador/numérico, `join -v` cita el anti-join huérfanas. Las tres son huellas post-mortem hermanas, mismo patrón `_find_*` determinista, misma voz, sin pisar `curriculum.json` ni `shell.py`. La glosa 127 convierte el rechazo mudo de `join` fuera del Faro en mapa («tables cross there (chapter 6).») — precedente `scp`/`/etc/hosts`, frontera honesta sin regalar `join` a ch4. La tabla viva del troncal cierra el espejo del Faro en la web (Faro tabla | distrito, Troncal tabla | origen): el jugador ve `TR-003|EN_COLA` 512 bytes 03:14 como dato que pesa esperándolo, mismo fallback que `chapter4.py`. Manus verifica que las tres piezas nuevas no rompan la prosa 6+6 (ch4.e2 «El volcado que no pesa» con alma intacta, dato4 «El cruce» con `grep 000` intacto, dato5 «La persiana» con `START 11:04` intacto) y que el pack POSTMORTEM siga correctamente en recámara (claves de SEÑAL vs formulario).

**ENTREGABLE:**
- `backlog/historia/INDICE.md` entrada 12/09 → 13/09 (tríada completa + coherencia Faro/Troncal, 698/24-28/47)
- `docs/worklog/2026/09/13.md` (esta entrada)
- Verificación en vivo: 698 passed, 24/28, 47 bundle, `postmortem.auditor.join` + `generate(42,4, contract_id='story.ch4.e2')` + `generate(42,6)` + `grep -nEi` 0 hits

**RELEVO:**
- **Gwyndolin (11:00):** 698/24-28/47 verificados, bundle 47 fresco, tríada Auditor completa (`corte`/`orden`/`join` 7 claves), 127 didáctico `join` (solo fuera de ch6), tabla troncal viva en web (`?chapter=4&seed=42`). No replanificar `auditor_join`/`auditor_corte`/`auditor_orden`/`ch4.e2`/`dato4`/`dato5`/`c.join`; están cerrados y verdes. Pack `POSTMORTEM.md` (5 claves) sigue en recámara con Manus sin urgencia — no tocar hasta Q con dueño en caliente (octava noche revisada). Zona 🔬 de Gwyn 12/09 vigente (auditor_join + glosa 127 + tabla troncal). Si hoy planificas dato nuevo, respeta DUEÑO gate flexible/guard SUBSET = Seath y ALLOWLIST OWNER declarado (nadie toca `DEFAULT_*` sin ADR).
- **Oscar (05:00) / Havel (07:00):** zona 🔬 de Gwyn 12/09 vigente — Prioridad 1 `auditor_join` (verificar `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv` con `-v` dispara `auditor_join`/`auditor_join_text`, sin `-v` no dispara, sin `join` byte-idéntico, `-v1`/`-av`/pipe+join deterministas). Prioridad 2 glosa 127 (`join` cap0→127 con `Try 'join --help' — tables cross there (chapter 6).`, `join` ch6→0 sin glosa, `foobar` ch4→127 seco) + tabla troncal web (`?chapter=4&seed=42` preview estático TR-001/002/003 EN_COLA 512/1024/2048 sin `id`, `cut -d'|' -f1` col 1 | resaltada, `?chapter=6` Faro intacto, restart limpia). Smoke esperado **698/24-28/47**, ch4 allowlist 13 con `tail`→127, shell 2 pipes OK / 4 KO, `grep -v`→127 no soportado (filtro positivo `grep TR-`/`grep 000` sigue lección).
- **Ornstein/Smough/Seath:** `postmortem.auditor.join` (7ª clave) y `web/app.js` troncal (`parseTroncalCut`/`previewTroncalTabla`) ya en main — no piséis esos prefijos sin coordinar; `c.join` 6 con `c.cut`+`c.sort` y `c.scp` 4 con `c.cut` 4 intactos.

CICLO: verde — tríada Auditor cerrada y coherencia Faro/Troncal verificada, suite 698/24-28/47, sin deuda narrativa abierta.

## 05:00 — [Oscar de Astora] Guardián de la experiencia — tríada Auditor + 127 + tabla viva (MODO B, zona 🔬 ejecutada COMPLETA desde save limpio)

**QUÉ:** relevo Gwyn → Oscar (05:00, PRIMERO del día, dueño del save limpio) → Havel. Ejecuté la zona 🔬 de Gwyn 13/09 (Prioridad 1: `auditor_join` anti-join; Prioridad 2: glosa 127 que nombra el Faro + tabla viva del troncal en web) sobre build jugable real (MODO B, no papel).

**Run de referencia desde save limpio (harness `generate` + `new_session` + `Shell` + `build_postmortem`):**
- `auditor_join` — `generate(42,6, contract_id='story.ch6.dato4')` + `new_session` `cd /srv/camara-faro` + `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv` exit 0 `sujeto|…`+`000|PR-0091|EN BLANCO|--|ENSAYO`+`000483|PR-0092|…|EN BLANCO, revisado` (header + 2 huérfanas, coma-trampa). `build_postmortem(shell.to_dict())` con `-v` → `auditor_join_text` «Expediente 000: cruce registrado — join anti-join (-v): huérfanas de la primera tabla. Continuidad del ensayo: estable.» + `lines_resolved` 2 (pico + join); `join -v1`/`-v 1`/`-av`/pipe `cat purgas.csv | join -t'|' -1 3 -2 1 -v 1 - registro.csv` también disparan deterministas; `join` sin `-v` → 1 línea sin `auditor_join` (solo pico); sin `join` (`cat purgas.csv`) → 1 línea byte-idéntica a antes (sin key `auditor_join`). Sin filtrar fila (`PR-0091` ausente, solo `Expediente 000`). ✔
- Glosa 127 — `generate(42,0)` + `join` → exit 127 `sh: command not found: join` + `Try 'join --help' — tables cross there (chapter 6).`; `generate(42,4)` + `join` → idéntica glosa; `foobar`/`tail` en ch4 → exit 127 seco sin glosa; `generate(42,6)` + `join -t'|' -1 3 -2 1 purgas.csv registro.csv` → exit 0 sin glosa (funciona). ✔
- `story.ch4.e2` «El volcado que no pesa» — `generate(42,4, contract_id='story.ch4.e2')` determinista 42×2, `new_session` `cat /etc/hosts` exit 0 `10.6.0.5 faro`+`10.6.1.10 troncal-01` (seed 1 → +`troncal-02` 3 hosts); `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` exit 0 deja `/tmp/volcado.csv` con `id|origen|destino|bytes|estado` + `TR-001|OK 1024` + `TR-002|OK 2048` + `TR-003|EN_COLA 512`; `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` exit 0 `TR-001/TR-002/TR-003` sin `id`; `cut` sola → `id`+3 (equivocar enseñando, sano); `GameState.to_dict/from_dict` roundtrip idéntico preservando `hosts` multi-host y `/tmp/volcado.csv`. ✔
- Regresión: `generate(42,4)` sin contract → `story.ch4.e1` (prefiere e1); `generate(42,6)` intacto → `join | grep PR-0091`→1 línea; `ps aux | grep 11:04`→1 línea `PR-0091` sin señuelo; seeds 7/99 stable; `DEFAULT_CH4_COMMANDS` 13 sin `join`, `DEFAULT_CH6_COMMANDS` 16 con `join`. ✔
- Fronteras: `tail`→127 en ch4, 1 pipe OK / 3 pipes KO honesto, `grep -v`→exit 2 (filtro positivo `grep TR-`/`grep 000` sigue lección). ✔

**Perspectiva veterano (20+ h → run 30):** la tríada `corte→orden→join` ya es ritual — el veterano reconoce la segunda línea del post-mortem sin leer la primera; el 127 es mapa mental (no muro) que recuerda dónde vive cada verbo; el troncal es mini-Faro (`cut|grep TR-` vs `cut|sort|uniq -c` vs `join -v` son el mismo verbo en 3 altitudes: cortar→agrupar→cruzar). `TR-003|EN_COLA 512 bytes 03:14` sigue la pista que pesa y `id` el fantasma que el veterano filtra sin pensar, ahora también en la tabla viva. Hub/eco del espejo (🧭9) y agregado multi-run (🧭17) siguen pendientes — el eco «copiaste el volcado / cruzaste tablas / leíste el reloj» daría cuerpo a ch4.e2+dato4/5.

**POR QUÉ:** Gwyn 13/09 cargó la zona con dos prioridades que nacen de la misma frontera pedagógica: el Faro enseña `join` y el resto del mundo lo niega con voz; el post-mortem cita el cruce solo si lo viste. Como Oscar (primero del día, dueño del save limpio) debo recorrer COMPLETA la zona desde cero y responder con mediciones a sus tres preguntas de sabor (¿el Auditor vio tu cruce? ¿la glosa enseña o marea? ¿la tabla da que hacer?), comprobar que el viaje del novato sigue apto y que la regresión ch4+ch6 no degrada.

**Hallazgos (dirección, no rotura):** 🧭20/21/22/23 CERRADAS; 🧭24 CERRADA con matiz pre-puebla P3 (mantener, doc drift menor); 🧭25/26/27 PERSISTEN; 🧭28 CERRADA (FICHA alma); **🧭29/30 NUEVOS CERRADOS** (verificación positiva: tríada con voz + glosa que enseña sin marear + tabla viva que da que hacer — preview estático pre-scp, tabla viva-live post-cut, cap. 4 only, restart limpio, duplicación `TRONCAL_STATIC` byte-idéntica señalada). Ningún [BUG] nuevo que rompa el camino; smoke 698/0, gate 24/28, bundle 47 fresco 396.7 KiB.

**Entregable:**
- `docs/ESTADO-JUGADOR.md` actualizado (13/09, MODO B, 698/24-28/47, run de referencia y veterano con auditor_join+127+tabla viva)
- `backlog/notas-manana.md` §🧭 con Oscar 13/09 (SOBRESCRITA mi sección, 🧭20-30 con sabor y propuestas de dirección)
- `docs/worklog/2026/09/13.md` (esta entrada)
- Sin [BUG] nuevo en `backlog/tareas/pendiente/abierto.md` (solo observaciones persistentes 🧭25/26/27)

**Relevo:**
- **Havel (07:00):** zona 🔬 ya ejecutada completa desde save limpio — te toca continuarla con ojos de novedad (no repitas el save limpio, tu capa es lo nuevo + smoke sin reset). Prioridad 1: juega `story.ch6.dato4` con `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv` y abre `build_postmortem` para ver la segunda línea «cruce registrado»; prueba `-v1`/`-v 1`/pipe+join y sin `-v` para confirmar el no-disparo. Prioridad 2: teclea `join` en cap. 0/4 y mira la glosa 127 que nombra el Faro, luego en web `?chapter=4&seed=42` preview estático TR-001/002/003 → `scp` → `cut -d'|' -f1 | grep TR-` y mira la tabla nacer; cambia a `?chapter=6` y verifica que el panel troncal desaparece; haz restart y confirma que limpia ambos paneles. Smoke esperado 698/0, gate 24/28, bundle 47, allowlist 13 con `tail`→127, `grep -v`→127 no soportado (filtro positivo).
- **Gwyn (23:00):** 🧭20/21/22/23 cerradas, 🧭24 cerrada con matiz pre-puebla P3 (mantener, informo no decido), 🧭25/26/27 en recámara, 🧭28 cerrada, **🧭29/30 cerrados positivos** (las 3 preguntas de sabor se responden SÍ ×3). Sin [BUG] que validar; zona responde completa. Si planificas dato6 coma-trampa, recuerda allowlist y duplicación `TRONCAL_STATIC` como deuda.

CICLO: verde — la zona 🔬 se ejecutó completa (auditor_join 5 variantes + 127 4 casos + troncal shell+web con restart) y el viaje del novato sigue apto; la cuarta huella no rompe el byte-idéntico sin cruce.

## 07:00 — [Havel la Roca] Vidente-creativo — tríada completa + 127 que enseña + tabla viva (novedad+conjunto)

**QUÉ (zona 🔬 de Gwyn 13/09, relevo Oscar → Havel, capa novedad+conjunto — SIN save limpio):**
- **Lo nuevo por `git log --since="24 hours ago"`:** merges #47 (`auditor_join` 4ª huella anti-join), #48 (127 `join` con glosa Faro), #49 (tabla viva troncal web) — todo ya recorrido por Oscar desde save limpio a las 05:00; verificado por Manus 03:00 que no hay drift narrativo.
- **Jugado LO NUEVO (timebox 15 min, mundo real):** `generate(42,6, contract_id='story.ch6.dato4')` + `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv` → 3 líneas `sujeto|…`+`PR-0091|000`+`PR-0092|000483` (coma-trampa), `build_postmortem(shell.to_dict(), {noise_budget:12})` con `-v` → `auditor_join`+`auditor_join_text` «Expediente 000: cruce registrado — join anti-join (-v): huérfanas de la primera tabla. Continuidad del ensayo: estable.» + `lines_resolved` 2, sin `-v` → 1 línea sin `auditor_join`, sin `join` (`cat purgas.csv`) → byte-idéntico (sin `auditor_join` key), variantes `-v1`/`-av`/`pipe+join` (`cat purgas.csv | join -t'|' -1 3 -2 1 -v 1 - registro.csv`) deterministas, sin filtrar `PR-0091` (solo `Expediente 000`); 127: `generate(42,0)`+`join` → exit 127 + `Try 'join --help' — tables cross there (chapter 6).`, `generate(42,4)`+`join` idéntica, `foobar`/`tail`/`sort` en ch4 → 127 seco sin glosa, `generate(42,6)`+`join -t'|'` → exit 0 sin glosa; `generate(42,4, contract_id='story.ch4.e2')` + `cat /etc/hosts`+`scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/`+`cut -d'|' -f1 | grep TR-` → `TR-001/TR-002/TR-003` sin `id`, `GameState(shell).to_dict/from_dict` roundtrip preserva hosts y `/tmp/volcado.csv`; web `TRONCAL_STATIC` byte-idéntica a `chapter4.py`, `previewTroncalTabla`/`parseTroncalCut` con cap. 4 only, `hideTroncalTabla` en restart limpia ambos paneles. ✔
- **Smoke del CONJUNTO (SIN save limpio):** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **698 passed / 0 xfailed**, gate 24/28, `DEFAULT_CH4_COMMANDS` 13 / `DEFAULT_CH6_COMMANDS` 16, bundle 47 fresco 396.7 KiB. ✔
- **Preguntas de sabor Gwyn:** ¿Auditor saw your cross? **Sí** — retícula `corte→orden→join` se siente como misma voz, formulario sin fila. ¿Glosa enseña o marea? **Enseña** — solo `join` nombra Faro, resto seco, ch6 sin glosa. ¿Tabla da que hacer? **Da que hacer** — preview estático pre-`scp`, viva-live post-`cut|grep`, columna 1 resaltada, cap. 4 only, restart limpio.

**Ideas nuevas (10, para Gwyndolin/Gwyn — decidir, no implementar):** dato6 coma-trampa jugable, TR-003 karma rescatar vs quemar, eco Gris/Auditor (🧭9), sinergia `join|grep|cut` HOSP-47-C, glosa hermana `sort`/`tail`, troncal interactivo tooltip, harness tríada, `join --help` Bandit, volcado mini-Lista, E3 puerta que pregunta `ssh`.

**Entregable:** 10 ideas nuevas en `backlog/tareas/pendiente/abierto.md` (sección 13/09), esta entrada. Firmado Havel la Roca en main.

CICLO: verde — lo nuevo (tríada `corte→orden→join` + 127 que nombra Faro + troncal tabla viva) funciona y mola, el conjunto sigue verde (698/24-28/47); preguntas de sabor SÍ ×3; sin bugs bloqueantes.

## 11:00 — [Gwyndolin, Dark Sun] Planificador — plan 13/09

**QUÉ:** lido `PROJECT-MAP`/`AGENTES`/`abierto.md`/`activo.md`/`notas-manana`/`historia`/worklog de ayer; `git branch -a` + `gh pr list` verificados (0 PRs abiertos; 15 `feat/*` del 02–06/09 con 0 commits siguen huérfanas). Plan `backlog/planes/2026/09/13.md` con 3 tareas: **O1** Ornstein — eco del espejo v0 (`postmortem.espejo.*`, P1 del día); **S2** Smough — quest `story.ch6.dato6` coma-trampa (ALLOWLIST OWNER: NADIE; GATE OWNER: Smough, flexible 28↔29); **T1** Seath — web: badge `TR-003|EN_COLA` + header `id` tachado. `activo.md` actualizado con las 3 líneas `[EN CURSO]` + constancias de cierre del 12/09.

**POR QUÉ:** dirección de Oscar 13/09 (🧭29/30 cerradas, «los 3 verbos son el mismo verbo en altitudes») + 🧭9 viva con la única `[P1]` del backlog (eco diegético) + prioridades de Gwyn 12/09 (dato6 con spec decidida pronto; TR-003 SIN ADR no se toca; pack POSTMORTEM no urgente). Higiene sin re-clasificaciones: está prohibido retirar líneas `[HECHO]` que Gwyn pueda archivar (lección 01/09); solo se anotó que los marcados «ELEGIDA para HOY» antiguos son obsoletos como señal.

**ENTREGABLE:** `backlog/planes/2026/09/13.md` + `activo.md` al día. Delta combinado esperado al cierre: 698 → ≥705, gate 24/29.

**RELEVO — Artorias (21:00):** ensayo con base 698; verificar deltas declarados; bundle: solo S2 roza data/ (Smough regenera en SU rama; O1/T1 no tocan bundle).
**RELEVO — Gwyn (23:00):** merge engine→sandbox→meta-ui; archivar `[HECHO]`; higiene de ramas viejas (borrar 15 `feat/*` muertas); nota para ADR de TR-003 si Juanma juega.

## 13:00 — [Ornstein] Ejecutor 1 — Eco del espejo v0: el Auditor nombra tu repertorio

**PLAN DE IMPLEMENTACIÓN (hitos) — antes de codear:**
1. **Hito 1 — Detectores deterministas:** `_has_espejo_volcado` (scp→cut|grep, orden scp antes de cut+grep, cubre pipe y separado), `_has_espejo_reloj` (ps aux+grep con `aux` o `\d:\d`), reutiliza `_find_join` existente para ②. Solo `shlex`/`re`/substring, sin import sandbox, determinista por orden history. Testea helpers aislados.
2. **Hito 2 — Integración en build_postmortem:** calcula huellas en orden ①→②→③, construye `huellas_str` ("A", "A y B", "A, B y C" español), resuelve `postmortem.espejo.repertorio` vía `_resolve_auditor_text`, añade `auditor_espejo`+`auditor_espejo_text` y extiende `lines_resolved` (tras join, antes de lectura). Sin firma → byte-idéntico (no key, no línea).
3. **Hito 3 — Textos:** `src/data/textos.json` nueva clave `postmortem.espejo.repertorio` = "Expediente 000: repertorio — {huellas}. Continuidad del ensayo: estable." Prefijo disjunto, sin datos de fila, voz formulario.
4. **Hito 4 — Tests + suite:** `test_postmortem_espejo.py` con 5 tests (① volcado / ② testigos con auditor_join coexistiendo / ③ reloj / sin firma byte-idéntico con check no sandbox / ①+②+③ en una línea orden ①→②→③). Ajuste mínimo en `test_auditor_join.py` (len 2 → in (2,3) por coexistencia espejo+join, mismo dueño). Suite 698→703 (+5, ≥702 exigido) y bundle regenerado 47 ficheros 400.2 KiB.

**QUÉ:** Implementado el eco diegético del espejo v0 en `src/core/engine/postmortem.py` (80 líneas nuevas: `LINE_KEY_ESPEJO`, helpers `_has_espejo_volcado`/`_has_espejo_reloj`, bloque espejo en `build_postmortem`). Textos en `src/data/textos.json` (`postmortem.espejo.repertorio`). Tests en `src/tests/core/engine/test_postmortem_espejo.py` (5). Bundle regenerado `web/bundle/core.json` (400.2 KiB). `test_auditor_join.py` actualizado para coexistencia (dueño Ornstein, misma rama).

**POR QUÉ:** La tríada `corte→orden→join` ya es ritual del Auditor; faltaba que el juego NOMBRE el repertorio como un mismo verbo en 3 altitudes (🧭9 viva, dirección Oscar 13/09: cut|grep TR- en Troncal, cut|sort|uniq en Faro, join -v en Cámara). El eco no enseña comando nuevo ni filtra fila: nombra lo que el jugador ya hizo ("copiaste el volcado / cruzaste dos testigos / leíste el reloj") en una sola línea formulario, determinista ①→②→③, sin popup ni contador, sin tocar curriculum/shell/gates.

**ENTREGABLE:**
- `src/core/engine/postmortem.py` — eco espejo v0
- `src/data/textos.json` — `postmortem.espejo.repertorio`
- `src/tests/core/engine/test_postmortem_espejo.py` — 5 tests (4 exigidos + bonus 3-firmas)
- `src/tests/core/engine/test_auditor_join.py` — compat espejo (len 2→in 2,3)
- `web/bundle/core.json` — 47 ficheros regenerado (400.2 KiB)
- Suite: **703 passed** (698 antes → +5, ≥702), bundle fresco, gate 24/28 intacto (ch6 dato6 es de Smough)

**RELEVO:**
- **Artorias (21:00):** rama `feat/engine-2026-09-13` con 5 tests espejo + 1 ajuste join; base 698, rama 703 (+5). Verificar deltas declarados en PR, orden ①→②→③, byte-idéntico sin firma, y que `test_bundle_fresco` pasa (bundle regenerado). Solo `web/bundle/core.json` tocado por necesidad (data tocada).
- **Gwyn (23:00):** merge engine→sandbox→meta-ui (engine primero, sin conflicto de prefijo `postmortem.espejo.*` vs `story.ch6.dato6.*`/`web/app.js`). Archivar `[HECHO]` en `activo.md` con PR. No requiere ADR.

CICLO: verde — eco espejo v0 entrega la voz que faltaba sin romper byte-idéntico.
