# WORKLOG — 22/09 (Gwyndolin, planificador — turno 11:00)

## QUÉ
Plan del día (`backlog/planes/2026/09/22.md`) + higiene del backlog + 2 propuestas de auto-mejora:
- **O1 — Ornstein 13:00 · `feat/engine-2026-09-22`** — E1 «La puerta que dejaste»: `chmod 600` vs `chmod 777` como dilema kármico (díptico con E3 kill HUP/-9 de ayer). Textos incluidos; regen bundle EN SU RAMA.
- **S1 — Smough 16:00 · `feat/sandbox-2026-09-22`** — el [BUG] `grep -v`/`-i` (🧭27, único bug vivo) sube P3→P2-code: `_run_grep` con flags, byte-idéntico sin flags.
- **T1 — Seath 19:00 · `feat/meta-ui-2026-09-22`** — lente del veredicto: el post-mortem del juicio (auditor_hup/kill) visible en la web bajo `#custodia-postmortem`.
## POR QUÉ
- E1 es la idea P2 de Havel 21/09 que completa el díptico moral de la Subestación (mismo Linux, dos salas, verbo distinto, karma distinto) — direcciones de Oscar 21/22 apuntan ahí; es lo más denso que desbloquea la Subestación jugable entera.
- `grep -v` es el único [BUG] de código abierto y una deuda clásica del flujo del jugador (🧭27).
- El veredicto del juicio vive en un post-mortem sin lente web: la insignia ANUNCIA, el post-mortem VEREDICTA — Oscar lo marcó P3 natural y Gwyn lo validó.
## ENTREGABLE
- `backlog/planes/2026/09/22.md` (plan completo con AC por tarea)
- `tareas/en-curso/activo.md` — 3 líneas [EN CURSO] asignadas + cabecera de costuras (ALLOWLIST/GATE: NADIE; bundle: solo Ornstein)
- `tareas/pendiente/abierto.md` — higiene: BUG 🧭27 P3→P2 + nota ↩ TOMADA
- `mejoras/pendiente/propuestas.md` — 2 [NUEVA] (det. de turnos cortados; limpieza ramas residuales)
## RELEVO
- Ejecutores 13/16/19: sus líneas en `activo.md`; sin dependencias entre ellos.
- Artorias 21:00: gate técnico de 3 ramas.
- Gwyn 23:00: merges engine→sandbox→meta-ui + decidir las 2 propuestas.
- COBERTURA: la investigación del turno fallido de Artorias 21:00 (🎯 Gwyn) está HECHA — causa: provider emitió tags corruptos (`<atem:ather>`→`<atem:parameter>`) en el output crudo; scheduler lo archivó «completed». La regla HARD de huella no puede prevenirlo; propuesta sistémica en `propuestas.md`.

CICLO: verde — base 788/0 verificada, plan coherente con DESIGN §3.1/§3.3/§8.6 y con la campaña E1-E4 de la Subestación.

---

## 16:00 — Smough (ejecutor 2) — grep -v / -i en _run_grep — BUG 🧭27 P2-code

### PLAN DE IMPLEMENTACIÓN (obligatorio antes de codificar — 4 hitos ~1h c/u)

**H1 — Parser de flags GNU (`grep [flags] PATRON [FICHERO]`):** Extender `_run_grep` para consumir flags líderes `-v` / `-i` combinables (`-v`, `-i`, `-vi`, `-iv`, `-v -i`). Bucle consume args que empiezan por `-` y no son `--`/ `-` solo; cada char `v`→invert, `i`→ignore_case; flag desconocido → `grep: invalid option -- 'x'` exit 2. Soportar `--` terminador. Primer no-flag = patrón; resto = ficheros. Sin patrón → `grep: missing pattern` (existente). Crit "hecho": `grep -v` y `grep -vi` parsean sin romper `grep PATRON` actual.

**H2 — Lógica de match con `v` e `i`:** `_scan` usa `_matches(line)` = `(pattern.lower() in line.lower() if i else pattern in line)`; si `v` invierte: `not _matches`. Mantener exit codes GNU: 0 si ≥1 línea seleccionada (invertida o no), 1 si 0 líneas, 2 si error FS. `stdin` vía pipe cuando `files==()` y `stdin` no vacío: `_scan(stdin)` con nueva lógica. Ficheros: iterar `fs.read_file`, acumular `matched` y `err_lines`. Crit "hecho": `grep -v sujeto purgas.csv` filtra header, `grep -i` insensitive, combinado `-vi` funciona.

**H3 — Byte-idéntico sin flags + ruido intacto:** Sin flags, camino idéntico a hoy: `pattern in line` exacto. `noise_event(GREP_NAME, argv, tick)` igual, `GREP_SPEC.noise==2` no cambia, `shell.py`/`curriculum.json`/`web/` no tocados. Tests existentes de `test_texto.py`/`test_shell.py` deben pasar sin cambio. Edge: patrón vacío nunca ocurre (shlex lo evita); fichero inexistente → exit 2 con mensaje `_grep_fs_message`.

**H4 — Tests + verificación AC + suite:** Añadir `src/tests/core/sandbox/test_grep_flags.py` (5±2 tests): (1) `grep -v sujeto purgas.csv` → 0 filtra, (2) `ps aux | grep -v root` via shell pipe → 0, (3) `-i` case-insensitive, (4) `-v -i` combinado, (5) sin flags byte-idéntico (re-ejecuta golden actual), (6) `-v` sin match invertido → exit 0 cuando hay línea no matching, 1 cuando todo matchea. Ejecutar `/home/juanma/CyberRoot/.venv/bin/python -m pytest -q` → 788→793 (+5±2), `pytest -k grep` verde. Declarar PR `tests antes: 788 · tests rama: M · delta esperado: +5`.


### QUÉ (Smough 16:00 — implementación)
- `src/core/sandbox/commands/texto.py` — `_run_grep` ahora parsea `-v`/`-i` combinables (`-v`, `-i`, `-vi`, `-iv`, `-v -i`, `--`) en orden GNU `grep [flags] PATRON [FICHERO]`; `_scan` con `ignore_case` + `invert`; exit codes 0/1/2 intactos; `stdin` vía pipe; byte-idéntico sin flags.
- `src/tests/core/sandbox/test_grep_flags.py` — 6 tests nuevos AC: `-v` filtra header purgas.csv, `ps aux | grep -v root` pipe, `-i` insensitive, `-vi` combinado, byte-idéntico sin flags, `-v` exit 1 cuando todo matchea.
- Worklog con plan de 4 hitos documentado arriba.

### POR QUÉ
BUG 🧭27 único [BUG] de código vivo; deuda clásica del flujo (`purgas.csv` necesita filtrar header; `ps aux | grep -v root` es patrón Unix). Cierra higiene P3→P2-code sin tocar `curriculum.json`/`shell.py`/`web`.

### ENTREGABLE
- Rama `feat/sandbox-2026-09-22` + PR (tests antes: 788 · tests rama: 792 core (+6) / 793 con bundle stale pendiente Gwyn · delta esperado: +6)
- Suite: 792 passed `--ignore=src/tests/web` (786→792), 793 passed + 1 failed (`test_bundle_fresco` stale — NO regen por ownership; Gwyn regen canónico post-merge engine→sandbox)
- `grep --help` no existe; flags desconocidos → `grep: invalid option -- 'x'` exit 2 (GNU-honesto)

### RELEVO (rama + PR para Artorias/Gwyn)
- Rama `feat/sandbox-2026-09-22` lista para review Artorias 21:00 (gate técnico).
- PR declara bundle stale honesto (ownership: solo Ornstein regen hoy; se deja a Gwyn post-merge).
- Orden de merges: engine (Ornstein) → sandbox (Smough) → meta-ui (Seath).
