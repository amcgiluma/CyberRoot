# WORKLOG — 2026/09/01

## 11:00 — [Gwyndolin, Dark Sun] Planificador — plan del día 01/09

**QUÉ:**
- **Plan de HOY** en `backlog/planes/2026/09/01.md` con la dirección de Gwyn
  (🎯 31/08): 1º `sudo` GANADO al motor (forma firmada §6.1) → O1 (Ornstein:
  credencial como FICHERO que el generator coloca en la sala) + S1 (Smough:
  comando `sudo` + `c.sudo` a datos, con contrato O1↔S1 declarado); 2º primer
  paquete de TEXTOS 🧭12 (Seath T1: voz del Auditor + cap. 1) + `story.ch5.*`
  a datos (T2); 3º familia conteo `head`/`tail`/`sort`/`uniq` (Smough S2);
  4º O2 (harness: métrica de ánimo de novedad, idea de Havel 27/08);
  5º Manus 03:00 del 02/09: M1 worldbuilding del censo (PUERTA del Acto 3) +
  M2 cap. 6 «Faro» con el fragmento 6 GARANTIZADO (🧭5) — la narrativa
  completa (0–6) quedaría materializada.
- **Higiene del backlog** (ver plan §Higiene): 2 líneas mal clasificadas
  reparadas en `abierto.md` (residuo de O2 ya ejecutado; BUG 🧭11 resuelto →
  archivado en `hecho/2026-09.md`, creado hoy); 1 prioridad ajustada
  (variante «medir solo la extracción» P2→P3, obsoleta por PR #15); sin
  ramas huérfanas ni PRs abiertos; `mejoras/` sin nada pendiente; INDICE
  intacto.

**POR QUÉ:** dirección explícita de Gwyn por orden de prioridad; el `sudo` es
el cierre natural del cap. 3 (la familia Procesos ya está en datos) y la
credencial como fichero del mundo respeta «hosts como FS» y el principio
necesidad-antes-que-boon. Los textos (T1) desbloquean que el eco 🧭9 y el
post-mortem SE SIENTAN — es lo único que falta con tubo y datos ya vivos.
Render v0 queda reservado para mañana (día verde) como pidió Gwyn; es el
desbloqueo del deploy `[P1]`.

**ENTREGABLE:** `backlog/planes/2026/09/01.md`; `activo.md` relevado (2 franjas
nuevas + 6 tareas [EN CURSO] con dueño); `abierto.md` saneado (2 reparaciones +
1 ajuste de prioridad + nota); `hecho/2026-09.md` creado (archivado BUG 🧭11);
esta entrada.

**RELEVO:**
- **Ornstein 13:00:** O1 → O2. O1 trabaja CONTRA el contrato S1 (fichero-
  credencial + auth.log): define la ruta/marca del fichero y declárala en el PR.
- **Smough 16:00:** S1 (respetar el contrato de O1; el rechazo diegético
  NOMBRA el fichero) → S2 (solo 4 comandos; `tee`/`less` NO).
- **Seath 19:00:** T1 (test de cobertura de claves — falla si falta una) → T2
  (prereqs solo con conceptos vivos; sin inventar).
- **Artorias 21:00:** ensayo de integración CRÍTICO hoy: verifica el circuito
  sudo completo (generator coloca ↔ sandbox lee/firma) en el worktree.
- **Gwyn 23:00:** merges #engine → #sandbox → #meta-ui; archiva M1/M2 de
  Manus de esta madrugada (fragmento 5 + cap. 5). Decisiones tuyas con datos:
  (1) validar la materialización v0 de la credencial como fichero (propuesta
  mía; alternativa = objeto de estado pleno, tarea de Seath mañana);
  (2) guía de voz de los primeros textos (T1) — el momento de fijar el tono
  formulario del Auditor en `data/`.

CICLO de entrada: verde (421/0, sin bugs, sin ramas huérfanas).

## 03:00 — [Manus, Padre del Abismo] Historiador — M1 (fragmento 5) + M2 (cap. 5 «Subestación»)

**QUÉ:**
 - **M1 — Fragmento 5 «El expediente»** (`backlog/historia/FRAGMENTOS.md`,
  `[LISTA]`, estado 5/6): expediente médico de salud laboral del hospital del
  Muelle, folio `OH-HOSP-47-C-0191`, admisión 04:12 del día de la firma,
  campo «empresa» = `VESPER DE GESTIÓN S.L.` (misma sociedad del fragmento
  4), campo «paciente» con un nombre NO vacío que no es el de los Apagados,
  tarjeta de cita posterior en 3 meses que repite «vuelve el jueves» de la
  pulsera (fragmento 2). Es el historial que cuelga al lado del de Vela
  (dirección de Gwyn 28/08: «el de Vela existe, ¿y este?»). Sostiene H1 (un
  nombre pedido a propósito, una empresa fachada que paga, la cita reescrita)
  y H2 (el empleador que ya no existe en el censo, la revisión muerta «sin
  emplazar» — un sistema que archiva a alguien que no debería dejar rastro)
  a la vez. Cronología de Vela intacta.
- **M2 — Capítulo 5 «Subestación»** (`backlog/historia/CAPITULOS/05-subestacion.md`,
  `[LISTA]`, beat 9 §2.5): la ÚNICA incursión invertida del juego (§6.1, cap.
  5). Vela localiza la Subestación COMO CONSECUENCIA de las alertas acumuladas
  del jugador, no por guion. Regla de la luz invertida en los Muelles (Lumen
  entra con hombres, no sensores; el ruido ES volumen, §ESCENARIOS). 4
  encargos `story.ch5.e1`–`e4` (E1 azul, E2 gris, E3 rojo, E4 cierre de doble
  salida), familia auditoría/defensa (§6.2): leer logs (`last`/`who`/auth.log
  simulado), cerrar permisos (`chmod`/`chown`), detectar movimiento (`ps`,
  columna USER — S1 PR #14). Grieta de Ceniza (beat 6) escrita en la casa
  (E2). **Giro del Auditor suma su 2.ª sombra** (E1 + escena Hub `story.ch5.auditor`):
  la pregunta adicional del cap. 4 por la reactivación recibe eco, y el
  formulario pasa a preguntar por su propia Oficina (la recomendación de
  revisar expedientes del activo no censado), arco §9 SIN resolver.
  **Fragmento 5 cae en E4** (cajón 29 del archivo de fragmentos, ESCENARIOS:
  la numeración 27–31 vacía). Gancho: coste escrito en la casa + el censo
  (cap. 6).
- **Documentos vivos**: `INDICE.md` (fragmento 5 + cap. 5 `[LISTA]`,
  siguiente = cap. 6); `FRAGMENTOS.md` estado 5/6; `activo.md` M1/M2 →
  `[HECHO]`; ESCENARIOS ya cubría el asalto del cap. 5 (Muelles) y la
  numeración de fragmentos. PERSONAJES: sin fichas nuevas (Zeta, que el cap.
  4 no convocaba, aparece y respeta su ficha).
- **AUTO-PASS ANTI-SLOP** (capa 3 de SKILLS-ANTISLOP, respuesta escrita):
  - Grep de palabras trampa (testamento/pivotante/cabe destacar/no solo…
    sino/panorama/curiosamente/en esencia/simbolizando/reflejando/actúa
    como/la verdadera pregunta/vamos a analizar…): LIMPIO en ambos ficheros.
  - Señales estructurales (SKILLS-ANTISLOP «señales sutiles»): vigilé el
    rótulo `X:` (patrón 1) y el checklist comercial troceado (patrón 2);
    ninguno cuela — las frases en dos puntos son listas reales o
    cadencia de informe de Ceniza/Auditor, no rótulos de modelo.
  - Una comprobación de voz: en el cierre, Gris dice LA PRIMERA cifra SIN la
    segunda — es una ruptura intencional de su tic (el asalto le quita el
    juego de precios), anotada en las notas del integrador para que el
    revisor no la marque como violación accidental de ficha.
  - Test del nombre tapado: el Auditor en su escena de Hub (frase de
    formulario + nota fuera de registro + «continuidad del ensayo: estable»);
    Ceniza en apertura/post-mortem (condición de salida, dato antes que
    ánimo); Zeta (cortante, comparativa, nada de «nosotros»); Vela en el
    boletín (serenidad de servicio, falsedad que ella no reconoce). El
    narrador no enuncia tema (§2.6.7): todo lo carga el archivo, el censo y
    la cifra.
  - Correcto el beat 9: la localización de la Subestación es consecuencia
    acumulada de alertas, NO guion (requisito explícito de DESIGN §2.5).

**POR QUÉ:** plan de Gwyndolin del 31/08 (asignación a Manus de 03:00 del
01/09) + dirección de Gwyn (28/08): colchón del Acto 2 hacia el giro. El cap.
5 materializa el beat 9 y garantiza que TODO jugador practique auditoría/
defensa aunque su perfil sea rojo (coherente §3.1: misma materia, lentes
distintas); el fragmento 5 cierra la última pieza del pasado antes de la
confrontación del Acto 3. Con esto, Manus deja el colchón narrativo hasta el
cap. 6 y despeja los 5/6 fragmentos.

**ENTREGABLE:** `FRAGMENTOS.md` (fragmento 5 `[LISTA]`, estado 5/6),
`CAPITULOS/05-subestacion.md` (nuevo, `[LISTA]`), `INDICE.md`, `activo.md`
(2× `[HECHO]`), esta entrada. Todo commitado como Manus y en origin (rama
main).

**RELEVO:**
- **Gwyndolin (11:00)**: M1/M2 cerradas; sin deuda narrativa de Manus para el
  plan del 01/09. El siguiente hito narrativo es el cap. 6 «Faro» (beats
  10–12), con el **worldbuilding del censo** como puerta (§9, dueño Manus en
  Fase 1; bloqueante de producción, NO de diseño — marcado en INDICE y
  ESCENARIOS). No hay prisa: el cap. 6 entra cuando el colchón del Acto 2
  haya desembocado en el Acto 3, y Smough aún no ha integrado `story.ch4.*`
  ni `story.ch5.*` a datos.
- **Integrador (Smough cuando toque `src/data/story/`)**: ⚠️ el cap. 5 es la
  ÚNICA excepción estructural §6.4.5 — campaña guionizada, NO muestreo de
  sala normal. Verificar que el flujo de encargo (O1/S1 de Ornstein) permita
  esta variante invertida antes de integrar `story.ch5.*`. Reutiliza
  conceptos ya en currículo (`ps`, `chmod`/permisos, grep/logs de cap. 2,
  `kill` de cap. 3) como MANTENIMIENTO; no exige comandos inéditos
  obligatorios.
- **Artorias (21:00)**: nada de código por mi parte. Si revisas el diff de
  historia: verifica que el Auditor NO resuelve su arco (§9, sigue en
  acumulación de casos — E1/E3/Hub suman sombras, no cierres) y que el beat 9
  se cumple como CONSECUENCIA acumulada (no guion). La cifra única de Gris al
  cierre es intencional (anotada).
- **Gwyn (23:00)**: sin decisiones de diseño de tu plan abiertas que me toquen
  (el cap. 4 y el sudo/red son tuyas y ya firmadas). El cap. 6 queda
  enganchado por el gancho (censo + confrontación); cuando quieras el colchón
  del Acto 3, pido el worldbuilding del censo como puerta de entrada.

CICLO: verde — historia del cap. 5 y fragmento 5 listos; coherencia con los
8 beats previos y los 4 fragmentos anteriores verificada en la redacción.
---
## 05:00 — [Oscar de Astora] Guardián de la experiencia — zona 🔬: el PRIMER CRUCE de capítulo (cap. 0 → cap. 2) desde SAVE LIMPIO

**QUÉ (zona 🔬 de Gwyn ejecutada COMPLETA — el relevo Gwyn → Oscar → Havel
empieza aquí):**
- **Smoke del conjunto:** suite desde raíz → **421 passed / 0 xfailed** exactos
  (deja de Gwyn: 385 + 13 + 18 + 5). Guard intacto. ✓
- **RUN DE REFERENCIA desde SAVE LIMPIO (mi capa)** — cap. 0 canónico (cwd `/`,
  host `oficina-vecinal-muelle-norte`): `ls /srv/oficina-vecinal-muelle-norte` →
  `cat …/nombre_de_proveedor.txt` → `cp … /usb/` → `cd /srv` → `ls`. **Total
  noise 6 / budget 12, sin errores.** Tras ello: `evaluate_unlocks` domina `c.cp`
  (mastered `{tick:5, order:1}`) y `evaluate_logros` da «Mano de seda» (no «Cero
  rastro», correcto). ✓
- **El ECO 🧭9 viaja (verificado hoy):** al dominar `c.cp`, el bus común recibe
  `event.progression.unlocked` con `{concepto:'c.cp', tick:5, order:1}`. ✓
- **EL CRUCE al cap. 2 existe y es real (lo nuevo):** vitrina del cap. 2 con solo
  knowledge del cap. 0 → los 5 encargos NO abribles, cada uno con `falta=[c.grep,
  c.wc, c.pipe]` (rechazo accionable legible sin docs). Con knowledge amplio,
  `abrir story.ch2.e1` monta la sala (cwd `/`) y la golden se juega:
  `grep 11:04 centralita/turnos/turno.log | wc -l` → `2`, exit 0, noise 3.
  `cerrar_encargo(session, modo="completado")` adjunta post-mortem:
  `total_noise=3 ≤ 12`, `dentro_presupuesto=True`, factura `{cd:1, grep:1,
  errores:0}`, Auditor cita su **línea PICO `grep` (amount 3)**. **El paso de
  «Trabajo en frío» a «Facturas» se siente como el MISMO oficio con más
  herramientas** (mismo FS, mismo host, mismo cobre de ruido; cambia el verbo:
  contar, no copiar). ✓
- **Logro «Cero rastro» RECALIBRADO verificado con números propios (🧭11
  RESUELTA):** canónica noise 6 (sin errores) NO gana; min-honesto sin errores
  (ls→cat→cp) noise 5 SÍ gana; min con error (ls -l falla) noise 5 NO gana nada.
  El umbral 5 distingue frugalidad de pulcritud como diseñó Gwyn. ✓
- **Veterano (run 30):** el eco es IDEMPOTENTE entre runs — run nueva con
  inventario que ya domina `c.cp` NO re-descubre (`newly=[]`) NI re-emite al bus.
  El render se suscribirá sin ecos fantasma. ✓ (La variedad a largo plazo sigue
  esperando los pools caps. 1–3/i—16 conceptos / 16 quests ya en currículo.)

**DOS HALLAZGOS (ninguno rompe el camino — CICLO verde):**
1. **🟡 UX del cap. 2 — la golden es REBELDE si no sabes dónde estás (🧭13).**
   Al `abrir story.ch2.e1` la sesión nace con `cwd=/` y la golden usa ruta
   RELATIVA (`centralita/turnos/turno.log`). Ejecutada desde `/` el `grep` falla
   y el pipeline devuelve `0\n` con exit 0 (el exit lo decide el `wc`; semántica
   GNU real, no es bug de sandbox). Para resolverla hay que `cd` a la oficina
   primero (lo hace la canónica CH2). Es la primera fricción de ORIENTACIÓN del
   cruce entre capítulos: el cap. 0 siempre usó rutas absolutas y de golpe el
   cap. 2 pide relativas desde un cwd desconocido. Datos para Gwyn: la pista
   diegética de ubicación al abrir e1 (o cwd en la oficina, o `pwd`).
2. **🟡 Sigue sin entrypoint de run ÚNICO que encadene capítulos** (el cruce lo
   ejercito vía API del flujo, no por un launcher): el `engine/game.py`
   orquestador sigue siendo la pieza que convierte «puedo recorrer cada
   capítulo» en «partida completa». Seguimiento, no bug.
3. **🟡 Claves `postmortem.auditor.*` sin texto en `data/` (🧭12, vigente):** el
   flujo devuelve `line_key`+`args`; falta el paquete de textos que lo pinte.

**PARA DIRECCIÓN (🧭13 en `notas-manana.md`, sección sobrescrita):** 🧭13 nueva
(UX del cap. 2: pista de orientación al cruzar — pwd/cwd en la oficina o pista
diegética). Saldo: 🧭11 RESUELTA anoche (verificada hoy), 🧭9 con tubo e
idempotente, 🧭12 vigente.

**RELEVO:**
- **Havel (07:00):** la zona es tuya a las 07:00 con ojos de NOVEDAD — lo nuevo
  del cap. 3: `ps`/`ps aux` (¿la columna USER ceniza-521 vs censo-522 se lee como
  PISTA, no como dato?) y `env` ordenado; fuerza un rechazo de `ps` en cap. 0/2
  (exit 127, regresión). El cruce cap. 0→2 YA lo recorrí completo desde save
  limpio (no lo rehagas): súmale tú lo nuevo del sandbox y el smoke del conjunto.
  Mi hallazgo 🧭13 lo puedes confirmar como jugador si te pareces al novato que
  escribe la golden desde `/`.
- **Gwyndolin (11:00):** dejé 1 tarea `[PENDIENTE][P3]` (🧭13, UX del cap. 2) y
  marqué ✅ RESUELTO el `[BUG][P2]` del 31/08 (logro «Cero rastro», ya recalibrado
  y verificado). 🧭12 sigue de cola (packaging). Todo lo demás del camino sano.
- **Artorias (21:00):** sin PRs míos (solo docs/NOTAS). Dato para tus gates de
  hoy: la suite 421/0 la verifico yo a las 05:00 y sigue intacta; el cruce
  cap. 2 usa `session.py` (O1) y ya está probado en test + en mi run.
- **Gwyn (23:00):** 2 notas de dirección nuevas/rodantes — 🧭13 (UX del cap. 2,
  con la pista concreta de pwd/cwd o pista diegética) y 🧭12 (claves del Auditor
  en `data/`). El resto del día sano; la zona te devuelve el primer cruce real
  verificado.

CICLO: verde — la zona 🔬 (cruce cap. 0 → cap. 2) se ejecutó COMPLETA y el viaje
del novato ya engloba dos capítulos; logro y eco verificados; los hallazgos son
una pista de UX (🧭13) y packaging (🧭12), ninguno rompe el camino.
---
## 07:00 — [Havel la Roca] Vidente-creativo — zona 🔬 (cap. 3 + eco 🧭9) con ojos de novedad + smoke del conjunto

**QUÉ (el cruce cap. 0→2 ya lo recorrió Oscar desde save limpio; mi capa es lo
NUEVO del sandbox del cap. 3 y el tubo del eco, SIN save limpio):**
- **Smoke del conjunto:** suite desde raíz → **421 passed / 0 xfailed** exactos
  (lo que dejó Gwyn: 385 + 13 + 18 + 5). ✓
- **`ps` / `ps aux` / `env` del cap. 3 (PR #14/S1) jugados:** `ps` compacto sale
  con cabecera GNU real (`    PID TTY          TIME CMD`); **`ps aux` muestra la
  columna USER que DELATA al par gemelo** (521 `ceniza` vs 522 `censo`, misma
  imagen `demonio-11:04`). Confirmo el sabor de la prosa de Manus: la diferencia
  entre los dos cabe en UNA columna, y se LEE como PISTA, no como dato. `env`
  imprime `LANG/PATH/SHELL/USER` ordenado por clave (reproducible byte a byte).
  Ruido: `ps`=1 + `ps aux`=1 + `env`=1 (lectura barata, como `ls`/`cat`; factura
  de 3 en la sesión). ✓
- **Regresión cap. 0/2 intacta:** `ps` en sesión cap. 0 y `env` en sesión cap. 2
  → `exit 127: sh: command not found` (el capítulo en curso no sabe de procesos).
  Confirmado por medida propia; no duplico el test, lo juego. ✓
- **Eco 🧭9 (PR #15/T2) verificado con suscriptor de prueba:** jugando la
  canónica del cap. 0 (seed `havel-01sep-eco`), al dominar `c.cp` el bus recibe
  `event.progression.unlocked` con **payload COMPLETO** `{concepto:'c.cp',
  tick:4, order:1}` en un suscriptor registrado; re-evaluar tras la run **NO
  re-emite** (idempotente, sin ecos fantasma para el render futuro). ✓
- **1 hallazgo de SABOR, no bug:** el `ps` COMPACTO no expone la USER; solo
  `ps aux` delata al par gemelo. La pista de Manus («la diferencia cabe en una
  columna») exige que el jugador conozca `aux`. Es MÉTODO (enseña que el
  comando correcto según la pregunta), no bug — lo dejo como idea `[PENDIENTE]`.

**IDEAS PRINCIPALES (6, `[PENDIENTE]` en abierto.md · decidir Gwyn/Gwyndolin):**
1. **`ps aux` = leer el censo en clave de proceso** (caps. 3→6: USER = nombres
   de los censados; Lumen hace de las personas lo que el kernel de los procesos).
2. **Auditor como ghost en `ps`**: un proceso en TU sesión que no lanzaste
   materializa «morir alimenta a tu cazador» (§2.4) como dato en vivo.
3. **`env` como canal de lore**: variables de entorno muertas del nodo como
   fragmentos coleccionables (Balatro sin UI nueva).
4. **`kill`/señales como verbo kármico** del cap. 3: `kill -9 522` (rojo) vs
   reconfigurar + `kill -HUP 522` (azul) — la tesis §3.1 hecha comando.
5. **Familia Procesos como prerequisito del cap. 5**: detectar el intruso en tu
   propia casa = leer `ps` con ojos de novato (asalto al Hub jugable).
6. **Enmienda de sabor**: guiar `ps aux` en el cap. 3 (el compacto no delata).

**RELEVO:**
- **Gwyndolin (11:00):** sin `[BUG]` de camino (smoke 421/0 pasó); 6 ideas
  `[PENDIENTE]` nuevas ancladas en lo jugado (la 1 y la 4 son P2, el resto P3).
  La zona 🔬 de hoy (cruce cap. 0→2 + lo nuevo del cap. 3 + eco) queda agotada
  con las dos miradas (Oscar viaje, Havel novedad).
- **Artorias (21:00):** nada de código por mi parte. Confirmé con ejecución el
  contrato de datos de los PRs #14 (ps/env + regresión 127) y #15 (eco con
  payload + idempotencia) — si quieres verificar el gate, la suite 421/0 es la
  misma que dejaste y la replico intacta a las 07:00.
- **Gwyn (23:00):** el cap. 3 queda preparado para las decisiones que firmaste
  (sudo GANADO + red simulada); el par ceniza/censo que S1 trajo a datos es un
  cebo kármico listo para cuando `kill` entre — ver idea 4.

CICLO: verde — el smoke del conjunto pasó (421/0); lo nuevo del cap. 3 FUNCIONA
y MOLA; la regresión cap.0/2 y el eco 🧭9 verificados con ejecución propia; sin
bugs de camino.
---
## 21:00 — [Artorias del Abismo] Revisor filtro — capa técnica de los 3 PRs del día

**QUÉ:** filtro técnico de PR #16 (engine, O1 sudo+O2 harness), #17 (sandbox, S1 sudo ganado + S2 familia conteo) y #18 (meta-ui, T1 textos + T2 cap. 5), con el ENSAYO DE INTEGRACIÓN pre-merge obligatorio (3 ramas sobre origin/main).

**POR QUÉ:** protocolo TESTEO-DIARIO §3 (capa técnica «¿está bien hecho?») + regla hard de huella escrita + el contrato O1↔S1 que pedía verificar el circuito sudo completo esta noche.

**ENTREGABLE:**
- **ENSAYO** (worktree desechable, 3 ramas → origin/main d95f1ba): merge encadenado engine→sandbox→meta-ui. Conflictos SOLO en `activo.md` y `worklog/2026/09/01.md` (conservadas TODAS las huellas: 13:00 Ornstein + 16:00 Smough + 19:00 Seath) + **1 conflicto de DATOS**: `test_loader.py` (conteo 21/16 vs 16/20) → reconciliado a **21 conceptos / 20 quests**. Suite combinada **con fix: 478 passed / 0 failed** (1.83s).
- **HALLAZGO GORDO (traje exactamente para esto):** PR #16 verde aislado (432) ROMPE la combinada → **2 tests stale** de O1 que asumen el currículo REAL sin `c.sudo` (S1 lo añade): `test_errores.py` y `test_sala_sudo.py`. Con S1 la generación del cap. 3 SÍ produce la sala; esos tests piden `GeneratorError`. Arreglo exacto documentado en `activo.md` → **💥 PR #16**.
- **GATE DE DATOS VERDE**: `load_curriculum()` carga 21/20; credencial+auth.log presentes; `textos.json` resuelve.
- **SMOKE REAL del circuito sudo O1↔S1**: generé la sala-credencial cap. 3 y la inyecté en el sandbox → `sudo cat` ejecuta, factura base+premium=4, firma en auth.log (`tick 0 operator : sudo cat …`). Literales coinciden exactos.
- **Veredictos en `activo.md`**: #17 ✅, #18 ✅, #16 💥 (fix de 2 tests). Aviso + notas de gusto en `notas-manana.md` (🎯).

**RELEVO:**
- **Gwyn (23:00):** mergea **#17 → #18**; **NO #16** hasta el fix. Verificar suite real: con #17+#18 → 466 passed; con #16+fix → 478. Archivar `[HECHO]` de Manus (M1/M2).
- **Ornstein (mañana, 1º):** arreglar los 2 tests stale de O1 en #16 (fix exacto en `activo.md`), luego re-subir el PR.

**CICLO: ámbar** — los PRs #17 y #18 sanos (✅) y la combinada verde CON el fix de #16; el bloqueo es puntual (2 tests stale del propio #16, sin culpa de datos ni del conjunto). No es rojo porque el camino de main no se rompe (todo lo nuevo es aditivo de PRs sin mergear aún).
