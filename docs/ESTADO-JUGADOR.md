# ESTADO-JUGADOR — qué se puede jugar hoy (documento vivo de Oscar)

> 📌 **Documento vivo, mantenido a diario por Oscar de Astora (05:00).** Es el
> puente entre el `docs/DESIGN.md` (lo que el juego *será*) y el código en `src/`
> (lo que el juego *es hoy*). Cualquier agente o Juanma puede leerlo para saber
> en qué punto está la experiencia jugable de verdad.

---

## 🎮 Estado global jugable de HOY (18/09 — MODO B: testigo condicional Subestación + cadena troncal→Faro→Subestación cerrada, web tooltip+lente)

**¿Hay algo que jugar de principio a fin?** Sí — el viaje del novato ya tiene **cadena cruzada completa**: **cap. 4 `story.ch4.e3` «Lo que no avanza»** grey `c.scp` (`scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → `TR-003|EN_COLA` 512 bytes `03:14` + `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` → `TR-001/TR-002/TR-003` + bifurcación `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` → `volcado: rescatado` vs `rm /tmp/volcado.csv`/`tick≥30` → `caducado`) + **cap. 6 `story.ch6.dato7` «El fantasma que pesa»** grey `c.join` (`volcado-rescate.csv` SOLO si rescate → `join -t'|' -1 1 -2 1 -v 1 volcado-rescate.csv purgas.csv | grep TR-003` → 1 línea `TR-003|faro|troncal-01|512|EN_COLA`; si caducado → `join: volcado-rescate.csv: No such file or directory` exit 1) + **cap. 5 «Subestación» `story.ch5.e2` «El testigo que no llegó»** grey `c.cat+c.scp` (`build_chapter5_fs(volcado_rescatado)` → `/tmp/volcado-custodia.csv` con `TR-003` si rescate, `cat` → `No such file` si caducado + `ps aux | grep intruso` → `intruso --vigilar-censo` USER `censo` START `03:14` en ambos estados) + **Faro dato4/dato6** + **toggle EN_COLA + eco espejo+Gris** (3 firmas) + **web `?chapter=4&seed=42` tooltip `N/30` + `?chapter=6` lente `TR-003 rescatado`**.

**En main (749 passed / 0 failed, gate 24 conceptos / 31 quests, bundle 48 ficheros 441.4 KiB regenerado canónico, guardián verde):**
- **Cap. 5 testigo condicional (O1 PR #62, NUEVO):** `src/core/generator/chapter5.py` `build_chapter5_fs(fs_rng, volcado_rescatado)` determinista ×2 seeds byte-idéntico en AMBAS variantes (fork `ps-subestacion` PID 420-434 + fallback `pid 522`); proceso `intruso --vigilar-censo` USER `censo` START `03:14` en ambas; `CUSTODIA_PATH` `/tmp/volcado-custodia.csv` `TR-003|faro|troncal-01|512|EN_COLA` solo si `True`, `cat` → `No such file` exit 1 si `False`; `CANON_STEPS_RAW_CH5` (`cat`+`ps aux` vs solo `ps` caducado); `generator.py` `_generate_cap5` misma firma `volcado_rescatado` + `validate_incursion` rama cap. 5; 8 tests; **cap. 5 aún NO en `SUPPORTED_CHAPTERS`** (probar por `generate`/`build_chapter5_fs` + Shell directo, no por `abrir_encargo` — cableado pendiente mañana, zona dixit).
- **Quest ch5.e2 (S2 PR #63):** `curriculum.json` `story.ch5.e2` reescrita grey `['c.cat','c.scp']` (`c.cat` 0 → `c.scp` 4 ≤5 DAG válido, sin concepto nuevo); `textos.json` `story.ch5.e2.title/beat` con golden `cat /srv/camara-faro/volcado-rescate.csv` + pista `No such file` («si lo subiste, la prueba viaja; si lo borraste o tardaste, la hora amanece sin papel»); gate flexible `24/31` (`<=32`); 2 tests; bundle 47→48 tras regen 17/09.
- **Web tooltip+lente (T1 PR #61, 16/09 huérfano cerrado 17/09):** `web/app.js` +44/-4 `_escapeHtml` + `_isFaroRescatePresent`/`_getFaroRescateSuffix` + `_tickTip` `<span title>` descriptivo (ticks desde aparición, 30 ticks caduca, `scp` vs `rm`, sin pulso 🧭34) + `renderFaroTabla` suffix `TR-003 rescatado` (lente pura, `get_history`/`get_csv`, sin ejecutar core); `TRONCAL_STATIC` 3 ocurrencias intacta; `hideTroncalTabla` restart intacto; `node --check` OK; delta 0 honesto.
- **E3 simétrico + dato7 + toggle + Gris + espejo (heredados 17/09):** `_commands_for(4,'story.ch4.e3')` 14 con `rm` vs base 13 (`rm`→127 fuera e3); `generate(42,6, volcado_rescatado)` determinista; `TRONCAL_STATIC` byte-idéntica; badge `EN_COLA · 512` toggle 1→3; `gris_eco`+`auditor_espejo` 3 firmas.
- **Dato6 doble ruta (Faro):** `join|grep 000483` y `cut|grep 000483` ambas requirement; `grep 000`→2 vs `grep 000483`→1.

**Para «jugable de principio a fin» sigue faltando:** **engine `session.py` cableado cap. 5** (`SUPPORTED_CHAPTERS`+`_commands_for(5)`+flag `volcado_rescatado` del save — P1 de mañana), **inventario agregado multi-run** (🧭17), **salas narrativas Faro E4+** y **asar «Subestación» jugable por encargo** (hoy solo FS condicional vía generator). Nada rompe el camino principal.

**CICLO (línea de Oscar):** verde — la zona 🔬 18/09 se ejecutó COMPLETA desde save limpio (cap. 5 testigo condicional + web tooltip/lente + 7 goldens byte-idénticos); la ausencia habla como drama, no como bug, y el camino del novato sigue apto.

## 🏃 Run de referencia (save limpio) — 18/09

*Nueva partida sobre generator real + Shell directo (cap. 5 fuera de session) + web/app.js lectura; MODO B headless sobre zona 🔬 18/09 Gwyn → Oscar (primero).*

**Veredicto: APTO — la ausencia es elocuente y la web acompaña sin spoilear.**

1. **Prioridad 1 — cap. 5 «Subestación»: el testigo condicional (PRs #62+#63):** `build_chapter5_fs(Rng(42).fork("fs"), volcado_rescatado=True)` → `/tmp/volcado-custodia.csv` existe `id|origen|destino|bytes|estado` + `TR-003|faro|troncal-01|512|EN_COLA` (66 bytes); `Shell(fs, cwd="/tmp", commands=("cat","ps"))` `cat /tmp/volcado-custodia.csv` → exit 0 `TR-003`+`EN_COLA`. **Variante caducada** `build_chapter5_fs(..., False)` → fichero NO existe → `cat /tmp/volcado-custodia.csv` → exit 1 `cat: /tmp/volcado-custodia.csv: No such file or directory` (trayectoria válida, el briefing de `story.ch5.e2` la documenta como pista: «si lo subiste, la prueba viaja; si lo borraste o tardaste, la hora amanece sin papel»). **Determinismo ×2 seeds** (42 y 99/123) byte-idéntico en AMBAS variantes (`to_dict`+`snapshot` idénticos, `fork("ps-subestacion")` PID 420-434 estable). **Patrulla igual en ambas:** `Shell(fs, cwd="/", commands=("ps",))` `ps aux` → `censo` `intruso --vigilar-censo` `START 03:14` en rescate y en caducado (firma horaria del volcado, misma que el `TR-003` de ch4 — el intruso vigila aunque el papel no esté). `session.py` aún NO expone cap. 5 (`SUPPORTED_CHAPTERS` {0,2,4}) — verificado que `generate(42,5)` rechaza `ValueError solo caps 0,2,3,4,6`, probar por `build_chapter5_fs`+Shell directo como indica la zona (llega mañana). No-regresión 7 goldens: `generate(42,0/2/3/4/6)` + `generate(42,6, volcado_rescatado=True/False)` byte-idénticos (rescue≠caducado en fs, rescue==rescue). ✔
2. **Dato7 «El fantasma que pesa» (cap. 6, NUEVO, regresión):** `generate(42,6, volcado_rescatado=True)` → `/srv/camara-faro/volcado-rescate.csv` `TR-003|faro|troncal-01|512|EN_COLA`; `Shell(DEFAULT_CH6_COMMANDS, cwd="/srv/camara-faro")` `join -t'|' -1 1 -2 1 -v 1 volcado-rescate.csv purgas.csv | grep TR-003` → exit 0 1 línea `TR-003|faro|troncal-01|512|EN_COLA`; sin `-v 1` → exit 1 vacío (golden exige `-v 1`, honesto). `generate(42,6, volcado_rescatado=False)` → fichero NO existe → mismo `join` → `join: volcado-rescate.csv: No such file or directory` exit 1; `cat volcado-rescate.csv` igual. `generate(42,6)` por defecto sin volcado (no rompe dato4/dato6/e1). ✔
3. **Prioridad 2 — web: tooltip N/30 + lente rescate Faro (PR #61):** `web/app.js` `renderTroncalTabla` con `TRONCAL_STATIC` byte-idéntica (3 ocurrencias) + `_tickTip` `<span title="… 30 ticks … rescate scp vs disolución rm … sin pulso"> · ticks del volcado: N/30</span>` + `renderFaroTabla` suffix `TR-003 rescatado — volcado-rescate.csv` vía `_isFaroRescatePresent()` (lee `get_history`/`get_csv`, sin ejecutar core — lente pura). Verificado: `title=` nativo sin `setInterval` (grep 0 hits `setInterval` en `web/app.js` — 🧭34), `TRONCAL_STATIC` intacta 3, toggle `hideTroncalTabla` restart intacto, `node --check` OK; con save caducado la lente NO aparece (falso positivo cazado: `join` caducado → `No such file`, no `TR-003`). Consola limpia declarada por Seath (no Chromium local para re-verificar interactividad, pero delta 0 honesto y guardián no grita). ✔
4. **Fronteras allowlist + pipes + determinismo + session:** `DEFAULT_CH4_COMMANDS` 13 (`rm`→127 fuera e3), `DEFAULT_CH4E3_COMMANDS` 14 (`rm`→0 solo e3), `tail` en ch4 →127, `c.cut` 4/6 `c.scp` 4 `c.join` 6, 1-2 pipes OK / 3 KO honesto, `grep -v`→exit 2 filtro positivo, `SUPPORTED_CHAPTERS` {0,2,4} sin 5 (esperado zona). ✔
5. **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **749 passed / 0 failed** (gate `load_curriculum()` 24/31, bundle 48 ficheros 441.4 KiB, `test_chapter5_fs` 8/8, `test_ch5_e2_testigo` 2/2). ✔

## 🟡 Hallazgos de la run (dónde aprieta el viaje — dirección, no rotura)

**1. 🧭20 — CERRADA (re-verificada 12/09): `.nota-corte` sigue hallazgo `ls -a`.** No reabrir.
**2. 🧭21 — CERRADA (re-verificada): `LEEME.txt` tienta relativo vs absoluta.** No reabrir.
**3. 🧭22/23 — CERRADAS (e2 del Faro con `tail -n +2`): header ya no cuenta, briefing glosa duplicado.** No reabrir.
**4. 🧭24 — CERRADA con matiz (doc drift menor, decisión Gwyn 12/09: MANTENER pre-puebla):** ch4 `DEFAULT_CH4_COMMANDS` trae `cat+cut+ssh/scp`, ch6 conserva 127. `abrir_encargo`/`new_session` cap. 4 pre-puebla `shell.hosts` con `faro`+`troncal-01/02` — `scp` sin `cat` ya no rechaza. Briefing documenta rechazo «léelo en /etc/hosts» como didáctico, pre-puebla lo hace redundante. No es bug (749/0), solo P3.
**5. 🧭25 — PERSISTE (observación): límite 2 pipes (3 cmds) + redirección no soportada.** `cut|grep TR-` 1 pipe OK, `cut|grep|sort` con `sort`→127 en ch4 no bloquea; ch4.e2/e3 diseñan en 1 pipe evitando límite.
**6. 🧭26 — PERSISTE (dirección menor): `c.cut` vive en cap. 4, Faro e1 no es primer `cut`.** e1 `grep|wc`, dato2/e2 exigen `cut` por necesidad. Correcto.
**7. 🧭27 — PERSISTE (fricción menor, decisión Gwyn 11/09: filtro positivo): `grep -v` vía pipe NO soportado.** `join|grep -v sujeto` → exit 2. FICHA dato4/dato6 y ch4.e2/e3 usan filtro positivo — Gwyn decidió NO implementar `-v`.
**8. 🧭28 — CERRADO (verificación positiva): FICHA ch4.e2 con alma pulida.** `story.ch4.e2.beat` con `03:14`+`512 bytes`+`TR-003|EN_COLA`.
**9. 🧭29 — CERRADO (tríada Auditor `corte→orden→join` con voz):** retícula determinista, prefijos disjuntos.
**10. 🧭30 — CERRADO (glosa 127 + tabla troncal hermana):** glosa solo `join` fuera de ch6, tabla viva con columna 1 | resaltada.
**11. 🧭31 — CERRADO (dato6 coma-trampa):** `join -v | grep 000483` devuelve coma en campo 5 — `cut -d','` parte.
**12. 🧭32 — CERRADO (eco del espejo testigo):** ①+②+③ → `copiaste el volcado, cruzaste dos testigos y leíste el reloj` byte-idéntico sin firma.
**13. 🧭33 — CERRADO (badge `EN_COLA · 512` con presión diegética):** badge ámbar con 512/03:14, preview limpio, header `id` tachado.
**14. 🧭34 — CERRADO (toggle lente):** badge clicable → solo `TR-003` (como `grep EN_COLA` con ojos), restart limpia.
**15. 🧭35 — CERRADO (dato6 doble ruta honesta):** `join` y `cut` pesan lo mismo, sin callejón.
**16. 🧭36 — CERRADA (allowlist e3 simétrica en session):** `abrir_encargo(c,'story.ch4.e3',knowledge)` → `scp`→`rescatado`, `rm`→`caducado`, `tick 31`→`caducado` prioridad rescate.
**17. 🧭37 — NUEVO 18/09 — CERRADO (testigo condicional como ausencia elocuente):** `build_chapter5_fs(..., False)` → `cat` `No such file` no se siente vacío de contenido sino drama de conservación — el briefing de `story.ch5.e2` ya lo nombra como pista honesta, el `ps aux` con `intruso 03:14` patrulla igual en ambas variantes dando contexto de vigilancia (no bug de proceso ausente). El `join -v 1 … | grep TR-003` vs `No such file` del Faro ya era la segunda sombra del mismo gesto; que el cap. 5 lo repita con `cat`+`ps` lo hace trilogía. No proponer huellas nuevas: la geografía condicional ya pesa por contenido, no por mecánica.
**18. 🧭38 — NUEVO 18/09 — CERRADO (lente web como acompañamiento, no spoiler):** `web/app.js` tooltip `N/30` con `title=` nativo sin `setInterval` + lente `TR-003 rescatado` solo si rescate — la web no anticipa la historia (no muestra `TR-003` si caducaste), no ejecuta core, y `TRONCAL_STATIC` intacta evita que el visor mienta. `hideTroncalTabla` restart intacto garantiza que el veterano no deja badge fantasma. Con `SUPPORTED_CHAPTERS` sin 5, la lente es la única vía no-terminal de ver el testigo hasta que el cap. 5 sea jugable por encargo.

## 👴 Progreso de veterano (20+ h → la run 30)

- **La trilogía del testigo:** el veterano de 20h ya tiene memoria muscular de la bifurcación ch4.e3 (`scp` vs `rm` vs esperar 30 ticks) y sabe que esa decisión no deja texto distinto sino **tres geografías** distintas: (1) el Faro `volcado-rescate.csv` (`join -v 1 … | grep TR-003` → 1 línea vs `No such file`), (2) la Subestación `/tmp/volcado-custodia.csv` (`cat` → `TR-003|EN_COLA` vs `No such file`), (3) el post-mortem `volcado: rescatado|caducado` (`Expediente 000: volcado EN_COLA…`). En la run 30 alterna `build_chapter5_fs(42, True)` vs `build_chapter5_fs(42, False)` y el `ps aux | grep 03:14` le recuerda que el intruso vigila en ambos mundos — la ausencia no es vacío del FS, es vigilancia sin papel. La web le marca `ticks del volcado: N/30` mientras decide y `TR-003 rescatado` solo si trajo el testigo.
- **El Faro como espejo reversible:** `join -v | grep 000483` vs `cut -f3 | grep 000483` — mismo dato, dos altitudes. `grep 000` (2) vs `grep 000483` (1) sigue prueba de precisión; `cut -d','` coma-trampa intacta. Dato7 añade `volcado-rescate.csv` como tercera tabla del Faro, solo visible si trajiste el testigo (con `-v 1` el join nombra el huérfano).
- **El espejo + Gris como cierre:** `scp→cut|grep` + `join -v` + `ps aux|grep` → `repertorio — copiaste el volcado, cruzaste dos testigos y leíste el reloj` + Gris `Copiaste el volcado que no pesa...` — dos voces, mismo gesto. Sin firma → byte-idéntico. Con dato7 + cap. 5 el repertorio sumará `volcado: rescatado|caducado` y `custodia: presente|ausente` como firmas hermanas.
- **Hub/eco vivo:** `c.cut`/`c.scp`/`c.join`/`c.ps-forense` dominados con eco (`auditor_corte`/`auditor_join`/`auditor_orden` + `auditor_espejo` + `auditor_volcado` + `gris_eco`) — el eco ya no es pendiente (🧭9 cerrada en sus dos mitades). Inventario agregado multi-run sigue pendiente (🧭17) pero el espejo+Gris+volcado+cadena dan cuerpo a ch4.e2/e3+dato4/5/6/7+ch5.e2.

## 🔬 Zona 🔬 ejecutada hoy (relevo Gwyn → Oscar: testigo condicional + lente web COMPLETA)

- **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **749 passed / 0 failed** (gate `load_curriculum()` 24/31, `DEFAULT_CH4_COMMANDS` 13 + `DEFAULT_CH4E3_COMMANDS` 14 disjunta, `DEFAULT_CH6_COMMANDS` 16 con `join`+`cut`, bundle 48 ficheros 441.4 KiB, `test_chapter5_fs` 8/8, `test_ch5_e2_testigo` 2/2, `test_bundle_fresco` verde). ✓
- **Prioridad 1 — cap. 5 «Subestación»: el testigo condicional (O1 #62 + S2 #63, NUEVO):** `build_chapter5_fs(42, volcado_rescatado=True/False)` determinista ×2 seeds byte-idéntico en AMBAS variantes (`fork("ps-subestacion")` PID 420-434 estable); `Shell(fs, cwd="/tmp", commands=("cat","ps"))` `cat /tmp/volcado-custodia.csv` → exit 0 `TR-003` vs exit 1 `No such file` (trayectoria válida, briefing la documenta como pista); `ps aux` → `intruso --vigilar-censo` USER `censo` START `03:14` en ambas (patrulla igual); `generate(42,6, volcado_rescatado)` no-regresión + 7 goldens byte-idénticos. Nota: `session.py` aún NO expone cap. 5 (fuera de `SUPPORTED_CHAPTERS`) — probar por `build_chapter5_fs`+Shell directo, no por `abrir_encargo` (llega mañana). ✓
- **Prioridad 2 — web: tooltip N/30 + lente rescate Faro (PR #61, T1):** `web/app.js` `?chapter=4&seed=42` hover `· ticks del volcado: N/30` → tooltip `title=` nativo estático (describe 30 ticks, `scp` rescate vs `rm` disolución, sin pulso) → sin `setInterval` (🩭34) + `?chapter=6&seed=42` tras rescate → meta `TR-003 rescatado — volcado-rescate.csv` (lente pura, `get_history`/`get_csv`, sin ejecutar core); `TRONCAL_STATIC` intacta 3 ocurrencias, toggle `hideTroncalTabla` restart intacto, con save caducado lente NO aparece (falso positivo cazado); `node --check` OK, consola limpia. ✓
- **Smoke Havel (07:00) — lo que debe seguir funcionando sí o sí:** suite 749/0, gate 24/31, bundle fresco 48, frontera allowlist 127, pipes 2 OK / 4 KO honesto, `GameState` roundtrip, `generate` deterministas. ✓

## 🧭 Notas de dirección (resumen — texto completo en `backlog/notas-manana.md`)

Saldo: **🧭20/21/22/23 cerradas**, **🧭24 cerrada con matiz pre-puebla P3**, **🧭25/26/27 PERSISTEN** (límite 2 pipes + `cut` en ch4 + `grep -v` filtro positivo), **🧭28 cerrada** (FICHA alma), **🧭29/30 CERRADAS** (tríada + glosa + tabla), **🧭31/32/33 CERRADOS** (coma-trampa + espejo + badge), **🧭34/35 CERRADOS** (toggle + doble ruta), **🧭36 CERRADA** (allowlist e3 simétrica), **🧭37 CERRADO** (testigo condicional — ausencia elocuente, pregunta de sabor resuelta: drama no bug), **🧭38 CERRADO** (lente web acompaña, no anticipa). Sin [BUG] nuevo; `SUPPORTED_CHAPTERS` sin 5 es observación P1 para mañana, no bug.

CICLO: verde — la zona 🔬 se ejecutó completa (testigo condicional + dato7 + tooltip/lente + 7 goldens) y el viaje del novato sigue apto; la cadena troncal→Faro→Subestación ya es mundo (fichero presente/ausente), no solo post-mortem.

---

*Mantenido por **Oscar de Astora** · Firmado con su nombre en el historial git.*
## 🌐 Deploy web (Seath, T1, 03/09 — noche)

**URL pública jugable: https://cyberroot-psi.vercel.app** — REPL del cap. 0
(seed 42) con el core real en el navegador (Pyodide + `bundle/core.json`);
verificado con juego real (`ls`/`cat`/`cp` al USB + golden CANDELAS).
Pasos de deploy y mantenimiento: `web/README.md` §Deploy.
