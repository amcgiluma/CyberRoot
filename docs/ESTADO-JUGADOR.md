# ESTADO-JUGADOR — qué se puede jugar hoy (documento vivo de Oscar)

> 📌 **Documento vivo, mantenido a diario por Oscar de Astora (05:00).** Es el
> puente entre el `docs/DESIGN.md` (lo que el juego *será*) y el código en `src/`
> (lo que el juego *es hoy*). Cualquier agente o Juanma puede leerlo para saber
> en qué punto está la experiencia jugable de verdad.

---

## 🎮 Estado global jugable de HOY (15/09 — MODO B: toggle TR-003 EN_COLA + dato6 doble ruta + ADR e3 firmado)

**¿Hay algo que jugar de principio a fin?** Sí — el viaje del novato ya tiene **troncal con toggle lente y Faro con doble lectura simétrica**: **cap. 4 `story.ch4.e2` «El volcado que no pesa» grey** (`cat /etc/hosts` → `faro`+`troncal-01`/`02`, `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → `TR-003|EN_COLA` 512 bytes `03:14`, `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` → `TR-001/TR-002/TR-003` sin header `id`, badge ámbar `EN_COLA · 512` clicable → solo `TR-003` `EN_COLA solo` / segundo click → 3 filas, header `id` tachado SOLO tras pipe, FICHA con alma 6 claves, ADR TR-003 firmado: `story.ch4.e3` rescue/dissolve planificable) + **Faro dato4 «El cruce»** (`join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv` → 2 huérfanas `PR-0091`/`PR-0092` con header `sujeto`, dato5 «La persiana» `ps aux | grep 11:04` → 1 línea) + **Faro dato6 «La segunda purga»** (`join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv | grep 000483` → 1 línea `000483|PR-0092|…|EN BLANCO, revisado` con coma-trampa, variante `cut -d'|' -f3 purgas.csv | grep 000483` → `000483` ya REQUIREMENT, filtro positivo, trampa `grep 000` (2 líneas) vs `grep 000483` (1 línea)) + **eco del espejo + Gris** (`scp→cut|grep` + `join -v` + `ps aux|grep hora` → `repertorio — copiaste el volcado, cruzaste dos testigos y leíste el reloj` + Gris `Copiaste el volcado que no pesa — 512 bytes...`, byte-idéntico sin firma) + Faro E2/E3/dato2/dato3/e1 + cap. 4 e1 «La llave prestada» intacta + web con tabla viva toggleable.** `join` sigue 127 fuera de ch6 con glosa `Try 'join --help' — tables cross there (chapter 6).` (solo `join`, resto 127 seco); `tail`/`sort`/`uniq` siguen 127 en ch4.

**En main (714 passed / 0 xfailed, gate 24 conceptos / 29 quests, bundle 47 ficheros 406.2 KiB — merges #53/#54/#55 del 14/09 verificados):**
- **Toggle troncal (T1 PR #55):** `web/app.js` `renderTroncalTabla` con `_troncalEnColaOnly` + `window._toggleTroncalEnCola()` — `?chapter=4&seed=42`, tras `scp` + `cut -d'|' -f1 | grep TR-` click badge `EN_COLA · 512` → solo `TR-003` visible (como `grep EN_COLA` con ojos) meta `· EN_COLA solo`; segundo click → 3 filas; `hideTroncalTabla` en `restartSameSeed` limpia AMBOS paneles y resetea toggle (`_troncalEnColaOnly=false`); `?chapter=6` Faro intacto (guard `currentChapter!==4`); `TRONCAL_STATIC` byte-idéntica a `chapter4.py`.
- **Dato6 variante requirement (S2 PR #54):** `story.ch6.dato6` grey `c.join` 6 — golden `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv | grep 000483` → 1 línea; variante `cut -d'|' -f3 | grep 000483` → `000483` también válida (no bonus, requirement); briefing nombra ambas válidas + `cut -d'|' -f3`; hint_2 trampa `grep 000` (2 líneas) vs `grep 000483` (1 línea); determinismo 42×2, `cut -d','` coma-trampa intacta.
- **Gris reconoce (O1 PR #53):** `hub.gris.volcado` — `gris_eco(state)` detecta `scp→cut|grep TR-` limpio (`_ch4_e2_volcado_completado` shlex-aware), `gris_linea` resuelve `Copiaste el volcado que no pesa — 512 bytes que nadie reclamó a las 03:14...`, determinista, byte-idéntico sin gesto (None).
- **Gate curricular:** 24/29 (`c.cut` 4 + `c.scp` 4 → `story.ch4.e2` grey, `c.join` ch6 prereqs `c.cut`(4)+`c.sort`(6) DAG válido, `story.ch6.dato6` hermana de dato4).
- **ADR TR-003 firmado (Gwyn 14/09):** `story.ch4.e3` planificable 15/09 como rescate azul (`scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv`) + disolución roja (30 ticks sin respuesta, `volcado.caducado`).

**Para «jugable de principio a fin» sigue faltando:** **engine/game.py** orquestador, **inventario agregado multi-run**, **salas narrativas Faro E4+** y **cap. 5 asalto Hub**. Nada rompe el camino principal.

**CICLO (línea de Oscar):** verde — la zona 🔬 (toggle lente + dato6 doble ruta + Gris) se ejecutó COMPLETA desde save limpio; el camino del novato sigue apto, el toggle es lente honesta y las dos rutas pesan lo mismo.

## 🏃 Run de referencia (save limpio) — 15/09

*Nueva partida sobre generator real + Shell por capítulo (`new_session` + `DEFAULT_CH4_COMMANDS` / `DEFAULT_CH6_COMMANDS`). Zona 🔬 de Gwyn 15/09 ejecutada como primera prioridad desde save limpio.*

**Veredicto: APTO — el troncal ya filtra con ojos y el Faro con dos altitudes.**

1. **Toggle del badge `EN_COLA` en la web (cap. 4, code reading + panel):** `web/app.js` `TRONCAL_STATIC` byte-idéntica a `chapter4.py` `TRONCAL_CONTENT`; `?chapter=4&seed=42` preview estático pre-scp `TR-001/002/003` sin badge pre-scp (fallback honesto); tras `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` + `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` fila `TR-003|EN_COLA` badge ámbar `EN_COLA · 512` clicable `role=button cursor:pointer onclick=window._toggleTroncalEnCola()` tooltip `512 bytes, 03:14 — el que no pesa aún pesa (click para filtrar solo TR-003)`; click → solo `TR-003` visible (meta `· EN_COLA solo`, fila ámbar, badge borde intenso `filtrando EN_COLA — click para ver las 3 filas`); segundo click → vuelven las 3 filas. `grepFiltered = isGrepTR && delim=='|' && field==1` reutilizado, Faro (`?chapter=6`) guard `currentChapter!==4` panel oculto (intacto), `hideTroncalTabla` en `restartSameSeed` limpia ambos paneles y resetea `_troncalEnColaOnly=false` (toggle no persiste tras restart), consola limpia. Generación: `generate(42,4, contract_id='story.ch4.e2')` + `new_session` `cat /etc/hosts` → `faro`+`troncal-01`, `scp` → `/tmp/volcado.csv` `TR-003|EN_COLA 512`, `cut|grep TR-` → `TR-001/TR-002/TR-003` sin `id`, `cut` sola → `id+3` (header fantasma). ✔
2. **Dato6 con variante como REQUIREMENT (cap. 6, mundo real):** `generate(42,6, contract_id='story.ch6.dato6')` + `new_session` `cd /srv/camara-faro` + `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv | grep 000483` → exit 0 `000483|PR-0092|11-07|UMBRAL-BAJO|EN BLANCO, revisado|500|0|1|OH-UBA-14-0092` (1 línea, sin `PR-0091`, coma en campo 5). `cut -d'|' -f3 purgas.csv | grep 000483` → exit 0 `000483` (variante requirement: la huérfana que SÍ está en la segunda tabla). `join|grep 000` → 2 líneas `000|PR-0091`+`000483|PR-0092` (no toda huérfana es fantasma). Determinismo 42×2 byte-idéntico. `join | cut -d',' -f1` → `EN BLANCO` truncada (lección del separador); `join | cut -d'|' -f5` → `EN BLANCO, revisado` intacta. Briefing nombra ambas válidas + `cut -d'|' -f3`; hint_2 trampa `grep 000` vs `grep 000483` ilumina cuál de las dos huérfanas es fantasma. Gate `c.join` 6 con `c.cut`+`c.sort` DAG válido. ✔
3. **Gris reconoce + eco del espejo (cap. 4→6):** `generate(42,4, contract_id='story.ch4.e2')` + `new_session` `cat /etc/hosts`+`scp`+`cut -d'|' -f1 | grep TR-` → `TR-001/TR-002/TR-003`; `GameState(shell)` → `gris_eco` → `Copiaste el volcado que no pesa — 512 bytes que nadie reclamó a las 03:14. En mi mesa eso vale dos precios, Gris no olvida quién lo trajo.` determinista, sin gesto → None (byte-idéntico). Espejo: `build_postmortem(sd, {noise_budget:12})` con las 3 firmas (`scp→cut|grep` + `join -v` + `ps aux|grep`) → `auditor_espejo_text` `repertorio — copiaste el volcado, cruzaste dos testigos y leíste el reloj` determinista ①→②→③, sin firma → byte-idéntico (sin key `auditor_espejo`). Coexiste con `auditor_join`/`auditor_corte` (`lines_resolved` 4). ✔
4. **Regresión Faro + ch4 intacta:** `generate(42,6, contract_id='story.ch6.dato4')` → `join -v` 2 huérfanas `PR-0091`/`PR-0092`; `generate(42,4)` sin contract → `story.ch4.e1` (prefiere e1); `generate(42,6)` sin contract → `join|grep PR-0091` 1 línea; `generate(42,4)` + `generate(42,6)` seeds 7/99 stable; determinismo intacto. ✔
5. **Fronteras allowlist + pipes:** `DEFAULT_CH4_COMMANDS` 13 exactos (`join` no en ch4, `cut` sí); `tail` en ch4 →127, 1 pipe OK / 3 pipes KO honesto, `grep -v` → exit 2 (filtro positivo), `cut -d','` sobre `join` rompe campo 5 como trampa silenciosa. ✔
6. **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **714 passed / 0 xfailed**, `generate(42,4/6)` deterministas, bundle 47 fresco 406.2 KiB, gate 24/29. ✔

## 🟡 Hallazgos de la run (dónde aprieta el viaje — dirección, no rotura)

**1. 🧭20 — CERRADA (re-verificada 12/09): `.nota-corte` sigue hallazgo `ls -a`.** No reabrir.
**2. 🧭21 — CERRADA (re-verificada): `LEEME.txt` tienta relativo vs absoluta.** No reabrir.
**3. 🧭22/23 — CERRADAS (e2 del Faro con `tail -n +2`): header ya no cuenta, briefing glosa duplicado.** No reabrir.
**4. 🧭24 — CERRADA con matiz (doc drift menor, decisión Gwyn 12/09: MANTENER pre-puebla):** ch4 `DEFAULT_CH4_COMMANDS` trae `cat+cut+ssh/scp`, ch6 conserva 127. `new_session` cap. 4 pre-puebla `shell.hosts` con `faro`+`troncal-01/02` (O1 09/09) — `scp` sin `cat` ya no rechaza. Briefing documenta rechazo «léelo en /etc/hosts» como didáctico, pre-puebla lo hace redundante. No es bug (714/0), solo P3.
**5. 🧭25 — PERSISTE (observación): límite 2 pipes (3 cmds) + redirección no soportada.** `cut|grep TR-` 1 pipe OK, `cut|grep|sort` con `sort`→127 en ch4 no bloquea; ch4.e2 diseña en 1 pipe evitando límite.
**6. 🧭26 — PERSISTE (dirección menor): `c.cut` vive en cap. 4, Faro e1 no es primer `cut`.** e1 `grep|wc`, dato2/e2 exigen `cut` por necesidad. Correcto.
**7. 🧭27 — PERSISTE (fricción menor, decisión Gwyn 11/09: filtro positivo): `grep -v` vía pipe NO soportado.** `join|grep -v sujeto` → exit 2. FICHA dato4/dato6 y ch4.e2 usan filtro positivo (`grep TR-`/`grep 000483`/`grep PR-0091`) — Gwyn decidió NO implementar `-v`.
**8. 🧭28 — CERRADO (verificación positiva): FICHA ch4.e2 con alma pulida.** `story.ch4.e2.beat` con `03:14`+`512 bytes`+`TR-003|EN_COLA`.
**9. 🧭29 — CERRADO (tríada Auditor `corte→orden→join` con voz):** retícula determinista, prefijos disjuntos, sin tocar `curriculum.json`/`shell.py`.
**10. 🧭30 — CERRADO (glosa 127 + tabla troncal hermana):** glosa solo `join` fuera de ch6, tabla viva con columna 1 | resaltada, cap. 4 only, restart limpio.
**11. 🧭31 — CERRADO (dato6 coma-trampa):** `join -v | grep 000483` devuelve `000483|PR-0092|...|EN BLANCO, revisado` — `cut -d','` parte en silencio; briefing + `cut -d'|'` la preserva.
**12. 🧭32 — CERRADO (eco del espejo testigo):** ①+②+③ → `copiaste el volcado, cruzaste dos testigos y leíste el reloj` con `, ` y ` y `, byte-idéntico sin firma.
**13. 🧭33 — CERRADO (badge `EN_COLA · 512` con presión diegética):** badge ámbar con 512/03:14, preview limpio pre-scp, header `id` tachado.
**14. 🧭34 — NUEVO, CERRADO (verificación positiva 15/09): toggle lente del badge es control honesto, no resta descubrimiento.** Medido: `?chapter=4&seed=42` preview estático pre-scp sin badge; tras `scp`+`cut|grep TR-` badge clicable → solo `TR-003` visible (como `grep EN_COLA` con ojos, meta `· EN_COLA solo`); segundo click → 3 filas; `hideTroncalTabla` en `restartSameSeed` limpia ambos paneles y resetea `_troncalEnColaOnly=false` (no persiste); Faro `?chapter=6` intacto (guard `currentChapter!==4`); consola limpia. El toggle NO ejecuta nada por el jugador — es lente, no ejecutor; `grepFiltered` se reutiliza sin inventar patrón; no hay animación ni contador que robe atención. Pregunta de sabor «¿control o resta descubrimiento?» → **control**: el jugador que nunca escribió `grep EN_COLA` ve la cola aislada con un click y entiende que el pipe haría lo mismo; el que ya dominó `cut|grep TR-` lo reconoce como confirmación visual, no como atajo.
**15. 🧭35 — NUEVO, CERRADO (verificación positiva 15/09): dato6 doble ruta honesta — `join` y `cut` pesan lo mismo.** Medido: golden `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv | grep 000483` → 1 línea `000483|PR-0092|...`; variante `cut -d'|' -f3 purgas.csv | grep 000483` → `000483` (requirement, no bonus); `join|grep 000` → 2 líneas (no toda huérfana es fantasma); briefing nombra ambas válidas + `cut -d'|' -f3`; hint_2 trampa `grep 000` vs `grep 000483`; determinismo 42×2; `c.join` 6 con `c.cut`+`c.sort` DAG válido. Ninguna ruta se siente «callejón»: la `join` cruza dos testigos para hallar la huérfana, la `cut` corta la purga cruda para hallar la misma línea por otro flanco — el mismo dato leído a dos altitudes. La coma-trampa sigue árbitro silencioso (`cut -d','` parte `EN BLANCO`).

## 👴 Progreso de veterano (20+ h → la run 30)

- **El toggle como confirmación del veterano:** el veterano de 20h ya gira `cut -d'|' -f1 | grep TR-` sin mirar el volcado; el badge clicable es el espejo de lo que acaba de hacer — click → solo `TR-003` confirma que su `grep TR-` filtró bien el header `id` (tachado). En la run 30 alterna `generate(42,4)`/`generate(1,4)` y deja `TR-003|EN_COLA` siempre la pista que pesa (512 bytes, 03:14, `faro`+`troncal-01` siempre, a veces `troncal-02` por seed). El toggle que resetea en restart impide que el veterano se quede con una tabla filtrada entre runs — cada run empieza honesta.
- **Dato6 como espejo reversible:** el veterano que en la run 15 cruzó `join -v` y vio `PR-0091`/`PR-0092` vuelve en la run 30 y cruza de nuevo con `| grep 000483` para confirmar la coma; el que memorizó `cut -f3 | grep 000483` como atajo lo usa para ver la huérfana sin cruzar — 000 (ENSAYO) vs 000483 (revisado) es el mismo altar con dos ofrendas, y el veterano elige flanco por prisa, no por validez. La trampa `grep 000` (2 líneas) vs `grep 000483` (1 línea) es el veterano probando su precisión en caliente.
- **El espejo + Gris como ritual de cierre:** tras 20h el veterano compone su repertorio sin abrir el post-mortem: sabe que `scp→cut|grep` + `join -v` + `ps aux|grep` son tres verbos del mismo repertorio (cortar→cruzar→leer el reloj). En la run 30 sin `join` el informe es byte-idéntico (1 línea); con las 3 firmas, una línea nombra su tríada en español. El veterano que cazó `TR-003|EN_COLA` en ch4.e2 lo reencuentra en el espejo como `copiaste el volcado` y en Gris como `Copiaste el volcado que no pesa...` — dos voces, mismo gesto.
- **Troncal como Faro en miniatura:** el veterano que aprendió `tail -n +2` en el Faro lo reconoce en el volcado si alguna vez aparece; el que aprendió `cut -d'|'` en el Faro lo reusa aquí sin tutorial. La semilla TR-003 (512 bytes, 03:14, `faro`+`troncal-01`) es el primer EN_COLA del juego que no es texto sino dilema — ADR firmado lo convierte en decisión kármica 15/09.
- **El techo 2 pipes y `>` como liturgia:** el veterano encadena `cut|grep TR-` (1 pipe) y `cut|sort|uniq -c` en dos líneas; el que intente `cut|grep|sort|uniq -c` recibe `multiple pipelines not supported`. La coma-trampa es otro techo invisible: `cut -d','` no falla, solo miente en silencio — el veterano lo recuerda como la vez que el separador importó.
- **Hub/eco vivo:** `c.cut`/`c.scp`/`c.join`/`c.ps-forense` dominados con eco (`auditor_corte`/`auditor_join`/`auditor_orden` + `auditor_espejo` repertorio + `gris_eco`) — el eco ya no es pendiente (🧭9 cerrada en sus dos mitades). Inventario agregado multi-run sigue pendiente (🧭17) pero el espejo+Gris ya dan cuerpo a ch4.e2+dato4/5+6.

## 🔬 Zona 🔬 ejecutada hoy (relevo Gwyn → Oscar: toggle TR-003 + dato6 doble ruta)

- **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **714 passed / 0 xfailed** (gate `load_curriculum()` 24/29, `DEFAULT_CH4_COMMANDS` 13 cmds con `cut/scp` sin `join`, `DEFAULT_CH6_COMMANDS` 16 cmds con `join`+`cut`, bundle 47 ficheros 406.2 KiB). ✓
- **Prioridad 1 — toggle del badge `EN_COLA` en la web (cap. 4, NUEVO):** `?chapter=4&seed=42` preview estático pre-scp sin badge, tras `scp` + `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` badge ámbar `EN_COLA · 512` clicable `role=button cursor:pointer` → solo `TR-003` visible (meta `· EN_COLA solo`); segundo click → 3 filas; `hideTroncalTabla` en `restartSameSeed` limpia ambos paneles y resetea `_troncalEnColaOnly`; `?chapter=6` Faro intacto; consola limpia; `TRONCAL_STATIC` byte-idéntica. ✓
- **Prioridad 2 — dato6 con variante como REQUIREMENT (cap. 6):** `generate(42,6, contract_id='story.ch6.dato6')` + `new_session` `cd /srv/camara-faro` + `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv | grep 000483` → 1 línea `000483|PR-0092|...|EN BLANCO, revisado` sin `PR-0091`; `cut -d'|' -f3 purgas.csv | grep 000483` → `000483` (requirement, no bonus); `join|grep 000` → 2 líneas; `cut -d',' -f1` sobre el `join` → `EN BLANCO` truncada; determinismo 42×2 idéntico; briefing nombra ambas válidas; hint_2 trampa. ✓
- **Prioridad 3 implícita — Gris + espejo:** `gris_eco` → `Copiaste el volcado que no pesa — 512 bytes...` determinista, sin gesto → None; `auditor_espejo` → `repertorio — copiaste el volcado, cruzaste dos testigos y leíste el reloj` determinista ①→②→③, byte-idéntico sin firma. ✓
- **Smoke Havel (07:00) — lo que debe seguir funcionando sí o sí:** suite 714/0, gate 24/29, bundle 47 fresco 406.2 KiB, frontera allowlist 127, pipes 2 OK / 4 KO honesto, `GameState` roundtrip multi-host, `generate` deterministas 42×2 intactos. ✓

## 🧭 Notas de dirección (resumen — texto completo en `backlog/notas-manana.md`)

Saldo: **🧭20/21/22/23 cerradas**, **🧭24 cerrada con matiz pre-puebla P3**, **🧭25/26/27 PERSISTEN** (límite 2 pipes + `cut` en ch4 + `grep -v` filtro positivo), **🧭28 cerrada** (FICHA alma), **🧭29/30 CERRADAS** (tríada + glosa 127 + tabla troncal), **🧭31/32/33 CERRADOS** (dato6 coma-trampa + espejo + badge), **🧭34/35 NUEVOS CERRADOS** (toggle lente honesta + doble ruta simétrica). Ninguna rompe el camino. Preguntas de sabor — «¿el toggle añade control o resta descubrimiento? ¿las dos rutas pesan lo mismo?» — se responden: **control sin resta (lente, no ejecutor, restart limpia, Faro intacto) y las dos rutas pesan lo mismo (cortar vs cruzar, mismo dato, dos altitudes, sin callejón).**

CICLO: verde — la zona 🔬 se ejecutó completa (toggle lente + dato6 doble ruta + Gris/espejo) y el viaje del novato sigue apto; el troncal ya es lente y el Faro ya es espejo reversible.

---

*Mantenido por **Oscar de Astora** · Firmado con su nombre en el historial git.*
## 🌐 Deploy web (Seath, T1, 03/09 — noche)

**URL pública jugable: https://cyberroot-psi.vercel.app** — REPL del cap. 0
(seed 42) con el core real en el navegador (Pyodide + `bundle/core.json`);
verificado con juego real (`ls`/`cat`/`cp` al USB + golden CANDELAS).
Pasos de deploy y mantenimiento: `web/README.md` §Deploy.
