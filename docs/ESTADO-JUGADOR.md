# ESTADO-JUGADOR — qué se puede jugar hoy (documento vivo de Oscar)

> 📌 **Documento vivo, mantenido a diario por Oscar de Astora (05:00).** Es el
> puente entre el `docs/DESIGN.md` (lo que el juego *será*) y el código en `src/`
> (lo que el juego *es hoy*). Cualquier agente o Juanma puede leerlo para saber
> en qué punto está la experiencia jugable de verdad.

---

## 🎮 Estado global jugable de HOY (14/09 — MODO B: dato6 coma-trampa + eco del espejo + huella troncal EN_COLA)

**¿Hay algo que jugar de principio a fin?** Sí — el viaje del novato ya tiene **troncal con huella visible y Faro con doble lectura del cruce**: **cap. 4 `story.ch4.e2` «El volcado que no pesa» grey** (`cat /etc/hosts` → `faro`+`troncal-01`/`02`, `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → `TR-003|EN_COLA` 512 bytes `03:14`, `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` → `TR-001/TR-002/TR-003` sin header `id`, badge ámbar `EN_COLA · 512` + header `id` tachado SOLO tras el pipe, FICHA con alma 6 claves) + **Faro dato4 «El cruce»** (`join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv` → 2 huérfanas `PR-0091`/`PR-0092` con header `sujeto`, dato5 «La persiana» `ps aux | grep 11:04` → 1 línea) + **Faro dato6 «La segunda purga» NUEVA** (`join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv | grep 000483` → 1 línea `000483|PR-0092|...|EN BLANCO, revisado` con coma-trampa, variante `cut -d'|' -f3 | grep 000483` → `000483` sin `join`, filtro positivo) + **eco del espejo** (`scp→cut|grep` + `join -v` + `ps aux|grep hora` → una línea `repertorio — copiaste el volcado, cruzaste dos testigos y leíste el reloj`, byte-idéntico sin firma) + Faro E2/E3/dato2/dato3/e1 + cap. 4 e1 «La llave prestada» intacta + web con tabla viva hermana.** `join` sigue 127 fuera de ch6 con glosa `Try 'join --help' — tables cross there (chapter 6).` (solo `join`, resto 127 seco); `tail`/`sort`/`uniq` siguen 127 en ch4; `auditor_join` cita anti-join y `auditor_espejo` nombra repertorio sin filtrar fila.

**En main (708 passed / 0 xfailed, gate 24 conceptos / 29 quests, bundle 47 ficheros 402.3 KiB — merges #50/#51/#52 del 13/09 verificados):**
- **Eco del espejo (O1 PR #50):** `postmortem.espejo.repertorio` — `build_postmortem` detecta ① `scp` antes de `cut -d'|' -f1 | grep TR-` (`_has_espejo_volcado` shlex-aware), ② `join` con `-v` (`_find_join` pipe-aware), ③ `ps aux|grep` con hora (`_has_espejo_reloj`); orden determinista ①→②→③, una línea `Expediente 000: repertorio — {huellas}. Continuidad del ensayo: estable.` con `, ` y ` y `, sin firma byte-idéntico, coexistencia con `auditor_join`/`auditor_corte` verificada (`lines_resolved` 1→4).
- **Quest dato6 (S2 PR #51):** `story.ch6.dato6` grey `c.join` 6 — golden `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv | grep 000483` → exit 0 `000483|PR-0092|11-07|UMBRAL-BAJO|EN BLANCO, revisado|...` sin `PR-0091`; variante `cut -d'|' -f3 purgas.csv | grep 000483` → `000483`; `cut -d',' -f1` sobre el `join` parte `EN BLANCO` sin `, revisado` (la coma-trampa es la lección del separador); determinismo 42×2, briefing nombra la `,` y el `|` como separador que rompe.
- **Huella troncal visible (T1 PR #52):** `web/app.js` `renderTroncalTabla` con `grepFiltered` — `?chapter=4&seed=42` preview estático `TR-001/002/003` sin badge pre-scp, tras `scp` + `cut -d'|' -f1 | grep TR-` fila `TR-003|EN_COLA` badge ámbar `EN_COLA · 512` + header `id` tachado `line-through`, `?chapter=6` Faro intacto, restart limpia ambos paneles. `TRONCAL_STATIC` byte-idéntica a `chapter4.py` `TRONCAL_CONTENT`.
- **Cap. 4 e2 + Faro dato4/dato5 intactos:** `generate(42,4, contract_id='story.ch4.e2')` determinista 42×2; `cat /etc/hosts` → `faro`+`troncal-01` (seed 1 → +`troncal-02`); `scp` → `/tmp/volcado.csv` `TR-003|EN_COLA 512`; `generate(42,6)` → `join -v` 2 huérfanas + `ps aux|grep 11:04` 1 línea; `cut|sort|uniq -c` y `sort -t'|' -k12 -n|head` intactos.
- **Gate curricular:** 24/29 (`c.cut` 4 + `c.scp` 4 → `story.ch4.e2` grey, `c.join` ch6 prereqs `c.cut`(4)+`c.sort`(6) DAG válido, nueva `story.ch6.dato6` hermana de dato4).
- **Bandit + Shell límites:** `ls`5 vs `ls -a`6, `tail -n +2 | cut | sort` 2 pipes OK, `tail|cut|sort|uniq -c` 3 pipes → `multiple pipelines not supported` exit 2; `grep -v` vía pipe → exit 2 (filtro positivo como lección).

**Para «jugable de principio a fin» sigue faltando:** **engine/game.py** orquestador, **inventario agregado multi-run**, **salas narrativas Faro E4+** y **cap. 5 asalto Hub**. Nada rompe el camino principal.

**CICLO (línea de Oscar):** verde — la zona 🔬 (dato6 + espejo 3 firmas + badge TR-003) se ejecutó COMPLETA desde save limpio; el camino del novato sigue apto y la coma-trampa enseña mintiendo con la verdad.

## 🏃 Run de referencia (save limpio) — 14/09

*Nueva partida sobre generator real + Shell por capítulo (`new_session` + `DEFAULT_CH4_COMMANDS` / `DEFAULT_CH6_COMMANDS`). Zona 🔬 de Gwyn 14/09 ejecutada como primera prioridad desde save limpio.*

**Veredicto: APTO — el Faro ya miente con la verdad y el espejo recuerda tu repertorio.**

1. **`story.ch6.dato6` «La segunda purga» — golden y coma-trampa (cap. 6, mundo real):** `generate(42,6, contract_id='story.ch6.dato6')` + `new_session` `cd /srv/camara-faro` + `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv | grep 000483` → exit 0 `000483|PR-0092|11-07|UMBRAL-BAJO|EN BLANCO, revisado|500|0|1|OH-UBA-14-0092` (1 línea, sin `PR-0091`, coma dentro de campo 5). `cut -d'|' -f3 purgas.csv | grep 000483` → exit 0 `000483` (variante bonus: la huérfana que SÍ está en la segunda tabla — 000 vs 000483). `join -t'|' -1 3 -2 1 -v 1 | grep 000` → 2 líneas `000|PR-0091`+`000483|PR-0092` (no toda huérfana es fantasma). Determinismo: repetir la golden 42×2 → misma fila byte-idéntica. `join | cut -d',' -f1` → `EN BLANCO` sin `, revisado` (partir por `,` rompe el campo — lección del separador). `join | cut -d'|' -f5` → `EN BLANCO, revisado` intacta. Gate: `c.join` requiere `c.cut`(4)+`c.sort`(6) DAG válido, quest no accesible sin dato4. ✔
2. **Eco del espejo — las 3 firmas en una línea (cap. 4→6, mundo real):** `generate(42,4, contract_id='story.ch4.e2')` + `new_session` `cat /etc/hosts`+`scp`+`cut -d'|' -f1 | grep TR-` → `TR-001/TR-002/TR-003`; luego en `generate(42,6)` `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv` → 2 huérfanas + `ps aux | grep 11:04` → 1 línea. `build_postmortem(shell.to_dict(), {noise_budget:12})` con las 3 firmas → `auditor_espejo_text` `Expediente 000: repertorio — copiaste el volcado, cruzaste dos testigos y leíste el reloj. Continuidad del ensayo: estable.` + `auditor_join_text` y `auditor_corte_text` coexistiendo (`lines_resolved` 4: pico + corte + join + espejo). Sin firma (`cat purgas.csv` sola) → 1 línea byte-idéntica (sin key `auditor_espejo`). Con UNA sola (`scp→cut|grep` sola) → `repertorio — copiaste el volcado.` Orden ①→②→③ determinista (no ③→①). ✔
3. **Huella troncal visible en web (cap. 4, code reading + panel):** `web/app.js` `TRONCAL_STATIC` byte-idéntica a `chapter4.py` `TRONCAL_CONTENT`; `parseTroncalCut` detecta `/tmp/volcado.csv` + `-d'|'`+`-f1`; `renderTroncalTabla(cutInfo, csv, {grepFiltered})` — `grepFiltered = isGrepTR && delim=='|' && field==1`; con `grepFiltered` header `id` → `line-through` tooltip `filtrado por grep TR- — el header miente`, fila `TR-003|EN_COLA` → fondo ámbar + badge `EN_COLA · 512` tooltip `512 bytes, 03:14 — el que no pesa aún pesa`; `updateTroncalTabla` detecta `grep TR-` en la línea; `previewTroncalTabla` sin grep → sin badge/tachado (fallback honesto); `?chapter=6` guard `currentChapter!==4` → panel oculto (Faro intacto); `hideTroncalTabla` en `restartSameSeed` limpia ambos paneles, consola limpia. ✔
4. **Regresión Faro + ch4 intacta:** `generate(42,6, contract_id='story.ch6.dato4')` → `join -v` 2 huérfanas `PR-0091`/`PR-0092` (coma en 0092); `generate(42,4)` sin contract → `story.ch4.e1` (prefiere e1); `generate(42,6)` sin contract → `join|grep PR-0091` 1 línea; determinismo seeds 7/99 stable. ✔
5. **Fronteras allowlist + pipes:** `DEFAULT_CH4_COMMANDS` 13 exactos (`join` no en ch4, `cut` sí); `tail` en ch4 →127, 1 pipe OK / 3 pipes KO honesto, `grep -v` → exit 2 (filtro positivo), `cut -d','` sobre `join` rompe campo 5 como trampa silenciosa y didáctica. ✔
6. **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **708 passed / 0 xfailed**, `generate(42,4/6)` deterministas, bundle 47 fresco 402.3 KiB, gate 24/29. ✔

## 🟡 Hallazgos de la run (dónde aprieta el viaje — dirección, no rotura)

**1. 🧭20 — CERRADA (re-verificada 12/09): `.nota-corte` sigue hallazgo `ls -a`.** No reabrir.
**2. 🧭21 — CERRADA (re-verificada): `LEEME.txt` tienta relativo vs absoluta.** No reabrir.
**3. 🧭22/23 — CERRADAS (e2 del Faro con `tail -n +2`): header ya no cuenta, briefing glosa duplicado.** No reabrir.
**4. 🧭24 — CERRADA con matiz (doc drift menor, decisión Gwyn 12/09: MANTENER pre-puebla):** ch4 `DEFAULT_CH4_COMMANDS` trae `cat+cut+ssh/scp`, ch6 conserva 127. `new_session` cap. 4 pre-puebla `shell.hosts` con `faro`+`troncal-01/02` (O1 09/09) — `scp` sin `cat` ya no rechaza. Briefing documenta rechazo «léelo en /etc/hosts» como didáctico, pre-puebla lo hace redundante. No es bug (708/0), solo P3.
**5. 🧭25 — PERSISTE (observación): límite 2 pipes (3 cmds) + redirección no soportada.** `cut|grep TR-` 1 pipe OK, `cut|grep|sort` con `sort`→127 en ch4 no bloquea; ch4.e2 diseña en 1 pipe evitando límite.
**6. 🧭26 — PERSISTE (dirección menor): `c.cut` vive en cap. 4, Faro e1 no es primer `cut`.** e1 `grep|wc`, dato2/e2 exigen `cut` por necesidad. Correcto.
**7. 🧭27 — PERSISTE (fricción menor, decisión Gwyn 11/09: filtro positivo): `grep -v` vía pipe NO soportado.** `join|grep -v sujeto` → exit 2. FICHA dato4/dato6 y ch4.e2 usan filtro positivo (`grep TR-`/`grep 000483`/`grep PR-0091`) — Gwyn decidió NO implementar `-v`.
**8. 🧭28 — CERRADO (verificación positiva): FICHA ch4.e2 con alma pulida.** `story.ch4.e2.beat` con `03:14`+`512 bytes`+`TR-003|EN_COLA`.
**9. 🧭29 — CERRADO (tríada Auditor `corte→orden→join` con voz):** retícula determinista, prefijos disjuntos, sin tocar `curriculum.json`/`shell.py`.
**10. 🧭30 — CERRADO (glosa 127 + tabla troncal hermana):** glosa solo `join` fuera de ch6, tabla viva con columna 1 | resaltada, cap. 4 only, restart limpio.
**11. 🧭31 — NUEVO, CERRADO (verificación positiva 14/09): dato6 coma-trampa se nota SIN señal roja pero SE entiende al romperte.** Medido: `join -v | grep 000483` devuelve `000483|PR-0092|...|EN BLANCO, revisado` — la coma es visible al final del campo 5 si lees la fila, pero NO hay badge ni hint que la señale. `cut -d',' -f1` sobre el `join` la parte (`EN BLANCO` sin `, revisado`) — error silencioso que NO avisa (exit 0, output truncado). La variante `cut -d'|' -f3 | grep 000483` ilumina la huérfana real por contraste (000 vs 000483). La lección vive en el briefing que nombra la `,` como separador que rompe y en el `cut -d'|'` que la preserva — el jugador la tropezará al probar `cut -d','` y la corregirá al probar `cut -d'|'`. No es «susto injusto» (el `join` nos da el `|` gratis en su `-t'|'`), pero el feedback de la trampa es silencioso: no hay `stderr` que diga «estás cortando mal». Aceptable porque el Faro ya enseña `cut -d'|'` en E2/E3/dato4.
**12. 🧭32 — NUEVO, CERRADO (verificación positiva 14/09): eco del espejo suena como testigo que recuerda, no como lista.** Medido: ①+②+③ → `copiaste el volcado, cruzaste dos testigos y leíste el reloj` en UNA línea con `, ` y ` y ` (prosa barata, no CSV), determinista ①→②→③; sin firma → byte-idéntico (sin key `auditor_espejo`, `lines_resolved` 1 vs 4); con UNA firma → `copiaste el volcado.` sola. La enumeración no lista filas ni cuenta bytes: nombra gestos. El `auditor_join` coexistente no pisa la firma (misma voz `Continuidad del ensayo: estable.`). Pregunta de sabor «¿suenas como testigo o como lista?» → testigo.
**13. 🧭33 — NUEVO, CERRADO (verificación positiva 14/09): badge `EN_COLA · 512` mete presión de timeline genuina sin ruido visual.** Medido: `?chapter=4&seed=42` preview estático `TR-001/002/003` sin badge (falso positivo evitado), tras `scp` + `cut -d'|' -f1 | grep TR-` badge ámbar + fila ámbar `TR-003|EN_COLA` + header `id` tachado con tooltip, `?chapter=6` panel oculto, restart limpia ambos paneles. El badge no es popup ni barra: es el volcado que pesa (512 bytes, 03:14) visto en la columna correcta con el header tachado que explica por qué `grep TR-` excluye `id`. Como PRIMER aviso diegético de TR-003 (bifurcación kármica futura), la presión es literal — el dato que espera tiene peso y timestamp — pero sin contador ni urgencia artificial. Ruido visual mínimo: solo la fila 3 y el header, que ya existían.

## 👴 Progreso de veterano (20+ h → la run 30)

- **Dato6 como segunda lectura del mismo cruce:** el veterano que en la run 15 cruzó `join -v` y vio `PR-0091`/`PR-0092` vuelve en la run 30 y cruza de nuevo con `| grep 000483` para confirmar que la coma vive dentro del campo 5. El que probó `cut -d','` alguna vez en E2 ya sabe el precio de la coma; el que solo usó `cut -d'|'` sigue intacto. La variante `cut -d'|' -f3 | grep 000483` es el atajo del veterano que ya no necesita el `join` para ver la huérfana real — 000 (ENSAYO) vs 000483 (revisado) es el mismo altar con dos ofrendas.
- **El espejo como ritual de cierre:** tras 20h el veterano compone su repertorio sin abrir el post-mortem: sabe que `scp→cut|grep` + `join -v` + `ps aux|grep` son tres verbos del mismo repertorio (cortar→cruzar→leer el reloj). En la run 30 sin `join` el informe es byte-idéntico (1 línea); con las 3 firmas, una línea nombra su tríada en español con ` y `. El veterano que cazó `TR-003|EN_COLA` en ch4.e2 lo reencuentra en el espejo como `copiaste el volcado` — el mismo gesto contado por el Auditor.
- **Troncal como Faro en miniatura:** el veterano alterna `generate(42,4)`/`generate(1,4)` y deja `TR-003|EN_COLA` siempre la pista que pesa (512 bytes, 03:14, `faro`+`troncal-01` siempre, a veces `troncal-02` por seed) — el header `id` siempre fantasma que el veterano filtra sin pensar, ahora con badge que confirma que el `grep TR-` hizo lo correcto y con header tachado que explica por qué.
- **El techo 2 pipes y `>` como liturgia:** el veterano encadena `cut|grep TR-` (1 pipe) y `cut|sort|uniq -c` en dos líneas; el que intente `cut|grep|sort|uniq -c` recibe `multiple pipelines not supported` y aprende el límite. La coma-trampa es otro techo invisible: `cut -d','` no falla, solo miente en silencio — el veterano lo recuerda como la vez que el separador importó.
- **Hub/eco vivo:** `c.cut`/`c.scp`/`c.join`/`c.ps-forense` dominados con eco (`auditor_corte`/`auditor_join`/`auditor_orden` + `auditor_espejo` repertorio) — el eco ya no es pendiente (🧭9 cerrada). Inventario agregado multi-run sigue pendiente (🧭17) pero el espejo ya da cuerpo a ch4.e2+dato4/5+6.

## 🔬 Zona 🔬 ejecutada hoy (relevo Gwyn → Oscar: dato6 coma-trampa + eco espejo + badge TR-003)

- **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **708 passed / 0 xfailed** (gate `load_curriculum()` 24/29, `DEFAULT_CH4_COMMANDS` 13 cmds con `cut/scp` sin `join`, `DEFAULT_CH6_COMMANDS` 16 cmds con `join`+`cut`, bundle 47 ficheros 402.3 KiB). ✓
- **Prioridad 1 — dato6 «La segunda purga» (cap. 6, NUEVA, mundo real):** `generate(42,6, contract_id='story.ch6.dato6')` + `new_session` `cd /srv/camara-faro` + `join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv | grep 000483` → exit 0 `000483|PR-0092|...|EN BLANCO, revisado` sin `PR-0091`; `cut -d'|' -f3 purgas.csv | grep 000483` → `000483` (bonus: huérfana que SÍ está en la segunda tabla); `cut -d',' -f1` sobre el `join` → `EN BLANCO` truncada (lección del separador); determinismo 42×2 idéntico; `c.join` 6 con `c.cut`+`c.sort` DAG válido. ✓
- **Prioridad 2 — eco del espejo + huella troncal (cap. 4→6 + web):** `scp→cut|grep TR-`+`join -v`+`ps aux|grep` → `auditor_espejo_text` `repertorio — copiaste el volcado, cruzaste dos testigos y leíste el reloj` determinista ①→②→③, byte-idéntico sin firma, una firma sola → línea con esa firma; `web/app.js` `TRONCAL_STATIC` byte-idéntica a `chapter4.py`, `?chapter=4&seed=42` preview estático pre-scp sin badge, tras `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` badge ámbar `EN_COLA · 512` + header `id` `line-through`, `?chapter=6` Faro intacto, restart limpia ambos paneles. ✓
- **Smoke Havel (07:00) — lo que debe seguir funcionando sí o sí:** suite 708/0, gate 24/29, bundle 47 fresco 402.3 KiB, frontera allowlist 127, pipes 2 OK / 4 KO honesto, `GameState` roundtrip multi-host, `generate` deterministas 42×2 intactos. ✓

## 🧭 Notas de dirección (resumen — texto completo en `backlog/notas-manana.md`)

Saldo: **🧭20/21/22/23 cerradas**, **🧭24 cerrada con matiz pre-puebla P3**, **🧭25/26/27 PERSISTEN** (límite 2 pipes + `cut` en ch4 + `grep -v` filtro positivo), **🧭28 cerrada** (FICHA ch4.e2), **🧭29/30 CERRADAS** (tríada + glosa 127 + tabla troncal), **🧭31/32/33 NUEVOS CERRADOS** (dato6 coma-trampa visible al romperse + espejo como testigo + badge EN_COLA como primer aviso diegético con presión real). Ninguna rompe el camino. Preguntas de sabor — «¿la coma-trampa se nota sin señal roja? ¿las 3 firmas suenan como testigo o como lista? ¿el badge EN_COLA mete presión o es ruido?» — se responden: **se nota al romperte (el `cut -d','` parte en silencio, el briefing y el `cut -d'|'` la preservan), el espejo suena como testigo que recuerda (una línea con `, ` y ` y `), y el badge mete presión genuina sin ruido (preview limpio pre-scp, ámbar solo tras `cut|grep TR-`, con timestamp 03:14 y 512 bytes).**

CICLO: verde — la zona 🔬 se ejecutó completa (dato6 golden/variante/coma + espejo 3 firmas/byte-idéntico/1 firma + troncal web badge/tachado/restart) y el viaje del novato sigue apto; el Faro ya miente con la verdad.

---

*Mantenido por **Oscar de Astora** · Firmado con su nombre en el historial git.*
## 🌐 Deploy web (Seath, T1, 03/09 — noche)

**URL pública jugable: https://cyberroot-psi.vercel.app** — REPL del cap. 0
(seed 42) con el core real en el navegador (Pyodide + `bundle/core.json`);
verificado con juego real (`ls`/`cat`/`cp` al USB + golden CANDELAS).
Pasos de deploy y mantenimiento: `web/README.md` §Deploy.
