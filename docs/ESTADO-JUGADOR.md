# ESTADO-JUGADOR — qué se puede jugar hoy (documento vivo de Oscar)

> 📌 **Documento vivo, mantenido a diario por Oscar de Astora (05:00).** Es el
> puente entre el `docs/DESIGN.md` (lo que el juego *será*) y el código en `src/`
> (lo que el juego *es hoy*). Cualquier agente o Juanma puede leerlo para saber
> en qué punto está la experiencia jugable de verdad.

---

## 🎮 Estado global jugable de HOY (21/09 — MODO B: Subestación 4/4 ABRIBLE + física per-encargo + lente custodia web)

**¿Hay algo que jugar de principio a fin?** Sí — la cadena **cap. 4 `story.ch4.e3` «Lo que no avanza»** grey `c.scp` (`scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → `TR-003|EN_COLA` 512 bytes `03:14` + `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` → `TR-001/TR-002/TR-003` + bifurcación `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` → `volcado: rescatado` vs `rm /tmp/volcado.csv`/`tick≥30` → `caducado`) + **cap. 6 `story.ch6.dato7` «El fantasma que pesa»** grey `c.join` (`volcado-rescate.csv` SOLO si rescate → `join -t'|' -1 1 -2 1 -v 1 volcado-rescate.csv purgas.csv | grep TR-003` → 1 línea `TR-003|faro|troncal-01|512|EN_COLA`; si caducado → `join: volcado-rescate.csv: No such file` exit 1) + **cap. 5 «Subestación» COMPLETO 4/4** por **`abrir_encargo(c, 'story.ch5.e{1,2,3,4}', requires, volcado_rescatado=True/False)`** con `SUPPORTED_CHAPTERS {0,2,4,5}` y `_commands_for(5)=(cat,scp)` → `build_chapter5_fs(volcado_rescatado)` → `/tmp/volcado-custodia.csv` `TR-003|faro|troncal-01|512|EN_COLA` EXISTE solo si `volcado: rescatado`; si caducaste → `cat` → exit 1 `No such file` en LOS 4 + proceso `intruso --vigilar-censo` USER `censo` START `03:14` patrulla igual en ambos mundos. **PUERTA ABIERTA 20/09 PR #68:** guard `encargo sin flujo materializado` borrado, los 4 abren por la misma rutina (prereqs → `missing [...]` honesto / OK → `EncargoSession` seed `quest:run_seed`). **Física per-encargo** `DEFAULT_CH5E1` (`ls,ps,chmod,kill`+cat,scp) / `E3` (`ps,env,kill`+cat,scp) / `E4` (`chmod,chown,tail,ls`+cat,scp) — **allowlist en `shell.py` verificada `<= set(...)` y frontera 127 honesta**, con `chmod 600`/`+x`/`chown gris:apagados` GNU-honesto (ruido 1, mtime) y **golden e3 `kill -HUP <pid>`→ HUP_<pid>=1 / `kill -9 <pid>`→ intruso desaparece del `ps`** — jugable vía `Shell` directo (pid dinámico `420+fork("ps-subestacion").below(15)` → 426 seed 42, 427 seed 99 vs 522 handmade). **⚠️ CABLEADO PENDIENTE:** `session.py _commands_for(5, quest_id)` aún devuelve base `(cat,scp)` para los 4 — `ps`/`ls`/`chmod`/`env`/`tail` vía `abrir_encargo` →127 aun con prereqs (la física existe pero no viaja por la puerta; cap. 4 sí ramifica `CH4E3`). No bloquea abrible/testigo/lente; bloquea la promesa de verbos de E3 en la prosa. **Lente custodia web** `CUSTODIA_STATIC` + `_isCustodiaPresent` (file TR-003 como verdad) + `parseParams ?chapter=5` + `restartSameSeed` limpia 3 lentes (`TRONCAL_STATIC` 3 intacta).

**En main (774 passed / 0 failed, gate 24 conceptos / 31 quests, bundle 49 ficheros 453.5 KiB regenerado canónicamente, guardián verde):**
- **LA PUERTA COMPLETA (O1 PR #68, NUEVO 20/09 mergeado):** `src/core/engine/session.py` guard borrado (7 líneas) + docstring «los 4 encargos abren por la misma puerta, volcado_rescatado propaga», `SUPPORTED_CHAPTERS == {0,2,4,5}` y `_commands_for(5)==(cat,scp)` intactos, `volcado_rescatado` → `generate(seed,5, volcado_rescatado)` en los 4, `test_session_ch5.py` 7→12 (+5), suite aislada 773+1 stale → 774 tras `build_bundle.py`, `SUPPORTED` exacto y determinismo ×2 seeds byte-idéntico verificado por Gwyn en vivo (los 4 `abrible True` con requires, rechazo `missing` sin prereqs).
- **Física per-encargo (S1 PR #66, 19/09 mergeado):** `src/core/sandbox/shell.py` `DEFAULT_CH5E1` 6 / `E3` 5 / `E4` 6 forma `<= set(...)` verificada, frontera 127 honesta por encargo (`ls` fuera E3→127, `chmod` fuera E3→127, `kill` fuera E4→127), `chmod`/`chown` GNU-honesto (ruido 1, 600/octal/+x, `gris:apagados`, mtime), golden E3 `kill -HUP 426`→`HUP_426=1` / `kill -9 426`→ps sin intruso (FS real seed 42) y `kill -HUP 522`/`kill -9 522` handmade; 9 tests `test_ch5_allowlist` 9/9, `PYTHONPATH=src pytest` aislado 774 passed tras regen.
- **Lente custodia web (T1 PR #67, 19/09 mergeado):** solo `web/app.js` + `web/index.html` (+docs), `parseParams` `[0,2,3,4,6]`→`[0,2,3,4,5,6]` (5 nuevo, 3 intacto no-regresión superset), lente custodia `CUSTODIA_STATIC` byte-idéntica a `CUSTODIA_CONTENT`, `_isCustodiaPresent()` file `TR-003` como verdad (falso positivo caducado cazado), `hideCustodiaTabla` en `restartSameSeed` limpia 3 lentes (+ `hideFaroTabla`/`hideTroncalTabla`), `TRONCAL_STATIC` 3 intacta, `node --check web/app.js` OK, consola limpia `?chapter=4/5/6` (custodia oculta sin error cuando caducado), suite delta 0.
- **Smoke:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **774 passed** (769+5, delta real +5, PR declaró +4 off-by-1 honesto); gate 24/31; bundle 49 ficheros 453.5 KiB fresco (guardián verde); `grep -v` sigue honesto (exit 2, 🧭27 P3 recámara); tooltips N/30 + toggle EN_COLA + lente Faro/Troncal byte-idénticos.

**Para «jugable de principio a fin» sigue faltando:** **inventario agregado multi-run** (🧭17), **salas narrativas Faro E4+** y **cableado `session._commands_for` per-encargo** (la puerta abre pero los verbos extra de E1/E3/E4 aún no viajan — ver hallazgo 🧭44). Nada rompe el camino principal abrible/testigo/lente.

**CICLO (línea de Oscar):** ámbar — la zona 🔬 21/09 se ejecutó COMPLETA desde save limpio (4/4 abrible + testigo condicional en los 4 + determinismo + lente custodia); el camino del novato es APTO y la puerta es REAL (Gwyn validó en vivo post-merge). El ámbar es solo el cableado per-encargo (ps vía session →127) que deja a E3 sin sus verbos prometidos aunque la física exista vía Shell directo.

## 🏃 Run de referencia (save limpio) — 21/09

*Nueva partida sobre `abrir_encargo` real + `Shell` directo para física + `generate` determinista + web lente; MODO B headless sobre zona 🔬 21/09 Gwyn → Oscar (primero).*
**Veredicto: APTO con matiz — la puerta abre 4/4 y el testigo es determinista; la lente acompaña; el único lunar es `_commands_for` sin ramificar (ps vía session →127).**

1. **Prioridad 1 — LA PUERTA ABRE: 4/4 abrible por `abrir_encargo` con requires del curriculum:**
   - `listar_encargos(cur,5, knowledge={c.cat,c.scp,c.ls-la,c.chmod,c.chown,c.grep,c.ps,c.env})` → 4 ids `story.ch5.e1/e2/e3/e4` todos `abrible True` (knowledge completa). `SUPPORTED_CHAPTERS == {0,2,4,5}` y `_commands_for(5)==("cat","scp")`. ✔
   - **Requires verificados:** e1 `{c.ls-la,c.cat,c.chmod}` → `abrir_encargo(c,'story.ch5.e1',[],run_seed=1)` → `abrible False` + `missing` incluye `c.ls-la` (rechazo accionable, no crash); con `{c.ls-la,c.cat,c.chmod}` run_seed 42 volcado True → `abrible True` + `session` con shell + incursion. e3 `{c.ps,c.env}` → sin `c.env` → `missing [c.env]`; con ambos → `abrible True`. e4 `{c.chmod,c.chown,c.cat,c.grep}` → sin → `missing`; con → `abrible True`. e2 `{c.cat,c.scp}` intacta. ✔
   - **Testigo condicional en LOS 4:** `abrir_encargo(c,'story.ch5.e{1,3,4,2}', requires, volcado_rescatado=True)` → `cat /tmp/volcado-custodia.csv` → exit 0 con `id|origen|destino|bytes|estado` + `TR-003|faro|troncal-01|512|EN_COLA` (34 bytes payload + header 66 bytes). `volcado_rescatado=False` → mismo `abrible True` → `cat` → exit 1 `cat: /tmp/volcado-custodia.csv: No such file or directory` (misma semántica en e1/e2/e3/e4; no solo e2). `postmortem.auditor.custodia` «custodia leída en casa: el testigo viajó…» solo si `cat` exit 0 (cuarta huella intacta). ✔
   - **Pid del intruso estable por seed + determinismo ×2 seeds:** `generate(42,5, volcado_rescatado=True).room.fs` byte-idéntico ×2; `generate(99,5, volcado_rescatado=False)` idem; `True vs False` en misma seed difiere (`/tmp/volcado-custodia.csv` presente vs ausente). `Shell(DEFAULT_CH5E3_COMMANDS)` sobre `generate(42,5)` → `ps aux` → `censo 426 intruso --vigilar-censo START 03:14` (seed 99 → 427 con mismo START); `420+below(15)` estable por seed, fallback handmade 522 en `test_ch5_allowlist` (forma `<= set(...)`). `kill -HUP 426`→`HUP_426=1` + `ps` sigue con `--reloaded`; `kill -9 426`→ `ps` sin `426`/`intruso` (jugable, no decorado). `ps aux` vía `abrir_encargo` e3 → 127 observado (ver §matiz). ✔
   - **e2 intacta física y allowlist:** `abrir_encargo(c,'story.ch5.e2', ['c.cat','c.scp'], volcado True)` → `session.shell.available_commands == {'cat','scp'}` sin sangrar de E1/E3/E4; `cat` testigo 0 vs 1 idéntico a pre-puerta. ✔

2. **Prioridad 2 — SINERGIA puerta+lente: encargos abiertos llena la lente custodia web:**
   - Flujo completo: `abrir_encargo` e1/e3/e4 con prereqs + `volcado_rescatado=True` → `cat` exit 0 → `web/app.js` `parseParams ?chapter=5` consola limpia, panel «Subestación — custodia» rinde `TR-003` solo si `_isCustodiaPresent()` (file `TR-003` como verdad; no consola roja). Con `volcado_rescatado=False` → `hideCustodiaTabla()` sin error (ausencia elocuente, misma que Faro `join: No such file`). ✔
   - `restartSameSeed` limpia 3 lentes (`hideFaroTabla(); hideTroncalTabla(); hideCustodiaTabla()`) + dispatch triple + boot `previewCustodiaTabla`; `CUSTODIA_STATIC` byte-idéntica a `CUSTODIA_CONTENT` (`id|origen|destino|bytes|estado` + `TR-003|faro|troncal-01|512|EN_COLA`); `TRONCAL_STATIC` 3 ocurrencias intacta; `node --check web/app.js` OK; `parseParams` acepta `[0,2,3,4,5,6]` (5 nuevo, 3 no-regresión, fallback 0). ✔
   - Con e3: `ps aux` vía `Shell` directo (no vía session) + `kill -HUP`/`kill -9` + web custodia coexisten: el intruso muere o se recarga sin huérfanos ni recursos muertos; `HUP_<pid>=1` en `env` sin afectar al panel custodia (circuitos disjuntos). Vía session el `ps` es 127 (matiz §1). ✔

3. **Matiz — `_commands_for` sin ramificar per-encargo (no bloquea abrible/testigo/lente, sí promesa de verbos):**
   - `abrir_encargo(c,'story.ch5.e3',['c.ps','c.env'],...)` → `session.shell.available_commands == {'cat','scp'}` → `ps aux` → exit 127 `sh: command not found: ps`; `ls`, `chmod`, `env`, `tail`, `chown` →127 igual en e1/e4 (verificado e1/e3/e4). `Shell(DEFAULT_CH5E3_COMMANDS)` directo sobre el mismo FS → exit 0 con `censo 426 ... 03:14` y kills jugables. Causa: `session.py L74-75` devuelve base sin `quest_id` (cap. 4 sí ramifica `CH4E3` con `quest_id=="story.ch4.e3"`). Manus 03:00 ya dejó propuesta en `backlog/mejoras/pendiente/propuestas.md` (21/09) con forma `cap. 4`: ramificar `_commands_for(5, quest_id)` a `DEFAULT_CH5E{1,3,4}`; impacto P2 sin prosa/curriculum. No abro duplicado: dejo hallazgo 🧭44 y referencio su propuesta + [BUG] P2 en `tareas/pendiente/abierto.md`. ✔ (observación documentada, no crash)

4. **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **774 passed / 0 failed** (gate `load_curriculum()` 24/31, bundle 49 ficheros 453.5 KiB, `test_ch5_allowlist` 9/9, `test_bundle_fresco` verde, `DEFAULT_CH4_COMMANDS` 13 / `CH4E3` 14 disjunta, `DEFAULT_CH6_COMMANDS` 16 con `join`+`cut`, pipes 1 OK / `grep -v`→2 honesto). `SUPPORTED_CHAPTERS` {0,2,4,5}. ✔

## 🟡 Hallazgos de la run (dónde aprieta el viaje — dirección, no rotura)

**1. 🧭20 — CERRADA (re-verificada 12/09): `.nota-corte` sigue hallazgo `ls -a`.** No reabrir.
**2. 🧭21 — CERRADA (re-verificada): `LEEME.txt` tienta relativo vs absoluta.** No reabrir.
**3. 🧭22/23 — CERRADAS (e2 del Faro con `tail -n +2`): header ya no cuenta, briefing glosa duplicado.** No reabrir.
**4. 🧭24 — CERRADA con matiz (doc drift menor, decisión Gwyn 12/09: MANTENER pre-puebla):** ch4 `DEFAULT_CH4_COMMANDS` trae `cat+cut+ssh/scp`, ch6 conserva 127. `abrir_encargo`/`new_session` cap. 4 pre-puebla `shell.hosts` con `faro`+`troncal-01/02` — `scp` sin `cat` ya no rechaza. Briefing documenta rechazo «léelo en /etc/hosts» como didáctico, pre-puebla lo hace redundante. No es bug (774/0), solo P3.
**5. 🧭25 — PERSISTE (observación): límite 2 pipes (3 cmds) + redirección no soportada.** `cut|grep TR-` 1 pipe OK, `cut|grep|sort` con `sort`→127 en ch4 no bloquea; ch4.e2/e3 diseñan en 1 pipe evitando límite.
**6. 🧭26 — PERSISTE (dirección menor): `c.cut` vive en cap. 4, Faro e1 no es primer `cut`.** e1 `grep|wc`, dato2/e2 exigen `cut` por necesidad. Correcto.
**7. 🧭27 — PERSISTE (fricción menor, decisión Gwyn 11/09: filtro positivo): `grep -v` vía pipe NO soportado.** `join|grep -v sujeto` → exit 2. FICHA dato4/dato6 y ch4.e2/e3 usan filtro positivo — Gwyn decidió NO implementar `-v`.
**8. 🧭28 — CERRADO (verificación positiva): FICHA ch4.e2 con alma pulida.** `story.ch4.e2.beat` con `03:14`+`512 bytes`+`TR-003|EN_COLA`.
**9. 🧭29 — CERRADO (tríada Auditor `corte→orden→join` con voz):** retícula determinista, prefijos disjuntos.
**10. 🧭30 — CERRADO (glosa 127 + tabla troncal hermana):** glosa solo `join` fuera de ch6, tabla viva con columna 1 | resaltada.
**11. 🧭31 — CERRADO (dato6 coma-trampa):** `join -v | grep 000483` devuelve coma en campo 5 — `cut -d','` parte.
**12. 🧭32 — CERRADO (eco del espejo testigo):** ①+②+③ → `copiaste el volcado, cruzaste dos testigos y leíste el reloj` byte-idéntico sin firma.
**13. 🧭33 — CERRADO (badge `EN_COLA · 512` con presión diegética):** badge ámbar con 512/03:14, preview limpio, header `id` tachado.
**14. 🧭34 — CERRADO (toggle lente):** badge clicable → solo `TR-003` (como `grep EN_COLA` con ojos), restart limpia.
**15. 🧭35 — CERRADO (dato6 doble ruta honesta):** `join` y `cut` pesan lo mismo, sin callejón.
**16. 🧭36 — CERRADA (allowlist e3 simétrica en session):** `abrir_encargo(c,'story.ch4.e3',knowledge)` → `scp`→`rescatado`, `rm`→`caducado`, `tick 31`→`caducado` prioridad rescate.
**17. 🧭37 — CERRADO (testigo condicional como ausencia elocuente 18/09):** `build_chapter5_fs(..., False)` → `cat` `No such file` como drama, no bug; `ps aux` intruso 03:14 patrulla igual. Trilogía Faro `join -v 1 | grep TR-003` → cap. 5 `cat`+`ps` lo hace trilogía.
**18. 🧭38 — CERRADO (lente web como acompañamiento, no spoiler 18/09):** tooltip `N/30` sin `setInterval` + lente `TR-003 rescatado` solo si rescate — la web no anticipa, no miente.
**19. 🧭39 — CERRADO (LA PUERTA: cap. 5 por `abrir_encargo`):** `SUPPORTED_CHAPTERS {0,2,4,5}` + `_commands_for(5)=(cat,scp)` + `abrir_encargo(c,'story.ch5.e2',['c.cat','c.scp'],volcado_rescatado=True)` → `abrible True`, `cat /tmp/volcado-custodia.csv` → exit 0 `TR-003|EN_COLA`; `volcado_rescatado=False` → `abrible True`, `cat` → exit 1 `No such file` (trayectoria válida documentada); `story.ch5.e1/.e3/.e4` → `abrible False` pre-20/09 (hoy 4/4 abrible, ver §run 21/09).
**20. 🧭40 — CERRADO (cuarta huella `postmortem.auditor.custodia`):** `cat /tmp/volcado-custodia.csv` exit 0 → `build_postmortem` → `auditor_custodia` «Expediente 000: custodia leída en casa: el testigo viajó. Continuidad del ensayo: estable.»; si caducaste (sin fichero / exit 1) → huella NO aparece (byte-idéntico sin custodia — detector por exit 0, no por presencia). Prefijo `postmortem.auditor.custodia` disjunto; coexiste con rescate+espejo (3 firmas vivas).
**21. 🧭41 — CERRADO (física per-encargo activa y honesta):** `DEFAULT_CH5E1` 6 / `E3` 5 / `E4` 6 forma `<= set(...)` verificada, `ls`/`ps`/`chmod`/`kill`/`env`/`chown`/`tail` solo en su encargo (fuera→127 honesto, `cat`+`scp` base intacta en los 3), `chmod 600`/`+x` ruido 1 mtime+1 GNU-honesto, `chown gris:apagados` cambia owner/group, `tail` E4 lee, `DEFAULT_CH4*` intacta sin sangrar. No-regresión 774/0.
**22. 🧭42 — CERRADO (golden e3 `kill` jugable, mecanismo por señal):** `Shell(E3).execute("kill -HUP <pid>")`→env `HUP_<pid>=1` (reinicia, `--reloaded` en ps), `kill -9 <pid>`→intruso desaparece del `ps aux` (segunda Shell para no consumir). Handmade (pid 522 fijo) y FS real (pid dinámico `420+below(15)` → 426 en seed 42, 427 en 99, `START 03:14` estable en ambos) coinciden en semántica; la diferencia de pid es determinismo por seed, no bug — la golden del test usa handmade fallback 522, la del mundo usa fork. El intruso es objetivo jugable, no decorado.
**23. 🧭43 — CERRADO (lente custodia web acompaña, no anticipa):** `parseParams` abraza `[0,2,3,4,5,6]` (5 nuevo, 0 fallback no-regresión), `?chapter=5` consola limpia, panel custodia visible SOLO con testigo rescatado `_isCustodiaPresent` (file `TR-003` como verdad; caducado→`hideCustodiaTabla()` sin consola roja, ausencia elocuente), `restartSameSeed` limpia 3 lentes (`hideFaroTabla`+`hideTroncalTabla`+`hideCustodiaTabla`), `TRONCAL_STATIC` 3 ocurrencias intacta, `CUSTODIA_STATIC` byte-idéntica a `CUSTODIA_CONTENT`, `node --check` OK.
**24. 🧭44 — NUEVO 21/09 — ABIERTO P2 (cableado `session._commands_for` cap. 5):** la puerta abre 4/4 y el testigo es determinista en los 4, pero `session.py` L74-75 `if chapter==5: return _DEFAULT_CH5_COMMANDS` ignora `quest_id` — `Shell(DEFAULT_CH5E3_COMMANDS)` directo → `ps aux` exit 0 con `censo 426 ... 03:14` + kills jugables, pero `abrir_encargo(c,'story.ch5.e3',['c.ps','c.env'])` → `session.shell.available_commands == {'cat','scp'}` → `ps aux`/`env`/`ls`/`chmod`/`tail`→127 aun con prereqs. Manus 03:00 ya dejó propuesta en `backlog/mejoras/pendiente/propuestas.md` (21/09) con forma `cap. 4`: ramificar a `DEFAULT_CH5E1/E3/E4` según `quest_id`. La prosa de `05-subestacion.md` promete `ps`/`kill` en E3 y `ls`/`chmod`/`chown` en E1/E4 — la promesa no viaja por la puerta. No rompe abrible/testigo/lente (774/0), pero el veterano que abre E3 no puede leer al intruso por la puerta normal (hoy 127; ayer también). Verificado e1/e3/e4 vía session →127; vía Shell directo →0. [BUG][P2] abierto en `tareas/pendiente/abierto.md` referenciando la propuesta de Manus.

## 👴 Progreso de veterano (20+ h → la run 30)

- **La Subestación como mixto de puertas 4/4:** el veterano ya domina `c.cat`/`c.scp` (cap. 4) y `c.join`/`c.cut` (cap. 6). En la run 29 hizo `scp` rescate y en la 30 `rm` disolución: ambas le dieron `abrible True` en los 4 pero `cat` diametralmente opuesto (0 con `TR-003|EN_COLA` vs 1 `No such file`) y post-mortem con/sin `auditor_custodia` («el testigo viajó» vs silencio). Hoy puede abrir E1 con `c.ls-la/c.cat/c.chmod`, E3 con `c.ps/c.env`, E4 con `c.chmod/c.chown/c.cat/c.grep` — los 3 con testigo condicional; el `ps aux` del intruso `censo 426 --vigilar-censo START 03:14` (seed 42, 427 en 99) es su brújula. Sabe que `kill -HUP 426`→`HUP_426=1` + `--reloaded` (reconfigurar, lente azul) vs `kill -9 426`→desaparece (ejecutar, lente roja) — dos karmas con el mismo verbo, ya jugables vía `Shell` directo, aún no vía `abrir_encargo` (🧭44 P2). La prosa `05-subestacion.md` ya promete esas bifurcaciones; la física las cumple, la puerta aún no las entrega.
- **La trilogía del testigo sigue cerrando 4 geografías:** Faro `join -v 1 … | grep TR-003` → 1 línea vs `No such file` + Subestación `cat /tmp/volcado-custodia.csv` → `TR-003|EN_COLA` vs `No such file` + post-mortem `auditor_custodia` vs silencio + web `?chapter=5` custodia visible vs oculta. Cuatro geografías, una decisión. El veterano que vuelve con `?chapter=5&seed=99` ve el mismo `START 03:14` con pid 427 — la hora no miente aunque el pid baile. `volcado_del_save(postmortem)` le deja reusar `rescatado/caducado` sin brujería.
- **El Faro como espejo reversible:** `join -v | grep 000483` vs `cut -f3 | grep 000483` — mismo dato, dos altitudes. `grep 000` (2) vs `grep 000483` (1) prueba de precisión; `cut -d','` coma-trampa intacta. Dato7 añade `volcado-rescate.csv` como tercera tabla del Faro, solo visible si trajiste el testigo (con `-v 1` el join nombra el huérfano).
- **Loop y Hub vivo:** `c.cut`/`c.scp`/`c.join`/`c.ps-forense` dominados con eco (`auditor_corte`/`auditor_join`/`auditor_orden` + `auditor_custodia` + `auditor_espejo` + `auditor_volcado` + `gris_eco`) — el eco ya no es pendiente (🧭9 cerrada). Inventario agregado multi-run sigue pendiente (🧭17) pero el espejo+Gris+volcado+cadena dan cuerpo a ch4.e2/e3+dato4/5/6/7+ch5.e2/e3/e4. La lente custodia web le dice «TR-003 custodiado — volcado-custodia.csv» solo si el save trae `volcado: rescatado`; si no, el panel calla — el veterano no ve spoiler, ve ausencia. El soporte `?chapter=5&seed=` ya es semilla compartible (semilla del veterano como enlace).

## 🔬 Zona 🔬 ejecutada hoy (relevo Gwyn → Oscar: LA PUERTA ABRE 4/4 + sinergia puerta+lente COMPLETA)

- **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **774 passed / 0 failed** (gate `load_curriculum()` 24/31, `DEFAULT_CH4_COMMANDS` 13 + `DEFAULT_CH4E3_COMMANDS` 14 disjunta + `DEFAULT_CH5_COMMANDS` 2 (`cat,scp`) + `DEFAULT_CH5E1` 6 + `E3` 5 + `E4` 6, `DEFAULT_CH6_COMMANDS` 16 con `join`+`cut`, bundle 49 ficheros 453.5 KiB, `test_ch5_allowlist` 9/9, `test_bundle_fresco` verde). ✓
- **Prioridad 1 — LA PUERTA ABRE (PR #68, 4/4 abrible + testigo condicional en los 4 + determinismo):** `listar_encargos(cur,5, full knowledge)` → 4 ids, todos `abrible True`; `abrir_encargo` e1/e3/e4 con requires correctos del curriculum → `abrible True` + `session`, sin knowledge → `missing` accionable (`c.ls-la`/`c.env`/`c.chown`…); `volcado_rescatado=True`→`cat /tmp/volcado-custodia.csv` exit 0 `TR-003|faro|troncal-01|512|EN_COLA` en LOS 4, `False`→exit 1 `No such file` en LOS 4 (e2 intacta, misma semántica); pid intruso e3 `censo 426` seed 42 / `427` seed 99 (`420+fork("ps-subestacion").below(15)` estable, `START 03:14` invariante); `generate(42,5)`/`(99,5)` byte-idéntico ×2 seeds en ambas variantes (`True≠False` difiere). Pregunta de sabor: ¿el capítulo se siente ABIERTO o DESGUARDado? → ABIERTO: la misma puerta con `missing` honesto da pared con sentido, no desguarde. ✓
- **Prioridad 2 — SINERGIA puerta+lente (trilogía Faro `join` → Subestación `cat` → web custodia):** `abrir_encargo` + `cat volcado-custodia.csv` + post-mortem `auditor_custodia` + `?chapter=5` web forman circuito completo; panel Subestación refleja `TR-003` SOLO si testigo viajó (`_isCustodiaPresent` file `TR-003` como verdad; sin testigo `hideCustodiaTabla` sin consola roja — ausencia elocuente); con e3: `Shell` directo `kill -HUP`/`-9` + web coexisten sin huérfanos; `restartSameSeed` limpia 3 lentes + `previewCustodiaTabla` byte-idéntica. Pregunta de sabor: ¿caso cerrado o primer expediente? → primer expediente del jugador: el `cat` + lente + post-mortem lo hacen caso cerrado sin spoilear el siguiente capítulo. ✓
- **Matiz documentado — `_commands_for` sin ramificar (no bloquea abrible/testigo/lente):** `abrir_encargo` e3 → `ps aux` vía session →127 (observado y reproducido en e1/e4); `Shell(DEFAULT_CH5E3_COMMANDS)` directo →0 con kills jugables. Propuesta Manus 21/09 en `mejoras/pendiente/propuestas.md` ya con forma `cap. 4` (coste 1 tarea engine, owner Ornstein). [BUG][P2] 🧭44 en `tareas/pendiente/abierto.md` referencia. ✓ (amar)

## 🧭 Notas de dirección (resumen — texto completo en `backlog/notas-manana.md`)

Saldo: **🧭20/21/22/23 cerradas**, **🧭24 cerrada con matiz pre-puebla P3**, **🧭25/26/27 PERSISTEN** (límite 2 pipes + `cut` en ch4 + `grep -v` filtro positivo), **🧭28 cerrada** (FICHA alma), **🧭29/30 CERRADAS** (tríada + glosa + tabla), **🧭31/32/33 CERRADOS** (coma-trampa + espejo + badge), **🧭34/35 CERRADOS** (toggle + doble ruta), **🧭36 CERRADA** (allowlist e3 simétrica ch4), **🧭37 CERRADO** (testigo condicional — ausencia elocuente), **🧭38 CERRADO** (lente web acompaña, no anticipa), **🧭39 CERRADO** (LA PUERTA e2), **🧭40 CERRADO** (cuarta huella custodia), **🧭41 CERRADO** (física per-encargo forma `<=` y frontera 127), **🧭42 CERRADO** (golden e3 `kill` jugable, HUP vs -9), **🧭43 CERRADO** (lente custodia web acompaña), **🧭44 NUEVO P2** (cableado session per-encargo — la puerta abre pero no entrega los verbos). Sin bloqueo del camino principal; el ámbar es solo la promesa de E3 sin `ps` por la puerta normal.

CICLO: ámbar — la zona 🔬 21/09 se ejecutó completa (puerta 4/4 + testigo condicional en los 4 + determinismo + lente web) y el viaje del novato sigue APTO; la pared 127 protege la progresión pero el lunar `_commands_for` sin ramificar deja a E3 sin `ps` por `abrir_encargo` (física existe vía Shell directo, propuesta Manus 21/09 lista).

---

*Mantenido por **Oscar de Astora** · Firmado con su nombre en el historial git.*
## 🌐 Deploy web (Seath, T1, 03/09 — noche)

**URL pública jugable: https://cyberroot-psi.vercel.app** — REPL del cap. 0
(seed 42) con el core real en el navegador (Pyodide + `bundle/core.json`);
verificado con juego real (`ls`/`cat`/`cp` al USB + golden CANDELAS).
Pasos de deploy y mantenimiento: `web/README.md` §Deploy.
