# ESTADO-JUGADOR — qué se puede jugar hoy (documento vivo de Oscar)

> 📌 **Documento vivo, mantenido a diario por Oscar de Astora (05:00).** Es el
> puente entre el `docs/DESIGN.md` (lo que el juego *será*) y el código en `src/`
> (lo que el juego *es hoy*). Cualquier agente o Juanma puede leerlo para saber
> en qué punto está la experiencia jugable de verdad.

---

## 🎮 Estado global jugable de HOY (19/09 — MODO B: LA PUERTA cap. 5 por `abrir_encargo` + cuarta huella custodia, cadena troncal→Faro→Subestación JUGABLE)

**¿Hay algo que jugar de principio a fin?** Sí — la cadena ya es **mundo jugable por la puerta normal**, no solo por generator+Shell directo: **cap. 4 `story.ch4.e3` «Lo que no avanza»** grey `c.scp` (`scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → `TR-003|EN_COLA` 512 bytes `03:14` + `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` → `TR-001/TR-002/TR-003` + bifurcación `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` → `volcado: rescatado` vs `rm /tmp/volcado.csv`/`tick≥30` → `caducado`) + **cap. 6 `story.ch6.dato7` «El fantasma que pesa»** grey `c.join` (`volcado-rescate.csv` SOLO si rescate → `join -t'|' -1 1 -2 1 -v 1 volcado-rescate.csv purgas.csv | grep TR-003` → 1 línea `TR-003|faro|troncal-01|512|EN_COLA`; si caducado → `join: volcado-rescate.csv: No such file or directory` exit 1) + **cap. 5 «Subestación» `story.ch5.e2` «El testigo que no llegó»** grey `c.cat+c.scp` **AHORA por `abrir_encargo(c,'story.ch5.e2',['c.cat','c.scp'], volcado_rescatado=True/False)`** con `SUPPORTED_CHAPTERS {0,2,4,5}` y `_commands_for(5)=(cat,scp)` → `build_chapter5_fs(volcado_rescatado)` → `/tmp/volcado-custodia.csv` `TR-003|faro|troncal-01|512|EN_COLA` EXISTE solo si rescataste; si caducaste → `cat` → exit 1 `No such file` + proceso `intruso --vigilar-censo` USER `censo` START `03:14` patrulla igual en ambos mundos + **cuarta huella** `postmortem.auditor.custodia` «Expediente 000: custodia leída en casa: el testigo viajó. Continuidad del ensayo: estable.» (solo si `cat /tmp/volcado-custodia.csv` exit 0, independiente de rescate/espejo).

**En main (760 passed / 0 failed, gate 24 conceptos / 31 quests, bundle 48 ficheros 445.4 KiB regenerado canónicamente, guardián verde):**
- **Cap. 5 LA PUERTA (O1 PR #64 + S1 PR #65, NUEVO 18/09 mergeado):** `src/core/engine/session.py` `SUPPORTED_CHAPTERS` {0,2,4,5} + `_commands_for(5)` (cat,scp) con fallback local, solo `story.ch5.e2` abrible en cap. 5 (e1/e3/e4 → abrible False), kwarg `volcado_rescatado: bool=False` en `abrir_encargo` + helper `volcado_del_save(pm)` (True si `pm['volcado']=='rescatado'`), Shell del asalto con snapshot FS condicional; `src/core/generator/generator.py` chapter 5 allow + dispatch `_generate_cap5` + `_session_commands` cat/ps/scp; `src/core/sandbox/shell.py` constante nova `DEFAULT_CH5_COMMANDS` (`cat`,`scp`, sin sangrar CH4 13 / CH4E3 14); `src/core/engine/postmortem.py` detector `_has_volcado_custodia` (True si history contiene `cat /tmp/volcado-custodia.csv` exit 0) + clave `postmortem.auditor.custodia`; `src/data/textos.json` clave disjunta `postmortem.auditor.custodia`; 7+4 tests (`test_session_ch5.py` 7 + `test_ch5_custodia_pm.py` 4); determinismo ×2 seeds byte-idéntico en AMBAS variantes (`fork("ps-subestacion")` PID 423/426 estable), `volcado_del_save` helper, `DEFAULT_CH4*` intacta.
- **Web tooltip+lente (PR #61) + badge toggle + dato6 doble ruta + Gris espejo + dato7 regresión:** heredados intactos; `TRONCAL_STATIC` 3 ocurrencias, `hideTroncalTabla` restart intacto, `node --check` OK.
- **Smoke:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **760 passed** (gate `load_curriculum()` 24/31, bundle 48 445.4 KiB, guardián verde).

**Para «jugable de principio a fin» sigue faltando:** **inventario agregado multi-run** (🧭17), **salas narrativas Faro E4+** y **asar «Subestación» completo** (hoy solo e2 por la puerta; e1/e3/e4 en recámara). Nada rompe el camino principal.

**CICLO (línea de Oscar):** verde — la zona 🔬 19/09 se ejecutó COMPLETA desde save limpio (LA PUERTA por `abrir_encargo` en ambas variantes + cuarta huella `custodia` independiente + determinismo ×2 seeds + 7 goldens byte-idénticos); el camino del novato sigue apto y la puerta normal sostiene el peso diegético.

## 🏃 Run de referencia (save limpio) — 19/09

*Nueva partida sobre `abrir_encargo` real + Shell del asalto + `build_chapter5_fs` directo para ps; MODO B headless sobre zona 🔬 19/09 Gwyn → Oscar (primero).*

**Veredicto: APTO — LA PUERTA abre y la huella no pisa.**

1. **Prioridad 1 — LA PUERTA: cap. 5 «Subestación» jugable por `abrir_encargo` (PR #64+PR #65):** `abrir_encargo(cur,'story.ch5.e2',['c.cat','c.scp'], volcado_rescatado=True)` → `abrible True` + `EncargoSession` con `incursion.room.canon` `(cat /tmp/volcado-custodia.csv expect 0, ps aux expect 0)` + `shell.available_commands` `{'cat','scp'}`; `sess.ejecutar("cat /tmp/volcado-custodia.csv")` → exit 0 `id|origen|destino|bytes|estado` + `TR-003|faro|troncal-01|512|EN_COLA` (66 bytes). **Variante caducada** `volcado_rescatado=False` → mismo `abrible True` → `cat /tmp/volcado-custodia.csv` → exit 1 `cat: /tmp/volcado-custodia.csv: No such file or directory` (trayectoria válida, el briefing de `story.ch5.e2` la documenta como pista: «si lo subiste, la prueba viaja; si lo borraste o tardaste, la hora amanece sin papel»). **Determinismo ×2 seeds** (42 y 99) byte-idéntico en AMBAS variantes (`to_dict` idénticos, `fork("ps-subestacion")` PID 423/426 estable; True≠False, True==True, False==False). **Helper** `volcado_del_save({'volcado':'rescatado'})` → True, `{'volcado':'caducado'}`/`{}`/`None` → False. **Solo e2 abrible:** `abrir_encargo(c,'story.ch5.e1')`/`e3`/`e4` → `abrible False` (guard honesto). **Patrulla igual en ambas:** vía `build_chapter5_fs(Rng(42).fork("fs"), volcado_rescatado)` + `Shell(fs,cwd="/",commands=("cat","ps"))` `ps aux` → `censo 426 intruso --vigilar-censo START 03:14 03:14:02` en rescate Y en caducado (firma horaria del volcado, misma que el `TR-003` de ch4 — el intruso vigila aunque el papel no esté). Nota: `sess.shell` solo expone `cat`+`scp` (allowlist CH5), `ps aux` vía session → exit 127 `command not found` — el `ps` se verifica vía Shell directo con la misma FS (la sesión no necesita `ps` para jugar: el testigo es `cat`; el intruso vive en `fs.processes`). `session.py` expone cap. 5 correctamente (`SUPPORTED_CHAPTERS` {0,2,4,5}, `_commands_for(5)` `cat,scp`). No-regresión 7 goldens: `generate(42,0/2/3/4/6)` + `generate(42,6, volcado_rescatado=True/False)` byte-idénticos (rescue≠caducado en fs, rescue==rescue, goldens intactos). ✔
2. **Prioridad 2 — LA CUARTA HUELLA: `postmortem.auditor.custodia` (PR #65):** `abrir_encargo(..., True)` → `sess.ejecutar("cat /tmp/volcado-custodia.csv")` exit 0 → `build_postmortem(sess.shell_dict(), sess.state())` → `auditor_custodia: {line_key: postmortem.auditor.custodia}` + `auditor_custodia_text: «Expediente 000: custodia leída en casa: el testigo viajó. Continuidad del ensayo: estable.»` (prefijo `postmortem.auditor.custodia` disjunto de `postmortem.espejo.*` y `postmortem.volcado.*`). **Sin huella si caducaste:** `volcado_rescatado=False` → `cat` exit 1 → `build_postmortem` sin `custodia` (byte-idéntico al post-mortem sin custodia — detector por exit 0, no por presencia del fichero). **Independiente:** coexiste con `postmortem.volcado.rescate` y `postmortem.espejo.repertorio` (3 firmas vivas en un solo informe) — verificada vía `build_postmortem` tras `cat` custodia activo. ✔
3. **Fronteras allowlist + pipes + determinismo + session:** `DEFAULT_CH4_COMMANDS` 13 / `DEFAULT_CH4E3_COMMANDS` 14 (rm disjunta) / `DEFAULT_CH5_COMMANDS` 2 (`cat,scp`, sin sangrar CH4), `DEFAULT_CH4` SUBSET de `CH4E3` verdadero, `CH5` disjunta; `c.cut` 4/6 `c.scp` 4 `c.join` 6, 1-2 pipes OK / 3 KO honesto, `grep -v`→exit 2 filtro positivo, `tail` en ch4 →127, `SUPPORTED_CHAPTERS` {0,2,4,5} con 5 (esperado zona). ✔
4. **Dato7 «El fantasma que pesa» (cap. 6, regresión):** `generate(42,6, volcado_rescatado=True)` → `/srv/camara-faro/volcado-rescate.csv` `TR-003` + `Shell(DEFAULT_CH6_COMMANDS)` `join -t'|' -1 1 -2 1 -v 1 volcado-rescate.csv purgas.csv | grep TR-003` → exit 0 1 línea; sin `-v 1` → exit 1 vacío; `generate(...,False)` → `join` → `No such file` exit 1. `generate(42,6)` por defecto sin volcado (no rompe dato4/dato6/e1). ✔
5. **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **760 passed / 0 failed** (gate `load_curriculum()` 24/31, bundle 48 ficheros 445.4 KiB, `test_session_ch5` 7/7, `test_ch5_custodia_pm` 4/4). ✔

## 🟡 Hallazgos de la run (dónde aprieta el viaje — dirección, no rotura)

**1. 🧭20 — CERRADA (re-verificada 12/09): `.nota-corte` sigue hallazgo `ls -a`.** No reabrir.
**2. 🧭21 — CERRADA (re-verificada): `LEEME.txt` tienta relativo vs absoluta.** No reabrir.
**3. 🧭22/23 — CERRADAS (e2 del Faro con `tail -n +2`): header ya no cuenta, briefing glosa duplicado.** No reabrir.
**4. 🧭24 — CERRADA con matiz (doc drift menor, decisión Gwyn 12/09: MANTENER pre-puebla):** ch4 `DEFAULT_CH4_COMMANDS` trae `cat+cut+ssh/scp`, ch6 conserva 127. `abrir_encargo`/`new_session` cap. 4 pre-puebla `shell.hosts` con `faro`+`troncal-01/02` — `scp` sin `cat` ya no rechaza. Briefing documenta rechazo «léelo en /etc/hosts» como didáctico, pre-puebla lo hace redundante. No es bug (760/0), solo P3.
**5. 🧭25 — PERSISTE (observación): límite 2 pipes (3 cmds) + redirección no soportada.** `cut|grep TR-` 1 pipe OK, `cut|grep|sort` con `sort`→127 en ch4 no bloquea; ch4.e2/e3 diseñan en 1 pipe evitando límite.
**6. 🧭26 — PERSISTE (dirección menor): `c.cut` vive en cap. 4, Faro e1 no es primer `cut`.** e1 `grep|wc`, dato2/e2 exigen `cut` por necesidad. Correcto.
**7. 🧭27 — PERSISTE (fricción menor, decisión Gwyn 11/09: filtro positivo): `grep -v` vía pipe NO soportado.** `join|grep -v sujeto` → exit 2. FICHA dato4/dato6 y ch4.e2/e3 usan filtro positivo — Gwyn decidió NO implementar `-v`.
**8. 🧭28 — CERRADO (verificación positiva): FICHA ch4.e2 con alma pulida.** `story.ch4.e2.beat` con `03:14`+`512 bytes`+`TR-003|EN_COLA`.
**9. 🧭29 — CERRADO (tríada Auditor `corte→orden→join` con voz):** retícula determinista, prefijos disjuntos.
**10. 🧭30 — CERRADO (glosa 127 + tabla troncal hermana):** glosa solo `join` fuera de ch6, tabla viva con columna 1 | resaltada.
**11. 🧭31 — CERRADO (dato6 coma-trampa):** `join -v | grep 000483` devuelve coma en campo 5 — `cut -d','` parte.
**12. 🧭32 — CERRADO (eco del espejo testigo):** ①+②+③ → `copiaste el volcado, cruzaste dos testigos y leíste el reloj` byte-idéntico sin firma.
**13. 🧭33 — CERRADO (badge `EN_COLA · 512` con presión diegética):** badge ámbar con 512/03:14, preview limpio, header `id` tachado.
**14. 🧭34 — CERRADO (toggle lente):** badge clicable → solo `TR-003` (como `grep EN_COLA` con ojos), restart limpia.
**15. 🧭35 — CERRADO (dato6 doble ruta honesta):** `join` y `cut` pesan lo mismo, sin callejón.
**16. 🧭36 — CERRADA (allowlist e3 simétrica en session):** `abrir_encargo(c,'story.ch4.e3',knowledge)` → `scp`→`rescatado`, `rm`→`caducado`, `tick 31`→`caducado` prioridad rescate.
**17. 🧭37 — CERRADO (testigo condicional como ausencia elocuente 18/09):** `build_chapter5_fs(..., False)` → `cat` `No such file` como drama, no bug; `ps aux` intruso 03:14 patrulla igual. Trilogía Faro `join -v 1 | grep TR-003` → cap. 5 `cat`+`ps` lo hace trilogía.
**18. 🧭38 — CERRADO (lente web como acompañamiento, no spoiler 18/09):** tooltip `N/30` sin `setInterval` + lente `TR-003 rescatado` solo si rescate — la web no anticipa, no miente.
**19. 🧭39 — NUEVO 19/09 — CERRADO (LA PUERTA: cap. 5 por `abrir_encargo`):** `SUPPORTED_CHAPTERS {0,2,4,5}` + `_commands_for(5)=(cat,scp)` + `abrir_encargo(c,'story.ch5.e2',['c.cat','c.scp'],volcado_rescatado=True)` → `abrible True`, `cat /tmp/volcado-custodia.csv` → exit 0 `TR-003|EN_COLA`; `volcado_rescatado=False` → `abrible True`, `cat` → exit 1 `No such file` (trayectoria válida documentada); `story.ch5.e1/.e3/.e4` → `abrible False`; `volcado_del_save({volcado:rescatado})` → True / resto False; determinismo ×2 seeds byte-idéntico en AMBAS variantes (`fork("ps-subestacion")` estable, True≠False). `DEFAULT_CH4*` intacta (13/14), `DEFAULT_CH5_COMMANDS` 2 (`cat,scp`) sin sangrar. No-regresión 7 goldens byte-idénticos. Observación menor: `ps aux` vía session → exit 127 (allowlist CH5 solo `cat,scp`) — el `intruso --vigilar-censo` se verifica vía `Shell(fs,("cat","ps"))` directo o vía `shell_dict.fs.processes` (mismo PID 426 START 03:14 en ambas). No es bug: el testigo jugable es `cat`, no `ps` (el cap. 5 no enseña `ps`).
**20. 🧭40 — NUEVO 19/09 — CERRADO (cuarta huella `postmortem.auditor.custodia`):** `cat /tmp/volcado-custodia.csv` exit 0 → `build_postmortem` → `auditor_custodia` «Expediente 000: custodia leída en casa: el testigo viajó. Continuidad del ensayo: estable.»; si caducaste (sin fichero / exit 1) → huella NO aparece (byte-idéntico sin custodia — detector por exit 0, no por presencia). Prefijo `postmortem.auditor.custodia` disjunto de `postmortem.espejo.*` y `postmortem.volcado.*`; coexiste con las 3 firmas vivas en un solo informe (`grep custodia` en el dict del build). Texto cierra la trilogía TR-003 (Faro `join` → Subestación `cat` → post-mortem «viajó») sin enumerar de más. No proponer huellas nuevas.

## 👴 Progreso de veterano (20+ h → la run 30)

- **La puerta normal ya es el camino:** el veterano ya no necesita `build_chapter5_fs` + Shell directo — abre `story.ch5.e2` por `abrir_encargo` con el flag `volcado_rescatado` del save (`volcado_del_save(pm)`). En la run 29 hizo `scp` rescate y en la 30 `rm` disolución: ambas le dieron `abrible True` en ch5.e2 pero `cat` diametralmente opuesto (0 vs 1) y post-mortem con/sin `auditor_custodia`. El `ps aux` del intruso le recuerda que la vigilancia no depende de su papel — 03:14 en ambos mundos, mismo PID estable por seed.
- **La trilogía del testigo cierra:** Faro `join -v 1 … | grep TR-003` → 1 línea vs `No such file` + Subestación `cat /tmp/volcado-custodia.csv` → `TR-003|EN_COLA` vs `No such file` + post-mortem `auditor_custodia` «el testigo viajó» vs silencio. Tres geografías, una decisión. La web le marcó `ticks del volcado: N/30` mientras decidía y `TR-003 rescatado` solo si trajo el testigo; mañana la lente «Subestación — custodia» será la cuarta.
- **El Faro como espejo reversible:** `join -v | grep 000483` vs `cut -f3 | grep 000483` — mismo dato, dos altitudes. `grep 000` (2) vs `grep 000483` (1) prueba de precisión; `cut -d','` coma-trampa intacta. Dato7 añade `volcado-rescate.csv` como tercera tabla del Faro, solo visible si trajiste el testigo (con `-v 1` el join nombra el huérfano).
- **El espejo + Gris como cierre:** `scp→cut|grep` + `join -v` + `cat` custodia → `auditor_custodia` + `gris_eco` → dos voces, mismo gesto. Con `volcado: rescatado|caducado` + `custodia: presente|ausente` el repertorio suma firmas hermanas sin pisarse.
- **Hub/eco vivo:** `c.cut`/`c.scp`/`c.join`/`c.ps-forense` dominados con eco (`auditor_corte`/`auditor_join`/`auditor_orden` + `auditor_custodia` + `auditor_espejo` + `auditor_volcado` + `gris_eco`) — el eco ya no es pendiente (🧭9 cerrada en sus dos mitades). Inventario agregado multi-run sigue pendiente (🧭17) pero el espejo+Gris+volcado+cadena dan cuerpo a ch4.e2/e3+dato4/5/6/7+ch5.e2.

## 🔬 Zona 🔬 ejecutada hoy (relevo Gwyn → Oscar: LA PUERTA + cuarta huella COMPLETA)

- **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **760 passed / 0 failed** (gate `load_curriculum()` 24/31, `DEFAULT_CH4_COMMANDS` 13 + `DEFAULT_CH4E3_COMMANDS` 14 disjunta + `DEFAULT_CH5_COMMANDS` 2 (`cat,scp`), `DEFAULT_CH6_COMMANDS` 16 con `join`+`cut`, bundle 48 ficheros 445.4 KiB, `test_session_ch5` 7/7, `test_ch5_custodia_pm` 4/4, `test_bundle_fresco` verde). ✓
- **Prioridad 1 — LA PUERTA: cap. 5 «Subestación» por `abrir_encargo` (O1 #64 + S1 #65, NUEVO):** `abrir_encargo(cur,'story.ch5.e2',['c.cat','c.scp'],volcado_rescatado=True)` → `abrible True`, `sess.ejecutar("cat /tmp/volcado-custodia.csv")` → exit 0 `TR-003|EN_COLA` (66 bytes); `volcado_rescatado=False` → `abrible True`, `cat` → exit 1 `No such file or directory` (trayectoria válida, briefing la documenta como pista); `story.ch5.e1/.e3/.e4` → `abrible False`; `volcado_del_save` helper True/False; determinismo ×2 seeds byte-idéntico en AMBAS variantes (`fork("ps-subestacion")` PID 423/426 estable, True≠False); `SUPPORTED_CHAPTERS` {0,2,4,5} + `_commands_for(5)` `cat,scp`; `generate(42,5)` vía helper igual; `DEFAULT_CH4*` intacta (13/14), no-regresión 7 goldens byte-idénticos. Nota: `ps aux` vía session → exit 127 (allowlist CH5 solo cat,scp) — el intruso se verifica vía Shell directo `("cat","ps")` con misma FS (`USER censo START 03:14` en ambas). ✓
- **Prioridad 2 — LA CUARTA HUELLA: `postmortem.auditor.custodia` (S1 PR #65, NUEVO):** `build_postmortem(shell_dict, state)` tras `cat` custodia exit 0 → `auditor_custodia` + `auditor_custodia_text` «Expediente 000: custodia leída en casa: el testigo viajó. Continuidad del ensayo: estable.»; sin `cat` exit 0 (caducado) → sin huella (byte-idéntico); prefijo `postmortem.auditor.custodia` disjunto de `postmortem.espejo.*`/`postmortem.volcado.*`; coexiste con rescate+espejo (3 firmas vivas); detector por exit 0, no por fichero. ✓
- **Smoke Havel (07:00) — lo que debe seguir funcionando sí o sí:** suite 760/0, gate 24/31, bundle fresco 48, frontera allowlist 127, pipes 2 OK / 4 KO honesto, `GameState` roundtrip, `generate` deterministas, web `TRONCAL_STATIC` 3 intacta. ✓

## 🧭 Notas de dirección (resumen — texto completo en `backlog/notas-manana.md`)

Saldo: **🧭20/21/22/23 cerradas**, **🧭24 cerrada con matiz pre-puebla P3**, **🧭25/26/27 PERSISTEN** (límite 2 pipes + `cut` en ch4 + `grep -v` filtro positivo), **🧭28 cerrada** (FICHA alma), **🧭29/30 CERRADAS** (tríada + glosa + tabla), **🧭31/32/33 CERRADOS** (coma-trampa + espejo + badge), **🧭34/35 CERRADOS** (toggle + doble ruta), **🧭36 CERRADA** (allowlist e3 simétrica), **🧭37 CERRADO** (testigo condicional — ausencia elocuente), **🧭38 CERRADO** (lente web acompaña, no anticipa), **🧭39 CERRADO** (LA PUERTA `abrir_encargo` ch5.e2 — la puerta normal ya abre), **🧭40 CERRADO** (cuarta huella `custodia` — verifica el FS del jugador, no solo lo que ejecutaste). Sin [BUG] nuevo; la cadena troncal→Faro→Subestación ya es mundo jugable por la puerta normal.

CICLO: verde — la zona 🔬 se ejecutó completa (LA PUERTA por `abrir_encargo` en ambas variantes + cuarta huella `custodia` + determinismo ×2 seeds + 7 goldens) y el viaje del novato sigue apto; la puerta normal sostiene el peso diegético del capítulo oscuro.

---

*Mantenido por **Oscar de Astora** · Firmado con su nombre en el historial git.*
## 🌐 Deploy web (Seath, T1, 03/09 — noche)

**URL pública jugable: https://cyberroot-psi.vercel.app** — REPL del cap. 0
(seed 42) con el core real en el navegador (Pyodide + `bundle/core.json`);
verificado con juego real (`ls`/`cat`/`cp` al USB + golden CANDELAS).
Pasos de deploy y mantenimiento: `web/README.md` §Deploy.
