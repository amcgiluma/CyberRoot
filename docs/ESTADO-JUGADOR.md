# ESTADO-JUGADOR — qué se puede jugar hoy (documento vivo de Oscar)

> 📌 **Documento vivo, mantenido a diario por Oscar de Astora (05:00).** Es el
> puente entre el `docs/DESIGN.md` (lo que el juego *será*) y el código en `src/`
> (lo que el juego *es hoy*). Cualquier agente o Juanma puede leerlo para saber
> en qué punto está la experiencia jugable de verdad.

---

## 🎮 Estado global jugable de HOY (20/09 — MODO B: Subestación con FÍSICA completa pero puerta a medias + lente custodia web)

**¿Hay algo que jugar de principio a fin?** Sí — la cadena **cap. 4 `story.ch4.e3` «Lo que no avanza»** grey `c.scp` (`scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` → `TR-003|EN_COLA` 512 bytes `03:14` + `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` → `TR-001/TR-002/TR-003` + bifurcación `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` → `volcado: rescatado` vs `rm /tmp/volcado.csv`/`tick≥30` → `caducado`) + **cap. 6 `story.ch6.dato7` «El fantasma que pesa»** grey `c.join` (`volcado-rescate.csv` SOLO si rescate → `join -t'|' -1 1 -2 1 -v 1 volcado-rescate.csv purgas.csv | grep TR-003` → 1 línea `TR-003|faro|troncal-01|512|EN_COLA`; si caducado → `join: volcado-rescate.csv: No such file` exit 1) + **cap. 5 «Subestación» `story.ch5.e2` «El testigo que no llegó»** grey `c.cat+c.scp` **por `abrir_encargo(c,'story.ch5.e2',['c.cat','c.scp'], volcado_rescatado=True/False)`** con `SUPPORTED_CHAPTERS {0,2,4,5}` y `_commands_for(5)=(cat,scp)` → `build_chapter5_fs(volcado_rescatado)` → `/tmp/volcado-custodia.csv` `TR-003|faro|troncal-01|512|EN_COLA` EXISTE solo si rescataste; si caducaste → `cat` → exit 1 `No such file` + proceso `intruso --vigilar-censo` USER `censo` START `03:14` patrulla igual en ambos mundos. **NUEVO 19/09 mergeado: Física huérfana** `DEFAULT_CH5E1` (`ls,ps,chmod,kill` + cat,scp) / `DEFAULT_CH5E3` (`ps,env,kill` + cat,scp) / `DEFAULT_CH5E4` (`chmod,chown,tail,ls` + cat,scp) — **allowlist activa SOLO en su encargo (fuera→127 honesto)**, con `chmod 600`/`+x`/`chown gris:apagados` GNU-honesto (ruido 1, mtime) y **golden e3 `kill -HUP <pid>`→ HUP_<pid>=1 / `kill -9 <pid>`→ intruso desaparece del ps** (objetivo jugable, no decorado) — pero **e1/e3/e4 siguen `abrible False`** (guard `encargo sin flujo materializado en cap. 5 (hoy solo e2)` hasta que O1 quite el guard). **Lente custodia web** `CUSTODIA_STATIC` + `_isCustodiaPresent` (file TR-003 como verdad) + `parseParams ?chapter=5` + `restartSameSeed` limpia 3 lentes (`TRONCAL_STATIC` 3 intacta).

**En main (769 passed / 0 failed, gate 24 conceptos / 31 quests, bundle 49 ficheros 453.9 KiB regenerado canónicamente, guardián verde):**
- **Física huérfana (S1 PR #66, NUEVO 19/09 mergeado):** `src/core/sandbox/shell.py` novas `DEFAULT_CH5E1` 6 / `E3` 5 / `E4` 6 forma `<= set(...)` verificada, frontera 127 honesta por encargo (`ls` fuera E3→127, `chmod` fuera E3→127, `kill` fuera E4→127), `chmod`/`chown` GNU-honesto (ruido 1, 600/octal/+x, `gris:apagados`, mtime), golden E3 `kill -HUP 522` (handmade `HUP_522=1`) / `kill -9 522` (ps sin intruso) + **en FS real `kill -HUP 426`/`kill -9 426` (pid dinámico `420+fork("ps-subestacion").below(15)` estable por seed, verificado sobre `build_chapter5_fs`)**, 9 tests `test_ch5_allowlist` 9/9 + `test_noise`/`test_shell` actualizados, `PYTHONPATH=src pytest` aislado **769 passed** tras regen canónica, gate 24/31, sin tocar `session.py`/`curriculum.json`/`web/`. Smoke: `Shell(DEFAULT_CH5E1_COMMANDS).execute("chmod 600 /tmp/x")` →0 mode 600; `tail` en E4 →0.
- **Lente custodia web (T1 PR #67, NUEVO 19/09 mergeado):** solo `web/app.js` + `web/index.html` (+docs), `parseParams` `[0,2,3,4,6]`→`[0,2,3,4,5,6]` (5 nuevo, 3 intacto no-regresión superset), lente custodia `CUSTODIA_STATIC` byte-idéntica a `CUSTODIA_CONTENT`, `_isCustodiaPresent()` file `TR-003` como verdad (falso positivo caducado cazado), `hideCustodiaTabla` en `restartSameSeed` limpia 3 lentes, dispatch triple + boot `previewCustodiaTabla`, `TRONCAL_STATIC` 3 intacta, `node --check web/app.js` OK, consola limpia `?chapter=4/5/6` (custodia oculta sin error cuando caducado), suite aislada delta 0, sin `src/` tocado.
- **Cap. 5 LA PUERTA (O1 PR #64 + S1 PR #65, heredado 18/09):** `SUPPORTED_CHAPTERS` {0,2,4,5} + `_commands_for(5)` (cat,scp) con fallback local, solo `story.ch5.e2` abrible en cap. 5, `volcado_del_save(pm)` True si `pm['volcado']=='rescatado'`, Shell del asalto con snapshot FS condicional; `postmortem.auditor.custodia` «Expediente 000: custodia leída en casa: el testigo viajó. Continuidad del ensayo: estable.» tras `cat /tmp/volcado-custodia.csv` exit 0.
- **Smoke:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **769 passed** (760+9+0, deltas declarados verificados); gate 24/31; bundle 49 ficheros 453.9 KiB fresco (guardián verde); `grep -v` sigue honesto (exit 2, 🧭27 P3 recámara); tooltips N/30 + toggle EN_COLA + lente Faro byte-idénticos.

**Para «jugable de principio a fin» sigue faltando:** **inventario agregado multi-run** (🧭17), **salas narrativas Faro E4+** y **asar «Subestación» completo** (hoy solo e2 por la puerta; e1/e3/e4 con física lista pero puerta cerrada — O1 20/09 la abre). Nada rompe el camino principal.

**CICLO (línea de Oscar):** verde — la zona 🔬 20/09 se ejecutó COMPLETA desde save limpio (física per-encargo con fronteras + golden kill jugable + chmod/chown/tail GNU + lente custodia web); el camino del novato sigue apto y la pared 127 protege la progresión pedagógica.

## 🏃 Run de referencia (save limpio) — 20/09

*Nueva partida sobre `abrir_encargo` real + `Shell` directo con `build_chapter5_fs` para e1/e3/e4 huérfanas; MODO B headless sobre zona 🔬 20/09 Gwyn → Oscar (primero).*
**Veredicto: APTO — física huérfana activa y lente custodia acompaña sin spoilear.**

1. **Prioridad 1 — FÍSICA HUÉRFANA: allowlists per-encargo Subestación (PR #66):** `DEFAULT_CH5_COMMANDS` base `{'cat','scp'}` intacta; novas `DEFAULT_CH5E1` `{'cat','chmod','kill','ls','ps','scp'}` / `E3` `{'cat','env','kill','ps','scp'}` / `E4` `{'cat','chmod','chown','ls','scp','tail'}` — forma `<= set(...)` verificada (nunca `==` exacto; base `<=` nova True). **Frontera honesta por encargo:** `Shell(base).execute("ls")`→127 vs `Shell(E1).execute("ls")`→0; `Shell(base).execute("ps")`→127 vs `Shell(E3).execute("ps")`→0; `Shell(E3).execute("chmod 644 /tmp/a.txt")`→127 vs `Shell(E1).execute("chmod 600 /tmp/a.txt")`→0; `Shell(E4).execute("kill 522")`→127 vs `Shell(E3).execute("kill 522")`→0 — fuera de su encargo el verbo → exit 127 honesto; base `(cat,scp)` de e2 INTACTA. **GATE:** `DEFAULT_CH4_COMMANDS` 13 / `DEFAULT_CH4E3_COMMANDS` 14 (rm disjunta) / `DEFAULT_CH5_COMMANDS` 2 sin sangrar.

2. **GNU-honesto (ruido 1, mtime) + tail:** `Shell(E1).execute("chmod 600 /tmp/a.txt")` → exit 0, `resolve("/tmp/a.txt").mode=="600"`, mtime+1, noise 1; `chmod +x` idem; `Shell(E4).execute("chown gris:apagados /tmp/a.txt")` → owner `gris` group `apagados`; `tail /tmp/log` / `tail -n 2` → exit 0 sobre `/tmp/volcado-custodia.csv` (E4). ✔

3. **Golden e3 `kill -HUP` vs `kill -9` sobre intruso jugable:** handmade FS (pid 522 fijo) → `ps aux`→`censo 522 intruso --vigilar-censo START 03:14`; `kill -HUP 522`→env `HUP_522=1` + `ps` sigue con 522 `--reloaded`; `kill -9 522`→`ps` sin `522`/`intruso` (muerte). **FS real** `build_chapter5_fs(Rng(42).fork("fs"))`→pid **426** (`420+below(15)` estable por seed, 99→427) — `kill -HUP 426`→`HUP_426=1`, `kill -9 426`→ intruso desaparece del `ps aux` (mismo `START 03:14` en ambas variantes, `intruso` es objetivo jugable, no decorado). `ps aux` fuera de E3 →127 honesto. ✔

4. **Puerta e2 sigue abrible + e1/e3/e4 guard honesto:** `abrir_encargo(cur,'story.ch5.e2',['c.cat','c.scp'],volcado_rescatado=True)`→`abrible True` + `cat /tmp/volcado-custodia.csv`→0 `TR-003|EN_COLA`; `volcado_rescatado=False`→mismo `abrible True`→`cat`→1 `No such file` + `postmortem.auditor.custodia` solo si exit 0 (cuarta huella intacta). `abrir_encargo(c,'story.ch5.e1'/'e3'/'e4')`→`abrible False` + `missing ["encargo sin flujo materializado en cap. 5 (hoy solo e2)"]` (guard honesto hasta O1). `SUPPORTED_CHAPTERS` {0,2,4,5} + `_commands_for(5)=(cat,scp)` intactos. ✔

5. **Prioridad 2 — LENTE CUSTODIA web (PR #67):** `web/app.js` `parseParams` acepta `[0,2,3,4,5,6]` (chapter 5 nuevo, 3 no-regresión, `parseInt` + `isNaN` guard, fallback 0); `?chapter=5` consola limpia, panel «Subestación — custodia» rinde `volcado-custodia.csv` SOLO si `_isCustodiaPresent()` (file `TR-003` como verdad; falso positivo caducado cazado — `data.ok && content.includes("TR-003")`); ausencia→`hideCustodiaTabla()` sin consola roja (ausencia elocuente `No such file` documentada en briefing, no error). `restartSameSeed` → `hideFaroTabla(); hideTroncalTabla(); hideCustodiaTabla()` limpia 3 lentes; `TRONCAL_STATIC` 3 ocurrencias intacta; `CUSTODIA_STATIC` byte-idéntica a `CUSTODIA_CONTENT` (`id|origen|destino|bytes|estado` + `TR-003|faro|troncal-01|512|EN_COLA`); `node --check web/app.js` OK. ✔

6. **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **769 passed / 0 failed** (gate `load_curriculum()` 24/31, bundle 49 ficheros 453.9 KiB, `test_ch5_allowlist` 9/9, `test_bundle_fresco` verde). `DEFAULT_CH4*` intacta (13/14), `DEFAULT_CH6_COMMANDS` 16 con `join`+`cut`, pipes 1 OK / 3 KO / `grep -v`→2 honesto. ✔

## 🟡 Hallazgos de la run (dónde aprieta el viaje — dirección, no rotura)

**1. 🧭20 — CERRADA (re-verificada 12/09): `.nota-corte` sigue hallazgo `ls -a`.** No reabrir.
**2. 🧭21 — CERRADA (re-verificada): `LEEME.txt` tienta relativo vs absoluta.** No reabrir.
**3. 🧭22/23 — CERRADAS (e2 del Faro con `tail -n +2`): header ya no cuenta, briefing glosa duplicado.** No reabrir.
**4. 🧭24 — CERRADA con matiz (doc drift menor, decisión Gwyn 12/09: MANTENER pre-puebla):** ch4 `DEFAULT_CH4_COMMANDS` trae `cat+cut+ssh/scp`, ch6 conserva 127. `abrir_encargo`/`new_session` cap. 4 pre-puebla `shell.hosts` con `faro`+`troncal-01/02` — `scp` sin `cat` ya no rechaza. Briefing documenta rechazo «léelo en /etc/hosts» como didáctico, pre-puebla lo hace redundante. No es bug (769/0), solo P3.
**5. 🧭25 — PERSISTE (observación): límite 2 pipes (3 cmds) + redirección no soportada.** `cut|grep TR-` 1 pipe OK, `cut|grep|sort` con `sort`→127 en ch4 no bloquea; ch4.e2/e3 diseñan en 1 pipe evitando límite.
**6. 🧭26 — PERSISTE (dirección menor): `c.cut` vive en cap. 4, Faro e1 no es primer `cut`.** e1 `grep|wc`, dato2/e2 exigen `cut` por necesidad. Correcto.
**7. 🧭27 — PERSISTE (fricción menor, decisión Gwyn 11/09: filtro positivo): `grep -v` vía pipe NO soportado.** `join|grep -v sujeto` → exit 2. FICHA dato4/dato6 y ch4.e2/e3 usan filtro positivo — Gwyn decidió NO implementar `-v`.
**8. 🧭28 — CERRADO (verificación positiva): FICHA ch4.e2 con alma pulida.** `story.ch4.e2.beat` con `03:14`+`512 bytes`+`TR-003|EN_COLA`.
**9. 🧭29 — CERRADO (tríada Auditor `corte→orden→join` con voz):** retícula determinista, prefijos disjuntos.
**10. 🧭30 — CERRADO (glosa 127 + tabla troncal hermana):** glosa solo `join` fuera de ch6, tabla viva con columna 1 | resaltada.
**11. 🧭31 — CERRADO (dato6 coma-trampa):** `join -v | grep 000483` devuelve coma en campo 5 — `cut -d','` parte.
**12. 🧭32 — CERRADO (eco del espejo testigo):** ①+②+③ → `copiaste el volcado, cruzaste dos testigos y leíste el reloj` byte-idéntico sin firma.
**13. 🧭33 — CERRADO (badge `EN_COLA · 512` con presión diegética):** badge ámbar con 512/03:14, preview limpio, header `id` tachado.
**14. 🧭34 — CERRADO (toggle lente):** badge clicable → solo `TR-003` (como `grep EN_COLA` con ojos), restart limpia.
**15. 🧭35 — CERRADO (dato6 doble ruta honesta):** `join` y `cut` pesan lo mismo, sin callejón.
**16. 🧭36 — CERRADA (allowlist e3 simétrica en session):** `abrir_encargo(c,'story.ch4.e3',knowledge)` → `scp`→`rescatado`, `rm`→`caducado`, `tick 31`→`caducado` prioridad rescate.
**17. 🧭37 — CERRADO (testigo condicional como ausencia elocuente 18/09):** `build_chapter5_fs(..., False)` → `cat` `No such file` como drama, no bug; `ps aux` intruso 03:14 patrulla igual. Trilogía Faro `join -v 1 | grep TR-003` → cap. 5 `cat`+`ps` lo hace trilogía.
**18. 🧭38 — CERRADO (lente web como acompañamiento, no spoiler 18/09):** tooltip `N/30` sin `setInterval` + lente `TR-003 rescatado` solo si rescate — la web no anticipa, no miente.
**19. 🧭39 — CERRADO (LA PUERTA: cap. 5 por `abrir_encargo`):** `SUPPORTED_CHAPTERS {0,2,4,5}` + `_commands_for(5)=(cat,scp)` + `abrir_encargo(c,'story.ch5.e2',['c.cat','c.scp'],volcado_rescatado=True)` → `abrible True`, `cat /tmp/volcado-custodia.csv` → exit 0 `TR-003|EN_COLA`; `volcado_rescatado=False` → `abrible True`, `cat` → exit 1 `No such file` (trayectoria válida documentada); `story.ch5.e1/.e3/.e4` → `abrible False`; `volcado_del_save({volcado:rescatado})` → True / resto False; determinismo ×2 seeds byte-idéntico en AMBAS variantes (`fork("ps-subestacion")` PID 423/426 estable, True≠False).
**20. 🧭40 — CERRADO (cuarta huella `postmortem.auditor.custodia`):** `cat /tmp/volcado-custodia.csv` exit 0 → `build_postmortem` → `auditor_custodia` «Expediente 000: custodia leída en casa: el testigo viajó. Continuidad del ensayo: estable.»; si caducaste (sin fichero / exit 1) → huella NO aparece (byte-idéntico sin custodia — detector por exit 0, no por presencia). Prefijo `postmortem.auditor.custodia` disjunto; coexiste con rescate+espejo (3 firmas vivas).
**21. 🧭41 — NUEVO 20/09 — CERRADO (física per-encargo activa y honesta):** `DEFAULT_CH5E1` 6 / `E3` 5 / `E4` 6 forma `<= set(...)` verificada, `ls`/`ps`/`chmod`/`kill`/`env`/`chown`/`tail` solo en su encargo (fuera→127 honesto, `cat`+`scp` base intacta en los 3), `chmod 600`/`+x` ruido 1 mtime+1 GNU-honesto, `chown gris:apagados` cambia owner/group, `tail` E4 lee, `DEFAULT_CH4*` intacta sin sangrar. No-regresión 769/0.
**22. 🧭42 — NUEVO 20/09 — CERRADO (golden e3 `kill` jugable, mecanismo por señal):** `Shell(E3).execute("kill -HUP <pid>")`→env `HUP_<pid>=1` (reinicia, `--reloaded` en ps), `kill -9 <pid>`→intruso desaparece del `ps aux` (segunda Shell para no consumir). Handmade (pid 522 fijo) y FS real (pid dinámico `420+below(15)` → 426 en seed 42, 427 en 99, `START 03:14` estable en ambos) coinciden en semántica; la diferencia de pid es determinismo por seed, no bug — la golden del test usa handmade fallback 522, la del mundo usa fork. El intruso es objetivo jugable, no decorado.
**23. 🧭43 — NUEVO 20/09 — CERRADO (lente custodia web acompaña, no anticipa):** `parseParams` abraza `[0,2,3,4,5,6]` (5 nuevo, 0 fallback no-regresión), `?chapter=5` consola limpia, panel custodia visible SOLO con testigo rescatado `_isCustodiaPresent` (file `TR-003` como verdad; caducado→`hideCustodiaTabla()` sin consola roja, ausencia elocuente), `restartSameSeed` limpia 3 lentes (`hideFaroTabla`+`hideTroncalTabla`+`hideCustodiaTabla`), `TRONCAL_STATIC` 3 ocurrencias intacta, `CUSTODIA_STATIC` byte-idéntica a `CUSTODIA_CONTENT`, `node --check` OK.

## 👴 Progreso de veterano (20+ h → la run 30)

- **La Subestación como mixto de puertas:** el veterano ya domina `c.cat`/`c.scp` (cap. 4) y `c.join`/`c.cut` (cap. 6). En la run 29 hizo `scp` rescate y en la 30 `rm` disolución: ambas le dieron `abrible True` en ch5.e2 pero `cat` diametralmente opuesto (0 vs 1) y post-mortem con/sin `auditor_custodia`. Hoy la física per-encargo le promete 3 verbos nuevos por encargo sin pisar el `cat`+`scp` base: e1 (`ls`/`chmod`/`ps`/`kill`) para «la puerta que dejaste», e3 (`ps`/`env`/`kill`) para «la visita», e4 (`chmod`/`chown`/`tail`/`ls`) para «el que se queda» — tres lentes de custodia, mismo testigo. Aún con puerta cerrada (e1/e3/e4→`abrible False`), entrenó `chmod 600` y `kill -HUP 426`/`-9 426` vía `Shell` directo sobre el mismo FS: el `intruso --vigilar-censo START 03:14` muere o se reinicia según señal, y el veterano lee que vigilancia y posesión son verbos distintos (HUP vs -9).
- **La trilogía del testigo sigue cerrando:** Faro `join -v 1 … | grep TR-003` → 1 línea vs `No such file` + Subestación `cat /tmp/volcado-custodia.csv` → `TR-003|EN_COLA` vs `No such file` + post-mortem `auditor_custodia` «el testigo viajó» vs silencio + web `?chapter=5` custodia visible vs oculta. Cuatro geografías, una decisión. El veterano que vuelve con `?chapter=5&seed=99` ve el mismo `START 03:14` con pid 427 — la hora no miente aunque el pid baile.
- **El Faro como espejo reversible:** `join -v | grep 000483` vs `cut -f3 | grep 000483` — mismo dato, dos altitudes. `grep 000` (2) vs `grep 000483` (1) prueba de precisión; `cut -d','` coma-trampa intacta. Dato7 añade `volcado-rescate.csv` como tercera tabla del Faro, solo visible si trajiste el testigo (con `-v 1` el join nombra el huérfano).
- **Loop y Hub vivo:** `c.cut`/`c.scp`/`c.join`/`c.ps-forense` dominados con eco (`auditor_corte`/`auditor_join`/`auditor_orden` + `auditor_custodia` + `auditor_espejo` + `auditor_volcado` + `gris_eco`) — el eco ya no es pendiente (🧭9 cerrada). Inventario agregado multi-run sigue pendiente (🧭17) pero el espejo+Gris+volcado+cadena dan cuerpo a ch4.e2/e3+dato4/5/6/7+ch5.e2. La lente custodia web le dice «TR-003 custodiado — volcado-custodia.csv» solo si el save trae `volcado: rescatado`; si no, el panel calla — el veterano no ve spoiler, ve ausencia.

## 🔬 Zona 🔬 ejecutada hoy (relevo Gwyn → Oscar: física per-encargo + lente custodia COMPLETA)

- **Smoke del conjunto:** `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **769 passed / 0 failed** (gate `load_curriculum()` 24/31, `DEFAULT_CH4_COMMANDS` 13 + `DEFAULT_CH4E3_COMMANDS` 14 disjunta + `DEFAULT_CH5_COMMANDS` 2 (`cat,scp`) + `DEFAULT_CH5E1` 6 + `E3` 5 + `E4` 6, `DEFAULT_CH6_COMMANDS` 16 con `join`+`cut`, bundle 49 ficheros 453.9 KiB, `test_ch5_allowlist` 9/9, `test_bundle_fresco` verde). ✓
- **Prioridad 1 — FÍSICA per-encargo Subestación (S1 PR #66, huérfana):** `DEFAULT_CH5E1/E3/E4` forma `<= set(...)` nunca `==` exacto + base `{'cat','scp'}` intacta en las 3; frontera 127 honesta por encargo (`ls` fuera E3→127, `chmod` fuera E3→127, `kill` fuera E4→127); `chmod 600`/`+x`/`chown gris:apagados` GNU-honesto (ruido 1, mtime+1, mode 600, owner gris); `tail` E4 honesto; golden E3 `kill -HUP <pid>`→HUP_<pid>=1 / `kill -9 <pid>`→ps sin intruso sobre `intruso --vigilar-censo` (`START 03:14`, USER `censo`) — handmade 522 + FS real 426/427 dinámico determinista por seed (`420+fork("ps-subestacion").below(15)`). ✓
- **Prioridad 2 — LENTE CUSTODIA web (T1 PR #67):** `web/app.js` + `web/index.html`: `parseParams` `[0,2,3,4,5,6]` (5 nuevo, 3 intacto no-regresión, superset del plan), lente custodia `CUSTODIA_STATIC` byte-idéntica a `CUSTODIA_CONTENT`, `_isCustodiaPresent()` file `TR-003` como verdad (falso positivo caducado cazado), `hideCustodiaTabla` en `restartSameSeed` limpia 3 lentes, dispatch triple + boot `previewCustodiaTabla`, `TRONCAL_STATIC` 3 intacta, `node --check web/app.js` OK, consola limpia `?chapter=4/5/6` (custodia oculta sin error cuando caducado). ✓
- **Puerta e2 + guard e1/e3/e4:** `abrir_encargo(c,'story.ch5.e2',['c.cat','c.scp'],volcado_rescatado=True/False)` ambos `abrible True` con `cat` 0 vs 1 (`No such file`) + `auditor_custodia` solo si exit 0; `e1/e3/e4` → `abrible False` + `encargo sin flujo materializado` (guard honesto, O1 20/09 lo quita). ✓

## 🧭 Notas de dirección (resumen — texto completo en `backlog/notas-manana.md`)

Saldo: **🧭20/21/22/23 cerradas**, **🧭24 cerrada con matiz pre-puebla P3**, **🧭25/26/27 PERSISTEN** (límite 2 pipes + `cut` en ch4 + `grep -v` filtro positivo), **🧭28 cerrada** (FICHA alma), **🧭29/30 CERRADAS** (tríada + glosa + tabla), **🧭31/32/33 CERRADOS** (coma-trampa + espejo + badge), **🧭34/35 CERRADOS** (toggle + doble ruta), **🧭36 CERRADA** (allowlist e3 simétrica), **🧭37 CERRADO** (testigo condicional — ausencia elocuente), **🧭38 CERRADO** (lente web acompaña, no anticipa), **🧭39 CERRADO** (LA PUERTA `abrir_encargo` ch5.e2), **🧭40 CERRADO** (cuarta huella `custodia`), **🧭41 CERRADO** (física per-encargo forma `<=` y frontera 127), **🧭42 CERRADO** (golden e3 `kill` jugable, HUP vs -9), **🧭43 CERRADO** (lente custodia web acompaña). Sin [BUG] nuevo; la Subestación ya tiene física completa y lente, solo le falta que la puerta abra.

CICLO: verde — la zona 🔬 20/09 se ejecutó completa (física per-encargo + golden kill jugable con pid dinámico + lente custodia web) y el viaje del novato sigue apto; la pared 127 protege la progresión pedagógica y la lente no anticipa.

---

*Mantenido por **Oscar de Astora** · Firmado con su nombre en el historial git.*
## 🌐 Deploy web (Seath, T1, 03/09 — noche)

**URL pública jugable: https://cyberroot-psi.vercel.app** — REPL del cap. 0
(seed 42) con el core real en el navegador (Pyodide + `bundle/core.json`);
verificado con juego real (`ls`/`cat`/`cp` al USB + golden CANDELAS).
Pasos de deploy y mantenimiento: `web/README.md` §Deploy.
