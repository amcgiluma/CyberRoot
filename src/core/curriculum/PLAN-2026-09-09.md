# PLAN DE IMPLEMENTACIÓN — Smough 09/09 — O2 Quest ch4.e1 «La llave prestada» + c.scp

## Contexto
- Base: main 2fe0aa3 + plan 09/09 (635 passed, 22/24). Rama Smough `feat/sandbox-2026-09-09` desde origin/main.
- Dependencia O1 (Ornstein) `chapter4.py` + `DEFAULT_CH4_COMMANDS` aún no en main (PR #39). Costura declarada: nombres exactos `generate(42,4)` / `DEFAULT_CH4_COMMANDS`. Si no existe, ensayo con FS handmade + nota de costura.
- Bloqueo DAG: `c.cut` está en cap 6 con prereq `c.uniq` (6). `c.scp` prereq `c.cut` en cap 4 violaría validator (`cut` 6 > scp 4) y quest ch4.e1 requiere scp (4) también violaría si scp=6. Solución honesta: mover `c.cut` a cap 4 con prereq `c.wc` (2) — `wc` 2 <= cut 4 <= scp 4 <= quest 4. `cut` sigue válido para dato2/dato3 (4 <=6) y sort/uniq quedan en 6 (test cap6 intacto). Cambio mínimo, DAG válido, suite verde.

## Hitos ( ~1h cada, verificable)

### Hito 1 — Preparar rama y verificar base
- QUÉ: rama `feat/sandbox-2026-09-09` desde origin/main, identidad Smough, `git fetch` + `rev-list` 0 stale, `pytest -q` 635.
- HECHO SI: `git branch --show-current` = sandbox..., `git log --format='%an'` solo Smough, suite 635.

### Hito 2 — curriculum.json (gate 22→23 / 24→25)
- QUÉ: 
  - `c.cut`: `chapter` 6→4, `prerequisites` ["c.uniq"]→["c.wc"] (DAG válido, motivo documentado).
  - Nuevo `c.scp`: `{id:"c.scp", family:"red", chapter:4, prerequisites:["c.cut"], summary_key:"concept.scp.summary"}`.
  - Nueva quest `story.ch4.e1`: `{id:"story.ch4.e1", chapter:4, tint:"blue", requires:["c.scp"], title_key:"story.ch4.e1.title", beat_key:"story.ch4.e1.beat"}` insertada entre ch3 y ch5.
  - Validar: `load_curriculum()` no lanza, `len(concepts)==23`, `len(quests)==25`, `quest ch4.e1` existe con prereq correcto, `dato2/dato3/e1/e2` intactos, DAG sin ciclo (validation).
- INTERFAZ: `src/data/curriculum.json`.
- TEST: manual `python3 -c "from core.curriculum import load_curriculum; ..."` + `test_loader` actualizado.
- HECHO SI: gate 23/25 y `curriculum_from_dict` no grita.

### Hito 3 — textos.json (claves story.ch4.e1.* + concept.scp.summary)
- QUÉ: añadir a `src/data/textos.json`:
  - `story.ch4.e1.title` = "La llave prestada"
  - `story.ch4.e1.beat` = prosa del CAP 04-troncales E1 (sesión prestada, troncal, faro)
  - `story.ch4.e1.brief` / `story.ch4.e1.briefing` = instrucción con rutas absolutas: `cat /etc/hosts` descubre, `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` (y `faro:` alternativo), nota de 2-3 hosts, comentario `#`.
  - `story.ch4.e1.hint_1`, `hint_2`, `detail` (rutas absolutas, límite 2 pipes, no 4 eslabones)
  - `concept.scp.summary` breve
  - Validar `json.tool` OK, `load_textos` resuelve todas.
- HECHO SI: `resolve("story.ch4.e1.title")` y brief contienen `/etc/hosts` y `troncal-01` y `volcado.csv` con rutas absolutas.

### Hito 4 — Tests golden + gate (rutas sandbox)
- QUÉ: crear `src/tests/core/curriculum/test_ch4_e1_llave_prestada.py` (o `src/tests/core/sandbox/test_ch4_scp.py`) con:
  - `test_gate_25_quests_23_concepts_dag_valido` — carga curriculum, asserts 23/25, e1 presente, prereq scp==[c.cut], cut chapter 4 prereq wc, dato2/dato3/e1/e2 intactos, DAG válido.
  - `test_golden_scp_troncal_via_generate_or_handmade` — intenta `generate(42,4)` + `new_session`; si ImportError/No chapter4, construye FS handmade (local hosts 2 entradas + remoto troncal con volcado.csv), usa `Shell` con `DEFAULT_CH4_COMMANDS` si existe sino tuple manual, ejecuta `cat /etc/hosts` (exit 0, descubre), `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` (exit 0, ruido 3, fichero en /tmp/volcado.csv), verifica contenido `TR-001`, respeta límite 2 pipes (ejecuta `cat /etc/hosts | grep troncal` 1 pipe OK, `cat volcado.csv | grep OK` 1 pipe, nunca 4 eslabones).
  - `test_discovery_leer_descubre_listar_no` — `ls /etc` no descubre, `cat /etc/hosts` sí.
  - Tests usan rutas absolutas, no tocan `generator/chapter4.py` ni `shell.py`.
- HECHO SI: `pytest src/tests/core/curriculum/test_ch4_e1*.py -q` 3/3 verde contra FS handmade; contra `generate(42,4)` también verde cuando O1 mergeado.

### Hito 5 — Actualizar test_loader y verificar suite completa
- QUÉ: parchear `src/tests/core/curriculum/test_loader.py` expectativa 22→23 y 24→25 (comentario con fecha 09/09).
- Ejecutar `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` en rama realineada, comprobar delta +N (mín +2, esperado +3), `generate(42,6)` intacto byte-idéntico (no tocado).
- HECHO SI: suite 635→638 (o 639) sin fallos, deltas declarados verificables.

### Hito 6 — Documentación + PR
- QUÉ: README del módulo (`src/core/curriculum/README.md` o `src/core/sandbox/README.md`) nota ch4, worklog `docs/worklog/2026/09/09.md` entrada Smough con QUÉ/POR QUÉ/ENTREGABLE/RELEVO + hitos, `backlog/tareas/en-curso/activo.md` marcar `[HECHO] PR #xx`, push rama, `gh pr create` a main con cuerpo `tests antes: 635 · tests rama: 638 · delta esperado: +3`.
- HECHO SI: PR abierto, identidad Smough solo, `git status` limpio salvo rutas permitidas.

## Orden y delegación
- Hitos 2 y 3 son independientes tras Hito 1 → delegables en paralelo con contexto completo.
- Hito 4 depende de 2+3.
- Hito 5 depende de 4.
- No tocar `src/core/generator/*`, `src/core/sandbox/shell.py`, `curriculum.json` fuera de lo descrito.

## Riesgos
- `cut` movido a 4 con prereq `wc` cambia cadena pedagógica sort→uniq→cut; aceptado por DAG y narrativamente cut es delimitador `|` necesario en troncal (TRONCAL_HEADER pipe). Documentar como deuda mínima.
- Si O1 no está mergeado, test handmade debe declarar costura exacta (nombres TRONCAL_PATH, HOSTS_PATH).
