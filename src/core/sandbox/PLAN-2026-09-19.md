# PLAN-2026-09-19 — Smough S1: allowlists ch5 e1/e3/e4 + chmod/chown física mínima

**Tarea Gwyndolin 19/09 S1 — `feat/sandbox-2026-09-19`**
Base: 760/0, gate 24/31, bundle 48. ALLOWLIST OWNER: Smough. GATE OWNER: Ornstein. Bundle: solo Seath.

## Objetivo
Pattern `DEFAULT_CH4E3_COMMANDS` (PR #57) llevado a ch5: 3 allowlists per-encargo con los verbos que exige su quest/historia (e1 ls,ps,chmod,kill; e3 ps,env,kill; e4 chmod,chown,tail,ls). Base (cat,scp) intacta para e2; fuera del encargo → 127 frontera honesta. Sin tocar session.py/curriculum/web.

## Hitos (secuenciales, ~1h cada)

### H1 — Constantes allowlist en shell.py
- Definir `DEFAULT_CH5E1_COMMANDS = ("cat","chmod","kill","ls","ps","scp")` (base+ls,ps,chmod,kill)
- `DEFAULT_CH5E3_COMMANDS = ("cat","env","kill","ps","scp")`
- `DEFAULT_CH5E4_COMMANDS = ("cat","chmod","chown","ls","scp","tail")`
- Orden lexicográfico, tupla, comentario S1 19/09. Verificación: `grep DEFAULT_CH5 shell.py`.

### H2 — Física mínima chmod/chown
- Nuevo `src/core/sandbox/commands/permisos.py` con `chmod MODE FILE...` y `chown OWNER[:GROUP] FILE...`
- Octal 3-4 dígitos + symbolic +x/-x alias; chown owner regex; ambos mutan node.mode/owner/group + mtime; errores GNU (missing, invalid mode/owner, No such file) exit 1; ruido 1.
- `noise.py`: añadir `"chmod":1,"chown":1`.
- `shell.py`: import PERMISOS_SPECS, añadir a SPECS_ALL.
- Test manual: `Shell(commands=E4).execute("chmod 600 /tmp/a.txt")` → mode 600; `chown gris:apagados` → owner gris.

### H3 — Frontera honesta + golden e3
- Test `test_ch5_allowlist.py` 9 tests:
  - base intacta (`=={"cat","scp"}`) y supertests `<= set(E*)`
  - frontera base vs E1/E3/E4: base →127, per-encargo →0
  - golden E3: `ps aux` muestra `censo intruso --vigilar-censo 03:14`; `kill -HUP 522` → env HUP_522=1 y sigue en ps; `kill -9 522` → desaparece
  - chmod/chown mutación real
- Criterio: suite 760→768+ (+9), gate 24/31 intacto, bundle stale honesto.

### H4 — Guardias y regresión
- Actualizar `test_noise.py` y `test_shell.py` para incluir chmod/chown en sets esperados (forma `<= set`).
- Verificar suite headless: `.venv/bin/python -m pytest src -o addopts= -q` → 768 passed +1 bundle stale (honesto, solo Seath regenera regla 12/09).

## Interfaces
- `shell.py`: 3 tuplas nuevas exportadas.
- `commands/permisos.py`: specs `CHMOD_SPEC`,`CHOWN_SPEC` con concepts `chmod`/`chown`.
- `noise.py`: perfil extendido.

## Edge cases
- chmod sin file → missing operand after; modo inválido → invalid mode; file no existe → cannot access; dir → válido (cambia mode del dir)
- chown sin file → missing; owner inválido → invalid user; ambos con múltiples files → errores acumulados, exit 1 si alguno falla
- Allowlist: fuera de encargo → 127 nunca 1; dentro → ejecuta aunque falle por FS (exit 1 no 127)

## Orden
H1 → H2 → H3 → H4. H2 delegable (flash sub-agent), H3 verificación manual.
