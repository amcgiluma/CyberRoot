# PLAN — Smough 16:00 · 13/09/2026 · S2 dato6 «La segunda purga»

## Objetivo
Quest `story.ch6.dato6` coma-trampa PR-0092 como dato (segunda huérfana), reusa mundo dato4, sin tocar generator.py/chapter6.py, gate flexible 28→29, bundle regen.

## Hitos concretos (cada ~1h, verificable)

### Hito 1 — curriculum.json (QUÉ: añadir quest, criterios HECHO)
- Leer src/data/curriculum.json (24c/28q) y validar DAG (prereqs existen, sin ciclos).
- Añadir quest `story.ch6.dato6` chapter 6 tint grey requires ["c.join"] title_key story.ch6.dato6.title beat_key story.ch6.dato6.beat (idéntico a dato4 requisitos, hermano; no toca DAG viejo, no crea concepto nuevo).
- Verificar: `python -c "load_curriculum(); len(quests)==29 and concepts==24"` y `validate()` no lanza; `python -m pytest src/tests/architecture -q` no rompe.
- Edge: ensure id único, no duplicar, orden estable (append tras dato5).
- HECHO cuando: curriculum 24/29, DAG válido, suite parcial 698→699 conceptual.

### Hito 2 — textos.json (prefijo disjunto)
- Añadir 6 claves prefijo `story.ch6.dato6.*`: title, beat, briefing, hint_1, hint_2, detail.
- Contenido: briefing nombra coma `,` como separador que rompe, golden `join -t'|' -1 3 -2 1 -v 1 /srv/camara-faro/purgas.csv /srv/camara-faro/registro.csv | grep 000483` → 1 línea PR-0092, sin PR-0091, variante bonus `cut -d'|' -f3 purgas.csv | grep 000483`. Mencionar PR-0091=000 ENSAYO vs PR-0092=000483 coma real, rutas absolutas, filtro positivo.
- Verificar: `python -m json.tool` sin errores, `grep story.ch6.dato6` 6 hits, anti-slop `grep -nEi "testamento|..."` 0 hits, prefijo disjunto de postmortem.* y otros story.*.
- HECHO cuando: textos  +6 claves, JSON válido, briefing menciona `,`.

### Hito 3 — gate flexible (dueño exclusivo Smough)
- Editar src/tests/core/state/test_ch6_datos_circuit.py: ampliar assert flexible de `[(23,25),(24,27),(24,28)]` a incluir `(24,29)` (pattern 28↔29). No tocar allowlist ni otros gates.
- Verificar: load_curriculum ahora 24/29 pasa, suite sigue verde; ningún otro fichero tocado.
- Edge: mantener compatibilidad con main 24/28 si se ejecuta en main (por eso lista incluye 28).
- HECHO cuando: gate acepta 24/28 y 24/29.

### Hito 4 — test propio test_ch6_dato6_circuit.py (dueño de SU fichero)
- Crear src/tests/core/state/test_ch6_dato6_circuit.py con helpers handmade idénticos a test_ch6_datos_circuit (PURGAS_CONTENT/PR-0092 coma-trampa, REGISTRO_CONTENT, Fs handmade).
- Tests (3-5):
  1) golden_join_grep_000483: `join -t'|' -1 3 -2 1 -v 1 purgas registro | grep 000483` exit0, stdout contiene "PR-0092" y "000483|PR-0092" y NO contiene "PR-0091", 1 línea.
  2) variante_cut_grep_000483: `cut -d'|' -f3 purgas.csv | grep 000483` exit0 → "000483".
  3) leccion_filtro_positivo: join sin grep da 2 huérfanas (PR-0091+PR-0092) y `| grep 000` da 2 líneas, `| grep 000483` da 1.
  4) determinismo seed 42×2 idéntico (si generator disponible, fallback si no).
  5) briefing_nombra_coma: verifica textos contiene ',' y "EN BLANCO, revisado".
- Cada test usa Shell + DEFAULT_CH6_COMMANDS, fallback a _run_join/_run_cut si shell 127 (join no expuesto), declara fallback honesto si generator exige branch (no tocar generator.py).
- Verificar: pytest -q nuevos 4 passed, suite total 698→702/703.
- Edge: joined output format "000483|PR-0092|11-07|UMBRAL-BAJO|EN BLANCO, revisado..." incluye coma cruda, no rota; grep 000483 no confunde 000.
- HECHO cuando: +4 tests verde, golden exit0 con PR-0092 sin PR-0091.

### Hito 5 — bundle + verificación final + entrega
- Regen bundle: `python tools/web/build_bundle.py` (solo rama data tocada).
- Ejecutar suite completa `PYTHONPATH=src .venv/bin/python -m pytest -q` contar passed (esperado 698+4=702 +1 gate =703? Real: O1 ya hizo +5 → 703 en engine branch pero main sigue 698; en sandbox rama esperamos 698→702/703). Guardar "tests antes: 698 · tests rama: M · delta: +K" para PR.
- Verificar determinismo: `generate(42,6)` ×2 byte-idéntico si disponible, o FS handmade idéntico.
- Preparar rama `feat/sandbox-2026-09-13` realineada (ya lo está, 0 behind), configurar git identity Smough antes de cada commit, push, abrir PR con delta declarado, marcar [HECHO] en activo.md, escribir worklog y README.
- HECHO cuando: bundle 48 ficheros fresco verde (o 47 si no cambia contenido bundle? curriculum es data, bundle debe cambiar), PR abierto, activo.md marcado.

## Interfaces / nombres
- curriculum.json: Quest(id="story.ch6.dato6", chapter=6, tint="grey", requires=["c.join"], title_key="story.ch6.dato6.title", beat_key="story.ch6.dato6.beat")
- textos.json claves: story.ch6.dato6.title/.beat/.briefing/.hint_1/.hint_2/.detail
- test file: _handmade_ch6_fs() -> FileSystem con PURGAS_CONTENT+REGISTRO_CONTENT+processes dato5, _shell_ch6() helper, _has_join() guard.

## Orden secuencial
1→2→3→4→5 (cada hito verifica antes del siguiente). No paralelizar 1/2 (comparten data), 3 depende de 1, 4 depende de 1/2.

## Qué NO tocar
- generator.py / chapter6.py / shell.py / web/ / postmortem.py / allowlist DEFAULT_* / test de otro dueño (solo gate flexible propio).
