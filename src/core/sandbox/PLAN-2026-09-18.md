# PLAN Smough 18/09 — allowlist ch5 + huella custodia (S1)

## Contexto
Plan Gwyndolin 18/09 (S1 P2): crear la lente física del cap.5 Subestación.
- Ornstein (O1) ya expone SUPPORTED_CHAPTERS {0,2,4,5} y _commands_for(5)=("cat","scp") con fallback.
- Mi pieza aporta la constante NOVA DEFAULT_CH5_COMMANDS y el detector del post-mortem para la custodia.

## Hitos (secuenciales, ~1h cada)

### H1 — shell.py: constante nova DEFAULT_CH5_COMMANDS
- **Qué:** Añadir `DEFAULT_CH5_COMMANDS: tuple[str,...] = ("cat","scp")` junto a DEFAULT_CH4* existentes, sin tocar ninguna allowlist previa. Documentar con comentario S1 18/09.
- **Dónde:** `src/core/sandbox/shell.py` líneas 78-85 (tras DEFAULT_CH4E3_COMMANDS).
- **Hecho si:** `from core.sandbox.shell import DEFAULT_CH5_COMMANDS` devuelve ("cat","scp"); `DEFAULT_CH4_COMMANDS` y `DEFAULT_CH4E3_COMMANDS` siguen byte-idénticos; tests allowlist previos pasan.
- **Riesgo:** Ningún assert previo toca esta constante → OWNER Smough, no colisiona.

### H2 — src/data/textos.json: clave postmortem.auditor.custodia
- **Qué:** Añadir clave `"postmortem.auditor.custodia": "Expediente 000: custodia leída en casa: el testigo viajó. Continuidad del ensayo: estable."` disjunta de `postmortem.espejo.*` y `postmortem.volcado.*`.
- **Dónde:** `src/data/textos.json` junto a postmortem.volcado.* (orden alfabético cercano).
- **Hecho si:** `from data.textos import resolve; resolve("postmortem.auditor.custodia",{})` devuelve el texto exacto; JSON válido; no se rompe ninguna clave previa.
- **Edge:** Prefijo `postmortem.auditor.custodia` no colisiona; verificar que no existe ya (grep).

### H3 — postmortem.py: detector _has_volcado_custodia + integración en build_postmortem
- **Qué:** 
  - Definir `LINE_KEY_CUSTODIA = "postmortem.auditor.custodia"`
  - Función `_has_volcado_custodia(shell_dict) -> bool`: True si history contiene línea con `cat` y `/tmp/volcado-custodia.csv` y exit_code==0. Hermano de `_has_volcado_rescate` (misma rigurosidad: shlex split verifica argv[0]=="cat", pero fallback substring válido para historia real).
  - En `build_postmortem`, tras bloque volcado, añadir: si _has_volcado_custodia → resolver texto, `base["auditor_custodia"]={line_key,args:{}}`, `base["auditor_custodia_text"]=texto`, `base["lines_resolved"].append(texto)`. Sin cat → informe byte-idéntico a hoy (no añade clave).
  - No tocar lógica espejo/volcado existente; custodia independiente (pueden coexistir 2-3 líneas espejo+volcado+custodia).
- **Hecho si:** tests del plan pasan (ver H4), y byte-idéntico sin gesto se mantiene.
- **Edge cases:** cat con ruta relativa debe resolver? No: spec dice literal `/tmp/volcado-custodia.csv` en history line substring, igual que _has_volcado_rescate usa "scp"+"volcado-rescate.csv" substring. Usaremos substring + exit 0 + opcional shlex verificación (si shlex falla, fallback True). También cubrir `cat /tmp/volcado-custodia.csv` en pipe (`cat ... | grep`): la línea contiene cat y ruta → dispara (coherente con _has_espejo_volcado que busca cut+grep en misma línea).
- **Test de regresión:** sin cat → "auditor_custodia" not in inf; con cat exit 1 → no dispara; determinismo.

### H4 — Tests src/tests/core/sandbox/test_ch5_custodia_pm.py (3–4 tests)
- **Qué:** Crear fichero con:
  1. `test_custodia_detectada`: shell_dict con cat custodia exit 0 → `auditor_custodia` presente, line_key correcta, texto resuelto, lines_resolved len 2 (pico+custodia) o >=2 si hay corte/orden, y determinismo.
  2. `test_sin_custodia_byte_identico`: history solo cat genérico o cut → auditor_custodia ausente, byte-idéntico a build sin custodia (no new keys).
  3. `test_custodia_falla_exit1_no_dispara`: cat custodia exit 1 → no custodia.
  4. `test_custodia_determinismo`: doble build mismo dict → ==.
- **Hecho si:** pytest src/tests/core/sandbox/test_ch5_custodia_pm.py -v pasa 3-4; suite global 749→752-756.

### H5 — Verificación + bundle + PR
- **Qué:** 
  - `pytest -q` (suite global) → 749 antes (base realineada), 752-756 después.
  - `python tools/web/build_bundle.py` (toca textos.json) → bundle 48 fresco verde.
  - Guard identidad y rama realineada verificados antes de push.
  - Commit con `git -c user.name="Smough" -c user.email="smough@cyberroot" commit`
  - Push + `gh pr create` a main con body incluyendo tests antes/rama/delta.
- **Hecho si:** PR abierto, worklog y activo.md marcados [HECHO].

## Orden
H1 → H2 → H3 → H4 → H5. H1 y H2 independientes pero H3 depende de ambos. H4 depende de H3.

## Verificación por hito
Cada hito: leer fichero, ejecutar snippet python -c import, pytest del fichero afectado.
