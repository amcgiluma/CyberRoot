# PLAN S2 14/09 — Smough · dato6 variante bonus → requirement + hint precisión

## Objetivo (plan Gwyndolin 14/09 S2)
Quest `story.ch6.dato6` acepta 2 goldens: `join|grep 000483` y variante `cut -d'|' -f3 | grep 000483`. hint_2 trampa `grep 000` vs `grep 000483` ("¿no toda huérfana es fantasma?"). Gate 24/29 intacto (variante, no quest nueva). ALLOWLIST OWNER: NADIE. GATE OWNER: Smough.

## Hitos (secuenciales, ~1h c/u)

### Hito 1 — Textos (`src/data/textos.json`) — criterio hecho: briefing nombra ambas goldens como válidas, hint_2 = trampa filtro positivo
- Qué: editar 3 claves `story.ch6.dato6.*`:
  - `briefing`: cambiar "Variante bonus: cut ..." → "Variante: cut ... — ambas válidas. Golden ..." + mantener lección filtro positivo y coma-trampa. Debe contener ambas cadenas exactas `join -t'|' -1 3 -2 1 -v 1` + `cut -d'|' -f3 /srv/camara-faro/purgas.csv | grep 000483` y `grep 000` vs `grep 000483`.
  - `hint_2`: reemplazar bonus por `prueba | grep 000 y luego | grep 000483 — compara: ¿no toda huérfana es fantasma?` (idea 14/09, texto exacto del plan).
  - `detail`: actualizar para decir "join|grep 000483 → PR-0092 y cut|grep 000483 → 000483, ambas válidas" + coma-trampa.
- Interfaces: ninguna API Python, solo JSON. No tocar `curriculum.json` (gate 24/29).
- Edge: briefing/detail deben seguir conteniendo `EN BLANCO, revisado` y `,` y `-d'|'` para que test coma siga verde. No añadir comandos nuevos.
- Test: `test_ch6_dato6_briefing_nombra_coma` seguirá verde; nuevo test de hint_2 verificará substring.

### Hito 2 — Tests (`src/tests/core/state/test_ch6_dato6_circuit.py`) — criterio hecho: 3 tests nuevos/actualizados verifican variante como requirement
- Qué:
  - Actualizar `test_ch6_dato6_variante_cut_grep_000483` docstring/comentario: de "bonus" → "variante requirement" (sigue exit 0, 1 línea).
  - Actualizar `test_ch6_dato6_leccion_filtro_positivo` para que siga verificando 2 vs 1 huérfana.
  - Añadir o actualizar `test_ch6_dato6_hint_2_trampa_filtro` que lee `textos.json` y assert `grep 000` y `grep 000483` y `huérfana es fantasma` en hint_2.
  - Añadir `test_ch6_dato6_ambas_variantes_validas` que ejecuta ambas goldens y assert ambas exit 0 (ya existe implícito, reforzar como requirement).
  - Mantener determinismo 42×2.
- No tocar `test_ch6_datos_circuit.py` salvo si gate flexible necesita patrón `<=` — hoy no (24/29 ya está en lista).
- Verificación: `PYTHONPATH=src .venv/bin/python -m pytest src/tests/core/state/test_ch6_dato6_circuit.py -q` verde, + `src/tests/core/state/test_ch6_datos_circuit.py` verde.

### Hito 3 — Suite, gate, bundle, PR — criterio hecho: suite 708→711-713, gate 24/29, bundle fresco, PR abierto
- Qué: `PYTHONPATH=src .venv/bin/python -m pytest -q` → 708 base +3-5 = 711-713 passed. Verificar `load_curriculum()` 24/29. `python tools/web/build_bundle.py` no necesario (no tocamos src/data que genere bundle? sí tocamos textos.json → regen canónico SOLO si Gwyn lo hace, nosotros NO regeneramos web/bundle per AGENTES.md §OWNERSHIP BUNDLE: solo quien toca src/data regenera; nosotros sí tocamos textos.json → debemos regenerar? Regla: Solo ejecutor cuya rama toca src/data regenera bundle EN SU RAMA. Nosotros tocamos textos.json → sí regeneramos). Pero plan dice "Solo O1 y S2 rozan textos.json → regen canónico al cierre (una única vez, tras el último merge; guardián como red)." + §OWNERSHIP BUNDLE dice solo quien toca src/data regenera. Contradicción con "una única vez al cierre". Decisión: NO regenerar en rama S2 si O1 ya tocó textos.json y habrá doble regen — dejar al cierre de Gwyn. Pero para guardián verde, regenerar localmente y verificar `test_bundle_fresco` pasa sin commit? Evaluar: si tests pasan sin regen, no regen. Si fallan, regen.
- Luego: `git add` solo rutas permitidas (`src/data/textos.json`, `src/tests/core/state/test_ch6_dato6_circuit.py`, docs), commit como Smough, push, `gh pr create`, editar activo.md + worklog.
- Entregable: PR `feat/sandbox-2026-09-14` con delta declarado, tests antes:708 tests rama:711-713.

Orden: H1 → H2 → H3. Cada hito verifica antes de pasar al siguiente.
