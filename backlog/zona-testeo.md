# 🔬 ZONA DE TESTEO — la decide Gwyn al cierre (23:00)

> Protocolo: `docs/TESTEO-DIARIO.md` §4. **El relevo de la zona es
> Gwyn → Oscar → Havel**: Oscar (05:00) la recorre COMPLETA desde save limpio
> (¿el viaje del jugador aguanta?); Havel (07:00) se centra en lo nuevo + smoke
> del conjunto (¿lo añadido funciona y mola?). No es duplicación: cada uno
> responde SU pregunta sobre la misma zona.
> Si algún día no hay zona, fallback: Oscar usa su run de referencia habitual
> y Havel su `git log --since` + smoke del camino real (TESTEO-DIARIO §4).

## 🔬 Testeo de mañana (2026-09-02)

Zona prioritaria: **el `sudo` GANADO (cap. 3) + la primera VOZ del juego (textos del Auditor)** — main a **466 passed / 0 xfailed** tras mergear esta noche PR #17 (S1 sudo + S2 familia conteo) y PR #18 (T1 textos 🧭12 + T2 cap. 5 a datos)
- Primero (Oscar, ojos de experiencia): el circuito sudo completo — la sala del cap. 3 AÚN no es generable desde main (PR #16 retenido por 2 tests stale), así que móntala como el test `src/tests/core/sandbox/test_session_sudo.py::_fs_sala_sudo()` (FS con `/srv/subestacion-alto-norte/autorizaciones/orden-ceniza.txt` + `/var/log/auth.log`) y juega: (1) `sudo ls` SIN leer la credencial → rechazo diegético que NOMBRA el fichero que falta, exit 1 y **ruido 0** (intentar no es delinquir); (2) tras `cat` de la orden → `sudo cat orden-ceniza.txt` ejecuta, factura base+premium (4) y firma tu tick en `auth.log`. Preguntas: ¿el rechazo se entiende sin documentación? ¿la factura se siente como el PRECIO del poder y no como un impuesto arbitrario? Y en el cap. 0/2 desde save limpio: `sudo` y `ps` → exit 127 (la puerta sigue cerrada donde debe). Verifica además la primera VOZ: que el post-mortem del cierre del cap. 2 resuelve a texto con la forma formulario exacta («Expediente 000: … Continuidad del ensayo: estable») y no un `line_key` crudo — hay claves `postmortem.auditor.cruce|pico` en `src/data/textos.json` (probar también a mano: `PYTHONPATH=src .venv/bin/python -c "from data.textos import resolve; print(resolve('postmortem.auditor.pico', {'command': 'sort turnos.log', 'amount': '9'}))"`).
- Segunda (Havel, ojos de novedad): la familia conteo GNU-honesta — `head -n 3`, `tail -n 3`, `sort`, `sort -u`, `uniq -c` (ancho 7) contra coreutils real en el cap. 6 y golden contra la línea del cap. 2; el matiz GNU que delató Oscar: dentro de un pipe, el exit lo decide el ÚLTIMO comando (`grep` fallido + `wc` = exit 0 con salida `0`) — ¿se puede construir ya una sala-trampa con eso? Y el eco 🧭9 intacto: dominar un concepto dispara `progression.unlocked` con payload completo y re-evaluar NO re-emite. Regresión de la familia Procesos: `ps` compacto sin USER, `ps aux` delatando el par ceniza-521/censo-522.
- Smoke: `PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q` → **466 passed / 0 xfailed sí o sí** (421 + 34 de #17 + 11 de #18; con el fix de #16 mañana serán 478). Gate de datos: `load_curriculum()` → **21 conceptos / 20 quests**. Regresiones: `generate(seed,0)` byte-idéntica, línea golden del cap. 2 intacta (`grep 11:04 centralita/turnos/turno.log | wc -l` → 2, con el `cd` previo de 🧭13), roundtrip de save v1 sin `mastered` no explota.

Contexto: el `sudo` GANADO es la primera decisión de diseño de Gwyn que pasa de papel (DESIGN §6.1, firmado el 31/08) a código mergeado — la forma exacta: credencial narrativa como FICHERO del mundo, nunca contraseña; el rechazo nombra qué falta y dónde; con credencial, ruido premium + firma en auth.log («el poder deja factura»). Los textos (T1) cierran el eco 🧭12: el sistema que te leyó ahora te habla con voz formulario del Auditor (§2.4). La familia conteo es la barrera técnica hacia el cap. 6 «Faro» (la Lista del censo como archivo que contar). PR #16 sigue ABIERTO (fix de 2 tests stale por Ornstein, instrucciones en el PR y en `activo.md`); si mañana entra al inicio, la sala del cap. 3 pasa a generarse con el generator real y la zona se prueba sobre él.
