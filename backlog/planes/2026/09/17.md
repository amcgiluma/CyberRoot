# PLAN — CyberRoot (jueves 17/09/2026)

**Gwyndolin, Dark Sun** — planificador 11:00.

Base verificada (notas-mañana 17/09 + activo.md): suite **738 + 1 stale →
739 tras regen canónico de Gwyn en su cierre 16/09**, gate de datos
**24 conceptos / 31 quests** (`story.ch6.dato7` la última), bundle
**47 ficheros (~426 KiB)**. Ramas/PRs: **PR #61
(`feat/meta-ui-2026-09-16`, tooltip N/30 + lente rescate Faro) sigue
ABIERTO, sin veredicto de Artorias** — rama huérfana de ayer; su dueño
es Seath. Sin otras ramas ni PRs abiertos.

DIRECCIÓN del día (Gwyn/Oscar convergen): la cadena TR-003 está cerrada
(e3 jugable + dato7 verde). La pieza con más ROI es **cap. 5 «Subestación»
que LEA el testigo** con verbos ya dominados: el fichero `volcado-rescate.csv`
existe o no según la bifurcación de hace 2 capítulos — la primera vez que una
decisión cambia la geografía de OTRO capítulo. Sin comandos ni huellas nuevas
(dirección explícita de Oscar 17/09).

- **ALLOWLIST OWNER: NADIE** — nadie toca asserts de
  `DEFAULT_CH4*` / `DEFAULT_CH4E3_COMMANDS` / `DEFAULT_CH6_COMMANDS`.
- **GATE OWNER: Smough** (gate flexible de curriculum/loader, forma `<=`).
- **Bundle:** Gwyn ya regeneró canónicamente (739). Hoy solo regenera quien
  toque `src/data/` (regla 12/09).

---

## Asignaciones (Ornstein 13:00 · Smough 16:00 · Seath 19:00)

### T1 — Seath (19:00) · rama `feat/meta-ui-2026-09-16` · PR #61: CIERRE de la rama huérfana (PRIORIDAD 1)

**Módulo:** `web/app.js` SOLO. NO toca `src/`, NO regenera bundle.

**Qué hace (criterios):**
1. Verifica que el PR #61 sigue aplicable sobre la main de hoy (era viable
   sobre main con #59 ya mergeado: solo toca `web/app.js` — sin conflicto
   esperado; si hay conflicto, lo resuelve ÉL porque es su rama).
2. Cuerpo del PR honesto: tests antes/rama = 739, delta **0**
   (solo `web/app.js`), `node --check` OK, consola limpia,
   `hideTroncal*-restart` intacto, `TRONCAL_STATIC` intocada,
   tarjetas `?chapter=4` y `?chapter=6` intactas.
3. Marca `[HECHO] (+PR #61)` en su línea del `activo.md`.

**Riesgo:** si Artorias ya no confía en la rama (💥), Gwyn decide merge
parcial o descarte documentado — Seath NO reescribe la lente de nuevo, solo
la re-verifica. Si a las 19:00 la rama sigue sin poder cerrarse, su franja
muere sin trabajo adicional y Artorias lo marcará.

### O1 — Ornstein (13:00) · `feat/engine-2026-09-17` · cap. 5 pieza 1: FS del asalto con testigo condicional (PR #62)

**Módulo:** `src/core/generator/` (nuevo `chapter5.py` + conector en
`generator.py`) + su `README.md`.

**Qué hace (criterios):**
- `build_chapter5_fs(fs_rng, volcado_rescatado: bool)` — FS de la
  Subestación con: (a) proceso `intruso --vigilar-censo` con USER censo y
  `START 03:14` (misma hora del volcado — la firma que ya midió Oscar);
  (b) el testigo como FICHERO: si rescate, el FS conocido muestra
  `/tmp/volcado-custodia.csv` como COPIA LOCAL legible con
  `TR-003|faro|troncal-01|512|EN_COLA`; si caducado, NO existe
  (`cat` → `No such file` — la ausencia HONESTA es el detector).
- Conector `_generate_cap5` en `generator.py` con la MISMA firma
  `volcado_rescatado=False` que ya usa dato7. NO toca `session.py`
  (cap. 5 queda fuera de `SUPPORTED_CHAPTERS` HOY — sesión del asalto
  para mañana si el día sale bien).
- 8 tests en `src/tests/core/generator/test_chapter5_fs.py`:
  FS con/sin testigo byte-deterministas ×2 seeds, `ps aux` con
  `START 03:14`, ausencia→`No such file`, no-regresión (7 goldens
  actuales byte-idénticos incl. dato7 rescue/absence).
- **Criterios numéricos:** suite 739 → **≥ 745** (delta declarado),
  0 xfailed, determinismo verificado.

**No toca:** `curriculum.json`/`textos.json` (dueño Smough), `session.py`,
`shell.py`, `web/`, `postmortem.*` (intocados).

### S2 — Smough (16:00) · `feat/sandbox-2026-09-17` · quest `story.ch5.e2` «El testigo que no llegó» (PR #63)

**Módulo:** `src/data/{curriculum,textos}.json` + tests de curriculum
(GATE OWNER: Smough).

**Qué hace (criterios):**
- Quest `story.ch5.e2` **grey**, requires `['c.cat', 'c.scp']` (c.cat ya
  existe cap. 1; c.scp heredado ch4 — sin concepto nuevo, DAG válido).
- Golden canónico: `cat /srv/camara-faro/volcado-rescate.csv` → exit 0 con
  `TR-003|...|EN_COLA` si rescate (el fichero existe porque el FS lo
  planta dato7 — la quest NO depende del FS del cap.5: se redacta con
  FLEXIBILIDAD si O1 no entrega).
- Si caducado → `No such file` y el briefing lo DOCUMENTA como pista
  (prosa verde de Oscar: «si lo subiste, la prueba viaja; si lo borraste
  o tardaste, la hora amanece sin papel»). Prosa de Manus en
  `05-subestacion.md` intacta; textos prefijo `story.ch5.e2.*` disjunto.
- **GATE OWNER:** `load_curriculum()` flexible 24/31 → **24/32**,
  forma `<=` (los demás gates FLEXIBLES intactos — nadie más los toca).
- Suite en su rama 739 → **≥ 741** (+2: loader + presencia/ausencia),
  afirmada sobre main del momento (si O1 ya mergeó, re-basa +2 → numero
  final 747 — delta NO acumulable en el PR: declarar base).
- **Regen bundle en SU rama** (única que toca `src/data/`):
  `python tools/web/build_bundle.py` → 47 ficheros, guardián
  `test_bundle_fresco` verde.

**No toca:** allowlists (OWNER NADIE), `chapter5.py`/`generator.py`
(dueño O1), `session.py`, `web/`.

---

## Costuras y ownership (17/09)

| Costura | Dueño | Nota |
|---|---|---|
| ALLOWLIST (`DEFAULT_CH4*`, `DEFAULT_CH6*`) | **NADIE** | cero asserts tocados |
| GATE flexible curriculum/loader | **Smough** | 24/31 → 24/32, forma `<=` |
| Bundle `web/bundle/core.json` | **Smough** (único en tocar `src/data/`) | regen en su rama |
| `chapter5.py` / `generator.py` | **Ornstein** | Smough/Seath no entran |
| `web/app.js` | **Seath** (PR #61) | nadie más toca `web/` |
| `postmortem.*` / pack `POSTMORTEM.md` | NADIE | intocados |

## Gestión de riesgo

- **O1 no entrega** (precedente 11/09): S2 es FLEXIBLE (quest sobre datos
  ya existentes, no sobre el FS nuevo) y T1 no depende — el día no cae
  entero. Artorias marcará 💥 y Gwyn reposiciona mañana.
- **PR #61 conflicto tras #59/#60:** Seath lo resuelve ÉL (su rama); si no
  cabe, confesión en el PR y descarte documentado en `activo.md`.
- Si Artorias detiene un PR (💥) esta noche: Gwyn decide merge parcial — el
  día conserva los otros dos sin cascada sobre el roto (precedente 14/09).

## Recámara (NO agendar hoy, vigilar)

- `TRONCAL_STATIC` duplicada (5ª noche) — Seath la respeta; despierta solo
  el día que Manus toque el volcado para narrativa.
- 🧭24/25/26/27, `grep -v`/`-i`, karma 521/522, karma volcado (azul/rojo sin
  popup), `shred`/`stat`/`tr`, logros de recámara — sin dueño, intactas.
