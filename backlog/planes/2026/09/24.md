# PLAN — 24/09 (Gwyndolin, Dark Sun)

## Base verificada (11:00)
- main post-cierre 23/09 (PR #75/#76/#77 mergeados por Gwyn en orden engine→sandbox→meta-ui): **suite 809 passed / 0 failed**, gate **25 conceptos / 31 quests**, bundle **50 ficheros (484.0 KiB)** fresco, `TRONCAL/CUSTODIA_STATIC` intactas.
- `git branch -a`: solo `main`. `gh pr list`: **0 PRs abiertos**. `git status`: limpio. Sin ramas ni PRs huérfanos.
- Notas de mañana: 🧭47 NUEVO P3 (calibración micro-karma a 20 runs); 🧭48 PERSISTE (decisión de diseño P3, no deuda); Oscar veredicto APTO + 4 «no tocar» validadas por Gwyn; Artorias verde completo.
- Trade de visas/díptico: la Subestación 4/4 de huellas moral (E1 chmod + E3 kill + E4 chown) — la integración va ``por necesidad``, LECTURAS de único día que pueden romper 800 tests si las deja un turno.

## Higiene del backlog (11:00)
- `abierto.md` / `activo.md` / `hecho/2026-09.md` / `descartado/` / mejoras: sin [HECHO] ni [EN CURSO] en `abierto.md`, sin [PENDIENTE]/[BUG] en `hecho/` ni `descartado/`, sin [HECHO] vivo con veredicto fuera de archivo pendiente de Gwyn. Cruces prefijo↔carpeta consistentes.
- Sin líneas sin prioridad. Sin índice que rever (estructura intacta). INDICE.md sin cambios.
- Los residuos con etiqueta "TOMADA/ELEGIDA" se mantienen como trazabilidad histórica (precedente), no se replanifican.

## Elección del día
Respetando la prioridad como señal y el saldo de Gwyn 23:09/Artorias 21:00:
1. **[P2] E2 «grep del intruso»** (línea 1 de Gwyn) — 4/4 de huellas ya saldado; E2 es la lectura del díptico. gira con la decada del verbo en cada sala.
2. **[P3] 🧭45 calibración micro-karma** (línea 2 de Gwyn) — medir antes de escribir por prosa.
3. **[P3] lente web chown** (web P3, 5º estado del semáforo «Grid») — web puro, sin pieza core.

Descartes: pack `POSTMORTEM.md` (sigue esperando Q con Manus, SIN CAMBIO de destino — Gwyn 24/09 y 23/09 reafirmado). Recámara (`grep -v root` pipe jugable, trampas chown, logros, `?seed=`) queda para días que rindan más.

## Asignaciones — COSTURA DEL DÍA
| Ejecutor | Rama | Rutas (disjuntas) |
|---|---|---|
| O1 Ornstein 13:00 | `feat/engine-2026-09-24` | `src/core/engine/session.py`, `src/core/sandbox/shell.py`, `src/data/curriculum.json`, `src/data/textos.json`, `src/tests/` |
| S1 Smough 16:00 | `feat/sandbox-2026-09-24` | `tools/harness/` (sólo él), sin `src/` |
| T1 Seath 19:00 | `feat/meta-ui-2026-09-24` | `web/app.js`, `web/index.html` (sin core, sin data) |

- **ALLOWLIST OWNER: Ornstein** (crea la nova `DEFAULT_CH5E2_COMMANDS` en `shell.py`, forma `<= set(...)` nunca `==` exacto, base `(cat,scp)` intacta). Nadie más toca asserts de allowlists hoy.
- **GATE OWNER: Ornstein** (único que toca `curriculum.json`: añade `c.grep` a requires de `story.ch5.e2`; gate espera 25/31 → 25/32 si la nueva quest exige curación; validar con `load_curriculum`, no con grep manual).
- **Bundle: SOLO Ornstein regenera** (toca `src/data/textos.json` + `curriculum.json`); Smough NO toca `src/`, Seath NO toca data → Gwyn regenera canónico post-merge.
- Orden de merges: **engine → sandbox → meta-ui**.
- **postmortem.py: INTACTO** (no hay detectores nuevos hoy — E2 valida por canon, no por huella).

---

### T1 (Ornstein 13:00) — E2 «greveal del intruso»: `ps aux | grep censo` como lectura del díptico

**Módulos:** `src/core/engine/session.py` + `src/core/sandbox/shell.py` + `src/data/curriculum.json` + `src/data/textos.json` + `src/tests/test_session_ch5.py` (y de ser preciso `test_grep_flags.py`).

**Qué hacer:**
1. Permitir en E2 el `grep`: nueva constante `DEFAULT_CH5E2_COMMANDS = ("cat","scp","ps","grep")` en `shell.py` (forma `<= set(...)` verificada; base `('cat','scp')` de e2 INTACTA — se marca con la nueva, no se sustituye la existente: la nova añade, no reempaza).
2. Ramificar en `session.py::_commands_for(5, quest_id)`: `story.ch5.e2 → DEFAULT_CH5E2_COMMANDS`; resto de cap. 5 conserva el patrón por-encargo vigente (e1/E3/E4 ya tienen su nova — NO TOCAR sus asserts).
3. `curriculum.json`: `story.ch5.e2.rerequires` añade `c.grep` (prereq `c.cat` ya existe); `abrible` sigue honesto con `missing ['c.grep']` sin él.
4. `textos.json`: `story.ch5.e2.hint_2` (o `detail`) «`ps aux | grep censo` — el vigilante es del censo; `grep ceniza` no delata nada.» + variante `grep -i censo` (ya soportado en texto.py) como atajo veterano — SEVA, no canon obligatorio.
5. Tests: `abrible True` con prereqs completos, `missing` honesto sin `c.grep`, `grep censo` exit 0 1 línea vía `abrir_encargo`, `grep ceniza` exit 1 (motivo derecho), frontera 127 honesta (`chmod` en e2 → 127, `kill` en e2 → 127), base e2 `{'cat','scp'}` intacta.

**Criterio de aceptación:** suite 809 → **≥815** (deltas declarados por Ornstein como `tests rama: N · delta: +K`); gate 25/31 o 25/32 coherente con curriculum; determinismo ×2 seeds byte-idéntico; `CUSTODIA/TRONCAL_STATIC` intactas; bundle regenerado en rama (50 ficheros, liga de delta en el PR). **Rutas disjuntas garantizadas** (postmortem.py, generator/ y web/ intocados).

### T2 — S1 Smough (16:00, `feat/sandbox-2026-09-24`): 🧭45 calibración micro-karma (P3)

**Qué hacer (Oscar 24/09 + Gwyn validado):**
- Con `tools/harness/run_seeds.py`: corpus **20×3 pares kármicos** (20×HUP vs 20×-9, 20×600 vs 20×777, 20×gris vs 20×root) sobre e1/e3/e4 con `N=8` y §34.4 (sin๎จา del micro-karma N=8 (`§3.4`) ya vivo), y pesos por interés del stock de Gris (`§≈20`). Medir: % de runs cuya ventana `N=8` cruce `K ≥ +3` vs `K ≤ -3` por perfil, divergencia azul/rojo al cumular los tres verbos en la misma run, y el umbral de `blue:1`/`red:1` actual en 20 runs.
- Hay que llamar y entregar la MEDIDA a arte (no pesas ni prosa aún): cada síntesis por perfil con «deltas esperados si weight/blue:2» en el worklog + `notas-manana.md` 🧭45 — pesos antes que prosa (§8.6).

**Criterio de aceptación:** N=20 corre completo sin error; tabla de pesos/métricas en `docs/worklog/2026/09/24.md` + Colombia de 1 línea en `notas-manana.md` (🎯 de Smouch); NO toca `src/` ni game code, ni allowlist, ni curriculum, ni web. Delta de suite esperada: **+0** (la calibración no añade tests de core).

### T3 — Seath (19:00, `feat/meta-ui-2026-09-24`): lente web chown — 5º estado del semáforo (P3)

**Qué hacer:**
1. `web/app.js`: insignia `#ch5-e4-owner` bajo la tabla custodia — 3 estados: `operator:operator` (neutro gris), `gris:apagados` (azul `#5dade2`), `root:root` (rojo `#e74c3c`); leer `get_ls_owner()`/`get_chown_history()` (ya expushestos por `shell.to_dict`, sin ejecutar nada) — las hermanas `_getIntrusoStatus`/`_updateCustodiaPostmortem` de costura de enseñanza (Gwyn ⭐).
2. `restartSameSeed` limpia también `#ch5-e4-owner` (el semáforo debe limpiar TODOS los estados).
3. Solo si O1 aterrizó antes de las 19:00 en main: incluir `grep censo` del intruso en la insignia `#custodia-intruso` (6º estado: verde/azul/ámbar + censo-vs-ceniza) — SI NO: hueco honesto declarado, delta +0, `node --check` OK, sin bundle.
4. NO toca `src/core/` ni `src/data/` (estados byte-idénticas garantizados).

**Criterio de aceptación:** `node --check web/app.js` OK; consola limpia en `?chapter=5` en los 4 estados (sin chown / chown gris / chown root / chown gris+HUP); `CUSTODIA_STATIC` y `TRONCAL_STATIC` byte-idénticas; sin bundle (regen canónico de Gwyn post-merge).

---

## Colisiones verificadas (matriz del día, válidas para el filtro de Artorias)
- `session.py`/`shell.py`: solo O1. `tools/harness/`: solo S1. `web/`: solo T1. `curriculum.json`/`textos.json`: solo O1. Sin carpeta compartida, sin banda de actualización cruzada.
- Los 3 tocan rutas distintas entre sí → sin permite bandas de postmortem/update torturadas ni semáforos de módulos cruzados (huella moral ya dias 4/4).

## AUTO-MEJORA
- Sin fallas de proceso hoy: la higiene mañanera verde y el fichero `notas-manana` neta por líneas. No propongo cambios de prompt.

## Relevo
- Al 25/09 (Gwyndolin): O1 salda el díptico jugable, S1 deja la medida §8.6 lista para206 tipo-campos, T1 cierra el tríptico web intruso+veredicto+propietario. Recámara: trampas chown (P3), logro «Dos puertas...» y `?seed=` para cuando haya costura curable.
