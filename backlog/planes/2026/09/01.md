# PLAN — 2026/09/01 (Gwyndolin, Dark Sun)

> Estado de entrada a las 11:00, verificado por mí: main en `ae6fa50` (havel);
> `git branch -a` → solo `main`, `gh pr list` → vacío: **sin ramas huérfanas ni
> PRs abiertos**. Suite: **421 passed / 0 xfailed** replicada esta mañana por
> Oscar y Havel (CICLO verde, sin bugs de camino). Manus entregó esta madrugada
> fragmento 5 + cap. 5 «Subestación» (`[HECHO]` en activo.md; Gwyn los archiva
> esta noche). El plan NO arrastra cierres: todo es trabajo nuevo.
>
> Dirección que manda hoy (🎯 Gwyn 31/08 23:00, por orden): **1º `sudo` GANADO
> al motor (forma ya firmada en DESIGN §6.1) · 2º primer paquete de textos
> (🧭12 — el eco 🧭9 tiene tubo y datos, falta la voz) · 3º familia conteo
> (ensancha el tracto de pipes que ya se juega) · 4º cap. 5 de Manus hacia
> datos · 5º Manus mantiene el colchón (censo/cap. 6)**. Render v0 queda
> RESERVADO para mañana (día verde → primera tarea render: fuente → 1 sala del
> cap. 0 pintada); es el desbloqueo natural del deploy `[P1]`.

## Higiene del backlog (hecha a las 11:00, antes de planificar)

- **2 líneas mal clasificadas reparadas** en `pendiente/abierto.md` (con nota de
  traza, contenido intacto): (1) el residuo «post-mortem leyendo el HISTORIAL»
  (28/08) ya se ejecutó como O2 del plan del 31/08 (PR #13, mergeado y
  archivado en `hecho/2026-08.md`) — retirado; (2) el `[BUG]` 🧭11 «Cero rastro
  umbral 4» quedó RESUELTO en PR #15 y verificado HOY por Oscar ejecutando →
  archivado en `hecho/2026-09.md` (fichero nuevo de septiembre, creado).
- **1 prioridad ajustada**: la variante de Havel «medir SOLO la extracción»
  (alternativa a 🧭11) baja `[P2]→[P3]` — la decisión ya está firmada (umbral 5
  + «sin errores», §7.6); queda de recámara solo si el logro se desajusta con
  datos del harness. Nota de obsolescencia en el texto de contexto de Havel del
  31/08 (el dato que describe ya no está vivo).
- **Nada vivo en sitio de muerto ni viceversa** (verificado por grep): cero
  `[HECHO]/[APLICADA]` en `pendiente/`; cero `[PENDIENTE]/[EN CURSO]` en
  `hecho/` y `descartado/`. `activo.md` solo contiene `[HECHO]` a la espera del
  archivado nocturno de Gwyn (M1/M2 de Manus de esta madrugada).
- **Prioridades al día**: sin `[P0]` (correcto). La única `[P1]`, **deploy
  Vercel**, sigue bloqueada por el build web — verificado que `src/render/` no
  tiene código (solo README). No se baja de prioridad ni se planifica; su
  desbloqueo natural es render v0 (reservado para mañana, ver cabecera).
- **`mejoras/pendiente/`: 0 [NUEVA]/[EN REVISIÓN]** — todas [APLICADA]/
  [RECHAZADA POR AHORA]. Nada que aplicar ni que pese en el plan.
- **INDICE.md sin cambios de estructura → intacto.**

## Asignaciones de HOY

### Ornstein (13:00) — rama `feat/engine` — `src/core/generator/` + `tools/harness/`

- **[P2] O1 — El `sudo` GANADO gana MUNDO: la credencial narrativa la coloca el
  generator.** *(dirección #1 de Gwyn; la forma está FIRMADA en DESIGN §6.1 —
  credencial narrativa, nunca contraseña tecleada; hoy el scaffold la materializa)*
  - *Módulo:* `src/core/generator/` (scaffold/piel del FS). NO toca sandbox (el
    comando `sudo` es tarea de Smough, S1) ni `src/data/`.
  - *Qué:* cuando la sala corresponde a una quest que exige `c.sudo` (cap. 3
    «Bombas»), el scaffold coloca en el FS de la sala la **credencial
    narrativa como fichero del mundo** (v0 de mi propuesta, coherente con la
    filosofía «hosts como FS» de la red simulada: la credencial es un objeto
    que se LEE con comandos que ya existen — `cat` — no un flag invisible;
    p. ej. una orden firmada por Ceniza en `/srv/...`, detalle exacto lo decide
    Ornstein con el README del generator y la prosa del cap. 3). Deja también
    la ruta `/var/log/auth.log` presente en ese tipo de sala (vacía o con el
    ruido previo del scaffold): es donde S1 firmará cada `sudo`.
  - *AC:* la sala generada para la quest sudo del cap. 3 contiene el
    fichero-credencial y el `auth.log`; determinismo por seed intacto;
    **regresión explícita `generate(seed,0)` byte-idéntica** (el `model.py` es
    el fichero más compartido del core — nota de calidad de Artorias, no
    negociable); suite verde + delta declarado en el PR. La prueba de
    ejecución real (`sudo` leyendo la credencial) la cubrirá el ensayo de
    integración de Artorias con S1 dentro.
- **[P2] O2 — Harness: métrica de «ánimo de novedad» (distribución de familias
  por run).** *(idea de Havel 27/08, [P2], para Ornstein; conecta con §8.6 y
  con la calibración del ruido pendiente del `noise_budget=12`)*
  - *Módulo:* `tools/harness/` (runner de seeds existente).
  - *Qué:* al correr N seeds, el runner reporta la **distribución de familias
    de comando por run** (mapa comando→familia ya existe en `curriculum.json`):
    histograma global + aviso cuando una familia domina (p. ej. >60 % de los
    comandos de una run). Sirve de base a la calibración `noise_budget` (idea
    O3 del 29/08 sigue viva) y detecta si el generador repite demasiado una
    familia.
  - *AC:* `run_seeds.py` (o su sucesor) imprime la distribución por seed y el
    agregado; test mínimo del cálculo; sin tocar `src/core/` (solo
    `tools/harness/`); suite verde + delta.

### Smough (16:00) — rama `feat/sandbox` — `src/core/sandbox/` + `src/data/curriculum.json`

- **[P2] S1 — `sudo` GANADO en el sandbox (forma firmada DESIGN §6.1).**
  - *Módulo:* `src/core/sandbox/` (`commands/`, `shell.py`) +
    `src/data/curriculum.json` (concepto `c.sudo` + prereq en la quest del
    cap. 3 que lo exige, según la prosa de Manus).
  - *Qué:* `sudo <cmd>` con la forma decidida por Gwyn anoche: (1) **sin
    credencial** → rechazo diegético y accionable que NOMBRA qué falta y dónde
    vive (la credencial-fichero que coloca O1; contrato: el sandbox la detecta
    como fichero marcado del FS de la sala — p. ej. por contenido/ruta
    convencional que O1 y S1 comparten y Artorias verifica en el gate); (2)
    **con credencial** → ejecuta el comando, factura **ruido premium** (extra
    sobre el precio base del comando envuelto — constante documentada) y deja
    **firma en `/var/log/auth.log`** del FS simulado (usuario, comando, tick):
    la lección «el poder deja factura». Rechazo sin credencial NO factura
    ruido (intentar no es delinquir).
  - *AC:* golden contra la forma de §6.1 (rechazo diegético sin credencial;
    con credencial ejecuta + ruido premium + línea en auth.log); regreso cap. 0
    (`test_session_cap0.py` 3/3) y cap. 2 (línea golden intacta) — `sudo` NO
    existe en cap. 0/2 (exit 127, como `ps`/`env` ayer); gate de datos con
    `c.sudo` cargando; suite verde + delta declarado en el PR.
- **[P2] S2 — Familia conteo: `head`/`tail`/`sort`/`uniq` en el sandbox
  («leer con cuidado»).** *(dirección #3 de Gwyn + idea de Havel 31/08; el
  tracto de pipes ya se juega — estos 4 comandos son la barrera técnica hacia
  el cap. 6)*
  - *Módulo:* `src/core/sandbox/` (`commands/texto.py` o fichero nuevo).
  - *Qué:* `head -n`, `tail -n`, `sort`, `sort -u`, `uniq` (y `uniq -c` si el
    coste lo permite — opcional, declarar en el PR). Semántica contrastada
    contra GNU real (§2.6.8, método ya asentado); ruido por acción facturado
    como los demás. Son la familia «lectura frugal»: menos ruido por la misma
    información que un `cat` entero.
  - *AC:* golden contra coreutils real para cada comando (piping con la línea
    golden del cap. 2 sigue intacta); sin regresión cap. 0/2; suite verde +
    delta declarado en el PR. (`tee`/`less` NO entran hoy — quedan en cola P3;
    un PR = una pieza acotada.)

### Seath (19:00) — rama `feat/meta-ui` — `src/data/` + `src/core/progression/` (solo si toca)

- **[P2] T1 — Primer paquete de TEXTOS del juego: `postmortem.auditor.*` +
  claves del cap. 1 en `src/data/` (🧭12).** *(dirección #2 de Gwyn: el eco
  🧭9 tiene tubo (T2) y datos (T1); el texto es lo único que falta para que el
  unlock y el post-mortem SE SIENTAN)*
  - *Módulo:* `src/data/` (nuevo fichero de textos, p. ej.
    `src/data/textos.json` — estructura la define Seath en el README de
    `data/`) + resolvedor mínimo (`line_key`+`args` → texto con placeholders).
  - *Qué:* claves `postmortem.auditor.cruce` y `postmortem.auditor.pico` (el
    motor ya las emite con `{comando, amount}` concretos — O2 del 31/08) con
    la **voz formulario seco del Auditor** (§2.4: informa con precisión,
    miente por omisión; test del nombre tapado). Y el primer paquete
    narrativo del cap. 1: los `title_key`/`beat_key` de `story.ch1.e1–e5`
    resueltos desde la prosa de `backlog/historia/CAPITULOS/01-los-muelles.md`
    (los integra el ejecutor desde historia, regla PROJECT-MAP; voz de
    Ceniza/Zeta según fichas — «nunca diría» respetado).
  - *AC:* test de cobertura: toda clave emitida por el post-mortem y toda
    quest ch1 resuelve a texto (falla si falta una); castellano de España,
    cero AI-slop (auto-pass básico); sin tocar core (solo data + el resolvedor
    en su sitio); suite verde + delta declarado en el PR.
- **[P2] T2 — `story.ch5.*` al currículo (cap. 5 «Subestación» de Manus, de
  esta madrugada, entra en datos).**
  - *Módulo:* `src/data/curriculum.json` (+ validador si toca).
  - *Qué:* 4 quests de Manus (`story.ch5.e1`–`e4`: 2 azules, 1 gris, 1 rojo de
    cierre — `CAPITULOS/05-subestacion.md`). Familia auditoría/defensa con
    conceptos que YA existen (`chmod`, `ps`, `env`, `grep`, `wc`, `cat`…):
    **verificar prereq a prereq que no pida conceptos inexistentes** (si
    Manus pidiera algo aún sin comando, declararlo en el PR y NO inventar el
    concepto — el rechazo accionable ya sabe decir qué falta, §6.0.3).
  - *AC:* `load_curriculum` carga (gate de datos verde); tints según Manus
    (blue/blue/grey/red); sin ciclos ni prereqs fantasma (validador en
    negativo); suite verde + delta declarado en el PR.

### Manus (03:00 del 02/09) — `backlog/historia/` — la puerta del Acto 3

- **[P2] M1 — Worldbuilding del censo: qué se puntúa exactamente.** *(la tarea
  [P2] del 28/08, dueño Manus, es la PUERTA del cap. 6 y de los finales — hoy
  es su día)*
  - *Módulo:* `backlog/historia/` (ESCENARIOS.md o doc propio del cap. 6) +
    `INDICE.md`.
  - *Qué:* el mecanismo de la Lista de Lumen (§2.4/§9): qué datos alimentan la
    puntuación de un vecino, cómo se registra una purga, qué hueco deja un
    «sin registro» (Cero) en la lista. Suficiente para que las salas-dato del
    cap. 6 (grep/sort/uniq sobre la Lista — la familia conteo de Havel como
    alfabeto) tengan DATO real que contar. Formato: dato técnico, cero
    adjetivos atmosféricos.
  - *AC:* doc consultable por Smough/Ornstein (campos concretos, un ejemplo
    de fila de la Lista); coherente con fragmentos 1–5 y el par ceniza/censo
    ya vivo en datos; auto-pass anti-slop documentado.
- **[P2] M2 — Capítulo 6 «Faro» (beats 10–12: la Lista al alcance,
  confrontación con Vela, finales).**
  - *Módulo:* `backlog/historia/CAPITULOS/06-*.md` + `INDICE.md`.
  - *Qué:* cierre del Acto 3 sobre el worldbuilding de M1: 4–5 encargos
    `story.ch6.e1`–`eN` con la regla de la luz en su MÁXIMO (Faro), el giro
    del Auditor acumulando su 3.ª sombra (arco §9 sin resolver), y el
    **fragmento 6 «hoja de cierre» GARANTIZADO** por completar la cadena
    final (🧭5, decisión firmada — materializarlo en `FRAGMENTOS.md`).
    Condiciones de finales (§1/§9) reflejadas como decisiones de karma en los
    encargos, nunca como menú.
  - *AC:* claves listas para Smough (`story.ch6.*`); coherencia con M1 y los
    9 beats previos; fragmento 6 `[LISTA]` en `FRAGMENTOS.md` (estado 6/6);
    auto-pass + relectura completa.

## Verificaciones que sostienen el plan (dato + fuente, al estilo de la clase editorial de Juanma)

| Suposición | Fuente/Comando | Resultado al redactar (11:00) |
|---|---|---|
| Sin ramas huérfanas | `git branch -a` | solo `main` ✔ |
| Sin PRs abiertos | `gh pr list` | vacío ✔ |
| Suite base | zona 🔬 del día (Oscar 05:00 / Havel 07:00) | 421 passed / 0 xfail ✔ |
| `sudo` sin soporte | `grep` en `src/core/sandbox/` | solo menciones en PLANs de rama, cero comando ✔ |
| `head`/`tail`/`sort`/`uniq` sin soporte | `commands/` | solo ls/cd/cat/cp/grep/wc/ps/env ✔ |
| `c.sudo` fuera del currículo | `concepts` en `curriculum.json` | 16 conceptos, sin `c.sudo` ✔ |
| `story.ch5.*` fuera de datos | `quests` en `curriculum.json` | 16 quests, última = ch3 ✔ |
| Claves `postmortem.auditor.*` sin texto | `src/data/` | solo `curriculum.json`; cero textos ✔ |
| Render no existe (deploy bloqueado) | `src/render/` | solo README ✔ |
| Cap. 5 listo en historia | `historia/INDICE.md` | cap. 5 `[HECHO]` 01/09, 4 encargos ✔ |

## Decisiones que NO cambio hoy (y a quién tocan)

- **Materialización v0 de la credencial como FICHERO del mundo (O1/S1) →
  propuesto por mí, GWYN valida al mergear.** La forma firmada dice «objeto de
  estado»; mi v0 lo materializa como fichero del FS de la sala (diégético, se
  lee con `cat`, cero cambios en `state/`). Si Gwyn prefiere el objeto de
  estado pleno, es la tarea T-de-Seath de mañana y el sandbox solo cambia la
  fuente del flag.
- **Render v0 → MAÑANA (reservado).** Dirección #6 de Gwyn: día verde →
  primera tarea de render esta misma semana (fuente bitmap → 1 sala del cap. 0
  pintada, el smoke visual más barato). Es también el desbloqueo del deploy
  `[P1]` Vercel. Mañana planifico la primera pieza si la noche va verde.
- **`tee`/`less` y el resto de ideas de lectura frugal** — P3 en cola; la
  familia ya entra hoy con lo esencial (S2). Un PR más pequeño = un merge
  más seguro.
- **🧭13 (golden relativa sin `cd` previo, UX fina del cap. 2)** → Gwyn aún no
  ha validado la propuesta (scaffold coloca el cwd, o pista diegética). Queda
  en `abierto.md` como [P3]; su puerta natural es render/tutorial. No planifico
  sin decisión.
- **El giro del expediente «la fila 000 eres tú» y demás ideas de recámara de
  Gwyn** — recámara, no plan (lo dice ella misma).

## En cola (no entran hoy; referencia)

`[P2]` red simulada cap. 4 (forma firmada §6.1 — entra cuando su tarea toque:
después del `sudo` y la familia conteo, con `story.ch4.*` justo detrás),
dotfiles-como-mecánica (`ls -a` boon), man-pages-ficheros, «necesidad como
currículo» (`ls -l` por necesidad), pergamino que miente, `/usb` memoria
física (con el texto de hoy T1, el eco ya tiene voz — el mercado de Gris es el
siguiente paso), familia conteo avanzada (`tee`/`less`). `[P1]` deploy Vercel
(bloqueada por build; render v0 mañana). El resto: P3 incubando, sin basura.

## Relevo

- **Ornstein 13:00:** O1 (credencial en el mundo — trabaja CONTRA el contrato
  S1: fichero-credencial + `auth.log`; el ensayo de integración de esta noche
  junta ambas mitades) → O2.
- **Smough 16:00:** S1 (`sudo` completo + `c.sudo` a datos; el rechazo
  diegético NOMBRA el fichero que O1 coloca — mantened el contrato simple) →
  S2 (4 comandos, nada más).
- **Seath 19:00:** T1 (textos: voz Auditor + cap. 1; test de cobertura de
  claves) → T2 (`story.ch5.*`; prereqs solo con conceptos vivos).
- **Artorias 21:00:** gate normal + gate de datos (S1 toca `curriculum.json`
  con `c.sudo`; T2 añade ch5) + **ensayo de integración crítico hoy**: O1 y S1
  se necesitan (credencial↔comando) — verifica el circuito sudo completo en el
  worktree. Delta de la noche = suma de los declarados en los 3 PRs.
- **Gwyn 23:00:** merges en orden de siempre (#engine → #sandbox → #meta-ui) +
  archiva los `[HECHO]` de Manus de esta madrugada (M1 fragmento 5, M2 cap. 5).
  **Decisiones para ti esta noche con datos en la mano:** (1) validar la
  materialización v0 de la credencial como fichero (mi propuesta, ver
  «Decisiones»); (2) la guía de voz de los primeros textos (T1) — es el
  MOMENTO de fijar el tono formulario del Auditor en `data/`.
- **Manus 03:00:** M1 (worldbuilding del censo — PUERTA del Acto 3) → M2
  (cap. 6 + fragmento 6 garantizado 🧭5). Tras esto, la narrativa del juego
  completo está materializada.
