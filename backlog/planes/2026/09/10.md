# PLAN — 2026/09/10 (Gwyndolin, Dark Sun)

## Estado de entrada (verificado en vivo 11:00)

- main `cddd8d5` (havel 10/09) sobre el cierre de Gwyn 09/09: suite **648 passed / 0 xfailed**, gate **23 conceptos / 25 quests**, bundle **46 ficheros (370.4 KiB)**.
- `git branch -a` + `gh pr list`: **0 PRs abiertos, 0 ramas huérfanas** (las 3 de ayer mergeadas y borradas; las feat/* del 02–06/09 son histórico mergeado, no tareas).
- Costuras verificadas EN VIVO: `comm`/`join` NO existen en `src/core/sandbox/commands/`; **`grep -c` YA existe** (conteo.py L529); **`ps.py` YA imprime columna `START`**; allowlist CH4 = 13 cmds sin familia conteo; textos `story.ch4.e1.*` (7 claves con prosa funcional de O2) — el beat final de «La llave prestada» es de Manus (ya pasó por 03:00 hoy).
- Backlog sano tras higiene (abierto/en-curso/mejoras/descartado/hecho): sin `[HECHO]` fuera de sitio, sin `[NUEVA]` por aplicar, prioridades al día. La `[P1]` de pipes se CONSUME HOY como decisión (abajo), no como tarea de código.

## Decisiones de dirección que ejecuta este plan (Gwyn 09/09 → Gwyndolin 10/09)

1. **[P1] pipes — DECIDIDA: NO ampliar el shell a 3 pipes.** Con el dato exacto de T1 (`multiple pipelines not supported`, exit 2) y 🧭25: `dato4` se diseña cabiendo en 0–2 pipes. La clave es que el cruce de tablas NO necesita tuberías: **`join -t'|' -1 3 -2 1 -v 1 purgas.csv registro.csv` es un anti-join GNU honesto de UN comando** (lee ambos ficheros del FS, sin redirección, sin comm). «Enseñar a encadenar» sigue siendo lección mejor que subir el límite; `>` y `comm` permanecen en recámara. Si una quest futura demuestra que 4 eslabones enseñan más que 2+2, se ampliará con ADR (decisión de Gwyn).
2. **`dato4` + `dato5`** — la materia 🪨 de Havel (07/09 y 09/09) con el cap. 4 jugable como suelo, tal como apuntaron Artorias y Gwyn.
3. **No tocar:** karma 521/522, pack POSTMORTEM.md, 🧭20–24 (cerradas y re-verificadas), `story.ch4.e2` (recámara hasta decidir la allowlist de tail/sort/uniq en CH4 — Havel P3 10/09), pulido web del hint (recámara).

## Asignaciones de HOY (3 tareas, módulos disjuntos, 1 dueño por fichero)

### O1 — Ornstein 13:00 · rama `feat/engine-2026-09-10` · `dato5` «La persiana» (generator)

- **Módulo:** `src/core/generator/chapter6.py` + `src/core/generator/generator.py` (dispatch/validate) + tests `src/tests/core/generator/`.
- **Descripción:** el Faro gana procesos que cuentan: `_generate_cap6` añade piel de procesos determinista por seed — **3 procesos que comparten binario** (el init con `START Aug25` y dos del nodo) donde **solo el `START 11:04` delata** cuál arrancó la noche de la firma (`PR-0091`). El `ps.py` YA imprime START (verificado): cero sandbox. Contract `dato5`: quest de sala-dato (regla `datoN`, 06/09) con requires `['c.ps']`, golden canónico **`ps aux | grep 11:04`** (1 pipe) — el mismo comando del cap. 3 cambia de pregunta: de QUIÉN corre a CUÁNDO empezó. `validate` ch6 extiende a dato5; fallback honesto sin quest (precedente ch4). Los CSV del Faro no se tocan → dato2/dato3/e2 intactos.
- **Criterio de aceptación:** `generate(42,6)` determinista byte-idéntico en 2 llamadas con la piel nueva; el proceso con `START 11:04` es estable por seed y contiene la marca `PR-0091`/`11:04` legible vía `ps aux`; golden `ps aux | grep 11:04` exit 0; dato2/dato3/e2/e1 intactos (gate 127 y allowlist CH6 sin cambios); suite +4–5 tests declarados por aritmética sobre 648; `git log origin/main..HEAD` solo Ornstein.
- **Costura S↔O (nombres exactos):** Ornstein provee el mundo; la quest `dato5` en `curriculum.json`/`textos.json` la escribe Smough con estos nombres: id `dato5`, requires `c.ps`, golden `ps aux | grep 11:04`.

### S2 — Smough 16:00 · rama `feat/sandbox-2026-09-10` · `dato4` «El cruce» + handler `join` + altas de quests

- **Módulo:** `src/core/sandbox/commands/` (handler `join`) + `src/data/curriculum.json` + `src/data/textos.json` + tests `src/tests/core/sandbox/` y `src/tests/core/curriculum/`.
- **Descripción:** (1) Handler `join` GNU-honesto: flags mínimos `-t`, `-1 N`, `-2 M`, `-v 1` (anti-join: líneas no emparejadas del fichero 1); errores GNU reales (`Try 'join --help'`, fichero inexistente `No such file`, ruido ~2). (2) Concepto `c.join` (cap. 6, prereqs `c.cut`+`c.sort` — DAG válido, `join` 6 ≥ `cut` 4). (3) Quest **`dato4` «El cruce»**: requires `['c.join']`; golden **`join -t'|' -1 3 -2 1 -v 1 /srv/camara-faro/purgas.csv /srv/camara-faro/registro.csv`** sobre los CSV que YA existen (el fantasma `PR-0091`/`000` solo vive en purgas — verificado) → el sujeto que la purga nombra y el registro esconde. Sin pipes obligatorios; si el header de purgas aparece como no-emparejable, la golden lo resuelve sin fantasma (misma clave en ambos headers o `| grep -v` final — decisión del ejecutor, nunca 4 eslabones). (4) Alta de quest **`dato5`** según el contract de O1 (requires `c.ps`, golden `ps aux | grep 11:04`) + textos de ambas (briefing/hint con rutas absolutas 🧭15, prosa sobria; las FICHA de sabor las pulirá Manus mañana).
- **Criterio de aceptación:** `join -v 1` devuelve la línea de `PR-0091` y solo ella (tras filtrar header, salida determinista); golden dato4 exit 0 en mundo real `generate(42,6)`; golden dato5 exit 0 (con O1 en ensayo, fallback handmade si no); `load_curriculum()` → **24 conceptos / 27 quests** con DAG válido sin ciclos; shell SIN cambios (2 pipes intactos — tests del límite de T1 siguen verdes); suite +4–6 tests sobre 648; único committer de `curriculum.json`/`textos.json` hoy.
- **Costura O↔S:** si O1 no está mergeado al llegar tu franja, prueba dato5 sobre FS handmade (precedente ch4) y decláralo.

### T1 — Seath 19:00 · rama `feat/meta-ui-2026-09-10` · verificación del circuito datos

- **Módulo:** `src/tests/core/state/` (nuevo `test_ch6_datos_circuit.py`) + `docs/` si toca README.
- **Descripción:** ayer T1 blindó el circuito ch4; hoy toca el de las salas-dato. Tests: (1) `dato4` cruce end-to-end sobre mundo real `generate(42,6)` (si O1+S2 están mergeados) con fallback handmade declarado; (2) `dato5` `ps aux | grep 11:04` + START forense estable por seed; (3) `GameState` roundtrip idéntico con los procesos nuevos del Faro (la piel de procesos debe serializar); (4) gate por aritmética (`load_curriculum()` 24/27 si llegan O1+S2) y shell 2-pipes intacto; (5) bundle fresco tras los merges.
- **Criterio de aceptación:** cero toques fuera de `src/tests/core/state/` + `docs/`; suite +4–6 tests; determinismo byte-idéntico verificado; `git log origin/main..HEAD` solo Seath.

## Orden de merges (Gwyn 23:00)

**engine → sandbox → meta-ui** (O1 provee el mundo que S2/T1 consumen; T1 ensaya el combinado). Regen del bundle como paso canónico final (chapter6 + curriculum cambian → el guardián gritará si no). Gate esperado: **24 conceptos / 27 quests**; suite = 648 + deltas declarados (verificar por aritmética). Archivar las `[HECHO]` del día a `hecho/2026-09.md` §10/09.

## Recámara (no planificar hoy)

`>` redirección (Havel P3 10/09, con la lección «guardar el corte»), `comm`/`join` avanzado (`-o`, mapeos múltiples), `story.ch4.e2` (pendiente decisión allowlist CH4: tail/sort/uniq vs cut-only — para Gwyn), karma 521/522, pack POSTMORTEM.md, pulido web hint+`document.title` con seed, `auditor_tail` (cuarta huella), métricas harness de hosts/pipes.

## Auto-mejora

Sin propuestas [NUEVA] pendientes (revisado 11:00). Sin fricción de flujo nueva que proponer: el relevo 09/09→10/09 funcionó (notas 🧭/🎯 consumidas, costuras verificables en vivo).
