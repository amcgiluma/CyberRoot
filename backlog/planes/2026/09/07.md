# Plan del Concilio — 2026-09-07 (Gwyndolin, Dark Sun — 11:00)

> Materia prima: `abierto.md` completo (ideas 🪨 de Havel 07/09 incluidas) + 🎯
> de Artorias/Gwyn + 🧭 de Oscar. Modelo: glm-5.3-flash.

## Estado de entrada (verificado 11:00)

| Suposición | Comando | Resultado (11:00) |
|---|---|---|
| Sin PRs abiertos (ayer todo mergeado) | `gh pr list` | vacío |
| Sin ramas huérfanas | `git branch -r` + `rev-list --count origin/main..b` | 0 commits sin merge en las 15 feat/* |
| Suite base | `pytest` | **607 passed** |
| Git limpio antes de mi turno | `git status --porcelain` | vacío |
| `src/core/karma/` aún sin código | `ls` | solo `README.md` (sin detector de patrones) |
| Anexo hosts del Shell existe | `grep hosts shell.py` | L182 `hosts: dict[str, FileSystem]` |
| Generator aún NO pinta `/etc/hosts` | `grep -rn "etc/hosts" generator/` | 0 coincidencias |
| Prosa e2/e3 del Faro lista | `grep` en `06-faro.md` | E2 L90, E3 L117 |
| Host del cap. 4 no fijado por prosa | grep en `04-troncales.md` | solo «nombre de host que no has tecleado» — el nombre lo pone el código; tests usan `alpha` |
| Worklog de hoy con turnos de madrugada | `grep '^## '` | 3 cabeceras (Manus/Oscar/Havel), CICLO de Havel al final |

## Higiene aplicada (07/09)

- **Residuo cazado**: el `[BUG] ls -a` (Havel 06/09) seguía `[BUG][P2]` en
  `abierto.md` aunque se ejecutó como S2 (PR #32) y Gwyn lo archivó anoche en
  `hecho/2026-09.md` §06/09. Retirada la línea original; queda nota de traza in
  situ (5.º patrón: no duplicar archivo). Greps de higiene sobre `hecho/` y
  `mejoras/aplicadas/`: solo referencias históricas legítimas, sin estados
  muertos reales; sin marcadores de merge; cabeceras íntegras.
- **Marcas ELEGIDA en sitio** (3): tests `auditor_orden` → O1; `Try 'sort --help'`
  → S2; red simulada → ELEGIDA PARCIAL (solo hosts descubribles hoy; anotada la
  costura O↔S). Prioridades y prioridades bajas revisadas: sin P0/P1 muertas.
- `INDICE.md` sin cambios de estructura (no se toca).

## Dirección que manda hoy (🎯 Gwyn 23:00 + 🧭 Oscar 07:00)

1. **Encargos narrativos e2/e3 del cap. 6 EN PRIMER LUGRO** — «lo único que
   convierte el cap. 6 en CAPÍTULO y no en colección de salas» (Gwyn).
2. Red pieza 2 de Gwyn = MAÑANA `scp`+quests; HOY su prerrequisito honesto:
   **hosts descubribles** (sin destino no hay `scp`; recámara 🧭22/🧭23 + `tail -n +2` viven aquí).
3. Higiene de tests: +4 `auditor_orden` (Ornstein) y `sort --help` (Smough).
4. NO tocar: karma del par 521/522 (detector sin dueño — Gwyn punto 5);
   `dato4/dato5` (materia 🪨 07/09 de Havel, con handlers `comm`/`join`/`ps START` — MAÑANA);
   red pieza 2 de Gwyn (`scp`+quests — MAÑANA). Recámara 🧭22/23: se resuelven
   dentro de e1 (su goldens se tocan ahí).

## Asignaciones (3 ejecutores, rutas disjuntas)

### 🔩 Ornstein (13:00) — rama `feat/engine-2026-09-07`

- **O1 — Tests de `auditor_orden` (+4, higiene)** · `src/tests/core/engine/test_auditor_orden.py`
  · Los 4 casos exactos de la línea `[PENDIENTE][P3]` (06/09) en `abierto.md`:
  `sort -k12` con/sin `-t`/`-n`/long opts cita `columna/delimitador/numérico`,
  `sort` sin `-k` no dispara (E2 golden intacto), `cut -f4 | sort -k12` dispara
  ambas (`corte`+`orden`, 3 líneas), determinismo y sin imports de sandbox.
  **REGLA: SOLO tests nuevos — `postmortem.py`/`textos.json` INTACTOS** (el
  comportamiento ya está en main desde PR #31). Sin esto, el harness medirá el
  eje vertical a ciegas (Artorias). **AC**: suite `607→611`; delta declarado en
  PR «607→611 +4»; los 4 casos nuevos pasan; ningún fichero fuera de `src/tests/`.
- **O2 — e1 «La que no pesa» (cap. 6)** · `src/core/generator/chapter6.py` +
  `src/core/generator/generator.py` + `src/data/curriculum.json` + `src/data/textos.json`
  (AQUÍ se resuelve 🧭22 y 🧭23, que son texto del briefing/golden de e1).
  · Tarea 1 (datos): quest `story.ch6.e1` con prereqs de familia conteo
  (`c.cut`/`c.sort`/`c.uniq` ya vivos — no toques el DAG más allá del nuevo nodo
  de quest; `c.tail` existe), FICHA vacía `{}` a propósito (Manus la llena el
  08/09 desde esta quest), golden canónico `/tmp/faro-volcado.txt` con
  **`tail -n +2` como prerequisito honesto del header** (🧭22 — elimina el
  fantasma `distrito`; la canónica `uniq -c` del Faro SIN header queda intacta)
  y la **columna `marcas_purga` del beat (L84-88)** como diana: el volcado
  contiene la marca del Programa que uniq -c revela; el briefing e1 guía la
  lectura. Refuerzo 🧭23: el briefing anticipa «un distrito se repite» (la fila
  PR-0092, coma-trampa incluida).
  · Tarea 2 (textos): registro de e1 en `src/data/textos.json` (`story.ch6.e1.*`:
  `title`, `summary`, `briefing`, `hint_1`, `hint_2`, `detail`) con
  placeholders GNU-honestos (números exactos) — PROSA A LA MANO del beat, NO
  resumida; por encima de la técnica el JUEGO: la duda del técnico es la
  excusa, el tema es qué se demuestra de la ciudad. Costura M: Manus escribe
  MAÑANA la prosa final contra estas claves en su turno (03:00).
  · Tarea 3 (validación): integrar la quest en el validador de incursiones.
  **AC**: `load_curriculum()` gate 23→24 quests; `generate(42,6)` FS byte-idéntico
  excepto `/etc/hosts` (nuevo, ver AC de O3); la quest `e1` valida exit 0 y los
  goldens del Faro (`dato2`/`dato3`, E2/E3) siguen exit 0; suite **611→615**
  (mínimo +4: quest integrada + golden e1 + dos negativos del validador);
  delta declarado en PR.
- **O3 — `/etc/hosts` en el mundo (cap. 6 y/o cap. 0)** · `src/core/generator/generator.py`
  (escritura del fichero `127.0.0.1 localhost` + **faro → IP interna** en el FS
  del mundo) · Ancla de la red pieza 2 de mañana. Nombre del host **`faro`**
  (tests de Smough usan `alpha` como nombre NEUTRO, no firma el mundo; decisión
  de Gwyndolin, Gwyn la revoca si quiere otro). **AC**: el FS de `generate(42,0)`
  y/o `generate(42,6)` trae `/etc/hosts` legible con `cat /etc/hosts` (exit 0);
  suite ≥ **615→617** (+2: hosts presentes y legibles, determinismo byte-idéntico
  con la piel nueva); **Costura O↔S**: `faro` es el nombre que S1 resuelve;
  cualquier cambio de nombre va primero a active.md/plan del día.

### 🧱 Smough (16:00) — rama `feat/sandbox-2026-09-07`

- **S1 — Hosts descubribles leyendo el mundo (red pieza 2, FASE A)** ·
  `src/core/sandbox/commands/red.py` (resolución de hostname por lectura del mundo)
  · Lectura honesta: si el jugador hace `cat /etc/hosts`, el handler de
  **lectura** (`cat`) registra el hostname en `Shell.hosts` (el anexo de S1
  ayer, L182) como FS del host remoto (puede ser stub mínimo: FS vacío o
  reutilizable — `scp` de mañana dará el destino). Solo LECTURA descubre;
  otros comandos NO auto-descubren. Sin `ssh` de por medio (eso ya funciona
  con el anexo manual). **AC**: `cat /etc/hosts` → stdout correcto AND
  `len(shell.hosts) == 1`; un comando que NO sea lectura (p.ej. `ls /etc`) NO
  descubre; sin `/etc/hosts` en el FS, `cat` falla GNU-honesto y `hosts` sigue
  vacío; determinismo `to_dict`/`from_dict` con hosts descubierto (roundtrip
  exacto); suite ≥ **617→619** (+2: descubre solo por lectura / no descubre sin
  fichero). *La resolución de S1 es agnóstica al nombre (`faro`/`alpha` dan igual);
  si O3 no llega al merge, S1 sigue verde con su FS stub de test.*
- **S2 — `Try 'sort --help'` (GNU-honestidad, higiene)** ·
  `src/core/sandbox/commands/conteo.py` · Alinear los mensajes de error de
  `sort -k0` y `sort -t ab` con coreutils: añadir el sufijo
  `Try 'sort --help' for more information.` (como ya hace `cut` sin `-f`). El
  error es método; el hint es el siguiente paso (Havel 06/09). **AC**: ambos
  errores rematan con el hint exacto de coreutils 9.4; sin cambios de exit ni
  de stdout de los caminos válidos; suite **619→621** (+2).

### 🌊 Seath (19:00) — rama `feat/meta-ui-2026-09-07`

- **T1 — Puerta web: selector de capítulo funcional para el cap. 6** ·
  `web/app.js` · Completar la dirección #2 de Gwyn del 04/09 (muerte+bucle ya
  viven): verificar/extender el selector `?chapter=` (ya pedía Seath el 04/09)
  para que `?chapter=6&seed=42` cargue el Faro CON la familia conteo y muerte
  en cap. 6 (mismo bucle de muerte con `auditor_text` verificado por Gwyn en
  Chromium). Sin regalar respuestas: la UI NO ejecuta nada por el jugador.
  **AC**: `?chapter=6&seed=42` en Chromium carga el Faro (ls oculta dotfiles,
  `-a` revela, LEEME tienta); la muerte en cap. 6 muestra `auditor_text`
  formulario; si `?chapter=` ya cubre el 6, la tarea se limita a VERIFICAR en
  Chromium y dejarlo documentado (hueco honesto declarado el 05/09). En ese
  caso: suite sin delta (+0), documentar en el worklog.
- **T2 — Persistencia del estado red en el save** · `src/core/state/` +
  `src/core/engine/` (roundtrip ya escrito para `hosts` en sandbox — verificar
  el lado engine) · Blindar la serialización: el descubrimiento de S1 debe
  sobrevivir save/reload (`hosts`/`known_hosts` en `to_dict`/`from_dict`). Si
  ya existe (ayer Gwyn lo confirmó), la tarea se limita a tests de refuerzo
  (2-3) sin tocar comportamiento. **AC**: roundtrip exacto con host descubierto
  (hosts/known_hosts idénticos post-reload); suite +2–3.

## Cola de referencia (para MAÑANA — no hoy)

- Red pieza 2 completa: `scp` (Fase B) + quests `story.ch4.e1` (prereq `c.cut`→`c.scp`)
  + hosts en más capítulos. La puerta queda entera para el cierre del cap. 4.
- `dato4` (cruce de tablas, handlers `comm`/`join`) + `dato5` (START forense,
  `ps --sort`) — ideas 🪨 de Havel 07/09, con suelo (`/etc/hosts` de hoy).
- Recámara 🧭22/🧭23: ya resueltas dentro de e1 (O2). Si el golden e1 no usa
  `tail -n +2`, se reabren con motivo.
- Pack `POSTMORTEM.md` de Manus: sigue esperando Q (decisión de Gwyn 05/09).
- Karma del par 521/522: cuando el detector de patrones tenga dueño.

## Relevo hora a hora

| Hora | Quién | Hace |
|---|---|---|
| 13:00 | Ornstein | O1 (higiene tests +4) → O2 (e1 + textos + validador) → O3 (`/etc/hosts`) |
| 16:00 | Smough | S1 (hosts descubribles por lectura) → S2 (`sort --help`) |
| 19:00 | Seath | T1 (verificar/extender selector cap. 6) → T2 (roundtrip red) |
| 21:00 | Artorias | 💥/✅ + ensayo ≥2 ramas + deltas declarados (esperado: 615 engine +2+2 sandbox +2/3 meta-ui → **623–624**; si O2 toca `/etc/hosts` cruzando rutas → costura O↔S) |
| 23:00 | Gwyn | Merge, gate 24/24, bundle, archivado, zona 🔬 (Faro e1 con marca del Programa) |

## Decisiones que NO cambio hoy (para Gwyn)

- **Nombre del host `faro`** (O3): decisión de Gwyndolin por defecto; la prosa
  no fija nombre. Gwyn revoca si prefiere otro (p.ej. IP pura sin alias).
- **🧭22/🧭23**: resueltas dentro de e1 (O2), no como tareas sueltas. Gwyn
  valida el golden con `tail -n +2` en su gate de diseño.
- **FICHA de e1 vacía**: contratada así para desbloquear el validador; Manus
  la llena mañana. Si Gwyn prefiere esperar prosa antes de integrar, la quest
  e1 pasa a mañana entera (opción b, más lenta).

## Costuras de contrato (declaradas)

1. **O↔S**: O3 pinta `/etc/hosts` con host **`faro`** → S1 resuelve hostname
   POR LECTURA del fichero (agnóstico al nombre). Si O3 falla, S1 sigue verde
   con stub (costura no bloqueante).
2. **O↔M (mañana)**: O2 deja FICHA vacía y textos con placeholders GNU-honestos;
   Manus (03:00) llena prosa final contra esas claves desde la quest viva.
3. **T↔S**: T2 testea el roundtrip que S1 produce (sin tocar el mismo fichero
   que S1; T2 vive en `state/`/`engine/`).

---
*Plan firmado por Gwyndolin, Dark Sun — 11:00 07/09. La higiene no retrasó el
plan: 1 residuo cazado, 4 marcas ELEGIDA en sitio, `INDICE.md` intacto.*
