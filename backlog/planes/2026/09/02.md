# PLAN — 2026/09/02 (Gwyndolin, Dark Sun)

> Estado de entrada a las 11:00, verificado por mí: suite base **466 passed /
> 0 xfailed** (replicada esta madrugada por Oscar 05:00 y Havel 07:00; mi
> ejecución: 6 bloques de puntos, cero F/E, `[100%]`). Gate de datos **21
> conceptos / 20 quests**. Manus entregó esta madrugada **M1 censo**
> (`CENSO-LISTA.md`) + **M2 cap. 6 «Faro» + fragmento 6** (`[HECHO]` en
> activo.md; los archiva Gwyn esta noche — yo NO los toco).
> La madrugada dejó **CICLO verde sin bugs de camino**: los hallazgos son una
> decisión de gate (🧭14, para Gwyn) y dos ideas P2 de integración (Havel).

## Tabla de verificaciones (regla de Gwyn: comando + resultado al lado)

| Suposición | Comando | Resultado (11:00) |
|---|---|---|
| Suite base 466/0 | `pytest src/ -q` desde raíz | 6×72 puntos + 34 = cero F/E, `[100%]` (réplica 466/0 de Oscar/Havel) |
| PR #16 abierta (rama viva) | `gh pr list` / `git branch -a` | **#16 OPEN**; rama `feat/engine-2026-09-01` en origin, 2 commits propios |
| El fix de #16 es la receta exacta de Artorias | `activo.md` bloque 💥 | Presente (test_errores + test_sala_sudo, 478 en combinada) |
| `kill` NO existe en el sandbox | `grep -rn kill src/core/sandbox/commands/` | Vacío (v0 parte de cero) |
| Familia conteo es ISLA (dato de Havel) | `grep DEFAULT_CH6_COMMANDS shell.py`; `ls generator/`; quests ch6 en curriculum.json | 0; solo chapter0/chapter2; **0 quests ch6** |
| Render v0 parte de cero | `ls src/render/` | Solo `README.md` + PLAN (sin código) |
| El resolvedor de textos EXISTE (para O4) | `ls src/data/textos.py` | Existe (T1, PR #18) |
| La quest sudo del cap. 3 YA está en main | curriculum.json (S1, PR #17) | `c.sudo` + story.ch3.e4/e5 presentes (gate 21/20) |
| La tarifa de ruido vive en sandbox (Ornstein no necesita curriculum para kill) | `grep noise shell.py` | `NOISE_PROFILE` en `core/sandbox/noise.py` |
| 🧭14 sigue SIN decisión de Gwyn | `abierto.md` línea 🧭14 | Presente como `[PENDIENTE][P2]` |
| main y origin/main sincronizados, árbol limpio | `git fetch && git status` | Limpio; turnos de madrugada ya pusheados |

## Higiene del backlog (hecha a las 11:00, antes de planificar)

- **1 residuo retirado con nota de traza**: la línea «🧭12 RESUELTA» (claves
  `postmortem.auditor.*`) pedía archivado en `abierto.md`; ya vive en
  `hecho/2026-09.md` (T1, PR #18) y Oscar la verificó hoy resolviendo con args
  reales → retirada de `abierto.md` SIN duplicar archivo (regla del 5.º residuo).
- **1 prioridad/pesca al día**: 🧭14 queda marcada «EN VENENO para Gwyn esta
  noche» en su línea — NO la planifico como tarea ejecutable (decisión suya).
- **Marcas ELEGIDA en sitio**: las 2 ideas P2 de Havel del 02/09 (sala-trampa
  pipe-0 → S2; des-islar el conteo → O3+S2) marcadas en `abierto.md`.
- **Verificado limpio por grep**: cero estados contradictorios con su carpeta
  (`hecho/`, `descartado/`, `mejoras/aplicadas/`); cero marcadores residuales
  (`=======`) en huellas; `mejoras/pendiente/` con 0 `[NUEVA]`/`[EN REVISIÓN]`;
  sin `[P0]` (correcto) y la única `[P1]` (deploy Vercel) sigue bloqueada por el
  build web — su desbloqueo natural es T1 render v0, HOY.
- **activo.md**: solo `[HECHO]` de Manus a la espera del archivado nocturno de
  Gwyn (regla: no los toco). Mi sección nueva del 02/09 añadida ARRIBA de las
  asignaciones, secciones históricas intactas.
- **INDICE.md**: sin cambios de estructura → intacto.

## Ramas/PRs huérfanas

- **PR #16** (`feat/engine-2026-09-01`, OPEN desde ayer, retenida por 2 tests
  stale que rompen la combinada): su cierre es la **tarea 1 de Ornstein** (fix
  EXACTO ya recetado por Artorias en `activo.md`/PR). Retención limpia: la rama
  se mantiene; `--delete-branch` jamás en PR retenido (lección de Gwyn).
- Sin más ramas ni PRs abiertos. Sin huérfanas de otros ejecutores.

## Asignaciones de HOY

### Ornstein (13:00) — rama `feat/engine-2026-09-01` (fix) + `feat/engine-2026-09-02` (nueva, apilada sobre la 0901 tras el fix)

- **[P1] O1·FIX — Desbloquear PR #16: arreglo de los 2 tests stale** (receta
  EXACTA de Artorias en `activo.md`).
  - *Módulo:* `src/tests/…/test_errores.py` + `test_sala_sudo.py`. Realineación
    obligatoria a origin/main ANTES (gate de rama).
  - *Qué:* (1) renombrar `test_chapter3_curriculum_real_sin_quest_sudo_generator_error`
    → `test_chapter3_curriculo_real_genera_sala_sudo` (assert `inc.chapter == 3`
    y `inc.room.id.startswith("room-ch3-")`); (2) en `test_sala_sudo.py` sustituir
    el caso stale por el positivo (`generate(1, 3, curriculum=load_curriculum())`
    → sala CON la credencial) y conservar el guard de honestidad con currículo
    sin quest sudo (`_real_sin_quest_sudo()` → `GeneratorError`).
  - *AC:* suite de la rama realineada = **478 passed / 0 xfailed** (466 de main
    hoy + 11 propios de #16 + 1 neto del test dividido); re-push de la rama +
    comentario en el PR con el delta nuevo («antes 466 · rama 478 · +12»); NADA
    más en este PR (fix mínimo → mergeable). Gwyn lo mergea esta noche.
- **[P2] O2 — El cap. 3 «Bombas» REAL: `chapter3.py` consume la quest sudo.**
  *(dirección #1 de Gwyn: «desbloquea que la sala del cap. 3 sea generable de
  verdad»; la pieza que conecta la sala-credencial de #16 con el currículo que
  ya vive en main)*
  - *Módulo:* `src/core/generator/chapter3.py` (nuevo, estilo chapter0/chapter2)
    + sus tests. NO toca `curriculum.json` (la quest story.ch3.e4/e5 ya está en
    main; si falta un campo de sala, lo informa en `abierto.md`, no lo inventa).
  - *Qué:* `generate(seed, chapter=3)` produce la sala del cap. 3 reutilizando
    la sala-credencial de O1 (credencial + `auth.log`); validación canónica con
    guard de honestidad (sin quest ch3 en el currículo → `GeneratorError`
    accionable, mismo patrón que existe hoy).
  - *AC:* determinismo por seed; **regresión `generate(seed,0)` byte-idéntica**
    (model.py: el fichero más compartido del core — tocarlo solo si imprescindible
    y verificado explícito); guard testado; suite verde + delta declarado.
- **[P2] O3 — Des-islar el cap. 6 (mitad generator): `chapter6.py` con la sala-
    dato de la Lista + el cebo pipe-0.** *(mitad de las 2 ideas P2 de Havel del
    02/09 — ELEGIDAS; la otra mitad es S2 de Smough. Esto es el O1↔S1 de hoy)*
  - *Módulo:* `src/core/generator/chapter6.py` (nuevo) + tests. Rama 0902.
  - *Qué:* la sala-dato `story.ch6.e1` (E1 de Manus): FS con `registro.csv` y
    `purgas.csv` al formato EXACTO de `backlog/historia/CENSO-LISTA.md`
    (delimitador `|`, fila `PR-0091` con fecha en blanco, pista `grep 000`), y
    el **cebo pipe-0** de Havel/Gwyn: un fichero/nombre trampa que produce un
    conteo 0 falso con `grep … | wc -l` (nombre mal escrito o columna
    desplazada) — el «0 miente». Guard de honestidad si la quest no existe.
  - *⚠️ LECCIÓN de los 2 tests stale de ayer:* el test que asuma la quest REAL
    `story.ch6.e1` va con `skipif` (la pondrá Smough esta tarde, PR distinto) —
    prohibido testear en firme datos que aún no están en main. La costura la
    verifica Artorias en la combinada.
  - *AC:* sala generable con currículo-en-memoria (quest conforme al CONTRATO
    ch6 de este plan); guard testado; regresa `0`/`2` intactos; suite verde +
    delta.
- **[P3] O4 — OPCIONAL (solo si la franja rinde; si no, primera de mañana):
  el cierre del encargo HABLA — el post-mortem imprime la voz resuelta.**
  *(dirección #3 de Gwyn: «pieza pequeña, gran dopamina»)*
  - *Módulo:* `src/core/engine/postmortem.py` (y el ciclo de cierre que imprime).
  - *Qué:* tras construir `line_key`+`args`, resolver vía `core.data.textos.resolve`
    (core→data: import permitido por ADR-0001) y exponer el texto resuelto
    (p. ej. `lines_resolved`) que el REPL ya puede imprimir sin render. La voz
    «Expediente 000…» se OYE hoy sin UI.
  - *AC:* post-mortem del cierre de cap. 0/2 devuelve texto resuelto ejecutable
    en Python; fallback honesto si una clave no resuelve (clave cruda, nunca
    crash); suite verde + delta.

### Smough (16:00) — rama `feat/sandbox-2026-09-02` (PR único; S1 y S2 comparten `shell.py` → encadena commits)

- **[P2] S1 — `kill`/señales sobre el par ceniza/censo (v0).** *(dirección #4
  de Gwyn; idea P2 de Havel 01/09 con AC en su idea)*
  - *Módulo:* `src/core/sandbox/commands/` (handler nuevo, p. ej. `senal.py`) +
    `noise.py` (tarifa). NO toca `curriculum.json` (concepto `c.kill` y quest:
    tarea futura — lo declara en su README) ni `karma/` (sin contabilidad aún).
  - *Qué:* `kill [-9|-HUP] <pid>` sobre los procesos de la sala (el par
    ceniza-521/censo-522 existe en el cap. 3): default SIGTERM; `-9` mata;
    `-HUP` reinicia con config distinta (efecto visible en `ps`/`env`). Emite
    evento al bus (`sandbox.signal`, payload pid/señal) para que karma/escenas
    lo consuman DESPUÉS. La bifurcación kármica de Havel (matar=rojo /
    reconfigurar=azul) es la DIRECCIÓN; v0 = física + evento, sin bandas.
  - *AC:* golden GNU honesto (`kill: (522) - No such process`, exit 1 si no
    existe); `-9` vs `-HUP` con efectos distintos observables en `ps`;
    regresión de los tests del cap. 3; gate 127 intacto en cap. 0/2; suite
    verde + delta.
- **[P2] S2 — Des-islar el conteo (mitad currículo/sandbox): quest `ch6.e1` +
    `DEFAULT_CH6_COMMANDS`.** *(idea P2 de Havel «la familia conteo aún es una
    isla» — ELEGIDA; completa O3)*
  - *Módulo:* `src/data/curriculum.json` (quest `story.ch6.e1` — ÚNICO dueño de
    este fichero hoy) + `src/core/sandbox/shell.py` (`DEFAULT_CH6_COMMANDS`
    con head/tail/sort/uniq + familia ya viva en cap. 6).
  - *Qué:* quest conforme al CONTRATO ch6 (abajo): objetivo contar/verificar la
    purga `PR-0091` sobre registro/purgas; `requires` SOLO conceptos vivos
    (familia conteo + pipes; **`cut` NO es jugable — no lo exija**; la pista
    `cut -d'|'` de CENSO-LISTA queda para cuando exista el comando).
  - *AC:* `load_curriculum()` → **21 conceptos / 21 quests** (gate de datos de
    Artorias); la quest valida con prereqs vivos; suite verde + delta.

**CONTRATO ch6 (costura O3↔S2, la escribo yo como hice con O1↔S1):**
quest `story.ch6.e1` · chapter `6` · tints grey · `requires` = familia conteo
(c.head/c.tail/c.sort/c.uniq) + conteo wc/grep ya vivos · objetivo: revelar la
purga `PR-0091` (el «número que sobra» de E1 de Manus, pista `grep 000`) ·
sala: `registro.csv` + `purgas.csv` formato CENSO-LISTA.md + cebo pipe-0 ·
literales compartidos: nombres de ficheros, fila `PR-0091`, cabecera `|`.
Verificación de la costura: ensayo de integración de Artorias (el flujo
listar→abrir→jugar→cerrar del cap. 6 completo).

### Seath (19:00) — rama `feat/meta-ui-2026-09-02`

- **[P2] T1 — RENDER v0: una sala del cap. 0 pintada.** *(dirección #2 de Gwyn:
  «desbloqueo natural del deploy [P1]»; reserva de Gwyndolin confirmada ayer)*
  - *Módulo:* `src/render/` + `src/assets/` (fuente bitmap 5×7 existente).
    ÚNICO importador de pyxel (ADR-0001); capa delgada: dibuja estado, cero
    lógica; SOLO LEE core (si necesita un dato que core no expone → `abierto.md`,
    no lo cambia).
  - *Qué:* componer la pantalla de UNA sala del cap. 0 generada por el generator
    (seed fija) a partir de estado real (no hardcoded): línea(s) de sesión con
    la fuente rasterizada + **prompt con la convención diegética
    `usuario@nodo:/ruta$` usando la ruta real del cwd** (avanza 🧭13 gratis —
    decisión de Gwyn ya firmada).
  - *AC:* (1) demo reproducible que genera la sala y pinta; (2) evidencia visual
    (screenshot PNG) committeada para validación a ojo de Artorias/Gwyn; (3)
    suite verde sin tocar `src/core` (delta mínimo: tests de composición
    offscreen si pyxel lo permite; si no, lo declara y la evidencia es el
    screenshot + smoke de import); (4) delta declarado.

### Manus (03:00) — colchón ya entregado

M1 (CENSO-LISTA.md) + M2 (cap. 6 + fragmento 6) `[HECHO]` esta madrugada. El
plan de hoy los consume (CONTRATO ch6 nace de M1; E1 de Manus es O3/S2).
Archivado: Gwyn esta noche. Manus no tiene tarea pendiente adicional hoy.

## Decisiones que NO cambio hoy (veneno para Gwyn, 23:00)

- **🧭14 — el sudo: (a) llave ambiental vs (b) exigir LEERLA.** Opciones en la
  línea de `abierto.md` (Oscar: datos + circuito verificado; Havel corrobora
  como jugador). Mi lectura como estructurador: **(b) protege el beat
  «aprender por necesidad»** del cap. 3 y el coste es bajo (marca de sesión en
  `escalada.py` + `state/`), PERO tocaría sandbox=Smough → NO planificable hoy;
  si (b), entra en el plan de mañana como tarea de Smough. La vigencia de la
  llave (idea P3 de Havel) conversa con (b) sin contradecirla.
- **Acusación verificable del Auditor** (recámara de Gwyn): no la planifico;
  O4 opcional de hoy es su precursor natural.
- **`cut` y `sort -k12`**: conceptos/pistas del cap. 6 que exigen comandos no
  vivos — honestidad de prereqs: NO entran hasta que su comando exista.

## Cola de referencia (no planificadas hoy)

Red simulada cap. 4 [P2] (forma firmada §6.1, encargo de Smough cuando toque) ·
sesiones multi-operador, `-k`/`uniq -c` como mecánica, tee/less (P3) ·
calibración `noise_budget=12` con harness (viva, O3 del 29/08) ·
«logro Trabajo en frío» y demás ideas P3 de Havel sin fecha.

## Relevo hora a hora

- **13:00 Ornstein:** O1·fix (realinea 0901 → fix → suite 478 → re-push/comentario
  en PR #16) → rama 0902 apilada → O2 (chapter3) → O3 (chapter6 con skipif) →
  O4 si sobra franja. PRs: #16 actualizado + PR nueva (0902).
- **16:00 Smough:** S1 (kill) → S2 (quest ch6.e1 + DEFAULT_CH6_COMMANDS). Un PR.
- **19:00 Seath:** T1 (render v0 + screenshot). Un PR.
- **21:00 Artorias:** ensayo de integración con **4 ramas** (0901-fixed, 0902,
  sandbox, meta-ui). Costuras a verificar: O3↔S2 (cap. 6 jugable completo) y el
  cap. 3 real sobre generator (re-jugar la zona sudo sin el FS del test — deuda
  de Oscar). Gate de datos: **21/21**. Suite esperada: **478 + Σ deltas
  declarados** (la aritmética sale de los deltas de cada PR, no a mano: 478 =
  main tras #16 con fix; sobre esa base suman los deltas de 0902/sandbox/meta-ui).
- **23:00 Gwyn:** mergea #16 PRIMERO (fix verificado), luego el resto según
  veredictos; archiva M1/M2 de Manus (inventario completo del activo); decide
  🧭14; consumes zona 🔬 y notas 🎯.

## Coherencia

- DESIGN §6.1 (sudo ganado/credencial en el mundo) intacto — O2 solo lo
  hace generable de verdad. §2.4 (voz del Auditor) avanza con O4. §4.4.3
  (concepto por necesidad): el cebo pipe-0 y la sala-dato de la Lista son
  exactamente ese método. §6.6.4/§9: el cap. 6 nace del doc M1 de Manus — la
  narrativa manda el DATO, el código lo materializa. Sin contradicciones con
  historia: la fila `PR-0091` y `HOSP-47-C` se cruzan como dejó Manus.
