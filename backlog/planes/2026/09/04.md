# PLAN — 2026/09/04 (Gwyndolin, Dark Sun)

> **Contexto de la mañana:** main `93afee6`, suite 529/21-21, sin PRs ni ramas
> huérfanas (verificado `git branch -a` + `gh pr list`). Ayer entró el mayor
> desbloqueo externo: **URL pública** https://cyberroot-psi.vercel.app (T1),
> el sudo se GANA LEYENDO (S1) y el demonio vive en el generator real (O1).
> Sin entrada de Oscar a las 05:00 hoy (ver relevo a Gwyn al final).

## Dirección consumida (Gwyn 03/09, 23:00)

1. **El recurso escaso ahora es el feedback HUMANO**: Juanma ya puede jugar en
   el navegador. El plan es LIVIANO y deja hueco — si Juanma juega esta noche,
   su feedback vale más que otra sala y mañana el plan pivota hacia él.
2. **Web slice 2**: selector de capítulo + bucle de muerte (la lección completa
   del cap. 3 jugable en el navegador + post-mortem visible).
3. **Acusación verificable del Auditor** (pieza de recámara madura): el
   post-mortem citando pruebas — hoy aterriza la mitad `read_marks`.
4. Ideas P2 de Havel en cola; el gate y el demonio cerraron las dos «islas» v0.

## Tareas de HOY (4, todas pequeñas, sin colisiones)

### O1 — Ornstein (13:00, rama `feat/engine-2026-09-04`) — El Auditor cita lo que LEÍSTE

- **Módulo:** `src/core/engine/postmortem.py` + `src/data/textos.json` +
  tests (`src/tests/core/engine/`, `src/tests/data/test_textos.py`).
- **QUÉ:** idea P2 de Havel (04/09) — el post-mortem consume `read_marks`
  (ya viaja en `Shell.to_dict()` desde S1, shell.py L407) como SEGUNDA fuente
  de verdad tras el historial. Regla v0 (sin imports de sandbox): si el
  `history` de la sesión contiene al menos un `sudo`:
  - `read_marks` no vacío → línea nueva con clave `postmortem.auditor.lectura`
    y args `{path}`: «leyó la orden antes de elevar» citando la ruta leída
    (forma formulario §2.4 — dato, nunca moralina).
  - `read_marks` vacío → variante «elevó sin leer ninguna orden» (en un mundo
    con credencial delata prisa; en un mundo sin ella, no dispara).
  - Sin `sudo` en history → SIN línea nueva (cap. 0/2 no ganan texto).
- **Criterios de aceptación:**
  1. Sesión cap. 3 (`cat orden → sudo`) → el informe añade la línea de
     lectura con la ruta exacta; la voz resuelve (nunca `line_key` crudo).
  2. Sesión con `sudo` sin lectura previa → variante «a ciegas».
  3. Sesión cap. 0 (sin `sudo`) → informe byte-idéntico al de hoy.
  4. Roundtrip y determinismo intactos; suite completa verde con delta
     declarado (base 529).
- **NO tocar:** `src/core/sandbox/` (S1 no cambia), `web/`, `curriculum.json`.

### S1 — Smough (16:00, rama `feat/sandbox-2026-09-04`) — `cut` en el sandbox: la Lista es una tabla cortable

- **Módulo:** `src/core/sandbox/commands/` (handler de `cut`) +
  `shell.py` (`DEFAULT_CH6_COMMANDS` añade `cut`) + `src/data/curriculum.json`
  (concepto `c.cut`) + tests de sandbox.
- **QUÉ:** idea P2 de Havel (03/09) — `CENSO-LISTA.md` (M1) pone como pista
  real de Manus `cut -d'|' -f4,12 | uniq -c` y hoy ese comando no existe (14
  cmds en el cap. 6). Semántica GNU honesta: `-d` delimitador, `-f` lista de
  campos con rangos (`f4,12`, `f1-3`), salida con el mismo delimitador; línea
  sin delimitador se imprime entera; sin `-f` → error GNU
  («you must specify a list of bytes, characters, or fields», exit 1). Ruido
  de clase lectura (igual que `head`, ≤ que `grep`) — lo fija Smough por
  coherencia con la familia.
- **Criterios de aceptación:**
  1. `cut -d'|' -f4 purgas.csv` → columna de estados (`EN BLANCO`…);
     `cut -d'|' -f4,12 | uniq -c` ejecuta la pista de M1 tal cual.
  2. Gate de datos 22/22 (`c.cut` con prereq razonable dentro de la familia
     texto/pipes del cap. 6; el DAG valida).
  3. Gate 127 intacto en cap. 0/2 (`cut` NO en `DEFAULT_CAP0_COMMANDS` ni
     `DEFAULT_CH2_COMMANDS`).
  4. Determinismo por seed intacto; delta declarado sobre 529.
- **NO tocar:** `engine/`, `web/`, `textos.json`. La quest que use `cut`
  (E2/E3 del Faro) queda en recámara — solo el verbo y el concepto.

### T1 — Seath (19:00, rama `feat/meta-ui-2026-09-04`) — Guardián de la puerta: test de frescura del bundle

- **Módulo:** `src/tests/web/test_bundle_fresco.py` (nuevo; leer
  `web/bundle/core.json` y `tools/web/build_bundle.py`).
- **QUÉ:** idea P2 de Havel (04/09) — NINGÚN test guarda el bundle hoy
  (grep en `src/tests` → 0). El test reconstruye el manifest en memoria desde
  `src/core/` actual y lo compara con `web/bundle/core.json` commiteado;
  divergencia → fallo con mensaje «bundle stale: regenera
  (`python tools/web/build_bundle.py`) y commitea». Primer día en que la
  puerta pública entra al smoke del conjunto.
- **Criterios de aceptación:**
  1. Verde sobre main actual (el bundle de ayer está fresco).
  2. Mutación local de cualquier fichero del core sin regenerar → rojo con
     mensaje accionable (comprobado en el turno, no commiteado).
  3. Solo añade tests; NO toca `web/` ni `src/core/`; corre en el smoke.
- **⚠️ Nota de integración para Gwyn (23:00):** si el `cut` de Smough entra
  antes de tu ensayo, ESTE TEST GRITARÁ — y está bien: es el guardián
  funcionando. Arreglo: regenerar el bundle y commitear en el ensayo.

### T2 — Seath (19:00, misma rama, DESPUÉS de T1) — Web slice 2: semilla por URL, capítulo elegible y bucle de muerte

- **Módulo:** `web/app.js` + `web/index.html` (+ regenerar bundle si toca).
  Cero cambios en `src/core/` (el bundle ya lleva el core entero).
- **QUÉ:** dirección #2 de Gwyn + idea P3 de Havel (`?seed=`):
  1. `?seed=` y `?chapter=` parseados de la URL → `generate(seed, chapter)` +
     `Shell(DEFAULT_CHx_COMMANDS)`; mínimo cap. 0 (hoy) y cap. 3 — la lección
     COMPLETA jugable: leer la orden → ganar sudo → ver el par 521/522.
  2. Bucle de muerte: `total_noise > noise_budget` → pantalla post-mortem con
     `build_postmortem` (la voz del Auditor en el navegador) + reiniciar
     (misma seed o nueva). «Morir es el método de estudio» llega a la puerta.
  3. El status muestra seed y capítulo activos → cada bug reporta su
     reproducción en la propia URL.
- **Criterios de aceptación:**
  1. `/?chapter=3&seed=42`: `cat` de la orden gana la marca; `sudo` sin leer
     rechaza (gate S1 visible); tras leer eleva y firma; `ps aux` muestra
     ceniza/censo; `kill -9 522` borra.
  2. Exceder el presupuesto → post-mortem con `auditor_text` resuelto, nunca
     clave cruda; reiniciar funciona.
  3. Sin query string = comportamiento de hoy byte-idéntico (no-regresión).
  4. Verificación en navegador real (Chromium) o local con http.server; si el
     entorno no tiene navegador, declarar el hueco de cobertura honestamente
     (precedente de ayer) y que Gwyn lo verifique con el suyo.
- **NO tocar:** `src/core/`, `src/data/`, ramas de Ornstein/Smough.

## Recámara (NO planificada hoy, orden por desbloqueo)

- **Red simulada del cap. 4** (P2 del 31/08, forma firmada §6.1): la pieza
  grande pendiente — fraccionar mañana (hosts como FS + `ssh` básico como
  pieza 1 de Smough) si el feedback de Juanma no pide otra cosa.
- **Karma del par 521/522** (Havel 04/09): espera a que el detector de
  patrones tenga dueño y la quest E3 esté integrada.
- **Auto-deploy por git push** (nota de Seath ayer) — el siguiente paso
  natural de la puerta, barato de planificar cuando toque.
- **E2/E3 del Faro + `sort -k12`/`uniq -c`**: las salas-dato siguientes,
  con `cut` ya disponible tras S1.

## Hueco para el feedback humano (dirección #1 de Gwyn)

Si Juanma juega la URL y deja feedback (libreta o mensaje), mañana el plan
PIVOTA: sus hallazgos mandan sobre la recámara. El plan de hoy deja hueco
a propósito: Smough solo tiene una pieza pequeña, nadie pinta salas nuevas.

## Reparto sin colisiones

| Tarea | Dueño | Rama | Rutas |
|---|---|---|---|
| O1 read_marks | Ornstein | `feat/engine-2026-09-04` | `engine/`, `data/textos.json`, sus tests |
| S1 cut | Smough | `feat/sandbox-2026-09-04` | `sandbox/`, `curriculum.json`, sus tests |
| T1+T2 web | Seath | `feat/meta-ui-2026-09-04` | `src/tests/web/`, `web/`, `tools/web/` |

Rutas disjuntas. Único punto de encuentro: el bundle (nota de integración arriba).
Orden de merge sugerido para Gwyn: engine → sandbox → meta-ui (como ayer);
T2 regenera el bundle al final y absorbe el `cut` de Smough sin drama.
