# PLAN — CyberRoot (viernes 18/09/2026)

**Gwyndolin, Dark Sun** — planificador 11:00.

Base verificada 11:00 (comandos ejecutados, no suposiciones):

| Suposición | Comando | Resultado |
|---|---|---|
| Suite base | `pytest src -o addopts= -q` | **749 passed / 0 failed** |
| Gate de datos | `load_curriculum()` | **24 conceptos / 31 quests** |
| PRs abiertos | `gh pr list` | **ninguno** (#62/#63/#61 mergeados 17/09) |
| Ramas de trabajo | `git branch -a` | sin PRs abiertos; **4 ramas locales 09-15/16 aún no borradas** (`feat/engine-2026-09-15/16`, `feat/meta-ui-2026-09-15`, `feat/sandbox-2026-09-15/16`) — borrado de limpieza, turno GWYN hoy |
| Estado del árbol | `git status` | limpio; main == origin/main (`c18fe43`) |
| Bundle | guardián | verde, 48 ficheros |
| Mapa del repo | lectura | sesiones por `abrir_encargo` (session.py), post-mortem con `volcado: rescatado/caducado` (O1 15/09, fields `postmortem.volcado`/`auditor_volcado`), quest `story.ch5.e2` grey `('c.cat','c.scp')` ya viva en `curriculum.json` + textos |

## Dirección del día (de las notas 🧭13/09-18/19 y 🎯 Gwyn 17/09)

Cadena e3→dato7 ya cerrada como MUNDO (testigo condicional validada
como drama, no bug — 🧭36/37 CERRADAS). La pieza con más ROI — y la
que Gwyn señaló explícitamente como P1 para Gwyndolin — es:

> **Que el cap. 5 «Subestación» sea JUGABLE por la puerta normal
> (`abrir_encargo`), leyendo la geografía condicional con el flag
> heredado del post-mortem e3.**

Solo e2 abre HOY (la quest que el FS del asalto ya soporta —
`custodia` canon). Las quests `ch5.e1/e3/e4` quedan RECÁMARA (su
scaffold de canon steps guiados sufrirían muros por allowlist, fuera
de la pieza del día).

---

## ASIGNACIONES (Ornstein 13:00 · Smough 16:00 · Seath 19:00)

### O1 — Ornstein (13:00) · rama `feat/engine-2026-09-18` · session ch5 (P1)

**Módulo:** `src/core/engine/session.py` + tests. NO toca
`postmortem.py`, NO toca `curriculum.json`, NO toca `shell.py`
(la nueva allowlist la crea Smough, pieza S1).

1. `SUPPORTED_CHAPTERS: {0,2,4}` → `{0,2,4,5}`.
2. Mensaje de error de `listar_encargos` (L113-116) que aún dice
   `v0: 0, 2 y 4` → actualizar a `0, 2, 4 y 5`.
3. `_commands_for(5)` → devuelve la tupla `("cat", "scp")` de la
   allowlist nueva que CREARÁ Smough en S1 (`DEFAULT_CH5_COMMANDS`).
   Como aún no existe dentro de la rama de Ornstein, consume con
   fallback/honestidad (try/except o constante local equivalente) y
   el TEST SÍ exige exactamente `("cat", "scp")`.
4. Solo e2 abre en cap. 5: si `quest_id` empieza por
   `story.ch5.` y no es `story.ch5.e2`, devolver
   `{"abrible": False, "missing": ["encargo sin flujo materializado en cap. 5 (hoy solo e2)"]}`
   — patrón idéntico al guard de `SUPPORTED_CHAPTERS`.
5. **flag `volcado_rescatado`** (pieza clave de la cadena e3→Faro→
   Subestación): `abrir_encargo` acepta un kwarg opcional
   `volcado_rescatado: bool = False` (determinista y honesto: sin
   flag, la sala nace CADUCADO — el default no engaña) y se lo pasa
   a `generate(seed, 5, contract_id=quest_id,
   volcado_rescatado=flag)`.
   - Expone además un helper público `volcado_del_save(pm) -> bool`
     que lee `pm.get("volcado") == "rescatado"` de un post-mortem ya
     construido — el caller que cierre e3 puede reusar el mismo
     dict del Hub sin brujerías internas.
6. `Shell` montado con la allowlist ch5 y con `snapshot()` del FS
   del asalto igual que lo hace `new_session` en `_generate_cap5`
   (hoy en el generator, no en session — esa es la diferencia que
   este PR crea).
7. Tests `src/tests/core/engine/test_session_ch5.py` (5–6):
   - SUPPORTED_CHAPTERS incluye 5; `_commands_for(5)` == ("cat","scp").
   - `listar_encargos(cur, 5)` devuelve 4 encargos (e1..e4) y solo e2
     `abrible` (los otros 3 `falta`).
   - `abrir_encargo(c, 'story.ch5.e2', {'c.cat','c.scp'},
     run_seed=42, volcado_rescatado=True)` → `abrible True`;
     `session.ejecutar('cat /tmp/volcado-custodia.csv')` exit 0 con
     `TR-003|faro|troncal-01|512|EN_COLA`.
   - misma sesión sin flag (`volcado_rescatado=False`) →
     `cat /tmp/volcado-custodia.csv` exit 1 con
     `No such file or directory` (la ausencia honesta).
   - `rechazo_accionable` para e1/e3/e4 nombra `missing` y NO genera
     sala (retrocompatible con la exploración del Hub).
   - Determinismo ×2 seeds byte-idéntico por `to_dict()` del FS
     (mismo estándar que #62 ya probó para generator puro).
8. Criterio de aceptación (AC): suite 749→**753–755** declarada como
   delta `tests antes / tests rama / delta`; flexible `<=` en los
   asserts de gate; INTACTA la allowlist `DEFAULT_CH4*` de ayer;
   bundle — NO genera (no toca `src/data/`, delta de directivas de
   la regla 12/09); solo `session.py` + tests nuevos.

### S1 — Smough (16:00) · rama `feat/sandbox-2026-09-18` · la lente física (P2)

**Módulo:** `src/core/sandbox/shell.py` + `src/core/engine/postmortem.py`
(header único, ver costura abajo).

**Qué hace (criterios):**
1. Define en `shell.py`:
   `DEFAULT_CH5_COMMANDS: tuple[str, ...] = ("cat", "scp")` — la
   allowlist del cap. 5, 2 comandos — la pieza sesionada por O1 hoy
   a través de `_commands_for(5)`.
2. Detector del post-mortem: `_has_volcado_custodia(shell_dict)`
   hermano de `_has_volcado_rescate` — True si `history` contiene
   `cat /tmp/volcado-custodia.csv` con exit 0. Añade a
   `build_postmortem` la línea `postmortem.auditor.custodia`
   (nueva clave en `textos.json`, disjunta: la **línea** del
   Expediente 000 dice `custodia leída en casa: el testigo viajó.
   Continuidad del ensayo: estable.`) sin romper `auditor_espejo`
   ni `volcado` — la ficha del Auditor pasa a aceptar 2 gestos con
   2 fuentes (fiel a la tríada que el informe ya firma).
3. Tests `src/tests/core/sandbox/test_ch5_custodia_pm.py` (3–4):
   con cat custodia → 2 líneas resueltas pico+custodia; sin cat →
   byte-idénticos a hoy; determinismo history.
4. AC: suite 749+6+3 → **755–756**; **ALLOWLIST OWNER: SMOUGH** en el
   sentido 12/09 (el que TOCA el test base) — pero exacto: hoy
   **nadie toca asserts de `DEFAULT_CH4*` existentes**; Smough crea
   una constante NOVA que no choca con guard SUBSET. Quien mergea
   toma el plan de costura engine→sandbox→meta-ui como dictado.
5. Regenera bundle al final (toca `src/data/textos.json` — regla
   **12/09**: SOLO quien toca `src/data/` regenera → **Smough**
   ejecuta `python tools/web/build_bundle.py` en su rama; Gwyn al
   cierre VERIFICA guardián verde superior a la conversión que ya
   hizo; si TOCÓ textos, regen en la mañana).

### T1 — Seath (19:00) · rama `feat/meta-ui-2026-09-18` · verificar `?chapter=5` (P2-δ)

**Módulo:** `web/app.js` SOLO + `web/index.html` (hint del cap. 5).
NO toca `src/`, NO regenera bundle.

**Qué hace (criterios):**
1. Si O1 aterrizó en main (SUPPORTED_CHAPTERS incluye 5): extender
   `parseParams` a los caps `[0,2,3,4,5,6]` y permitir `?chapter=5`;
   título + `<title>` habilitados solo si el cap. 5 existe.
   Panel «Subestación — custodia» como THIRD viva (`volcado-custodia.csv`)
   con el MISMO patrón que T1 16/09 usó para el rescate del Faro
   (`_isCustodiaPresent()` lente pura, lee `get_history`/`get_csv`,
   **NUNCA ejecuta pipe del core**).
2. Si O1 NO aterrizó: sin pieza hoy; PR body declara **delta 0
   honesto** (solo `node --check`) y el hueco queda registrado — NO
   se planta una lente muerta; el cap. 5 web se planifica MAÑANA.
3. AC: suite delta honesto +0..+2; consola limpia en
   `?chapter=4`, `?chapter=5` si aterriza la allowlist.

### Verificación cruzada y costuras del día

- **ALLOWLIST OWNER:** NADIE toca asserts de las allowlists constants
  existentes (`DEFAULT_CH4*`, `DEFAULT_CH4E3`, `DEFAULT_CH6`). La
  NUEVA `DEFAULT_CH5_COMMANDS` la crea Smough (16:00) como
  constante NOVA (no depende de nada previo). Ornstein la consume
  y declara `delta +N` honesto con fallback local idéntico — así
  la rama 13:00 es ejecutable ANTES de la de Smough sin choque de
  git.
- **GATE OWNER:** nadie toca el gate (no adds quests). La suite
  NO sube a 755 por add test-ch5 aunque el gate sea flexible;
  `<=32` sigue sin tocar — nadie toca asserts de curriculum.
- **Bundle:** hoy lo regenera SOLO Smough (toca `src/data/textos.json`),
  al final DE SU rama. Ornstein y Seath NO tocan `src/data/` ni
  `web/bundle/`.

---

## CIERRE DEL DÍA (Gwyn 23:00)

1. Merge O1 → S1 → T1 en orden dictado engine→sandbox→meta-ui.
2. Verificar suite ≥752 y guardián de bundle verde.
3. Borrado de limpieza: ramas locales/remote ya integradas
   `feat/engine-2026-09-15/16`, `feat/sandbox-2026-09-15/16`,
   `feat/meta-ui-2026-09-15` (borrar locales vía `git branch -d`;
   los remotos aún presentes se evalúan con `git ls-remote` antes
   de `git push origin --delete`). Las de 09-14 y anteriores, el
   Vigilante decide.
4. Conflictos de huellas: si `_commands_for` (O1) y
   `DEFAULT_CH5_COMMANDS` (S1) chocan, resolver por unión de
   terminal (`grep -c '^<<<<<<<'` = 0 antes de pushear; suite verde
   antes de cada commit de merge).

## Cola de referencia (NO planificado hoy; recámara)

- `karma.volcado.*` (ideas de Havel 17-18/09, P2/P3, pendientes de ADR
  de Gwyn; el día que la firme, el plan arranca ahí).
- `TRONCAL_STATIC` duplica el volcado — deuda de DUPLICACIÓN viva
  (6ª noche, vigilada en notas 🎯 17/09): despierta el día que Manus
  toque el volcado para narrativa.
- Quest `story.ch5.e3` (`ps` + `env` sobre el intruso 03:14) —
  RECÁMARA de mañana, coherente con la dirección de Oscar
  («cap. 5 lee el testigo con verbos ya dominados, sin verbos nuevos»).

## Contingencia del plan (por si el día falla)

Si O1 falla (💥 Ornstein), S1 puede vivir SIN la allowlist ch5 para
session: su detector del post-mortem no depende `_commands_for(5)`;
la mitad Smough deja el día con el detector nuevo y la rama propia
verde. Si S1 falla, Ornstein mantiene su delta limpio con su
allowlist local de fallback — el gate de la noche come ese valor
verificado por Artorias.
PENDIENTE hoy: si el día llega sin O1, Artorias anota el delta real
en el ensayo y Gwyn decide merge parcial (session SIN flag del
save NO planifica — queda para mañana).
