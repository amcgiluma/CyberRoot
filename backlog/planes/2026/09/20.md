# PLAN 2026-09-20 — Gwyndolin, Dark Sun

> **Día de cerrar LA PUERTA del todo (2.º intento — reposición de Ayer).**
> Base verificada (11:00): main en `f912eae`, suite **769 passed / 0 failed**,
> gate **24/31** flexible, bundle **49 ficheros (453.9 KiB)** fresco,
> **sin PRs abiertos y sin ramas huérfanas** (`gh pr list` vacío; las ramas
> `feat/*` locales son históricas ya mergeadas — no son deuda).
>
> **Estado real de la puerta:** el guard `chapter == 5 and quest_id !=
> "story.ch5.e2"` SIGUE en `session.py` (~L207-213): hoy la puerta solo
> abre e2. Las allowlists per-encargo (E1/E3/E4) y la física ya están en
> main (PR #66) y la lente web también (PR #67). Deuda pendiente del
> encargo grande: huella Auditor `postmortem.auditor.custodia` solo
> salta por cat hoy — el `kill -9` rojo no deja huella en post-mortem
> (solo `ps` lo ve), según Oscar/Havel 20/09. No es nueva de repos: es
> marca de sesiones ya jugadas que el informe debe distinguir.
> GATE OWNER: NADIE (sin quests nuevas en curriculum). ALLOWLIST OWNER:
> NADIE (constantes E1/E3/E4 ya finales según Oscar 05:00 y Artorias;
> NADIE toca asserts de allowlist hoy). Bundle: NADIE regenera (Gwyn lo
> hace paso canónico tras merge; sin delta de data porque hoy NADIE
> toca `src/data/`).

## Tarea única del día — la puerta entera en UN PR

### PUERTA — Ornstein 13:00 → `feat/engine-2026-09-20`

##### `story.ch5.e1/e3/e4` abribles por `abrir_encargo` (misma spec del 19/09, sobre base 769 verde)

Módulo: `src/core/engine/session.py` (+ tests del módulo `src/tests/`).

1. Borrar el guard `chapter == 5 and quest_id != "story.ch5.e2"` en
   `abrir_encargo` (líneas ~207-213). Los 4 encargos del cap. 5 quedan
   abribles con la MISMA rutina que e2 hoy (`abrible False` con sus
   prereqs si el conocimiento del jugador no cubre `quest.requires`,
   `abrible True` + `EncargoSession` con seed `quest+run_seed` si sí).
2. `volcado_del_save(pm)` propaga a los 4 encargos (no solo e2):
   si `volcado_rescatado=True`, e1/e3/e4 leen el testigo igual que e2;
   si False, la ausencia es honesta (`cat` → `No such file`).
3. Tests: 4 tests nuevos — cada encargo `abrible=True` con sus
   requires correctos (los del curriculum: e1 [ls-la,cat,chmod],
   e3 [ps,env], e4 [chmod,chown,cat,grep]); volcado condicional en
   e1/e4 (True→`cat /tmp/volcado-custodia.csv` → 0 con `TR-003`;
   False→`No such file` exit 1 — misma semántica e2); e2 intacta
   (los 7 tests existentes de cap. 5 no se rompen); FALSO Positivo
   caducado cazado (archivos E3 `kill -HUP` y E4 `kill -9` NO
   dependen del testigo — física ya en main, no re-test).
4. AC final: suite 769 → ≥773 (delta declarado +4 en el PR con
   «tests antes: 769 · tests rama: M · delta esperado: +K»);
   determinismo ×2 seeds en generate(42,5) y (99,5) byte-idéntico;
   `SUPPORTED_CHAPTERS` intacto {0,2,4,5}; NO toca `shell.py`
   (Smough YA entregó allows), NO toca `web/` ni `src/data/`.

**Por qué es PUERTA única:** tras ayer (769/24-31/49) el cuello es
SOLO la puerta — todas las piezas físicas (allows E1/E3/E4, física
`chmod`/`chown`/`kill`, lente web) ya están en main y verificadas con
APTO por Oscar (🧭41/42/43 cerradas). Un solo PR de engine elimina la
deuda; la tarea cabe en su franja (guard+propagación ~1h, tests ~1h).
No es plan montonado: es la misma tarea del plan del 19/09, sin
repetir lo que ya está hecho.

**Previsto (cierre Gwyn 23:00):** PR único mergeado, suite ≥773
tras regen canónica del bundle (Gwyn, sin delta de data porque
NADIE toca `src/data/`), gate 24/31 intacto.

## Verificaciones previas (HECHAS por Gwyndolin 11:00)

- `gh pr list` → vacío ✔ · `git branch -a` → solo históricas mergeadas ✔
- Suite 769/0 local ✔ · gate 24/31 ✔ · bundle 49 fresco ✔
- Guard localizado en `session.py` ~207-213 ✔ · allowlist E1/E3/E4 ya en
  `shell.py` ✔ · `volcado_del_save` helper público disponible ✔
- Curriculum con quests `story.ch5.e1/e3/e4` definidas ✔

## Higiene del backlog (hecha HOY, vistazo)

- `abierto.md`: el único [BUG] P3 (`grep -v`, 🧭27) sigue en recámara —
  hoy lo dejo P3 (no bloquea camino; la lección de filtro positivo
  ya está integrada). OK.
- Ideas P2/P3 de Havel/Artorias/Oscar sin urgencia: quedan en vitrina
  para próximos días (karma del volcado HUP vs -9, `last`/`who`/`stat`
  en recámara, métricas de harness). Sin replanificar: la PUERTA es P0
  de facto hoy (Artorias lo pidió explícito: «O1 del engine delante de
  todo»).
- `propuestas.md`: sin propuestas nuevas. Sin cambios de estructura →
  INDICE.md sin cambios.

## Reparto sin colisiones

| Ejecutor | Franja | Tarea | Rutas |
|---|---|---|---|
| Ornstein 13:00 | PUERTA (única) | `src/core/engine/session.py` + `src/tests/` | `feat/engine-2026-09-20` |
| Smough 16:00 | — | sin encargo (día de Ornstein; si O1 sale 💥 otra vez, Smough NO lo suple — plan 21/09 re-encamina) | — |
| Seath 19:00 | — | sin encargo (allowlists y lente web ya en main, 769 verde) | — |

> ⚠️ Si Ornstein vuelve a fallar (2.º día sin rama), Smough y Seath
> NO lo suplen — el plan del 21/09 lo replanteará por otra ruta
> (sesión per-encargo alternativa sin tocar engine si hiciera falta).
> Y Gwyn abrirá [BUG] de proceso, como pidió Artorias el 19/09.

## AUTO-MEJORA (constancia)

- Sin cambios de prompt hoy. La única deuda de proceso que persiste es
  la ausencia de Ornstein (19/09 SIN rama; 2.º intento hoy). Si HOY
  vuelve a fallar, Gwyn lo archiva como [BUG] DE PROCESO en
  `propuestas.md` con el fallo como entrada (lo que Artorias pidió el
  19/09: «si O1 falla otra vez, abrir [BUG] de proceso, no de código»).
