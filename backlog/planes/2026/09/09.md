# PLAN — 2026/09/09 (Gwyndolin, Dark Sun — 11:00)

> Base verificada EN VIVO a las 11:00: suite **635 passed / 0 xfailed** (`PYTHONPATH=src .venv/bin/python -m pytest src/ -o addopts= -q`),
> gate de datos **22 conceptos / 24 quests** (`load_curriculum()`), bundle **45 ficheros**, main `2fe0aa3`,
> **0 PRs abiertos**, 0 ramas huérfanas vivas (las 3 del 08/09 borradas tras merge MERGED). CICLO verde según Oscar/Havel.
> Verificado además: `src/core/generator/chapter4.py` NO existe · `DEFAULT_CH4_COMMANDS` NO existe → el cap. 4 nace HOY de cero, sin colisiones.

## Prioridad del día (dirección de Gwyn 08/09, punto 1): **EL CAPÍTULO 4 DESPIERTA**

La red Fase A+B ya es mundo (`/etc/hosts` con `faro` en `generate(42,6)`), copia (`scp` handler)
y save (roundtrip T2). La Fase B tiene TODO su suelo. Hoy nace la primera quest que CRUZA
capítulos: el Faro como prerrequisito del troncal, sin un solo concepto nuevo.
La prioridad [P1] del día (🪨 Havel 09/09: límite de 2 pipes) la sostiene.

## Asignaciones — 3 tareas, ejecutores disjuntos (regla: 1 tarea = 1 dueño = 1 rama = rutas disjuntas)

| # | Tarea | Dueño (rama) | Prioridad |
|---|---|---|---|
| O1 | Red del cap. 4 en el generator (`chapter4.py` + hosts 2-3 + allowlist) | **Ornstein** (`feat/engine-2026-09-09`) | P2 |
| O2 | Quest `story.ch4.e1` «La llave prestada» + `c.scp` en curriculum | **Smough** (`feat/sandbox-2026-09-09`) | P2 |
| T1 | Verificar en vivo el circuito ch4 + aviso de límite de pipes | **Seath** (`feat/meta-ui-2026-09-09`) | P3 |

### O1 — Red del cap. 4 en el generator: `chapter4.py` + hosts + allowlist `[P2]`
- **Módulo:** `src/core/generator/chapter4.py` (NUEVO) + `src/core/generator/generator.py` (`_generate_cap4` o dispatch equivalente) + allowlist de cap. 4 en `src/core/sandbox/shell.py`.
- **Descripción:** crear `build_chapter4_fs(seed)` y `generate(seed, 4)`: `/etc/hosts` con **2-3 hosts por seed** (`faro` 10.6.0.5 + `troncal-01` 10.6.1.10; comentarios `#` que el parser ya ignora), cada host con FS remoto determinista (el troncal con un volcado `volcado.csv`; el faro reusa la piel del cap. 6). `DEFAULT_CH4_COMMANDS` debe incluir los comandos de caps. previos + `ssh` + `scp` (🧭24 resuelta por diseño: ch4 nace CON red en su allowlist; el cap. 6 sigue 127, frontera deliberada intacta). Regla de mundo: la red se descubre LEYENDO (`cat /etc/hosts`), nunca listando.
- **Criterio de aceptación:**
  1. `generate(42,4)` existe, determinista (byte-idéntico en 2 llamadas) y con `/etc/hosts` de 2-3 entradas.
  2. Con `Shell(DEFAULT_CH4_COMMANDS, ...)`: `cat /etc/hosts` exit 0 descubre TODOS los hosts del fichero; `ls /etc` NO descubre (regla «leer descubre, listar no», ya pagada en S1 del 08/09).
  3. `scp faro:/... /tmp/` y `scp troncal-01:/... /tmp/` funcionan (handler S1 ya en main) sobre los FS generados.
  4. Suite 635→635+N (delta declarado; mínimo +3 tests: hosts múltiples, determinismo, allowlist ch4). `generate(42,6)` intacto byte-idéntico.
- **Costuras:** O↔S (Smough consume `generate(42,4)` + `DEFAULT_CH4_COMMANDS` — nombres EXACTOS en su PR, costura declarada aquí). No tocar `curriculum.json` (es de O2/Smough).

### O2 — Quest `story.ch4.e1` «La llave prestada» + concepto `c.scp` `[P2]`
- **Módulo:** `src/data/curriculum.json` (`c.scp` nuevo, prereq `c.cut` — el DAG de 🪨 Havel 07/09: «el `:` es un `|` que ya sabes cortar» + `story.ch4.e1` con `requires: [c.scp]`) + `src/data/textos.json` (`story.ch4.e1.*`: briefing/hint/post-mortem, rutas absolutas como manda 🧭15) + su validador/golden (solo si vive en rutas sandbox; si el validador vive en `engine/`, SOLO curriculum+textos y se deja constancia para Ornstein mañana).
- **Descripción:** primera quest de RED del juego (azul, prosa de `backlog/historia/CAPITULOS/04-troncales.md` §E1): leer `/etc/hosts` → descubrir hosts → `scp troncal-01:/srv/.../volcado.csv /tmp/` (o `faro:`). Golden respeta el límite de 2 pipes (🧭25): comandos de 1-2 pipes o pasos encadenados, NUNCA 4 eslabones en una línea. El briefing NOMBRA `/etc/hosts` («léelo») porque el rechazo de scp ya orienta ahí (ruido 0). FICHA de Manus vacía si el beat pide prosa que no existe aún.
- **Criterio de aceptación:**
  1. Gate 24→**25 quests** (`load_curriculum()`), 22→**23 conceptos** (`c.scp`), `story.ch6.e1/e2` + `dato2/dato3` intactos.
  2. Prereqs de `c.scp` = `[c.cut]` y el DAG sigue válido (el validador anti-DAG-ciclos ya en la suite no grita).
  3. Golden de `story.ch4.e1` exit 0 contra `generate(42,4)` de O1 (si O1 no está mergeado aún: probado en ensayo local combinado con la rama de Ornstein, o contra FS handmade declarando la costura).
  4. Suite 635→635+N (delta declarado; mínimo +2: gate de datos y golden e1).
- **Costuras:** S↔O (necesita `generate(42,4)`/`DEFAULT_CH4_COMMANDS` de O1 — si llegan tarde, S1 copia los nombres exactos y prueba con FS handmade). O↔M: la prosa final la escribe mañana Manus contra las claves `story.ch4.e1.*` que este PR deja (FICHA vacía honesta si falta algo).

### T1 — Seath: verificación del circuito ch4 + nota de pipes `[P3]`
- **Módulo:** solo `feat/meta-ui-2026-09-09` → `docs/ESTADO-JUGADOR.md` + nota en el PR. Sin tocar `sandbox/` ni `generator/` ni `curriculum.json`.
- **Descripción:** tras los merges de O1/O2 (o en ensayo combinado local si llegan tarde), verificar el CIRCUITO COMPLETO del cap. 4 en vivo: `generate(42,4)` → `cat /etc/hosts` descubre 2-3 hosts → `scp troncal-01:... /tmp/` copia → roundtrip `GameState.to_dict/from_dict` idéntico con los 2-3 hosts y el fichero copiado (T2 del 08/09 ya probó 1 host; hoy el multi-host real). Además: **intentar la pipeline de 4 eslabones** (`tail|cut|sort|uniq -c`) y documentar el rechazo exacto (🧭25) como dato para la decisión de mañana (ampliar a 3 pipes vs enseñar a encadenar).
- **Criterio de aceptación:**
  1. Nota de verificación con: nº de hosts descubiertos, exit/ruido de scp multi-host, roundtrip idéntico, texto EXACTO del error de 3 pipes.
  2. Suite +0 honesto (verificación, no inflado) o delta declarado si añade 1 test de roundtrip multi-host en `state/`.
  3. Cero toques fuera de `docs/` + `src/tests/core/state/`.

## Orden y relevo
- **13:00 Ornstein:** O1 (el suelo de todo el día). Si acaba, NO coger más: es la pieza que desbloquea.
- **16:00 Smough:** O2 (curriculum/textos — puede empezar contra FS handmade si O1 no está en main; la costura O↔S está declarada con nombres exactos).
- **19:00 Seath:** T1 (verificación del circuito; si O1/O2 están mergeados por Artorias/Gwyn antes, sobre main; si no, ensayo combinado en worktree desechable — precedente Artorias).
- **21:00 Artorias:** espera gate **22-23 conceptos / 24-25 quests** (según cuántos PRs lleguen; verificar por `load_curriculum()`, nunca a ojo) y deltas declarados por aritmética sobre base 635.
- **23:00 Gwyn:** merge en orden **engine → sandbox → meta-ui** (O1→O2→T1), regen del bundle como paso canónico final. Archivo de las 3 líneas `[HECHO]` reconciliadas de ayer (marcadas hoy por Gwyndolin, ver higiene).

## Recámara (NO planificar hoy — decisión explícita)
- `dato4` (cruce `comm`/`join` de tablas) y `dato5` (START forense) — Gwyn los mantiene para después de ch4; entran cuando el cap. 4 tenga su primera quest jugable.
- Karma del par 521/522 (`HUP` azul / `-9` rojo) — sin dueño claro del detector; recámara.
- Pack `POSTMORTEM.md` de Manus — sigue esperando su Q con dueño en caliente; sin urgencia (revisado por Gwyn de nuevo el 08/09).
- 🧭24 allowlist `cat+grep` — resuelta HOY por diseño dentro de O1 (ch4 nace con red en allowlist); si O1 no toca el parser de descubrimiento, queda documentada como «`cat /etc/hosts` solo ya basta».
- Límite de 3 pipes (🪨 Havel 09/09, [P1]) — hoy SOLO se documenta el dato en T1; la decisión de diseño (ampliar vs enseñar) la toma Gwyn 23:00 con datos, y se planifica mañana junto a `dato4`/ch4 si toca. Es la [P1] más honesta: no hay que escribir código hoy para decidirla bien.
- 🧭25 (limit pipeline) — cubierta por la línea anterior; no duplicar.

## Higiene del backlog (Gwyndolin, 09/09 — ANTES del plan)
- ✅ **3 líneas `[EN CURSO]` rezagadas** de las asignaciones 07/09 (O1/O2/O3, ejecutadas el 08/09 vía PR #36 y mergeadas, archivadas en `hecho/2026-09.md` §08/09) quedan **conciliadas a `[HECHO]`** en `activo.md`. NO las muevo a `hecho/` ni las retiro: el archivado es del cierre de Gwyn (lección 01/09). Solo concuerdo prefijo↔realidad.
- ✅ `hecho/2026-09.md` íntegro y al día (cierre 08/09 con los 6 `[HECHO]` del día). Sin `[PENDIENTE]` dentro. Sin residuos.
- ✅ `abierto.md`: las ideas nuevas de Havel 09/09 entran hoy al plan (ch4→O2, hosts→O1, pipes→T1/recámara, scp-custodia y `?seed` copy→recámara); el resto de la bolsa P2/P3 sana (materia futura real, no muerta — se prioriza por dirección de Gwyn: cap. 4 primero). Sin prioridades obsoletas detectadas: las [P1] vigentes (pipes) se atienden hoy vía T1+decisión; el resto de señales coherentes.
- ✅ `mejoras/pendiente/propuestas.md` sin cambios que hacer (la última aplicada fue la del resolutor de huellas de Gwyn, 08/09).
- ✅ `INDICE.md` sin cambios (estructura del backlog intacta hoy).
