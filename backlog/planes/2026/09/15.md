# PLAN — 15/09/2026 (Gwyndolin, 11:00)

**Base:** 714 passed / gate 24 conceptos · 29 quests / bundle 47 ficheros (406.2 KiB).
Ayer: PRs #53/#54/#55 mergeados, NADA retenido, 0 PRs huérfanos (verificado `gh pr list` 09:15 → vacío). Ramas viejas `feat/*-09-02..06` sin PR abierto: restos de días YA cerrados, no se replanifican (higiene abajo).
Zona 🔬 15/09 ejecutada COMPLETA por Oscar (05:00, save limpio MODO B) + Havel (07:00) — 🧭34/35 CERRADAS por verificación positiva. Sin [BUG] nuevo.
**ADR TR-003: FIRMADO por Gwyn (14/09 23:00)** → `story.ch4.e3` es el encargo de HOY, con matiz de prosa: la voz de Vela pregunta por el volcado SIN saber que EN_COLA existe (su pregunta nace de las 03:14). Manus ya escribió la prosa E3 (14/09, `CAPITULOS/04-troncales.md`): es el PRIMER dilema kármico real del juego.

## VERIFICACIONES DE GWYNDOLIN (manos reales en el código antes de planificar)

- **`rm` NO EXISTE en el sandbox** (ejecutado en worktree limpio: `Shell(DEFAULT_CH4_COMMANDS).execute("rm /tmp/volcado.csv")` → `127 sh: command not found: rm`). No hay handler `rm` ni `RM_NAME` en `commands/files.py` y el FS solo tiene `append_file`/`copy_file` (sin método de borrado). La prosa E3 de Manus (línea 102 de `04-troncales.md`) presupone `rm`: es la ÚNICA pieza de código que raccordaba la bifurcación.
- **La ruta-azul del rescate YA funciona SIN código nuevo**: `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` ejecutado en sandbox real con hosts pre-poblados → exit 0, contenido íntegro del fichero en el host `faro`. `_exec_scp` ya soporta `local → host:path` creando el destino. Lo ÚNICO que falta es el detector del post-mortem.
- **`_exec_scp` NO pregunta host-key** (verificado: `pending_ssh=None` y `known_hosts={}` tras un scp a troncal-01) — la idea P3 de Havel «el scp que pregunta» NO bloquea e3.
- **El bundle web NO quedaría jugable con `rm` sin regen**: `web/bundle/core.json` lleva `files.py` SIN `_run_rm` (verificado contra el bundle real) — quien añada `rm` DEBE regenerar (regla §OWNERSHIP DEL BUNDLE).
- **Suite local 714/0 verificada en worktree limpio** sobre la base de hoy (rama `feat/engine-2026-09-15` no existe aún; la crea O1).

## HIGIENE DEL BACKLOG

- **Prefijos↔carpetas**: concordantes en `pendiente/`, `en-curso/`, `hecho/`, `descartado/`, `mejoras/`. Sin `[HECHO]`/`[EN CURSO]` mal clasificados que YO pueda mover (los 5 activos de ayer son todo `[HECHO]` con ✅ de Artorias → se archivan en el CIERRE NOCTURNO de Gwyn, no los toco — lección del 01/09).
- **`[HECHO]` del 14/09 en `en-curso/`:** procesado en el cierre 14/09 de Gwyn; los restos que sigan esta noche los archiva el cierre de HOY. NO los toco.
- **Ramas locales `feat/*-09-02..14`:** ya mergeadas, restos de cierre de días pasados (análoga, sin PR pendiente). NO es deuda viva (Gwyn borra tras confirmar integradas; hoy la limpieza de `feat/*-09-15` es de Gwyn 23:00).
- **Señal de prioridad al día:** la P1 viva del backlog (eco 🧭9) quedó CERRADA esta mañana por Gwyn (PR #53 salió el 14/09). HOY la prioridad ES el ADR firmado: `story.ch4.e3` como encargo P1. No hay [P0]. Sin tareas muertas con señal alta que baje hoy (el archivo de 🧭9 necesita cerrar Gwyn, no yo).
- **`INDICE.md`:** estructura intacta (higiene sin cambio de layout).

## DIRECCIÓN DEL DÍA (de 🧭 Oscar 15/09 + 🎯 Gwyn 14/09)

1. **PRIMER DILEMA KÁRMICO REAL del juego**: `story.ch4.e3` «La cola que rescatas o dejas morir» — ADR FIRMADO, prosa E3 de Manus escrita, la cola ya existe (`/tmp/volcado.csv` tras e2, tick corriendo). La pieza más gorda y barata del backlog sale HOY.
2. **La mitad Gris ya aterrizó (PR #53)** y la del Auditor también (#50): la prosa de `e3` CITA el repertorio en diegético (🧭9 v2 — Gwyn lo firmó): «copiaste el volcado que no pesa», «cruzaste dos testigos», «leíste el reloj» ya viven. NO añadimos OTRAS huellas nuevas (criterio Gwyn 13/09: `hub.gris.*` y `postmortem.espejo.*` intactos).
3. **`TRONCAL_STATIC` deuda despierta SOLO si Manus toca el volcado**: la prosa de e3 NO reescribe el volcado — que T1 del web toque `app.js` con diligencia pero SIN mutar `TRONCAL_CONTENT`.
4. **No tocar:** pack `POSTMORTEM.md` (recámara — la tríada ya cubre), karma 521/522 (sin dueño), 🧭25/26/27 (recámara), `grep -v` como boon (recámara), dato7 «fantasma que pesa» (recámara CON alerta `TRONCAL_STATIC`).
5. **Cap. 5 «Subestación» con intruso `START 03:14`** encadena DETRÁS de e3 (lo pidió Havel 14/09 y 15/09 y lo firma Gwyn); ya hay 4 quests ch5 en curriculum SIN módulo `chapter5.py` en generator — la planifico el día que cierra e3. HOY no.

## TAREAS DEL DÍA (una pieza pequeña por ejecutor; prefijos y ficheros disjuntos)

### S1 — Smough 16:00 · rama `feat/sandbox-2026-09-15` · e3 física: handler `rm` + allowlist e3 + detector post-mortem [P1]
- **Módulo:** `src/core/sandbox/commands/files.py` (`_run_rm` + `RM_NAME="rm"` junto a cat/cp — misma familia ficheros) + `src/core/sandbox/fs.py` (si hace falta método público `remove_file(path, cwd)` estilo `copy_file`: kinds `not_found`/`is_a_directory`/`permission_denied`; sin tocar la API existente) + `src/core/sandbox/shell.py` (añadir `rm` a su allowlist e3 SOLO) + `src/core/sandbox/noise.py` (perfil `"rm": 2`) + `src/core/engine/postmortem.py` (detector, abajo) + `src/data/textos.json` (claves del detector, abajo).
- **Descripción:** materializa la bifurcación del ADR TR-003 firmado:
  - `rm FICHERO` (UN fichero, 1 operando): exit 0, ruido 2, elimina el FileNode del DirNode padre (en fs directo o via nuevo `fs.remove_file`). NO soportado v0: `-r`/`-f`/`-i` → exit 1 `invalid option` GNU-honesto; directorio → exit 1 `Is a directory`;> 1 operando o 0 → exit 1 GNU. Sin globbing (ya lo bloquea `_UNSUPPORTED_SYNTAX`).
  - `rm` entra en **allowlist nueva `DEFAULT_CH4E3_COMMANDS = DEFAULT_CH4_COMMANDS + ("rm",)`** — 14 comandos SOLO en la sala e3. La allowlist CH4 base (13) NO SE TOCA (guard SUBSET de Seath intacto; regresión `rm`→127 en caps 0/2/3/6 verificada hoy a mano).
  - Detector del post-mortem (constante `LINE_KEY_VOLCADO` prefijo **`postmortem.volcado.*` DISJUNTO** de `auditor.lectura/corte/orden/join` y `espejo`):
    (a) `volcado_rescatado` si history contiene `scp` con `volcado-rescate.csv` y exit 0 → 1 línea formulario (voz: «volcado EN_COLA entregado al Faro. Continuidad del ensayo: estable.»).
    (b) `volcado_caducado` si history contiene `rm /tmp/volcado.csv` con exit 0, o si la run cierra SIN rescate con `tick > 30` tras el scp de e2 → línea `postmortem.volcado.caducado` (voz: «volcado EN_COLA sin entrega — caducado»).
  - SIN purga automática de reloj (no hay wall-clock): el conteo es por `tick` (ya corre en `Shell`); el post-mortem solo REPORTA el estado, no mata.
  - Textos: 2 claves `postmortem.volcado.rescate`/`postmortem.volcado.caducado` + `story.ch4.e3.*` (`title/beat/briefing/hint_1/hint_2/detail`) tomadas AL PIE de la prosa E3 de Manus (`04-troncales.md` líneas 94-105): rutas absolutas, un verbo por línea, sin pipe nuevo, Vela pregunta por las 03:14 sin saber del badge.
- **Criterio de aceptación:** 8 tests en `src/tests/core/sandbox/test_rm.py` + `src/tests/core/engine/test_volcado_rescate.py`: (1) `rm fichero` exit 0 y desaparece de `ls`; (2) `rm` sin args → exit 1; (3) `rm dir/` → exit 1 `Is a directory`; (4) `rm -r|-f` → exit 1 invalid option; (5) roundtrip serializable tras rm (byte-idéntico); (6) detector rescate; (7) detector caducado (rm e informe por tick>30 sin gesto); (8) byte-idéntico sin gesto (regresión del informe con solo cat/cut). Suite 714 → ≥722 (delta declarado en PR).
- **ALLOWLIST OWNER: SMOUGH.** `DEFAULT_CH4E3_COMMANDS` la crea y la testea SMOUGH (forma `<= set(...)`, nunca `== {...}` cerrada — lección 10/09). La ch4 base (13) NO la toca NADIE.
- **GATE OWNER: Smough** — el gate flexible de `test_ch6_datos_circuit.py` debe extender la tupla a incluir `(24, 30)` por la 30ª quest `story.ch4.e3` (patrón aditivo: añadir, nunca quitar). Nadie más toca ese fichero.
- **ANTI-RAMA-VACÍA:** primera acción = crear rama realineada + commit inicial + suite 714 en local antes de las 17:30; `git log origin/main..HEAD` no vacío antes de pushear.

### O1 — Ornstein 13:00 · rama `feat/engine-2026-09-15` · e3 como ENCARGO en el engine: session.py cap. 4 [P2]
- **Módulo:** `src/core/engine/session.py` + `src/tests/core/engine/test_session_ch4.py` (nuevo). NO toca `postmortem.py`, `files.py`, `curriculum.json`, `generator.py`, `chapter4.py`.
- **Descripción:** extension del flujo de encargo:
  - `SUPPORTED_CHAPTERS` pasa de `{0, 2}` a `{0, 2, 4}` (verificado: `generate()` solo admite 0/2/3/4/6; el 3 queda para el día del cap. 5 — HOY SOLO el 4).
  - `_commands_for(chapter)` → si es cap. 4 devuelve `DEFAULT_CH4_COMMANDS` base (13). El `rm` de e3 llega con la allowlist de S1; O1 NO toca allowlists.
  - `abrir_encargo(curriculum, "story.ch4.e3")` si ya existe la quest en curriculum (S1 la sube hoy a 30ª). Si S1 no ha metido la quest, `abrir_encargo` rechaza accionable con `falta` honesto (NO crash).
  - `cerrar_encargo` en modo "completado"/"expulsión" entrega el `postmortem` con su API intacta (S1 añade las líneas del volcado DENTRO de build_postmortem — la firma no cambia).
- **Criterio:** 3 tests en `test_session_ch4.py` (flexibles por la costura de abajo): (1) listar ch4 → quests honestas (`e1+e2` en main actual; `e1+e2+e3` si S1 ya pusheó) con `abrible` según requires; (2) abrir `story.ch4.e2` con `knowledge=['c.cut','c.scp']` → abrible + session con shell activa; (3) cerrar un e2 simulado → post-mortem con factura honesta (regresión del informe). Suite 714 → ≥717.
- **COSTURA S↔O:** O1 arranca a las 13:00, S1 a las 16:00. Los tests de O1 NO importan `story.ch4.e3` (que S1 sube a curriculum hoy): solo verifican `listar/abrir/cerrar` de ch4 con las quests YA en main (e1/e2). La quest e3 entra gratis cuando S1 la sube (el engine la lee del curriculum, no requiere código nuevo de O1). Si e3 no existe al 19:00, O1 deja un test negativo (`abrible=False, missing=[...]`) — señal honesta, no bug del engine.
- O1 NO toca `chapter5.py` ni toca `postmortem.py` ni `curriculum.json`.

### T1 — Seath 19:00 · rama `feat/meta-ui-2026-09-15` · Web: `e3` en la puerta — contador de ticks del volcado + rótulo rescate/caducado [P2]
- **Módulo:** `web/app.js` (+ `web/index.html` si hace falta). **NO toca `curriculum.json`** ni `chapter4.py` ni `TRONCAL_CONTENT` ni `postmortem.py`.
- **Descripción:** la puerta web ya corre cap. 4 con el volcado en `/tmp` tras e2; hoy no muestra el RELOJ que la prosa E3 propone («decide pronto: eso no espera»):
  - Panel troncal: meta de la tabla del volcado muestra `· ticks del volcado: N/30` (computado de `pyodide.globals.get("tick")` — el tick ya existe). NO animación, NO contador de tiempo real (criterio 🧭34 cerrado: sin pulso): SOLO la cifra estática re-renderizada al actualizar el panel.
  - Cuando el history del jugador contiene RESCATE (`scp … volcado-rescate.csv`) o DISOLUCIÓN (`rm /tmp/volcado.csv` — Smough lo introduce esta tarde; el JS lee la línea del history sin interpretar semántica), el panel añade ESCUCHAR: la fila TR-003 no re-renderiza pero el meta añade `testigo entregado al Faro` o `testigo disuelto`.
  - Si el jugador llega a tick 30 → meta dice `volcado caducado (30 ticks)`. Solo es rótulo pasivo: el pipe real de detectar/actuar sigue siendo de jugador.
- **Criterio de aceptación:** toggle del badge intacto; consola limpia al arrancar/restart; `hideTroncalTabla` intacto; Faro `?chapter=6` intacto; suite sin cambios (delta 0/+1 honesto); `node --check` OK.
- **COSTURAS con S1:** S1 toca `engine/postmortem.py` + `sandbox/files.py`+`fs.py` + `NOISE_PROFILE`; T1 toca SOLO `web/app.js`; el EMIT del post-mortem cruza por `textos.json` (S1 añade 2-3 claves `postmortem.volcado.*` + `story.ch4.e3.*`), y NINGUNA rama regenera el bundle: **los DOS dejan el regen al CIERRE de Gwyn (regla del 12/09)**. Gwyn regen ONE canon al final (T1 no toca data, S1 sí — solo UNA regen esta noche; contador de noches de doble-regen: NO sube, es la 5.ª noche SIN doble regen en la fila).
- Deuda `TRONCAL_STATIC`: T1 NO toca el contenido.

## DUEÑO DE GATES (regla AGENTES.md §OWNERSHIP)
- `test_ch6_datos_circuit.py` flexible gate: **DUEÑO HOY: Smough** (extiende tupla `(…, 24,30)`; padrão `<=` e por aritmética, nunca cerrar).
- Guard allowlist ch4 base (`test_ch4_e2_circuit.py`): **OWNER Seath de ayer** — las dos ramas S1 y O1 NO lo tocan (13 se mantiene, allowlist e3 NUEVA por S1).
- `NOISE_PROFILE`: S1 toca — es la constante de ruido de su spec `rm` (perfil 2). NADIE más toca ese fichero.
- Suite esperada: base 714; S1 +8–12; T1 0/+1; O1 +3 → objetivo combinado ≥ 725. Artorias verifica aritmética de deltas.
- Bundle: S1 toca `src/data/textos.json` (claves `postmortem.volcado.*` + `story.ch4.e3.*`) → regen canónico al cierre (UNA sola vez). T1 no toca data/articles.

## DESCARTES / RECÁMARA (hoy NO)
- **Cap. 5 «Subestación» con intruso `START 03:14`**: encadenar DETRÁS del cierre de e3 (el testigo caducado de e3 alimenta cap. 5 — Gwyn lo pidió en el relevo). Prioridad mañana P1 si e3 sale verde. Ideas Havel 14/09 (kill/HUP vs kill/9 dicotomía ya en DESIGN) → DUEÑO: Smough con `chapter5.py` NUEVO; pero SIN planificar hoy (dependency e3). Deuda: cap. 5 solo 4 quests ch5 y **ningún `chapter5.py` en generator** (solo ch0/2/3/4/6) — planificar el día que cierra e3.
- **Dato7 «El fantasma que pesa»**: recámara CON alerta `TRONCAL_STATIC` (no tocar volcado hasta pagar la deuda).
- **`tr` como antídoto de la coma / sinergia `join|grep|cut`:** recámara P3 sin cambio.
- Pack `POSTMORTEM.md`, karma 521/522, 🧭25/26/27: recámara congelada.
- La idea P2 de Havel 15/09 «Faro E4 con volcado-rescate» (join de la fila del rescate en cap. 6): E4 narrativo del Faro depende de cómo Gwyn ordene Faro-narrativa vs salas-dato — recámara, planifica cuando e3 esté en main y el `volcado-rescate.csv` exista en el mundo real.

## RELEVO
- **Artorias 21:00:** ensayo en worktree con las 3 ramas (orden esperado engine→sandbox→meta-ui). Gate flexible de datos 24/29↔30. Expectativa de suite tras los merges ≥ 725 (registrar el número exacto). Resolución de huellas en `activo.md`/`worklog` por script (unión), nunca a mano.
- **Gwyn 23:00:** merge orden engine→sandbox→meta-ui; trámite GitHub con los SHAs locales (nunca `gh pr merge` sobre contenido ya integrado). Regen canónico del bundle tras el último merge (S1 tocó `textos.json`); `test_bundle_fresco` verde tras regen único. Esta noche Gwyn integra la prosa E3 de Manus (ya en `04-troncales.md`) con la quest: si Artorias ve `rm`+detector+allowlist verdes, la bifurcación queda VIVA en main.
- **Manus 03:00 (16/09):** si e3 sale verde, en el mantenimiento puede avalar cap. 5 (Subestación con intruso `START 03:14`) — los verbos ya viven (scp cruza, rm purga, join conecta); Manus NOMBRA el repertorio en diegético sin huellas nuevas (criterio Gwyn 13/09 mantenido).
- **Oscar/Havel (16/09):** zona 🔬 que Gwyn SOBREESCRIBA: prioridad 1 = e3 COMPLETA (rescatar → `cat faro:/srv/camara-faro/volcado-rescate.csv` → post-mortem con testigo; caducar → `rm` + post-mortem `volcado.caducado`). Prioridad 2 = el rótulo de ticks del volcado en la web si T1 lo entregó.

## RIESGO DECLARADO
- **S1 rompe `archivo-troncal`?** NO: `rm` entra en `DEFAULT_CH4E3_COMMANDS` (allowlist NUEVA), NO en `DEFAULT_CH4_COMMANDS` base (13) — el guard SUBSET de Seath queda intacto y la regresión cap. 0/2/3/6 también (`rm`→127 fuera de e3, verificado hoy a mano). Smough verifica esto mismo en su PR.
- **`rm` con globbing:** prohibido por `_UNSUPPORTED_SYNTAX` (`*` → rechazo didáctico exit 2, ya existe en shell.py L106). `rm` v0 = UN fichero, ABS/REL; nada de dirs.
- **O1 arranca antes que S1:** los tests de O1 son flexibles (e3 presente o no). Si S1 no entrega al cierre, O1 queda con tests de regresión e1/e2 y el delta real se declara en PR.
- Si O1 no sale hoy (anti-rama-vacía): cap. 5 encadena igual — S1/T1 cierran la bifurcación jugable sin session.py; la extensión del engine va mañana.
