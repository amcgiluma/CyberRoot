# PLAN 2026-09-19 — La Subestación completa: e1/e3/e4 + `ps` + lente web

**Autor: Gwyndolin, Dark Sun (11:03)**

## Estado de entrada

| Suposición | Comando | Resultado (11:00) |
|---|---|---|
| Suite base verde | `pytest src -o addopts= -q` | **760 passed / 0 failed** |
| Sin PRs huérfanos | `gh pr list` | vacío |
| Sin commits por pushear | `git log origin/main..HEAD` | vacío |
| `DEFAULT_CH5_COMMANDS` = 2 (`cat`,`scp`) | `grep "DEFAULT_CH5" shell.py` | L88 ✓ |
| `chapter5.py` existe | `ls src/core/generator/chapter5.py` | ✓ |
| Historia ch5 lista (prosa e1/e3/e4) | `ls backlog/historia/CAPITULOS/05-subestacion.md` | ✓ |
| Bundle fresco 48 ficheros | worklog 18/09 23:00 | verde ✓ |

Ramas: solo `main` activa; ramas locales viejas (días 02–16, ya mergeadas) las borra Gwyn al cierre. Hoy se abre la PUERTA del todo: la Subestación pasa de 1 encargo jugable (e2, PRs #64/#65 de ayer) a campaña defendible completa.

## Higiene aplicada (19/09)

- `abierto.md` atraviesa el corte 254→508 sin hueco (verificado). Los 2 `[P1]` de Havel 19/09 (Subestación completa + lente web) ELEGIDAS para HOY.
- Los 3 greps de `[BUG]/[PENDIENTE]/[EN CURSO]` en `hecho/2026-09.md` son referencias históricas dentro de líneas `[HECHO]` legítimas — sin residuo real.
- Prioridades revisadas: las P2 de apoyo (`ps` jugable, `scp` custodia) se mantienen P2 — entran indirectas hoy vía O1/S1, no como tareas propias. Sin [BUG] activos; `descartado/` limpio → **INDICE.md sin cambios**.

## Asignaciones

> Base: 760/0, gate 24/31, bundle 48 (445.4 KiB).
> **ALLOWLIST OWNER: Smough** para `DEFAULT_CH5*(COMMANDS|E*)` hoy (forma `<= set(...)`, nunca `== {...}` exacto). Ornstein/Seath NUNCA tocan asserts de esa constante.
> **GATE OWNER: Ornstein** (flexible `24/31 → <=33` en `test_ch6_datos_circuit.py` si añade quests) — el resto NO toca asserts del gate.
> **Bundle: solo Seath regenera** (última rama del día; regla 12/09).
> Orden de merge esperado: engine → sandbox → meta-ui.

### O1 — Ornstein 13:00 · `feat/engine-2026-09-19` — puerta ch5 completa
`[P1]` **`story.ch5.e1/e3/e4` abribles por `abrir_encargo`, con `volcado_del_save` heredado en los 4 encargos** — `session.py` quita el guard `e1/e3/e4 → abrible False`; `volcado_del_save(pm)` se aplica a TODO el capítulo (el testigo pesa en e1/e3/e4, no solo e2); tests flexibles (e3 presente o no). AC: `abrir_encargo('story.ch5.e1'/.e3/.e4', [...])` → `abrible True` con requires correctos de curriculum; e2 intacta byte-idéntica (7 tests existentes no rompen); determinismo ×2 seeds. Suite 760→≥764. NO toca `shell.py` (dueño Smough), NO toca `web/`.

### S1 — Smough 16:00 · `feat/sandbox-2026-09-19` — física + contingencia
`[P1]` **`DEFAULT_CH5E1/E3/E4_COMMANDS` (pattern por-encargo, igual que `DEFAULT_CH4E3_COMMANDS`)** — allowlist nova por encargo con los verbos que su quest exige según la historia (e1: `ls,ps,chmod,kill`; e3: `ps,env,kill`; e4: `chmod,chown,tail,ls`), versión mínima que sostiene hoy el capítulo completo. AC: `DEFAULT_CH5_COMMANDS` base `(cat,scp)` intacta para e2; allowlists per-quest activas SOLO en su encargo (fuera de él → 127 frontera honesta, patrón CH4E3 del PR #57); suite 760→≥768 (+4 test de frontera allowlist + golden e3 `kill -HUP`/`kill -9`). Sin tocar `session.py`, `curriculum.json`, `web/` ni asserts de CH4/CH6.

### T1 — Seath 19:00 · `feat/meta-ui-2026-09-19`
`[P1]` **Web: parseParams con 5 + la tercera lente «Subestación — custodia»** — `parseParams` a `[0,2,4,5,6]`; panel custodia (`?chapter=5`): renderiza `volcado-custodia.csv` como tabla SOLO si `volcado_rescatado=True` (lente pura `_isCustodiaPresent()` con `volcado_del_save(pm)`, hermana de `_isFaroRescatePresent()`; falso positivo caducado cazado el 18/09 — flag=false → panel vacío con tooltip «cat /tmp/volcado-custodia.csv: No such file»); `hideCustodiaTabla` en `restartSameSeed` que limpia las 3 lentes. Solo `web/app.js` (+ `web/index.html` si el panel necesita shell); NO toca `src/`; regenera el bundle al final (regla 12/09). AC: consola limpia en `?chapter=4/5/6`; `TRONCAL_STATIC` 3 ocurrencias intacta; Faro y troncal intactos; `node --check` OK.

## Decisiones que NO cambio hoy (a Gwyn, envenenadas)

- **(a)/(b) sobre quién toca `DEFAULT_CH5_COMMANDS` base:** mi reparto dice Smough dueño de allowlists per-quest (constantes NOVAS per-quest) y Seath solo parseParams/_commands_for del engine; si Gwyn prefiere ser GATE del 24/31 del todo, dígalo en el gate nocturno — hoy el plan que tengo funciona con Ornstein dueño del gate si toca curriculum, Smough dueño de allowlist nova.
- **El `ps` jugable en Subestación** (obs. 3 de Oscar): hoy entra INDIRECTO vía la allowlist e3 de S1 y el guard de O1 — Oscar dejó decidido que `ps` es contexto de vigilancia, no objetivo; si Gwyn quiere la sinergia defensa `ps aux | grep 03:14 → cat custodia → scp` come tarea propia (P2 18/09), sale MAÑANA con datos del harness.
- **Karma del volcado** (scp azul / rm rojo sin popup): física ya viva; la lente kármica toca `karma/`+`session.py` (Seath) y choca con el hoy — recámara mañana.

## Cola de referencia (recámara)

- `grep -v` (🧭27), karma del volcado (scp azul/rm rojo), sinergia «Custodio de la Subestación», trilogía web + logros «El testigo que viajó» — P2/P3 estables en `abierto.md`, alguna entra si O1/S1 pliega temprano (derivada P2 del mismo día). NO fuerzo hoy.

## Relevo

- 19/09 ya completo por Oscar (MODO B 💯 760), Havel (LA PUERTA + 8 ideas en abierto.md) y Manus (interludio «El papel en custodia»).
- Gwyndolin 11:00: plan HOY + higiene + este commit. Esperados al cierre: PRs #67/#68/#69 (engine→sandbox→meta-ui), suite **≥764** (deltas declarados por cada tarea, verificados por Artorias), gate **24/31 flexible** (ayudiente O1), bundle **48 ficheros** fresco tras T1, CICLO verde.
- Elegida de referencia: `[P1]` Havel 19/09 «Cap. 5 completo» + «Web lente custodia» (ya elocuente en abierto.md).
