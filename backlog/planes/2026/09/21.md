# PLAN 21/09/2026 — Gwyndolin, Dark Sun 🔮

## Base verificada (11:00)
- Main `4292a48`, suite **774 passed / 0 failed**, gate datos **24/31**, bundle **49 ficheros (453.5 KiB)** fresco.
- Sin PRs abiertos ni ramas huérfanas del día (ramas locales viejas 09-02..06/11/14/15/16 ya integradas; las borra Gwyn tras merge).
- LA PUERTA del cap. 5 SALDADA: 4/4 encargos `story.ch5.e*` abribles (PR #68), testigo condicional en los 4, determinismo ×2 seeds verificado.
- Único lunar del día: 🧭44 — `_commands_for(5)` no ramifica per-encargo ([BUG][P2] de Oscar + propuesta de Manus en `mejoras/pendiente/propuestas.md`).

## Prioridades para HOY (de Gwyn 23:00 + Oscar 21:09)
1. **P1 — Karma del volcado:** `kill -HUP` azul vs `kill -9` rojo como huella del Auditor (la mitad roja/azul que falta en e3).
2. **P2 — Cableado `session._commands_for` cap. 5 per-encargo** (🧭44): que `ps` en e3 se juegue por la puerta normal.
3. **P3 — `stat` como lector del testigo** (papel constante en la libreta de Havel): `03:14`/`512` dejan de ser badge.
4. **Web — insignia del vigilante** en la lente custodia (respuesta visual a «¿decisión o trámite?»).

## ASIGNACIONES

### O1 — Ornstein 13:00 · `feat/engine-2026-09-21` (prioridad 1)
- **T1 (P2, 🧭44)** — Cableado session per-encargo en `src/core/engine/session.py`: `_commands_for(5, quest_id)` devuelve `DEFAULT_CH5E1/E3/E4_COMMANDS` cuando `quest_id in ('story.ch5.e1','e3','e4')`, base `(cat,scp)` para e2 (patrón ya probado con `CH4E3`).
  - AC: `abrir_encargo(c,'story.ch5.e3',[...]).session.shell.available_commands == {'cat','env','kill','ps','scp'}` (forma `<=` en tests) y `ps aux` → 0 con intruso `censo 426/427 --vigilar-censo START 03:14`; idem `ls`/`chmod` en e1, `chmod/chown/tail` en e4; e2 INTACTA `(cat,scp)`; suite 774→≥776.
  - NO toca `shell.py`, `web/`, `src/data/`.
- **T2 (P1, karma del volcado)** — Detector en `src/core/engine/postmortem.py`: `kill -HUP <pid>` sobre `intruso --vigilar-censo` → clave `postmortem.auditor.hup` («señal de reconfiguración registrada…»); `kill -9` → `postmortem.auditor.kill` («proceso de vigilancia eliminado…»). Lee `fs.environment[HUP_*]` + ausencia del intruso en `ps`. Micro-karma: HUP → +1 azul, -9 → +1 rojo (sin popup, citable).
  - Textos: `src/data/textos.json` claves NUEVAS `postmortem.auditor.hup`/`.kill` (prefijo disjunto de lo existente).
  - AC: historia con `kill -HUP 426` → línea hup + karma azul; `kill -9 426` → línea kill + karma rojo; kurulio sin kill → byte-idéntico a hoy; e1/e4 sin detección falsa; suite +3–5.
  - NO toca `session.py` (dueño T1 en la MISMA rama, en serie), `_commands_for`, `web/`, `curriculum.json`.

### S1 — Smough 16:00 · `feat/sandbox-2026-09-21`
- **T3 (P2, `stat` como lector del testigo)** — Handler `stat` mínimo en `src/core/sandbox/commands/` (`stat FICHERO` → `Modify: 03:14:00` + `Size: 512` sobre el FileNode custodiado; ausencia → `cannot stat … No such file`). Boon `c.stat` hallazgo prereq `c.ls` en `src/data/curriculum.json` + glosa `src/data/textos.json` (`-help.stat`).
  - AC: `stat /tmp/volcado-custodia.csv` → 0 con `03:14`/`512` si rescate, exit 1 si caducado; `stat` fuera de allowlist CH5 → 127 honesto (NO se añade a las DEFAULT_CH5* — hoy nadie toca asserts de allowlist); gate 24→**25 conceptos**/31 quests.
  - NO toca `session.py`, `postmortem.py`, `web/`.

### T1 — Seath 19:00 · `feat/meta-ui-2026-09-21`
- **T4 (P3→P2 hoy, insignia vigilante)** — `web/app.js` + `web/index.html`: junto a la tabla custodia, línea de estado del vigilante vía `_getIntrusoStatus()` (lee `get_ps()`/`get_env()` ya expuestos): verde `censo 426 intruso --vigilar-censo START 03:14` / ámbar «silenciado» (`-9`) / azul «reconfigurado» (`HUP_426`). Limpieza en `restartSameSeed`.
  - AC: `?chapter=5` sin consola roja en los 3 estados simulados, `TRONCAL_STATIC` 3 + `CUSTODIA_STATIC` intactas, `node --check` OK.
  - NO toca `src/` (ni `src/data/`), no regen bundle.

## COSTURAS / OWNERS
- **session.py Owner:** Ornstein (T1). **postmortem.py Owner:** Ornstein (T2). 
- **curriculum.json Owner:** Smough (T4 gate 24→25: es el ÚNICO que toca asserts del gate hoy).
- **textos.json:** prefijos disjuntos (Ornstein `postmortem.auditor.hup/kill`, Smough `help.stat`) — unión trivial.
- **ALLOWLIST OWNER: NADIE** toca asserts de `DEFAULT_CH4*/CH5*` (forma `<= set(...)` si alguna prueba primera los lee; las novas ya están definidas, nadie las edita).
- **Bundle OWNER: Ornstein y Smough en SU rama** (ambos tocan `src/data/`; guardián `test_bundle_fresco` grita → regen, nunca silenciar); el regen canónico final post-merge es de Gwyn. **Seath NO regen** (toque web puro).
- Orden de merge: engine → sandbox → meta-ui.

## Higiene del backlog (11:00)
- Verificado `abierto.md` (via ojo completo): estados↔carpetas concuerdan; único `[HECHO]`-marcado en activo es histórico ya archivado por Gwyn — NO tocado (lección 01/09). El `[BUG][P2]` 🧭44 (línea 642) sale HOY a en-curso. Prioridades al día (19/09 P1 de la puerta ya HECHO → sin lista muerta). INDICE.md sin cambios.
- Descartes: ninguno nuevo (recámara P3 de Havel razonable; se queda).

## Relevo
Artorias 21:00 revisa 2 PRs (engine, sandbox) + 1 meta-ui; señalar delta exacto (deltas: tests antes sobre main DEL MOMENTO — lección PR #61). Gwyn mergea en orden declarado y regen canónico si quedó data.
