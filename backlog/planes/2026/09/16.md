# PLAN 16/09 — Gwyndolin, Dark Sun

> Base: main con e3 verde (suite 727 tras regen canónico, gate 24/30, bundle 47/418.3 KiB).
> Primer dilema kármico (TR-003) jugable. `gh pr list` vacío — SIN ramas/PRs huérfanas.
> Higiene: prefijos↔carpetas concordantes en todo el backlog; los `[HECHO]` del
> 15/09 en `activo.md` NO se tocan (su archivado es del turno de Gwyn 23:00).

## 0. HIGIENE

- `pendiente/abierto.md`: solo [PENDIENTE]/[BUG] vivos, prioridades razonadas. ✅
- `en-curso/activo.md`: los `[HECHO]` del 15/09 quedan para el archivado de
  Gwyn esta noche (regla 01/09 — NO los re-clasifica Gwyndolin). ✅
- `hecho/`/`mejoras/aplicadas/`: NO visitar (nada que replanificar). ✅
- `descartado/`: sin cambios. ✅
- Prioridad actualizada HOY: la P1 viva (eco 🧭9) quedó CERRADA (PR #53+PR #50).
  La señal de máxima ahora es la cadeneta post-e3 (🧭36 + cap. 5), que es P2
  pero con dirección de diseño explícita de Gwyn — planificables.
- `backlog/INDICE.md`: sin cambios de estructura.

## 1. SELECCIÓN — lo más valioso de HOY (día de LA CADENA)

Orden de ataque: (1) 🧭36 cablear session→e3 (la única deuda funcional del
dilema: el rojo solo era jugable por tiempo, no por verbo), (2) primera pieza
encadenada detrás de e3: `dato7` del Faro, la tabla que SOLO existe si
rescataste (primera consecuencia cruzada de capítulos del juego — el testigo
del e3 viaja al Faro). (3) web.

Por qué: la bifurcación e3 deja `volcado: rescatado|caducado` en el
post-mortem y la dirección de Gwyn pide que el testigo (presente si rescate,
ausente si caducado) ALIMENTE el arco del Auditor. dato7 convierte esa decisión
en un FICHERO del mundo del Faro: el `volcado-rescate.csv` que viaja de
troncal a Faro es la consecuencia más barata y más jugable del dilema —
reutiliza join/cut/grep ya dominados, cero comandos nuevos.

## 2. ASIGNACIONES 16/09 — 3 piezas acotadas, ejecutores disjuntos

### 🟠 O1 — Ornstein 13:00 · feat/engine-2026-09-16 · 🧭36: session→e3 cablea `rm`
- **Módulo:** `src/core/engine/session.py`.
- **Descripción:** `_commands_for(4)` devuelve `DEFAULT_CH4E3_COMMANDS` (14,
  con `rm`) cuando contract_id/quest_id == 'story.ch4.e3'; base ch4 13
  INTACTA en el resto de encargos. Sin tocar `shell.py` (allowlist ya existe
  del PR #57; NO toca asserts de allowlist — dueño ya pagado).
- **Criterio:** `abrir_encargo(c,'story.ch4.e3')` con base e3 → `rm /tmp/volcado.csv`
  exit 0 → post-mortem `volcado: caducado` por `rm` (no solo tick≥30);
  `abrir_encargo` e1/e2 sigue con base 13 (rm→127 en ellas, frontera intacta).
  Tests FLEXIBLES (como ayer): e1/e2 con base 13, e3 con base 14. Suite 727→731
  (+4). ALLOWLIST OWNER: NADIE hoy (no toca `DEFAULT_CH4E3_COMMANDS`).
- **Por qué hoy:** es la única deuda funcional que deja el dilema e3 sin
  simetría scp vs rm (🧭36 de Oscar, confirmada por Havel 07:00).

### ⚪ S1 — Smough 16:00 · feat/sandbox-2026-09-16 · `dato7` «El fantasma que pesa» — el volcado que viaja al Faro
- **Módulo:** `src/core/generator/` (scaffold de `chapter6`/`generator`: el
  FS del Faro planta o no el fichero según el estado de la sesión) +
  `src/data/curriculum.json` (quest nueva `story.ch6.dato7`, grey, requires
  `c.join`. Gate 24/30→31: **GATE OWNER de dato7: Smough**, curriculum es suyo
  y sigue la regla vigente).
- **Descripción:** materialización de dato7 «El fantasma que pesa» (Havel
  16/09): si history contiene rescate (`scp ...volcado-rescate.csv exit0`) →
  faro planta `/srv/camara-faro/volcado-rescate.csv` con `TR-003|EN_COLA`
  (nueva tabla CSV del faro, pipe-able con join/cut); si caducado → NO debe
  existir el fichero (join devolverá «No such file» y post-mortem nombra
  `volcado.caducado`). Golden: `join -t'|' -1 1 -2 3 -v 1 purgas.csv volcado-rescate.csv | grep TR-003` → exit 0.
- **Costura:** la sala del Faro lee el ESTADO de la sesión (rescatado/caducado);
  usa los detectores que ya existen (`_has_volcado_rescate` etc.) para NO
  duplicar lógica. Si necesita exponer un flag desde engine, DECLÁRALO en tu
  PR y deja la pieza al dueño (O1 tiene session.py HOY).
- **Criterio:** sala con 2 salidas byte-determinista por seed; no rompe dato6
  ni ningún golden del faro; suite 727→734. **GATE OWNER de dato7: Smough
  (curriculum.json es suyo, sigue la regla vigente).**

### 🔵 T1 — Seath 19:00 · feat/meta-ui-2026-09-16 · web: tooltip del N/30 + Faro — nueva tabla si rescate
- **Módulo:** `web/app.js` SOLO.
- **Descripción:** dos piezas web mínimas: (a) tooltip sobre `· ticks del
  volcado: N/30` (hover, `title=`, consume get_history() ya expuesto — sin
  pulso, respetando 🧭34); (b) nueva fila en el panel del faro cuando detecto
  `volcado-rescate` (lente pura: panel lateral que muestra TR-003 — NADA que
  ejecute el core). Sin tocar TRONCAL_STATIC (deuda vigente, no abre hoy).
- **Criterio:** tooltip descriptivo, Faro `?chapter=6` intacto, toggle badge
  intacto, restart limpia todo. Sin delta de suite (727→727 honesto). No
  toca `src/` en absoluto (regla 12/09).

### ⚠️ COSTURAS DECLARADAS (nada toca lo de otro)
- **El test real [P1 04/09 — test_bundle_fresco]**: lo toca SOLO el que
  regenere el bundle. HOY: Smough añade quest al curriculum → REGENERA bundle;
  Seath no debe regen (regla 12/09 se mantiene 6.ª noche salvo conflicto).
- **No tocar:** resumes de e3 (Artorias ✅ ya), `postmortem.auditor.*` (voz
  del Auditor ya triple-capada), pack `POSTMORTEM.md` (recámara — Gwyn 15/09).

## 3. REGLAS DE TAMAÑO + COHERENCIA
- Cada tarea es UNA pieza de 1–2h: session→e3 (cable), dato7 (una tabla
  condicional + una quest), tooltip (JS de 65 líneas de estilo ya visto).
- `DESIGN.md`: cap. 5 «Subestación» sigue §6.1/§2.5 beat 9 — no contradice nada.
- Historia: el 05-subestacion.md ya está escrito y [LISTA]; dato7 respeta su
  gancho (prosa 06-faro e4 «El trato» NOMBRARÍA el rescate — pero hoy solo el
  DATO, sin narrativa nueva).

## 4. RELEVO (resumen en cristiano para Telegram)
- Ornstein 13:00 → cablea session→e3 (rojo jugable por verbo, no solo tiempo).
- Smough 16:00 → dato7: la tabla del Faro TR-003 aparece SOLO si la rescataste.
- Seath 19:00 → web: tooltip del N/30 + panel rescate (solo web/app.js).
- Artorias 21:00 → ensayo con 3 ramas (engine→sandbox→meta-ui), gate 24/30↔31.
- Gwyn 23:00 → merge en ese orden, regen canónico ÚNICO, archivar `[HECHO]`.
- Gwyn 23:00 → REGLA del 12/09 se mantiene: SOLO Smough regenera bundle.
