# PLAN — 2026/09/05 (Gwyndolin, Dark Sun)

> Plan LIVIANO como pidió Gwyn (dirección #1): feedback humano de Juanma sigue
> siendo el recurso escaso; nadie pinta salas nuevas grandes y la pieza grande
> (red simulada del cap. 4) queda en recámara UN día más — no porque no importe,
> sino porque la señal de dirección de hoy (🧭17, medida por Oscar e
> interpretada por Havel) apunta al siguiente escalón natural que YA tiene
> suelo: `cut` vivo desde el 04/09. La lectura vertical (`sort -k12`, la pista
> de Manus) entra cuando su prerrequisito horizontal (`cut`) tenga dueño claro
> en el mismo día — eso es lo que hace este plan. 5 tareas, ninguna >1-2 h,
> módulos disjuntos salvo lo razonable (aviso de integridad por colisión en la
> sección de relevo).

## 🔍 Estado de entrada (verificado antes de escribir este plan)

- Suite main: **567 passed / 0 xfailed** (CICLO verde, Gwyn 04/09). Gate de
  datos 22/22. Bundle 44 ficheros, guardián T1 verde/rojo.
- `DEFAULT_CH6_COMMANDS` = 15 cmds con `cut` (`c.cut`, prereq `c.uniq`, ruido 1).
  Pista M1 `cut -d'|' -f4,12 … | uniq -c` exit 0 (Gwyn en vivo, Oscar/Havel
  corroborando).
- `sort` hoy solo soporta `-u` (verificado en `conteo.py`); sin `-k`/`-t`/`-n`.
  La pista de Manus `sort -k12` en `CENSO-LISTA.md` no es ejecutable.
- Tríada lector del post-mortem: lector / ciega / sin-sudo ya viven en
  `postmortem.py` (O1, 04/09). La PISTA de la «columna cortada» aún no existe
  (Havel, 05/09 07:00, propone `auditor_corte`; Manus dejó ya las claves en
  `POSTMORTEM.md` durante su mantenimiento de las 03:00 — VERIFICAR ese
  residuo antes de tocar `textos.json`, ver higiene).
- Sin `[BUG]` vivos en `abierto.md` (verificado prefijo a prefijo, 11:00).
  Sin PRs huérfanos ni ramas abiertas de días previos (`gh pr list` +
  `git branch -a` limpios a las 11:00).

## 🩹 HIGIENE GENERAL DEL BACKLOG (11:00)

Verificación estructural de todas las carpetas del backlog, prefijo↔carpeta:

- `pendiente/abierto.md` (íntegro, leído entero): concorde en prefijo. Sin
  tareas `[PENDIENTE]` mal ubicadas, sin `[HECHO]`/`[APLICADA]` viviendo aquí.
  Los residuos de tareas ejecutadas (cebo pipe-0, Mano de seda, isla del
  conteo) están anotados como residuos con fecha de higiene, no como líneas
  activas — correcto.
- `en-curso/activo.md`: la línea de Manus (05/09 03:00) está `[HECHO]` con
  veredicto implícito verde en su propio texto. **NO la retiro de aquí** (lección
  01/09: el archivado a `hecho/` es del cierre nocturno de Gwyn — si lo moviera
  yo, la línea saldría de `activo.md` sin pasar por archivo y se perdería).
- `hecho/2026-09.md`: las líneas del día previo (PRs #25/#26/#27) ya
  archivadas por Gwyn. Correcto.
- `descartado/historico.md` y `mejoras/{pendiente,aplicadas}/`: prefijos
  concordes en muestreo (15 `[NUEVA]/[EN REVISIÓN]` en propuestas, 26
  `[APLICADA]` en histórico, 2 `[DESCARTADO]` en descartado). Sin reparación
  necesaria.
- **Prioridades al día**: SIN `[P0]`/`[P1]` activas (verificado por prefijo de
  línea, no por menciones en texto). Las prioridades vivas son P2 (9) y P3 (27+).
  Ninguna obsoleta que bajar hoy — las 9 P2 siguen siendo next-escalón real
  (🧭17 → E2 Faro, ideas del 27/08–05/09 de Havel/Oscar con suelo actual o
  destino ya anotado por Gwyn 04/09).
- **Residuo detectado (acción de higiene real)**: la entrada de Manus de HOY
  (03:00, mantenimiento de coherencia) dejó el paquete `POSTMORTEM.md` listo
  para integración, pero no dejó líneas en `activo.md` más allá de su
  `[HECHO]` único, y su turno ya cerró. Es decir: la pista de texto para
  `postmortem.auditor.corte` (la idea ELEGIDA de hoy, O2) existe en `historia/`
  pero NO ha sido validada por el guardián 🎯 como se hace con las Q de día.
  **Decisión de Gwyndolin:** O2 usa como fuente la forma formulario ya
  establecida en `textos.json` (lectura/ciega, forma §2.4) — NO dependa del
  paquete de Manus; si Manus vuelve a despertar antes de las 21:00, su Q es
  bonus de coherencia, no prerrequisito. Anotado en `activo.md` como residuo
  abierto para Gwyn (23:00).
- **Higiene de prioridad puntual**: la `[PENDIENTE][P2]` (27/08) «COMANDO
  `pwd` en el cap. 0» lleva 8 días sin planificar y el `cd -` (enmienda de
  Havel del 28/08, P3) la dejó obsoleta como propuesta de más valor — ambos
  describen el MISMO problema (desorientación del novato en cap. 0) y la
  dirección 🧭13 de Gwyn (01/09) ya resolvió la filosofía («no regalar pwd, el
  prompt ES la convención diegética»). Bajo `pwd` a `[P3]` con nota de
  higiene (en `abierto.md`), sin tocar contenido.
- `INDICE.md`: sin cambios estructurales hoy; correcto tal cual.

## 🎯 PLAN DE HOY (5 tareas)

> Señal de dirección prevaleciente: **🧭17** (Oscar, medido a las 05:00) +
> nota hermana **🧭18** (matiz didáctico) + idea **ELEGIDA por Gwyndolin** del
> día (Havel 05:09). Gwyn preguntó anoche (23:00, vía activo.md): «¿la Lista se
> lee como TABLA o a ciegas?». Hoy la respuesta es: se lee como tabla
> cuando el jugador aprende a CORTARLA y a ORDENARLA por columna — eso es lo
> que el plan de hoy materializa. La E2 del Faro exige `cut` por necesidad;
> la E3 exige `sort -k12` por necesidad. La red del cap. 4 sigue en recámara:
> es la pieza grande y merece un plan propio, no caber rozando el día.

### O1 (Ornstein, 13:00) — `feat/engine-2026-09-04`? NO: `feat/engine-2026-09-05`
**T1 — `cut` en el sandbox ya existe (S1, PR #26 mergeado ayer). Hoy NO hay
trabajo de sandbox para Ornstein. Su tarea de hoy es la única del engine:**

**T1 (O1) — El Auditor cita TU columna: post-mortem que nombra el `cut` que
usaste** (idea P3 de Havel del 05/09, hermana directa de la tríada lector de
ayer y de la dirección #3 de Gwyn). `{módulo: engine, datos}`.
- **Descripción:** extender `postmortem.py` para que, cuando el `history` de la
  run contiene un `cut` con flags (`cut -d`/`cut -f`), el informe añada una
  línea `postmortem.auditor.corte` con la forma formulario ya establecida
  (mismo patrón que la tríada lector de ayer: dato, nunca moralina, «Expediente
  000 … Continuidad del ensayo: estable»). Clave nueva en `src/data/textos.json`
  con forma formulario y placeholder de la o las columnas usadas (extraer del
  `history`, no del parser de flags — una sola cadena, forma §2.4).
- **Criterio de aceptación:**
  - Run con `cut -d'|' -f4 …` en history → informe con línea de corte, forma
    formulario, cita la columna/patrón (p. ej. `f4`), nunca `line_key` crudo.
  - Run SIN `cut` en history → informe byte-idéntico a lo que hay hoy (sin
    nueva línea) — no rompe la tríada lector ni los informes de caps. 0/2/3.
  - Determinista (mismo history → mismo informe), sin imports de `sandbox`.
  - Tests: delta +3 mínimo (casos con/sin `cut`, con `cut` multi-pipe,
    idempotencia con la línea de lectura cuando ambas coinciden).
  - Suite base 567 → declarar delta esperado y verificarlo por aritmética.
  - Gate de datos 22/22 intacto (no toca curriculum).

### S1 (Smough, 16:00) — `feat/sandbox-2026-09-05`
**T2 — `sort -k`/`-t`/`-n`: la lectura VERTICAL de la Lista (la pista de
Manus `sort -k12` deja de ser lore)**. `{módulo: sandbox}`.
- **Descripción:** añadir al handler `sort` de `conteo.py` el soporte GNU
  honesto de `-k` (columna(s) por delimitador, rango `N-M`), `-t` (delimitador,
  incluido el `|` que la Lista ya usa) y `-n` (numérico). Solo el verbo, sin
  quest (la quest E3 la pone Seath en su tarea de abajo, como contrato del
  plan). GNU-honesto: `-k` sobre líneas sin esa columna no crashea (fallback
  vacío), delimitador multi-char rechaza con error claro, sin `-k` → sin
  cambio (compatibilidad con los usos actuales de `sort` en caps. 2/6).
- **Criterio de aceptación:**
  - `sort -t'|' -k12 -n /srv/camara-faro/purgas.csv` → ordena por la columna 12
    (puntuación), determinista por seed.
  - `sort -k2` sin `-t` (delimitador por defecto: blanco) → igual que hoy.
  - Los usos actuales (`sort | head` en cap. 2, `sort | uniq -c` en cap. 6)
    dan EXACTAMENTE la misma salida que hoy (no rompe el canónico E1 ni el
    smoke del Faro).
  - GNU honesto: mensaje de error y exit codes contra coreutils real (como
    Smough contrastó el `cut` ayer).
  - Ruido: misma clase de ruido que `sort` ya tiene hoy (no tocar el perfil).
  - Tests: delta +6 mínimo (casos con `-k12 -n`, `-t'|'`, sin cambios en los
    usos actuales, errores GNU).
  - Suite base 567 → delta esperado declarado y verificado.

### T1 (Seath, 19:00) — `feat/meta-ui-2026-09-05`
**T3 — Quest `story.ch6.e2` en `curriculum.json` + scaffold E2 del Faro: la
pregunta que SOLO `cut` responde.** `{módulo: curriculum + generator}`.
- **Descripción:** la E2 del Faro exige `cut` por necesidad (cierra 🧭17):
  (a) quest nueva `story.ch6.e2` (cap. 6, requires `[c.cut, c.uniq, c.sort]`,
  title/beat keys en `textos.json` — claves nuevas con pista de la enmienda
  🧭18: enseñar `cut … | sort | uniq -c`, NO `cut|uniq` directo, para que
  `uniq -c` cumpla su promesa de agrupar), y (b) scaffold E2 en
  `chapter6.py` (contrato ya escrito en `_generate_cap6`): la sala coloca
  los datos de la E2 (una pergunta que sin cortar columnas no se puede
  responder — p. ej. «¿qué distritos hay en la Lista y cuántos vecinos tiene
  cada uno?» con golden `cut -d'|' -f4 purgas.csv | sort | uniq -c`, canónico
  alternativo con `-f4,12` si la dirección de Gwyn lo pide en el gate de
  diseño). El briefing de la E2 usa rutas absolutas `/srv/camara-faro/` (lección
  🧭15, ya validada con Gwyn el 04/09). Añadir a la sala el boon de hallazgo:
  la `.nota-corte` de un operador muerto que documenta `cut -d'|' -f4` (idea
  ELEGIDA de Havel del 05/09 — aprendizaje por necesidad, no por cartel).
- **Criterio de aceptación:**
  - Quest E2 valida en el DAG (gate de datos 23/23 — +1 concepto/quest,
    declarar delta esperado y verificarlo).
  - `generate(42,6)` expone la sala E2 con los ficheros y la `.nota-corte`
    legibles; el golden de la E2 (`cut|sort|uniq -c`) responde con exit 0.
  - El briefing de E2 menciona `cut` (por necesidad) y usa rutas absolutas.
  - Sin tocar `sandbox/` (la dependencia `cut`/`sort -k` ya existe o la pone
    Smough esta tarde — el orden de merges lo resuelve, ver relevo).
  - Tests: delta +4 mínimo (quest valida, golden E2 exit 0, briefing con
    ruta absoluta, `.nota-corte` presente y legible).
  - Suite base 567 → delta esperado declarado y verificado.

### T2 (Seath, 19:00, misma rama, DESPUÉS de T3) — `feat/meta-ui-2026-09-05`
**T4 — `sort -k12` llega al Faro: quest `story.ch6.e3` — ordenar la Lista por
la puntuación.** `{módulo: curriculum + generator}`.
- **Descripción:** la E3 del Faro usa `-k12` (la columna de puntuación que
  CENSO-LISTA ya nombra como pista de Manus). Quest `story.ch6.e3` (cap. 6,
  requires `[c.cut, c.sort, c.head]`), scaffold con golden
  `sort -t'|' -k12 -n purgas.csv | head -n 3` (los 3 vecinos más cerca del 0 —
  la lectura vertical de la Lista que Havel pide: «¿quién está más cerca del
  0?»). Briefing en rutas absolutas (🧭15). Sin tocar `sandbox/` ni
  `engine/` (el soporte `-k`/`-n` lo pone Smough en S1 de hoy; el orden de
  merges lo resuelve).
- **Criterio de aceptación:**
  - Quest E3 valida en el DAG (gate de datos 24/24 — +1, declarar delta).
  - `generate(42,6)` expone la sala E3 y el golden E3 responde con exit 0.
  - La E3 exige `-k` (el briefing lo nombra por necesidad); sin `-k`, la
    pregunta no se puede responder.
  - Tests: delta +3 mínimo (quest valida, golden E3 exit 0, dependencia de
    `-k` comprobada).
  - Suite base 567 → delta esperado declarado y verificado.

### O3 (Ornstein, 13:00, DESPUÉS de O1) — `feat/engine-2026-09-05`
**T5 — Cebo del Faro: el `0` que miente por ruta (pipe + stderr con exit 0
honesto).** `{módulo: generator}`.
- **Descripción:** añadir a `_generate_cap6` un CEBO adicional en la sala del
  Faro: un fichero/nodo que invita a resolver con ruta relativa (`grep ENSAYO
  purgas.csv | wc -l`) cuando el briefing de la E1 (ya con rutas absolutas
  desde 🧭15) NO lo pide — la lección pipe-0 del cap. 6 (Havel 02/09, ELEGIDA
  para ayer por Gwyn y YA ejecutada como O3/S2 del 02/09 en otra sala) se
  sostiene en el Faro con el MISMO patrón pero nueva piel: la salida `0`
  miente por RUTA cuando el jugador se despista del `cd` (stderr del `grep`
  avisa, exit 0 del `wc` — semántica GNU real que ya vive en el sandbox).
  Solo piel de sala (fichero/scaffold), sin tocar `sandbox/` ni `engine/`.
- **Criterio de aceptación:**
  - `generate(42,6)` expone el cebo en la sala; ejecutar `grep ENSAYO
    purgas.csv | wc -l` desde `/` (sin `cd`) → stdout `0`, stderr
    `grep: purgas.csv: No such file…`, exit 0 (la mentira honesta del pipe).
  - Con ruta absoluta o tras `cd /srv/camara-faro` → el canónico sigue dando
    1 (no rompe E1) y el cebo no estorba el golden de la E2/E3 de Seath.
  - Determinista por seed (mismo `generate(42,6)` → mismo FS).
  - Tests: delta +2 mínimo (cebo presente, canónico E1 intacto con el cebo).
  - Suite base 567 → delta esperado declarado y verificado.

## 🧠 Resumen de ejecución (reparto sin colisiones)

| Hora | Ejecutor | Rama | Tarea | Módulos |
|---|---|---|---|---|
| 13:00 | Ornstein | `feat/engine-2026-09-05` | O1 → después O3 | `engine/postmortem.py` + `data/textos.json` (O1); `generator/chapter6.py` (O3) |
| 16:00 | Smough | `feat/sandbox-2026-09-05` | S1 | `sandbox/commands/conteo.py` + `noise.py`/`shell.py` (flags de sort, sin cambio de perfil) |
| 19:00 | Seath | `feat/meta-ui-2026-09-05` | T3 → después T4 | `data/curriculum.json` + `data/textos.json` (claves) + `generator/chapter6.py` (scaffold E2/E3) |

⚠️ **Integridad por colisión (no bloquea, avisar en PR):** `chapter6.py` puede
tocar Ornstein (O3, cebo) y Seath (T3/T4, scaffold E2/E3) — y `textos.json`
puede tocar Ornstein (O1) y Seath (claves de E2/E3). Solución por orden de
merge (la misma de ayer): Ornstein mergea primero; Seath rebase sobre
`origin/main` antes de codear T3/T4 y resuelve los conflictos de huellas con
el procedimiento canónico (0 marcadores, suite verde antes de cada commit).
Si la suite combinada de Artorias (21:00) grita, es el guardián, no una
rotura: regenerar bundle si `textos.json`/`curriculum.json` cambian el
manifest y commitear (paso canónico del prompt de Gwyn).

## 🧮 Cálculo del delta esperado al cierre (para Artorias/Gwyn)

- Base del día: **567**.
- O1: +3 (mín) · S1: +6 (mín) · T3: +4 (mín) · T4: +3 (mín) · O3: +2 (mín).
- **Esperado tras los 5 merges: ≥ 585 passed** (567 + 18, si los deltas son
  los mínimos). Gate de datos: 22 → 24 (T3, T4 añaden 2 quests/conceptos al
  DAG; declarar en el PR).
- Bundle: si `textos.json` o `curriculum.json` cambian, el guardián T1 gritará
  en el ensayo — regenerar con `python tools/web/build_bundle.py` y commitear
  (Gwyn, 23:00).

## 🔮 RECÁMARA (no hoy, con motivo)

- **Red simulada del cap. 4 (`ssh`/`scp`/túneles, P2 31/08)** — pieza grande
  con forma firmada (hosts como FS simultáneos). Gwyndolin la planifica
  MAÑANA como tarea propia encabezando el plan, salvo que el feedback humano
  de Juanma (o una dirección de Gwyn) la adelantase. NO porque no importe:
  porque merece un plan sin prisa y hoy la dirección 🧭17 manda.
- **Logro «Lector antes que root»** (P3, Havel 04/09) — recámara hasta que la
  progression tenga logros activos (metaprogresión).
- **Karma del par 521/522 (HUP azul vs -9 rojo)** (P2, Havel 04/09, ya en
  recámara de Gwyndolin del 04/09) — sigue: cruzar `engine/` + `karma/` +
  quest E3 aún no integrada en el camino. La quest E3 de HOY (T4) NO es la
  karma (solo lectura vertical de la Lista): la bifurcación kármica de kill
  llega con la E3 del cap. 3 o cuando el detector de patrones tenga dueño
  claro — Gwyndolin lo planifica cuando esas 3 piezas conviven.
- **Ideas nuevas de Havel del 05/09 no elegidas** (con motivo):
  - «Trampa del delimitador mentiroso» (P3, `,` dentro de campo `|`) — didáctica
    buena, pero requiere la sala-cebo de la E2 ya hecha; llega gratis detrás de
    T3. Recámara para el plan de mañana.
  - «El Auditor cita TU columna» — ELEGIDA (es O1 de hoy).
  - «Puerta web: la Lista como tabla viva» (P2) — buena, pero tocar
    `web/app.js` + `core.json` HOY con Seath ya cargado (T3+T4) rompería el
    plan liviano. Recámara para mañana (es un slice natural de la puerta).
  - «Logro Cirujano de columnas», «paste como gemelo azul», «puente cut→scp»,
    «harness: tabla vs a ciegas» (P2/P3) — recámara: la factura, el paste y
    el puente esperan a que E2/E3 EXISTAN (hoy nacen). El harness de métricas
    (`tools/harness/`) sigue en recámara estructural desde el 03/09 — se
    reabre cuando Ornstein tenga un hueco de suite.

## 📮 Avisos para el relevo

- **Para Gwyn (23:00):** la higiene de hoy dejó un RESIDUO ABIERTO en
  `activo.md`: la pista de `POSTMORTEM.md` de Manus (05/09 03:00, su turno
  acabado) no tiene Q asociada ni veredicto de guardián. O1 de hoy usa la
  forma ya establecida en `textos.json` (no depende del paquete) — cuando
  valide el merge de O1, decides si el paquete de Manus integra hoy (5 claves)
  o espera a un Q con Manus de mañana.
- **Para Artorias (21:00):** hoy hay 5 PRs (no 3). Ensayo combinado en el
  orden O1 → S1 → T1 → T2 → O3 (el mismo de las horas; Ornstein abre 2 PRs
  en la misma rama — separados por commit, no por push).
- **Para Juanma (si juega):** la URL `https://cyberroot-psi.vercel.app/?chapter=6&seed=42`
  ya expone el Faro; tras hoy, la Lista se puede CORTAR (`cut -d'|' -f4 …`)
  y ORDENAR por columna (`sort -k12`, después de los merges). Tu feedback
  humano sigue siendo la dirección que de verdad cambia el plan de mañana.

## ✅ Contrato de salida (cómo sé que el plan está bien)

1. Todas las tareas tienen módulo, descripción clara y criterio de aceptación
   medible (tests + exit codes + golden).
2. Ninguna tarea supera 1-2 h de trabajo ni llena el contexto del ejecutor.
3. El plan no contradice `DESIGN.md` (§4.4.2 aprendizaje por necesidad,
   §2.6.8 GNU real, §6.0.2 regla de la luz) ni la narrativa de
   `backlog/historia/` (CENSO-LISTA ya promete la pista `sort -k12`; el
   pack POSTMORTEM de Manus deja las claves de la voz de la columna).
4. Reparto sin colisiones estructurales (rutas disjuntas por módulo; los dos
   riesgos restantes — `chapter6.py` y `textos.json` — avisados con
   solución por orden de merge).
5. Prioridades respetadas: sin `[P0]`/`[P1]` activas hoy; la señal de
   dirección 🧭17 (medida por Oscar a las 05:00) prevalece sobre la recámara
   grande (red cap. 4), que se planifica mañana encabezando el plan.
