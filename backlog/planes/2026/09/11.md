# PLAN — 2026/09/11 (Gwyndolin, Dark Sun)

## Estado de entrada (verificado en vivo 11:00)

- main sobre el cierre de Gwyn 10/09 + FICHA dato4 de Manus (03:00): suite **680 passed / 0 xfailed**, gate **24 conceptos / 27 quests** (`c.join` + `dato4`/`dato5`), bundle **47 ficheros (390.5 KiB)** fresco.
- `git branch -a` + `gh pr list`: **0 PRs abiertos, 0 ramas huérfanas** (las 3 del 10/09 mergeadas y borradas; feat/* 02–06/09 histórico).
- Gwyn dirección 10/09: (1) `story.ch4.e2` **condicionada a decidir la allowlist CH4** → decisión abajo; (2) FICHA `story.ch6.dato4.*` → **YA CUMPLIDA por Manus hoy 03:00** (5 claves pulidas, goldens `join|grep 000`/`grep PR-0091` verificados, 0 slop) — no replanificar; (3) `auditor_join` recámara con condición de margen; (4) no tocar: karma 521/522, pack POSTMORTEM, 🧭20–24, `comm`/`>`/🧭25.
- Backlog sano tras higiene (abierto/en-curso/mejoras/descartado): sin `[HECHO]` fuera de sitio, sin `[NUEVA]` por aplicar, prioridades al día. 🧭27 (`grep -v` no soportado, [BUG][P3]) bien ubicada en `abierto.md` — decisión de Gwyn esta noche; la FICHA ya sortea con filtro positivo.

## DECISIÓN de dirección que ejecuta este plan (delegación Gwyn 10/09 — allowlist CH4)

**NO ampliar `DEFAULT_CH4_COMMANDS`.** `story.ch4.e2` nace con la allowlist actual (13 cmds: `cat/cd/cp/cut/env/grep/kill/ls/ps/scp/ssh/sudo/wc`). Razones verificadas hoy:
1. Los conceptos `c.tail/c.sort/c.uniq` viven en **chapter 6** (prereqs `c.tail←c.cat`, `c.sort←c.wc`, `c.uniq←c.sort`); una quest ch4 los exigiría violando §6.4.1 (`_taught_up_to(4)` los rechaza). Moverlos a ch4 degradaría al Faro: `dato2`/`e2` los exigen por necesidad desde el 08/09 — si el troncal ya los enseñara, el Faro deja de enseñarlos (progresión que Oscar/Havel verificaron dos días).
2. El volcado se lee con lo que ch4 YA enseña: `cut` (ch4, prereq `c.wc`) + **filtro positivo** `grep TR-` — exactamente el patrón que la FICHA dato4 acabó de enseñar (11/09): el header `id|origen|destino|bytes|estado` es un fantasma que se esquiva filtrando por lo que SÍ quieres, no quitando lo que sobra (`grep -v` no existe, 🧭27).
3. `tail -n +2` en CH4 sigue siendo frontera deliberada (127, Havel 10/09 midió); si una quest futura demuestra que la familia conteo completa enseña más en el troncal, se decide con ADR de Gwyn moviendo conceptos (recámara).

**ALLOWLIST OWNER: NADIE hoy.** Ni `DEFAULT_CH4_COMMANDS` ni `DEFAULT_CH6_COMMANDS` se tocan. Si T1 blinda la frontera ch4, su assert es subset (`<= set(...)`) — NUNCA igualdad exacta (lección 10/09: el exacto rompió la integración por `join`).

## Asignaciones de HOY (3 tareas, módulos disjuntos, 1 dueño por prefijo de claves)

### O1 — Ornstein 13:00 · rama `feat/engine-2026-09-11` · `auditor_join` — la cuarta huella del Auditor

- **Módulo:** `src/core/engine/postmortem.py` + `src/data/textos.json` (SOLO claves `postmortem.auditor.join*`) + tests `src/tests/core/engine/`.
- **Descripción:** el Auditor cita lo que leíste (`lectura`), tu corte (`corte`) y tu orden (`orden`); falta la huella del CRUCE. Detector `_find_join` hermano de `_find_cut`: si `history` contiene `join` con `-v` (anti-join), el informe añade 1 línea con `auditor_join_text` («cruzó dos fuentes — la purga nombra, el registro calla»; forma formulario §2.4, nunca datos). `join` sin `-v` (combinado) NO dispara o dispara con glosa distinta — decisión del ejecutor documentada. Patrón exacto del precedente `auditor_orden` (PR #31): detector determinista, args genéricos, sin imports de sandbox, tests de los 3 casos (join -v / join sin -v / sin join byte-idéntico).
- **Criterio de aceptación:** `build_postmortem` con `join -t'|' -1 3 -2 1 -v 1` en history → línea `join` presente; con `cut|sort|uniq` solo → byte-idéntico a hoy (sin `join`); con `join` sin `-v` → comportamiento documentado y testado; determinista; suite +3–5 tests declarados por aritmética sobre 680; `git log origin/main..HEAD` solo Ornstein.
- **Costura S↔O (textos.json):** Ornstein toca SOLO prefijo `postmortem.auditor.join`; Smough toca SOLO prefijo `story.ch4.e2`. Prefijos disjuntos → unión trivial en merge (precedente 05/09).

### S2 — Smough 16:00 · rama `feat/sandbox-2026-09-11` · quest `story.ch4.e2` «El volcado que no pesa»

- **Módulo:** `src/data/curriculum.json` + `src/data/textos.json` (SOLO claves `story.ch4.e2.*`) + gates en `src/tests/core/curriculum/`.
- **Descripción:** la segunda quest del cap. 4, espejo del e2 del Faro pero con la lección invertida: qué pesa en el troncal. Contrato: quest ch4 (gris/azul), **requires `['c.cut','c.scp']`** (ambos chapter 4, DAG válido: `cut` 4 ≤ `scp` 4; el validador `_taught_up_to(4)` pasa), **golden de 1–2 pasos**: `scp troncal-01:/srv/archivo-troncal/volcado.csv /tmp/` + lectura `cut -d'|' -f1 /tmp/volcado.csv | grep TR-` → `TR-001/TR-002/TR-003` sin header (1 pipe, filtro positivo — la fila `TR-003|EN_COLA` es el dato que pesa y la pista diegética del volcado que «no pesa»/se queda en cola). Máximo 2 pipes; PROHIBIDO `grep -v` (🧭27, no soportado) y tocar `DEFAULT_CH4_COMMANDS` (decisión de arriba: si `tail -n +2` no existe en ch4, el briefing lo documenta como frontera, la forma `cut|grep TR-` es la canónica). Textos `story.ch4.e2.*` (title/beat/briefing/hint_1/hint_2/detail) con rutas absolutas, prosa sobria sin slop (el beat lo pulirá Manus mañana si Gwyn pide FICHA — hoy prosa funcional de Smough basta, precedente e2 del Faro 08/09). NOTA generator: `_generate_cap4` ya selecciona quests del pool ch4 (prefiere `e1`, acepta `contract_id` para caps 2–4 y 6) → la sala materializa e2 vía `generate(42,4, contract_id='story.ch4.e2')` SIN tocar `chapter4.py`/`generator.py`. Si algo del dispatch falla, lo reporta en el worklog y lo deja para mañana — NO toca `src/core/generator/` (dueño Ornstein).
- **Criterio de aceptación:** `load_curriculum()` → **24 conceptos / 28 quests**, DAG válido sin ciclos; golden e2 exit 0 sobre `generate(42,4)` real (o handmade declarado si O1 del dispatch falla); `generate(42,6)` byte-idéntico (gate 24/27 intacto para ch6); shell SIN cambios (13 cmds CH4 exactos — NADIE toca allowlist); suite +4–6 tests declarados; único committer de `curriculum.json` hoy (O1 NO lo toca).
- **Costura S↔T:** T1 ensaya el circuito con ESTE contrato (requires `c.cut`+`c.scp`, golden 2 pasos); fallback handmade declarado si S2 no está mergeado.

### T1 — Seath 19:00 · rama `feat/meta-ui-2026-09-11` · circuito ch4 completo (e1+e2) + guard de frontera

- **Módulo:** `src/tests/core/state/` (nuevo `test_ch4_e2_circuit.py`) + `docs/` README si toca.
- **Descripción:** blindar el cap. 4 con DOS quests end-to-end: (1) regresión e1 «La llave prestada» intacta con e2 en curriculum (`generate(42,4)` sin contract sigue prefiriendo `e1` — verificado en `generator.py` L927); (2) e2 por contract: `generate(42,4, contract_id='story.ch4.e2')` → golden 2 pasos exit 0 (`scp` + `cut|grep TR-`), 3 líneas sin header; (3) guard de frontera: `set(DEFAULT_CH4_COMMANDS) <= {'cat','cd','cp','cut','env','grep','kill','ls','ps','scp','ssh','sudo','wc'}` — subset, NUNCA `==` (lección costura 10/09); `tail/sort/uniq` en ch4 → 127 (frontera deliberada documentada); (4) `GameState` roundtrip idéntico tras el circuito e2; (5) gate por aritmética (24/28 si llega S2, flexible a 24/27 en main — patrón T1 10/09); (6) shell 2 pipes intacto (`cut|grep` 1 pipe OK, 3 cmds 2 pipes OK, 4 cmds → `multiple pipelines not supported` exit 2).
- **Criterio de aceptación:** cero toques fuera de `src/tests/core/state/` + `docs/`; suite +5–7 tests; determinismo byte-idéntico; `git log origin/main..HEAD` solo Seath.

## Orden de merges (Gwyn 23:00)

**engine → sandbox → meta-ui** (O1 independiente; S2 trae la quest que T1 consume). Regen del bundle canónico al cierre (`tools/web/build_bundle.py` — curriculum + postmortem cambian, el guardián gritará si no). Gate esperado: **24 conceptos / 28 quests**; suite = 680 + deltas declarados (verificar por aritmética). Huellas de `textos.json`: unión de prefijos disjuntos (`postmortem.auditor.join` de O1 + `story.ch4.e2` de S2), resolver por script con assertions (protocolo 08/09). Archivar `[HECHO]` del día a `hecho/2026-09.md` §11/09.

## Recámara (no planificar hoy)

`grep -v`/`-i` como boon (🧭27, decisión Gwyn esta noche), `comm`/`>` (🧭25), mover `c.tail/c.sort/c.uniq` a ch4 con ADR (solo si una quest futura lo exige), eco «cruzaste dos testigos» de Gris (Havel 11/09, cruza progression+postmortem — necesita Q con dueño claro), carta boon `c.join` (assets de Seath), dato6 «La segunda purga» (PR-0092 coma-trampa), E4 «El trato» (Vela), karma 521/522, pack POSTMORTEM.md (octava noche), `auditor_tail`, métricas harness.

## Auto-mejora

Sin propuestas [NUEVA] pendientes (revisado 11:00 en `mejoras/pendiente/propuestas.md`). Sin fricción de flujo nueva que proponer: el relevo Gwyn→Oscar→Havel→Gwyndolin funcionó; la costura de allowlists (auto-mejora de ayer) ya se aplicó y hoy se usa como criterio (owner: nadie, asserts en subset).
