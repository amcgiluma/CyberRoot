# PLAN — 12/09/2026 (Gwyndolin, 11:00)

**Base:** 691 passed / gate 24 conceptos · 28 quests / bundle 47 ficheros.
Ayer: PR #45+#46 mergeados (quest ch4.e2 «El volcado que no pesa» + guard SUBSET).
O1 `auditor_join` 💥 sin rama (3.ª replanificación de Ornstein).

## HIGIENE DEL BACKLOG
- `abierto.md` y `activo.md`: prefijos↔carpetas concordantes; sin `[HECHO]`/`[APLICADA]` fuera de sitio. La línea 💥 de O1 (11/09) queda in situ como constancia (no es `[HECHO]`; sin riesgo de brecha de archivado). `INDICE.md` sin cambios de estructura.
- 🧭28 (FICHA ch4.e2 con alma) ejecutada esta madrugada por Manus (commit 9683598) — la prioridad 2 de Gwyn ya está servida. CERRADA.
- 🧭27 (`grep -v`): decisión de Gwyn cerrada ayer (filtro positivo como lección; `-v` en recámara P3). No se planifica.
- Gwyn 11/09 pedía ownership de tests de gate ANTES de que salga otra vez: hoy lo institucionalizo en el plan (ver DUEÑO DEL GATE, abajo).

## DIAGNÓSTICO ORNSTEIN (pedido de Gwyn — con datos, no intuición)
Replanificaciones de Ornstein: 07/09 sin rama, 11/09 💥, 11/09 💥. El patrón NO es tamaño de tarea ni mala señal del plan: la spec del 11/09 era buena y nadie la cambió; lo que falló es que la rama quedó CON 0 COMMITS (ramas vacías, no código roto). Además, Ornstein SÍ entregó bien el 10/09 (PR #31/P#42) cuando el otro punto del mismo rol. Lectura: falla de ARRANQUE del turno de 13:00 (turno caído / output cortado en tool-call, patrón ya conocido del Concilio), no de capacidad.
**Acción hoy:** (1) tarea de un solo hito, menos superficie de fallo; (2) criterio temprano: si a las ~14:00 `git log origin/main..feat/engine-2026-09-12` sigue vacío, asumir turno caído y RELANZAR manual (precedente mem.); no esperar a Artorias 21:00. (3) Si vuelve a fallar HOY, propongo a Juanma mini-ADR sobre el turno de 13:00.

## TAREAS DE HOY (una por ejecutor; ramas y prefijos disjuntos)

### O1 — Ornstein 13:00 · rama `feat/engine-2026-09-12` · REPOSICIÓN `auditor_join` [P1]
La cuarta huella del Auditor: el post-mortem cita el `join` del jugador (hermano de `corte`/`orden`). Spec intacta de ayer.
- **Módulo:** `src/core/engine/postmortem.py` + `src/data/textos.json` (SOLO prefijo `postmortem.auditor.join*`) + tests `src/tests/core/engine/test_auditor_join.py`.
- **Descripción:** detector `_find_join` — si `history` contiene un `join` con `-v` (anti-join), añade 1 línea formulario citando la operación (cruce de tablas, anti-join, huérfanas como concepto; NUNCA datos de la fila). Sin `join` en history → informe byte-idéntico a hoy. Determinista (sorted, como `_find_cut`).
- **Criterio de aceptación:** 3 tests (con `-v` dispara / `join` sin `-v` documentado no dispara / sin `join` byte-idéntico); suite 691 → ≥694; PR abierto con delta declarado.
- **COSTURAS:** `textos.json` prefijo propio disjunto de `story.ch4.e2.*` y `auditor.corte/orden`. NO toca `curriculum.json`, `shell.py`, ni `test_ch6_datos_circuit.py`.
- **ANTI-RAMA-VACÍA:** PRIMERA acción del turno = crear rama realineada y commit inicial; verificar `git log origin/main..HEAD` no vacío antes de pushear.

### S2 — Smough 16:00 · rama `feat/sandbox-2026-09-12` · 127 que enseña: `join` nombra dónde vive [P2]
- **Módulo:** `src/core/sandbox/shell.py` (mensaje de fallo por allowlist) — idea de Havel 11/09, aprobada como dirección.
- **Descripción:** hoy `join` fuera del cap. 6 da 127 seco. Que el 127 de `join` (y solo `join` hoy) añada una línea didáctica accionable tipo: `Try 'join --help' — tables cross there (chapter 6).` Precedente interno: el `scp` de puertas nombra `/etc/hosts`. GNU no aplica (no es error de coreutils).
- **Criterio:** `join` en cap. 0/4 → 127 + glosa que nombra el Faro; `generate(42,6)` intacto; suite +2–4.
- **COSTURAS:** si la glosa va a `textos.json`, prefijo propio `story.ch6.frontera-join*` disjunto (unión trivial con O1). **ALLOWLIST OWNER: NADIE** — ninguna tarea de hoy añade/quita comandos en `DEFAULT_CH4_COMMANDS`/`DEFAULT_CH6_COMMANDS`; ese día no llega.

### T1 — Seath 19:00 · rama `feat/meta-ui-2026-09-12` · Tabla viva del troncal (web) [P2]
- **Módulo:** `web/app.js` (+ tests propios en `src/tests/web/` si procede). NO toca `curriculum.json`/`textos.json`/`shell.py`.
- **Descripción:** hermana de la tabla del Faro (05/09): panel lateral «Volcado del Troncal» que renderiza el `volcado.csv` del cap. 4 como tabla HTML (preview con `?chapter=4&seed=42`; refleja la columna que el jugador corta con `cut -d'|' -f1`). No ejecuta `cut` por el jugador. Reusa bundle core.json (ya lleva el CSV).
- **Criterio:** tabla visible en la web con `?chapter=4&seed=42`; consola limpia; mismo componente/patrón que el del Faro; suite sin cambios o +1.

### DUEÑO DEL GATE (declarado — regla de Gwyn 11/09)
El gate flexible `24/27↔24/28` y el guard SUBSET viven en `src/tests/core/state/` → **DUEÑO: Seath (meta-ui)**. O1 y S2 NO tocan hoy ese fichero ni ningún assert de allowlist. Si una costura future exigiera tocarlo, el plan debe transferir el ownership explícitamente ese día.

## DESCARTES / RECÁMARA (hoy NO)
- **dato6 «La segunda purga»** (Artorias+Havel 11/09): cruza engine+curriculum+textos en un día; necesita partición en piezas — recámara hasta turno con hueco.
- **sort -k en ch4** (Havel 12/09): exige mover `c.sort` de capítulo con ADR — recámara hasta decisión de Gwyn (frontera 🧭25/26 ya documentada).
- **TR-003 rescate/quemar + karma scp+chmod:** esperan Q con Manus (karma sin dueño). Recámara.
- 🧭25 (pipes), 🧭26 (cut ya en ch4), pack `POSTMORTEM.md`: sin cambio.

## RELEVO
- **Artorias 21:00:** ensayo con 1 rama (base 691; delta esperado +3–5 de O1). Gwyn 23:00: merge si verde; `python tools/web/build_bundle.py` canónico si `data/` cambió (S2 puede tocar textos.json → muy probable regen).
