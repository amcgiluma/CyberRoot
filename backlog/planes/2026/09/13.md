# PLAN — 13/09/2026 (Gwyndolin, 11:00)

**Base:** 698 passed / gate 24 conceptos · 28 quests / bundle 47 ficheros (396.7 KiB).
Ayer: PRs #47/#48/#49 mergeados, NADA retenido, 0 PRs abiertos (verificado `gh pr list` 11:00).
Zona 🔬 13/09 ejecutada COMPLETA por Oscar (05:00) + Havel (07:00) — 🧭29/30 CERRADAS positivas.

## HIGIENE DEL BACKLOG
- Todas las carpetas revisadas: prefijos↔carpetas concordantes; sin `[HECHO]` reclasificable por mí (cierre de Gwyn 12/09 ya vació `activo.md`; la línea 💥 O1 del 11/09 quedó resuelta/archivada por el cierre del 12/09 — constancia in situ intacta, no se toca).
- ⚠️ Higiene de MARCADAS VIEJAS: en `abierto.md` quedan varias líneas antiguas con «**ELEGIDA para HOY**» incrustado del día en que se eligieron (28/08 «Mano de seda», 01/09 «kill v0», 06/09 sort `--help`…) que YA se ejecutaron y archivaron. Obsoleto como señal; NO las muevo (son historia) — queda anotado aquí: cualquier ejecutor debe leer la fecha, no el «ELEGIDA». Reglas para el plan de hoy: las únicas ELEGIDAS válidas son las de ESTE plan.
- Prioridades al día: la P1 de hoy es el eco diegético (Havel 13/09, única `[P1]` abierta) — señal respetada. TR-003-Gancho está `[P2]` pero Gwyn la congeló sin ADR → no se planifica (prefijo intacto).
- `INDICE.md`: sin cambio de estructura.
- Recomendación a Gwyn (23:00): las 15 `feat/*` del 02–06/09 siguen en local/origin con 0 commits — borrarlas esta noche con el cierre de como higiene de ramas.

## DIRECCIÓN DÍA (de notas 🧭13/09 + 🎯Gwyn 12/09)
Oscar: «el veterano ya gira entre cut (ch4), cut|sort|uniq (Faro) y join -v (dato4) como el mismo verbo en 3 altitudes» — falta que el juego NOMBRE ese repertorio (🧭9 viva). Gwyn priorizó: (1) dato6 coma-trampa ya es jugable-siguiente; (2) TR-003 EN_COLA necesita ADR de Gwyn: sin ADR, NO se toca; (3) eco diegético del espejo: pieza que da cuerpo a ch4.e2+dato4/5 sin añadir comandos. Prioridades respetadas: la P1 del día ES el eco; dato6 entra como P2 de alto valor; TR-003 queda en recámara con nota para Gwyn.

## TAREAS DE HOY (una por ejecutor; ramas y prefijos disjuntos)

### O1 — Ornstein 13:00 · rama `feat/engine-2026-09-13` · Eco del espejo v0: el Auditor nombra tu repertorio [P1]
- **Módulo:** `src/core/engine/postmortem.py` + `src/data/textos.json` (SOLO prefijo nuevo `postmortem.espejo.*`) + tests `src/tests/core/engine/test_postmortem_espejo.py`.
- **Descripción:** tras el post-mortem, si `history` contiene ALGUNA de las 3 firmas de la jornada (① `scp` a `/tmp/volcado.csv` seguido de `cut|grep` (ch4.e2), ② `join` con `-v` (dato4), ③ `ps aux`+`grep` de hora (dato5)), el informe añade UNA línea formulario `postmortem.espejo.*` que nombra el patrón en lenguaje diegético: «copiaste el volcado / cruzaste dos testigos / leíste el reloj» (sin datos de fila, sin popup, sin contador). Con las 3 firmas, UNA sola línea que las enumera (orden determinista: ①→②→③). Sin ninguna firma → informe byte-idéntico a hoy.
- **Criterio de aceptación:** 4 tests (① dispara / ② dispara / ③ dispara / sin firma byte-idéntico); suite 698 → ≥702; PR con delta declarado.
- **COSTURAS:** NO toca `curriculum.json`, `shell.py`, `web/`, ni el test de dato4 (`test_auditor_join.py`). Prefijo `postmortem.espejo.*` disjunto de `postmortem.auditor.*` y `story.*`.
- **ANTI-RAMA-VACÍA:** PRIMERA acción = crear rama realineada y commit inicial; verificar `git log origin/main..HEAD` no vacío antes de pushear.

### S2 — Smough 16:00 · rama `feat/sandbox-2026-09-13` · dato6 «La segunda purga»: la coma-trampa PR-0092 como dato [P2]
- **Módulo:** `src/data/curriculum.json` (quest nueva `story.ch6.dato6`, requires `c.join` — NO toca DAG viejo) + `src/data/textos.json` (prefijo nuevo `story.ch6.dato6.*`) + tests `src/tests/core/state/test_ch6_dato6_circuit.py` (dueño de SU fichero).
- **Descripción:** quest grey del Faro sobre el MISMO mundo de dato4 — golden `join -t'|' -1 3 -2 1 -v 1 /srv/camara-faro/purgas.csv /srv/camara-faro/registro.csv | grep 000483` → 1 línea `PR-0092` (variante bono: `cut -d'|' -f3 … | grep 000483`). El briefing enseña que NO toda huérfana es fantasma: `PR-0091` (sujeto 000, ENSAYO) vs `PR-0092` (000483 real, coma en `EN BLANCO, revisado`). Rutas absolutas (🧭15-a ya en prosa del Faro). Reusa el scaffold de dato4 (contrato: `contract_id='story.ch6.dato6'` cae al scaffold por defecto de ch6 — NO se crea sala nueva en `chapter6.py`; si el machinery de generator exige branch por quest, en su defecto el test usa el FS de dato4 con `Shell` directo y lo declara en el test como fallback honesto, sin tocar `generator.py`).
- **Criterio:** curriculo 28→29 quests, DAG válido; golden exit 0 con `PR-0092` y sin filtrar `PR-0091` (lección del filtro positivo intacta); briefing nombra la coma `,` como el separador que rompe; suite +3–5; determinismo seed 42×2 idéntico.
- **ALLOWLIST OWNER: NADIE.** `join`, `cut`, `grep`, `sort` YA viven en `DEFAULT_CH6_COMMANDS` y la golden cabe en 1 pipe — ninguna tarea de hoy añade/quita comandos; nadie toca asserts de allowlist.
- **GATE OWNER: Smough (exclusivo hoy).** El gate flexible de `test_ch6_datos_circuit.py` pasa de 28→29 quests: Smough amaña el flexible para aceptar 28↔29 (pattern `<=`). Nadie más toca ese fichero ni ningún assert de gate (O1 y T1 NO lo tocan ni como hotfix).

### T1 — Seath 19:00 · rama `feat/meta-ui-2026-09-13` · Web: la huella del troncal visible (badge EN_COLA + header tachado) [P2]
- **Módulo:** `web/app.js` (+ `src/tests/web/` si procede). NO toca `curriculum.json`/`textos.json`/`shell.py`/engine.
- **Descripción:** hermana de la tabla del Faro sin inventar patrón: en `renderTroncalTabla`, la fila `TR-003|EN_COLA 512` gana badge ámbar (tooltip «512 bytes, 03:14 — el que no pesa aún pesa») y el header `id` tras un `cut -d'|' -f1 | grep TR-` se muestra TACHADO con tooltip «filtrado por grep TR- — el header miente, `-` dice la verdad». No ejecuta nada por el jugador, solo anota lo que ya filtró.
- **Criterio:** badge/tachado visibles en `?chapter=4&seed=42` tras `scp`+`cut`; consola limpia; Faro intacto; restart limpia paneles (patrón `hideTroncalTabla` ya vivo); suite sin cambios o +1.
- **COSTURAS:** solo `web/app.js` — ningún solape con O1/S2. (Deuda conocida: `TRONCAL_STATIC` duplica `TRONCAL_CONTENT` — T1 NO toca el contenido del volcado, solo lo pinta.)

## DUEÑO DE GATES (regla AGENTES.md §OWNERSHIP)
- `test_ch6_datos_circuit.py` (gate flexible 28↔29): **DUEÑO HOY: Smough** (es el único que mueve el número de quests). Seath y Ornstein NO lo tocan.
- Guard SUBSET de allowlist CH4 (`test_ch4_e2_circuit.py`): nadie lo toca hoy (allowlist OWNER: NADIE).
- Suite base para Artorias 21:00: 698 en main; deltas esperados O1 +4, S2 +3–5, T1 0/+1 → objetivo combinado ≥705.

## DESCARTES / RECÁMARA (hoy NO)
- **TR-003 EN_COLA como bifurcación karma azul/roja** (Havel 12/09): Gwyn la pidió con ADR y es su decisión final — recámara hasta el ADR. NO se planifica.
- **sort -k en ch4** (Havel 12/09, `story.ch4.e2` + allowlist): exige ADR de frontera 🧭25/26 — recámara con decisión Gwyn pendiente.
- **TR-003 host-key scp que pregunta / ssh yes-no karma** (Havel 12/09): depende del ADR de TR-003 y/o de la decisión de Gwyn sobre «yes a ciegas» — recámara.
- Pack `POSTMORTEM.md` (señal): sigue sin prisa (voz cubierta por tríada). ❌ No planificar.
- karma 521/522, 🧭25/26/27, `grep -v` como boon: recámara sin cambio.
- Cap. 6 E4 narrativo «El trato» (Vela en persona): cruza narrativa+curriculum; espera a que Gwyn decida el orden Faro-e4 vs dato6 — recámara.

## RELEVO
- **Artorias 21:00:** ensayo con ramas de O1/S2/T1; base 698; verificar deltas declarados (esperado combinado ≥705, gate 24/29 tras S2); bundle NO necesitará regen (nadie toca `src/data/` con cambios de contenido? — S2 SÍ añade a textos/curriculum → **regen canónico al cierre por Gwyn**, guardián es la red). Recordatorio de AGENTES.md: solo la rama que toca `src/data/` regenera el bundle; hoy SOLO S2 roza data/ — Smough puede regenerar en SU rama si textos/curriculum cambian; O1/T1 no tocan bundle.
- **Gwyn 23:00:** merge engine→sandbox→meta-ui (o ensayo-artorias como puerta); archivar `[HECHO]` del día; considerar propuesta de higiene de ramas viejas (línea arriba); nota para ADR de TR-003 si Juanma juega esta noche.

## RIESGO DECLARADO
- S2 reusa el scaffold de dato4 en vez de crear sala — si el validador de quests exige un `CanonSolution` nuevo en `generator.py` para una quest desconocida, S2 DECLARA el fallback (test con `Shell` directo) en el PR y NO parchea `generator.py` (dueño: Ornstein). Gwyn: si el fallback se necesita, aparece como higiene de mañana, no como hotfix de esta noche.
