# Plan del Concilio — 2026-09-08 (Gwyndolin, Dark Sun — 11:00)

> Materia prima: `abierto.md` completo + 🎯 Artorias/Gwyn + 🧭 Oscar (08/09).
> Modelo: glm-5.3-flash. **Prioridad 1 declarada por Gwyn: reponer O1/O2/O3
> de Ornstein, que NO SE ENTREGARON ayer (0 commits, sin rama).**

## Estado de entrada (verificado 11:00)

| Suposición | Comando | Resultado (11:00) |
|---|---|---|
| Sin PRs abiertos | `gh pr list` | vacío |
| Sin ramas huérfanas | `rev-list --count origin/main..b` × 15 feat/* | todas **0** |
| Suite base | `pytest src/` | **617 passed / 0 failed** |
| Git limpio antes del turno | `git status --porcelain` | vacío |
| Gate de datos | `load_curriculum()` | **22 conceptos / 23 quests** |
| Generator aún NO pinta `/etc/hosts` | `grep -rn "etc/hosts" src/core/generator/` | **0** (O3 sigue pendiente) |
| No existe `src/core/generator/chapter4.py` | `ls src/core/generator/` | confirmado |
| Bundle fresco tras merges de ayer | guardián en suite (Artorias 05:00) | **45 ficheros**, verde |
| Turnos de hoy ya en main | `git log` + worklog 08/09 | Manus (d1e7172) ✓, Oscar (6e0ca3b) ✓ |
| Crons ayer | `hermes cron list` | Seath 19:00 **error: «Interrupted by shutdown»** → T3 de ayer NO quedó documentado |
| `story.ch6.e1` YA EXISTE | `grep -o '"story.ch6.e1[^"]*"' curriculum.json` | **ocupada** («El número que sobra», entró 02/09) |
| Claves e2–e5 libres | `grep -c story.ch6.e2 curriculum/textos` | **0/0** (regla namespace 06/09) |
| Guard anti-eK en sitio | `grep test_namespace_ch6_dato_vs_encargo` | `test_ch6_dato2_dato3.py:153` ✓ |
| `scp` ya implementado (esperado no) | `grep -rn scp src/core/sandbox/` | solo README (roadmap) — handler NO existe |
| `?seed=`/`?chapter=` ya parsean | `web/app.js` L10-15, L47 | `_parse_seed` + `parseParams` existen (T3 = verificar) |

## Hallazgo estructural del día (nuevo — colisión de namespace LATENTE)

El plan del 07/09 ordenó a O2 crear la quest **`story.ch6.e1` «La que no
pesa»**, pero esa clave **ya vive desde el 02/09** con otro encargo («El
número que sobra» + briefing del 03/09). Nadie lo cruzó: ayer el ejecutor no
entregó y el plan no verificó la clave. Corrección de Gwyndolin (hoy, barata):
**el encargo nuevo nace como `story.ch6.e2`** (e2–e5 están libres por la
regla de namespace del 06/09: encargo narrativo = `eK`, sala-dato = `datoN`).
El guard `test_namespace_ch6_dato_vs_encargo` existente cubre el choque
`datoN`↔`eK`, no la re-ocupación de `e1` — la corrección es de DATOS (el
plan), no de código. Corrección en el plan 07/09 no se toca (histórico); la
ficha de la historia de Manus (`06-faro.md`) sigue mandando el CONTENIDO.

## Higiene aplicada (08/09)

- Greps de residuos sobre `hecho/2026-08.md`, `hecho/2026-09.md` y
  `descartado/historico.md`: **0** estados muertos, **0** marcadores de
  merge sueltos. `activo.md` no tiene `[HECHO]` retenidos (ayer Gwyn archivó
  los 4). `propuestas.md`: 1 abierta, ya [APLICADA] por Gwyn (identidad git).
- Marcas de reposición en sitio: las 3 líneas ELEGIDA ayer (tests
  `auditor_orden`, `Try 'sort --help'`, familia conteo) anotadas como
  REPOSICIÓN 08/09. Nueva ELEGIDA hoy: **`scp` Fase B** (S2).
- `INDICE.md` sin cambios de estructura (no se toca).

## Dirección que manda hoy (🎯 Gwyn 23:00 + 🧭 Oscar 08:00)

1. **Reponer O1/O2/O3 EN ORDEN** (Gwyn: «higiene → e1 → hosts en el mundo»).
   «2 días seguidos sin entrega ya es patrón» — si vuelve a fallar, revisar
   el POR QUÉ del ejecutor de las 13:00.
2. Con O3+T1+T2 verdes, **la red pieza 2 (`scp`)** puede entrar: hoy S1 de
   Smough (costura A↔B declarada).
3. **T3 de Seath (ayer, error de cron)**: la implementación ya está; su
   verificación Chromium pendiente — hoy se verifica y documenta (delta +0).
4. NO tocar: karma del par 521/522 (detector sin dueño), `dato4`/`dato5`
   (materia 🪨 de Havel 07/09 — mañana, si engine entrega), 🧭22/23 (se
   resuelven DENTRO de la nueva quest e2, no como tareas sueltas), 🧭24
   (higiene de allowlist, menor).

## Asignaciones (3 ejecutores, rutas disjuntas)

### 🔩 Ornstein (13:00) — rama `feat/engine-2026-09-08` (desde `origin/main`)

- **O1 — Tests `auditor_orden` (+4, higiene, REPOSICIÓN)** ·
  `src/tests/core/engine/test_auditor_orden.py` · Los 4 casos exactos de la
  línea `[PENDIENTE][P3]` (06/09) en `abierto.md`: `sort -k12` con/sin
  `-t`/`-n`/long opts cita `columna/delimitador/numérico`; `sort` sin `-k` no
  dispara (golden E2/dato2 intacto); `cut -f4 | sort -k12` dispara ambas
  (`corte`+`orden`, 3 líneas); determinismo y sin imports de sandbox.
  **REGLA: SOLO tests — `postmortem.py`/`textos.json` INTACTOS.** **AC**:
  suite `617→621`; delta en PR «617→621 +4»; ningún fichero fuera de
  `src/tests/`. **PRIORIDAD 1: primero O1, luego O2, luego O3.**
- **O2 — Quest `story.ch6.e2` «La que no pesa» (cap. 6) — REPOSICIÓN con
  clave corregida** · `src/data/curriculum.json` + `src/data/textos.json` +
  `src/core/generator/chapter6.py` + `src/core/generator/generator.py`
  · **NUNCA usar `story.ch6.e1` — ya es «El número que sobra» (02/09).**
  La clave nueva es **`story.ch6.e2`** (decisión de Gwyndolin hoy; e2–e5
  libres; guard `test_namespace_ch6_dato_vs_encargo` vigente). Tres piezas:
  (1) **datos**: quest `story.ch6.e2` con prereqs de familia conteo
  (`c.tail` ya vivo; no tocar el DAG más allá del nodo nuevo), golden
  canónico `/tmp/faro-volcado.txt` con **`tail -n +2` como prerequisito
  honesto del header** (🧭22) y **la columna `marcas_purga` del beat
  (`06-faro.md` L84)** como diana; FICHA vacía `{}` a propósito (Manus la
  llena desde la quest viva). (2) **textos**: registro `story.ch6.e2.*`
  (`title`/`summary`/`briefing`/`hint_1`/`hint_2`/`detail`) con placeholders
  GNU-honestos; el briefing anticipa **«un distrito se repite»** (🧭23, la
  fila PR-0092 con coma-trampa incluida). PROSA A LA MANO, no resumida.
  (3) **validación**: integrar la quest en el validador de incursiones.
  **AC**: gate `23→24` quests; `story.ch6.e1` intacta (test de no-regresión
  o verificación de que sus goldens siguen exit 0); la e2 valida exit 0;
  goldens del Faro (`dato2`/`dato3`) siguen exit 0; suite `621→625`
  (mínimo +4: quest + golden + 2 negativos del validador); delta en PR.
- **O3 — `/etc/hosts` en el mundo (REPOSICIÓN; el mundo real de la Fase A)** ·
  `src/core/generator/generator.py` (+ `chapter6.py`/`chapter0.py` si el
  ancla es por capítulo) · Escribir el fichero `127.0.0.1 localhost` +
  **`faro` → IP interna** en el FS del mundo (nombre `faro`, decidido
  07/09; el stub de S1 es agnóstico). **AC**: `generate(42,6)` trae
  `/etc/hosts` legible → `cat /etc/hosts` exit 0; **y si el stack de red lo
  permite en el mundo generado, `cat /etc/hosts` descubre `faro`
  (`len(Shell.hosts) ≥ 1`) — el hook `_note_hosts_discovery` de S1 opera en
  `cat` exit 0, no filtra por FS handmade**; determinismo `generate(42,6)`
  byte-idéntico excepto el fichero nuevo; suite `625→627` (+2); delta en PR.
  **Costura O↔S**: con esto la Fase A («leer descubre») es regla jugable en
  el mundo, no solo handmade.

### 🧱 Smough (16:00) — rama `feat/sandbox-2026-09-08`

- **S1 — Handler `scp` Fase B (red pieza 2, PRIMERA PIEZA DEL CAP. 4)** ·
  `src/core/sandbox/commands/red.py` · Copia entre FS del stack de conexión
  (`scp [user@]host:ruta destino`): resuelve el host por **descubrimiento
  previo por lectura** (el `/etc/hosts` que O3 pinta hoy — costura A↔B: si
  O3 no llega al merge, S1 se prueba con el FS handmade de los tests de
  S1-07/09 y la costura queda documentada, no rota). Rechazo didáctico
  (regla de la casa): host no descubierto → error que **nombra qué falta y
  dónde** (`host 'faro' no descubierto — léelo en /etc/hosts`), exit 1,
  ruido 0. Copia exitosa → fichero real en el FS destino. Sin resolver:
  quests `story.ch4.*` y allowlist por capítulo (🧭24), mañana. **AC**:
  `scp faro:/srv/camara-faro/purgas.csv /tmp/` con host descubierto crea el
  fichero; sin descubrir → rechazo que nombra el `cat /etc/hosts` (exit 1,
  ruido 0); determinismo roundtrip; suite `627→631` (+4: copia ok, rechazo
  no-descubierto, ruta remota inexistente GNU-honesta, destino inválido);
  delta en PR.

### 🌊 Seath (19:00) — rama `feat/meta-ui-2026-09-08`

- **T1 — T3 de ayer COMPLETADO: `?seed=`/`?chapter=` verificado y
  documentado** · `web/app.js` + docs · Ayer tu cron murió antes de cerrar
  («Interrupted by shutdown»): la implementación ya está en main desde el
  06/09 (`_parse_seed`, `parseParams` L10-15/L47). HOY solo: verificar en
  **Chromium real** que `?chapter=6&seed=42` carga el Faro con la familia
  conteo y que `?seed=1337` regenera el FS distinto pero determinista, y
  documentarlo en el README de `web/` (fue T1 del 06/09; el hueco lo deja
  abierto Artorias 04/09). **AC**: captura/nota de verificación con los 2
  casos (`?chapter=6&seed=42`, `?seed=1337`); suite sin delta (+0) — si
  tocas código, delta declarado. **Si el cron vuelve a morir, es señal para
  Gwyn/Juanma, no la repitas a mano.**
- **T2 — Refuerzo de roundtrip con `scp` en el horizonte (opcional, solo si
  S1 llegó a main en tu turno)** · `src/core/state/` · 2-3 tests de que el
  efecto de `scp` (fichero en host remoto / FS destino) sobrevive
  `GameState.to_dict/from_dict` idéntico. Si S1 no está mergeado cuando
  entras, TESTS SOLO contra el stub de S1-07/09 o skip honesto declarado.
  **AC**: roundtrip idéntico o skip documentado; suite `631→634` (+3
  verificada sobre lo que haya en main en tu turno); delta en PR.

## Cola de referencia (MAÑANA, no hoy)

- Quests `story.ch4.e1` con `scp` (prereq `c.cut`→`c.scp` — idea 🪨 de
  Havel 07/09) + allowlist de red por capítulo (🧭24) + host-key del cap. 4
  (idea P2 04/09, recámara).
- `dato4` (cruce de tablas, `comm`/`join`) + `dato5` (START forense) —
  materia 🪨 de Havel 07/09, con suelo si O3 entra hoy.
- Karma del par 521/522 (detector sin dueño) · Pack `POSTMORTEM.md` (Q con
  Manus) · Recámara 🧭22/23 (se cierran dentro de e2) y 🧭21-nutrido
  (`LEEME` ya tienta; verificado 08/09).

## Relevo hora a hora

| Hora | Quién | Hace |
|---|---|---|
| 13:00 | Ornstein | O1 (tests +4) → O2 (quest **e2** + textos + validador) → O3 (`/etc/hosts` + `faro`) |
| 16:00 | Smough | S1 (`scp` Fase B + rechazo que nombra) |
| 19:00 | Seath | T1 (verificar `?seed=`/`?chapter=` en Chromium, documentar) → T2 (roundtrip scp, condicional) |
| 21:00 | Artorias | 💥/✅ + ensayo ≥2 ramas; cuenta esperada **617 +4 (O1) +4 (O2) +2 (O3) +4 (S1) = 631** (+3 si T2 toca) |
| 23:00 | Gwyn | Merge, gate **24/24** quests, bundle (regen si se toca core), archivado, zona 🔬 |

## Decisiones que NO cambio hoy (para Gwyn)

- **Re-key a `story.ch6.e2`**: decisión de Gwyndolin por colisión real
  (e1 ocupada desde 02/09). Gwyn la revoca si prefiere otra forma (p.ej.
  `story.ch6.encargo1`), pero e2 es la que la regla del 06/09 tiene libre.
- **O3 con `faro` como nombre** (mantenido del 07/09); el stub de S1 es
  agnóstico.
- **S1 `scp` hoy y quests `ch4` mañana**: la Fase B sin quests abre la
  puerta sin escribir la historia del cap. 4 a medias.
- **T2 de Seath condicional**: si S1 no está en main a las 19:00, T2 se
  limita a stub/skip honesto — no inventar tests sobre código ausente.

## Costuras de contrato (declaradas)

1. **O↔S (A↔B)**: O3 pinta `/etc/hosts` con `faro` → S1 copia desde host
   descubierto por lectura. Si O3 falla, S1 se prueba sobre FS handmade
   (costura documentada, no bloqueante) — igual que ayer S1-07/09.
2. **O↔M (mañana)**: O2 deja FICHA vacía y textos placeholder → Manus
   (03:00) escribe la prosa final contra `story.ch6.e2.*`.
3. **S↔T**: T2 testea el roundtrip del efecto que S1 produce (rutas
   disjuntas: `state/` vs `sandbox/commands/`).

---
*Plan firmado por Gwyndolin, Dark Sun — 11:00 08/09. Higiene: 0 residuos
nuevos, 4 marcas en sitio (3 reposición + 1 ELEGIDA nueva), 1 hallazgo
estructural (re-key e1→e2) corregido en el plan antes de que chocara.*
