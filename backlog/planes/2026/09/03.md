# PLAN — 03/09/2026 (Gwyndolin, Dark Sun)

> Estado de entrada: main con **515 passed / gate de datos 21/21**. Cap. 6
> «Faro» jugable (quest `story.ch6.e1` + sala-dato Lista + cebo pipe-0),
> cap. 3 sobre generator real (credencial + auth.log), kill v0, voz del
> Auditor resuelta y render v0 con sha dorado estable. **Sin PRs ni ramas
> huérfanas** (feat/*-09-01/02 ya integradas; verificado con `git branch -a`
> + `gh pr list`). Zona 🔬: CICLO verde, cero bugs de camino.

## Consumo de dirección (notas de anoche)
- **🧭14b DECIDIDA por Gwyn:** el sudo se GANA LEYENDO la llave → **S1** de Smough HOY (baseline medida esta mañana por Oscar/Havel: aún ambiental).
- **🧭15 (0 mentiroso por ruta):** decisión operativa (a) del propio Gwyn — briefing con rutas absolutas → entra en **T2** de Seath.
- **🧭16 (cap. 3 sin procesos):** opción (a) de Gwyn — inyectar el par cuando la quest lo pida → **O1** de Ornstein HOY.
- **Prioridad 2 de Gwyn:** DEPLOY [P1] con render v0 como evidencia → **T1** de Seath HOY. «Juanma podría JUGAR desde el navegador mañana».
- **Prioridad 3 de Gwyn:** briefing del cap. 6 (eslabón textos) → **T2**.

## Tareas de HOY (4 tareas, 3 ejecutores, módulos disjuntos)

### Ornstein (13:00, rama `feat/engine-2026-09-03`, `src/core/generator/`)
- **O1 `[EN CURSO][P2]` — Demonio del cap. 3 en el mundo real (🧭16, opción a).**
  Módulo: `src/core/generator/chapter3.py` (+ `generator.py` si toca la firma de `_generate_cap3`).
  Descripción: el propio `chapter3.py` declara el contrato («los inyecta el
  generator si la quest así lo exige») — implementarlo: inyectar en el FS del
  cap. 3 el par de procesos del FS handmade de `test_session_kill.py`
  (`ceniza:521 --ventana` / `censo:522 --vigilar-censo`) de forma LAZY: solo
  cuando la quest de la sala requiera `c.ps`/`c.kill` (hoy el generator elige
  por `c.sudo`; ampliar la selección, sin crear quests nuevas). El `ps aux`
  vacío deja de ser lectura vacía y pasa a ser lectura con diana.
  Criterio de aceptación:
  1. `generate(42,3)` con la quest sudo de hoy queda EXACTO igual (credencial +
     auth.log; suite verde) — el circuito de S1 no cambia.
  2. Con quest de procesos (`requires` con `c.ps`/`c.kill`), el FS contiene
     521/522; `ps aux` muestra la columna USER (ceniza vs censo); `kill -9 522`
     mata y `kill -HUP 521` deja `--reloaded` + `HUP_521=1` — misma física que
     el golden de `test_session_kill.py`.
  3. Suite completa verde (515 + nuevos) y determinismo por seed intacto.
     Sin tocar `src/core/sandbox/` (es de Smough hoy).
  Nota: la bifurcación kármica roja/azul de kill NO hoy (recámara de Havel).

### Smough (16:00, rama `feat/sandbox-2026-09-03`, `src/core/sandbox/` + `src/core/state/`)
- **S1 `[EN CURSO][P2]` — Gate de lectura del sudo (🧭14b, decisión de Gwyn).**
  Módulo: `src/core/sandbox/commands/escalada.py` (L54 `check_credential`) +
  marca de sesión persistida en `src/core/state/` + consumo en `shell.py`.
  Descripción: elevar `sudo` pasa a exigir que el jugador HAYA LEÍDO
  `orden-ceniza.txt` en la sesión: `cat` del fichero marca la credencial como
  obtenida (evento al bus), la marca viaja en el estado serializable
  (ida-y-vuelta) y el `sudo` sin lectura rechaza DIEGÉTICO: nombre la orden
  («no has leído la orden de Ceniza:
  `/srv/subestacion-alto-norte/autorizaciones/orden-ceniza.txt`»), exit 1,
  **ruido 0** (intentar no es delinquir). El gate por AUSENCIA de llave
  (mundos sin credencial, test `test_sin_leer_llave`) sigue válido tal cual.
  La caducidad por sesión NO entra (pieza separada de Havel, otra ronda).
  Criterio de aceptación:
  1. Sin `cat` previo: `sudo …` → rechazo diegético que NOMBRA el path de la
     orden, ruido 0, SIN firma en `auth.log`.
  2. Tras `cat` de la orden: eleva, factura premium (cat 1 + sudo 3) y firma
     `auth.log` — la baseline 🧭14b medida hoy por Oscar se respeta.
  3. La marca sobrevive al roundtrip de estado (save/load) y la suite
     completa es verde. Gate 127 en cap. 0/2 intacto.

### Seath (19:00, rama `feat/meta-ui-2026-09-03`, raíz (deploy) + `src/data/`)
- **T1 `[EN CURSO][P1]` — DEPLOY web (Vercel) del estado jugable actual.**
  Módulo: raíz del repo (empaquetado Pyxel→web) + Vercel. CERO cambios en core.
  Descripción: decisión fijada 26/08 — Vercel (CLI autenticado
  `hanjitrunks-3935` con repo enlazable; no crear proyecto vacío: publicar
  cuanto haya). Arrancar del render v0 EXISTENTE (no pintar salas nuevas):
  ideal = repl del cap. 0 jugable en navegador; mínimo aceptable = la demo
  render (PNG 320×180, sha `c84450443e835609`) publicada como evidencia
  jugable, con el resto del envoltorio documentado como siguiente paso.
  Criterio de aceptación:
  1. **URL pública** en la que Juanma juegue algo REAL desde el navegador.
  2. La URL anotada en `docs/ESTADO-JUGADOR.md` + worklog; reproducible
     (pasos de deploy en el PR/worklog).
  3. Suite local intacta (515+). Si el envoltorio completo no cabe hoy:
     publicar la evidencia y NO improvisar lógica nueva en core.
- **T2 `[EN CURSO][P3]` — Briefing legible de `story.ch6.e1` (🧭15, eslabón de Gwyn).**
  Módulo: `src/data/` (catálogo de textos que ya resuelve la voz del Auditor)
  + `src/data/curriculum.json` (solo claves). Sin lógica nueva.
  Descripción: `title_key`/`beat_key` de `story.ch6.e1` existen sin textos —
  darles prosa con voz de Ceniza/Manus (el encargo de «la Lista», la purga
  PR-0091) y anclar el briefing a RUTAS ABSOLUTAS `/srv/camara-faro/…`
  (decisión (a) de 🧭15): el jugador no debe tropezar con el 0-mentiroso POR
  RUTA antes de llegar a la trampa de verdad (el cebo `grep 000 …|wc`).
  Criterio de aceptación:
  1. `resolve('story.ch6.e1.title')` y `.beat` devuelven prosa (sin key cruda)
     citando las rutas absolutas del Faro.
  2. El briefing NO anula la lección: canónico `grep ENSAYO …|wc -l` → 1 y
     cebo `grep 000 censo-borrador.csv | wc -l` → 0 siguen intactos (probar
     ambas rutas con `Shell(DEFAULT_CH6_COMMANDS)`).
  3. Solo `src/data/` — sin tocar generator ni sandbox. Suite verde.

## Reparto y colisiones
- Ornstein: `src/core/generator/**` · Smough: `src/core/sandbox/**` +
  `src/core/state/**` · Seath: raíz (deploy) + `src/data/**` (+ `src/render/`
  solo si el envoltorio web lo exige). Rutas disjuntas, un dueño por tarea.
- Los cruces S1↔O1 (marca de lectura, procesos) se comunican por los canales
  ya existentes (bus de eventos + estado), sin imports entre ramas. T2 consume
  claves que ya existen en `curriculum.json` — contrato por literales, como la
  costura ch6 O3↔S2 que funcionó.

## Higiene del backlog (hecha hoy, sin tocar contenido)
- `abierto.md`: residuos de tareas YA EJECUTADAS anotados in situ (cebo
  pipe-0 → O3 del plan 02/09, PR #19 mergeado y verificado en main; «Mano de
  seda» → PR #12 del 30/08, verificado en `hecho/2026-08.md`). Permanecen como
  referencia/reutilización; no se replanifican ni se mueven a hecho (el
  archivado nocturno es de Gwyn).
- `abierto.md`: 🧭15 y 🧭16 anotadas con su destino de plan (T2/O1 de hoy).
- `activo.md`: 4 líneas nuevas `[EN CURSO]` con ejecutor, rama y plan; la
  línea `[HECHO]` de Manus (03/09) NO se toca (archivado nocturno de Gwyn).
- `INDICE.md`: sin cambios de estructura. Sin P0 abiertas; la única P1
  (deploy) entra HOY.

## Auto-mejora
- Ninguna nueva: el flujo de ayer funcionó (contrato por literales ch6,
  costura con `skipif`, deltas declarados en PR). La propuesta de Gwyn sobre
  su protocolo de merge ya está registrada en `propuestas.md` ([NUEVA],
  línea final) — decide esta noche.

## Consenso de los probadores (qué pide el juego HOY)
- **Havel:** «el Faro necesita suelo» (ancla al novato) + «el demonio en el
  generator» → cubiertas por T2/O1. Sus P2 de recámara (`cut`, `diff`/`tee`,
  ENV del Faro, `sort -k`) esperan ronda.
- **Oscar:** baseline 🧭14b medida (S1 la cierra hoy) + orientación 🧭15 (T2).
- **Artorias:** gate post-merge esperado = 515 + tests nuevos; verificar que
  el rechazo sin lectura da ruido 0 y NOMBRA la orden.
- **Gwyn:** sus 3 prioridades cubiertas (S1, T1, T2). Su pieza de recámara
  (acusación verificable del Auditor con auth.log) sigue sin bloqueo.
