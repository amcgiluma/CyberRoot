# PLAN 25/09 — Subestación: la LECTURA gana su factura, su lente y su contraste

> Base verificada (11:00): main `09218b6` (post-cierre 24/09). Suite **818 passed / 0 failed**,
> gate **25 conceptos / 31 quests**, bundle **50 ficheros (481.3 KiB)** fresco. Sin PRs abiertos,
> sin ramas huérfanas (`git branch -a`: solo main; `gh pr list`: vacío), sin turnos fallidos.
> CICLO verde 4 noches. Cero `[BUG]` vivo.

## Selección (curación)

La Subestación quedó SALDADA 4/4 huellas + 1 lectura (E1 chmod / E2 grep / E3 kill / E4 chown).
Hoy no toca verbo nuevo ni quest nueva (Artorias: «no proponer grep/chmod/chown/kill nuevo
sobre ch5»). Toque de hoy: que lo ya jugado **pese y se recoja** —
1. P2 de Gwyn: el 6º estado web que RECONOCE la lectura `grep censo` (hueco honesto de Seath 24/09).
2. P3 🧭47 de Gwyn: el stock de Gris dejó el contraste en 0% estático — medir que pese, con la
   variante de Havel 25/09 (lectura intercalada entre huellas).
3. P2 de Havel 25/09: `grep -c` como vía frugal (bonus, no quest) + factura en el post-mortem —
   la dopamina Balatro «hay un pipe menos que el que trajiste». NO es verbo nuevo ni
   concepto nuevo: es una FLAG dentro del comando `grep` existente — no toca curriculum
   (gate 25/31 intacto) ni allowlist. Es la bandera del veterano, no un verbo nuevo.

Elección por prioridad de Gwyn primero (6º estado), luego valor: fecha 25/09 fresca > fecha 23-24/09.
Recámara (no hoy): awk/xargs pipeline, e5 «La hora que coincide», grep -v grep, tabla de verbos,
logros Testigo forense/Cuatro llaves, pack POSTMORTEM.md (sigue esperando Q con Manus — sin cambio).

## Costuras del día (leer ANTES de abrir la rama)

- **ORDEN DE MERGES: engine → sandbox(harness) → meta-ui**, como siempre.
- **ALLOWLIST OWNER: NADIE** — hoy NADIE añade/quita comandos de `DEFAULT_*_COMMANDS`.
  `grep -c` es una FLAG dentro de `_run_grep` (`.venv` handler ya existente), no un comando:
  los asserts `available_commands == {...}` de e1/e2/e3/e4 quedan intactos. Quien toque
  un assert de allowlist será rechazado por Artorias.
- **GATE OWNER: NADIE** — nadie toca `curriculum.json` (25/31 → 25/31). `grep -c` no añade
  concepto `c.grep_c` hoy (recámara): aceptación en `postmortem._greps`, no en curriculum.
- **BUNDLE: SOLO Ornstein regenera** (`python tools/web/build_bundle.py`) — toca
  `src/data/textos.json`. Smough y Seath NO tocan `src/data/` ni `web/bundle/`.
- **postmortem.py: SOLO Ornstein** lo toca hoy (factura frugal + detección `-c`).
- **shell.py: INTACTO hoy** — la lente web 6º estado lee `get_history()`/`get_ps()` YA expuestos
  en `web/app.js`; nadie añade getters ni toca sandbox/shell.py.

## Asignaciones

### O1 — Ornstein 13:00 · `feat/engine-2026-09-25` · `grep -c` + factura frugal del e2 (P2)

Módulo(s): `src/core/sandbox/commands/texto.py` (`_run_grep`), `src/core/engine/postmortem.py`,
`src/data/textos.json`, `src/tests/core/{sandbox,engine}/`.

No es quest nueva. Reconoce que hay DOS caminos al mismo `1` sobre `ps aux` y lo registra:
- H1. **`grep -c` en `_run_grep`:** flag `-c` devuelta como salida «cuanta»: el stdout es
  el NÚMERO de líneas seleccionadas + `\n` (una línea), igual que GNU (combinable con `-v`/`-i`).
  Sin ficheros usa stdin universalmente, igual que el resto de `_run_grep`.
- H2. **Estado actual honesto:** `_run_grep` hoy RECHAZA `-c` como
  `grep: invalid option -- 'c'` — H1 lo convierte en válido. Tests existentes del cap. 2
  que gritan `invalid option` para otros flags (`-F`, `-o`, `-l`) siguen iguales.

Garantías (byte-idéntica e2):
- `_run_grep(-c censo, stdin=ps aux)` vía pipe `ps aux | grep -c censo` → stdout `1\n`,
  exit 0. `ps aux | grep -c censo | cat` NO exigible hoy (multi-pipe sigue bloqueado por
  diseño — no probar).
- `grep -c ceniza` es la imagen inversa: exit 1, stdout `0\n` (GNU exacto: cuenta cero
  pero exit 1). Si difiere, documentar con test añadido.
- Chequeo cruzado: `-c censo` (ruido 2: ps+grep) es la vía frugal; `grep censo | wc -l`
  (ruido 3) queda documentado como la vía larga de ayer.
- H3. **Factura frugal en post-mortem (solo Ornstein):** si el último grep exit-0 de e2
  (o e1/e3/e4) fue `grep -c censo` → añadir línea `postmortem.auditor.grep_c_count`
  hermana del path que ya existe (`_has_sudo`/`_extract_greps` helper nuevo: UN único
  punto de lectura de lines-grep del historial, por el que pasan los detectores nuevos
  y los de ayer). La huella `auditor_grep` del e2 queda byte-idéntica; el `grep -c` AÑADE
  su línea, no sustituye ninguna.
- H4. **textos.json `story.ch5.e2.hint_2`:** añadir al hint_2 ACTUAL (expansión in-place,
  no claves nuevas; el texto resultante debe leer coherente sin contradecir el atajo
  `grep -i` que ya vive): «La factura frugal: `ps aux | grep -c censo` — la misma línea
  en un comando menos». Toca `src/data/textos.json` — por eso O1 regenera el bundle.

Garantías de entrega (Artorias ya las pide):
- Suite delta declarado **+2..+6 tests** sobre src: `test_grep_c.py` (sandbox) + factura
  frugal postmortem (engine) — Delta EXACTO lo declara Ornstein y lo verifica Artorias
  por aritmética + ensayo worktree; esperado base 818 → **≥820**.
- Los xfails NUEVOS hoy NO se aceptan: `test_grep_c.py` debe correr en verde puro o no
  subir (Artorias lo grita: byte-idéntico SIN sorpresas).
- `[EN CURSO][P2]` `grep -c` cuenta — exit 1 si no hay coincidencias, exit 0 si hay,
  igual que `_run_grep` GNU desde el 22/09 (PR #73); con tests de «0 líneas»
  (`-c ceniza` → `0` + exit 1) y de combinables (`-cv`, `-c -i`).

Costuras de O1: ALLOWLIST OWNER: NADIE. GATE OWNER: NADIE. Bundle: Ornstein REGEN (solo él
toca `src/data/`). `postmortem.py`: Ornstein es el ÚNICO que lo toca hoy — Smough y Seath
NO tocan `postmortem.py`.

### S1 — Smough 16:00 · `feat/sandbox-2026-09-25` · 🧭47 README — contraste stock de Gris + lecturas intercaladas (P3)

Módulo: `tools/harness/run_seeds.py` + `tools/harness/README.md`. CERO cambios en `src/`,
`web/`, `src/data/`, curriculum, allowlist ni postmortem.py (delta +0 tests).

- H1. **Medir el «stock de Gris» hoy (estado):** corpus nuevo `--stock-gris` que, para
  N=500 seeds, compute el `micro_karma` real de e1/e3/e4 (vía `_probar_micro_real()`
  del 24/09) + histograma de veredictos (azul/rojo por sala) — el objetivo NO es cambiar
  pesos (weight 1 quedó recomendado por 🧭45 del 24/09, decisión de Gwyn), es MEDIR la
  distribución real sobre la que §8.6 quiere trabajar. Criterio de observación: contraste
  N=8 K_ventana azul vs rojo (stock Gris 0% como estado hoy), export JSON `stock_gris`.
- H2. **Corpus lectura-intercalada (idea Havel 25/09, P3):** corpus que mezcle la
  LECTURA (`ps aux | grep censo` de e2, gris sin karma) INTERCALADA entre huellas
  (`chmod 600` antes y después de `grep censo`; micro-karma LECTURA+huella en la MISMA
  ventana N=8). Reusa `--micro-karma --karma-seeds 20` ya mergeado (PR #79): ¿el
  veterano que delata antes de decidir cambia K? Criterio: reporte numerado y el
  `% de contraste con lectura intercalada vs sin ella` declarado. Si `grep` debiera ser
  +0.3 azul tenue, es DECISIÓN de Gwyn — Smough SOLO reporta números.
- **Export:** usar la capa `--export` ya existente en `run_seeds.py`; NO crear carpeta
  nueva en harness. Verificación adicional del corpus: correrlo sin crash en N≥20 seeds.
- **AC:** `python tools/harness/run_seeds.py --stock-gris --karma-seeds 20 --export /tmp/stock.json`
  corre SIN ERROR y el payload `stock_gris` sale con números legibles (contraste azul/rojo);
  `pytest src -o addopts='' -q` == **818/+delta de O1** (delta +0 de Smough: la rama no
  ensucia nada). `tools/harness/README.md` actualizado con las cifras del día.

### T1 — Seath 19:00 · `feat/meta-ui-2026-09-25` · 6º estado `grep censo` en la lente del intruso (P2)

Módulo: `web/app.js` + `web/index.html` (+ `web/style.css` si hace falta). Cero cambios en
`src/core/`, `src/data/`, bundle (Gwyn regen canónico cubre), sandbox/shell.py (INTACTO: hoy
la lente usa **`get_history()`/`get_ps()` YA expuestos** en `web/app.js` — línea 153/165 —,
no se añaden getters ni se toca el shell).

- H1. **`_getIntrusoStatus` hermano 6º:** si `get_history()` contiene `ps aux | grep censo`
  (parse la misma rutina que las hermanas `#custodia-postmortem` utiliza para pasar los
  3 estados `cat` desde app.js — sin ejecutar: `MIRA, no toca`), `#custodia-intruso` pasa a
  patrón `grep-verde` (mismo `#2ecc71` con borde fino o icono `⌕`) y PERSISTE.
- H2. **Jerarquía de estados honesta:** HUP (`--reloaded`) y silenciado (-9) PREFIEREN sobre
  `grep-verde` (escribir el mundo > leerlo); `grep-verde` SOLO aparece cuando ni HUP ni kill
  están en el historial (la LECTURA son OJOS, no decisión — coherente §3.1 y con e2 «no karma»).
- H3. **`restartSameSeed` limpia** también el 6º estado; fuera de `?chapter=5` oculto;
  caps 1-4 sin ensuciar. `node --check web/app.js` OK; consola limpia en los 4 estados +
  el 6º; `CUSTODIA_STATIC`/`TRONCAL_STATIC` byte-idénticas.
- **AC:** badge `⌕` verde `grep censo` visible tras la lectura, invisible tras `grep ceniza`,
invisible tras `-HUP`/`-9` (prefieren los verbos). Delta declarado **+0 tests** (web puro),
suite 818→818..820 según Ornstein. Smoke `abrir_encargo` e2 intacto.

## Verificación del día (qué debe gritar verde a 23:00)

- Suite en main tras 3 merges: **base 818 + delta EXACTO declarado de O1** (Artorias lo
  verifica por aritmética + ensayo worktree), sin xfails nuevos; Smough y Seath +0.
- Gate **25/31** intacto; bundle **50 ficheros** fresco tras merge de O1.
- `CUSTODIA_STATIC`/`TRONCAL_STATIC` byte-idénticas; `node --check web/app.js` OK;
  consola limpia; `restartSameSeed` limpia el 6º estado.
- `/tmp/stock.json` (o export declarado) con el payload `stock_gris` de Smough.
- postmortem.py: solo manos de Ornstein (verificado por `git diff --name-only` de su PR).
- activo.md: Gwyn archiva las 3 líneas `[HECHO]` del día al cierre — NINGUNA otra línea
  sale de activo.md sin pasar por archivo (lección 01/09: NO re-clasificar).

## Higiene hecha esta mañana (Gwyndolin)

- activo.md: REZAGADOS de secciones cerradas (16/09–19/09, 14/09, notas de balas archivadas)
  limpiados; las secciones del cierre de 24/09 (veredictos ✅ de las PRs del cierre de
  Gwyn) quedan INTACTAS hasta el archivo de Gwyn.
- abierto.md: `[BUG][P3→P2 22/09]` `grep -v` — ya resuelto (soporte `-v`/`-i` mergeado en
  PR #73, día 22/09): nota ↩ añadida esta mañana; la cola queda viva y vigente. Las
  P2/P3 de Havel 23–25/09 compiten sanamente — la prioridad se juega AL MOVER la línea
  (Gwyndolin al planificar), no al escribirla.
- Prioridades al día: 6º estado P2 (Gwyn) > stock Gris/lectura intercalada P3
  (Gwyn/🧭47) > bonus `grep -c` P2. Sin P0/P1 en cola — correcto (Camino principal APTO).

## El día en una línea (Telegram)

> **HOY:** Ornstein 13:00 `grep -c` + factura frugal del e2 (data + bundle), Smough 16:00 mide
> contraste stock de Gris y lecturas intercaladas (harness puro), Seath 19:00 6º estado web
> `grep censo` verde forense. Artorias 21:00 ensaya merge en orden engine→sandbox→meta-ui;
> Gwyn 23:00 cierra. Higiene: activo.md rezagados limpiados por Gwyndolin esta mañana.
