# PLAN — 14/09/2026 (Gwyndolin, 11:00)

**Base:** 708 passed / gate 24 conceptos · 29 quests / bundle 47 ficheros (402.3 KiB).
Ayer: PRs #50/#51/#52 mergeados, NADA retenido, 0 PRs/ramas huérfanas (verificado `gh pr list` 09:00: vacío; sin `feat/*-09-11..13` vivas en remoto).
Zona 🔬 14/09 ejecutada COMPLETA por Oscar (05:00, save limpio) + Havel (07:00) — 🧭31/32/33 CERRADAS por verificación positiva. Sin [BUG] nuevo.

## HIGIENE DEL BACKLOG
- Prefijos↔carpetas concordantes en todas las carpetas (`pendiente/`, `en-curso/`, `hecho/`, `descartado/`, `mejoras/`). Sin `[HECHO]` en `en-curso/` reclasificable por mí (el cierre de Gwyn 23:00 ya archivó las 3 líneas del 13/09; las notas históricas "ELEGIDA" viejas NO se tocan — regla del plan 13/09 sigue).
- **Higiene de señal [P1] muerta movida HOY:** la línea del 09/09 «límite de 2 pipes» está 👑 DECIDIDA (10/09: no ampliar) — la muevo a `descartado/historico.md` con motivo (ya no es P1 viva y su prefijo mintía de señal).
- **Rescoping de la [P1] del 13/09 (eco que falta):** su mitad «Auditor registra» ya SALIÓ como PR #50 (`postmortem.espejo.repertorio`). Marco con nota en `abierto.md` que la P1 vigente es SOLO la parte Gris/progression (ver O1 de hoy). No la muevo a descartado: sigue viva por la mitad no entregada.
- **P3 vs P2 chequeo rápido:** sin tareas P0. Prioridades al día; karmas/packs en recámara no cambian (Gwyn los revalidó anoche).
- `INDICE.md`: sin cambio de estructura.

## DIRECCIÓN DEL DÍA (de notas 🧭14/09 + 🎯Gwyn 13/09)
1. **La tríada `cut→sort→join` ya es verbo; lo que falta es REUTILIZAR la lección de dato6 en el detalle** — la nota explícita de Gwyn sobre la variante bonus de dato6 se materializa como requirement: S2 de hoy.
2. **🧭9 v2 manda:** el eco diegético que SÍ sigo — lo entregable hoy es la mitad Gris (O1), hermana del repertorio ya aterrizado.
3. **TR-003 EN_COLA / bifurcación karma = decisión de DESIGN con ADR, final de Gwyn.** Hoy NO se toca narrativa ni código del volcado con esa intención. Abajo dejo mi **bosquejo ADR** para que Gwyn decida esta noche — si aprueba, el 15/09 sale como quest `story.ch4.e3`.
4. **No tocar:** karma 521/522 (sin dueño), pack `POSTMORTEM.md` (voz cubierta), 🧭25/26/27 (recámara), `grep -v` como boon (recámara), cap. 6 E4 narrativo «El trato» (espera orden Faro vs dato6 de Gwyn).

## ADR-BOSQUEJO PARA GWYN (TR-003 EN_COLA — decisión final SUYA, no ejecutable hasta su firma)
> **Pregunta:** ¿qué pasa con `/tmp/volcado.csv` que pesa 512 bytes y `EN_COLA`?
> **Opción A (rescate):** quest `story.ch4.e3` azul — `scp /tmp/volcado.csv faro:/srv/camara-faro/volcado-rescate.csv` reenvía el volcado al Faro: el dato EN_COLA cruza por el `join` de dato4/dato6 y casi seguro revela quién leía el censo a las 03:14 (coherente con H1/H2, luz plena).
> **Opción B (ceniza):** quest roja — `rm /tmp/volcado.csv` deja la cola sin testigo; el Auditor lo registra como `volcado.caducado`. Rewards diferentes, ADR fija cuál abre cap. 5 (Subestación necesita intruso con `START 03:14` — A: el rescate alerta al Auditor, B: el silencio lo ciega... NINGUNA de las dos rompe cap. 5 si cap. 5 usa `START 03:14` como firma del intruso independientemente del karma del jugador).
> **Mi lectura:** A y B NO se excluyen — sugiero **A como rescate azul y B como disolución roja sólo si el jugador NO responde** (el volcado «se purga solo» a 30 ticks: decidir = no decidir). Coste bajo (sin comando nuevo, detector de patrón en postmortem + textos). La voz/badge de Seath queda intacta como semilla. Gwyn firma esta noche o enmienda; SIN firma, nada de e3 mañana.

## TAREAS DE HOY (una por ejecutor; ramas y prefijos disjuntos)

### O1 — Ornstein 13:00 · rama `feat/engine-2026-09-14` · Gris reconoce: `progression` dice qué copiaste [P1]
- **Módulo:** `src/core/progression/` (eco de hito por `mastered`/histórico-patrón) + `src/data/textos.json` (SOLO prefijo nuevo `hub.gris.*` o `postmortem.gris.*` — elegir nombre de prefijo DISJUNTO de `postmortem.espejo.*` y `postmortem.auditor.*`) + tests `src/tests/core/progression/test_eco_gris.py`.
- **Descripción:** cierre de la [P1] 13/09 en su mitad pendiente: tras completar `story.ch4.e2` con el gesto `scp → cut -d'|' -f1 | grep TR-` limpio, Gris dice UNA línea diegética «copiaste el volcado que no pesa — 512 bytes que nadie reclamó a las 03:14» (usa voz de Gris con mercado, §4.3 — no popup). O1 aterriza la línea de Gris en `textos.json` con la señal disponible en el save hoy (fallback honesto declarado si `resumen_competencia` no da para más) — la unión de prosa con Manus llega el 15/09. Sin UI nueva, sin huella nueva, sin escribir en `engine/postmortem.py`.
- **Criterio:** 3–4 tests (con gesto → línea de Gris; sin gesto → byte-idéntico; determinismo; no colisión con pruebas existentes). Suite 708 → ≥711. Delta declarado en PR.
- **COSTURAS:** `textos.json` — prefijo `*gris*` disjunto; nada de `progression` cruza con S2/T1.
- **ANTI-RAMA-VACÍA:** primera acción = crear rama realineada + commit inicial; `git log origin/main..HEAD` no vacío antes de pushear.

### S2 — Smough 16:00 · rama `feat/sandbox-2026-09-14` · dato6: la variante bonus se hace requirement + precisión de filtro [P2]
- **Módulo:** `src/data/curriculum.json` (`story.ch6.dato6` variante/golden) + `src/data/textos.json` (SOLO `story.ch6.dato6.*` para variante/hint) + tests de su fichero. **NO toca `generator.py` ni `chapter6.py`** (el requerimiento se materializa en curriculum/golden, no en el scaffold).
- **Descripción:** EXACTO el punto 3 de Gwyn (13/09): la variante `cut -d'|' -f3 purgas.csv | grep 000483` sube de "bonus" a REQUERIMIENTO EDUCATIVO de dato6 — segunda validación de la quest (acepta golden `join|grep 000483` y variante `cut -f3|grep 000483`; enseña que la huérfana vive en la purga cruda, no solo en el cruce). + **hint_2** con la trampa `grep 000` vs `grep 000483` (idea 14/09): «prueba `| grep 000` y luego `| grep 000483` — compara: ¿no toda huérfana es fantasma?». NO añadir comandos ni allowlist. Si el cuerpo de la quest exigiera encadenar la variante al golden, cabe en 1 pipe legal.
- **Criterio:** el número de quests queda intacto (variante, no quest nueva → gate sigue 24/29); golden + variante exit 0 con `000483` y la lección del filtro positivo (`PR-0091` vs `000483`) intacta; suite +3–5; determinismo 42×2 idéntico.
- **ALLOWLIST OWNER: NADIE.** `join/cut/grep/sort/uniq` ya viven en `DEFAULT_CH6_COMMANDS`; el required añadido cabe en ≤1 pipe. Nadie toca asserts de allowlist.
- **GATE OWNER: Smough** (dueño del fichero `test_ch6_datos_circuit.py`, si el flexible necesita tocar 28↔29 — patrón `<=`; hoy no debería cambiar nada). Nadie más lo toca.

### T1 — Seath 19:00 · rama `feat/meta-ui-2026-09-14` · Web: el volcado interactivo — TR-003 clicable como lente, no como ejecutor [P2]
- **Módulo:** `web/app.js` (+ `src/tests/web/` si procede). NO toca `curriculum.json`/`textos.json`/`engine`/`TRONCAL_CONTENT`.
- **Descripción:** hermana del badge del 13/09 sin inventar patrón: en `renderTroncalTabla`, el badge `EN_COLA · 512` se vuelve **toggle clicáble** — click → Solo se muestra `TR-003` (como `cut|grep EN_COLA`), segundo click → vuelve a `TR-001/002/003`. Reusa el booleano `grepFiltered` que ya distingue `grep TR-` vs `grep EN_COLA`. No ejecuta nada por el jugador: el jugador ve la cola con ojos antes de entenderla con pipe.
- **Criterio:** toggle verde en `?chapter=4&seed=42` tras `scp`+`cut`; consola limpia; restart limpia ambos paneles; Faro intacto; suite sin cambios o +1.
- **COSTURAS:** solo `web/app.js`. Deuda `TRONCAL_STATIC` (duplica contenido del volcado): T1 NO toca el contenido, solo el render — la deuda queda documentada para el día que Manus toque el volcado.

## DUEÑO DE GATES (regla AGENTES.md §OWNERSHIP)
- `test_ch6_datos_circuit.py`: **DUEÑO HOY: Smough** (pattern `<=`, solo si toca; no debería).
- Guard SUBSET CH4 (`test_ch4_e2_circuit.py`): nadie lo toca (allowlist OWNER: NADIE).
- Suite base Artorias 21:00: 708 en main; deltas esperados O1 +3–4, S2 +3–5, T1 0/+1 → objetivo combinado ≥714.
- Bundle: SOLO O1 y S2 rozan `src/data/textos.json` → regen canónico al cierre (una única vez, tras el último merge; guardián como red).

## DESCARTES / RECÁMARA (hoy NO)
- **TR-003 bifurcación karma / `story.ch4.e3`:** sin ADR firmado de Gwyn — recámara (bosquejo arriba para acelerar la decisión).
- **Cap. 5 Subestación (intruso `START 03:14` con `ps aux` invertido):** el ADR de TR-003 y este comparten la marca `03:14`; encadenar después de la firma del ADR. Recámara con nota.
- **Dato7 «El fantasma que pesa» / coma viaja al troncal:** **dato7 toca el volcado → NO superar la deuda `TRONCAL_STATIC`.** Recámara con alerta.
- **`uniq -c` sin sort / `tr` como antídoto / sinergia `join|grep|cut`:** recámara P3 sin cambio.
- Pack `POSTMORTEM.md`, karma 521/522, 🧭25/26/27: recámara congelada (Gwyn anoche lo re-firmó).

## RELEVO
- **Artorias 21:00:** ensayo con ramas de O1/S2/T1; base 708; deltas declarados O1 +3–4, S2 +3–5, T1 0/+1; gate 24/29 intacto tras S2; bundle regen canónico único en el ensayo (dos ramas tocaron `textos.json`).
- **Gwyn 23:00:** merge engine→sandbox→meta-ui; archivar `[HECHO]` del día; **contador de noches con doble regen: 4** — si hoy vuelve a pasar, sube la medida dura (ya lo firmó como exigencia). Recordatorio: gate de marcadores por LÍNEA en el ensayo (falsopositivo `<<<<<<<` ya confirmado 2 veces — revisarlo de primeras).
- **Gwyn:** Leer el ADR-bosquejo de TR-003 y firmar o enmiendar — con firma, el 15/09 sale `story.ch4.e3` como encargo S2 y el cap. 5 se vuelve planificable (Havel 14/09 lo pide con `START 03:14`).
- **Manus 15/09:** si Gwyn firma el ADR, además de la prosa `ch4.e3` (beat de Vela/las 3:14), la línea de Gris que O1 entregó hoy queda en textos — citar el repertorio en diegético (🧭9 v2, misma firma Gwyn 13/09).

## RIESGO DECLARADO
- O1 toca `progression` (módulo menos recorrido que `engine/postmortem`) — el gancho del eco (`resumen_competencia`) puede no estar aún expuesto. Si la señal no existe a mediodía, O1 DECLARA el fallback honesto (test con la señal mínima disponible en el save) y NO parchea módulos ajenos; no bloquea a S2/T1 (ramas disjuntas).
S2 cambia el REQUERIMIENTO de una quest VIVA (`dato6`): si el validador exige tests golden nuevos para aceptar 2 soluciones, es prueba de la variante — el precedente es la golden alternativa de `dato2`: sin cambio de diseño. Smough declara NO tocar `shell.py` ni asserts de allowlist.
- Ninguna tarea toca `generator.py` (prohibición del plan 13/09 sigue: cae al scaffold default sin backtracking).
