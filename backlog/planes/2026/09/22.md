# PLAN 2026/09/22 — Gwyndolin, Dark Sun

> Base verificada 11:00: main post-cierre 21/09, suite **788 passed / 0 failed**,
> gate **25 conceptos / 31 quests** (c.stat), bundle **50 ficheros (465.8 KiB)** fresco,
> **0 PRs abiertos · 0 líneas [EN CURSO] vivas** en `activo.md` (todo [HECHO] del 21/09
> archivado por Gwyn 23:00; lo que queda anidado allí es historial/cierres documentados).
> Ramas `feat/*` locales antiguas: residuo de días YA mergeados — no son huérfanas de trabajo.

## 1. HIGIENE DEL BACKLOG (vistazo estructural)

- `abierto.md`: prefijos↔carpetas concuerdan (0 `[HECHO]`/`[APLICADA]` en pendiente,
  0 `[PENDIENTE]` en hecho, 0 `[DESCARTADO]` fuera de su carpeta). → `INDICE.md` sin cambios.
- Prioridades al día: el único `[BUG]` de código vivo (`grep -v`, 🧭27) sube **P3→P2-code** HOY (S1).
- 🎯 Gwyn 21/09 consumida:
  - **Investigación Artorias 21:00 — HECHA.** Diagnóstico (`~/.hermes/cron/output/c4c98c5d8950/2026-09-21_21-02-36.md`): el turno se cortó a media tool-call porque el PROVIDER emitió tags de llamada inválidos (`<atem:parameter>` en vez del formato válido). El scheduler lo marcó «completed» de todos modos. Es el MISMO síntoma documentado de turnos truncados (incidencias del 11/09 y 19/09 en `hecho/2026-09.md`) — no bug de prompt ni de scheduler; el prompt no puede arreglarlo. Remedio sistémico propuesto (§5).
- Piezas listas para integrar: pack `POSTMORTEM.md` — SIN CAMBIO (15ª noche, sin urgencia).

## 2. TAREAS DE HOY (3 piezas pequeñas, 3 ejecutores)

### O1 — Ornstein 13:00 · `feat/engine-2026-09-22` · E1 «La puerta que dejaste»: `chmod 600` vs `chmod 777` como dilema kármico
- **Origen:** idea Havel 21/09 P2 → completa el DÍPTICO con E3 (kill HUP/-9, cerrada ayer): mismo Linux, dos salas, verbo distinto, misma tesis §3.1. Oscar 22/09 la da como recámara de hoy.
- **Módulo:** `src/core/generator/chapter5.py` (scaffold E1 con `mode 644` en `/srv/subestacion/sesiones/pts0` + canon `chmod 600`) + `src/core/engine/postmortem.py` (detector: tras `ls -l` en history → `chmod 600` = `auditor_cierre` azul + micro-karma `{blue:1}`; `chmod 777`/`chmod -R 777` = `auditor_puerta_abierta` rojo + `{red:1}`; sin chmod → byte-idéntico sin huella, como e3 ayer) + `src/data/textos.json` (SOLO prefijos `story.ch5.e1.*` y `postmortem.auditor.cierre*`/`puerta_abierta*`).
- **Criterio de aceptación:** (1) `abrir_encargo(c,'story.ch5.e1',{'c.ls-la','c.cat','c.chmod'},seed 42)` → `abrible True`; (2) `chmod 600` → post-mortem `auditor_cierre` + `{blue:1}` + `ls -l` confirma `-rw-------`; (3) `chmod 777` → `auditor_puerta_abierta` + `{red:1}`; (4) sin chmod → byte-idéntico sin huella (7 vs 11 claves, patrón e3); (5) e1 SIN falsa detección de `kill`; (6) determinismo ×2 seeds (42/99); (7) PR declara «tests antes: 788 · tests rama: M · delta esperado: +7±3»; (8) NO toca `curriculum.json` (gate queda 25/31), `shell.py` (allowlists intactas), ni asserts `DEFAULT_CH4*/CH5*`.
- **ALLOWLIST OWNER: NADIE** (nadie toca asserts de allowlists hoy). **GINE OWNER: NADIE.**
- **Bundle:** Ornstein toca `src/data/` → regen del bundle EN SU RAMA.

### S1 — Smough 16:00 · `feat/sandbox-2026-09-22` · `[BUG]` 🧭27 — `grep -v` / `-i` en `_run_grep`
- **Origen:** único [BUG] de código abierto (11/09, sube P3→**P2-code**); dirección de Oscar: (a) implementar el filtro negativo clásico como deuda.
- **Módulo:** `src/core/sandbox/commands/texto.py` — `_run_grep` parsea `-v`/`-i`; orden GNU real `grep [flags] PATRON [FICHERO]` / stdin vía pipe (`join | grep -v sujeto`).
- **Criterio de aceptación:** (1) `grep -v sujeto purgas.csv` → exit 0 filtra header; (2) `ps aux | grep -v root` → exit 0; (3) `grep -i` insensible a mayúsculas; (4) `grep` sin flags → byte-idéntico a hoy (cap. 2 no rompe); (5) delta declarado `+5±2`; (6) NO toca `curriculum.json`, `shell.py`, ni `web/`.
- **ALLOWLIST OWNER: NADIE** (handler interno, allowlist sin cambiar). **GATE OWNER: NADIE.**

### T1 — Seath 19:00 · `feat/meta-ui-2026-09-22` · Lente del veredicto: post-mortem del juicio EN LA WEB (`#custodia-postmortem`)
- **Origen:** 🧭 de Oscar 22/09 (propuesta 3, validada por Gwyn 21/09): la insignia ANUNCIA, el VEREDICTO vive en el post-mortem — y ese post-mortem aún no tiene lente web.
- **Módulo:** `web/app.js` (helper que lea post-mortem existente — `auditor_hup`/`auditor_kill`/estados de custodia ya serializados — y pinte bajo `#custodia-intruso` la frase del Expediente 000 con el color de insignia correspondiente; SOLO si los datos no llegan ya en el bundle, **declara hueco honesto con delta 0** y `node --check`) + `web/index.html` slot `#custodia-postmortem`.
- **Criterio de aceptación:** (1) `?chapter=5` muestra el veredicto textual conectado al estado (azul tras `--reloaded`, ámbar tras silenciado, verde vivo) — color ya decía ESTADO, la línea nueva dice VEREDICTO; (2) `node --check web/app.js` OK + consola limpia 3 estados; (3) `CUSTODIA_STATIC` byte-idéntica; (4) TRONCAL_STATIC intacta; (5) **NO toca `src/core/`** → sin tests, sin bundle.
- **ALLOWLIST OWNER: NADIE** (web puro). **Bundle: NO regen.**

## 3. ORDEN DE MERGES (Gwyn 23:00): engine → sandbox → meta-ui

- Costuras: solo O1 toca `src/data/` (textos) → bundle regen SOLO Ornstein; Smough código puro; Seath web puro; regen canónico final = Gwyn post-merge.
- `curriculum.json` intocado → gate **25/31** INTACTO hoy. Allowlists intocadas → asserts `<= set(...)` intactos.
- Costura O↔S en `textos.json`: prefijos disjuntos (`story.ch5.e1.*`/`postmortem.auditor.cierre*` vs nada de Smough) — unión trivial si llegaran a rozarse.

## 4. VIVO DE MAÑANA (parcial, sin urgencia)
- 🧭45 (P3): calibración del micro-karma HUP/-9 a 20+ runs con harness — tiene sentido DESPUÉS de que el díptico E1+E3 cierre HOY. Pesos antes que prosa (§8.6).
- 🧭25/26/27 recámara; 🧭24 cerrada con matiz P3.
- Recámara mayor (zoo de señales STOP/CONT, `stat -c '%y %s'`, ghost de Zeta, objeto «Llave del vigilante») — espera cuando la Subestación salde E1-E4 jugables, guiando por dirección de Oscar y notas de Gwyn.

## 5. AUTO-MEJORA (propuestas para Gwyn)
- **[NUEVA] (Gwyndolin 11:00) Remedio sistémico de turnos cortados por tags corruptos.** Problema: el turno de Artorias 21:00 de ayer se cortó a media tool-call porque el provider emitió tags inválidos (`<atem:parameter>`); el scheduler lo marcó «completed» sin trabajo real. La regla HARD de huella (15/09) ya existe y NO basta: la muerte ocurre en la emisión del provider, no en el agente. Propuesta: que Gwyn en su gate nocturno haga `grep -c "atem:" ~/.hermes/cron/output/*/2026-09-*.md` de la jornada — tags rotos en un output = turno FALLIDO DETECTABLE (replanificar al día siguiente), y anotarlo en `docs/USAGE.md`.
- **[NUEVA] (Gwyndolin 11:00) Limpieza de ramas residuales.** Hay 19 ramas `feat/*` locales de días YA mergeados. Gwyn, en sus cierres nocturnos que ya borran las ramas del día, haga `git branch -d` de las residuales (verificando antes con `git branch --merged main`): cosméticos, pero ensucian el `git branch -a` de todos los crons.

## 6. RELEVO
- Ornstein y Smough: sin dependencia mutua (rutas disjuntas). Seath: T1 no depende de O1/S1 — la lente web lee el post-mortem ya serializado; si el texto nuevo de O1 no llegó, hueco honesto.
- Artorias 21:00: gate técnico normal (3 ramas del día) + veredicto CICLO.
- Gwyn 23:00: merges + decidir las 2 propuestas del §5.

**Previsto (cierre Gwyn):** 3 PRs mergeados engine→sandbox→meta-ui, suite **≥795** (deltas declarados verificados), gate **25/31** (intacto), bundle regen por Ornstein en rama + regen canónico post-merge de Gwyn.
