# PLAN 26/09 (Gwyndolin, Dark Sun — 11:00)

> Base verificada a las 09:02 UTC: main `10a2fde` (oscar/havel 26/09), suite
> **837 passed / 0 failed**, gate **25/31**, bundle **50 ficheros (488.3 KiB)**
> fresco, **sin PRs abiertos, sin ramas huérfanas, sin líneas [EN CURSO] vivas**.
> Higiene del backlog hecha esta mañana: `pendiente/abierto.md` limpiado de
> señales muertas (líneas históricas «ELEGIDA» conservadas in situ como
> constancia, NADA re-clasificado), prioridades revisadas — no hay ninguna
> [P0]/[P1] viva sin planificar; la P1 de la PUERTA (19/09) ya saldada 20/09.
> INDICE.md: sin cambios estructurales.

## Dirección del día

La Subestación está SALDADA (4 huellas + 2 lecturas + tríptico web — Gwyn la
cierra: «no proponer verbos ch5 nuevos»). El siguiente NATURAL per Gwyn (🎯
anoche) es **cerrar el arco del Faro con E4 «El trato»**: la confrontación con
Vela que usa tus DOS pruebas (dato4 cruce de tablas + dato5 reloj 11:04) como
palanca legal (DESIGN §3.4.1, backlog/historia ya la premisa). Y la deuda
P3-visible dos noches: el **resolutor canónico de huellas**. Más recámara web
`?seed=` para Juanma.

## Costuras declaradas

- **ALLOWLIST OWNER: NADIE.** Nadie toca `DEFAULT_*_COMMANDS` ni asserts de
  allowlists hoy (E4 usa solo verbos ya vivos en `DEFAULT_CH6_COMMANDS` 16:
  `cat/grep/join/ls`).
- **GATE OWNER: SMOUGH** (único que toca `curriculum.json` — quest
  `story.ch6.e4` + textos). Ornstein NO parchea curriculum.json ni tests de
  gate; verifica su scaffold contra `generate(..., contract_id=...)` con
  aserciones de FS/golden propios en `tests/core/generator/`.
- **BUNDLE: SOLO Smough regenera** (toca `src/data/textos.json`). Ornstein
  (tools/) y Seath (web solo, regen canónico cubre) NO tocan `src/data/`.
- `postmortem.py` / `shell.py` / `session.py`: INTACTOS hoy.
- Orden de merges: engine → sandbox → meta-ui.

## Tareas

### O1 (Ornstein, `feat/engine`) — Resolutor canónico de huellas. [P3 pedida 2 noches por Gwyn]
- `{módulo: tools/, descripción: script `tools/resolutor_huellas.py` que resuelva
  colisiones de huellas (activo.md + worklog) sin reimprimir el ad-hoc cada
  noche: recibe fichero(s) + orden cronológico esperado de secciones `## HH:00`,
  expande marcadores anidados, mantiene UNA sola copia por sección (dedupe por
  contenido idéntico; si difiere, se queda la más reciente y lo advierte),
  assertions de contenido (toda `## HH:00` esperada presente al final) y salida
  con CERO `<<<<<<<` por línea (exit 1 si queda alguno).}`,
- **AC:** python 3 sin deps nuevas; `--check` mode que solo valida (0 marcadores
  + secciones completas); viene con `tests/tools/test_resolutor_huellas.py`
  (~5 tests: dedupe, sección faltante, warning de conflicto, exit codes) en
  delta **+5/0** esperado sobre 837; NO toca `src/`, `web/`, `src/data/`,
  bundle, curriculum, shell, session. README corto en `tools/`.
- PRECEDENTE: ambas noches 24/09 y 25/09 colisionó en exactamente
  `activo.md` + `worklog` — probar el resolutor contra esos dos ficheros en un
  fixture (contenido de ejemplo, no los ficheros reales).

### O2 (Ornstein, misma rama) — Faro E4 «El trato»: scaffold jugable del terreno. [P1]
- `{módulo: src/core/generator/chapter6.py + tests del generator, descripción:
  scaffold de la sala-dato/encargo `story.ch6.e4`: FS del Faro que PLANTA la
  carpeta `/tmp/prueba-custodia/` con los dos testigos que el jugador ya tiene
  — `prueba-cruce.txt` (salida persistida del `join -v 1` con `PR-0091`) y
  `prueba-reloj.txt` (salida de `ps aux | grep 11:04`), generados
  deterministas por seed a partir del mismo rng del Faro; flag estático (no
  depende de ejecución previa real en v0: los contenidos son el golden
  conocido, `PR-0091|EN BLANCO|…|HOSP-47-C` y `START 11:04`, documentado en el
  README del módulo). Sin tocar allowlist (usa solo `cat`, `grep`, `ls` — ya
  viven en CH6), sin tocar curriculum.json (contract_id
  `story.ch6.e4` aceptado si existe; si load_curriculum aún no lo trae, la
  puerta lo gestiona Smough — costura declarada).}`,
- **AC:** `generate(42,6, contract_id='story.ch6.e4')` byte-idéntico ×2 (deter-
  minismo), FS con los 2 testigos presentes (seed 42), navegación/vista con
  `cat` exit 0 sobre ambos, GATE intocado (Ornstein no añade tests de
  curriculum), delta de tests TODO en `tests/core/generator/` (~4), suite
  rama ≥ 841. NO regenera bundle.
- NOTA COSTURA: si Ornstein termina antes y Smough aún no trajo la quest en
  curriculum, el PR de Ornstein se puede mergear sin el de Smough (los tests
  del generator son autónomos); el PR de Smough ES el que abre la puerta
  turística. Orden real de merges: engine → sandbox igualmente.

### S1 (Smough, `feat/sandbox`) — Faro E4 «El trato»: quest + textos (GATE OWNER). [P1]
- `{módulo: src/data/curriculum.json + src/data/textos.json, descripción: quest
  grey `story.ch6.e4` «El trato» con requires `['c.join']` + prereqs
  transitivos ya vivos (c.cut, c.sort, c.ps vía dato4/dato5 — usar la MISMA
  filosofía que dato4: el gate pide `c.join`, la prosa nombra las DOS pruebas
  (cruce + reloj) para inyectar contexto sin enumerar comandos. Briefing/
  beat/hint_1/hint_2 en `textos.json` con prosa del Auditor formulario (§3.4.1
  EL TRATO: la palanca legal es «tengo las dos pruebas — cruzo y camino al
  reloj»), hints sin spoilear la golden (los ficheros son el área; la query es
  del jugador). Ceniza/Vela ya están en la prosa de dato4/dato5: reutilizar
  voz, cero texto nuevo que no sea la 4 líneas del briefing.}`,
- **AC:** `load_curriculum()` gate pasa de 25/31 → **25/32** (una quest nueva,
  cero conceptos nuevos — NADIE sube a 26 conceptos); `abrir_encargo` con
  `c.join` un AND/solo → `abrible True` con seed `story.ch6.e4:42`; sin
  practica de giras irreales de prereqs (el refactor del DAG no entra hoy);
  brief/hint resueltos con textos.json válido (JSON parse OK, bundle
  regenerado en SU rama → 50 ficheros, tamaño puede crecer levemente); test
  gate ~+4 (disjunto de los de Ornstein: `tests/data/test_quest_e4_gate.py`).
  Suite rama esperada 837+4=841.

### T1 (Seath, `feat/meta-ui`) — Recámara web `?seed=` compartible. [P3, pedida 04/09 + Gwyn 🎯]
- `{módulo: web/app.js (+ index.html si hace falta slot), descripción: 1)
  `parseParams` ya lee ?seed= y ?chapter= — hoy faltan las DOS piezas de
  JUICIO: título dinámico `document.title = 'CyberRoot — cap. N — seed M'`
  cuando params traen chapter/seed, para que el link compartible (04/09) sea
  autocolocable; 2) validar que cambiar seed en la URL re-genera un mundo
  distinto visible (no cachea FS viejo): `restartSameSeed` sigue funcionando
  y con seed nueva el FS muta (ch. útil: seed 1 ch4 = 3 hosts vs seed 42 = 2).}`,
- **AC:** `node --check web/app.js` OK; nótese que NO se toca lente
  intruso-owner ni el 6º estado ⌕ (ya saldado el día 25/09), NO toca
  `web/bundle/`, `CUSTODIA/TRONCAL_STATIC` byte-idénticas, consola limpia;
  delta de suite: +0 (web-only, fuente ya cubierta por `node --check`); tests
  NO tocan curriculum/postmortem/session (`shell.py` intacto — lente pureza).


### SIEMPRE (los 3 ejecutores)
- Cierre de PRs: `git status --porcelain` sin ficheros FUERA de rutas del
  dueño; ensayo pre-merge de Artorias OBLIGATORIO orden engine→sandbox→meta-ui;
 declarar en cada PR «tests antes: 837 · tests rama: M · delta esperado: +K».
- Suite esperada tras merges: **841 passed** (837 +5 resolutor +2 generator
 +2 gate +0 web), gate **25/32** (solo S1 sube quests), bundle regen por
 Smough al merge (50+ ficheros).
- Colisión esperada de huellas en `activo.md` + `worklog/2026/09/26.md` —
  Artorias y Gwyn procedan con `tools/resolutor_huellas.py` de O1 (si sale del
  día HOY, peor para mañana: primera vez que se usa en caliente).

## Coherencia narrativa
- E4 «El trato» es la pieza que Havel dejó escrita como P2 el 11/09 y que el
  plot de fase 6 promete (beats 10-11, confrontación Vela). No abre capítulos
  nuevos ni retoca DESIGN: usa EXACTAMENTE tu palanca (join + ps) que ya
  existe, sobre el Faro ya cerrado. Historia coherente: dos testigos de la
  misma noche, un trato con tu voz formulada.
- No se rompe DESIGN (§2.6.5 reglas de teaching, §3.4.1 forma formulario del
  Auditor, §7.5 boons — todo intacto).

## Auto-mejora
- Gwyn pidió explícito: «P3 resolutor canónico» dos noches — ya es tarea O1
  HOY, no propuesta. Nada más que añadir a `mejoras/pendiente` esta mañana.
