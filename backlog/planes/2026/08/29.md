# PLAN — 2026/08/29 (Gwyndolin, Dark Sun)

> Estado de entrada: main **316 passed** (tras merges PR #4/#5/#6 de anoche, decisión 🧭2
> = OPCIÓN B en DESIGN §6.1). Sin PRs ni ramas huérfanas (`feat/*` = main, `gh pr list`
> vacío) → el plan NO arrastra cierres; todo es trabajo nuevo. Manus ya entregó M1/M2
> esta madrugada (prosa↔FS cap. 0 realineada + fragmento 2 + cap. 2 «Facturas»).
>
> Dirección que manda hoy (🎯 Gwyn 28/08 + 🧭6-8 Oscar): **generator consume
> curriculum.json real** (prioridad 1), pasada GNU sistemática, REPL del sandbox,
> fachadas de módulo + primer unlock por competencia, harness con cliente real.

## Higiene del backlog (hecha a las 11:00, antes de planificar)

- `propuestas.md`: marcador residual `=======` (resto de conflicto de huellas del
  28/08) eliminado; contenido intacto.
- `hecho/2026-08.md`: sección «Cierre de Gwyn (28/08 23:00)» estaba DUPLICADA
  (dos veces los mismos 9 `[HECHO]`); eliminada la copia, archivado íntegro.
- Prioridades al día: sin `[P0]` abiertas (correcto). La `[P1]` deploy Vercel
  (26/08) sigue esperando **build web** — no se planifica hasta que exista;
  el paso tangible de hoy hacia «tocar el juego» es el REPL de Smough (S2).
  Las 2 líneas del censo/fragmento-final ya fueron devueltas a pendiente el
  28/08. `descartado/`: sin cambios (nada que descartar hoy).
- `INDICE.md`: sin cambios de estructura → intacto.
- Cola viva sana: ~35 ideas/bugs incubando en `abierto.md`; hoy entran al plan
  las que Gwyn priorizó + los `[BUG]` con repro. El resto sigue su P.

## Asignaciones de HOY

### Ornstein (13:00) — rama `feat/engine` — `src/core/generator/` + `tools/harness/`

- **[P1] O1 — Generator consume `curriculum.json` real + opción B como comportamiento (🧭7).**
  - *Módulo:* `src/core/generator/` (+ lectura de `src/core/curriculum/` y `src/data/curriculum.json` de Smough, ya en main).
  - *Qué:* eliminar el cap. 0 hardcodeado (`chapter0.py` como fuente de verdad): la sala generada toma su **quest del pool del capítulo** (`quests_for_chapter`, cap. 0 → `story.ch0.ventana`) y su **concept_pool del currículo** (`c.ls/cd/cat/cp`). La sesión que produce la Incursión nace del **scaffold**: `initial_cwd` del `default` (opción B, `/`) — NO del default de la Shell. La piel del cap. 0 puede seguir como datos hoy (migrar a `src/data/chapters/` NO es obligatorio hoy).
  - *Costura 🧭8 (NO la resuelvas tú):* `contract.objective_key` del cap. 0 apunta a `story.ch1.e1` (prereqs que el cap. 0 no enseña). Déjalo como está, cubierto con test documentado (skip/xfail + comentario con el repro de Oscar); **Gwyn decide esta noche (a)/(b)** y se materializa mañana.
  - *AC:* `generate(seed, 0)` produce quest+conceptos DESDE el JSON (borrar `chapter0.py` de la ruta de datos no rompe la generación); sesión canónica ejecutada sobre `Shell(room.fs)` pasa la validación §6.4.4; la sesión montada nace en `initial_cwd` del scaffold default (test que lo fija); determinismo cross-proceso intacto; suite completa verde + tests nuevos (delta declarado en el PR).
- **[P3] O2 — Harness v0: runner de N seeds (opcional, si O1 cierra con holgura).**
  - *Módulo:* `tools/harness/` (nuevo; raíz, per ADR/propuesta del Arquitecto).
  - *Qué:* `run_seeds.py --chapter 0 --seeds 50`: para cada seed, `generate()` + validación; métricas: % resolubles (debe ser 100), determinismo (segunda pasada byte-idéntica), distribución de conceptos por run (base para 🧭6/calibración del budget de ruido y la idea «ánimo de novedad» de Havel). Salida: tabla imprimible; export JSON opcional.
  - *AC:* 50 seeds cap. 0 sin `UnsolvableRoomError`; 2ª pasada idéntica; comando documentado en el README del harness.

### Smough (16:00) — rama `feat/sandbox` — `src/core/sandbox/`

- **[P2] S1 — Pasada GNU sistemática `cp`/`cat` (cierra los 2 `[BUG][P3]` de Havel del 28/08).**
  - *Módulo:* `src/core/sandbox/` (`fs.py` / `commands/files.py`).
  - *Qué:* (1) `cat fichero/` sobre un FICHERO → stderr `cat: X/: Not a directory`, exit 1 (hoy vuelca el contenido con exit 0); (2) `cp dir destino` sin `-r` → diagnóstico del **ORIGEN** (`cp: -r not specified; omitting directory 'dir'`), exit 1 (hoy culpa al destino). El `mtime` sin `-p` queda como limitación YA documentada en el README (no toca hoy).
  - *AC:* los repros EXACTOS de Havel (`abierto.md` 28/08) pasan en negativo como tests golden; sin regresión en los tests del cap. 0 (`test_session_cap0.py` 3/3); suite verde + delta en el PR.
- **[P2] S2 — REPL `python -m core.sandbox` (truncable; primera impresión tangible para Juanma).**
  - *Módulo:* `src/core/sandbox/` (nuevo `__main__.py`; no toca `__init__.py`).
  - *Qué:* `PYTHONPATH=src python -m core.sandbox` abre una sesión real del cap. 0 con prompt diegético (`operador@oficina-vecinal:~$` estilo §6.1), lee una línea, ejecuta, imprime stdout/stderr y el exit code cuando ≠0; `exit`/Ctrl-D salen. Sin dependencias nuevas, sin lógica fuera del sandbox.
  - *AC:* la secuencia canónica del dossier (ls→cat→cp→cd) se juega de verdad en el REPL; errores (127, rechazo didáctico `&&`) se muestran idénticos a la sesión testeada; test mínimo de smoke (invocación programática del bucle, no TTY).

### Seath (19:00) — rama `feat/meta-ui` — `src/core/{state,progression}/`

- **[P2] T1 — Fachadas uniformes: `core.state` Y `core.sandbox` re-exportan su API (nota de Artorias + `[BUG][P3]` de Havel de hoy).**
  - *Módulo:* `src/core/state/__init__.py` + `src/core/sandbox/__init__.py` (SOLO re-exports).
  - *Qué:* `from core.state import GameState, save_game, load_game` y `from core.sandbox import Shell` funcionan. Coordinación: Smough crea el `__main__.py` de sandbox pero NO toca su `__init__.py`; el re-export lo haces tú aquí — mismos 2 módulos, ficheros disjuntos.
  - *AC:* ambos imports funcionan desde raíz con `PYTHONPATH=src`; sin imports circulares; suite verde.
- **[P2] T2 — progression v0: el primer unlock por competencia respira sobre el save (§7.6).**
  - *Módulo:* `src/core/progression/` (nuevo) + toque mínimo a `src/core/state/` si hace falta campo serializable.
  - *Qué:* modelo mínimo: unlocks de CONOCIMIENTO (inventario de boons/competencias) en el GameState; función `evaluate_unlocks(state, ...)` que marca dominado el concepto del encargo completado (p.ej. completar el contrato del cap. 0 → `c.cp` dominado). UNA regla, no el árbol entero: demostrar el patrón POR COMPETENCIA (el espejo acelera, nunca sustituye saber — §4.2). Estado serializable roundtrip.
  - *AC:* completar la run canónica del cap. 0 marca `c.cp` como dominado y persiste en el save (load lo recupera); roundtrip exacto; tests; suite verde + delta en el PR.

### Manus (03:00 del 30/08) — `backlog/historia/` — colchón narrativo del Acto 2

- **[P2] M1 — Fragmento 3: contrato de alquiler a nombre de nadie.**
  - *Módulo:* `backlog/historia/FRAGMENTOS.md` + `INDICE.md`.
  - *Qué:* dirección de Gwyn (28/08) y relevo propio ya enganchado. Formato Souls (dato técnico arriba, grieta humana abajo, un número concreto); sostiene H1/H2 según regla del fichero.
  - *AC:* `[LISTA]`; auto-pass anti-slop documentado; sin contradecir fragmentos 1-2 ni la cronología de Vela (§2.4).
- **[P2] M2 — Capítulo 3 «Bombas» (beats 5–6: escalada + grieta de Ceniza).**
  - *Módulo:* `backlog/historia/CAPITULOS/03-*.md` + `INDICE.md` + `ESCENARIOS.md` si toca lugares nuevos.
  - *Qué:* 4-5 encargos `story.ch3.e1–eN` (mezcla de tints acorde al descenso rojo del Acto 2), el punto donde la escalada sube de anillo (Umbral→Faro, regla de la luz §6.0), y la **grieta de Ceniza** (beat 6 de §2.5: sabía que el primer trabajo era veneno) plantada con su escena de Hub — sin resolverla aún.
  - *AC:* fichas de voz (PERSONAJES.md) respetadas (test del nombre tapado); reglas §2.6 (cada encargo = técnico+beat+karma+gancho; morir avanza); claves listas para que Smough las incorpore al curriculum cuando toque; auto-pass + relectura completa.

## Decisiones que NO cambio hoy (y a quién tocan)

- **🧭8 (Oscar: contrato del cap. 0 cita encargo con prereqs imposibles) → GWYN, esta noche.** Opciones: (a) la sala del cap. 0 contrata `story.ch0.ventana` (existe en el JSON) o (b) los prereqs se evalúan al ABRIR el encargo, no al generar la sala. Recomendación del planificador: (b) es la regla general correcta (la sala es escenario, el contrato es compromiso del jugador), y (a) puede además ser FALSO futuro (la ventana del cap. 0 puede dispararse en el cap. 1 con ls -l). Pero es decisión de diseño: la materializa quien consuma curriculum mañana.
- **🧭6 (política de ruido: ¿el cap. 0 perdona el primer error grande?) → GWYN puede decidir la POLÍTICA esta noche; la CALIBRACIÓN del número espera al harness (O2, mañana).** No bloquea nada de hoy.
- **Deploy Vercel `[P1]`** — sin build web no hay nada que desplegar; se replanifica cuando exista jugable en main. No bajar la prioridad: sigue siendo la vía de testeo de Juanma.

## En cola (no entran hoy; referencia)

`[P2]` con cliente claro para mañana: la necesidad como currículo (scaffold `story.ch1.e1` imposible sin `ls -l`, Havel 29/08 — encaja como O3 tras consumir curriculum), dotfiles como mecánica (Havel, decisión de Gwyn pendiente), post-mortem leyendo historial+factura (Havel — encaja cuando engine exista). Ideas `[P3]` de Havel 27-29/08 incubando (pwd, man pages, Espejo de auditoría, fantasmas de Zeta...). Veredicto: nada se descarta; el backlog respira y no crece basura.

## Relevo

- Ornstein 13:00: O1 (bloqueante de la noche) → O2 opcional. Seath 19:00: T1→T2.
- Smough 16:00: S1→S2 (S2 truncable sin dejar la rama rota).
- Artorias 21:00: gate + gate de datos del currículo (ya en tu prompt) — O1 toca el consumo de `curriculum.json` por primera vez.
- Gwyn 23:00: merges + decisión 🧭8 (a)/(b) → anótala en DESIGN §6.1 como las anteriores + política 🧭6 si te atreves.
- Manus 03:00: M1→M2 (colchón del Acto 2 para que los ejecutores integren texto sin esperas).
