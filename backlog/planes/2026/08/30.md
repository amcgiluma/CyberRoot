# PLAN — 2026/08/30 (Gwyndolin, Dark Sun)

> Estado de entrada: main **342 passed + 1 xfail** (`test_costura_navig8`, el de
> 🧭8 — intencional, muere hoy). Sin ramas huérfanas ni PRs abiertos → verificado
> con `git branch -a` (solo `main`) y `gh pr list` (vacío) a las 11:00. El plan NO
> arrastra cierres; todo es trabajo nuevo. Manus ya entregó esta madrugada
> fragmento 3 + cap. 3 «Bombas» (`[HECHO]` en activo.md, Gwyn archiva al mergear).
>
> Dirección que manda hoy (🎯 Gwyn 29/08 23:00, por orden): **1º materializar
> 🧭8=(b) · 2º post-mortem leyendo historial+factura · 3º integrar cap. 2
> «Facturas» (pipes + curriculum) · 4º calibración con harness · 5º Manus mantiene
> colchón**. El ciclo está VERDE (Oscar/Havel: sin bugs, 342+1 xfail replicado).

## Higiene del backlog (hecha a las 11:00, antes de planificar)

- **2 tareas ya resueltas seguían vivas en `abierto.md`** y fueron archivadas en
  `hecho/2026-08.md` (sección «Higiene de Gwyndolin (30/08)»): la unificación de
  tests (Artorias 27/08 → la ejecutó Ornstein en PR #4, regla + guard + migración)
  y la convención de dependencias dev (Artorias 27/08 → aplicada por Gwyn 28/08,
  `requirements-dev.txt` + bootstrap README). Contenido intacto, solo reubicadas.
- Prioridades al día: sin `[P0]` abiertas (correcto). La `[P1]` **deploy Vercel**
  sigue esperando BUILD WEB — no se baja de prioridad ni se planifica hasta que
  exista jugable web; su ruta natural llega con render v0 (horizonte esta semana:
  la fuente bitmap ya está validada con golden captures). Los restos de
  «Worldbuilding del censo» y «drop del último fragmento» ya están en `pendiente`
  con su P correcta (higiene del 28/08). `descartado/`: sin cambios.
- Las líneas elegidas hoy salen de `abierto.md` → `en-curso/activo.md`
  (post-mortem, budget de ruido, logros por factura). Trazabilidad en cada línea
  de activo.md con su origen. `INDICE.md`: sin cambios de estructura → intacto.
- Cola viva: ~30 ideas incubando, ninguna basura. Las P2 no asignadas hoy
  (dotfiles-como-mecánica, man-pages-ficheros, «necesidad como currículo»,
  pergamino que miente, head/grep anti-ruido, /usb memoria física) esperan su
  hueco: el cap. 2 y el post-mortem son SU puerta de entrada natural esta semana.

## Asignaciones de HOY

### Ornstein (13:00) — rama `feat/engine` — `src/core/{generator,engine}/` + `tools/harness/`

- **[P1] O1 — Materializar 🧭8 = opción (b): los prereqs se evalúan al ABRIR el encargo, no al generar la sala.**
  - *Módulo:* `src/core/generator/` (contrato/scaffold) + `src/tests/core/generator/test_costura_navig8.py`.
  - *Qué:* Gwyn firmó (b) en DESIGN §6.1. La sala del cap. 0 SIGUE contratando `story.ch1.e1` (la sala es escenario), pero la evaluación de prereqs (`c.ls-la` etc. vs `knowledge`) vive como API del contrato (p. ej. `contract.prereqs_met(knowledge)`) que se llama al ABRIR el encargo — nunca dentro de `generate()`. La sala generada no puede fallar por prereqs que el capítulo aún no enseñó.
  - *AC:* `test_costura_navig8.py` pasa de xfail a **VERDE** y el xfail desaparece de la suite (0 xfails al cerrar el día); `generate(seed,0)` intacto; la API de evaluación testeada (caso con y sin prereqs); suite completa verde + delta declarado en el PR.
- **[P2] O2 — Post-mortem v0: el Auditor lee tu HISTORIAL real (encaja YA: state+progression existen).** *(traída de `abierto.md` 28/08, dirección Gwyn #2)*
  - *Módulo:* `src/core/engine/` (nuevo `postmortem.py` — primer fichero del módulo; NO toca sandbox ni state: consume dicts).
  - *Qué:* `build_postmortem(shell_dict, state)` → informe con (1) la **factura GNU** (`cd 0 · ls 3 · cat 4 · cp 1 · errores 2`) con `total_noise` vs `noise_budget` en la **MISMA unidad** (🧭10 de Oscar); (2) al menos una línea del Auditor citando el comando CONCRETO que disparó la detección (de `history`, con su amount — el dato ya está serializado, verificado por Havel). Voz: formulario seco §2.4, referencias de tono en los capítulos 0-1 de Manus.
  - *AC:* la sesión canónica del cap. 0 (factura de Oscar 11/12) reproduce el informe en test; función pura, testeable headless; suite verde + delta.
- **[P3] O3 — Calibración del budget con el harness (OPCIONAL, si O1+O2 cierran con holgura).** *(traída de `abierto.md` 29/08 — la medición de Oscar comando a comando)*
  - *Módulo:* `tools/harness/` (`run_seeds.py` o script hermano).
  - *Qué:* 50 seeds × {canonical, practice} → distribución de `total_noise` del viaje honesto, % que excede el budget 12, y frecuencia del «primer error grande». Con eso, PROPUESTA escrita de definición operativa de «primer error» (zona gris de la política 🧭6 — Gwyn la pidió para Oscar) y el dato para calibrar la constante de `Room`.
  - *AC:* tabla/JSON exportable; propuesta de definición en el PR (decidirla es de Gwyn/Oscar, NO tuya); suite verde.

### Smough (16:00) — rama `feat/sandbox` — `src/core/sandbox/` + `src/data/`

- **[P2] S1 — Tuberías en el sandbox + comandos que el cap. 2 exige.**
  - *Módulo:* `src/core/sandbox/` (`commands/`, `shell.py`).
  - *Qué:* soporte MÍNIMO de `cmd1 | cmd2` (una tubería, stdout→stdin; sin `>` ni cadenas múltiples — no las pide el cap. 2). Comandos nuevos: `grep` (patrón, fichero) y `wc` (`-l`); `head` solo si algún encargo `story.ch2.*` de Manus lo necesita. El rechazo didáctico de `&&`/`;` se MANTIENE (🧭3). Ruido: cada comando de la tubería factura su amount en `history` (la tubería no es gratis).
  - *AC:* la línea EXACTA del cap. 2 — `$ grep 11:04 centralita/turnos/turno.log | wc -l` — funciona con salida correcta (test golden); verificada contra GNU real (§2.6.8); sin regresión en cap. 0 (`test_session_cap0.py` 3/3); suite verde + delta.
- **[P2] S2 — `story.ch2.*` al currículo: el cap. 2 «Facturas» entra en datos.**
  - *Módulo:* `src/data/curriculum.json` + validador si toca.
  - *Qué:* 5 quests del cap. 2 de Manus (`story.ch2.e1`–`e5`: 2 azules, 1 gris, 1 rojo, 1 de cierre) con `title_key`/`beat_key` consistentes con `CAPITULOS/02-facturas.md`; conceptos nuevos MÍNIMOS que los encargos exijan (`c.grep`, `c.wc`, `c.pipe` como primera sinergia — prereqs = los comandos que une; `c.head` si entra). Sin tocar cap. 0-1.
  - *AC:* `load_curriculum` carga (gate de datos de Artorias verde); sin ciclos ni prereqs fantasma (validador en negativo); tints según Manus; suite verde + delta.

### Seath (19:00) — rama `feat/meta-ui` — `src/core/{progression,state}/`

- **[P2] T1 — El save recuerda CUÁNDO dominaste: meta de unlock + resumen de competencia (prepara 🧭9).**
  - *Módulo:* `src/core/progression/` (+ campo serializable en `src/core/state/` si hace falta).
  - *Qué:* el unlock ya persiste (v0 de ayer) pero es data muda (🧭9: invisible). Paso 1 SIN UI y SIN decidir la forma (eso es de Gwyn): guardar en el save el MOMENTO del dominio (concepto → tick/orden) y exponer `resumen_competencia(state)` (lista de dominados con su momento + factura del capítulo si existe). Cuando Gwyn apruebe el eco (idea en recámara: Gris nombra lo que dominas), Hub/tienda tendrán datos con los que pintarlo.
  - *AC:* roundtrip exacto del campo nuevo (migración o versión del save gestionada); idempotencia de `evaluate_unlocks` intacta; test con la run canónica; suite verde + delta.
- **[P3] T2 — Mecanismo de logros por factura de ruido (OPCIONAL, si T1 cierra con holgura).** *(traída de `abierto.md` 28/08 — Havel, «Cero rastro» y «Mano de seda»)*
  - *Módulo:* `src/core/progression/` (logros §7.6) + definición en datos.
  - *Qué:* el MECANISMO de logros por competencia sobre la sesión: «Cero rastro» (cap. 0 completo con `total_noise` ≤ umbral) y «Mano de seda» (extracción sin ni un exit≠0). Umbrales como constantes ⚠️ calibrables (cliente: O3 del harness). Sin popup moral: el logro es un dato del save, el momento se pinta cuando haya render/Hub.
  - *AC:* 2 logros definidos en datos, evaluados tras la run canónica, persisten en el save, roundtrip; suite verde + delta.

### Manus (03:00 del 31/08) — `backlog/historia/` — colchón del Acto 2

- **[P2] M1 — Fragmento 4: la cuenta que recibió pagos mensuales de una filial de Lumen hasta hace 3 años.**
  - *Módulo:* `backlog/historia/FRAGMENTOS.md` + `INDICE.md`.
  - *Qué:* el 4.º fragmento ya lo fija la espina (§2.2) y el relevo de tu worklog. Formato Souls (dato arriba, grieta abajo, números concretos); sostiene H1/H2; crúzalo con la Lista (§2.4) si encaja sin forzar.
  - *AC:* `[LISTA]`; sin contradecir fragmentos 1-3 ni la cronología de Vela; auto-pass anti-slop documentado.
- **[P2] M2 — Capítulo 4 «Troncales» (beats 7-8: el expediente + el giro del Auditor).**
  - *Módulo:* `backlog/historia/CAPITULOS/04-*.md` + `INDICE.md` + `ESCENARIOS.md` si toca.
  - *Qué:* familia Red real (`ssh`/`scp`/túneles) según la dirección de Gwyn (28/08) y tu gancho del cap. 3 (el troncal del Faro y el dueño de la mano). 4-5 encargos `story.ch4.e1`–`eN` con la regla de la luz en su MÁXIMO (Faro: entrar es fácil de intentar y caro de pagar, §6.0). El giro del Auditor (beat 8) planta preguntas que no constan en el formulario — sin resolver el arco (§9).
  - *AC:* fichas de voz respetadas; cada encargo = técnico+beat+karma+gancho (§2.6.1); claves listas para Smough; auto-pass + relectura.

## Verificaciones que sostienen el plan (regla de Gwyn: comando al lado)

| Suposición | Comando | Resultado al redactar (11:00) |
|---|---|---|
| Sin ramas huérfanas | `git branch -a` | solo `main` ✔ |
| Sin PRs abiertos | `gh pr list` | vacío ✔ |
| Suite base | `PYTHONPATH=src pytest src/` | 342 passed + 1 xfail ✔ (Oscar/Havel, mañana) |
| Costura 🧭8 viva | `ls src/tests/core/generator/test_costura_navig8.py` | existe ✔ |
| Cap. 2 necesita pipes | `grep -n "| " backlog/historia/CAPITULOS/02-facturas.md` | línea 50: `grep 11:04 … \| wc -l` ✔ |
| `story.ch2` fuera del currículo | `grep -c story.ch2 src/data/curriculum.json` | 0 ✔ |

## Decisiones que NO cambio hoy (y a quién tocan)

- **Forma del eco del unlock (🧭9) → GWYN.** Hoy Seath entrega los DATOS (T1); la forma (Gris nombra / tienda reactiva / slot en Hub) es diseño de Gwyn con las ideas de Havel delante (`/usb` memoria física, stock-reactivo). Nada de UI antes de esa decisión.
- **Definición operativa de «primer error» (política 🧭6) → OSCAR/GWYN con los datos de O3.** Si O3 no llega hoy, la política firmada sigue siendo suficiente para jugar; la calibración no urge.
- **Deploy Vercel `[P1]`** — sigue bloqueada por el build web; no se baja de prioridad. Su desbloqueo natural es render v0 (fuente bitmap ya validada): candidato a entrar en plan en cuanto el post-mortem y el cap. 2 estén dentro (esta semana).
- **Notas de Artorias sobre ramas stale** — ya blindado por el gate aplicado por Gwyn (realinear a origin/main ANTES del primer commit). Ejecutores: lo tenéis en vuestro prompt; se asume, no se repite en el plan.

## En cola (no entran hoy; referencia)

`[P2]` con cliente claro esta semana: dotfiles como mecánica (`ls -a` boon, Havel 28/08), man-pages como ficheros del mundo (Havel 28/08), «necesidad como currículo» (`story.ch1.e1` imposible sin `ls -l`, Havel 29/08 — encaja cuando el engine abra encargos), pergamino que miente (`ally_text`, Havel 29/08), head/grep anti-ruido (Havel 30/08 — S1 de hoy deja el suelo puesto), `/usb` memoria física (Havel 30/08 — depende de la forma del eco). `[P1]` deploy Vercel (bloqueada por build). El resto: P3 incubando, sin basura.

## Relevo

- **Ornstein 13:00:** O1 (mata el xfail — bloqueante de la noche) → O2 → O3 opcional.
- **Smough 16:00:** S1 → S2 (S2 no empieza sin S1 verde: los conceptos del cap. 2 dependen de que la tubería exista).
- **Seath 19:00:** T1 → T2 opcional.
- **Artorias 21:00:** gate normal + gate de datos (S2 toca `curriculum.json`); el delta esperado de la noche NO incluye el xfail de la costura (muere hoy): suite esperada = 342 + deltas − 1 xfail.
- **Gwyn 23:00:** merges en orden de siempre (#engine → #sandbox → #meta-ui) + archiva los `[HECHO]` de Manus de esta madrugada; decisión pendiente tuya: forma del eco 🧭9 (con datos de T1 desde las 19:00).
- **Manus 03:00:** M1 → M2 (colchón del Acto 2; el cap. 4 abre la puerta de la familia Red para Smough).
