# PLAN — 2026/08/28 (Gwyndolin, Dark Sun)

> Turno 11:00. Insumos: `pendiente/abierto.md` (post-higiene), `en-curso/activo.md`,
> notas 🧭 de Oscar (28/08) y 🎯 de Gwyn (noche del 28/08), trabajo de
> Manus/Artorias/Havel del ciclo de hoy. Sin ramas ni PRs huérfanas (`feat/*` = main,
> `gh pr list` vacío): el día empieza LIMPIO y sin cierres de ayer que arrastrar.

## 0. HIGIENE ejecutada hoy (antes de planificar)

1. **2 tareas de decisión del 27/08 archivadas** → `hecho/2026-08.md`: su decisión
   D1 ya está materializada (Manus) y verificada jugando (Oscar); eran pendientes
   de nombre: tutorial `cp` cap. 0 y run 0 falible.
2. **Censo (P1→P2)** y **drop del último fragmento (P3)** devueltos de
   `en-curso/activo.md` a `pendiente/abierto.md` — un `[PENDIENTE]` vive allí, no
   en en-curso. El censo baja CON MOTIVO: no desbloquea el camino crítico actual
   (curriculum/generator/state) ni los cap. 0–4.
3. `descartado/historico.md`: íntegro (1 `[DESCARTADO]` legítimo). `mejoras/`:
   todo `[APLICADA]`/parcial, nada que reparar. `hecho/2026-08.md`: íntegro.
   `INDICE.md`: sin cambios de estructura → sin retocar.
4. Prioridades al día: el `[BUG][P2]` del encadenado `&&`/`;` es el defecto de
   mayor impacto de jugador del día (rompe la PRIMERA sesión); los otros 2 bugs
   son P3 finos de contrato GNU.

## 1. Lectura del día (qué manda)

- **Dirección Gwyn (🎯)**: Smough = canje Event + `curriculum.json` v0;
  Ornstein = `generator` v0 contra el sandbox ya mergeado; Seath = `state` v0;
  Manus = fragmentos 2–3 / cap. 2. La adopto como espina del plan.
- **Oscar (🧭2)**: el andamiaje de ruta de la run 0 (cwd inicial / rutas del
  dossier) debe decidirse ANTES de que generator monte la piel del cap. 0 →
  **bola al pie de Gwyn esta noche (§4)**; Ornstein monta HOY con el andamiaje
  como DATO configurable, no como decisión propia.
- **Regla de tamaño**: cada tarea 1–2 h, truncable sin deuda. Lo que no quepa,
  pasa a mañana fileado, sin arrastrar bloqueos.

## 2. TAREAS DE HOY (asignación sin colisiones de git)

### Ornstein (13:00) — rama `feat/engine` — generator v0 + convención de tests
- **O1 · Convención única de tests** `[P1]` — Módulo: `src/tests/` (regla+layout).
  Qué: fijar en `src/tests/README.md` la regla propuesta por Artorias (27/08):
  tests SIEMPRE en `src/tests/core|render|assets/...` espejando el árbol;
  PROHIBIDO cualquier `__init__.py` que cree paquetes `tests` alternativos (la
  causa del bug del PR #3). Verificar que `src/assets/tests/` no deja ficheros.
  Criterio: suite desde raíz **225 passed**; grep negativo de `__init__.py` bajo
  cualquier `src/**/tests/` distinto de `src/tests/`.
- **O2 · generator v0: seed→sala con piel real** `[P1]` — Módulo:
  `src/core/generator/`. Qué: primera API determinista `generate(seed, chapter)`
  que produce UNA sala del cap. 0 con la piel exacta de `CAPITULOS/00-la-firma.md`
  (oficina-vecinal-muelle-norte, CANDELAS nº 47, ventana 11:04) y un encargo del
  cap. 1 (`story.ch1.e1`) apuntando a su técnico+beat. NO depende de
  `curriculum.json` (usa los conceptos ya activados: ls/cd/cat/cp). RNG: la
  splitmix64 de `common`. El andamiaje de la run 0 (cwd inicial, rutas del
  dossier) queda expuesto como DATOS/constante a la espera de la decisión de
  Gwyn de esta noche — hoy el plan NO decide esa dirección. Aceptación: 2 seeds
  distintas → 2 salas distintas; misma seed, 2 ejecuciones → salida idéntica;
  `./.venv/bin/python -m pytest src/tests/core/generator/ -o addopts=` verde;
  no toca `src/data/curriculum.json` (es de Smough) ni state/progression/karma.
- **O3 · (solo si O1+O2 cierran con margen) esqueleto del harness** `[P3]` —
  Módulo: `tools/harness/`. Qué: runner de N seeds + CSV de métricas básicas
  (ticks, ruido total, exits≠0) por seed. Sin métrica GNU todavía. Truncable
  sin daño; es el cliente futuro de la «factura GNU» de Havel.

### Smough (16:00) — rama `feat/sandbox` — curriculum v0 + bug encadenado
- **S1 · Canje dicts→`common.events.Event`** `[P1]` (deuda del 27/08 — PRIMERO) —
  Módulo: `src/core/sandbox/noise.py`. Qué: los eventos de ruido dejan de ser
  dicts con forma de `Event` y pasan a la clase de common (importación + ajuste
  de tests; quedó documentado en su worklog). Criterio: suite sandbox verde;
  suite desde raíz 225 passed.
- **S2 · `curriculum.json` v0 + validador** `[P1]` — Módulo:
  `src/core/curriculum/` + `src/data/curriculum.json`. Qué: DAG mínimo REAL de
  los cap. 0–1: los 4 conceptos activados + los encargos `story.ch1.e1`–`e5`
  como nodos-dato con prerrequisitos (los textos vienen de Manus, las claves ya
  existen); validador de ciclos/prerrequisitos + esquema documentado en el README
  del módulo. Es LA pieza que desbloquea generator mañana. Criterio: el validador
  rechaza DAG con ciclo y con prereq inexistente (tests en negativo); contrato
  de consumo anunciado en la PR para que Ornstein integre sin reescribir nada.
- **S3 · [BUG][P2] encadenado `&&`/`;` → rechazo didáctico** — Módulo:
  `src/core/sandbox/shell.py`. Qué: añadir `&` y `;` a `_UNSUPPORTED_SYNTAX` +
  tests en negativo con los 3 repros EXACTOS de Oscar (zona 🔬 28/08) y mensaje
  que insinúe el futuro («esta sesión va comando a comando; el encadenado llega
  después» — 🧭3 de Oscar). TRUNCABLE: si S1+S2 consumen la franja, S3 pasa a
  mañana sin deuda nueva (queda fileado en abierto). Criterio: los 3 repros
  devuelven el mensaje didáctico exit 2 (ninguno culpa a `cd` ni trata `&&` como
  operando); suite verde; 225 passed desde raíz.

### Seath (19:00) — rama `feat/meta-ui` — state v0
- **T1 · `state` v0: el primer save del juego** `[P1]` — Módulo: `src/core/state/`.
  Qué: `GameState` serializable ida-y-vuelta JSON con campo `version` de formato
  (saves versionados, PROJECT-MAP §3), envolviendo hoy la Shell del cap. 0
  (`to_dict`/`from_dict` ya existen). NO depende de generator/progression. El
  guardado será DIEGÉTICO, sin menús (§2.7). Aceptación: roundtrip idéntico sobre
  una sesión canónica (código de sesión + cwd + history + ruido); test que rechaza
  `version` desconocida con error claro; suite verde; 225 passed.
- **T2 · paleta: un idioma en `SEMANTIC`** `[P3]` (opcional, solo si T1 cierra con
  margen) — Módulo: `src/assets/palette.py`. Qué: claves de `SEMANTIC` a UN solo
  idioma (Seath propone y lo declara en la PR; eco de Artorias 27/08). Criterio:
  sin usos rotos (grep de claves viejas), suite verde. Gwyn valida al mergear.

### Manus (03:00, madrugada del 29/08) — narrativa
- **M1 · prosa↔FS en el cap. 0** `[P2]` — Módulo: `CAPITULOS/00-la-firma.md`.
  Qué: el listado que narra la escena tras `cd /srv` muestra UNA entrada (la
  oficina); `/usb` permanece en la RAÍZ — moverlo rompería la ruta canónica del
  dossier y del test (hallazgo de Oscar, confirmado por Havel). Criterio: bloque
  verificado byte a byte contra `test_session_cap0.py`; auto-pass anti-slop.
- **M2 · fragmento 2 + cap. 2 «Facturas»** `[P1]` — Módulo: `FRAGMENTOS.md` +
  `CAPITULOS/02-*.md`. Qué: fragmento 2 con la piel propuesta por Havel (pulsera
  HOSP-47-C que cruza Muelle/Subestación/Vela y sostiene H1+H2 a la vez) + beats
  del cap. 2 (el gancho ya existe: «la ventana de las 11:04 se abrió dos veces»).
  Criterio: fichas de voz respetadas, objetos estilo Souls, auto-pass anti-slop,
  relectura completa del fichero antes de commit.

## 3. Colas que HOY no se tocan (visibles, no olvidadas)

- **DEPLOY web (P1, Juanma)** — sigue esperando build jugable; en cuanto haya el
  mínimo jugable (headless→render), esta tarea salta al plan. No se asigna hoy:
  nada que empaquetar todavía.
- **LUZ PLENA (P2)** — texto de coherencia §3.4.1, no bloquea Fase 1; material
  para el turno de dirección de Gwyn.
- **Contrato GNU sistemático (P2, Smough)** — entra en primer plano cuando
  Smough añada flags (`ls -a`); hoy no toca código.
- **REPL `python -m core.sandbox` (P3, Smough)** — barato y de alto valor de
  primera impresión (verificado por Oscar); candidato natural a relleno de franja.
- Ideas de Havel/Artorias (dotfiles como mitad oculta, man pages como ficheros,
  post-mortem con historial real, reloj de patrulla, logros de ruido, ghosts de
  Zeta, métrica GNU del harness, vigilante con diff…): incubando en `abierto.md`;
  entran cuando sus módulos existan (la mayoría pide generator/engine o flags).

## 4. Decisiones para Gwyn (esta noche, 23:00)

- **Andamiaje de la run 0 (🧭2 de Oscar)**: elegir entre (a) la run 0 arranca en
  `/srv/oficina-vecinal-muelle-norte` con tutorial de navegación libre; (b) el
  dossier del cap. 0 SIEMPRE da rutas completas y las relativas se enseñan en el
  cap. 1; (c) mantener el error como primera lección del post-mortem nº 1. Lo que
  decida se escribe en DESIGN §6.1. Ornstein monta el andamiaje como datos
  preparados para cualquiera de las tres; la (b) es la más barata de materializar
  mañana por Manus (solo texto del dossier).
- **Extensión del PASO 0.5 + guard de push a Ornstein/Seath** — compromiso
  declarado del 28/08 en `mejoras/aplicadas/historico.md` («si funciona con
  Smough, se extiende el 29/08»); Mientras llega, el plan de hoy ya ordena a
  ambos ejecutores fijar y verificar identidad antes del primer commit y el
  guard `git log --format='%an' origin/main..HEAD | sort -u` antes de pushear.
- **Pendiente de su criterio también**: T2 (idioma de `SEMANTIC`) si Seath llega
  a proponerlo; M1 ya se planifica con la variante barata (una entrada en el
  listado, `/usb` en raíz) — la alternativa de mover `usb` bajo `/srv` NO se
  recomienda: rompe el dossier y el test.

## 5. Verificación del día (gate de Artorias, 21:00)

- Suite desde raíz = **225 passed** + los tests nuevos que traigan O2/S2/S3/T1.
- Sin `__init__.py` fuera de `src/tests/` (regla O1).
- Ensayo de integración de las feat/* abiertas antes del merge (protocolo
  obligatorio ya aplicado al prompt de Artorias): 0 errores de colección.

## 6. Auto-mejora (para `mejoras/pendiente/propuestas.md`)

- **[NUEVA] (28/08) — Gwyndolin — Gwyn:** extender PASO 0.5 + guard de push a los
  prompts de Ornstein y Seath (mecanismo estrenado con Smough; era el plan
  declarado para el 29/08). Mientras, el plan ya ordena la práctica a ambos.
- **[NUEVA] (28/08) — Gwyndolin — Artorias:** cuando S2 (`curriculum.json`) y O2
  (generator) convivan, añadir al gate del 21:00 un chequeo de que el
  `curriculum.json` de las ramas valida contra el validador de S2 (2 comandos
  pytest): evita que un dato roto pase el 225 passed y reviente generator al
  día siguiente.
- Sin bloqueos que escalar a Juanma. Nada descartado hoy.

## 7. Coherencia (comprobado)

- DESIGN: O2 genera piel del cap. 0 verificada contra el sandbox (determinismo
  §2.6.8; la lógica sigue en core); T1 guarda sin menús (§2.7); M1/M2 apoyan
  aprender-por-necesidad (§4.4, §6). Una tarea = un dueño = una rama = rutas
  disjuntas: nadie pisa `src/data/curriculum.json` salvo Smough, ni
  state/progression salvo Seath.
- Historia: M2 usa el gancho literal del cap. 1 y la piel HOSP-47-C coherente con
  ESCENARIOS (rack del Hub) y §2.4 (el expediente de Vela «existe»).
- Nada contradice la decisión D1 (cp activado, run 0 falible) ni las 🧭 vigentes.

— Gwyndolin, Dark Sun · 11:00
