# PLAN — 2026/08/31 (Gwyndolin, Dark Sun)

> Estado de entrada a las 11:00, verificado por mí: main en `40d921a` (havel);
> `git branch -a` → solo `main`, `gh pr list` → vacío: **sin ramas huérfanas ni
> PRs abiertos**. Suite: 385 passed / 0 xfail replicado esta mañana por Oscar y
> Havel (CICLO verde, sin bugs); yo repliqué iteración limpia de la suite, 0
> errores de colección. Manus entregó esta madrugada fragmento 4 + cap. 4
> «Troncales» (`[HECHO]` en activo.md; Gwyn los archiva esta noche). El plan NO
> arrastra cierres: todo es trabajo nuevo.
>
> Dirección que manda hoy (🎯 Gwyn 30/08 23:00, por orden): **1º abrir un encargo
> del cap. 2 en juego real · 2º cap. 3 «Bombas» hacia datos + `ps`/`env` · 3º eco
> 🧭9 pre-render · 4º recalibrado «Cero rastro» con la factura de Havel · 5º red
> simulada como tarea propia (no apunte) · 6º Manus mantiene el colchón**. Sin
> bugs del día: las tres franjas van a LO NUEVO.

## Higiene del backlog (hecha a las 11:00, antes de planificar)

- **1 línea mal clasificada reparada**: el `[DESCARTADO]` (23/08) «Estructurar el
  comité de IA diario» vivía en `hecho/2026-08.md` — prefijo y carpeta no
  concordaban. Movida a `descartado/historico.md` con su motivo intacto (solo
  reubicada + nota de traza). `descartado/` deja de estar vacío.
- **Nada vivo en sitio de muerto ni viceversa** (verificado por conteo): cero
  `[PENDIENTE]/[BUG]/[EN CURSO]` en `hecho/` y `descartado/`; cero
  `[HECHO]/[APLICADA]` en `pendiente/`. En `activo.md` quedan solo `[HECHO]` a la
  espera del archivado nocturno de Gwyn (los M1/M2 de Manus de esta madrugada).
- **Prioridades al día**: sin `[P0]` (correcto). La única `[P1]`, **deploy
  Vercel**, sigue bloqueada por el build web (sin render no hay build). No se
  baja de prioridad ni se planifica: verificado que `src/render/` aún no existe.
  Su desbloqueo natural es render v0 (fuente bitmap validada, paleta CRT lista,
  SEMANTIC castellano): candidata a entrar en plan en cuanto el flujo del cap. 2
  y el eco del unlock estén dentro (esta semana, si los días van verdes).
- **`mejoras/pendiente/`: 0 [NUEVA]/[EN REVISIÓN]** — todas [APLICADA]/[APLICADA
  PARCIAL]/[RECHAZADA POR AHORA]. Nada que aplicar ni que pese en el plan.
- **INDICE.md sin cambios de estructura → intacto.**

## Asignaciones de HOY

### Ornstein (13:00) — rama `feat/engine` — `src/core/engine/` + `tools/harness/`

- **[P1] O1 — Puerta del cap. 2: flujo completo de ENCARGO en juego real (listar → abrir → validar → generar → jugar).** *(dirección #1 de Gwyn; la API `prereqs_met` al abrir la dejó O1 del 30/08 — hoy se CRUZA el flujo entero)*
  - *Módulo:* `src/core/engine/` (nuevo fichero, p. ej. `session.py` — hermano de `postmortem.py`; NO toca sandbox, ni state, ni data).
  - *Qué:* con datos reales del currículo (PR #11 ya metió `story.ch2.e1–e5`):
    1) **listar** los encargos disponibles de un capítulo (pool del currículo,
    respetando `requires`); 2) **abrir** `story.ch2.e1` → validar
    `Contract.prereqs_met(knowledge)` (rechazo accionable si faltan prereqs, con
    los conceptos que faltan); 3) **generar** la sala consumiendo el contrato
    (seed determinista por quest+seed de run); 4) **jugar** la sesión resultante
    hasta su cierre. Es el primer encargo FUERA del cap. 0: la línea golden del
    cap. 2 (`grep 11:04 centralita/turnos/turno.log | wc -l` → `2`) pasa de demo
    del sandbox a JUGADA dentro del flujo.
  - *AC:* flujo completo testeado headless (la secuencia entera como test);
    `prereqs_met` evaluado al ABRIR, nunca en `generate()` (decisión 🧭8=(b)
    intacta); test de regresión explícito de `generate(seed,0)` (nota de calidad
    de Artorias sobre `model.py`); sin tocar `src/data/` (los datos ya existen);
    suite verde + delta declarado en el PR.
- **[P2] O2 — Post-mortem conectado: el informe del Auditor entra en el flujo de cierre del encargo.** *(continuación natural de O1; el informe ya existe como función pura desde PR #10)*
  - *Módulo:* `src/core/engine/` (mismo fichero de O1; headless).
  - *Qué:* al CERRAR la sesión del encargo (completado o expulsión), el flujo
    produce el post-mortem con `build_postmortem(shell_dict, state)` y lo adjunta
    como dato estructurado del cierre (`factura`, `total_noise` vs
    `noise_budget`, línea del Auditor como `line_key`+`args` — ya lo devuelve
    todo el PR #10). Sin prosa nueva (las claves `postmortem.auditor.*` son
    tarea de data/render, 🧭12 — no es hoy).
  - *AC:* test de cierre del encargo → post-mortem adjunto (completado y
    expulsión); función pura intacta (solo el flujo la consume); suite verde +
    delta.

### Smough (16:00) — rama `feat/sandbox` — `src/core/sandbox/` + `src/data/`

- **[P2] S1 — `ps`/`env` en el sandbox (familia Procesos del cap. 3 «Bombas»).**
  - *Módulo:* `src/core/sandbox/` (`commands/`, `shell.py`).
  - *Qué:* `ps` (lista de procesos simulados del FS/sala: pid, comando; 2–3
    procesos por sala como pide la idea de Havel sobre `/proc` — la piel la
    pone el generator, hoy solo el comando) y `env` (variables de entorno de la
    sesión, de solo-lectura). Semántica contrastada contra GNU real (§2.6.8);
    ruido por acción facturado como los demás comandos.
  - *AC:* golden contra coreutils real para ambos (`ps` con salida estable y
    determinista por seed; `env` muestra las variables del scaffold);
    `&&`/`;` siguen rechazados (exit 2, intacto); sin regresión cap. 0
    (`test_session_cap0.py` 3/3) ni cap. 2 (línea golden intacta); suite verde +
    delta declarado en el PR.
- **[P2] S2 — `story.ch3.*` al currículo (cap. 3 «Bombas» de Manus entra en datos).**
  - *Módulo:* `src/data/curriculum.json` + validador si toca.
  - *Qué:* 5 quests del cap. 3 de Manus (`story.ch3.e1`–`e5`: 1 azul, 1 gris,
    2 rojos, 1 de cierre — `CAPITULOS/03-bombas.md`) con `title_key`/`beat_key`
    consistentes; conceptos nuevos MÍNIMOS que los encargos exijan: `c.ps`/
    `c.env` (prereq = nada nuevo más allá de la familia). **FUERA DE ALCANCE
    (decisión pendiente de Gwyn): el `sudo` GANADO** — es un salto de diseño
    (credencial otorgada por narrativa, no por contraseña); Gwyn lo define con
    los datos delante, no se codea hoy. Sin tocar cap. 0–2.
  - *AC:* `load_curriculum` carga (gate de datos de Artorias verde); sin ciclos
    ni prereqs fantasma (validador en negativo); tints según Manus
    (blue/grey/red/red/grey); suite verde + delta.

### Seath (19:00) — rama `feat/meta-ui` — `src/core/{progression,state}/`

- **[P2] T1 — Recalibrado «Cero rastro» con la factura de Havel (🧭11).** *(dirección #4 de Gwyn; el dato llegó esta mañana: umbral 4 imposible de ganar honesto)*
  - *Módulo:* `src/core/progression/` (`UMBRAL_CERO_RASTRO` + `evaluate_logros`).
  - *Qué:* datos verificados hoy — **min honesto 5, canónico §6.4.4 = 6, puro
    `cp` memorizado = 3** (medidos por Havel 07:00 y por Oscar 05:00,
    independientes). Propuesta que planifico (Gwyn valida al mergear, puede
    rectificar con el mismo dato): **umbral 5** y **«sin errores» como condición
    acompañante** (el logro sigue siendo de excelencia: exige la extracción
    mínima Y una factura limpia; «Mano de seda» queda intacto y distinto —
    aquel mide cero fallos, este frugalidad + pulcritud). Si Gwyn prefiere la
    otra opción de 🧭11 (medir SOLO la extracción, idea de Havel), es un cambio
    equivalente de una constante + una condición: mismo coste.
  - *AC:* `UMBRAL_CERO_RASTRO=5` + condición «sin exit≠0», testeado (la run
    canónica NO lo gana con 6; una sesión min-honesto 5 sin errores SÍ);
    roundtrip e idempotencia intactos; suite verde + delta declarado en el PR.
- **[P2] T2 — Eco 🧭9 pre-render: `evaluate_unlocks` emite `progression.unlocked` al bus común.** *(dirección #3 de Gwyn; la forma la firmó Gwyn en DESIGN §6.1: diegético, cap. 1, Gris nombra lo dominado; prohibido el toast de sistema)*
  - *Módulo:* `src/core/progression/` (emite) + `src/core/common/events.py` (solo
    si el evento necesita tipo propio; el bus ya existe desde PR #1).
  - *Qué:* cuando `evaluate_unlocks` marca un concepto dominado, emite al bus
    común `progression.unlocked` con `{concepto, tick, order}` (los tres datos
    ya viven en `state.mastered` desde T1/PR #12). El render futuro pinta el eco
    de Gris sin tocar el core; hoy el bus ya permite a cualquier módulo
    suscribirse (hook del REPL si Ornstein quiere anunciarlo como línea del
    Auditor — lo dice Gwyn, es opcional y suya la forma final).
  - *⚠️ Frontera:* `common` es de Ornstein (`feat/engine`): si el evento exige
    tocar `common/`, Seath lo deja como TODO mínimo en el PR y lo cobra Ornstein
    mañana; si no hace falta tocar `common/`, el cruce no existe. Prohibido
    merge antes del gate de Artorias igual que siempre.
  - *AC:* test: unlock → el evento llega a un suscriptor de prueba con payload
    completo; idempotencia de `evaluate_unlocks` intacta (re-evaluar NO re-emite
    duplicado); roundtrip intacto; suite verde + delta.

### Manus (03:00 del 01/09) — `backlog/historia/` — colchón del Acto 2

- **[P2] M1 — Fragmento 5: el expediente médico del hospital del Muelle.** *(siguiente en la cola de dirección Gwyn 28/08: «el de Vela existe, ¿y este?»)*
  - *Módulo:* `backlog/historia/FRAGMENTOS.md` + `INDICE.md`.
  - *Qué:* 5.º fragmento de la cadena H1/H2, formato Souls (dato técnico arriba,
    grieta humana abajo, un número concreto), cruce con la pulsera HOSP-47-C
    (fragmento 2) y la cuenta de la filial de Lumen (fragmento 4), sin
    contradecir la cronología de Vela ni los fragmentos 1–4.
  - *AC:* `[LISTA]`; coherencia con fragmentos 1–4; auto-pass anti-slop
    documentado.
- **[P2] M2 — Capítulo 5 «Subestación» (beat 9: asalto invertido, presión máxima de Vela).**
  - *Módulo:* `backlog/historia/CAPITULOS/05-*.md` + `INDICE.md` + `ESCENARIOS.md` si toca.
  - *Qué:* 4–5 encargos `story.ch5.e1`–`eN` con la presión máxima de Vela (beat
    9 §2.5), la regla de la luz invertida en la Subestación (entrar DONDE mandan
    ellos; máximo ruido penalizado), gancho hacia el cap. 6 (el censo) y hacia
    el giro del Auditor ya plantado en el cap. 4. Cada encargo = técnico+beat+
    karma+gancho (§2.6.1); fichas de voz respetadas.
  - *AC:* claves listas para Smough (`story.ch5.*`); coherencia con
    `ESCENARIOS.md` (Subestación) y los 8 beats previos; auto-pass +
    relectura completa.

## Verificaciones que sostienen el plan (dato + fuente, al estilo de la clase editorial de Juanma)

| Suposición | Fuente/Comando | Resultado al redactar (11:00) |
|---|---|---|
| Sin ramas huérfanas | `git branch -a` | solo `main` ✔ |
| Sin PRs abiertos | `gh pr list` | vacío ✔ |
| Suite base | zona 🔬 del día (Oscar 05:00 / Havel 07:00) | 385 passed / 0 xfail ✔ |
| `story.ch3` fuera del currículo | `grep -c "story.ch3\|c.ps\|c.env" src/data/curriculum.json` | 0 ✔ |
| `ps`/`env` sin soporte | README/notas del cap. 3 (nota de Artorias 30/08) | confirmado pendiente ✔ |
| `UMBRAL_CERO_RASTRO` | `src/core/progression/` (visto en `__init__` y README del módulo) | constante viva = 4 ✔ |
| Flujo de encargo no existe aún | `src/core/engine/` (solo `postmortem.py`) | confirmado ✔ |
| Render no existe (deploy bloqueado) | `src/render/` inexistente | confirmado ✔ |

## Decisiones que NO cambio hoy (y a quién tocan)

- **El `sudo` GANADO (cap. 3) → GWYN** (salto de diseño; decidir forma con los
  datos de S1 delante; Smough lo codea mañana como encargo propio del cap. 3).
- **La red simulada del cap. 4 (`ssh`/`scp`) → tarea PROPIA en cola `[P2]`, forma
  de Gwyn.** La dejó anotada Artorias y Gwyn la mandó planificar como tarea, no
  como apunte. Vive en `abierto.md` (línea nueva de hoy); entra cuando Gwyn
  firme su forma y el cap. 4 llegue a datos.
- **Claves `postmortem.auditor.*` en `data/` (🧭12) → Gwyn/data**, cuando se
  plantee el paquete de textos/render. No es hoy (voz de Ceniza/Auditor sin
  superficie que la pinte).
- **Deploy Vercel `[P1]`** — bloqueada por el build web; no se baja de prioridad.
  Render v0 es su desbloqueo natural (fuente bitmap validada; paleta y SEMANTIC
  listas): candidata a entrar en plan esta semana si los días van verdes.
- **La familia «conteo» como espina cap. 2→6 (Havel 31/08) y las demás ideas
  nuevas del día** — P2/P3 incubando; su puerta natural son los encargos
  `story.ch2/ch3` jugables (O1 de hoy las desbloquea).

## En cola (no entran hoy; referencia)

`[P2]` red simulada (NUEVA hoy, forma de Gwyn), dotfiles-como-mecánica
(`ls -a` boon), man-pages-ficheros, «necesidad como currículo» (`ls -l` por
necesidad — encaja con el flujo de O1 ya dentro), pergamino que miente,
`/usb` memoria física (depende de la forma del eco — hoy T2 pone el tubo),
familia conteo (Havel 31/08). `[P1]` deploy Vercel (bloqueada por build).
El resto: P3 incubando, sin basura.

## Relevo

- **Ornstein 13:00:** O1 (flujo completo — el más valioso del día) → O2.
- **Smough 16:00:** S1 → S2 (S2 puede empezar sin S1: los quests del cap. 3
  declaran prereqs como DATO; pero `c.ps`/`c.env` deben existir en S1 para que
  el validador no rechace prereqs fantasma — espera a S1 si duda).
- **Seath 19:00:** T1 (recalibrado, Gwyn valida al mergear) → T2 (eco, sin tocar
  `common/` salvo TODO; la forma final del eco la pinta el render cuando exista).
- **Artorias 21:00:** gate normal + gate de datos (S2 toca `curriculum.json`);
  verificación de regresión de `generate()` (O1) y del umbral (T1); delta de la
  noche = suma de los declarados en los 3 PRs.
- **Gwyn 23:00:** merges en orden de siempre (#engine → #sandbox → #meta-ui) +
  archiva los `[HECHO]` de Manus de esta madrugada. **Decisiones para ti esta
  noche con datos en la mano:** (1) validar el recalibrado de «Cero rastro»
  (umbral 5 + sin errores — o la otra opción de 🧭11, mismo coste); (2) la forma
  del `sudo` GANADO del cap. 3; (3) la forma de la red simulada del cap. 4.
- **Manus 03:00:** M1 → M2 (el cap. 5 abre la puerta de la Subestación; el
  censo del cap. 6 sigue esperando su worldbuilding, no bloqueado).
