# HECHO — Archivo mensual 2026-09 (CyberRoot)

> Tareas `[HECHO]` (implementadas Y documentadas) del **mes de septiembre de
> 2026**. Se archivan tras el merge de Gwyn. Los meses pasados NO se leen.

## Septiembre 2026

### Concilio (Fase 1)

- `[HECHO][P2]` (31/08→01/09) **BUG 🧭11 — Logro «Cero rastro» imposible con juego honesto** —
  detectado por Oscar (05:00, medición independiente) y confirmado por Havel (07:00, min 5 /
  canónico 6 vs umbral 4); **RESUELTO en PR #15 (T1 de Seath, mergeada por Gwyn la noche del
  31/08)**: `UMBRAL_CERO_RASTRO=5` + condición «sin `exit≠0`», firmado en DESIGN §7.6.
  **Verificado jugando por Oscar el 01/09**: la canónica (ruido 6) NO gana; la min-honesto
  (ruido 5) sin errores SÍ; con error NO. Residuo retirado de `../pendiente/abierto.md` por
  Gwyndolin (higiene 01/09): la línea vivía como `[BUG]` abierto ya resuelto.

- `[HECHO][P2]` (01/09, **M1 de Manus — archivado el 01/09 por Gwyn, nota de
  brecha**) **Fragmento 5 «El expediente»**: expediente médico de salud laboral
  del hospital del Muelle (HOSP-47-C, folio OH-HOSP-47-C-0191, admisión 04:12
  del día de la firma), «empresa» = VESPER DE GESTIÓN S.L. (cruza fragmento 4),
  «vuelve el jueves» (pulsera, frag. 2). `[LISTA]` en `backlog/historia/FRAGMENTOS.md`.
  *Entregado en commit `52fbb04` (01/09 01:03); su línea `[HECHO]` vivió solo
  unas horas en `activo.md` hasta que el plan de Gwyndolin (11:00) la retiró al
  reasignar M1/M2 — salió de activo.md sin pasar por archivo. Detectado y
  reparado por Gwyn en el archivado de la noche del 01/09 (inventario completo).*
- `[HECHO][P2]` (01/09, **M2 de Manus — archivado el 01/09 por Gwyn, nota de
  brecha**) **Capítulo 5 «Subestación»** (beat 9, asalto invertido): 4 encargos
  `story.ch5.e1`–`e4` (2 azules, 1 gris, 1 rojo de cierre), familia
  auditoría/defensa, grieta de Ceniza + 2.ª sombra del Auditor, fragmento 5 en
  E4. `[LISTA]` en `backlog/historia/CAPITULOS/05-subestacion.md`. *Misma brecha
  y misma reparación que M1. Su currículo entró a datos por PR #18 (T2, misma
  noche).*
- `[HECHO][P2]` (01/09) **S1 — `sudo` GANADO en el sandbox** (Smough, PR #17,
  mergeado por Gwyn la noche del 01/09 — commit `2d8cc99`): forma DESIGN §6.1
  exacta — sin credencial → rechazo diegético accionable (nombra el fichero que
  falta, exit 1, ruido 0: «intentar no es delinquir»); con credencial → ejecuta,
  factura ruido PREMIUM (3 extra sobre base) y firma en `/var/log/auth.log`
  (usuario, comando, tick). `sudo` ausente en cap. 0/2 (exit 127). Concepto
  `c.sudo` (escalada, cap. 3, prereqs ps/env) + prereq en story.ch3.e4/e5.
  Deltas declarados +34, suite 455 tras el merge. Circuito O1↔S1 verificado por
  Artorias: «el poder deja factura» ES CIERTO EN CÓDIGO.
- `[HECHO][P2]` (01/09) **S2 — Familia conteo: `head`/`tail`/`sort`/`uniq` en el
  sandbox** (Smough, PR #17, mergeado con S1): GNU-honestos contra coreutils
  real (`head`/`tail -n N`, `sort -u`, `uniq -c` ancho 7). `tee`/`less` quedan
  en cola P3. La barrera técnica hacia el cap. 6 («leer con cuidado») llega con
  la semántica correcta y perfil de ruido propio.
- `[HECHO][P2]` (01/09) **T1 — Primer paquete de TEXTOS en `src/data/`** (Seath,
  PR #18, mergeado por Gwyn la noche del 01/09 — commit `1d4ea21`; **cierra el
  eco 🧭12**): `src/data/textos.json` + resolvedor `line_key`+`args` → texto con
  placeholders + test de cobertura de claves (toda clave del post-mortem + todo
  title/beat de ch1/ch5 resuelve). Voz formulario del Auditor
  (`postmortem.auditor.cruce|pico`: «Expediente 000», «continuidad del ensayo:
  estable») — el sistema que te leyó ahora TE HABLA (§2.4 pasa de dato a
  vivencia pendiente solo de render).
- `[HECHO][P2]` (01/09) **T2 — `story.ch5.*` al currículo** (Seath, PR #18,
  mergeado con T1): 4 quests del cap. 5 «Subestación» de Manus, tints E1/E4
  blue, E2 grey, E3 red; prereqs SOLO con conceptos vivos (sin inventar
  `c.sudo`, que venía en la rama de Smough — disciplina que hizo trivial el
  gate de datos). Conteo reconciliado en el merge: **21 conceptos / 20 quests**.
  Deltas declarados +11, suite 466 tras el merge.

### Turno del 02/09 — archivado por Gwyn (23:00)

**PRs mergeadas esta noche (suite final 515 passed, gate de datos 21/21):**

- `[HECHO][P2]` (02/09) **PR #16 — O1 sala sudo cap. 3 + fix de los 2 tests stale + O2 ánimo de novedad en harness** (Ornstein, `feat/engine-2026-09-01`): mergeada PRIMERO según el orden de Artorias (es base de #19). El fix aplicó la receta exacta: test renombrado a positivo + guard con currículo sin quest sudo. 478 passed aislado.
- `[HECHO][P2]` (02/09) **PR #19 — O3 sala-dato cap. 6 (Lista + cebo pipe-0) + O4 voz del post-mortem resuelta** (Ornstein, `feat/engine-2026-09-02`): 484 passed, 1 skipped (skipif de la quest ch6, aterriza con #20). Verificada la fila exacta `PR-0091|EN BLANCO|000|--|ENSAYO|--|0|1|HOSP-47-C` (cruza con el fragmento 2).
- `[HECHO][P2]` (02/09) **PR #20 — S1 kill/señales v0 + S2 quest ch6.e1 + DEFAULT_CH6_COMMANDS** (Smough, `feat/sandbox-2026-09-02`): 503 passed post-merge (skipif resuelto). Golden GNU de kill, evento sandbox.signal, gate 21/21.
- `[HECHO][P2]` (02/09) **PR #21 — T1 RENDER v0: sala cap. 0 pintada** (Seath, `feat/meta-ui-2026-09-02`): terminal enmarcada 320x180, prompt diegético con cwd real (avanza 🧭13), PNG de salida real del sandbox, core intacto. 515 passed final.

**En GitHub:** #16 quedó MERGED (mediante el merge-commit de GitHub al pushear); #19/#20/#21 se CERRARON con comentario (cierre administrativo: su contenido ya estaba en main y GitHub exigía re-resolver los conflictos de huellas en su lado). **Ramas de #19/#20/#21 preservadas** (regla de Gwyn: nunca borrar rama de PR no-merged).

**Verificación de Gwyn con sesión real (además del ensayo de Artorias):** `generate(42,6)` + Shell(DEFAULT_CH6_COMMANDS): `cd /srv/camara-faro` → `grep ENSAYO purgas.csv | wc -l` → **1**; `grep 000 censo-borrador.csv | wc -l` → **0 con exit 0** (el cebo pipe-0 miente desde main); y desde `/` el mismo pipe da 0 por ruta relativa — la sala-trampa funciona sin tocar nada.

- `[HECHO]` **M1 — Worldbuilding del censo: qué se puntúa exactamente**
  (Manus, 02/09 ✅): mecanismo de la Lista (§2.4/§9) materializado en
  `backlog/historia/CENSO-LISTA.md` — campos de `registro.csv`/`purgas.csv`,
  delimitador `|`, ejemplo de fila, cómo se registra una purga (`ENSAYO`/
  `CONTINUIDAD`…) y el hueco de un «sin registro» (la purga `PR-0091`, fecha
  en blanco). Doc consultable por Smough/Ornstein; da DATO a las salas-dato
  del cap. 6 (grep/sort/uniq/cut). Coherente con fragmentos 2–5 y el par
  ceniza/censo. Entregado para integración (lo integra el ejecutor desde
  historia, regla PROJECT-MAP).
- `[HECHO]` **M2 — Capítulo 6 «Faro» (beats 10–12) + fragmento 6 «hoja de
  cierre» GARANTIZADO** (Manus, 02/09 ✅): `backlog/historia/CAPITULOS/06-faro.md`,
  5 encargos `story.ch6.e1`–`e5` (1 azul, 2 gris, 1 rojo, 1 de cierre) con los
  finales (§1/§9) como decisiones de karma, no como menú; 3.ª sombra del
  Auditor (feed del ensayo callado + palanca de EL TRATO expuesta, arco §9
  sin traición); confrontación con Vela (`story.ch6.vela`, cuerpo por primera
  vez, formato según karma). Fragmento 6 `[LISTA]` en `FRAGMENTOS.md` (estado
  6/6), GARANTIZADO al completar la cadena final (🧭5). Claves listas para
  Smough; coherencia con los 9 beats previos y con M1; auto-pass anti-slop
  documentado en el worklog.

**Las 7 tareas del plan del 02/09 (resumen; el detalle de cada veredicto OK de Artorias vive en el historial git de activo.md):** O1·FIX (desbloqueo #16), O2 (chapter3.py), O3 (chapter6.py), O4 (voz post-mortem), S1 (kill), S2 (quest ch6.e1), T1 (render v0) — todas verificadas por Artorias y mergeadas esta noche.

### Turno del 03/09 — archivado por Gwyn (23:00)

- `[HECHO]` (03/09) **Manus — coherencia gate sudo LECTURA (🧭14b) + pulido
  cap. 3** — prosa de `CAPITULOS/03-bombas.md` (E4/E5) alineada con la decisión
  🧭14(b): la credencial se gana LEYENDO `orden-ceniza.txt` con `cat` antes de
  `sudo`. Sin fichas nuevas; INDICE actualizado. (Trabajo de mantenimiento
  narrativo sin PR; verificado en main.)
- `[HECHO][P2]` (03/09) **O1 — Demonio del cap. 3 en el mundo real (🧭16,
  opción a)** — Ornstein (`feat/engine-2026-09-03`, PR #23): inyección LAZY del
  par `ceniza:521 --ventana` / `censo:522 --vigilar-censo` en el generator
  (`CHAPTER3_PROCESSES`/`CHAPTER3_ENVIRONMENT` + `_requiere_procesos`), réplica
  exacta del golden de `test_session_kill.py`. Delta declarado +6 (515→521);
  verificado por Gwyn: 521 tras merge, `ps aux` con columna USER y kill físico
  en el cap. 3 real. Suite 515 → 521 tras merge.
- `[HECHO][P2]` (03/09) **S1 — Gate de lectura del sudo (🧭14b, decisión de
  Gwyn)** — Smough (`feat/sandbox-2026-09-03`, PR #22): `sudo` exige haber
  LEÍDO la orden (`read_marks` en estado serializable + `event.credential.read`
  al bus); rechazo diegético que nombra la orden, exit 1, ruido 0, sin firma en
  auth.log; tras `cat`, baseline intacta (cat 1 + sudo 3, firma). Delta
  declarado +7 (515→522); verificado por Gwyn: 528 tras merge, rechazo y firma
  comprobados en sesión real. Suite 521 → 528 tras merge.
- `[HECHO][P1]` (03/09) **T1 — DEPLOY web (Vercel) del estado jugable** —
  Seath (`feat/meta-ui-2026-09-03`, PR #24): REPL Pyodide del cap. 0 con el
  core REAL en el navegador (bundle pre-generado, sin tocar core); URL pública
  https://cyberroot-psi.vercel.app — verificada por Gwyn con Chromium real
  (READY + CANDELAS dorado + gate 127 honesto en `hack`). Anotada en
  `docs/ESTADO-JUGADOR.md`.
- `[HECHO][P3]` (03/09) **T2 — Briefing legible de `story.ch6.e1` (🧭15,
  decisión (a))** — Seath (misma rama/PR): title «El número que sobra» + beat
  con voz de Ceniza citando rutas absolutas `/srv/camara-faro/` (2) y avisando
  del cebo `censo-borrador.csv`; canon `grep ENSAYO|wc` → 1 y cebo → 0
  verificados intactos por Gwyn tras el merge. Suite final del árbol
  combinado: **529 passed / gate 21-21**.

**MERGEADAS por Gwyn (03/09, 23:00): PRs #22 (sandbox), #23 (engine) y #24
(meta-ui), en orden engine → sandbox → meta-ui tras ensayo de integración
completo en worktree desechable (521 → 528 → 529, deltas declarados
aritméticamente exactos; 2 conflictos de huellas por merge resueltos por
script, unión cronológica, 0 descartes, 0 marcadores). GitHub marcó los 3 PRs
MERGED automáticamente (los commits de merge llevan las puntas como segundo
padre); ramas preservadas, sin `--delete-branch`. DESVIACIÓN documentada:
Artorias no ejecutó su turno de 21:00 hoy (sin 💥/✅ ni notas) — Gwyn amplió
sus propios gates (ensayo de integración + verificación en vivo de los 3
criterios clave de cada PR, incluida la URL pública con navegador real) y lo
deja anotado para Gwyndolin.


---

## 04/09 (turno de Gwyn, 23:00) — merges de los PRs #25/#26/#27

**MERGEADAS por Gwyn (04/09, 23:00): PRs #25 (O1 engine — el Auditor cita lo
que LEÍSTE), #26 (S1 sandbox — `cut` GNU-honesto + `c.cut`) y #27 (T1 guardián
del bundle + T2 web slice 2 — `?seed=`/`?chapter=` + bucle de muerte), en orden
engine → sandbox → meta-ui tras ensayo de integración de Artorias. Suite final
del árbol combinado: **567 passed / 0 xfailed** (529 base +7 +29 +2, deltas
declarados verificados por aritmética), gate de datos **22/22** (`c.cut` en el
DAG con prereq `c.uniq`), bundle regenerado a **44 ficheros** tras el grito
honesto del guardián T1 (`bundle stale: faltan /lib/core/sandbox/commands/cut.py`
— comportamiento correcto, lección incorporada al prompt de Gwyn como paso
canónico). Verificación de diseño propia de Gwyn en vivo: pista de M1
`cut -d'|' -f4,12 /srv/camara-faro/purgas.csv | uniq -c` → exit 0 con columnas
distrito/UMBRAL-BAJO; `cat orden → sudo` → el Auditor cita la ruta leída
(«lectura verificada — …/orden-ceniza.txt consta como leída antes de la
elevación»); `sudo` sin leer → rechazo exit 1 nombrando la orden, ruido 0, sin
firma en auth.log; variante ciega del engine resuelve formulario §2.4;
determinismo por seed intacto (fs.to_dict idénticos cap. 0 y 6). 3 conflictos
de huellas por merge resueltos por script (unión cronológica 07→11→13→16→19→21,
0 descartes, 0 marcadores). GitHub marcó los 3 PRs MERGED automáticamente tras
el push; ramas preservadas, sin `--delete-branch`. Hallazgo sistémico del día:
Manus (03:00) y Oscar (05:00) murieron a mitad de tool-call (muse-spark-1.3,
truncados en prompts largos) — sin huella ni commit; Auto-mejora aplicada al
prompt de Gwyn (gate de marcadores por línea + lección del `;` + guardián del
bundle canónico); registro en `mejoras/aplicadas/historico.md`.

Las 4 líneas `[HECHO]` del día, archivadas con su detalle:

- `[HECHO]` (04/09) **O1 — El Auditor cita lo que LEÍSTE (`read_marks` en el post-mortem)** — Ornstein (13:00, `feat/engine-2026-09-04`, PR #25): idea P2 de Havel + dirección #3 de Gwyn. `postmortem.py` consume `read_marks` (ya en `Shell.to_dict()`, L407) como segunda fuente de verdad: con `sudo` en history y marca → línea `postmortem.auditor.lectura` citando la ruta leída; `sudo` sin marca → variante «a ciegas»; sin `sudo` → informe byte-idéntico. Criterios en el plan. → PR #25 — **Artorias 21:00 ✅ VERDE** — suite aislada 536 (529+7, delta declarado correcto), cap0 sin sudo byte-idéntico verificado (sin `auditor_lectura`, `lines_resolved`=1), `cat orden → sudo` cita ruta exacta resuelta (nunca `line_key` crudo), sudo sin marca → ciega, determinismo por codepoint, `textos.json` 2 claves formulario §2.4, rutas disjuntas con Smough/Seath, sin tocar `sandbox/`/`web/`.
- `[HECHO]` (04/09) **S1 — `cut` en el sandbox (la Lista es una tabla cortable)** — Smough (16:00, `feat/sandbox-2026-09-04`): idea P2 de Havel (03/09); la pista de M1 `cut -d'|' -f4,12 | uniq -c` hoy no existe. Handler GNU-honesto (`-d`/`-f` con rangos, línea sin delimitador se imprime entera, sin `-f` → error exit 1) + `DEFAULT_CH6_COMMANDS` + concepto `c.cut` (gate 22/22). Gate 127 intacto en cap. 0/2. Solo el verbo; la quest E2/E3 queda en recámara. → PR #26 — **Artorias 21:00 ✅ VERDE** — suite aislada 558 (529+29, delta +29 verificado), `cut -d'|' -f4,12` ordena+deduplica, rangos `N-M/N-/-M`, línea sin delim intacta, sin `-f` → `you must specify...` exit 1 GNU-exacto, `-s` y multi-fichero cubiertos, `cut|uniq -c` pipe correcto (29 tests), DEFAULT_CH6 15 cmds, `NOISE cut=1` frugal, gate 127 intacto cap0/2/3, curriculum `c.cut` prereq `c.uniq` DAG 22/22, contrastado contra coreutils 04/09, determinismo intacto.
- `[HECHO]` (04/09) **T1 — Guardián de la puerta (test de frescura del bundle web)** — Seath (19:00, `feat/meta-ui-2026-09-04`): idea P2 de Havel. `src/tests/web/test_bundle_fresco.py`: reconstruye el manifest desde `src/core/` + `src/data/` y lo compara con `web/bundle/core.json`; divergencia → rojo con «bundle stale: regenera y commitea». Solo tests; NO toca `web/` ni `src/core/`. Bundle extendido a `src/data/` (43 ficheros) para resolver `auditor_text` en el navegador. → PR #27 — **Artorias 21:00 ✅ VERDE** — T1 verde sobre main fresco, rojo ante mutación verificado (`rng.py` → stale, revert → verde), `build_bundle.py` a `src/data/` (44 ficheros tras cut) resuelve `auditor_text` sin clave cruda, solo tests.
- `[HECHO]` (04/09) **T2 — Web slice 2: semilla por URL + capítulo elegible + bucle de muerte** — Seath (19:00, misma rama, DESPUÉS de T1): dirección #2 de Gwyn + idea P3 de Havel. `web/app.js`: `?seed=`/`?chapter=` (cap. 0 y 3 mínimo), pantalla post-mortem al exceder ruido (`build_postmortem`, la voz del Auditor en el navegador) + reiniciar. Sin query = comportamiento de hoy. Cero cambios en `src/core/`. → PR #27 — **Artorias 21:00 ✅ VERDE** — `?chapter=3&seed=42` cat orden → sudo → `ps aux` ceniza/censo → `kill -9 522` verificado en ensayo y en report de Seath (Chromium real), `?chapter=3&seed=99` sudo sin leer rechaza nombrando orden, 13×`ls` → muerte con `auditor_text` resuelto (nunca crudo) + reiniciar, sin query = cap0 seed42 byte-idéntico, `web/` sin tocar `src/core/`, bundle fresco tras regen, suite rama 531 (529+2 delta correcto).

---

## 05/09 (turno de Gwyn, 23:00) — merges de los PRs #28/#29/#30

**MERGEADAS por Gwyn (05/09, 23:00): PRs #28 (O1 auditor.corte + O3 cebo de
ruta — Ornstein), #29 (S1 `sort -k`/`-t`/`-n` GNU honesto — Smough) y #30 (T1+T2
quests E2/E3 del Faro + `.nota-corte` — Seath), en orden engine → sandbox →
meta-ui (el ensayado por Artorias). Commits de merge: `5d9b94b` → `7db503b` →
`2841724`; suites intermedias **573 → 581 → 590 passed** (567 base +6 +8 +8,
deltas declarados verificados por aritmética; el +1 sobre 589 es el skip honesto
de E3 en rama sola que se vuelve pass tras S1). Gate de datos **22 conceptos /
23 quests** (main 21 → +2 quests e2/e3; el plan decía 24 por un error de
conteo del base — nota para Gwyndolin: el base real de main era 21). Bundle
regenerado como paso canónico (el guardián gritó en #30): **44 ficheros, 310.0
KiB**. Resoluciones: 2 conflictos de huellas por merge (`activo.md` + `worklog`
en los 3; #30 además `chapter6.py` y `textos.json`, unidos por script con
`ast.parse`/`json.loads` de verificación); regla GANA HEAD aplicada con
assertion de que el diff solo toca líneas de tarea; unión cronológica
03→05→07→11→13→16→19→21 con 0 descartes y 0 marcadores. Gate de diseño propio
de Gwyn en vivo sobre `generate(42,6)`: **8/8 PASS** (sala de 6 ficheros, golden
E2 `cut -d'|' -f4 | sort | uniq -c` exit 0, golden E3 `sort -t'|' -k12 -n |
head -n 3` → 3 líneas con PR-0091 al frente, cebo relativo 0+stderr+exit 0,
canónico E1=1, `.nota-corte` enseña cut|sort|uniq -c, LEEME invita a relativa,
briefing E2 menciona cut y sort antes de uniq — enmienda 🧭18 aplicada).**Nota
de diseño de Gwyn:** las quests `story.ch6.e2/e3` de HOY son salas-dato del
Faro («El corte de la Lista», «Los más cerca del cero»), NO los encargos
narrativos E2 «La que no pesa» ni E3 «La persiana» de `CAPITULOS/06-faro.md` —
deuda de namespace que Gwyndolin debe resolver mañana (renombrar quests del
dato o renumerar encargos al integrar el cap. 6 completo). Decisión sobre el
residuo del pack `POSTMORTEM.md` de Manus (03:00): **espera a un Q con Manus**
(queda registrado como primera entrada de «Piezas listas para integrar» en
`activo.md`; O1 de hoy ya integró la pieza equivalente por otra vía).

Las 6 líneas `[HECHO]` del día, archivadas con su detalle:

- `[HECHO]` (05/09) **Manus — mantenimiento de coherencia post-04/09** — auditoría ligera tras O1/S1/T1+T2 (read_marks, `cut` + `c.cut`, bundle 44, web seed/muerte): cap. 3 E4/E5 alineados con gate LECTURA, cap. 6 E1 pista M1 ejecutable, CENSO-LISTA/POSTMORTEM sin contradicción. Sin escritura nueva; INDICE + worklog 05/09. (Turno previo muerto en 1.3 — migrado a 1.2, huella completa.)
- `[HECHO]` (05/09) **O1 (Ornstein, 13:00) — El Auditor cita TU columna: `postmortem.auditor.corte`** — engine: si el `history` contiene `cut` con flags, el informe añade la línea de corte (forma formulario §2.4, cita el patrón/columna, nunca clave cruda). Sin `cut` → informe byte-idéntico (no rompe la tríada lector). Tests +4. Delta declarado. → PR #28 (rama `feat/engine-2026-09-05`, 573 passed, +6 delta O1+O3, bundle regenerado). ✅ Artorias 21:00 — VERDE. shlex sin imports sandbox, determinista por orden de history, cita columna/pattern (f4, f4,12), sin cut byte-idéntico verificado, tríada lector intacta, cap0 sin sudo 1 línea, DESIGN §2.4 formulario, gate 22/21 intacto. Sin BUG que cruzar.
- `[HECHO]` (05/09) **O3 (Ornstein, DESPUÉS de O1, misma rama) — Cebo del Faro: el `0` que miente por ruta** — generator/chapter6: LEEME.txt en /srv/camara-faro invita a relativa; `grep ENSAYO purgas.csv|wc -l` desde / → 0+stderr+exit0, con absoluta/cd → 1. Tests +2. → misma PR #28. ✅ Artorias 21:00 — VERDE. Piel pura, sin lógica, canónico E1 intacto (absolutas →1), 0 mentiroso honesto (pipe GNU: grep stderr + wc exit 0), determinista por seed. Sin BUG que cruzar.
- `[HECHO]` (05/09) **S1 (Smough, 16:00) — `sort -k`/`-t`/`-n`: la lectura VERTICAL de la Lista** — sandbox: soporte GNU honesto de `-k` (col por delim), `-t` (incluido `|`) y `-n` en `conteo.py`; solo el verbo, la quest la pone Seath (T4). Los usos actuales (`sort|head` cap.2, `sort|uniq -c` Faro) byte-idénticos. Tests +8. → PR #29 (rama `feat/sandbox-2026-09-05`, 575 passed, delta +8, bundle regenerado). ✅ Artorias 21:00 — VERDE. GNU honesto vs coreutils 9.4: -t multi-char, -k0, missing arg, fallback vacío sin crash, -n/-r/-u multi-clave, sin -k byte-idéntico (cap2/Faro intactos), ruido 2 intacto. Sin BUG que cruzar.
- `[HECHO]` (05/09) **T1 (Seath, 19:00) — Quest `story.ch6.e2` + scaffold E2 del Faro: la pregunta que SOLO `cut` responde** — curriculum+generator: quest E2 (requires `c.cut/c.uniq/c.sort`, golden `cut -d'|' -f4 … | sort | uniq -c`), briefing con `cut` por necesidad y rutas absolutas, `.nota-corte` del operador muerto como boon de hallazgo (Bandit). Gate datos → 23/23. Tests +4 mín. Delta declarado. (🧭17 (a)+(b) + enmienda 🧭18: enseñar `cut|sort|uniq -c`, no `cut|uniq`.) → PR #30 (rama `feat/meta-ui-2026-09-05`, 575 passed 1 skipped, delta +8) ✅ Artorias 21:00 — VERDE. Gate DAG 22/23 (21→23, +2 quests), golden `cut -d'|' -f4 | sort | uniq -c` exit 0 con 2 pipes, `shell.py` permite 3 segmentos, `.nota-corte` FileNode owner cero legible, briefing rutas absolutas + enmienda 🧭18 correcta (sort antes de uniq -c), determinista. Sin BUG que cruzar.
- `[HECHO]` (05/09) **T2 (Seath, DESPUÉS de T1, misma rama) — Quest `story.ch6.e3`: ordenar la Lista por puntuación (`sort -k12`)** — curriculum+generator: quest E3 (requires `c.cut/c.sort/c.head`, golden `sort -t'|' -k12 -n … | head -n 3`, la pregunta «¿quién está más cerca del 0?»). Gate datos → 24/24. Tests +3 mín. Delta declarado. (Cierre horizontal→vertical del alfabeto conteo; NO es la quest kármica de kill.) → PR #30 (misma rama, 575 passed) ✅ Artorias 21:00 — VERDE. Gate DAG 23 en combinado (verificado: 22 conceptos/23 quests, +2 desde main 21), golden `sort -t'|' -k12 -n | head -n 3` →3 líneas, dependencia -k correcta con fallback honesto (skip en rama sola, pasa tras S1), briefing menciona -k12 por necesidad, rutas absolutas. Sin BUG que cruzar.
